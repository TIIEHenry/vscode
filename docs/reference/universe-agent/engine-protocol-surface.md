---
title: "UniverseAgent 引擎协议面（本仓消费口径）"
type: reference
status: accepted
phase: N/A
updated: 2026-09-05
summary: "已知 gRPC 服务 / RPC 名与本仓用途；§4 含 G-CORE-1；§5 会话面含 onDynamicDidApplyFrame 首帧缓冲与 confirmPairing/cancelPairing/probeConnectionProfile；SessionEventStream onClosed 折 Actor streamClosed；ContinueGeneration / Regenerate / Rename / Cancel / CancelToolCall / RunToolInBackground / AgentService.StopShellTask / SetSessionGoal / MessageQueue 族 / Fork / Kill / DeleteMessage / EditMessage / SendClientToolResponse / ListSnapshots / ListLoopSnapshots / CreateSnapshot / RestoreSnapshot / DeleteSnapshot / SessionService.Info / SessionService.Resume / SessionService.Prewarm / SessionService.Shelve / SessionService.Unshelve / SessionService.Purge / SessionService.Export / SessionService.ResolveTurn / SessionService.ResolveAnchor / AgentService.Status / AgentService.Todo / AgentService.Compact / AgentService.Usage / AgentService.History / AgentService.Pause / AgentService.Resume / AgentService.Back / AgentService.Prune / AgentService.Reset / AgentService.Branch / AgentService.SuspendLoop / AgentService.ResumeLoop / AgentService.StopLoop / AgentService.RunToolInBackground / AgentService.StopShellTask / AgentService.SendShellSessionClientControl / AgentService.FetchToolUsageDetail / AgentService.FireTriggerWebhook / AgentService.InstallSessionDemoFake / AgentService.ClearSessionDemoFake / AgentService.SwitchWorkDir / AgentService.TestModelProfile / AgentService.SubscribeToolDetail / AgentService.InsertQueueItem / AgentService.DeleteQueueItem / AgentService.ReorderQueue / AgentService.RetryQueueItem / AgentService.RetryAllFailed / AgentService.SetQueueItemLocked / AgentService.InjectQueueItem / AgentService.SetQueueItemForkAnchor / AgentService.ChatSync / AgentService.SyncInputDelivery / ToolInfo / PermissionService.Respond / PermissionService.SyncPermissionRule / AgentService.RespondQuestion 已进 gRPC catalog；roster 接通后转发 Create / Rename / Cancel / SetSessionGoal / Fork / Kill / CancelToolCall / DeleteMessage / Respond / respondClientTool→SendClientToolResponse / RespondQuestion / MessageQueue 五操作 + Edit + Enqueue / updateUserTurnText→EditMessage / createSnapshot→CreateSnapshot（Inbox Stop 仅 connected+streaming；Inbox Goal 接通后可设/清；Fork / Kill 不造本地 catalog id；Kill 空 agentId 原样上线不默认 root，省略用末条 streaming 否则空串；时间线执行中工具行转 CancelToolCall；时间线删回合接通后转 DeleteMessage，空 turnId 不发，未指定 agent 用末条 streaming 否则 root；权限座接通后转 Respond，未接通仍 Chat 臂；问题座接通后转 RespondQuestion，空 questionId 不发，未接通仍 Chat 臂；时间线 clientTool 接通后转 SendClientToolResponse，空 callId 不发，未接通仍 Chat 臂；队列无 GetQueue 显示空；Inbox AutoDrive 接通 / 断连缓存诚实空，Team.TaskList 仍只给 Navigator；空 turnId / 空正文不发 EditMessage；ListSnapshots 由 Conversation SessionBar Snapshots 列表消费，断连 / 无 hook / 空 sessionId 不发；overlay Restore 直呼 restoreSnapshot，空 snapshotId / 断连 / 无 hook 不发；成功后 overlay 保持打开再拉 listSnapshots，失败 / 未发不刷新；overlay Delete 确认后转发 deleteSnapshot，空 snapshotId / 断连 / 无 hook / 空 session 不发；确认成功后 overlay 保持打开再拉 listSnapshots，取消 / 失败 / 未发不刷新；CreateSnapshot 接通后 Conversation 动作转发（空 sessionId / 断连 / 无 hook 不发；空 title 原样上线）；ListLoopSnapshots / SessionService.Info / SessionService.Resume / SessionService.Prewarm / SessionService.Shelve / SessionService.Unshelve / SessionService.Purge / SessionService.Export / SessionService.ResolveTurn / SessionService.ResolveAnchor / AgentService.Status / AgentService.Todo / AgentService.Compact / AgentService.Usage / AgentService.History / AgentService.Regenerate / AgentService.Pause / AgentService.Resume / AgentService.Back / AgentService.Prune / AgentService.Reset / AgentService.Branch / AgentService.SuspendLoop / AgentService.ResumeLoop / AgentService.StopLoop / AgentService.RunToolInBackground / AgentService.StopShellTask / AgentService.SendShellSessionClientControl / AgentService.FetchToolUsageDetail / AgentService.FireTriggerWebhook / AgentService.InstallSessionDemoFake / AgentService.ClearSessionDemoFake / AgentService.SwitchWorkDir / AgentService.TestModelProfile / AgentService.SubscribeToolDetail / AgentService.InsertQueueItem / AgentService.ReorderQueue / AgentService.DeleteQueueItem / AgentService.RetryQueueItem / AgentService.RetryAllFailed / AgentService.RetryQueueItemUpload / AgentService.PinQueueItem / AgentService.SetQueueItemLocked / AgentService.InjectQueueItem / AgentService.SetQueueItemForkAnchor / AgentService.ChatSync / AgentService.SyncInputDelivery / PermissionService.SyncPermissionRule / PermissionService.PromotePermissionRule / PermissionService.GetSessionRules / PermissionService.SetPermissionMode / TeamService.TaskUpdate / TeamService.TaskCancel / TeamService.MessageMember / TeamService.CreateTeam / TeamService.StartMember / TeamService.KillMember / TeamService.Abort / ConfigService.Get / ConfigService.Set / ConfigService.SwitchModel / ConfigService.ResolveModel / ConfigService.Watch / ConfigService.SetPermissionPolicy / ConfigService.GetModelPreferences / ConfigService.SetModelPreferences / ToolService.ListCommands / ToolService.GetCommandDef / FileService.ListFiles / FileService.ReadFile / FileService.GetFileInfo / FileService.WriteFile / FileService.ForceWriteFile / FileService.AgentMerge / GitService.ReadGitSummary / GitService.ReadGitChanges / GitService.ReadGitFileDiff / GitService.WriteGitStagePaths / GitService.WriteGitCommit / GitService.WriteGitApplyHunks / TokenUsageService.GetGlobalUsage / MemoryService.Save / MemoryService.Search / MemoryService.SearchDeep / MemoryService.Read / MemoryService.List / MemoryService.Delete / MemoryService.Reflect / MemoryService.Rebuild / MemoryService.Revert / MemoryService.History / FileTransferService.GetUploadProgress / FileTransferService.UploadAttachment / FileTransferService.DownloadAttachment / SystemService.Shutdown / DeviceService.ListDevices / DeviceService.PairApprove / DeviceService.Revoke / TriggerService.ListTriggers / TriggerService.UpsertTrigger / ClipboardService.Write / ClipboardService.Read / ContextVariableService.List / ContextVariableService.Read 仅 catalog + node；SessionBar History 仍回合索引）；未接通 Fork tab 仍本地 registerForkChat；Engine Tools 选中行拉 ToolInfo 只读详情（不画 schema 编辑器）；Composer 发送仍走 submitInput；G-NAV-* / G-REV-* / G-ENG-*；G-CONV-1 已消费 attribution""
---

# UniverseAgent 引擎协议面（本仓消费口径）

> 导航：[索引](INDEX.md)。RPC 名以外仓 proto 为准。§1 **Conversation 传输**行来自 `platform/universeAgent/node/grpc/grpcTransport.ts`（`UniverseAgentGrpcServices`，A1/A2 @ HEAD）。§1 **Engine catalog** 与 §7 UI 口径来自 [customizations-engine](../../../dev/plans/customizations-engine.md) 与 [engine-catalog](../../systems/workbench/engine-catalog.md)（九节两栏 + 六态 @ HEAD；Skills 正文 + `saveSkillContent?` @ `f3f2d366`；Agents/MCP/Tools 写 @ `f49615a1`；GC-6 Overview Model @ `f583073b`；Overview Provider 行 G-ENG-1 前省略）。

## 1. 已知服务与 RPC

| 服务 | RPC | 本仓消费面 | 备注 |
|------|-----|-----------|------|
| `SystemService` | `GetAuthNonce` | `IUniverseAgentConnection.connect` / pairing orchestrator S2–S4 | 请求：`client_identity_id`、`client_public_key` → `auth_nonce` + `engine_cert_fingerprint`（**transcript 只用本地 TLS 观测指纹**，自述值不一致 fail-closed） |
| `SystemService` | `Connect` + `device_auth` | 同上；非 loopback 一律 DeviceAuth | `DeviceAuth{ client_identity_id, client_public_key, auth_nonce, signature }`；transcript = `engineIdentityId ‖ 观测 leaf 指纹 ‖ authNonce ‖ clientIdentityId ‖ protocolVersion`；`supported_tools=[]` 于 provisional 首配 |
| `SystemService` | `ConnectResponse` | `session_token` → `isEngineConnected()`；capabilities | 已 Grant：`session_token` 非空 → connected。**未配对**：`pairing_nonce` + `sas_code`（Crockford `XXXX-XXXX`），**无** `session_token`；pairing-pending ≠ connected（ADR-003 D7）。S4 意外 token → 丢弃、recoverTrust |
| `SystemService` | `HealthCheck` | `IUniverseAgentConnection.healthCheck?`（unary） | **已进** `UniverseAgentGrpcServices.System` + node unary `healthCheck`（空 request `{}`；响应 `status`/`version`/`active_sessions`/`uptime_ms`）。合同可选。空 `status` / 空 `version` 原样映射；`active_sessions` / `uptime_ms` 0 原样映射。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `Connect` / `GetAuthNonce` / `Doctor` / `Shutdown` |
| `SystemService` | `Doctor` | `IUniverseAgentConnection.doctor?`（unary） | **已进** `UniverseAgentGrpcServices.System`（`universeagent.system.v1.SystemService`）+ node unary `doctor`（空 request `{}`；响应 `checks[]`：`name`/`passed`/`message`/`fix_hint` + `all_passed`）。合同可选。空 `name` / `message` / `fix_hint` 原样映射；`passed` / `all_passed` false 原样映射。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `Connect` / `GetAuthNonce` / `HealthCheck` / `Shutdown` |
| `SystemService` | `Shutdown` | `IUniverseAgentConnection.shutdown?`（unary） | **已进** `UniverseAgentGrpcServices.System` + node unary `shutdown`（snake_case `force`/`grace_period_ms`；响应 `accepted`/`message`）。合同可选。`force` false / `grace_period_ms` 0 原样上线；空 `message` 原样映射。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `Connect` / `GetAuthNonce` / `HealthCheck` / `Doctor` |
| `DeviceService` | `ListDevices` | `IUniverseAgentConnection.listDevices?`（unary） | **已进** `UniverseAgentGrpcServices.Device`（`universeagent.device.v1.DeviceService`）+ node unary `listDevices`（空 request `{}`；响应 `devices[]`：`device_id`/`display_name`/`role`/`platform`/`paired_at`/`last_seen_at`/`active`）。合同可选。空 `device_id` / `display_name` / `role` / `platform` 原样映射；`paired_at` / `last_seen_at` 0 原样映射；`active` false 原样映射。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `PairApprove` / `PairReject` / `Revoke` / `RotateToken` / `ListPending` |
| `DeviceService` | `PairApprove` | `IUniverseAgentConnection.pairApprove?`（unary） | **已进** `UniverseAgentGrpcServices.Device`（`universeagent.device.v1.DeviceService`）+ node unary `pairApprove`（snake_case `pairing_code`/`display_name`/`role`；响应 `success`/`device_id`/`message`）。合同可选。空 `pairing_code` / `display_name` / `role` 原样上线；空 `device_id` / `message` 原样映射；`success` false 原样映射。Reserved `device_token`（ADR-261）不映射。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListDevices` / `PairReject` / `Revoke` / `RotateToken` / `ListPending` |
| `DeviceService` | `Revoke` | `IUniverseAgentConnection.revoke?`（unary） | **已进** `UniverseAgentGrpcServices.Device`（`universeagent.device.v1.DeviceService`）+ node unary `revoke`（snake_case `device_id`；响应 `success`/`message`）。合同可选。空 `device_id` 原样上线；空 `message` 原样映射；`success` false 原样映射。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListDevices` / `PairApprove` / `PairReject` / `RotateToken` / `ListPending` / Hub `revokeDevice` |
| `TriggerService` | `ListTriggers` | `IUniverseAgentConnection.listTriggers?`（unary） | **已进** `UniverseAgentGrpcServices.Trigger`（`universeagent.trigger.v1.TriggerService`）+ node unary `listTriggers`（snake_case `scope`/`scope_id`/`type_filter`；响应 `triggers[]` TriggerDto：`trigger_id`/`name`/`type`/`prompt_template`/`enabled`/`pause_reason`/`target` DeliveryTargetDto oneof `self`/`bound_session`/`new_session`/`interval_ms`/`cron_expression`/`run_at_epoch_ms`）。合同可选。空 `scope` / 空 `scope_id` / 空 `type_filter` 原样上线；空 `trigger_id` / `name` / `type` / `prompt_template` / `pause_reason` / `cron_expression` 原样映射；`enabled` false 原样映射；`interval_ms` / `run_at_epoch_ms` 0 原样映射。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `UpsertTrigger` / `DeleteTrigger` / `SetTriggerEnabled` / `FireTrigger` / `AgentService.FireTriggerWebhook` / `DeviceService` |
| `TriggerService` | `UpsertTrigger` | `IUniverseAgentConnection.upsertTrigger?`（unary） | **已进** `UniverseAgentGrpcServices.Trigger`（`universeagent.trigger.v1.TriggerService`）+ node unary `upsertTrigger`（snake_case `scope`/`scope_id`/`trigger` TriggerDto：`trigger_id`/`name`/`type`/`prompt_template`/`enabled`/`pause_reason`/`target` DeliveryTargetDto oneof `self`/`bound_session`/`new_session`/`interval_ms`/`cron_expression`/`run_at_epoch_ms`；响应 `trigger`）。合同可选。空 `scope` / 空 `scope_id` 原样上线；空 `trigger_id` / `name` / `type` / `prompt_template` / `pause_reason` / `cron_expression` 原样上线并映射；`enabled` false 原样上线并映射；`interval_ms` / `run_at_epoch_ms` 0 原样上线并映射。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListTriggers` / `DeleteTrigger` / `SetTriggerEnabled` / `FireTrigger` / `AgentService.FireTriggerWebhook` / `DeviceService` |
| `SessionService` | `List` / `Create` / `Delete` / `Info` / `Resume` / `Prewarm` / `Shelve` / `Unshelve` / `Purge` / `Export` / `ResolveTurn` / `ResolveAnchor` / `GetHistory` / `SessionEventStream` | Conversation roster + 时间线 fold 输入（`GetHistory` + 流 → session-core Actor → `ViewFrame`）。`Info` / `Resume` / `Prewarm` / `Shelve` / `Unshelve` / `Purge` / `Export` / `ResolveTurn` / `ResolveAnchor` 仅 catalog + node | 无 `SwitchSession`；切换 = IDE 客户端投影。标题 proto 为 `AgentService.Rename`（catalog + node unary + **roster 接通后转 unary**）。`subscribeSessionEventStream` 第三参 `onClosed` 与 Chat bidi 同因（`remote` / `error`）；本地 dispose 不回调。宿主 `openStream` 把 remote/error 经 `postAndDrain` 折成 Actor `streamClosed` |
| `SessionService` | `Info` | `IUniverseAgentConnection.getSessionInfo?`（unary） | **已进** `UniverseAgentGrpcServices.Session` + node snake_case `session_id`。响应 `session_id` / `root_agent`（既有 `AgentInfo` → `UniverseAgentAgentTreeNode`）/ `created_at` / `last_accessed_at` / `provider` / `model`。合同可选。空 `sessionId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `Resume` / `List` / `Create` |
| `SessionService` | `Resume` | `IUniverseAgentConnection.resumeSession?`（unary） | **已进** `UniverseAgentGrpcServices.Session` + node snake_case `session_id`。响应 `success`/`message`/`root_agent`（既有 `AgentInfo` → `UniverseAgentAgentTreeNode`）。合同可选。空 `sessionId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `Agent.Resume` / `Agent.ResumeQueue` / Chat resume |
| `SessionService` | `Prewarm` | `IUniverseAgentConnection.prewarmSessions?`（unary） | **已进** `UniverseAgentGrpcServices.Session` + node snake_case `session_ids`。响应 `entries[]`（`session_id`/`outcome`/`message`）。合同可选。空 `sessionIds`（含空串）原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `Resume` / `Shelve` / `Unshelve` / `List` |
| `SessionService` | `Shelve` | `IUniverseAgentConnection.shelveSession?`（unary） | **已进** `UniverseAgentGrpcServices.Session` + node snake_case `session_id`。响应 `success`/`message`。合同可选。空 `sessionId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `Delete` / `Unshelve` / `Resume` |
| `SessionService` | `Unshelve` | `IUniverseAgentConnection.unshelveSession?`（unary） | **已进** `UniverseAgentGrpcServices.Session` + node snake_case `session_id`。响应 `success`/`message`。合同可选。空 `sessionId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `Shelve` / `Resume` / `Delete` |
| `SessionService` | `Purge` | `IUniverseAgentConnection.purgeSession?`（unary） | **已进** `UniverseAgentGrpcServices.Session` + node snake_case `session_id`。响应 `success`/`message`。合同可选。空 `sessionId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `Delete` / `Shelve` / `Unshelve` / `Export` |
| `SessionService` | `Export` | `IUniverseAgentConnection.exportSession?`（unary） | **已进** `UniverseAgentGrpcServices.Session` + node snake_case `session_id`/`format`。响应 `content`/`format`。合同可选。空 `sessionId` / `format` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `Shelve` / `Unshelve` / `Resume` / `Delete` |
| `SessionService` | `ResolveTurn` | `IUniverseAgentConnection.resolveTurn?`（unary） | **已进** `UniverseAgentGrpcServices.Session` + node snake_case `session_id`/`turn_id`/`current_leaf_turn_id`。响应 oneof `hit`/`tombstone`/`expired`（信封粗映射为 `unknown`）。合同可选。空 `sessionId` / `turnId` / `currentLeafTurnId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `ResolveAnchor` / `GetHistory` / `Export` |
| `SessionService` | `ResolveAnchor` | `IUniverseAgentConnection.resolveAnchor?`（unary） | **已进** `UniverseAgentGrpcServices.Session` + node snake_case `anchor.session_id`/`anchor.envelope_id`/`anchor.generation?`/`scope`/`current_leaf_turn_id?`。响应 oneof `hit`（`envelope`/`presence`/`generation?`）/ `tombstone`（`session_id`/`envelope_id`/`seq`/`turn_id?`/`generation?`）/ `expired`（`anchor`）。合同可选。空 `sessionId` / `envelopeId` / `currentLeafTurnId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `ResolveTurn` / `GetHistory` / `Compact` |
| `AgentService` | `Status` | `IUniverseAgentConnection.getAgentStatus?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`agent_id`。响应 `agent`（既有 `AgentInfo` → `UniverseAgentAgentTreeNode`）。合同可选。空 `sessionId` / `agentId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / FetchAgentStatus fold。≠ `Session.Info` / `Agent.Tree` / Team.MemberStatus |
| `AgentService` | `Compact` | `IUniverseAgentConnection.compact?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`agent_id`。响应 `success`/`message`/`tokens_before`/`tokens_after`/`outcome`/`reject_reason`。合同可选。空 `sessionId` / 空 `agentId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `Todo` / `Status` / `Tree` / `FetchToolUsageDetail` |
| `AgentService` | `Chat` | 发送 + 流内 question / clientTool 应答（Chat 双向流） | 权限座接通后改走 `PermissionService.Respond` unary（不双写 Chat 臂）；未接通仍 `permissionRespond`。问题座接通后改走 `AgentService.RespondQuestion` unary（不双写 Chat 臂）；未接通仍 `questionRespond`。clientTool 接通后改走 `AgentService.SendClientToolResponse` unary（不双写 Chat 臂）；未接通仍 `clientToolRespond` |
| `AgentService` | `ChatSync` | `IUniverseAgentConnection.chatSync?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`agent_id`/`session_input`/`timeout_seconds`/`last_known_message_ids`/`idempotency_key`。响应 `session_id`/`agent_id`/`text`/`stop_reason`/`input_tokens`/`output_tokens`/`turn_count`/`tool_results`/`error`/`input_delivery_events`。合同可选。空 `sessionId` / 空 `agentId` / 空 `idempotencyKey` / 空 `lastKnownMessageIds` / 空 `sessionInput.messageId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `Chat` / `SyncInputDelivery` / `ContinueGeneration` / `Resume` |
| `AgentService` | `SyncInputDelivery` | `IUniverseAgentConnection.syncInputDelivery?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`last_known_message_ids`。响应 `input_delivery_events`。合同可选。空 `sessionId` / 空 `lastKnownMessageIds` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `Chat` / `ChatSync` / `ContinueGeneration` / `Resume` |
| `AgentService` | `ContinueGeneration` | 宿主 `openContinuationStream?`（ADR-028）；时间线仍走 `SessionEventStream` | **已进** `UniverseAgentGrpcServices.Agent` + node `openContinuationStream`（server-stream `ChatResponse`）。宿主 remote/error `onClosed` 拆句柄并 warn，**不** `postAndDrain(streamClosed)`（那条闸是 SessionEventStream）；断连 / 替换先本地 dispose。Web / 无 hook 仍计 `intent.unhandled` |
| `AgentService` | `Regenerate` | `IUniverseAgentConnection.openRegenerateStream?`（server-stream `ChatResponse`） | **已进** `UniverseAgentGrpcServices.Agent` + node `openRegenerateStream`（`makeServerStreamClient`，snake_case `session_id`/`agent_id`/`turn_id`/`message_id`）。合同可选。空 `sessionId` / `agentId` / `turnId` / `messageId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / ADR-029 regenerateTurn。≠ `ContinueGeneration` / `Pause` / `History` |
| `AgentService` | `Rename` | `IUniverseAgentConnection.renameSession`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`title`；空 title 清自定义标题。Web stub `unsupported_environment`。**ConversationEngineRosterService** 接通后转发 unary（空/未变/未知 id 不发）；断连缓存只改本地标题。Lens / stub 未接通仍走本地 `renameSession` |
| `AgentService` | `Cancel` | `IUniverseAgentConnection.cancelGeneration`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`agent_id`。会话回合 Stop（≠ `CancelToolCall`）。Web stub `unsupported_environment`。**ConversationEngineRosterService** 接通后转发 unary（未知 id / 断连缓存不发；未指定 `agentId` 用末条 streaming `agentId` 否则 `root`）。Inbox Stop 仅在已连接且有 streaming 行时启用并调用 roster |
| `AgentService` | `CancelToolCall` | `IUniverseAgentConnection.cancelToolCall?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`agent_id`/`tool_call_id`（空 agent id 线为 `root`）。响应 `success`/`message`。合同可选。**ConversationEngineRosterService.cancelToolCall** 接通后转发 unary（空 `toolCallId` / 未知 id / 断连缓存 / 无 hook 不发；未指定 agent 用末条 streaming 否则 `root`）。时间线 process-fold 执行中工具行「Cancel Tool」走该转发（`toolCallId` ≡ 行 id；≠ Inbox Stop / Kill） |
| `AgentService` | `RunToolInBackground` | `IUniverseAgentConnection.runToolInBackground?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`agent_id`/`tool_call_id`。响应 `success`/`message`/`reason_code`。合同可选。空 `sessionId` / 空 `agentId` / 空 `toolCallId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `CancelToolCall` / `StopShellTask` / `Cancel` / `Resume` |
| `AgentService` | `StopShellTask` | `IUniverseAgentConnection.stopShellTask?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`task_id`。响应 `success`/`message`。合同可选。空 `sessionId` / 空 `taskId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `CancelToolCall` / `RunToolInBackground` / `Cancel` / `StopLoop` |
| `AgentService` | `SendShellSessionClientControl` | `IUniverseAgentConnection.sendShellSessionClientControl?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`tool_call_id`/`ref_id`/`control_payload_json`。响应 `success`/`error_message`/`error_code`/`debounced`/`delivered_to_subscribe`。合同可选。空 `sessionId` / 空 `toolCallId` / 空 `refId` / 空 `controlPayloadJson` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `StopShellTask` / `SubscribeToolDetail` / `FetchToolUsageDetail` / `CancelToolCall` / `RunToolInBackground` |
| `AgentService` | `FetchToolUsageDetail` | `IUniverseAgentConnection.fetchToolUsageDetail?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`tool_call_id`。响应 `success`/`tool_call_id`/`context_sources`（`source_type`/`source_agent_id`/`source_scope_id`/`message_id`/`estimated_tokens`）/`error_message`。合同可选。空 `sessionId` / 空 `toolCallId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `FetchToolDetail` / `SubscribeToolDetail` / `Usage` / `Compact` |
| `AgentService` | `FireTriggerWebhook` | `IUniverseAgentConnection.fireTriggerWebhook?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`trigger_id`/`payload_json`。响应 `status`/`event_id`/`reason`。合同可选。空 `sessionId` / 空 `triggerId` / 空 `payloadJson` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `FetchToolUsageDetail` / `SubscribeToolDetail` / `SwitchWorkDir` |
| `AgentService` | `InstallSessionDemoFake` | `IUniverseAgentConnection.installSessionDemoFake?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`queues_payload`/`content_type`/`playbook_id`。响应 `success`/`message`/`reason_code`。合同可选。空 `sessionId` / 空 `playbookId` / 空 `contentType` / 空 `queuesPayload` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `FireTriggerWebhook` / `ClearSessionDemoFake` / `ChatSync` / `SyncInputDelivery` |
| `AgentService` | `ClearSessionDemoFake` | `IUniverseAgentConnection.clearSessionDemoFake?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`。响应 `success`/`message`/`reason_code`。合同可选。空 `sessionId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `InstallSessionDemoFake` / `FireTriggerWebhook` / `ChatSync` / `SyncInputDelivery` |
| `AgentService` | `SwitchWorkDir` | `IUniverseAgentConnection.switchWorkDir?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`agent_id`/`new_work_dir`。响应 `success`/`previous_work_dir`/`current_work_dir`/`message`。合同可选。空 `sessionId` / 空 `agentId` / 空 `newWorkDir` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `FireTriggerWebhook` / `TestModelProfile` / `Connect.work_dir` |
| `AgentService` | `TestModelProfile` | `IUniverseAgentConnection.testModelProfile?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `provider_id`/`model_id`/`api_key`/`base_url`/`protocol`/`params`。响应 `success`/`error_message`。合同可选。空 `providerId` / 空 `modelId` / 空 `apiKey` / 空 `baseUrl` / 空 `protocol` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences。≠ `SwitchWorkDir` / `ListModels` / `SwitchModel` / `ConfigService.Get` / `ConfigService.Set` |
| `AgentService` | `SubscribeToolDetail` | `IUniverseAgentConnection.openSubscribeToolDetailStream?`（server-stream `SubscribeToolDetailChunk`） | **已进** `UniverseAgentGrpcServices.Agent` + node `openSubscribeToolDetailStream`（`makeServerStreamClient`，snake_case `session_id`/`tool_call_id`/`detail_kind`/`ref_id`/`from_revision` + 可选 `mime_type`/`tail_bytes`）。帧 `success`/`error_message`/`content`/`revision`/`truncated`/`total_bytes`/`mime_type`/`eof`/`content_mode`。合同可选。空 `sessionId` / `toolCallId` / `refId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `FetchToolDetail` / `FetchToolUsageDetail` / `SendShellSessionClientControl` |
| `PermissionService` | `SetSessionGoal` / `CancelSessionGoal` | `IUniverseAgentConnection.setSessionGoal?` / `cancelSessionGoal?`（unary） | **已进** `UniverseAgentGrpcServices.Permission`（`universeagent.session.v1.PermissionService`，与 Session 同 proto）+ node snake_case `session_id`/`goal`。响应 `success`/`error`。Web / 测试可省略。**ConversationEngineRosterService** 接通后转发 unary（空/未变/未知 id / 断连缓存不发；无 hook 不发）。Inbox Goal 接通后启用；输入非空 → `setSessionGoal`，已有本地目标时清空 → `cancelSessionGoal`。无 `GetSessionGoal`，文案只反映本机上次成功 set |
| `PermissionService` | `Respond` | `IUniverseAgentConnection.respondPermission?`（unary） | **已进** `UniverseAgentGrpcServices.Permission` + node snake_case `session_id`/`request_id`/`granted`/`metadata_json`。响应 `success`/`error`。合同可选。**ConversationEngineRosterService.resolveConfirmation** 接通后转发 unary（空 `turnId` / 未知 id / 断连缓存 / 无 hook 不发；`granted` = allowed，skipped → false）。时间线权限座接通后走该转发，**不**再 post Chat 臂 `permissionRespond`。未接通仍 Chat 臂 |
| `PermissionService` | `SyncPermissionRule` | `IUniverseAgentConnection.syncPermissionRule?`（unary） | **已进** `UniverseAgentGrpcServices.Permission` + node snake_case `session_id`/`tool_name`/`scope`/`action`/`reason`。响应 `success`/`rule_id`。合同可选。空 `sessionId` / 空 `toolName` / 空 `scope` / 空 `reason` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `Respond` / `SetSessionGoal` / `PromotePermissionRule` / `GetSessionRules` / `SetPermissionMode` |
| `PermissionService` | `PromotePermissionRule` | `IUniverseAgentConnection.promotePermissionRule?`（unary） | **已进** `UniverseAgentGrpcServices.Permission` + node snake_case `tool_name`/`scope`/`action`。响应 `success`。合同可选。空 `toolName` / `scope` 原样上线；`action` typed `RuleAction` wire。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `Respond` / `SetSessionGoal` / `SyncPermissionRule` / `GetSessionRules` || `AgentService` | `RespondQuestion` | `IUniverseAgentConnection.respondQuestion?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id` + nested `response.question_id`/`answers`（`selected_labels`）/`custom_text`。响应 `success`/`error`。合同可选。空 `questionId` 原样上线。**ConversationEngineRosterService.respondQuestion** 接通后转发 unary（空 `questionId` / 未知 id / 断连缓存 / 无 hook 不发）。时间线问题座接通后走该转发，**不**再 post Chat 臂 `questionRespond`。未接通仍 Chat 臂（≠ Permission.Respond / clientTool） |
| `PermissionService` | `GetSessionRules` | `IUniverseAgentConnection.getSessionRules?`（unary） | **已进** `UniverseAgentGrpcServices.Permission` + node snake_case `session_id`。响应 `rules[]`（`id`/`tool_name`/`scope`/`action`/`reason`/`created_at`/`expires_at?`/`source`）。合同可选。空 `sessionId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `SetSessionGoal` / `CancelSessionGoal` / `Respond` / `SyncPermissionRule` / `PromotePermissionRule` |
| `PermissionService` | `SetPermissionMode` | `IUniverseAgentConnection.setPermissionMode?`（unary） | **已进** `UniverseAgentGrpcServices.Permission` + node snake_case `session_id`/`mode`。响应 `success`/`error`。合同可选。空 `sessionId` 原样上线；`mode` typed `SessionToolPermissionModeProto` wire。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `SetSessionGoal` / `CancelSessionGoal` / `Respond` / `SyncPermissionRule` / `PromotePermissionRule` / `GetSessionRules` |
| `AgentService` | `RespondQuestion` | `IUniverseAgentConnection.respondQuestion?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id` + nested `response.question_id`/`answers`（`selected_labels`）/`custom_text`。响应 `success`/`error`。合同可选。空 `questionId` 原样上线。**ConversationEngineRosterService.respondQuestion** 接通后转发 unary（空 `questionId` / 未知 id / 断连缓存 / 无 hook 不发）。时间线问题座接通后走该转发，**不**再 post Chat 臂 `questionRespond`。未接通仍 Chat 臂（≠ Permission.Respond / clientTool） || `AgentService` | `EnqueueQueueItem` / `PauseQueue` / `ResumeQueue` / `ClearQueue` / `HoldQueueItem` / `ReleaseQueueItemHold` / `EditQueueItem` | `IUniverseAgentConnection` 七个 queue unary（`enqueueQueueItem` … `editQueueItem`） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`item_id`/`op_id`/`text` + hold `reason` / enqueue `priority` 枚举数。Web stub `unsupported_environment`。**ConversationEngineRosterService** 接通后转发 Enqueue / Pause / Resume / Clear / Hold / Release / Edit（未知 id / 空 item / 空正文 / 断连缓存不发）。`getMessageQueueState` 接通后诚实空（无 GetQueue，不把 fixture 冒充引擎队列）。Composer 发送仍走 `submitInput`，不自动改 Enqueue |
| `AgentService` | `InsertQueueItem` | `IUniverseAgentConnection.insertQueueItem?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`op_id`/`client_message_id`/`text`/`priority`/`before_item_id`。响应同 `QueueMutationResponse`。合同可选。空 `sessionId` / 空 `opId` / 空 `clientMessageId` / 空 `beforeItemId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `EnqueueQueueItem` / `ReorderQueue` / `EditQueueItem` / `SubscribeToolDetail` |
| `AgentService` | `ReorderQueue` | `IUniverseAgentConnection.reorderQueue?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`op_id`/`item_ids`。响应同 `QueueMutationResponse`。合同可选。空 `sessionId` / 空 `opId` / 空 `itemIds` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `EnqueueQueueItem` / `InsertQueueItem` / `EditQueueItem` / `DeleteQueueItem` |
| `AgentService` | `DeleteQueueItem` | `IUniverseAgentConnection.deleteQueueItem?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`item_id`/`op_id`。响应同 `QueueMutationResponse`。合同可选。空 `sessionId` / 空 `itemId` / 空 `opId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `ReorderQueue` / `InsertQueueItem` / `RetryQueueItem` / `EditQueueItem` |
| `AgentService` | `RetryQueueItem` | `IUniverseAgentConnection.retryQueueItem?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`item_id`/`op_id`。响应同 `QueueMutationResponse`。合同可选。空 `sessionId` / 空 `itemId` / 空 `opId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `DeleteQueueItem` / `ReorderQueue` / `InsertQueueItem` / `EditQueueItem` / `RetryAllFailed` |
| `AgentService` | `RetryAllFailed` | `IUniverseAgentConnection.retryAllFailed?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`op_id`。响应同 `QueueMutationResponse`。合同可选。空 `sessionId` / 空 `opId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `RetryQueueItem` / `RetryQueueItemUpload` / `DeleteQueueItem` / `ReorderQueue` |
| `AgentService` | `RetryQueueItemUpload` | `IUniverseAgentConnection.retryQueueItemUpload?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`item_id`/`op_id`。响应同 `QueueMutationResponse`。合同可选。空 `sessionId` / 空 `itemId` / 空 `opId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `RetryQueueItem` / `RetryAllFailed` / `DeleteQueueItem` / `PinQueueItem` |
| `AgentService` | `PinQueueItem` | `IUniverseAgentConnection.pinQueueItem?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`item_id`/`op_id`。响应同 `QueueMutationResponse`。合同可选。空 `sessionId` / 空 `itemId` / 空 `opId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `RetryQueueItem` / `RetryQueueItemUpload` / `DeleteQueueItem` / `SetQueueItemLocked` |
| `AgentService` | `SetQueueItemLocked` | `IUniverseAgentConnection.setQueueItemLocked?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`item_id`/`op_id`/`locked`。响应同 `QueueMutationResponse`。合同可选。空 `sessionId` / 空 `itemId` / 空 `opId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `PinQueueItem` / `InjectQueueItem` / `RetryQueueItemUpload` / `DeleteQueueItem` |
| `AgentService` | `InjectQueueItem` | `IUniverseAgentConnection.injectQueueItem?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`item_id`/`op_id`。响应同 `QueueMutationResponse`。合同可选。空 `sessionId` / 空 `itemId` / 空 `opId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `PinQueueItem` / `SetQueueItemLocked` / `RetryQueueItemUpload` / `DeleteQueueItem` |
| `AgentService` | `SetQueueItemForkAnchor` | `IUniverseAgentConnection.setQueueItemForkAnchor?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`item_id`/`op_id`/`fork_from_turn_id`/`fork_from_preview`。响应同 `QueueMutationResponse`。合同可选。空 `sessionId` / 空 `itemId` / 空 `opId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `PinQueueItem` / `SetQueueItemLocked` / `InjectQueueItem` / `HoldQueueItem` |
| `AgentService` | `Fork` | `IUniverseAgentConnection.forkAgent?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`parent_agent_id`/`name`/`task`/`model_type`/`system_prompt`（空父 id 线为 `root`）。响应 `success`/`agent_id`。合同可选。**ConversationEngineRosterService.forkSubAgent** 接通后转发 unary（未知 id / 断连缓存 / 无 hook 不发；未指定 parent 用末条 streaming 否则 `root`）。接通后用户 Fork 动作走该转发，**不**开本地 Fork tab、不造 catalog id（catalog 仍 liveAgentTree）。未接通仍走 `registerForkChat` |
| `AgentService` | `Kill` | `IUniverseAgentConnection.killAgent?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`agent_id`/`force`。响应 `success`/`message`。合同可选。空 `agent_id` 原样上线（**不**默认 `root`）。**ConversationEngineRosterService.killSubAgent** 接通后转发 unary（未知 id / 断连缓存 / 无 hook 不发；省略 agent 用末条 streaming 否则空串）。接通后用户 Kill 动作走该转发，**不**造本地 catalog 变更。未接通仍 no-op（≠ Cancel / CancelToolCall）。Navigator 仍只读，不挂 Kill 钮 |
| `AgentService` | `DeleteMessage` | `IUniverseAgentConnection.deleteMessage?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`turn_id`/`agent_id`/`operation_id`（空 agent id 线为 `root`）。响应 `success`/`message`/`current_turn_id`/`removed_turn_count`。合同可选。**ConversationEngineRosterService.deleteTurn** 接通后转发 unary（空 `turnId` / 未知 id / 断连缓存 / 无 hook 不发；未指定 agent 用末条 streaming 否则 `root`）。时间线「Delete message」走该转发，**不**本地删引擎回合。未接通 / stub 仍本地 `deleteTurn`（≠ session `Delete` / Cancel / CancelToolCall） |
| `AgentService` | `EditMessage` | `IUniverseAgentConnection.editMessage?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`turn_id`/`new_content`/`agent_id`/`operation_id`（空 agent id 线为 `root`）。响应 `success`/`message`。合同可选。**ConversationEngineRosterService.updateUserTurnText** 接通后转发 unary（空 `turnId` / 空正文 / 未知 id / 断连缓存 / 无 hook 不发；未指定 agent 用末条 streaming 否则 `root`）。时间线 `turnEdit` 保存走该转发（≠ EditQueueItem）。未接通仍本地改 stub 回合 |
| `AgentService` | `SendClientToolResponse` | `IUniverseAgentConnection.sendClientToolResponse?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id` + nested `response.call_id`/`is_error`/`content`/`metadata_json`/`canvas_refs`。响应 `success`/`error`。合同可选。空 `callId` 原样上线。**ConversationEngineRosterService.respondClientTool** 接通后转发 unary（空 `callId` / 未知 id / 断连缓存 / 无 hook 不发）。时间线 client-tool 座接通后走该转发，**不**再 post Chat 臂 `clientToolRespond`。未接通仍 Chat 臂（≠ CancelToolCall / Permission.Respond） |
| `AgentService` | `ListSnapshots` | `IUniverseAgentConnection.listSnapshots?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`。响应 `snapshots[]`（`id`/`session_id`/`title`/`description`/`created_at`/`turn_count`/`token_count`/`model_id`/`is_auto`）。合同可选。**Conversation** SessionBar extra control `ConversationEngineSnapshotsList` 接通后拉列表（id / title / created_at / turn_count）；行上 Restore 接通 + 有 `restoreSnapshot?` + 非空 snapshotId + 已知 session 才发 `connection.restoreSnapshot`；**成功 restore 后 overlay 保持打开并再拉本 unary 刷新行**；失败 / 未发不刷新。空 snapshotId / 断连 / 无 hook **不发** Restore。断连 / 无 list hook / 空 sessionId **不发** list，诚实空 / unavailable，不把 fixture 冒充引擎 snapshot。**不**改 SessionBar History（仍 `conversationTrajectoryList.ts` 回合索引）。CreateSnapshot / DeleteSnapshot 已进 catalog |
| `AgentService` | `ListLoopSnapshots` | `IUniverseAgentConnection.listLoopSnapshots?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`loop_id`。响应 `snapshots[]`（`timestamp`/`turn_id`/`loop_id`/`iteration`/`max_iterations`/`goal`/`exit_condition`/`tmp_file_relative_path`/`is_exit`/`self_supervise`/`terminal_reason`）。合同可选。空 `sessionId` / 空 `loopId` 原样上线。本刀只进 catalog + node 传输；**不**改 SessionBar History / Snapshots overlay / roster。≠ `ListSnapshots`（会话 checkpoint） |
| `AgentService` | `CreateSnapshot` | `IUniverseAgentConnection.createSnapshot?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`title`/`description`。响应 `success`/`snapshot`（同 `SessionSnapshotInfo`）/`error_message`。合同可选。空 `sessionId` / 空 `title` 原样上线。**ConversationEngineRosterService.createSnapshot** 接通后转发 unary（空 `sessionId` / 未知 session / 断连缓存 / 无 hook 不发；省略 title 用默认 `Snapshot`，空 title 原样上线）。Conversation 命令 `workbench.action.conversation.createSnapshot` 对活动会话走该转发（可提示标题）。**不**改 SessionBar History / Snapshots overlay |
| `AgentService` | `RestoreSnapshot` | `IUniverseAgentConnection.restoreSnapshot?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`snapshot_id`。响应 `success`/`error_message`。合同可选。空 `sessionId` / 空 `snapshotId` 原样上线。**Conversation** SessionBar Snapshots overlay 行 Restore 直呼该 unary（接通 + 有 hook + 非空 snapshotId + 已知 session）；空 snapshotId / 断连 / 无 hook **不发**。`ok` 成功后 overlay 保持打开并再拉 `listSnapshots`；失败 / 未发不刷新。不经 roster。**不**改 SessionBar History（仍回合索引） |
| `AgentService` | `DeleteSnapshot` | `IUniverseAgentConnection.deleteSnapshot?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`snapshot_id`。响应 `success`/`error_message`。合同可选。空 `sessionId` / 空 `snapshotId` 原样上线。**ConversationEngineSnapshotsList** 行 Delete 接通后转发 unary（空 `snapshotId` / 未知 session / 断连 / 无 hook 不发；`dialogService.confirm` warning 后才发）。`ok` 成功后 overlay 保持打开并再拉 `listSnapshots`；取消 / 失败 / 未发不刷新。**不**改 gRPC catalog、不新增 RPC、不替换 SessionBar History |
| `AgentService` | `Todo` | `IUniverseAgentConnection.getTodo?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`agent_id`。响应 `items[]`（`id`/`content`/`status`/`priority`/`require_confirm`/`blocked`）。合同可选。空 `sessionId` / 空 `agentId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `Status` / `Tree` / `Compact` |
| `AgentService` | `Usage` | `IUniverseAgentConnection.getUsage?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`agent_id`。响应 `total_input_tokens`/`total_output_tokens`/`total_turns`/`agent_usages[]`/`context_window`/`session_usage`/`recent_request_spans[]`。合同可选。空 `sessionId` / 空 `agentId` 原样上线（空 agentId = session 汇总）。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `Todo` / `Status` / `Tree` / `Compact` / `List` |
| `AgentService` | `History` | `IUniverseAgentConnection.getAgentHistory?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`agent_id`/`limit`/`offset`。响应 `entries[]`（`role`/`content`/`timestamp`/`agent_id`）/`total`。合同可选。空 `sessionId` / 空 `agentId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `Session.GetHistory` / `Compact` / `Usage` / `ListSnapshots` |
| `AgentService` | `Pause` | `IUniverseAgentConnection.pauseAgent?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`agent_id`。响应 `success`/`message`。合同可选。空 `sessionId` / 空 `agentId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `PauseQueue` / `Cancel` / `SuspendLoop` / `Resume` / `Back` |
| `AgentService` | `Resume` | `IUniverseAgentConnection.openResumeStream?`（server-stream `ChatResponse`） | **已进** `UniverseAgentGrpcServices.Agent` + node `openResumeStream`（`makeServerStreamClient`，snake_case `session_id`/`agent_id`）。合同可选。空 `sessionId` / `agentId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `Pause` / `ContinueGeneration` / `ResumeLoop` / `ResumeQueue` / `Session.Resume` |
| `AgentService` | `Back` | `IUniverseAgentConnection.back?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`agent_id`/`operation_id`。响应 `success`/`message`/`current_turn_id`。合同可选。空 `sessionId` / 空 `agentId` / 空 `operationId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `Pause` / `PauseQueue` / `Prune` / `DeleteMessage` / `Reset` / `Branch` |
| `AgentService` | `Prune` | `IUniverseAgentConnection.prune?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`agent_id`。响应 `success`/`message`/`removed_count`。合同可选。空 `sessionId` / 空 `agentId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `Reset` / `Back` / `Compact` / `History` / `DeleteMessage` |
| `AgentService` | `Reset` | `IUniverseAgentConnection.resetAgent?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`agent_id`/`clear_profile_only`。响应 `success`/`message`。合同可选。空 `sessionId` / 空 `agentId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `ResetAgentProfile` / `Prune` / `Branch` / `Back` / `Pause` |
| `AgentService` | `Branch` | `IUniverseAgentConnection.branch?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`agent_id`/`branch_index`/`turn_id`。响应 `success`/`message`/`current_branch`/`total_branches`/`current_turn_id`。合同可选。空 `sessionId` / 空 `agentId` / 空 `turnId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `Reset` / `Back` / `Prune` / `EditMessage` / `Fork` |
| `AgentService` | `SuspendLoop` | `IUniverseAgentConnection.suspendLoop?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`agent_id`。响应 `success`/`message`。合同可选。空 `sessionId` / 空 `agentId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `Pause` / `PauseQueue` / `Cancel` / `Resume` / `ResumeLoop` / `Branch` / `StopLoop` |
| `AgentService` | `ResumeLoop` | `IUniverseAgentConnection.resumeLoop?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`agent_id`。响应 `success`/`message`。合同可选。空 `sessionId` / 空 `agentId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `Pause` / `PauseQueue` / `Cancel` / `Resume` / `ResumeQueue` / `Session.Resume` / `SuspendLoop` / `Branch` / `StopLoop` |
| `AgentService` | `StopLoop` | `IUniverseAgentConnection.stopLoop?`（unary） | **已进** `UniverseAgentGrpcServices.Agent` + node snake_case `session_id`/`agent_id`/`detail`。响应 `success`/`message`。合同可选。空 `sessionId` / 空 `agentId` / 空 `detail` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay。≠ `Pause` / `Cancel` / `SuspendLoop` / `Resume` / `ResumeLoop` / `Branch` |
| `AgentService` | `Tree` | Navigator Agent 树（**host-only**，不经 renderer `IUniverseAgentConnection`） | m6 §11；`UNIMPLEMENTED` → `agentTree=UNSUPPORTED` |
| `AgentService` | `FetchToolDetail` | Conversation DetailRef 按需通道（**host-only**，lease `requestDetail`） | **P2a**；见 §1b；`subscribe=false` |
| `TeamService` | `MemberStatus` / `TaskList` / `TeamInfo` | Navigator Team 段（renderer `IUniverseAgentConnection.team`） | m6 §11 A1 unary |
| `TeamService` | `TaskUpdate` | `IUniverseAgentConnection.taskUpdate?`（unary） | **已进** `UniverseAgentGrpcServices.Team` + node snake_case `session_id`/`agent_id`/`task_id`/`new_status`/`message`。响应 `success`/`message`。合同可选。空 `sessionId` / 空 `agentId` / 空 `taskId` / 空 `newStatus` / 空 `message` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Navigator Team。≠ `TaskList` / `TaskCancel` / `MemberStatus` / `TeamInfo` / `SetPermissionMode` |
| `TeamService` | `TaskCancel` | `IUniverseAgentConnection.taskCancel?`（unary） | **已进** `UniverseAgentGrpcServices.Team` + node snake_case `session_id`/`agent_id`/`task_id`。响应 `success`/`message`。合同可选。空 `sessionId` / 空 `agentId` / 空 `taskId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Navigator Team。≠ `TaskList` / `TaskUpdate` / `MemberStatus` / `TeamInfo` / `SetPermissionMode` |
| `TeamService` | `MessageMember` | `IUniverseAgentConnection.messageMember?`（unary） | **已进** `UniverseAgentGrpcServices.Team` + node snake_case `session_id`/`agent_id`/`member_name`/`content`。响应 `success`/`message`。合同可选。空 `sessionId` / 空 `agentId` / 空 `memberName` / 空 `content` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Navigator Team。≠ `TaskUpdate` / `TaskList` / `TaskCancel` / `MemberStatus` / `TeamInfo` / `CreateTeam` |
| `TeamService` | `CreateTeam` | `IUniverseAgentConnection.createTeam?`（unary） | **已进** `UniverseAgentGrpcServices.Team` + node snake_case `session_id`/`agent_id`/`task_descriptions`。响应 `team_id`/`member_count`。合同可选。空 `sessionId` / 空 `agentId` / 空 `taskDescriptions`（含空串）原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Navigator Team。≠ `TaskList` / `TaskUpdate` / `TaskCancel` / `MessageMember` / `MemberStatus` / `TeamInfo` / `StartMember` |
| `TeamService` | `StartMember` | `IUniverseAgentConnection.startMember?`（unary） | **已进** `UniverseAgentGrpcServices.Team` + node snake_case `session_id`/`agent_id`/`member_name`/`preset_id`/`system_prompt`/`model_type`/`dynamic`。响应 `member_agent_id`/`member_name`/`dynamic`。合同可选。空 `sessionId` / 空 `agentId` / 空 `memberName` / 空 `presetId` / 空 `systemPrompt` / 空 `modelType` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Navigator Team。≠ `CreateTeam` / `TaskUpdate` / `TaskCancel` / `MessageMember` / `KillMember` / `MemberStatus` / `TeamInfo` |
| `TeamService` | `KillMember` | `IUniverseAgentConnection.killMember?`（unary） | **已进** `UniverseAgentGrpcServices.Team` + node snake_case `session_id`/`agent_id`/`member_name`。响应 `success`/`message`。合同可选。空 `sessionId` / 空 `agentId` / 空 `memberName` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Navigator Team。≠ `StartMember` / `CreateTeam` / `TaskCancel` / `MessageMember` / `MemberStatus` / `TeamInfo` / `Agent.Kill` |
| `TeamService` | `Abort` | `IUniverseAgentConnection.abort?`（unary） | **已进** `UniverseAgentGrpcServices.Team` + node snake_case `session_id`/`agent_id`/`team_id`/`reason`。响应 `success`/`message`/`stopped_members`。合同可选。空 `sessionId` / 空 `agentId` / 空 `reason` 原样上线；`teamId` 0 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Navigator Team。≠ `KillMember` / `CreateTeam` / `StartMember` / `MessageMember` / `TaskCancel` / `TeamInfo` |
| L3 `tool_runtime_snapshot` | `payload.file_mutation_payload` + `ToolCallLifecycleEvent` join | Sources Review 归因 chip / `reviewNav` 物化（**host-only** demux；contrib 消费 `onDidFileMutation`） | m6 §11 A2；禁止解析 L2 `arguments_json`；历史见 **G-REV-1** |
| `ToolService` | `ListSkills` / `SkillInfo` / `SetSkillEnabled` | **@ HEAD** 传输 + `EngineSkillsSection` list/toggle + 正文 **读**（E1） | 无独立 Create RPC；写文件后 `ListSkills` 刷新 |
| `ToolService` | `SaveSkillContent` | **@ HEAD** node gRPC @ `45fa7a35`/`040c823d` + common 可选 `saveSkillContent?` @ `f3f2d366` + **UI** textarea/Save @ `f3f2d366`/`3e986bde` | USER/PROJECT 可编、BUNDLED 只读；断连/`UNSUPPORTED` 不渲染；probe/`UNIMPLEMENTED` 时 `saveSkillContent?` 缺席、Save 隐藏；运行时 `UNIMPLEMENTED` → `{ ok: false }` |
| `ToolService` | `ListTools` / `ToolInfo` | **@ HEAD** `listTools` → `EngineToolsSection` 目录 + profile 启用 checkbox。`IUniverseAgentConnection.getToolInfo?` **已进** `UniverseAgentGrpcServices.Tool` + node unary `getToolInfo`（snake_case `tool_name`；响应 name / description / category / `input_schema_json` / destructive / `requires_permission` / aliases）。合同可选。**Engine Tools 选中行**拉 `getToolInfo` 只读详情（aliases / Destructive / Requires permission；有 schema 只标「Has input schema」），**不**画 schema 编辑器；无 hook 不展示详情 | 无 `SetToolEnabled`；enablement 经 `SaveAgentProfile` → `{profileDir}/tools.json`（`engineToolProfile.ts` @ `f49615a1`） |
| `ToolService` | `ListCommands` | `IUniverseAgentConnection.listCommands?`（unary） | **已进** `UniverseAgentGrpcServices.Tool` + node unary `listCommands`（空 request `{}`；响应 `commands`/`total`；条目 name / description / `SlashCommandSource` / `slash_enabled` / agent / model / subtask / `skill_source`）。合同可选。空 `name` / 空 `agent` / 空 `model` / 空 `skill_source` 原样映射。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer / 斜杠 UI。≠ `ListSkills` / `ListTools` / `ToolInfo` / `GetCommandDef` / `ResolveModel` |
| `ToolService` | `GetCommandDef` | `IUniverseAgentConnection.getCommandDef?`（unary） | **已进** `UniverseAgentGrpcServices.Tool` + node unary `getCommandDef`（snake_case `command_name`；响应 name / description / `SlashCommandSource` / template / agent / model / subtask / `mcp_server_id` / `mcp_prompt_name` / `mcp_argument_names` / `skill_source`）。合同可选。空 `commandName` / 空 `name` / 空 `agent` / 空 `model` / 空 `template` / 空 `mcp_server_id` / 空 `mcp_prompt_name` / 空 `skill_source` 原样映射。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer / 斜杠 UI。≠ `ListCommands` / `ListSkills` / `ListTools` / `ToolInfo` / `ResolveModel` |
| `FileService` | `ReadFile` | `IUniverseAgentConnection.readFile?`（unary） | **已进** `UniverseAgentGrpcServices.File` + node unary `readFile`（snake_case `path`/`session_id`/`start_line`/`end_line`/`max_bytes`；响应 `content`/`total_size`/`mime_type`/`line_count`/`content_hash`）。合同可选。空 `path` / 空 `sessionId` 原样上线；`start_line` / `end_line` / `max_bytes` 0 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListFiles` / `GetFileInfo` / `WriteFile` |
| `FileService` | `GetFileInfo` | `IUniverseAgentConnection.getFileInfo?`（unary） | **已进** `UniverseAgentGrpcServices.File` + node unary `getFileInfo`（snake_case `path`/`session_id`；响应 `file` FileEntry：`name`/`path`/`is_directory`/`size`/`last_modified`/`mime_type`）。合同可选。空 `path` / 空 `sessionId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Explorer / Engine Preferences / Composer。≠ `ListFiles` / `ReadFile` / `WriteFile` / `ForceWriteFile` / `AgentMerge` |
| `FileService` | `WriteFile` | `IUniverseAgentConnection.writeFile?`（unary） | **已进** `UniverseAgentGrpcServices.File` + node unary `writeFile`（snake_case `path`/`content`/`base_hash`/`session_id`/`base_content`；响应 `status`/`new_hash`/`size`/`modified_at`/`current_content`/`current_hash`/`merged_content`）。合同可选。空 `path` / 空 `sessionId` / 空 `baseHash` 原样上线；空 `content` / 空 `base_content` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListFiles` / `ReadFile` / `GetFileInfo` / `ForceWriteFile` / `AgentMerge` |
| `FileService` | `ForceWriteFile` | `IUniverseAgentConnection.forceWriteFile?`（unary） | **已进** `UniverseAgentGrpcServices.File` + node unary `forceWriteFile`（snake_case `path`/`content`/`session_id`；响应 `WriteFileResponse`：`status`/`new_hash`/`size`/`modified_at`/`current_content`/`current_hash`/`merged_content`）。合同可选。空 `path` / 空 `sessionId` 原样上线；空 `content` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Explorer / Engine Preferences / Composer。≠ `ListFiles` / `ReadFile` / `GetFileInfo` / `WriteFile` / `AgentMerge` |
| `FileService` | `AgentMerge` | `IUniverseAgentConnection.agentMerge?`（unary） | **已进** `UniverseAgentGrpcServices.File` + node unary `agentMerge`（snake_case `session_id`/`path`/`base_content`/`current_content`/`user_content`；响应 `accepted`）。合同可选。空 `sessionId` / 空 `path` 原样上线；空 `base_content` / `current_content` / `user_content` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListFiles` / `ReadFile` / `GetFileInfo` / `WriteFile` / `ForceWriteFile` |
| `GitService` | `ReadGitSummary` | `IUniverseAgentConnection.readGitSummary?`（unary） | **已进** `UniverseAgentGrpcServices.Git`（`universeagent.git.v1.GitService`）+ node unary `readGitSummary`（snake_case `session_id`；响应 `supported`/`reason`/`branch`/`change_count`）。合同可选。空 `sessionId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / SCM / Engine Preferences / Composer。≠ `ReadGitChanges` / `ReadGitFileDiff` / `WriteGitStagePaths` / `WriteGitCommit` / `WriteGitApplyHunks` |
| `GitService` | `ReadGitChanges` | `IUniverseAgentConnection.readGitChanges?`（unary） | **已进** `UniverseAgentGrpcServices.Git` + node unary `readGitChanges`（snake_case `session_id`；响应 `supported`/`reason`/`branch`/`entries`：`path`/`old_path`/`kind`/`index_state`）。合同可选。空 `sessionId` 原样上线；空 `path` / `old_path` / `kind` / `index_state` / `reason` / `branch` 原样映射。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Sources Review / Engine Preferences / Composer。≠ `ReadGitSummary` / `ReadGitFileDiff` / `WriteGitStagePaths` / `WriteGitCommit` / `WriteGitApplyHunks` |
| `GitService` | `ReadGitFileDiff` | `IUniverseAgentConnection.readGitFileDiff?`（unary） | **已进** `UniverseAgentGrpcServices.Git`（`universeagent.git.v1.GitService`）+ node unary `readGitFileDiff`（snake_case `session_id`/`path`/`index_state`；响应 `supported`/`reason`/`path`/`unified_diff`）。合同可选。空 `sessionId` / 空 `path` / 空 `indexState` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / SCM / Engine Preferences / Composer。≠ `ReadGitSummary` / `ReadGitChanges` / `WriteGitStagePaths` / `WriteGitCommit` / `WriteGitApplyHunks` |
| `GitService` | `WriteGitStagePaths` | `IUniverseAgentConnection.writeGitStagePaths?`（unary） | **已进** `UniverseAgentGrpcServices.Git`（`universeagent.git.v1.GitService`）+ node unary `writeGitStagePaths`（snake_case `session_id`/`commands[].argv`；响应 `supported`/`reason`/`success`/`error_message`/`exit_code`/`stdout`）。合同可选。空 `sessionId` 原样上线；空 `commands` / 空 `argv` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / SCM / Engine Preferences / Composer。≠ `ReadGitSummary` / `ReadGitChanges` / `ReadGitFileDiff` / `WriteGitCommit` / `WriteGitApplyHunks` |
| `GitService` | `WriteGitCommit` | `IUniverseAgentConnection.writeGitCommit?`（unary） | **已进** `UniverseAgentGrpcServices.Git` + node unary `writeGitCommit`（snake_case `session_id`/`message`/`sign_off`/`amend`；响应 `supported`/`reason`/`success`/`error_message`/`exit_code`/`stdout`）。合同可选。空 `sessionId` 原样上线；空 `message` 原样上线；`sign_off` / `amend` false 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / SCM / Engine Preferences / Composer。≠ `ReadGitSummary` / `ReadGitChanges` / `ReadGitFileDiff` / `WriteGitStagePaths` / `WriteGitApplyHunks` |
| `GitService` | `WriteGitApplyHunks` | `IUniverseAgentConnection.writeGitApplyHunks?`（unary） | **已进** `UniverseAgentGrpcServices.Git` + node unary `writeGitApplyHunks`（snake_case `session_id`/`argv`/`patches`；响应 `supported`/`reason`/`success`/`error_message`/`exit_code`/`stdout`）。合同可选。空 `sessionId` 原样上线；空 `argv` / 空 `patches` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / SCM / Engine Preferences / Composer。≠ `ReadGitSummary` / `ReadGitChanges` / `ReadGitFileDiff` / `WriteGitStagePaths` / `WriteGitCommit` |
| `TokenUsageService` | `GetGlobalUsage` | `IUniverseAgentConnection.getGlobalUsage?`（unary） | **已进** `UniverseAgentGrpcServices.TokenUsage`（`universeagent.tokenusage.v1.TokenUsageService`）+ node unary `getGlobalUsage`（空 request `{}`；响应 `usage` TokenUsageData：`input_tokens`/`output_tokens`/`thinking_tokens`/`cache_read_tokens`/`cache_write_tokens`/`total_cost_micros`/`currency`/`request_count`）。合同可选。空 `currency` 原样映射。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `GetSessionUsage` / `Agent.Usage` |
| `MemoryService` | `Save` | `IUniverseAgentConnection.saveMemory?`（unary） | **已进** `UniverseAgentGrpcServices.Memory`（`universeagent.memory.v1.MemoryService`）+ node unary `saveMemory`（snake_case `scope`/`content`/`category`；响应 `success`/`message`/`file_path`）。合同可选。空 `scope` / 空 `content` / 空 `category` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer / Memory pane。≠ `Search` / `SearchDeep` / `Read` / `List` / `Delete` / `Reflect` / `Rebuild` / `Revert` / `History` |
| `MemoryService` | `Search` | `IUniverseAgentConnection.searchMemory?`（unary） | **已进** `UniverseAgentGrpcServices.Memory`（`universeagent.memory.v1.MemoryService`）+ node unary `searchMemory`（snake_case `scope`/`query`/`keywords`/`limit`；响应 `results[]`：`category`/`filename`/`title`/`score`/`snippet`/`forgot`/`scope`）。合同可选。空 `scope` / 空 `query` / 空 `keywords` 原样上线；`limit` 0 原样上线；空 `category` / `filename` / `title` / `snippet` / `scope` 原样映射。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `Save` / `SearchDeep` / `Read` / `List` / `Delete` / `Reflect` / `Rebuild` / `Revert` / `History` |
| `MemoryService` | `SearchDeep` | `IUniverseAgentConnection.searchDeepMemory?`（unary） | **已进** `UniverseAgentGrpcServices.Memory`（`universeagent.memory.v1.MemoryService`）+ node unary `searchDeepMemory`（snake_case `scope`/`query`/`keywords`/`categories`/`limit`/`include_content`；响应 `results[]`：`category`/`filename`/`title`/`score`/`snippet`/`forgot`/`scope`；`searched_categories`）。合同可选。空 `scope` / 空 `query` / 空 `keywords` / 空 `categories` 原样上线；`limit` 0 原样上线；`include_content` false 原样上线；空 `category` / `filename` / `title` / `snippet` / `scope` 原样映射；空 `searched_categories` 原样映射。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `Save` / `Search` / `Read` / `List` / `Delete` / `Reflect` / `Rebuild` / `Revert` / `History` |
| `MemoryService` | `Read` | `IUniverseAgentConnection.readMemory?`（unary） | **已进** `UniverseAgentGrpcServices.Memory`（`universeagent.memory.v1.MemoryService`）+ node unary `readMemory`（snake_case `scope`/`category`/`filename`/`section`/`mode`/`forgot`；响应 `content`/`metadata` MemoryFileMetadata：`category`/`filename`/`title`/`tags`/`created_at`/`updated_at`/`version`）。合同可选。空 `scope` / 空 `category` / 空 `filename` / 空 `section` / 空 `mode` 原样上线；`forgot` false 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer / Memory pane。≠ `Save` / `Search` / `SearchDeep` / `List` / `Delete` / `Reflect` / `Rebuild` / `Revert` / `History` |
| `MemoryService` | `List` | `IUniverseAgentConnection.listMemory?`（unary） | **已进** `UniverseAgentGrpcServices.Memory`（`universeagent.memory.v1.MemoryService`）+ node unary `listMemory`（snake_case `scope`/`category`；响应 `categories[]`：`category`/`files[]`（`filename`/`title`/`updated_at`）/`file_count`）。合同可选。空 `scope` / 空 `category` 原样上线；空 `filename` / `title` / `category` 原样映射；`updated_at` / `file_count` 0 原样映射。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer / Memory pane。≠ `Save` / `Search` / `SearchDeep` / `Read` / `Delete` / `Reflect` / `Rebuild` / `Revert` / `History` |
| `MemoryService` | `Delete` | `IUniverseAgentConnection.deleteMemory?`（unary） | **已进** `UniverseAgentGrpcServices.Memory`（`universeagent.memory.v1.MemoryService`）+ node unary `deleteMemory`（snake_case `scope`/`category`/`filename`；响应 `success`/`message`）。合同可选。空 `scope` / 空 `category` / 空 `filename` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer / Memory pane。≠ `Save` / `Search` / `SearchDeep` / `Read` / `List` / `Reflect` / `Rebuild` / `Revert` / `History` |
| `MemoryService` | `Reflect` | `IUniverseAgentConnection.reflectMemory?`（unary） | **已进** `UniverseAgentGrpcServices.Memory`（`universeagent.memory.v1.MemoryService`）+ node unary `reflectMemory`（snake_case `scope`/`categories`；响应 `diagnoses[]`：`type`/`category`/`filename`/`description`/`suggestion` + `summary`）。合同可选。空 `scope` / 空 `categories` 原样上线；空 `type` / `category` / `filename` / `description` / `suggestion` / `summary` 原样映射。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer / Memory pane。≠ `Save` / `Search` / `SearchDeep` / `Read` / `List` / `Delete` / `Rebuild` / `Revert` / `History` |
| `MemoryService` | `Rebuild` | `IUniverseAgentConnection.openRebuildMemoryStream?`（server-stream `MemoryRebuildEvent`） | **已进** `UniverseAgentGrpcServices.Memory`（`universeagent.memory.v1.MemoryService`）+ node `openRebuildMemoryStream`（`makeServerStreamClient`，snake_case `scope`/`dry_run`）。帧 `phase`/`message`/`progress`/`files_processed`/`files_total`。合同可选。空 `scope` 原样上线；`dry_run` false 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer / Memory pane。≠ `Save` / `Search` / `SearchDeep` / `Read` / `List` / `Delete` / `Reflect` / `Revert` / `History` |
| `MemoryService` | `Revert` | `IUniverseAgentConnection.revertMemory?`（unary） | **已进** `UniverseAgentGrpcServices.Memory`（`universeagent.memory.v1.MemoryService`）+ node unary `revertMemory`（snake_case `scope`/`category`/`filename`/`target_version`；响应 `success`/`message`/`reverted_to_version`）。合同可选。空 `scope` / 空 `category` / 空 `filename` 原样上线；`target_version` 0 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer / Memory pane。≠ `Save` / `Search` / `SearchDeep` / `Read` / `List` / `Delete` / `Reflect` / `Rebuild` / `History` |
| `MemoryService` | `History` | `IUniverseAgentConnection.historyMemory?`（unary） | **已进** `UniverseAgentGrpcServices.Memory`（`universeagent.memory.v1.MemoryService`）+ node unary `historyMemory`（snake_case `scope`/`category`/`filename`/`limit`；响应 `changes[]`：`version`/`change_type`/`summary`/`timestamp`/`author`）。合同可选。空 `scope` / 空 `category` / 空 `filename` 原样上线；`limit` 0 原样上线；空 `change_type` / `summary` / `author` 原样映射；`version` / `timestamp` 0 原样映射。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer / Memory pane。≠ `Save` / `Search` / `SearchDeep` / `Read` / `List` / `Delete` / `Reflect` / `Rebuild` / `Revert` |
| `FileTransferService` | `GetUploadProgress` | `IUniverseAgentConnection.getUploadProgress?`（unary） | **已进** `UniverseAgentGrpcServices.FileTransfer`（`universeagent.filetransfer.v1.FileTransferService`）+ node unary `getUploadProgress`（snake_case `transfer_id`/`session_id`；响应 `exists`/`bytes_received`/`partial_path`）。合同可选。空 `transferId` / 空 `sessionId` 原样上线；`bytes_received` 0 / 空 `partial_path` 原样映射。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `UploadAttachment` / `DownloadAttachment` |
| `FileTransferService` | `UploadAttachment` | `IUniverseAgentConnection.openUploadAttachmentStream?`（client-stream `UploadChunk` → `UploadResponse`） | **已进** `UniverseAgentGrpcServices.FileTransfer`（`universeagent.filetransfer.v1.FileTransferService`）+ node `openUploadAttachmentStream`（`makeClientStreamClient`，snake_case `transfer_id`/`filename`/`mime_type`/`checksum_sha256`/`session_id`/`queue_item_id` + `chunk` bytes + `offset`）。响应 `success`/`file_path`/`checksum_sha256`/`error_message`/`error_code`。合同可选。空 `transferId` / `filename` / `sessionId` / `queueItemId` 原样上线；空 `chunk` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `DownloadAttachment` / `GetUploadProgress` |
| `FileTransferService` | `DownloadAttachment` | `IUniverseAgentConnection.openDownloadAttachmentStream?`（server-stream `DownloadChunk`） | **已进** `UniverseAgentGrpcServices.FileTransfer`（`universeagent.filetransfer.v1.FileTransferService`）+ node `openDownloadAttachmentStream`（`makeServerStreamClient`，snake_case `file_path`/`offset`/`max_bytes`/`session_id`/`artifact_id`）。帧 `offset`/`data`/`total_size`/`is_last`/`checksum_sha256`。合同可选。空 `filePath` / 空 `sessionId` / 空 `artifactId` 原样上线；`offset` / `maxBytes` 0 原样上线；空 `data` / 空 `checksumSha256` 原样映射。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `UploadAttachment` / `GetUploadProgress` |
| `ClipboardService` | `Write` | `IUniverseAgentConnection.writeClipboard?`（unary） | **已进** `UniverseAgentGrpcServices.Clipboard`（`universeagent.clipboard.v1.ClipboardService`）+ node unary `writeClipboard`（snake_case `session_id`/`agent_id`/`label`/`type`/`content`/`file_path`/`url`；响应 `clip_id`）。合同可选。空 `sessionId` / 空 `agentId` / 空 `label` / 空 `content` / 空 `filePath` / 空 `url` 原样上线；空 `clip_id` 原样映射。`type` 用 typed `ClipboardEntryType` wire（Write RPC 仅 TEXT；`file_path` / `url` 保留原样上线）。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `Read` / `List` / `Clear` / DeviceService / TriggerService |
| `ClipboardService` | `Read` | `IUniverseAgentConnection.readClipboard?`（unary） | **已进** `UniverseAgentGrpcServices.Clipboard`（`universeagent.clipboard.v1.ClipboardService`）+ node unary `readClipboard`（snake_case `session_id`/`clip_id`；响应 `entry` ClipboardEntry：`clip_id`/`label`/`type`/`content`/`created_by`/`created_at`）。合同可选。空 `sessionId` / 空 `clipId` 原样上线；空 `clip_id` / `label` / `content` / `created_by` 原样映射；`created_at` 0 原样映射。`type` 用 typed `ClipboardEntryType` wire。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `Write` / `List` / `Clear` / DeviceService / TriggerService |
| `ContextVariableService` | `List` | `IUniverseAgentConnection.listContextVariable?`（unary） | **已进** `UniverseAgentGrpcServices.ContextVariable`（`universeagent.contextvariable.v1.ContextVariableService`）+ node unary `listContextVariable`（snake_case `session_id`/`agent_id`；响应 `current[]` / `inherited[]`：`name`/`scope`/`updated_by`/`updated_at`/`content_preview`）。合同可选。空 `sessionId` / 空 `agentId` 原样上线；空 `name` / `updated_by` / `content_preview` 原样映射；`updated_at` 0 原样映射。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `Read` |
| `ContextVariableService` | `Read` | `IUniverseAgentConnection.readContextVariable?`（unary） | **已进** `UniverseAgentGrpcServices.ContextVariable`（`universeagent.contextvariable.v1.ContextVariableService`）+ node unary `readContextVariable`（snake_case `session_id`/`name`/`agent_id`；响应 `entry`：`name`/`content`/`scope`/`updated_by`/`updated_at`）。合同可选。空 `sessionId` / 空 `name` / 空 `agentId` 原样上线；空 `name` / `content` / `updated_by` 原样映射；`updated_at` 0 原样映射。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `List` |
| `AgentService` | `ListAgentProfiles` / `SaveAgentProfile` / `DeleteAgentProfile` / `ResetAgentProfile` | **@ HEAD** list + 写 RPC → `EngineAgentsSection`（New/Delete/Reset 工具栏 + **`AGENTS.md` 全文编辑器** @ `9419f583`） | 选中 profile textarea + Save → `SaveAgentProfile`；断连/`UNSUPPORTED` 不渲染；built_in 只读；built_in 不可 Delete、仅 Reset |
| `McpService` | `ListMcpServers` / `ToggleMcpServer` / `AddMcpServer` / `UpdateMcpServer` / `RemoveMcpServer` | **@ HEAD** list + toggle + 定义 CRUD → `EngineMcpSection` | 运行态 RPC 见 §1b（M7 P1a / E2-4） |
| `PluginService` | `List` / `Info` / `Enable` / `Reload` / `Unload` / `ScanNew` | Plugins 节（M7 E2-5；见 §1b） | Local 模式 Singularity 标 UNSUPPORTED；IDE 以 `List` probe 决定三态 |
| `ConfigService` | `ListModels` | Engine 页 **Model** 组只读注册表（M7 E2-2；见 §1b） | **P1b** `listModels()`；恒 `include_disabled=true`。会话级 `SwitchModel` 见下行 |
| `ConfigService` | `Get` | `IUniverseAgentConnection.getConfig?`（unary） | **已进** `UniverseAgentGrpcServices.Config` + node snake_case `key`/`scope`/`session_id`。响应 `values`/`scope`。合同可选。空 `key` / 空 `scope` / 空 `sessionId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences。≠ `Set` / `Watch` / `ListModels` / `SwitchModel` / `TestModelProfile` |
| `ConfigService` | `Set` | `IUniverseAgentConnection.setConfig?`（unary） | **已进** `UniverseAgentGrpcServices.Config` + node snake_case `key`/`value`/`scope`/`session_id`。响应 `success`/`message`。合同可选。空 `key` / 空 `value` / 空 `scope` / 空 `sessionId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences。≠ `Get` / `Watch` / `ListModels` / `SwitchModel` |
| `ConfigService` | `SwitchModel` | `IUniverseAgentConnection.switchModel?`（unary） | **已进** `UniverseAgentGrpcServices.Config` + node snake_case `session_id`/`agent_id`/`model_type`/`model_id`。响应 `resolved_model_id`/`provider`/`level`/`cost`/`speed`。合同可选。空 `sessionId` / 空 `agentId` / 空 `modelType` / 空 `modelId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListModels` / `TestModelProfile` / `Config.Get` / `Config.Set` / `GetModelPreferences` / `SetModelPreferences` / `SetPermissionPolicy` |
| `ConfigService` | `ResolveModel` | `IUniverseAgentConnection.resolveModel?`（unary） | **已进** `UniverseAgentGrpcServices.Config` + node snake_case `session_id`/`type`。响应 `selected`/`candidates`/`filtered`（`ModelEntryProto`）。合同可选。空 `sessionId` / 空 `type` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListModels` / `SwitchModel` / `Config.Get` / `Config.Set` / `GetModelPreferences` / `SetModelPreferences` / `Watch` |
| `ConfigService` | `SetPermissionPolicy` | `IUniverseAgentConnection.setPermissionPolicy?`（unary） | **已进** `UniverseAgentGrpcServices.Config` + node snake_case `session_id`/`tool_name`/`policy`。响应 `success`/`message`。合同可选。空 `sessionId` / 空 `toolName` 原样上线。`policy` 用 typed `PermissionPolicy` wire。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `SwitchModel` / `ListModels` / `Get` / `Set` / `GetModelPreferences` / `SetModelPreferences` / `SetPermissionMode` |
| `ConfigService` | `GetModelPreferences` | `IUniverseAgentConnection.getModelPreferences?`（unary） | **已进** `UniverseAgentGrpcServices.Config` + node snake_case `session_id`。响应 `min_level`/`max_cost`/`min_speed`/`strategy`。合同可选。空 `sessionId` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `SetModelPreferences` / `SwitchModel` / `ListModels` / `Get` / `Set` / `SetPermissionPolicy` |
| `ConfigService` | `SetModelPreferences` | `IUniverseAgentConnection.setModelPreferences?`（unary） | **已进** `UniverseAgentGrpcServices.Config` + node snake_case `session_id`/`min_level`/`max_cost`/`min_speed`/`strategy`。响应 `preferences.{min_level,max_cost,min_speed,strategy}`。合同可选。空 `sessionId` / 空 `maxCost` / 空 `minSpeed` / 空 `strategy` 原样上线；`minLevel` 0 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences。≠ `GetModelPreferences` / `ResolveModel` / `SwitchModel` / `ListModels` / `Get` / `Set` |
| `ConfigService` | `Watch` | `IUniverseAgentConnection.openWatchConfigStream?`（server-stream `ConfigChangedEvent`） | **已进** `UniverseAgentGrpcServices.Config` + node `openWatchConfigStream`（`makeServerStreamClient`，snake_case `keys`）。帧 `key`/`old_value`/`new_value`/`scope`/`timestamp`。合同可选。空 `keys` 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Engine Preferences。≠ `Get` / `Set` / `ListModels` / `SwitchModel` / `GetCommandDef` |
| `FileService` | `ListFiles` | `IUniverseAgentConnection.listFiles?`（unary） | **已进** `UniverseAgentGrpcServices.File`（`universeagent.file.v1.FileService`）+ node unary `listFiles`（snake_case `path`/`session_id`/`recursive`/`pattern`/`max_results`）。响应 `entries[]`（`name`/`path`/`is_directory`/`size`/`last_modified`/`mime_type`）/`total`。合同可选。空 `path` / 空 `sessionId` / 空 `pattern` 原样上线；`recursive` false / `maxResults` 0 原样上线。本刀只进 catalog + node 传输；**不**改 Conversation roster / SessionBar / Snapshots overlay / Explorer / Engine Preferences。≠ `ReadFile` / `GetFileInfo` / `WriteFile` / `ForceWriteFile` / `AgentMerge` |
| Local `RulesBridge` | list / create / update / delete / preview / health × global / workDir（12）+ `defaultAgentHome()` | **本仓不可达**：`RulesBridge` 是 Desktop/Singularity 进程内接口，IDE 只经 gRPC | **Remote gRPC 不存在** → Engine 页 Rules 一律 `UNSUPPORTED`（见 §4 G-ENG-2） |
| `MemoryService` | `Save` / `Search` / `SearchDeep` / `Read` / `List` / `Delete` / `Reflect` / `Rebuild` / `Revert` / `History` | 不在 Engine 页；未来独立 pane | catalog + node `saveMemory?` / `searchMemory?` / `searchDeepMemory?` / `readMemory?` / `listMemory?` / `deleteMemory?` / `reflectMemory?` / `openRebuildMemoryStream?` / `revertMemory?` / `historyMemory?` 已进；UI 仍未接。与 Instructions 分家 |
| `FileTransferService` | `GetUploadProgress` / `UploadAttachment` / `DownloadAttachment` | 不在 Engine 页 / Conversation | catalog + node `getUploadProgress?` / `openUploadAttachmentStream?` / `openDownloadAttachmentStream?` 已进 |
| `SystemService` | `HealthCheck` | 不在 Engine 页 / Conversation | catalog + node `healthCheck?` 已进 |
| `SystemService` | `Doctor` | 不在 Engine 页 / Conversation | catalog + node `doctor?` 已进 |
| `SystemService` | `Shutdown` | 不在 Engine 页 / Conversation | catalog + node `shutdown?` 已进 |
| `DeviceService` | `ListDevices` | 不在 Engine 页 / Conversation | catalog + node `listDevices?` 已进 |
| `DeviceService` | `PairApprove` | 不在 Engine 页 / Conversation | catalog + node `pairApprove?` 已进 |
| `DeviceService` | `Revoke` | 不在 Engine 页 / Conversation | catalog + node `revoke?` 已进 |
| `TriggerService` | `ListTriggers` | 不在 Engine 页 / Conversation | catalog + node `listTriggers?` 已进；其余 Trigger RPC 与 UI 仍未接。≠ `AgentService.FireTriggerWebhook` / `DeviceService` |
| `ClipboardService` | `Write` | 不在 Engine 页 / Conversation | catalog + node `writeClipboard?` 已进 |
| `ClipboardService` | `Read` | 不在 Engine 页 / Conversation | catalog + node `readClipboard?` 已进 |
| `ContextVariableService` | `List` | 不在 Engine 页 / Conversation | catalog + node `listContextVariable?` 已进 |
| `ContextVariableService` | `Read` | 不在 Engine 页 / Conversation | catalog + node `readContextVariable?` 已进 |
## 1b. M7 协议面（P0/P1a/P1b/P2a/P2b 已绑定）

非 RPC：`IUniverseAgentConnection` / `IUniverseAgentSessionView` / `IUniverseAgentHubService` 在 Web 以 **`registerSingleton`** 注册（**禁止** `registerMainProcessRemoteService`），实现位于 `platform/universeAgent/browser/`。`connectionHub.contribution.ts` **不再**静态 import electron-browser Hub；该注册仅在 `workbench.desktop.main.ts`。

| 服务 | Web 诚实断连（**P0 已落**） | 备注 |
|------|------------------------------|------|
| `IUniverseAgentConnection` | transport `idle`；capability 已有键全 `UNSUPPORTED` reason「Web 不支持本机 Engine 连接」；`connect()` 不 throw、无 token；`connectProfile` `{ ok:false, code:'unsupported_environment' }`；`getConnectionPhase` = `disconnected` | `workbench.web.main.ts` |
| `IUniverseAgentSessionView` | 空 lease；`onDynamicDidApplyFrame` 恒 `Event.None`；`requestDetail` 恒 `{ ok:false, reason:'unavailable' }` | 同上 |
| `IUniverseAgentHubService` | `getAuthStatus=unavailable`；mutating `ok:false`；不回显凭据 | 同上 |

| 服务 | RPC | 本仓消费面 | P 切片 | 状态 |
|------|-----|-----------|--------|------|
| `McpService` | `GetMcpServerStatuses` / `GetMcpServerTools` | Engine Runtime tab | **P1a** `getMcpServerStatuses` / `getMcpServerTools`；`mcpRuntime` | **已绑定** |
| `PluginService` | `List` / `Info` / `Enable` / `Reload` / `Unload` / `ScanNew` | Engine Plugins | **P1a** 八方法；`plugins` 由 `List` 真探测 | **已绑定** |
| `AgentService` | `FetchToolDetail(..., subscribe=false)` | Conversation DetailRef | **P2a** `requestDetail` + `DetailPatch.truncated/totalBytes`；browser 恒 unavailable；stub 源本地 `requestDetail` 已由 Q2 接通 | **已绑定** |
| `ConfigService` | `ListModels` | Engine Model 组 | **P1b** `listModels()`（`include_disabled=true`）；`models` | **已绑定** |
| `ToolService` | `ListCommands` | 无 UI（catalog + node） | `listCommands?`；空 request；空 `name` / `agent` / `model` / `skill_source` 原样映射 | **已进 catalog** |
| `ToolService` | `GetCommandDef` | 无 UI（catalog + node） | `getCommandDef?`；空 `commandName` / `name` / `agent` / `model` / `template` / `mcp_server_id` / `mcp_prompt_name` / `skill_source` 原样映射 | **已进 catalog** |
| `FileService` | `ReadFile` | 无 UI（catalog + node） | `readFile?`；空 `path` / `sessionId` 原样上线；`start_line` / `end_line` / `max_bytes` 0 原样上线 | **已进 catalog** |
| `FileService` | `GetFileInfo` | 无 UI（catalog + node） | `getFileInfo?`；空 `path` / `sessionId` 原样上线；响应 `file` FileEntry | **已进 catalog** |
| `FileService` | `WriteFile` | 无 UI（catalog + node） | `writeFile?`；空 `path` / `sessionId` / `baseHash` 原样上线；空 `content` / `base_content` 原样上线 | **已进 catalog** |
| `FileService` | `ForceWriteFile` | 无 UI（catalog + node） | `forceWriteFile?`；空 `path` / `sessionId` 原样上线；空 `content` 原样上线；响应 `WriteFileResponse` | **已进 catalog** |
| `FileService` | `AgentMerge` | 无 UI（catalog + node） | `agentMerge?`；空 `sessionId` / `path` 原样上线；空 `base_content` / `current_content` / `user_content` 原样上线 | **已进 catalog** |
| `GitService` | `ReadGitSummary` | 无 UI（catalog + node） | `readGitSummary?`；空 `sessionId` 原样上线；响应 `supported`/`reason`/`branch`/`change_count` | **已进 catalog** |
| `GitService` | `ReadGitChanges` | 无 UI（catalog + node） | `readGitChanges?`；空 `sessionId` 原样上线；空 `path` / `old_path` / `kind` / `index_state` / `reason` / `branch` 原样映射 | **已进 catalog** |
| `GitService` | `ReadGitFileDiff` | 无 UI（catalog + node） | `readGitFileDiff?`；空 `sessionId` / `path` / `indexState` 原样上线；响应 `supported`/`reason`/`path`/`unified_diff` | **已进 catalog** |
| `GitService` | `WriteGitStagePaths` | 无 UI（catalog + node） | `writeGitStagePaths?`；空 `sessionId` 原样上线；空 `commands` / 空 `argv` 原样上线；响应 `supported`/`reason`/`success`/`error_message`/`exit_code`/`stdout` | **已进 catalog** |
| `GitService` | `WriteGitCommit` | 无 UI（catalog + node） | `writeGitCommit?`；空 `sessionId` 原样上线；空 `message` 原样上线；`sign_off` / `amend` false 原样上线；响应 `supported`/`reason`/`success`/`error_message`/`exit_code`/`stdout` | **已进 catalog** |
| `GitService` | `WriteGitApplyHunks` | 无 UI（catalog + node） | `writeGitApplyHunks?`；空 `sessionId` 原样上线；空 `argv` / 空 `patches` 原样上线；响应 `supported`/`reason`/`success`/`error_message`/`exit_code`/`stdout` | **已进 catalog** |
| `TokenUsageService` | `GetGlobalUsage` | 无 UI（catalog + node） | `getGlobalUsage?`；空 request；空 `currency` 原样映射 | **已进 catalog** |
| `MemoryService` | `Save` | 无 UI（catalog + node） | `saveMemory?`；空 `scope` / `content` / `category` 原样上线；响应 `success`/`message`/`file_path` | **已进 catalog** |
| `MemoryService` | `Search` | 无 UI（catalog + node） | `searchMemory?`；空 `scope` / `query` / `keywords` 原样上线；`limit` 0 原样上线；空 `category` / `filename` / `title` / `snippet` / `scope` 原样映射 | **已进 catalog** |
| `MemoryService` | `SearchDeep` | 无 UI（catalog + node） | `searchDeepMemory?`；空 `scope` / `query` / `keywords` / `categories` 原样上线；`limit` 0 原样上线；`include_content` false 原样上线；空 `category` / `filename` / `title` / `snippet` / `scope` 原样映射；空 `searched_categories` 原样映射 | **已进 catalog** |
| `MemoryService` | `Read` | 无 UI（catalog + node） | `readMemory?`；空 `scope` / `category` / `filename` / `section` / `mode` 原样上线；`forgot` false 原样上线；响应 `content`/`metadata` | **已进 catalog** |
| `MemoryService` | `List` | 无 UI（catalog + node） | `listMemory?`；空 `scope` / `category` 原样上线；空 `filename` / `title` / `category` 原样映射；`updated_at` / `file_count` 0 原样映射 | **已进 catalog** |
| `MemoryService` | `Delete` | 无 UI（catalog + node） | `deleteMemory?`；空 `scope` / `category` / `filename` 原样上线；响应 `success`/`message` | **已进 catalog** |
| `MemoryService` | `Reflect` | 无 UI（catalog + node） | `reflectMemory?`；空 `scope` / `categories` 原样上线；空 `type` / `category` / `filename` / `description` / `suggestion` / `summary` 原样映射 | **已进 catalog** |
| `MemoryService` | `Rebuild` | 无 UI（catalog + node） | `openRebuildMemoryStream?`；空 `scope` 原样上线；`dry_run` false 原样上线；帧 `phase`/`message`/`progress`/`files_processed`/`files_total` | **已进 catalog** |
| `MemoryService` | `Revert` | 无 UI（catalog + node） | `revertMemory?`；空 `scope` / `category` / `filename` 原样上线；`target_version` 0 原样上线；响应 `success`/`message`/`reverted_to_version` | **已进 catalog** |
| `MemoryService` | `History` | 无 UI（catalog + node） | `historyMemory?`；空 `scope` / `category` / `filename` 原样上线；`limit` 0 原样上线；空 `change_type` / `summary` / `author` 原样映射；`version` / `timestamp` 0 原样映射；响应 `changes[]` MemoryChangeEntry | **已进 catalog** |
| `FileTransferService` | `GetUploadProgress` | 无 UI（catalog + node） | `getUploadProgress?`；空 `transferId` / `sessionId` 原样上线；`bytes_received` 0 / 空 `partial_path` 原样映射；响应 `exists`/`bytes_received`/`partial_path` | **已进 catalog** |
| `FileTransferService` | `UploadAttachment` | 无 UI（catalog + node） | `openUploadAttachmentStream?`；空 `transferId` / `filename` / `sessionId` / `queueItemId` 原样上线；空 `chunk` 原样上线；响应 `success`/`file_path`/`checksum_sha256`/`error_message`/`error_code` | **已进 catalog** |
| `FileTransferService` | `DownloadAttachment` | 无 UI（catalog + node） | `openDownloadAttachmentStream?`；空 `filePath` / `sessionId` / `artifactId` 原样上线；`offset` / `maxBytes` 0 原样上线；空 `data` / `checksumSha256` 原样映射 | **已进 catalog** |
| `ContextVariableService` | `List` | 无 UI（catalog + node） | `listContextVariable?`；空 `sessionId` / `agentId` 原样上线；空 `name` / `updated_by` / `content_preview` 原样映射；`updated_at` 0 原样映射；响应 `current[]` / `inherited[]` ContextVariableEntrySummary | **已进 catalog** |
| `ContextVariableService` | `Read` | 无 UI（catalog + node） | `readContextVariable?`；空 `sessionId` / `name` / `agentId` 原样上线；空 `name` / `content` / `updated_by` 原样映射；`updated_at` 0 原样映射；响应 `entry` ContextVariableEntry | **已进 catalog** |
| `SystemService` | `Doctor` | 无 UI（catalog + node） | `doctor?`；空 request；空 `name` / `message` / `fix_hint` 原样映射；`passed` / `all_passed` false 原样映射；响应 `checks` DoctorCheck + `all_passed` | **已进 catalog** |
| `SystemService` | `Shutdown` | 无 UI（catalog + node） | `shutdown?`；`force` false / `grace_period_ms` 0 原样上线；空 `message` 原样映射；响应 `accepted`/`message` | **已进 catalog** |
| `DeviceService` | `ListDevices` | 无 UI（catalog + node） | `listDevices?`；空 request；空 `device_id` / `display_name` / `role` / `platform` 原样映射；`paired_at` / `last_seen_at` 0 原样映射；`active` false 原样映射；响应 `devices` DeviceInfo | **已进 catalog** |
| `DeviceService` | `PairApprove` | 无 UI（catalog + node） | `pairApprove?`；空 `pairing_code` / `display_name` / `role` 原样上线；空 `device_id` / `message` 原样映射；`success` false 原样映射；响应 `success`/`device_id`/`message`；reserved `device_token` 不映射 | **已进 catalog** |
| `DeviceService` | `Revoke` | 无 UI（catalog + node） | `revoke?`；空 `device_id` 原样上线；空 `message` 原样映射；`success` false 原样映射；响应 `success`/`message` | **已进 catalog** |
| `TriggerService` | `ListTriggers` | 无 UI（catalog + node） | `listTriggers?`；空 `scope` / `scope_id` / `type_filter` 原样上线；空 `trigger_id` / `name` / `type` / `prompt_template` / `pause_reason` / `cron_expression` 原样映射；`enabled` false 原样映射；`interval_ms` / `run_at_epoch_ms` 0 原样映射；响应 `triggers` TriggerDto + DeliveryTargetDto | **已进 catalog** |
| `ClipboardService` | `Write` | 无 UI（catalog + node） | `writeClipboard?`；空 `sessionId` / `agentId` / `label` / `content` / `filePath` / `url` 原样上线；空 `clip_id` 原样映射；typed `ClipboardEntryType` wire；响应 `clip_id` | **已进 catalog** |
| `ClipboardService` | `Read` | 无 UI（catalog + node） | `readClipboard?`；空 `sessionId` / `clipId` 原样上线；空 `clip_id` / `label` / `content` / `created_by` 原样映射；`created_at` 0 原样映射；typed `ClipboardEntryType` wire；响应 `entry` ClipboardEntry | **已进 catalog** |
| `ConfigService` | `SetPermissionPolicy` | 无 UI（catalog + node） | `setPermissionPolicy?`；空 `sessionId` / `toolName` 原样上线；typed `PermissionPolicy` wire | **已进 catalog** |
| `ConfigService` | `Get` | Provider 候选 | catalog + node `getConfig?`；`providerConfig` 仍固定 `UNSUPPORTED`（G-ENG-1） | **已进传输** |
| `ConfigService` | `Set` | Provider 候选写 | **已进** catalog + node unary `setConfig`；空 `key` / `value` / `scope` / `sessionId` 原样上线；**不**改 Engine Preferences。`providerConfig` 仍 `UNSUPPORTED`（G-ENG-1） | **已进 catalog** |
| `ConfigService` | `GetModelPreferences` | 会话级模型策略读 | **已进** catalog + node unary `getModelPreferences`；空 `sessionId` 原样上线；**不**改 Engine Preferences / Composer | **已进 catalog** |
| `ConfigService` | `SetModelPreferences` | 会话模型策略写 | **已进** catalog + node unary `setModelPreferences`；空 `sessionId` / `maxCost` / `minSpeed` / `strategy` 原样上线；`minLevel` 0 原样上线；**不**改 Conversation / Engine Preferences。≠ `GetModelPreferences` / `ResolveModel` / `SwitchModel` / `ListModels` / `Get` / `Set` | **已进 catalog** |
| `ConfigService` | `ResolveModel` | 无 UI（catalog + node） | `resolveModel?`；空 `sessionId` / `type` 原样上线；响应 `selected`/`candidates`/`filtered` | **已进 catalog** |
| `ConfigService` | `Watch` | Provider 候选 | **已进** catalog + node server-stream `openWatchConfigStream`；空 `keys` 原样上线；**不**改 Engine Preferences。`providerConfig` 仍 `UNSUPPORTED`（G-ENG-1） | **已进 catalog** |
| `FileService` | `ListFiles` | 无 UI（catalog + node） | `listFiles?`；空 `path` / `sessionId` / `pattern` 原样上线；`recursive` false / `maxResults` 0 原样上线；响应 `entries`/`total` | **已进 catalog** |
| L2 compact 事件 | `branch_reason` / `CompactedSpanBlock` / `RangeReplaced(COMPACT)` | 轨迹 compacted 归因 | **P2b** host demux → `ItemAttribution.branchReason:'compact'` + `compacted{anchorTurnId,foldedLeafTurnId,compactBranchTurnId,summary?}`；browser 不产出 | **已绑定** |

**不存在的 RPC：** Provider CRUD；Instructions Rules gRPC；`ListHookPoints`；Skill 独立 Create；Agent profile `model.json` 写路径。会话级 `SwitchModel` / `ResolveModel` / `Watch` 已进 catalog + node，不进 Engine 页。`SubscribeToolDetail` / `FetchToolUsageDetail` / `Compact` 已进 catalog + node，Conversation / M7 手动触发仍不接。

## 2. 能力探测

UA 侧 `EngineSettingsCapabilities` / `CapabilitySupport`：`SUPPORTED | UNSUPPORTED | UNKNOWN` + `reason`。

| 键 | 来源 | IDE 处理（@ HEAD） |
|----|------|-------------------|
| `skills` | Connect 广告 `ToolService.ListSkills` + 运行时 probe | `grpcCapabilityProbe.ts` → `getCapabilitySnapshot()`；Engine Skills 节消费 |
| `mcp` | Connect 广告 + `McpService.ListMcpServers` probe | 同上 → MCP 节 |
| `agentProfiles` | Connect 广告 + `AgentService.ListAgentProfiles` probe | 同上 → Agents 节（IDE 三态，非 UA Boolean 直出） |
| `tools` | Connect 广告 + `ToolService.ListTools` probe | 同上 → Tools 节 |
| `agentTree` · `team` | IDE 推导（m6 §11） | `AgentService.Tree` / `TeamService.MemberStatus` probe |
| `mcpRuntime`（**M7 P1a**） | IDE 推导 | Connect 广告 `McpService.GetMcpServerStatuses` + probe；与 `mcp` 分开 |
| `plugins` | UA 侧键 | **P1a 已探测** `PluginService.List`；未广告 / `UNIMPLEMENTED` → 诚实 `UNSUPPORTED` |
| `models`（**M7 P1b**） | IDE 推导 | Connect 广告 `ConfigService.ListModels` + probe |
| `providerConfig`（**M7 P1b**） | IDE 推导 | 固定 `UNSUPPORTED`，reason「Provider 配置键合同未定」 |
| `globalRules` · `projectRules` | UA 侧键 / IDE 矩阵 | 固定 `UNSUPPORTED`（G-ENG-2） |
| `hooksMetadata` | UA 无握手字段 | 固定 `UNSUPPORTED` |
| IDE 传输失败 | 非 UA 概念 | list RPC `catch` → 传输失败文案，**不得**映射为 UNSUPPORTED 或「0 条」 |

**Web 形态（P0 已落）：** `workbench.web.main.ts` 以 `registerSingleton` 注册三者（`platform/universeAgent/browser/`）。Hub 的 electron-browser 注册只在 `workbench.desktop.main.ts`。后续 P 切片新增键或方法须同一提交同步 browser 断连。

Connect 后 `probeEngineCapabilities`：**仅**广告了 method 且 probe 非 `UNIMPLEMENTED` 才升 `SUPPORTED`。无连接：catalog 节 **disconnected**（E2-1 起节可达、零条目零写按钮），不用 Stub 填 `UNKNOWN`。

## 3. 引擎磁盘布局（进程内，IDE 断连时禁止扫）

| 面 | 路径 |
|----|------|
| AgentHome | `~/.universe-agent`（可被测试覆盖） |
| Skills | BUNDLED `resources/skills/{name}/SKILL.md`；USER `{AgentHome}/skills/`；PROJECT `{workDir}/.universe-agent/skills/` |
| Agents | `{AgentHome}/agents/{id}/`、`{workDir}/.universe-agent/agents/{id}/` |
| Rules | `{AgentHome}/rules/`、`{workDir}/.universe-agent/rules/` |
| Hooks | `{AgentHome}/hooks.json`；插件 `{AgentHome}/plugins/` |
| MCP | ConfigStore `mcp.servers`（`config.json`）；项目 `{workDir}/.universe-agent/mcp-servers.json` |

只有 **Engine-backed** 且能力 `SUPPORTED` 时，这些路径才经引擎的 Local 文件面被读写。

## 4. 已登记的协议缺口（由引擎仓 / 后续切片补）

| 缺口 | 阻塞 | 备注 |
|------|------|------|
| **G-NAV-1** | `SessionSummary` / `SessionInfoResponse` 无 `work_dir` | Projects 按项目分组；IDE 未补前用 connection `workDir` 单组。见 [navigator-engine-segments §7](../../../dev/plans/navigator-engine-segments.md) |
| **G-NAV-2** | 无 `ListTeams(session_id)`；`team_id` 只在不落库事件出现 | 重连后 `liveTeamId` 空 → 节标题省略团队状态；Members/Tasks 仍可用 |
| **G-REV-1** | 持久化历史无归一化 `file_mutation`（L3 不落库；L2 路径在 `arguments_json`） | 归因 / reviewNav 仅限本连接（含重播种）。见 [sources-review-progress §6](../../../dev/plans/sources-review-progress.md) |
| **G-ENG-1** Provider 配置键合同 | Engine 页 Provider 组的读/写/测试 | 引擎只有通用 `ConfigService.Get/Set` 与 `TestModelProfile`，没有 Provider 列表 / 凭据已配置查询；需引擎仓给出 provider 相关 config key 与"已配置但不回显"语义。闭合前 Provider 组 unsupported |
| **G-ENG-2** Rules Remote gRPC | Engine 页 Rules（Instructions） | `RulesBridge` 仅 Desktop 进程内；IDE 一律 unsupported。原「Rules Remote gRPC」行合并至此 |
| **G-ENG-3** `ListHookPoints`（或握手带版本化点位表） | Engine 页 Hooks「来自引擎」 | 原行改编号；闭合前 Hooks 节只显示 unsupported |
| **G-ENG-4** Agent profile `model.json` 写路径 | Agents 节 Model 子 tab | `SaveAgentProfileRequest.AgentProfileProto` 无 `model` / `modelType` / `maxTurns`，引擎 mapper 写死 null；Model 子 tab 在引擎补字段前只能是 unsupported |
| **G-CORE-1** session-core `CoreIntent` 无 `sessionId`；`takeIntents()` 为全局队列 | 多会话并发时 intent 归属只能靠「`core.post` 后立刻 drain」不变量 | **来源 vendored session-core，非 gRPC**。F2 @ `917a7f8d` 以宿主 `postAndDrain` 门禁守住该不变量并对未实现 intent 计 `intent.unhandled`。闭合建议：上游在 `emitIntent` 盖 `sessionId`，或提供 `takeIntents(sessionId)`；闭合后宿主归属退化为只读 `intent.sessionId`。见 [session-view-frame-fanout](../../../dev/plans/session-view-frame-fanout.md) |
| **G-CONV-1** compact 事实进入 session-core | 轨迹 `compacted` 记录 | **P2b 生产者已落** + **Q3 已消费**：host 只从 L2 取行身份（`branch_reason` compact / `EnvelopeRangeReplaced(reason=COMPACT)` 单独成立）→ `ItemAttribution.branchReason:'compact'` + `compacted{…}`。轨迹 `projectSnapshotToTrajectory` 只据此 emit；无 attribution 则零行。`ContextCompactedEvent` 仍非显示源、不订阅 |
| 独立 CreateSkill RPC（或等价新建 UI） | E1 新建技能 | Skill **新建** UI 已落 @ `e6167c45`（写文件后 `ListSkills` 刷新）；独立 RPC 仍缺 |
| `SaveSkillContent` node gRPC 传输 | —（**已闭** @ `45fa7a35`/`040c823d`） | `grpcClient` / `grpcTransport` / `universeAgentConnectionService` 动态绑定；单测 `universeAgentConnection.test.ts` |
| Agent profile `tools.json` / `model.json` 独立 UI | Engine 页目录三件套附件编辑 | `AGENTS.md` 正文 @ `9419f583` 已落；`tools.json` 经 Tools 节 checkbox；`model.json` 仍无独立编辑器 |
| `plugins` IDE probe | Plugins Engine 节 | **M7 P1a 已闭**（`PluginService.List` 探测）；`globalRules` 无 RPC，见 G-ENG-2 |

## 5. Conversation 会话面（A1/A2 @ HEAD 回填）

[IConversationRosterService](../../systems/conversation/stub-and-fixtures.md) 需要的引擎面；adapter 落层 [ADR-003](../../../dev/decisions/003-engine-adapter-boundary.md)（`platform/universeAgent`）；时间线 / 发送 / 权限流形态 [conversation-stream-timeline](../../../dev/plans/conversation-stream-timeline.md)。下表「HEAD 事实」来自 m6-engine-wave §3 与 `platform/universeAgent` + `ConversationEngineRosterService` 合入代码：

| IDE 需要 | 对应 stub 方法 | HEAD 事实 |
|----------|----------------|-----------|
| 会话枚举 / 创建 / 删除 | `getSessions` … `deleteSession` | `SessionService.List` / `Create` / `Delete`；`work_dir` 过滤随 Connect。已连接时首次 `List` 完成前 roster **不**含 stub 种子行。**ConversationEngineRosterService.createSession** 接通后转发 `SessionService.Create`，用引擎返回的 `sessionId` 入目录并切活动会话（同步返回 `''`，不把旧 active / stub `untitled` 冒充新会话）；空 id 忽略；断连缓存不发 unary、不造 stub 种子。传输 `getSessionInfo?` **已进** `SessionService.Info` catalog + node（空 `sessionId` 原样上线）；传输 `resumeSession?` **已进** `SessionService.Resume` catalog + node（空 `sessionId` 原样上线）；传输 `prewarmSessions?` **已进** `SessionService.Prewarm` catalog + node（空 `sessionIds` 原样上线）；`shelveSession?` **已进** `SessionService.Shelve` catalog + node（空 `sessionId` 原样上线）；传输 `unshelveSession?` **已进** `SessionService.Unshelve` catalog + node（空 `sessionId` 原样上线）；`purgeSession?` **已进** `SessionService.Purge` catalog + node（空 `sessionId` 原样上线）；传输 `exportSession?` **已进** `SessionService.Export` catalog + node（空 `sessionId` / `format` 原样上线）；传输 `resolveTurn?` **已进** `SessionService.ResolveTurn` catalog + node（空 `sessionId` / `turnId` / `currentLeafTurnId` 原样上线）；传输 `getAgentStatus?` **已进** `AgentService.Status` catalog + node（空 `sessionId` / `agentId` 原样上线）；传输 `getTodo?` **已进** `AgentService.Todo` catalog + node（空 `sessionId` / `agentId` 原样上线）；传输 `compact?` **已进** `AgentService.Compact` catalog + node（空 `sessionId` / `agentId` 原样上线）；传输 `resolveAnchor?` **已进** `SessionService.ResolveAnchor` catalog + node（空 `sessionId` / `envelopeId` / `currentLeafTurnId` 原样上线）；传输 `getUsage?` **已进** `AgentService.Usage` catalog + node（空 `sessionId` / `agentId` 原样上线）；传输 `getAgentHistory?` **已进** `AgentService.History` catalog + node（空 `sessionId` / `agentId` 原样上线）；传输 `pauseAgent?` **已进** `AgentService.Pause` catalog + node（空 `sessionId` / `agentId` 原样上线）；传输 `openResumeStream?` **已进** `AgentService.Resume` catalog + node server-stream（空 `sessionId` / `agentId` 原样上线）；传输 `back?` **已进** `AgentService.Back` catalog + node（空 `sessionId` / `agentId` / `operationId` 原样上线）；传输 `prune?` **已进** `AgentService.Prune` catalog + node（空 `sessionId` / `agentId` 原样上线）；传输 `resetAgent?` **已进** `AgentService.Reset` catalog + node（空 `sessionId` / `agentId` 原样上线）；传输 `branch?` **已进** `AgentService.Branch` catalog + node（空 `sessionId` / `agentId` / `turnId` 原样上线）；传输 `suspendLoop?` **已进** `AgentService.SuspendLoop` catalog + node（空 `sessionId` / `agentId` 原样上线）；传输 `resumeLoop?` **已进** `AgentService.ResumeLoop` catalog + node（空 `sessionId` / `agentId` 原样上线）；传输 `stopLoop?` **已进** `AgentService.StopLoop` catalog + node（空 `sessionId` / `agentId` / `detail` 原样上线）；传输 `runToolInBackground?` **已进** `AgentService.RunToolInBackground` catalog + node（空 `sessionId` / `agentId` / `toolCallId` 原样上线）；传输 `stopShellTask?` **已进** `AgentService.StopShellTask` catalog + node（空 `sessionId` / `taskId` 原样上线）；传输 `sendShellSessionClientControl?` **已进** `AgentService.SendShellSessionClientControl` catalog + node（空 `sessionId` / `toolCallId` / `refId` / `controlPayloadJson` 原样上线）；传输 `fetchToolUsageDetail?` **已进** `AgentService.FetchToolUsageDetail` catalog + node（空 `sessionId` / `toolCallId` 原样上线）；传输 `fireTriggerWebhook?` **已进** `AgentService.FireTriggerWebhook` catalog + node（空 `sessionId` / `triggerId` / `payloadJson` 原样上线）；传输 `switchWorkDir?` **已进** `AgentService.SwitchWorkDir` catalog + node（空 `sessionId` / `agentId` / `newWorkDir` 原样上线）；传输 `testModelProfile?` **已进** `AgentService.TestModelProfile` catalog + node（空 `providerId` / `modelId` / `apiKey` / `baseUrl` / `protocol` 原样上线）；传输 `insertQueueItem?` **已进** `AgentService.InsertQueueItem` catalog + node（空 `sessionId` / `opId` / `clientMessageId` / `beforeItemId` 原样上线）；传输 `reorderQueue?` **已进** `AgentService.ReorderQueue` catalog + node（空 `sessionId` / `opId` / `itemIds` 原样上线）；传输 `deleteQueueItem?` **已进** `AgentService.DeleteQueueItem` catalog + node（空 `sessionId` / `itemId` / `opId` 原样上线）；传输 `retryQueueItem?` **已进** `AgentService.RetryQueueItem` catalog + node（空 `sessionId` / `itemId` / `opId` 原样上线）；传输 `retryAllFailed?` **已进** `AgentService.RetryAllFailed` catalog + node（空 `sessionId` / `opId` 原样上线）；传输 `retryQueueItemUpload?` **已进** `AgentService.RetryQueueItemUpload` catalog + node（空 `sessionId` / `itemId` / `opId` 原样上线）；传输 `pinQueueItem?` **已进** `AgentService.PinQueueItem` catalog + node（空 `sessionId` / `itemId` / `opId` 原样上线）；传输 `setQueueItemLocked?` **已进** `AgentService.SetQueueItemLocked` catalog + node（空 `sessionId` / `itemId` / `opId` 原样上线）；传输 `injectQueueItem?` **已进** `AgentService.InjectQueueItem` catalog + node（空 `sessionId` / `itemId` / `opId` 原样上线）；传输 `setQueueItemForkAnchor?` **已进** `AgentService.SetQueueItemForkAnchor` catalog + node（空 `sessionId` / `itemId` / `opId` 原样上线）；传输 `chatSync?` **已进** `AgentService.ChatSync` catalog + node（空 `sessionId` / `agentId` / `idempotencyKey` / `lastKnownMessageIds` / `sessionInput.messageId` 原样上线）；传输 `syncInputDelivery?` **已进** `AgentService.SyncInputDelivery` catalog + node（空 `sessionId` / `lastKnownMessageIds` 原样上线）；传输 `syncPermissionRule?` **已进** `PermissionService.SyncPermissionRule` catalog + node（空 `sessionId` / `toolName` / `scope` / `reason` 原样上线）；传输 `getSessionRules?` **已进** `PermissionService.GetSessionRules` catalog + node（空 `sessionId` 原样上线）；传输 `setPermissionMode?` **已进** `PermissionService.SetPermissionMode` catalog + node（空 `sessionId` 原样上线）；传输 `clearSessionDemoFake?` **已进** `AgentService.ClearSessionDemoFake` catalog + node（空 `sessionId` 原样上线）；传输 `openRegenerateStream?` **已进** `AgentService.Regenerate` catalog + node server-stream（空 `sessionId` / `agentId` / `turnId` / `messageId` 原样上线）；无 roster / SessionBar 消费 || 切换 / 重命名 | `switchSession` / `renameSession` | 切换 = 客户端 `activeSessionId` 投影（**无** `SwitchSession` RPC）。传输 `IUniverseAgentConnection.renameSession` **已接** `AgentService.Rename`。**ConversationEngineRosterService** 接通后 `renameSession` 转发该 unary 并更新引擎目录标题；未接通 / 从未连过仍走 stub 本地标题 |
| 回合流（用户 / 助手 / thinking / tool / …） | `getTurns`、`onDidChangeSession`、`deleteTurn`、`updateUserTurnText` | `SessionService.GetHistory`（`cursor_seq`）+ `SessionEventStream` → session-core fold → `ViewFrame`；renderer 经 `IUniverseAgentSessionView.acquireLease` + **`onDynamicDidApplyFrame(leaseId)`**（F1 @ `c37bbc6e`：宿主 per-lease 事件；订阅前该 lease 的帧入 `pending`，首个 listener 按序 flush，首帧为 baseline；未知 / 已释放 id → `Event.None`）。已删除全局 `onDidApplyFrame`；渲染端**不**再按 leaseId 过滤全窗广播。传输 `IUniverseAgentConnection.deleteMessage?` **已进** `AgentService.DeleteMessage`；`editMessage?` **已进** `AgentService.EditMessage`。**ConversationEngineRosterService.deleteTurn** 接通后转发 DeleteMessage（空 id / 未知 session / 断连缓存 / 无 hook 不发；未指定 agent 用末条 streaming 否则 `root`；不本地删引擎回合）。**updateUserTurnText** 接通后转发 EditMessage（空 `turnId` / 空正文 / 未知 id / 断连缓存 / 无 hook 不发；未指定 agent 用末条 streaming 否则 `root`）。时间线 `turnEdit` 保存走 EditMessage。未接通 / stub 仍本地删 / 本地改。传输 `listSnapshots?` / `listLoopSnapshots?` / `createSnapshot?` / `restoreSnapshot?` / `deleteSnapshot?` **已进** catalog + node；Conversation SessionBar Snapshots 列表消费 `listSnapshots?`（断连 / 无 hook / 空 sessionId 不发）；overlay Restore 直呼 `restoreSnapshot?`（空 snapshotId / 断连 / 无 hook 不发；成功后 overlay 保持打开再拉 `listSnapshots`，失败 / 未发不刷新）；行 Delete 转发 `deleteSnapshot?`（空 `snapshotId` / 未知 session / 断连 / 无 hook 不发；确认后才发）；`listLoopSnapshots?` 仅传输、SessionBar / overlay 不消费；**ConversationEngineRosterService.createSnapshot** 接通后转发 CreateSnapshot（空 `sessionId` / 未知 session / 断连缓存 / 无 hook 不发；空 title 原样上线）；SessionBar History 仍回合索引，不把 snapshot 列表 / 新建 / 恢复 / 删除 checkpoint 冒充历史 |
| 轨迹记录 | `getTrajectoryRecords(sessionId, { filterAgentId? }?)` | HEAD：`projectSnapshotToTrajectory(snapshot, attribution, details, options)` 从 lease/帧源投影；stub 仅 `untitled` 且未连接时 ∪ fixture extras（**无** compacted 伪造行）；UA 会话**不** merge fixture；**P2a** `requestDetail` / `FetchToolDetail` 通道已接通（renderer 帧源 upsert `outcome.content`；stub 本地 `requestDetail`）；**P2b** 已投影 `ItemAttribution.compacted`（browser 不产出）；**Q3** 消费 attribution emit `compacted` 行（未投影则零行）。Overview 瀑布 Deferred。活 Event fold 全文仍 M6-D / PRD-008 |
| 权限请求 / 回执 | `resolveConfirmation`、`countPendingConfirmations`、`respondQuestion` | 流内 L4 `permission_request` → `pendingActions`。传输 `IUniverseAgentConnection.respondPermission?` **已进** `PermissionService.Respond`。**ConversationEngineRosterService.resolveConfirmation** 接通后转发 unary（空 id / 未知 session / 断连缓存 / 无 hook 不发；不双写 Chat 臂）。未接通 / stub 仍 `permissionRespond`。传输 `respondQuestion?` **已进** `AgentService.RespondQuestion` catalog + node unary。**ConversationEngineRosterService.respondQuestion** 接通后转发 unary（空 `questionId` / 未知 session / 断连缓存 / 无 hook 不发；不双写 Chat 臂）。时间线问题座接通后走该转发。未接通 / stub 仍 `questionRespond` |
| Inbox Stop | `cancelGeneration(sessionId, agentId?)` | 传输 `IUniverseAgentConnection.cancelGeneration` **已接** `AgentService.Cancel`。**ConversationEngineRosterService** 接通后转发 unary（未指定 agent 用末条 streaming 否则 `root`）；未接通 / 断连缓存 / 从未连过 stub 返回 false。Inbox Stop 已连接且时间线有 `streaming` 行才启用，点击走 roster。单工具 `cancelToolCall(sessionId, { toolCallId, agentId? })` 接通后转发 `AgentService.CancelToolCall`；时间线执行中工具行「Cancel Tool」走 roster（未指定 agent 用末条 streaming 否则 `root`） |
| MessageQueue | `getMessageQueueState` 与 Enqueue + 五个操作 | 传输 `IUniverseAgentConnection` **已接** `AgentService.EnqueueQueueItem` 族七 unary。**ConversationEngineRosterService** 接通后转发 Enqueue / Pause / Resume / Clear / Hold / Release / Edit（未知 id / 空 item / 空正文 / 断连缓存不发）。传输 `insertQueueItem?` **已进** `AgentService.InsertQueueItem` catalog + node（空 `sessionId` / `opId` / `clientMessageId` / `beforeItemId` 原样上线）；传输 `reorderQueue?` **已进** `AgentService.ReorderQueue` catalog + node（空 `sessionId` / `opId` / `itemIds` 原样上线）；传输 `deleteQueueItem?` **已进** `AgentService.DeleteQueueItem` catalog + node（空 `sessionId` / `itemId` / `opId` 原样上线）；传输 `retryQueueItem?` **已进** `AgentService.RetryQueueItem` catalog + node（空 `sessionId` / `itemId` / `opId` 原样上线）；传输 `retryAllFailed?` **已进** `AgentService.RetryAllFailed` catalog + node（空 `sessionId` / `opId` 原样上线）；传输 `retryQueueItemUpload?` **已进** `AgentService.RetryQueueItemUpload` catalog + node（空 `sessionId` / `itemId` / `opId` 原样上线）；传输 `pinQueueItem?` **已进** `AgentService.PinQueueItem` catalog + node（空 `sessionId` / `itemId` / `opId` 原样上线）；传输 `setQueueItemLocked?` **已进** `AgentService.SetQueueItemLocked` catalog + node（空 `sessionId` / `itemId` / `opId` 原样上线）；传输 `injectQueueItem?` **已进** `AgentService.InjectQueueItem` catalog + node（空 `sessionId` / `itemId` / `opId` 原样上线）；传输 `setQueueItemForkAnchor?` **已进** `AgentService.SetQueueItemForkAnchor` catalog + node（空 `sessionId` / `itemId` / `opId` 原样上线）；roster / Inbox **不**转发。无 GetQueue，`getMessageQueueState` 接通 / 断连缓存诚实空，不把 fixture 冒充引擎队列。Composer 发送仍走 `submitInput` |
| Inbox Goal | `setSessionGoal` / `cancelSessionGoal` / `getSessionGoal` | 传输 `IUniverseAgentConnection.setSessionGoal?` / `cancelSessionGoal?` **已进** `PermissionService` catalog + node unary。**ConversationEngineRosterService** 接通后转发（空/未变/未知 id / 断连缓存 / 无 hook 不发）。Inbox Goal 接通后启用；非空确认 set、已有本地目标时清空 cancel。无 `GetSessionGoal`，`getSessionGoal` 只记本机上次成功 set |
| AutoDrive / Task 列表 | `getAutoDriveTasks` | Inbox **无**任务列表 RPC（`Team.TaskList` 仍只给 Navigator）。**ConversationEngineRosterService** 接通 / 断连缓存诚实空，不把 fixture 冒充引擎任务；stub / 从未连过仍 fixture。与 Inbox Goal 分家 |
| fork / 子代理 catalog | `IConversationSessionChatService` + roster `onDidChangeLiveAgentTree` | GC-4 @ `22ce3013`：roster 观察活动会话 lease 的 **`liveAgentTree` 唯一源**预同步 catalog（`chatId` ≡ `agent_id`，根不登记）；`AgentService.Tree` 仍 host 首拉 + 事件再拉（§11）。传输 `IUniverseAgentConnection.forkAgent?` **已进** `AgentService.Fork` catalog + node unary。**ConversationEngineRosterService.forkSubAgent** 接通后转发；接通后用户 Fork 动作走该 unary，不造本地 catalog / Fork tab。未接通仍 `registerForkChat`。传输 `killAgent?` **已进** `AgentService.Kill` catalog + node unary。**ConversationEngineRosterService.killSubAgent** 接通后转发（空 `agentId` 原样上线、不默认 `root`；省略用末条 streaming 否则空串；未知 id / 断连缓存 / 无 hook 不发）。接通后用户 Kill 动作走该 unary，不造本地 catalog 变更。未接通仍 no-op。Navigator 仍只读 |
| `visualize` 工具输出 | fixture / tool turn | 仍 stub / 本地 fixture；引擎工具名待 T4+ |
| 引擎连接态 | `isEngineConnected` | `SystemService.Connect` 成功 + 非空 `session_token` + 活 channel；pairing-pending → false。StatusBar 文案 = `IUniverseAgentConnection.getConnectionPhase()`（H4b）；connected 芯片 command → Engine pane，否则 Connection |
| 配对确认 / 取消 / 探测 | `IUniverseAgentConnection` | **GC-1b / GC-3** @ `a551fdef` / `f74e151f`：`confirmPairing()` 写 trust 并完成拨号（`recover_trust` 时调 `confirmRecoverTrust`）；`cancelPairing()` 不写 trust、断开 pairing-pending；`probeConnectionProfile(profileId)` 对 active profile 建独立探测链只调 `GetAuthNonce`，**不** `Connect`、不改 `ConnectionPhase`。Web stub 三方法均为 `unsupported_environment`。**recoverTrust UI**：`connectProfile` 可返回 `recoverTrust` + `leafSha256Hex`；pane 独立 identity+fingerprint 对话框（Desktop ADR-031；0× SAS）后再 `confirmPairing` |
| Route / AgentProfile / Model / Permission / Tools 选项 | Composer 各下拉 | 无引擎 = 诚实空；Engine 页 Agents **list-only** 已接，Composer 下拉 **仍**待 profile/策略切片 |
| 本地会话缓存与引擎权威切换 | PRD-017 | D13 @ HEAD：`conversation.roster.v1` @ `StorageScope.WORKSPACE` + `StorageTarget.MACHINE`；引擎接通后本地存 stub + UA 断连快照（`source` 字段） |

## 7. Engine catalog 消费面（九节 @ HEAD；写路径自 `f49615a1`）

系统规格：[engine-catalog](../../systems/workbench/engine-catalog.md)。代码锚：`enginePreferencesPane.ts` · `engineCatalog.ts` · `engineOverviewSection.ts` · `engineProviderModelSection.ts` · `engineSkillsSection.ts` · `engineAgentsSection.ts` · `engineRulesSection.ts` · `engineHooksSection.ts` · `engineMcpSection.ts` · `enginePluginsSection.ts` · `engineToolsSection.ts`。

| 能力键 | List RPC | Toggle / 写 | Engine 页 @ HEAD |
|--------|----------|--------------|------------------|
| （Overview 聚合） | snapshot + 可选 `ListModels` | 无 | Connection / workDir / Transport / capability 摘要；GC-6 Model 计数；Provider 行 G-ENG-1 前省略 |
| `providerConfig` | **无**（G-ENG-1） | 无 | Provider 组 unsupported 完整态；零输入 |
| `models` | `ListModels`（`include_disabled=true`） | 无 | Provider & Model 节只读注册表；Overview 计数 |
| `skills` | `ListSkills` · `SkillInfo` | `SetSkillEnabled` · `SaveSkillContent`（node 传输 @ `45fa7a35`/`040c823d`；`saveSkillContent?` 契约 @ `f3f2d366`） | 分组 list + 开关 + 正文 textarea（Save 在 `saveSkillContent?` 存在且可写时展示） |
| `agentProfiles` | `ListAgentProfiles` | `SaveAgentProfile` / `DeleteAgentProfile` / `ResetAgentProfile` | 分组 list + New/Delete/Reset + **`AGENTS.md` 全文编辑器** @ `9419f583` |
| `globalRules` / `projectRules` | **无**（G-ENG-2） | 无 | Rules 节壳；已连接 unsupported |
| `hooksMetadata` | **无**（G-ENG-3） | 无 | Hooks 节壳；已连接 unsupported |
| `mcp` | `ListMcpServers` | `ToggleMcpServer` · `AddMcpServer` / `UpdateMcpServer` / `RemoveMcpServer` | Definitions tab：分组 list + checkbox + CRUD 工具栏 |
| `mcpRuntime` | `GetMcpServerStatuses` / `GetMcpServerTools` | Refresh | Runtime tab（P1a） |
| `plugins` | `PluginService.List` | `Enable` / `Reload` / `Unload` / `ScanNew` | 列表 + 启停/重载/扫描；无 marketplace |
| `tools` | `ListTools` + 选中行 `ToolInfo` | `SaveAgentProfile`（profile `tools.json`） | 目录 + profile 下拉 + 启用 checkbox + 只读详情（不画 schema 编辑器） |

断连 / unsupported / loading / failed / empty / ready 六态见 engine-catalog §2；**禁止** Copilot 盘或 vscode `IMcpService` 顶替 UA 定义面。

## 11. Navigator / Review host 面（m6 §11 @ HEAD）

§1 已列 RPC；本节只记 **renderer 不直接见的 host join 与 IDE 事件**（细节 [m6-engine-wave §11](../../../dev/plans/m6-engine-wave.md)）。

| 面 | 生产者 | 消费者 |
|----|--------|--------|
| `AgentService.Tree` → `agentTreeBound` | M6-A2 host（必拉 + 事件再拉；`requestAgentTreeRefresh`） | Navigator Agents Hierarchy（lease `liveAgentTree`）；Team manager 发现 |
| `team.*` unary + `onDidChangeTeamRuntime` | A1 传输 + A2 demux L3 `multi_agent_status` | Navigator Team Members/Tasks |
| `tool_call_id` join 表 + `onDidFileMutation` + turn settle | A2 host（`ToolCallLifecycleEvent` + L3 snapshot + `assistant_turn_id` settle） | Sources Review 归因 chip；`ItemAttribution.toolCallId` → `conversation.revealItem` |
| `onDidChangeTeamRuntime` / `requestAgentTreeRefresh` | connection（A1 类型；A2 实现） | Navigator Team / Agents Refresh |
| `isAgentTreeFetchFailed`（D21） | connection：`AgentService.Tree` 非 UNIMPLEMENTED 失败置位、成功清除；经 `onDidChangeConnection` 通知 | Hierarchy / Team 共用空态失败 note（**非** lease 快照字段；**非** `transport === 'failed'`） |

协议缺口 **G-NAV-1 / G-NAV-2 / G-REV-1** 见 §4。

## 6. 维护

- 外仓 RPC 改名 / 新增时改本页对应行并更新 `updated`；不在 plan 里重复枚举。
- 本页不得出现 `src/vs/` 类型名充当引擎类型；IDE 侧行为规格在 [engine-catalog](../../systems/workbench/engine-catalog.md) 与 [systems/conversation](../../systems/conversation/INDEX.md)。
- 写 RPC 合入后须同步 §1 行、§4 缺口、§7 表与 [engine-catalog §3–§4](../../systems/workbench/engine-catalog.md)；**不得**把 PRD-008 升 `implemented` 或写隔离 profile 冒烟已通过。
