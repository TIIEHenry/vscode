---
title: "Loop Deferred Gaps"
type: progress
status: accepted
phase: N/A
created: 2026-08-30
updated: 2026-09-17
| D24 | P2 | **其余 unary/stream 仍 `makeUnaryClient` + `JSON.stringify`**。**A 槽 `d24-permission-leftover-bytes` @ 2026-09-16 已转 bytes**（未关本行）：`Permission.SyncPermissionRule`（`tool_name`=2 `scope`=3 `action`=4 `reason`=5；响应 `rule_id`=2）、`PromotePermissionRule`（`tool_name`=1 `scope`=2 `action`=3）、`GetSessionRules`（`session_id`=1；`SessionRule` 1–8 不解未知 field）。`listTools`/`listSkills` 本刀不碰。**本刀盘点（工位 A `live-rpc-bytes` @ 2026-09-07）**：接通后已 bytes = `GetAuthNonce` / DeviceAuth `Connect` / `Session.List`（默认 ALL）/ `Create`（field 4 `client_session_id`）/ `Resume` / `GetHistory` / `SessionEventStream` / `Agent.Chat` / `openChatStream` / `ListModels` / `ListAgents` / `ListAgentProfiles` / `ListDevices` / `Agent.Tree`。**本刀已转**：`probeRpc` 空 proto（`encodeProbeRpcRequest`），不再 `Buffer.from('{}')`；接通后 capability probe（`probeEngineCapabilities` + `saveSkillContent` 绑定）打到引擎。**A 槽 `d24-session-unary-bytes` @ 2026-09-16 已转 bytes**（未关本行）：`Session.Delete`（`session_id`=1 同 Info）、`Agent.Rename`（`title`=2）、`Agent.Cancel`（`agent_id`=2 同 Chat）、`Permission.SetPermissionMode`（`mode`=2）、`Config.SwitchModel`（oneof `model_type`=10 / `model_id`=11）；未发明号；`listTools`/`listSkills` 本刀不碰。**A 槽 `d24-next-known-unaries` @ 2026-09-16 已转 bytes**（未关本行）：`Agent.ListSnapshots`（`session_id`=1；`SessionSnapshotInfo` 1–9）、`CreateSnapshot`（`title`=2 `description`=3）、`RestoreSnapshot`/`DeleteSnapshot`（`snapshot_id`=2）；未发明号；`listTools`/`listSkills` 本刀不碰。**A 槽 `d24-next-known-unaries-2` @ 2026-09-16 已转 bytes**（未关本行）：`Agent.CancelToolCall`（`tool_call_id`=3）、`Permission.SetSessionGoal`（`goal`=2）、`Team.MemberStatus`/`TaskList`（`agent_id`=2）、`Team.TeamInfo`（`team_id`=3）、`Agent.FetchToolDetail`（`tool_call_id`=2 `detail_kind`=3 `ref_id`=4；`subscribe` 默认 false 省略 10）；未发明号；`listTools`/`listSkills` 本刀不碰。**A 槽 `d24-next-known-unaries-3` @ 2026-09-16 已转 bytes**（未关本行）：`Permission.CancelSessionGoal`（`session_id`=1；响应同 Resume）、`Permission.Respond`（`request_id`=2 `granted`=3 `metadata_json`=4；`granted` false 省略）、`Agent.Fork`（空 parent 仍 `root`）、`Agent.Kill`（`force` false 省略；空 agent 不默认 root）、`Agent.DeleteMessage`/`EditMessage`（空 agent 仍 `root`）；未发明号；`listTools`/`listSkills` 本刀不碰。**A 槽 `d24-queue-mutation-bytes` @ 2026-09-16 已转 bytes**（未关本行）：`_queueMutation` 17 方法走 `makeUnaryBytesClient`；QueueItemRef `item_id`=2 `op_id`=3（不按 JSON key 序）；Enqueue/Insert 不发明 6/7/8；Edit 不发明 `expected_version`=5；不解 ErrorDetail=5。`listTools`/`listSkills` 本刀不碰。**A 槽 `d24-git-unary-bytes` @ 2026-09-16 已转 bytes**（未关本行）：Git 六条 `ReadGitSummary`/`ReadGitChanges`（`session_id`=1）、`ReadGitFileDiff`（`path`=2 `index_state`=3）、`WriteGitStagePaths`（repeated `GitArgvCommand commands`=2，`argv`=1）、`WriteGitCommit`（`message`=2 `sign_off`=3 `amend`=4，bool false 省略）、`WriteGitApplyHunks`（repeated `argv`=2 `patches`=3）；响应走现 mapper。`listTools`/`listSkills` 当时不碰。**A 槽 `d24-listtools-listskills-bytes` @ 2026-09-16 已转 bytes**（未关本行）：`Tool.ListTools` 空 request（`category`=1 `include_hidden`=2 本仓 API 不传故省略）；响应 `tools`=1 `total`=2；`ToolSummary` `name`=1 `description`=2 `category`=3 `destructive`=4 `requires_permission`=5。`Tool.ListSkills` 空 request；响应 `skills`=1 `total`=2；`SkillSummary` `name`=1 `description`=2 `source`=3 `slash_enabled`=4 `enabled`=5。字段号来自 J 仓 `tool_service.proto`（与 Git 六条同一来源），decode 成现有 Wire 再 mapper。ToolInfo / SetSkillEnabled 当时未转。**A 槽 `d24-mcp-plugin-clipboard-bytes` @ 2026-09-16 已转 bytes**（未关本行）：MCP `ListMcpServers`（不发 `transport_filter`=1；`enabled_only`=2 false 省略；`work_dir`=3；transport 数字映射 `stdio`/`sse`/`streamable_http`）/`GetMcpServerStatuses`/`GetMcpServerTools`（`force_refresh` false 省略）；Plugins `List` 空 proto（`EMPTY_PROTO_MESSAGE`）/`Info`（`map` 仅解码 MapEntry key=1 value=2）；Clipboard Write/Read/List/Clear（TEXT type=0 省略 field 4）。**A 槽 `d24-mcp-plugin-mutator-bytes` @ 2026-09-16 已转 bytes**（未关本行）：MCP Toggle/Add/Update/Remove（`McpServerConfig` 1–8；transport enum 数字 1/2/3，0 omit；env = MapEntry key=1 value=2）；plugin Enable（`enabled !== false` 默认 true，false omit）/Reload/Unload/ScanNew（空 request `encodeEmptyProtoMessage()`）。**A 槽 `d24-question-clienttool-bytes` @ 2026-09-16 已转 bytes**（未关本行）：`Agent.RespondQuestion`（`session_id`=1 nested `QuestionResponse`=2：`question_id`=1 map `answers`=2 MapEntry key=1 value=2=`QuestionAnswer`/`selected_labels`=1 `custom_text`=3 空 omit）；`Agent.SendClientToolResponse`（`session_id`=1 nested `ClientToolResponse`=2：`call_id`=1 `is_error`=2 false omit `content`=3 `metadata_json`=4 repeated `CanvasRefData`=5 号 1–4，空 `source_hash` omit）。**A 槽 `d24-tool-leftover-unaries-bytes` @ 2026-09-16 已转 bytes**（未关本行）：`Tool.SkillInfo`（`skill_name`=1；响应 `name`=1 `description`=2 `source`=3 `content`=4，无 enabled）；`SetSkillEnabled`（`enabled` false omit；响应 `status`=3 UNSPECIFIED=0 omit / OK=1 / NOT_FOUND=2 / FAILED=3，OK→现 mapper `ok`）；`ToolInfo`（`tool_name`=1；响应 1–7 `aliases`）；`ListCommands` 空 request `encodeEmptyProtoMessage()` 仍 sendMessage；响应 `commands`=1 `total`=2，`CommandSummary` 1–8，`source` 解 varint 走现 mapper；`GetCommandDef`（`command_name`=1；响应 1–11 `skill_source`）。同刀 `Agent.DeleteAgentProfile`（`id`=1 响应 `success`=1）。未发明号；未转 `SaveSkillContent`（J `tool_service.proto` 无该 RPC）。**F 槽 `d24-memory-unary-wire` @ 2026-09-16 已写 wire+测**（未关本行）。**G 槽 `d24-file-unary-wire` @ 2026-09-16 已写 File 六条 wire+测**（未关本行）。**A 槽 `d24-memory-file-unary-bytes` @ 2026-09-16 已接线 bytes**（未关本行）：Memory Save/Search/SearchDeep/Read/List/Delete/Reflect/Revert/History 走 `grpcMemoryUnaryWire`（字段号来自 J 仓 `memory_service.proto`；decode 成现有 `Memory*Wire`；proto3 空/0/false omit）。跳过 `openRebuildMemoryStream`（仍 JSON server-stream）。File `ListFiles`/`ReadFile`/`GetFileInfo`/`WriteFile`/`ForceWriteFile`/`AgentMerge` 走 `grpcFileUnaryWire`（`content`/`base_content` 等 `encodeBytesField`；`WriteFileResponse.Status` SAVED=0 omit）。未发明号。未转 SaveSkillContent / Health / Connect / openContinuationStream。`MemorySearchResult.score`=4 double 仍 unread（D487）；mapper 缺 score 时 `requiredDouble`→0。未 catalog `onOpenConnection`。**F 槽 `d24-chatsync-unary-wire` @ 2026-09-16 已写 wire+测**（未关本行；A 已接线）：ChatSync/SyncInputDelivery 见 `grpcChatSyncUnaryWire.ts`；`attachments`=3 因 TS 无该字段未编；`delivery` 仅 1/2/3（0 omit；其余 omit 不猜）；optional bool false 写出 varint0。**G 槽 `d24-team-mutator-wire` @ 2026-09-16 已写 Team mutators wire+测（A 已接线；未关本行；不得写成 D482）**：`StartMember`（`member_name`=3 `preset_id`=4 `system_prompt`=5 `model_type`=6 `dynamic`=7 false omit；响应 `member_agent_id`=1 `member_name`=2 `dynamic`=3 **bool** 非 string）/`KillMember`/`CreateTeam`（repeated `task_descriptions`=3）/`Abort`（`team_id`=3 `reason`=4；响应 repeated `stopped_members`=3）/`TaskUpdate`（`task_id`=3 `new_status`=4 `message`=5）/`TaskCancel`/`MessageMember`（`content`=4）。ListTeams/MemberStatus/TaskList/TeamInfo 不重做。字段号来自 J `team_service.proto`。decode 对齐 grpcClient JSON→TS。**A 槽 `d24-chatsync-team-mutator-bytes` @ 2026-09-16 已接线 bytes**（未关本行）：ChatSync/SyncInputDelivery 走 `grpcChatSyncUnaryWire`，decode 后仍 `mapChatSyncResponse`/`mapSyncInputDeliveryResponse`；Team 七 mutator（StartMember/KillMember/CreateTeam/Abort/TaskUpdate/TaskCancel/MessageMember）走 `grpcTeamUnaryWire`，decode 已是 TS 结果（`ok`/`message`/`memberAgentId`/`dynamic: boolean`/`teamId`/`stoppedMembers`）不再 JSON `{success}` map。ListTeams/MemberStatus/TaskList/TeamInfo 不重做。未发明号。未转 SaveSkillContent/Health/Rebuild/Connect/openContinuationStream。未 catalog `onOpenConnection`。**F 槽 `d24-system-unary-wire` @ 2026-09-16 已写 System HealthCheck/Shutdown wire+测**（未关本行、当时未接线 `grpcClient`；A 已接线）：HealthCheck 空 request（非 JSON `{}`）；Shutdown `force`=1 `grace_period_ms`=2（false/0 omit）。见 `grpcSystemUnaryWire.ts`。禁止 Doctor/Connect。**G 槽 `d24-context-variable-unary-wire` @ 2026-09-16 已写 ContextVariable List/Read wire+测（当时未接 `grpcClient`；未关本行；不是 D482）**：List `session_id`=1 `agent_id`=2；响应 repeated `current`=1 `inherited`=2；Read `session_id`=1 `name`=2 `agent_id`=3；响应 `entry`=1。Summary 1–5 / Entry 1–5；`VARIABLE_GLOBAL`=0 omit。字段号来自 J `context_variable_service.proto`。decode 对齐 `mapContextVariableListResponse` / `mapContextVariableReadResponse`。**A 槽 `d24-health-contextvariable-bytes` @ 2026-09-16 已接线 bytes**（未关本行）：`System.HealthCheck`/`Shutdown` 走 `grpcSystemUnaryWire`，decode 后仍 `mapHealthCheckResponse`/`mapShutdownResponse`；`ContextVariable.List`/`Read` 走 `grpcContextVariableUnaryWire`，decode 后仍 `mapContextVariableListResponse`/`mapContextVariableReadResponse`。未发明号。未转 SaveSkillContent/Rebuild/openContinuationStream；TokenUsage/Config Get+Set wire 已落未接线；Watch stream 不转。未 catalog `onOpenConnection`。**F 槽 `d24-token-usage-unary-wire` @ 2026-09-16 已写 GetSessionUsage/GetGlobalUsage wire+测**（未关本行、未接线 `grpcClient`）：`session_id`=1；响应 `usage`=1；Global 请求空 payload（reserved 1–10 不写）；`TokenUsageData` 1–8。**G 槽 `d24-config-get-set-unary-wire` @ 2026-09-16 已写 Config Get/Set wire+测（未接 `grpcClient`；未关本行；不是 D482）**：Get `key`=1 `scope`=2 `session_id`=3；响应 map `values`=1（MapEntry `key`=1 `value`=2）`scope`=2；Set `key`=1 `value`=2 `scope`=3 `session_id`=4；响应 `success`=1 `message`=2。禁止 Watch。未做 SwitchModel / ListModels / ResolveModel / SetPermissionPolicy。字段号来自 J `config_service.proto`。decode 对齐 grpcClient JSON `{values,scope}` / `{success,message}`。**活 JSON（有调用点）**：`openRebuildMemoryStream` 仍 JSON server-stream；ContinueGeneration / Regenerate / Resume **已** bytes（A `d24-continuation-stream-bytes`）；`saveSkillContent` 仍 JSON；TokenUsage GetSession/GetGlobal grpcClient **已** bytes（A 本波接线）；Config Get/Set grpcClient **已** bytes（A 本波接线）；SetPermissionPolicy/Doctor **已** grpcClient bytes；Watch stream 不转。**未用（不转）**：`Session.Info` 已是 bytes（roster/SessionBar 不转发，不重做）；`ChatSync` / `SyncInputDelivery` grpcClient **已** bytes（Gateway；decode 后仍 mapper）；Memory Rebuild 未接线 bytes（C 槽 wire+测已落，`openRebuildMemoryStream` 仍 JSON）。`SessionEventStream` 握手仍无 `client_id`、无 60s heartbeat uplink（host 未发/未期待则不发明）。`CreateSessionRequest` proto 无 `title`。`Agent.Chat` 响应只解 `session_id`/`agent_id`。**catalog / `IUniverseAgentConnection` 无 GetQueue / ListQueue**（Inbox 已诚实「Queue not listed」，不发明 list-queue RPC）**A 槽 `d24-clear-demo-fake-model-prefs-bytes` @ 2026-09-16 已转 bytes**（未关本行）：`Agent.ClearSessionDemoFake`（`session_id`=1；响应 `success`=1 `message`=2 `reason_code`=3）、`Config.GetModelPreferences`（`session_id`=1；响应 `min_level`=1 `max_cost`=2 `min_speed`=3 `strategy`=4）、`Config.SetModelPreferences`（`session_id`=1 optional `min_level`=2 `max_cost`=3 `min_speed`=4 `strategy`=5；响应 nested `preferences`=1）。proto3 空串/0 omit。未发明号。跳过 Connect / SaveSkillContent / Watch / Rebuild / ResolveTurn / SwitchWorkDir。**A 槽 `d24-continuation-stream-bytes` @ 2026-09-17 已转 bytes**（未关本行）：`Agent.ContinueGeneration` / `Regenerate` / `Resume` 走 `makeServerStreamBytesClient` + `decodeChatResponse`；Continue/Regenerate `session_id`=1 `agent_id`=2 `turn_id`=3 `message_id`=4；Resume `session_id`=1 `agent_id`=2。proto3 空串 omit。未发明号。Watch / Rebuild / Connect / SaveSkillContent / ResolveTurn / ResolveAnchor 仍 JSON。**C 槽 `d24-memory-rebuild-stream-wire` @ 2026-09-17 已写 wire+测**（未关本行、未接线 `grpcClient`）：MemoryService.Rebuild `MemoryRebuildRequest` `scope`=1 `dry_run`=2（bool false omit）；`MemoryRebuildEvent` `phase`=1 `message`=2 `progress`=3 `files_processed`=4 `files_total`=5。decode 成现有 `MemoryRebuildEventWire` 再 `mapMemoryRebuildEvent`。见 `grpcMemoryRebuildStreamWire.ts`。`openRebuildMemoryStream` 仍 JSON `makeServerStreamClient`。 | Create 恢复已下沉；ChatSync 是 Gateway 专用；本刀已转 Delete/Rename/Cancel/SetPermissionMode/SwitchModel 与 List/Create/Restore/Delete Snapshots、CancelToolCall/SetSessionGoal/MemberStatus/TaskList/TeamInfo/FetchToolDetail、CancelSessionGoal/Respond/Fork/Kill/DeleteMessage/EditMessage、SyncPermissionRule/PromotePermissionRule/GetSessionRules、Git 六条、`listTools`/`listSkills`、MCP list/status/tools、plugins list/info、clipboard 四条、MCP mutators / plugin mutators / respondQuestion / sendClientToolResponse / SkillInfo / SetSkillEnabled / ToolInfo / ListCommands / GetCommandDef / DeleteAgentProfile 已 bytes；未转 SaveSkillContent；Memory/File/ChatSync/Team mutators 已 bytes；A 已接线 HealthCheck/Shutdown + ContextVariable List/Read bytes（SaveSkillContent/Rebuild/TokenUsage/Config 仍 JSON）；F 已写 TokenUsage GetSessionUsage/GetGlobalUsage wire+测但 grpcClient 仍 JSON（未接线）；G 已写 Config Get/Set wire+测但 grpcClient 仍 JSON（未接线；Watch 不转）；GetQueue 不在本仓 proto 面 | 按实际打到引擎的 method 再改 bytes（须本仓已有该方法字段号）；`listTools`/`listSkills` 已 bytes；MCP list/status/tools、plugins list/info、clipboard 四条、MCP mutators / plugin mutators / respondQuestion / sendClientToolResponse / SkillInfo / SetSkillEnabled / ToolInfo / ListCommands / GetCommandDef / DeleteAgentProfile 已 bytes；未转 SaveSkillContent；Memory/File unary 已 bytes；ChatSync/SyncInputDelivery 与 Team 七 mutator **已** grpcClient bytes；HealthCheck/Shutdown + ContextVariable List/Read **已** grpcClient bytes；SaveSkillContent/Rebuild/TokenUsage/Config 仍 JSON；TokenUsage GetSession/GetGlobal **已** grpcClient bytes；Config Get/Set unary wire 已落、grpcClient 仍 JSON（未接线）；Watch stream 不转；`probeRpc` 已 empty proto；Delete/Rename/Cancel/SetPermissionMode/SwitchModel 与 List/Create/Restore/Delete Snapshots、CancelToolCall/SetSessionGoal/MemberStatus/TaskList/TeamInfo/FetchToolDetail、CancelSessionGoal/Respond/Fork/Kill/DeleteMessage/EditMessage 与 queue 17 mutation、SyncPermissionRule/PromotePermissionRule/GetSessionRules、Git 六条已 bytes。若日后 catalog 出现 GetQueue 再接线失败行 **A 槽 `d24-device-revoke-bytes` @ 2026-09-16 Device.Revoke grpcClient 已 bytes**（`device_id`=1；响应 `success`=1 `message`=2；未关本行）。**F 槽 `d24-trigger-unary-wire` @ 2026-09-16 Trigger 五 unary wire+测已落未接线**（List/Upsert/Delete/SetEnabled/Fire；跳过 RegisterSessionEngineTrigger；未关本行）。**A 槽 `d24-trigger-unary-bytes` @ 2026-09-16 Trigger 五 unary 现已在 grpcClient 走 bytes**（List/Upsert/Delete/SetEnabled/Fire；跳过 RegisterSessionEngineTrigger；未关本行）。**A 槽 `d24-session-lifecycle-bytes` @ 2026-09-16 已转 bytes**（未关本行）：`Session.Prewarm`（repeated `session_ids`=1；响应 repeated `entries`=1；`PrewarmSessionEntry` `session_id`=1 `outcome`=2 `message`=3；enum PrewarmSessionOutcomeProto 0–4）、`Shelve`/`Unshelve`/`Purge`（`session_id`=1；响应 `success`=1 `message`=2）、`Export`（`session_id`=1 `format`=2；响应 `content`=1 `format`=2）。未发明号。跳过 Connect / SaveSkillContent / GetModelPreferences / Watch / FireTriggerWebhook。**A 槽 `d24-fire-trigger-webhook-bytes` @ 2026-09-16 已转 bytes**（未关本行）：`Agent.FireTriggerWebhook`（`session_id`=1 `trigger_id`=2 optional `payload_json`=3 空/缺省 omit；响应 `status`=1 enum 0–4 `event_id`=2 `reason`=3）。同刀 `InstallSessionDemoFake`（`session_id`=1 `queues_payload`=2 bytes `content_type`=3 `playbook_id`=4；响应 `success`=1 `message`=2 `reason_code`=3）。未发明号。跳过 Connect / SaveSkillContent / GetModelPreferences / Watch / ResolveTurn / ClearSessionDemoFake。ClearSessionDemoFake / Get·SetModelPreferences **已** bytes（未关本行）。**A 槽 `d24-agent-five-unaries-bytes` @ 2026-09-16 已接线 bytes**（未关本行）：`Agent.SwitchWorkDir`（decode 后 `ok: wire.success === true` + previous/currentWorkDir/message）、`Compact`/`Todo`/`Usage`/`Status` decode 后仍 `mapCompactResponse`/`mapTodoResponse`/`mapUsageResponse`/`mapStatusResponse`。未发明号。跳过 Connect / SaveSkillContent / Watch / Rebuild / ResolveTurn / ResolveAnchor / Pause / History / Back / Prune / Branch / Reset / TestModelProfile。Status `children`/`model_info` 仍 unread。**A 槽 `d24-seven-agent-unaries-bytes` @ 2026-09-16 已接线 bytes**（未关本行）：`Agent.Pause`/`History`/`Back`/`Prune`/`Branch`/`Reset`/`TestModelProfile`。params 仍 unread。跳过 Connect / SaveSkillContent / Watch / Rebuild / ResolveTurn / ResolveAnchor / SuspendLoop。七条 loop/tool 已 grpcClient bytes（Suspend/Resume/StopLoop/RunToolBG/StopShellTask/ListLoopSnapshots/FetchToolUsageDetail；context_sources unread）。仍勿关本行。 C–I 本波 wire 已落未接线：GetRemoteSessionStatus(pending unread)/GetRemoteSessionHistory(messages unread)/CreateRemoteSession(SessionParams known)/GetUploadProgress/ResolveModel(ModelEntry unread)/SaveConfig(config nested scalars；connection_test unread)。仍勿关本行。 **A 槽 `d24-six-unaries-bytes` @ 2026-09-16 已接线 bytes**（未关本行）：SaveConfig / CreateRemoteSession / GetRemoteSessionStatus（pending unread）/ GetRemoteSessionHistory（messages unread）/ GetUploadProgress / ResolveModel（ModelEntry unread）。未发明号。跳过 Connect / SaveSkillContent / Watch / Rebuild / ResolveTurn / ResolveAnchor。仍勿关本行。 **A 槽 `d24-continuation-stream-bytes` @ 2026-09-17**：ContinueGeneration / Regenerate / Resume 已 grpcClient bytes。仍勿关本行。 **C 槽 `d24-memory-rebuild-stream-wire` @ 2026-09-17**：Rebuild 流 wire+测已落，grpcClient 仍 JSON。仍勿关本行。 | platform / universeAgent | open || D25 | P2 | **roster 不再远程 Create**（空 catalog 只 pending，host lease 唯一 Create）。**host leftover 已收（工位 A `ghost-bind-failed-ui` @ 2026-09-07）**：List 抛错 / ghost 行 lease bind 失败后 roster 与 `getActiveSession` 显示 bind-failed，不再回落 stub「Untitled session」或 `conversationLens.sessionNew`「New session」（SessionBar / StatusBar 读同一标题）。**引擎半已在 UA 闭（2026-09-10 · `7e498e7092` · `D-SESSION-STORE-MIGRATION-STUCK-1`）**：`ensureSchema` 仍做旧库兼容（`SCHEMA_VERSION=7` 版本门控 9 段增量，不是「只认新 schema」）；修的是半途失败自愈。`LookupFailed` deny 未放宽。见 [report](../reports/engine-session-store-migration-stuck-2026-09-09.md) §9。**2026-09-12 复验 PASS**（grpcurl `-insecure` @ `127.0.0.1:50061` PIN 已换）：List 先空列表（store 打开），Create 后再 List `total=3` 含 `session-100`/`session-101`/新 id。日志无 `Query does not return results`。本仓不改引擎代码 | host 面已收；引擎自愈已合；换钉复验已做 | List 可读且旧卡住目录出现在名单；与 D26 同批关 | conversation / engine | closed || D26 | P2 | **引擎 store 迁移半途卡死（host 已收；病因 2026-09-09 改口；引擎修法 2026-09-10 已合 UA）**：host Tree+recover 不变。**2026-09-09 根因仍成立**（[report](../reports/engine-session-store-migration-stuck-2026-09-09.md)）：卡住态指纹 + xerial 写语句走 `executeQuery` + `user_version` 停 0。**2026-09-12 账更正**：UA 未砍旧库迁移、未改「只认新 schema」。`SCHEMA_VERSION=7`，`ensureSchema` 仍版本门控；半途失败自愈（每段 stamp + `driver.execute`）已闭 `D-SESSION-STORE-MIGRATION-STUCK-1`（`7e498e7092`）。`LookupFailed` deny **未放宽**。**vscode 旧账过期**。**2026-09-12 复验**：Create 新 id PASS；旧 `session-100`/`session-101` 打开成功（非 `ALREADY_EXISTS`）；Info 可读。日志无 `Failed to open message store` / `ALREADY_EXISTS`。当时 Chat 另卡空 model catalog（`MODEL_PROFILE_REQUIRED` / `UNRESOLVED`），**不是**本行 store 症状，不挡关本行。**同日稍后** seed `provider:state` + 重启后 grpcurl Chat **PASS**（无 `MODEL_PROFILE_*`）。仍不升 PRD-008 | 引擎修法已合；换钉复验 store/Create 已做。禁止放宽 deny / 清 `.sessions` 当主线 | **原闭合条件「Create 先写 meta」仍撤回。** store 能打开 + Create 不再因迁移回 6 | engine | closed |  F/G/H/I/J wire 已接线 bytes：SwitchWorkDir / Compact / Todo / Usage / Status（children/model_info unread）。仍勿关本行。  C–J 本波七条已接线 bytes：Pause / Agent.History / TestModelProfile(params unread) / Back / Prune / Branch / Reset。仍勿关本行。  C–J 本波 wire 已落未接线：SuspendLoop / ResumeLoop / StopLoop / RunToolInBackground / StopShellTask / ListLoopSnapshots / FetchToolUsageDetail(context_sources unread)。仍勿关本行。 **A 槽 `d24-shell-remote-seven-bytes` @ 2026-09-16**：七条 SendShell/ListNodes/GetNode/Set·ExitMaintenance/ResetError/ListConfigs 已 grpcClient bytes。仍勿关本行。 **A 槽 `d24-remote-seven-bytes` @ 2026-09-16**：七条 GetConfig/DeleteConfig/Reload/Destroy·Cancel·Resume/CheckConnection 已 grpcClient bytes。仍勿关本行。
