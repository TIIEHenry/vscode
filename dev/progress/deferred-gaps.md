---
title: "Loop Deferred Gaps"
type: progress
status: accepted
phase: N/A
created: 2026-08-30
updated: 2026-09-17
summary: "D24 仍开。SessionStream permission_request=50 已 decode。Status model_info=9 仍 unread。Connect/SaveSkillContent/Watch/Resolve/Pty 仍 JSON。不是 leftover/pills 完成。"
---

# Deferred Gaps

> **SSOT**：当轮无法完成、但不阻塞当前目标的缺口写入本表。  
> 读路径：`dev/progress/status.md` → 本文件 → [research-queue.md](research-queue.md)。

| ID | Priority | Gap | Why Deferred | Exit Condition | Track | Status |
|:---|:---------|:----|:-------------|:---------------|:------|:-------|
| D1 | P3 | 套件 `dev/loop/overview.md` 引用的 `docs/guides/multi-agent-design-workflow.md` 缺失 | 门禁仅 warning 不阻塞 | 指南落盘且 `check-docs-health` 0 warning | docs | closed |
| D2 | P2 | 工位池基线未编译验绿 | 编译耗时长；建槽时未跑 | merge 槽 `569ce371`：`npm run compile` 绿；[worktree-pool.md](worktree-pool.md) 已标注 | infra | closed |
| D3 | P2 | **M0 compile 验证**（`compile-client` + `valid-layers-check`） | — | merge 槽 `a6137373`：`compile-client` + `valid-layers-check` 绿；17 域单测绿 | M4 | closed |
| D4 | P2 | **启动 T1–T3 演示**（目视：Conversation 中心、End Editor/Sources、互斥、四钮；M3 无 ChatEditor 默认路径、Sidebar stub Sessions） | 工位 A 首轮无构建产物（已记）；D3 绿后可在 merge 工位重跑。**Closer 是 M5 切片 4 V1–V8**，M4 切片 2 不得把本行标 closed | M5 V1–V8 隔离 profile 通过并有证据（含路由与 roster）；T1–T3 只作环境探测记录 | M5 | closed |
| D5 | P2 | **EH 探针冒烟**（LSP + layout 类扩展） | — | 三探针行 **已实测 @2026-09-02**（[wave3](d5-evidence/smoke-wave3-0001/)）；panel/terminal 记入矩阵 **待实测**，不阻塞本行 | M4 | closed |
| D6 | P3 | **Diff footprint 刷新** | slot C 已于 `b283fe19` 重测 `b5631393` | 页已更新 | docs | closed |
| D7 | P3 | titlebar LayoutControlMenu 产品四钮与原生 Panel/Aux 共存 | `2dcd5a0a` 已从 LayoutControlMenu 去掉 Panel/Aux；留 submenu | 默认窗只见四钮 | M0 | closed |
| D8 | **P3**（2026-09-02 由 P2 降） | **`valid-layers-check` 环境红**（`EditContext`/`GPUBufferUsage`/`FileSystemHandle` TS lib 报错）。**D147 已做 step 1**（worker 补充类型对齐；`fbc1bc47cba` worker native tsc **48→2**），step 2B 未开 | D8 深挖 @2026-09-02 **`c0dfee3d`**：Node **24.18.0** + PATH 钉死仍 **FAIL**（166× TS）；**browser/electron-browser 单跑 0 错**，红在 node / worker 系 checker 拉入 browser 源码却未套补充类型（见 D8 节），**不是业务分层违规**。**裁决 @2026-09-02：豁免为集成门禁**（[health-gates](health-gates.md) 改 `compile` + `eslint`）；分层由 ESLint `code-layering` + stream-timeline S1 boundary 测承担 | `npm run valid-layers-check` 六域全绿（按 D8 节「可执行修复」1 → 2B），再恢复为门禁；任一实施波不得以「D8 红」为 blocked 理由 | infra | open |
| D9 | **P3**（2026-09-02 由 P2 降） | **EH 矩阵次级探针**：`viewsContainers.panel` / `views`(panel) / `terminal` profiles·`onStartup` / 命令 + `editor/decoration` | panel + decoration **已实测 @2026-09-02**（[d9](d5-evidence/smoke-d9-0001/)）；仅 `terminal` 行 xterm 自动化 blocked。**裁决 @2026-09-02：不再列 status Blockers**——矩阵 terminal 行标「待人工」即为诚实状态，无产品功能依赖它 | 人工跑一次 `Terminals: Run` 并截图记入 [smoke-d9-0001](d5-evidence/smoke-d9-0001/)；或某切片首次依赖 terminal EH 面时顺带补 | docs | open |
| D10 | P3 | **PRD-012 T5** 轨迹搜索 / 虚拟化 / Overview 瀑布条 | fixture 三位数以下普通 DOM 够用。PRD-020 已 `accepted` 并写死上限（对话 1,000 / 轨迹 5,000）| 触发 = **M6-D 轨迹 T4 合入后**（引擎真数据前虚拟化无意义）；另在 stream-timeline S2 落地后用 1,000 回合 fixture 测 PRD-020 验收 1（≤ 200 ms）并记本行；实施后 [conversation-trajectory-lens](../plans/conversation-trajectory-lens.md) T5 行转 implemented | M6+ | closed |
| D12 | P2 | **PRD-010 产品身份落地**：`product.json` 名称/目录/协议族 + 三平台图标与发行文案 | M7 已建立 [product-identity](../plans/product-identity.md)；**I1 审计已完成（2026-09-02）**：UniverseAgentDesktop 无打包图标；**用户同日裁定**以 `Singular/logo/singularity.svg`（Singularity 标识）为唯一品牌源，I3 拆为 I3a（源入仓 + 生成脚本，含 ≤48px 粗线派生）/ I3b（三平台接线），不再阻塞。Darwin 发布域仍待发布方 | 按 I1–I5 落地；窗口标题、About、桌面入口、`universe-agent://` 与数据目录可识别为 UniverseAgentStudio | M7 product | open |
| D11 | P3 | **证据目录与索引卫生**：未跟踪 `d4-evidence/82582fe8`、`d4-evidence/rerun-2221`；`plans/INDEX.md` 指向不存在的 `dev/roadmap/` | 首轮验收产物未收编；roadmap 目录从未建立 | 两目录补 README 收编或删除，`git status` 干净；INDEX 改指 `status.md` Next 段；`check-docs-health` 0 warning | docs | closed |
| D13 | P2 | **PRD-017 本地会话持久化**（`accepted` @2026-09-02） | 落点已裁定：`StorageScope.WORKSPACE` + `StorageTarget.MACHINE`；引擎接通后不迁移，本地只存 stub 会话 + UA 断连快照缓存并标 `source` | S3 合入后作为独立切片（同工位串行）：重启后会话列表 / 当前会话 / 回合 / 权限记录一致；session 窗口布局恢复；无引擎无「已同步」文案；单测 + D4 式隔离 profile 重启验收 | M6 | closed |
| D14 | P3 | **PRD-018 第一阶段**：四钮默认键位 + F6 Part 焦点循环 | 键位选定属 M6-B 后续；本行不代表全部 chat tab / dialog / seat ARIA 已完成 | 四 toggle 命令可见且不冲突；F6 循环已落；其余可达性转 [M7 a11y/RWD](../plans/accessibility-responsive-ui.md) | M6+ | closed |
| D15 | P3 | **PRD-019 Web 冒烟**（`accepted` @2026-09-02）：验证义务，非新功能 | P0 browser 三服务与 E2-1 Web 省略桌面控件 **代码已落**。W1 已于 2026-09-07 工位 A 跑通 | `scripts/code-web.sh` 启动 → D4 式 V1–V3 通过；`getConnectionPhase()` = `disconnected`、不画连接控件；证据 [d15-evidence/w1-1556dde3](d15-evidence/w1-1556dde3/) | M7 | closed |
| D16 | P2 | **conversation 单测基线红（仍开）**。**切片 0 账本已落**（[d16-ledger.md](d16-ledger.md) @ `42eba1e6ff4`）：三 `--run` 均 exit 0，实测失败 **0**（账本 0 数据行）。「15」是 `0649602d` 历史观测，不是合同。切片 3 名单 @ `d2abb648c0e` **未**列本行三文件标题。Lens 夹具已补（sessionBar 实测宽 + `lens.layout` 叶宽 + ResizeObserver）。过时断言已改：删非末会话计数 −1；300px 验 `is-narrow` 不是像素；permission seat aria 走 `Permission` 合同。**identity 已绿**并改走共享 `conversationLensLayoutHarness`。**全部 Lens 夹具套已隔离**。stub `deleteTurn` 已改走 `createSession()`。**进口界空扫假绿已收**（四份测改扫 `src/` + `>=80`）。**T5 Exit `title` 债已收**。**visualize 三测已收**。**remainder 7 已分类并夹具收口**（工位 B `lens-d16-remainder-7`）。B/A 全量 Lens **97 pass / 0 fail**。**本刀 `lens-ro-uncaught`：harness 已补 mocha `Runner#_uncaught` + Node uncaught / errorHandler 吞 ResizeObserver loop（未闭本行）。** **本行仍开**（compile 门禁 / S2 leftover；切片 0/3 不闭、不开切片 1「已归零」） | 启动级 / identity / stub / 进口界 / Lens 叶宽夹具 + 全套共享 harness + 上述过时断言已修；T5 Exit title 债已修；visualize 三测已修；remainder 7 已在 B 收口为绿；切片 0 账本已记 0 失败 | 账本退出条件（方案 §3）全满足且 compile 门禁后再闭本行；**不得**仅因切片 0 零失败行关闭 | M6 / conversation | open |
| D17 | P2 | **M7 非阻塞验证债总账**：单测、E2E、视觉、a11y、性能与缺失 Engine/Hub/Web 证据。切片 3 三域非 D16 红见下节 | 用户裁定测试不阻塞 UI 开发；若每条红测都成为 blocker，会再次冻结不冲突 UI 槽 | 每项记录首次 SHA、场景、baseline/新增、owner、关闭证据；普通失败不进 `status` Blockers，只阻止对应 PRD/plan 升 `implemented` | M7 verification | open |
| D18 | P2 | **I3b 三平台安装包未验**：hicolor 已进 deb/rpm gulp+spec，`electron.ts` 已改公司名/HelpBook；未跑 prepare-deb/rpm、snapcraft、Inno、darwin 打包 | 2026-09-07 工位 B：本机已有 fakeroot+dpkg-deb，仍缺 rpmbuild/snapcraft/Inno/darwin；无 `VSCode-linux-x64` 产物，禁 compile 故未跑 prepare-deb。源资产八档 hicolor/ico/icns/bmp 在仓 ≠ 进包 | V 槽确认八档 hicolor 进包，Win/mac 检查 ico/bmp/icns 与 exe/plist 元数据 | product / packaging | open |
| D19 | P2 | **L1 源码复核残留**（[a11y-rwd-l1.md](a11y-rwd-l1.md)）：(1) **已收口 @ 2026-09-04**（方案改口 Engine/Connection **无动画节点**，pane CSS 文件头钉死不挂空 `.ua-motion`）；(2) **已收口 @ GC-7 2026-09-04**（`style.ts` 引入 `ua-common.css`，T1 + `productAccessibility.css` 覆盖 `.preferences-editor`）；(3) **已收口 @ 2026-09-04**（§5/§9 改口：Connection 窄宽为**单栏滚动**，无左导航/Back——产品本无 Engine 式 nav，不造假 Back）；**(4) Web 省略门控与点名文案已在 E2-1 收口**（`shouldDrawDesktopConnectionControls` 读 phase / capability / `unsupported_environment`，不再用 `isWeb`） | L1 只做源码清单；不阻塞 CS-3 或 W1 | (1)–(4) 均已收口。手测/axe/Web 冒烟失败仍记 D17/D15，不并入本行 | M7 a11y | closed |
| D20 | P2 | **CS-6 Settings 默认窗 300px 目视**：搜索框与 Client 七组标题在缩到约 300px 时是否仍可见；`SettingsEditor2` 在宽度低于 700px 已隐藏 TOC，源码未见 UA 专用挤占，但本轮禁止 electron / playwright | CS-6 代码完成线不阻塞；§6 产品验证另含重载 / 草稿不进 Sync / 两种 Enter / 通知语义 | 隔离 profile 打开默认窗 Settings，缩到 300px，确认搜索框与 Client 组标题仍可见、无 emptyCopy；失败记入 D17 证据，不并入本行关闭条件 | M7 verification | open |
| D21 | P2 | **Host 暴露 Agent 树首拉失败态**（GC-5b；对齐 [navigator-engine-segments §3](../plans/navigator-engine-segments.md)）：`IUniverseAgentConnection.isAgentTreeFetchFailed()` + `onDidChangeConnection`；共用空态失败 note「读取 Agent 树失败」优先于「正在读取」；Hierarchy / Team 同字串；Agents 不再用 `transport === 'failed'` 作树失败轴 | — | host 经 connection 提供 `treeFetchFailed`；两叶共用；单测：失败 ≠ 「没有团队」/ 「正在读取」 | M7 navigator | closed |
| D22 | P3 | **同窗口同会话多 lease 渲染端共享**（[session-view-frame-fanout](../plans/session-view-frame-fanout.md) F3）：F1 已按 lease 扇出；同窗多 UI 仍各持宿主 lease。**S1–S3 不含 F3**（[session-subscription-lifecycle](../plans/session-subscription-lifecycle.md)） | F1/F2 Exit 不含；须收口 `post` / `requestDetail` | 同窗同 session 共用一个宿主 lease；第二 lease 不二次 `acquireLease` | M7 conversation | open |
| D23 | P2 | **`sendHeartbeatAck` fire-and-forget 无总 catch**：`sessionViewHost.ts` `void this.sendHeartbeatAck(sessionId)`；resident `write({heartbeat_ack:{}})` 无 try/catch（`connection.chat` 一路有）。bind/write 失败会成未处理 rejection | A 槽 `host-write-retry` 已补 bind/`resident.write`/`chat` 三级 catch，并扩展 `sessionViewHostHeartbeatAck.test.ts` | `sendHeartbeatAck` 全程与 `writeChat` 同级 catch；`void` 调用点不再漏 rejection；补测 | M7 universeAgent | closed |
| D38 | P3 | **`void fillHistory` / `ensureEngineSession` 无 catch**：`sessionViewHost.ts` ~917 / ~1232 与已修 D23 同类，bind 失败可成未处理 rejection | A 槽 `host-bind-safety` 已补 bind/write catch，`void fillHistory` 不再漏 rejection；补 HistoryFill 测 | `fillHistory` 路径 bind/write 与 `writeChat` 同级 catch；补测 | M7 universeAgent | closed |
| D39 | P3 | **`requestDetail` await `ensureEngineSession` 无 catch**：bind 失败会成 Promise rejection 而非 `{ ok:false }` | A 槽 `host-bind-safety` 已回 `{ ok:false, reason:'failed' }`；补 bind 测 | `requestDetail` bind 与 `writeChat` 同级 catch；补测 | M7 universeAgent | closed |
| D40 | P3 | **`requestDetail` bind 成功后 `fetchToolDetail` 仍无 catch**：生产 connection 已吞错回 `{ ok:false }`；自定义 host 抛错仍会 reject | A 槽 `request-detail-fetch-catch` 已补 fetch catch；`{ ok:false }` 原样返回 | `fetchToolDetail` throw 与 writeChat 同级 catch 并回 `{ ok:false, reason:'failed' }`；补测 | M7 universeAgent | closed |
| D24 | P2 | **A 槽 `d24-listnodes-test-title-and-status-model-info` @ 2026-09-17**（未关本行）：ListNodes 测标题在 MERGE 已是 capabilities=7/load=8 已 decode，无 `field 7/8 unread` 残留。Usage `recent_request_spans`=12 RecentRequestSpan 1–9 已 decode（J `agent_service.proto`）；`context_window`=10 / `session_usage`=11 仍 unread。Status `model_info`=9 仍 unread。Connect / SaveSkillContent / Watch / ResolveTurn / ResolveAnchor / Pty 仍 JSON。**A 槽 `d24-upload-attachment-bytes` @ 2026-09-17 已接线 bytes**（未关本行）：`FileTransfer.UploadAttachment` 走 `makeClientStreamBytesClient` + `encodeUploadChunk`/`decodeUploadResponse`；写路径 `encodeUploadChunk`，仍 `mapUploadResponse`。未发明号。Connect / SaveSkillContent / Watch / ResolveTurn / ResolveAnchor 仍 JSON。同胞 identifier-scan 已对齐 Upload/Download bytes。**其余 unary/stream 仍 `makeUnaryClient` + `JSON.stringify`**。**A 槽 `d24-permission-leftover-bytes` @ 2026-09-16 已转 bytes**（未关本行）：`Permission.SyncPermissionRule`（`tool_name`=2 `scope`=3 `action`=4 `reason`=5；响应 `rule_id`=2）、`PromotePermissionRule`（`tool_name`=1 `scope`=2 `action`=3）、`GetSessionRules`（`session_id`=1；`SessionRule` 1–8 不解未知 field）。`listTools`/`listSkills` 本刀不碰。**本刀盘点（工位 A `live-rpc-bytes` @ 2026-09-07）**：接通后已 bytes = `GetAuthNonce` / DeviceAuth `Connect` / `Session.List`（默认 ALL）/ `Create`（field 4 `client_session_id`）/ `Resume` / `GetHistory` / `SessionEventStream` / `Agent.Chat` / `openChatStream` / `ListModels` / `ListAgents` / `ListAgentProfiles` / `ListDevices` / `Agent.Tree`。**本刀已转**：`probeRpc` 空 proto（`encodeProbeRpcRequest`），不再 `Buffer.from('{}')`；接通后 capability probe（`probeEngineCapabilities` + `saveSkillContent` 绑定）打到引擎。**A 槽 `d24-session-unary-bytes` @ 2026-09-16 已转 bytes**（未关本行）：`Session.Delete`（`session_id`=1 同 Info）、`Agent.Rename`（`title`=2）、`Agent.Cancel`（`agent_id`=2 同 Chat）、`Permission.SetPermissionMode`（`mode`=2）、`Config.SwitchModel`（oneof `model_type`=10 / `model_id`=11）；未发明号；`listTools`/`listSkills` 本刀不碰。**A 槽 `d24-next-known-unaries` @ 2026-09-16 已转 bytes**（未关本行）：`Agent.ListSnapshots`（`session_id`=1；`SessionSnapshotInfo` 1–9）、`CreateSnapshot`（`title`=2 `description`=3）、`RestoreSnapshot`/`DeleteSnapshot`（`snapshot_id`=2）；未发明号；`listTools`/`listSkills` 本刀不碰。**A 槽 `d24-next-known-unaries-2` @ 2026-09-16 已转 bytes**（未关本行）：`Agent.CancelToolCall`（`tool_call_id`=3）、`Permission.SetSessionGoal`（`goal`=2）、`Team.MemberStatus`/`TaskList`（`agent_id`=2）、`Team.TeamInfo`（`team_id`=3）、`Agent.FetchToolDetail`（`tool_call_id`=2 `detail_kind`=3 `ref_id`=4；`subscribe` 默认 false 省略 10）；未发明号；`listTools`/`listSkills` 本刀不碰。**A 槽 `d24-next-known-unaries-3` @ 2026-09-16 已转 bytes**（未关本行）：`Permission.CancelSessionGoal`（`session_id`=1；响应同 Resume）、`Permission.Respond`（`request_id`=2 `granted`=3 `metadata_json`=4；`granted` false 省略）、`Agent.Fork`（空 parent 仍 `root`）、`Agent.Kill`（`force` false 省略；空 agent 不默认 root）、`Agent.DeleteMessage`/`EditMessage`（空 agent 仍 `root`）；未发明号；`listTools`/`listSkills` 本刀不碰。**A 槽 `d24-queue-mutation-bytes` @ 2026-09-16 已转 bytes**（未关本行）：`_queueMutation` 17 方法走 `makeUnaryBytesClient`；QueueItemRef `item_id`=2 `op_id`=3（不按 JSON key 序）；Enqueue/Insert 不发明 6/7/8；Edit 不发明 `expected_version`=5；不解 ErrorDetail=5。`listTools`/`listSkills` 本刀不碰。**A 槽 `d24-git-unary-bytes` @ 2026-09-16 已转 bytes**（未关本行）：Git 六条 `ReadGitSummary`/`ReadGitChanges`（`session_id`=1）、`ReadGitFileDiff`（`path`=2 `index_state`=3）、`WriteGitStagePaths`（repeated `GitArgvCommand commands`=2，`argv`=1）、`WriteGitCommit`（`message`=2 `sign_off`=3 `amend`=4，bool false 省略）、`WriteGitApplyHunks`（repeated `argv`=2 `patches`=3）；响应走现 mapper。`listTools`/`listSkills` 当时不碰。**A 槽 `d24-listtools-listskills-bytes` @ 2026-09-16 已转 bytes**（未关本行）：`Tool.ListTools` 空 request（`category`=1 `include_hidden`=2 本仓 API 不传故省略）；响应 `tools`=1 `total`=2；`ToolSummary` `name`=1 `description`=2 `category`=3 `destructive`=4 `requires_permission`=5。`Tool.ListSkills` 空 request；响应 `skills`=1 `total`=2；`SkillSummary` `name`=1 `description`=2 `source`=3 `slash_enabled`=4 `enabled`=5。字段号来自 J 仓 `tool_service.proto`（与 Git 六条同一来源），decode 成现有 Wire 再 mapper。ToolInfo / SetSkillEnabled 当时未转。**A 槽 `d24-mcp-plugin-clipboard-bytes` @ 2026-09-16 已转 bytes**（未关本行）：MCP `ListMcpServers`（不发 `transport_filter`=1；`enabled_only`=2 false 省略；`work_dir`=3；transport 数字映射 `stdio`/`sse`/`streamable_http`）/`GetMcpServerStatuses`/`GetMcpServerTools`（`force_refresh` false 省略）；Plugins `List` 空 proto（`EMPTY_PROTO_MESSAGE`）/`Info`（`map` 仅解码 MapEntry key=1 value=2）；Clipboard Write/Read/List/Clear（TEXT type=0 省略 field 4）。**A 槽 `d24-mcp-plugin-mutator-bytes` @ 2026-09-16 已转 bytes**（未关本行）：MCP Toggle/Add/Update/Remove（`McpServerConfig` 1–8；transport enum 数字 1/2/3，0 omit；env = MapEntry key=1 value=2）；plugin Enable（`enabled !== false` 默认 true，false omit）/Reload/Unload/ScanNew（空 request `encodeEmptyProtoMessage()`）。**A 槽 `d24-question-clienttool-bytes` @ 2026-09-16 已转 bytes**（未关本行）：`Agent.RespondQuestion`（`session_id`=1 nested `QuestionResponse`=2：`question_id`=1 map `answers`=2 MapEntry key=1 value=2=`QuestionAnswer`/`selected_labels`=1 `custom_text`=3 空 omit）；`Agent.SendClientToolResponse`（`session_id`=1 nested `ClientToolResponse`=2：`call_id`=1 `is_error`=2 false omit `content`=3 `metadata_json`=4 repeated `CanvasRefData`=5 号 1–4，空 `source_hash` omit）。**A 槽 `d24-tool-leftover-unaries-bytes` @ 2026-09-16 已转 bytes**（未关本行）：`Tool.SkillInfo`（`skill_name`=1；响应 `name`=1 `description`=2 `source`=3 `content`=4，无 enabled）；`SetSkillEnabled`（`enabled` false omit；响应 `status`=3 UNSPECIFIED=0 omit / OK=1 / NOT_FOUND=2 / FAILED=3，OK→现 mapper `ok`）；`ToolInfo`（`tool_name`=1；响应 1–7 `aliases`）；`ListCommands` 空 request `encodeEmptyProtoMessage()` 仍 sendMessage；响应 `commands`=1 `total`=2，`CommandSummary` 1–8，`source` 解 varint 走现 mapper；`GetCommandDef`（`command_name`=1；响应 1–11 `skill_source`）。同刀 `Agent.DeleteAgentProfile`（`id`=1 响应 `success`=1）。未发明号；未转 `SaveSkillContent`（J `tool_service.proto` 无该 RPC）。**F 槽 `d24-memory-unary-wire` @ 2026-09-16 已写 wire+测**（未关本行）。**G 槽 `d24-file-unary-wire` @ 2026-09-16 已写 File 六条 wire+测**（未关本行）。**A 槽 `d24-memory-file-unary-bytes` @ 2026-09-16 已接线 bytes**（未关本行）：Memory Save/Search/SearchDeep/Read/List/Delete/Reflect/Revert/History 走 `grpcMemoryUnaryWire`（字段号来自 J 仓 `memory_service.proto`；decode 成现有 `Memory*Wire`；proto3 空/0/false omit）。`openRebuildMemoryStream` **已** bytes（A `d24-four-server-stream-bytes`）。File `ListFiles`/`ReadFile`/`GetFileInfo`/`WriteFile`/`ForceWriteFile`/`AgentMerge` 走 `grpcFileUnaryWire`（`content`/`base_content` 等 `encodeBytesField`；`WriteFileResponse.Status` SAVED=0 omit）。未发明号。未转 SaveSkillContent / Health / Connect / openContinuationStream。`MemorySearchResult.score`=4 double 仍 unread（D487）；mapper 缺 score 时 `requiredDouble`→0。未 catalog `onOpenConnection`。**F 槽 `d24-chatsync-unary-wire` @ 2026-09-16 已写 wire+测**（未关本行；A 已接线）：ChatSync/SyncInputDelivery 见 `grpcChatSyncUnaryWire.ts`；`attachments`=3 因 TS 无该字段未编；`delivery` 仅 1/2/3（0 omit；其余 omit 不猜）；optional bool false 写出 varint0。**G 槽 `d24-team-mutator-wire` @ 2026-09-16 已写 Team mutators wire+测（A 已接线；未关本行；不得写成 D482）**：`StartMember`（`member_name`=3 `preset_id`=4 `system_prompt`=5 `model_type`=6 `dynamic`=7 false omit；响应 `member_agent_id`=1 `member_name`=2 `dynamic`=3 **bool** 非 string）/`KillMember`/`CreateTeam`（repeated `task_descriptions`=3）/`Abort`（`team_id`=3 `reason`=4；响应 repeated `stopped_members`=3）/`TaskUpdate`（`task_id`=3 `new_status`=4 `message`=5）/`TaskCancel`/`MessageMember`（`content`=4）。ListTeams/MemberStatus/TaskList/TeamInfo 不重做。字段号来自 J `team_service.proto`。decode 对齐 grpcClient JSON→TS。**A 槽 `d24-chatsync-team-mutator-bytes` @ 2026-09-16 已接线 bytes**（未关本行）：ChatSync/SyncInputDelivery 走 `grpcChatSyncUnaryWire`，decode 后仍 `mapChatSyncResponse`/`mapSyncInputDeliveryResponse`；Team 七 mutator（StartMember/KillMember/CreateTeam/Abort/TaskUpdate/TaskCancel/MessageMember）走 `grpcTeamUnaryWire`，decode 已是 TS 结果（`ok`/`message`/`memberAgentId`/`dynamic: boolean`/`teamId`/`stoppedMembers`）不再 JSON `{success}` map。ListTeams/MemberStatus/TaskList/TeamInfo 不重做。未发明号。未转 SaveSkillContent/Health/Rebuild/Connect/openContinuationStream。未 catalog `onOpenConnection`。**F 槽 `d24-system-unary-wire` @ 2026-09-16 已写 System HealthCheck/Shutdown wire+测**（未关本行、当时未接线 `grpcClient`；A 已接线）：HealthCheck 空 request（非 JSON `{}`）；Shutdown `force`=1 `grace_period_ms`=2（false/0 omit）。见 `grpcSystemUnaryWire.ts`。禁止 Doctor/Connect。**G 槽 `d24-context-variable-unary-wire` @ 2026-09-16 已写 ContextVariable List/Read wire+测（当时未接 `grpcClient`；未关本行；不是 D482）**：List `session_id`=1 `agent_id`=2；响应 repeated `current`=1 `inherited`=2；Read `session_id`=1 `name`=2 `agent_id`=3；响应 `entry`=1。Summary 1–5 / Entry 1–5；`VARIABLE_GLOBAL`=0 omit。字段号来自 J `context_variable_service.proto`。decode 对齐 `mapContextVariableListResponse` / `mapContextVariableReadResponse`。**A 槽 `d24-health-contextvariable-bytes` @ 2026-09-16 已接线 bytes**（未关本行）：`System.HealthCheck`/`Shutdown` 走 `grpcSystemUnaryWire`，decode 后仍 `mapHealthCheckResponse`/`mapShutdownResponse`；`ContextVariable.List`/`Read` 走 `grpcContextVariableUnaryWire`，decode 后仍 `mapContextVariableListResponse`/`mapContextVariableReadResponse`。未发明号。未转 SaveSkillContent/Rebuild/openContinuationStream；TokenUsage/Config Get+Set wire 已落未接线；Watch stream 不转。未 catalog `onOpenConnection`。**F 槽 `d24-token-usage-unary-wire` @ 2026-09-16 已写 GetSessionUsage/GetGlobalUsage wire+测**（未关本行、未接线 `grpcClient`）：`session_id`=1；响应 `usage`=1；Global 请求空 payload（reserved 1–10 不写）；`TokenUsageData` 1–8。**G 槽 `d24-config-get-set-unary-wire` @ 2026-09-16 已写 Config Get/Set wire+测（未接 `grpcClient`；未关本行；不是 D482）**：Get `key`=1 `scope`=2 `session_id`=3；响应 map `values`=1（MapEntry `key`=1 `value`=2）`scope`=2；Set `key`=1 `value`=2 `scope`=3 `session_id`=4；响应 `success`=1 `message`=2。禁止 Watch。未做 SwitchModel / ListModels / ResolveModel / SetPermissionPolicy。字段号来自 J `config_service.proto`。decode 对齐 grpcClient JSON `{values,scope}` / `{success,message}`。**活 JSON（有调用点）**：`openRebuildMemoryStream` **已** bytes（A `d24-four-server-stream-bytes`）；ContinueGeneration / Regenerate / Resume **已** bytes（A `d24-continuation-stream-bytes`）；`saveSkillContent` 仍 JSON；TokenUsage GetSession/GetGlobal grpcClient **已** bytes（A 本波接线）；Config Get/Set grpcClient **已** bytes（A 本波接线）；SetPermissionPolicy/Doctor **已** grpcClient bytes；Watch stream 不转。**未用（不转）**：`Session.Info` 已是 bytes（roster/SessionBar 不转发，不重做）；`ChatSync` / `SyncInputDelivery` grpcClient **已** bytes（Gateway；decode 后仍 mapper）；Memory Rebuild **已** bytes（A `d24-four-server-stream-bytes`）。`SessionEventStream` 握手仍无 `client_id`、无 60s heartbeat uplink（host 未发/未期待则不发明）。`CreateSessionRequest` proto 无 `title`。`Agent.Chat` 响应只解 `session_id`/`agent_id`。**catalog / `IUniverseAgentConnection` 无 GetQueue / ListQueue**（Inbox 已诚实「Queue not listed」，不发明 list-queue RPC）**A 槽 `d24-clear-demo-fake-model-prefs-bytes` @ 2026-09-16 已转 bytes**（未关本行）：`Agent.ClearSessionDemoFake`（`session_id`=1；响应 `success`=1 `message`=2 `reason_code`=3）、`Config.GetModelPreferences`（`session_id`=1；响应 `min_level`=1 `max_cost`=2 `min_speed`=3 `strategy`=4）、`Config.SetModelPreferences`（`session_id`=1 optional `min_level`=2 `max_cost`=3 `min_speed`=4 `strategy`=5；响应 nested `preferences`=1）。proto3 空串/0 omit。未发明号。跳过 Connect / SaveSkillContent / Watch / Rebuild / ResolveTurn / SwitchWorkDir。**A 槽 `d24-continuation-stream-bytes` @ 2026-09-17 已转 bytes**（未关本行）：`Agent.ContinueGeneration` / `Regenerate` / `Resume` 走 `makeServerStreamBytesClient` + `decodeChatResponse`；Continue/Regenerate `session_id`=1 `agent_id`=2 `turn_id`=3 `message_id`=4；Resume `session_id`=1 `agent_id`=2。proto3 空串 omit。未发明号。Watch / Connect / SaveSkillContent / ResolveTurn / ResolveAnchor 仍 JSON。Rebuild 已 bytes。 | Create 恢复已下沉；ChatSync 是 Gateway 专用；本刀已转 Delete/Rename/Cancel/SetPermissionMode/SwitchModel 与 List/Create/Restore/Delete Snapshots、CancelToolCall/SetSessionGoal/MemberStatus/TaskList/TeamInfo/FetchToolDetail、CancelSessionGoal/Respond/Fork/Kill/DeleteMessage/EditMessage、SyncPermissionRule/PromotePermissionRule/GetSessionRules、Git 六条、`listTools`/`listSkills`、MCP list/status/tools、plugins list/info、clipboard 四条、MCP mutators / plugin mutators / respondQuestion / sendClientToolResponse / SkillInfo / SetSkillEnabled / ToolInfo / ListCommands / GetCommandDef / DeleteAgentProfile 已 bytes；未转 SaveSkillContent；Memory/File/ChatSync/Team mutators 已 bytes；A 已接线 HealthCheck/Shutdown + ContextVariable List/Read bytes（SaveSkillContent/Rebuild/TokenUsage/Config 仍 JSON）；F 已写 TokenUsage GetSessionUsage/GetGlobalUsage wire+测但 grpcClient 仍 JSON（未接线）；G 已写 Config Get/Set wire+测但 grpcClient 仍 JSON（未接线；Watch 不转）；GetQueue 不在本仓 proto 面 | 按实际打到引擎的 method 再改 bytes（须本仓已有该方法字段号）；`listTools`/`listSkills` 已 bytes；MCP list/status/tools、plugins list/info、clipboard 四条、MCP mutators / plugin mutators / respondQuestion / sendClientToolResponse / SkillInfo / SetSkillEnabled / ToolInfo / ListCommands / GetCommandDef / DeleteAgentProfile 已 bytes；未转 SaveSkillContent；Memory/File unary 已 bytes；ChatSync/SyncInputDelivery 与 Team 七 mutator **已** grpcClient bytes；HealthCheck/Shutdown + ContextVariable List/Read **已** grpcClient bytes；SaveSkillContent/Rebuild/TokenUsage/Config 仍 JSON；TokenUsage GetSession/GetGlobal **已** grpcClient bytes；Config Get/Set unary wire 已落、grpcClient 仍 JSON（未接线）；Watch stream 不转；`probeRpc` 已 empty proto；Delete/Rename/Cancel/SetPermissionMode/SwitchModel 与 List/Create/Restore/Delete Snapshots、CancelToolCall/SetSessionGoal/MemberStatus/TaskList/TeamInfo/FetchToolDetail、CancelSessionGoal/Respond/Fork/Kill/DeleteMessage/EditMessage 与 queue 17 mutation、SyncPermissionRule/PromotePermissionRule/GetSessionRules、Git 六条已 bytes。若日后 catalog 出现 GetQueue 再接线失败行 **A 槽 `d24-device-revoke-bytes` @ 2026-09-16 Device.Revoke grpcClient 已 bytes**（`device_id`=1；响应 `success`=1 `message`=2；未关本行）。**F 槽 `d24-trigger-unary-wire` @ 2026-09-16 Trigger 五 unary wire+测已落未接线**（List/Upsert/Delete/SetEnabled/Fire；跳过 RegisterSessionEngineTrigger；未关本行）。**A 槽 `d24-trigger-unary-bytes` @ 2026-09-16 Trigger 五 unary 现已在 grpcClient 走 bytes**（List/Upsert/Delete/SetEnabled/Fire；跳过 RegisterSessionEngineTrigger；未关本行）。**A 槽 `d24-session-lifecycle-bytes` @ 2026-09-16 已转 bytes**（未关本行）：`Session.Prewarm`（repeated `session_ids`=1；响应 repeated `entries`=1；`PrewarmSessionEntry` `session_id`=1 `outcome`=2 `message`=3；enum PrewarmSessionOutcomeProto 0–4）、`Shelve`/`Unshelve`/`Purge`（`session_id`=1；响应 `success`=1 `message`=2）、`Export`（`session_id`=1 `format`=2；响应 `content`=1 `format`=2）。未发明号。跳过 Connect / SaveSkillContent / GetModelPreferences / Watch / FireTriggerWebhook。**A 槽 `d24-fire-trigger-webhook-bytes` @ 2026-09-16 已转 bytes**（未关本行）：`Agent.FireTriggerWebhook`（`session_id`=1 `trigger_id`=2 optional `payload_json`=3 空/缺省 omit；响应 `status`=1 enum 0–4 `event_id`=2 `reason`=3）。同刀 `InstallSessionDemoFake`（`session_id`=1 `queues_payload`=2 bytes `content_type`=3 `playbook_id`=4；响应 `success`=1 `message`=2 `reason_code`=3）。未发明号。跳过 Connect / SaveSkillContent / GetModelPreferences / Watch / ResolveTurn / ClearSessionDemoFake。ClearSessionDemoFake / Get·SetModelPreferences **已** bytes（未关本行）。**A 槽 `d24-agent-five-unaries-bytes` @ 2026-09-16 已接线 bytes**（未关本行）：`Agent.SwitchWorkDir`（decode 后 `ok: wire.success === true` + previous/currentWorkDir/message）、`Compact`/`Todo`/`Usage`/`Status` decode 后仍 `mapCompactResponse`/`mapTodoResponse`/`mapUsageResponse`/`mapStatusResponse`。未发明号。跳过 Connect / SaveSkillContent / Watch / Rebuild / ResolveTurn / ResolveAnchor / Pause / History / Back / Prune / Branch / Reset / TestModelProfile。Status `children`=8 已解码、`model_info`=9 仍 unread。**A 槽 `d24-seven-agent-unaries-bytes` @ 2026-09-16 已接线 bytes**（未关本行）：`Agent.Pause`/`History`/`Back`/`Prune`/`Branch`/`Reset`/`TestModelProfile`。params 仍 unread。跳过 Connect / SaveSkillContent / Watch / Rebuild / ResolveTurn / ResolveAnchor / SuspendLoop。七条 loop/tool 已 grpcClient bytes（Suspend/Resume/StopLoop/RunToolBG/StopShellTask/ListLoopSnapshots/FetchToolUsageDetail；context_sources 已 decode）。仍勿关本行。 C–I 本波 wire 已落未接线：GetRemoteSessionStatus(pending 6/7 已 decode)/GetRemoteSessionHistory(messages unread)/CreateRemoteSession(SessionParams known)/GetUploadProgress/ResolveModel(ModelEntry unread)/SaveConfig(config nested scalars；connection_test unread)。仍勿关本行。 **A 槽 `d24-six-unaries-bytes` @ 2026-09-16 已接线 bytes**（未关本行）：SaveConfig / CreateRemoteSession / GetRemoteSessionStatus（pending 6/7 已 decode）/ GetRemoteSessionHistory（messages unread）/ GetUploadProgress / ResolveModel（ModelEntry unread）。未发明号。跳过 Connect / SaveSkillContent / Watch / Rebuild / ResolveTurn / ResolveAnchor。仍勿关本行。 **A 槽 `d24-continuation-stream-bytes` @ 2026-09-17**：ContinueGeneration / Regenerate / Resume 已 grpcClient bytes。仍勿关本行。  **本波 @ 2026-09-17（未关本行）**：SubscribeToolDetail / Memory Rebuild / RemoteChat / DownloadAttachment **已** grpcClient `makeServerStreamBytesClient`（A `d24-four-server-stream-bytes`；decode + 现 mapper）。UploadAttachment **已** grpcClient bytes（A `d24-upload-attachment-bytes`）；Status `children`=8 已递归解码、`model_info`=9 仍 unread；FetchToolUsageDetail `context_sources`=3 已 decode；GetRemoteSessionStatus pending 6/7 已 decode。Connect / SaveSkillContent / Watch / ResolveTurn / ResolveAnchor 仍 JSON。  **本波 @ 2026-09-17（未关本行）**：SubscribeToolDetail / Rebuild / RemoteChat / DownloadAttachment 已 grpcClient bytes；UploadAttachment **已** grpcClient bytes（A `d24-upload-attachment-bytes`）；GetRemoteSessionHistory `messages`=1 已 decode；ResolveModel nested ModelEntry 1–9 已 decode；SaveConfig `connection_test`=3 标量 1–4 已 decode；CheckConnection `capabilities`=5 `errors`=6 `load`=7 已 decode；TestModelProfile `params`=6 MapEntry 已 encode；GetNode `capabilities`=7 `load`=8 已 decode。Connect / SaveSkillContent / Watch / ResolveTurn / ResolveAnchor 仍 JSON。  **本波 @ 2026-09-17（未关本行）**：UploadAttachment 已 grpcClient `makeClientStreamBytesClient`；同胞 scan 已对齐 Download bytes；ListNodes `capabilities`=7 `load`=8 已 decode（复用 GetNode）；RemoteAgentConfig nested `endpoint`=5 `auth`=6 `default_permission_delegate`=10 `health_check`=11 已 Get/List decode + Save encode。Connect / SaveSkillContent / Watch / ResolveTurn / ResolveAnchor 仍 JSON。 **F 槽 `d24-checkconnection-share-decode` @ 2026-09-17**：CheckConnection nested `capabilities`/`errors`/`load` 空 repeated 对齐 GetNode/ListNodes（`undefined` 非 `[]`）；复用 GetNode `decodeCapabilities`/`decodeLoadMetrics`；GetNode `RemoteAgentInfoWire` 改 import catalog。未发明号。未关本行。未转 Connect / SaveSkillContent / Watch / ResolveTurn / ResolveAnchor。**A 槽 `d24-usage-nested-10-11` @ 2026-09-17**（未关本行）：Usage `context_window`=10 + `session_usage`=11 已 decode；仍 `mapUsageResponse`。Connect / SaveSkillContent / Watch / ResolveTurn / ResolveAnchor / Pty 仍 JSON。`model_info`=9 仍 unread。**D 槽 `d24-resume-root-agent` @ 2026-09-17**（未关本行）：Session.Resume `root_agent`=3 AgentInfo 1–8 已 decode；`model_info`=9 仍 unread。 | platform / universeAgent | open || D25 | P2 | **roster 不再远程 Create**（空 catalog 只 pending，host lease 唯一 Create）。**host leftover 已收（工位 A `ghost-bind-failed-ui` @ 2026-09-07）**：List 抛错 / ghost 行 lease bind 失败后 roster 与 `getActiveSession` 显示 bind-failed，不再回落 stub「Untitled session」或 `conversationLens.sessionNew`「New session」（SessionBar / StatusBar 读同一标题）。**引擎半已在 UA 闭（2026-09-10 · `7e498e7092` · `D-SESSION-STORE-MIGRATION-STUCK-1`）**：`ensureSchema` 仍做旧库兼容（`SCHEMA_VERSION=7` 版本门控 9 段增量，不是「只认新 schema」）；修的是半途失败自愈。`LookupFailed` deny 未放宽。见 [report](../reports/engine-session-store-migration-stuck-2026-09-09.md) §9。**2026-09-12 复验 PASS**（grpcurl `-insecure` @ `127.0.0.1:50061` PIN 已换）：List 先空列表（store 打开），Create 后再 List `total=3` 含 `session-100`/`session-101`/新 id。日志无 `Query does not return results`。本仓不改引擎代码 | host 面已收；引擎自愈已合；换钉复验已做 | List 可读且旧卡住目录出现在名单；与 D26 同批关 | conversation / engine | closed || D26 | P2 | **引擎 store 迁移半途卡死（host 已收；病因 2026-09-09 改口；引擎修法 2026-09-10 已合 UA）**：host Tree+recover 不变。**2026-09-09 根因仍成立**（[report](../reports/engine-session-store-migration-stuck-2026-09-09.md)）：卡住态指纹 + xerial 写语句走 `executeQuery` + `user_version` 停 0。**2026-09-12 账更正**：UA 未砍旧库迁移、未改「只认新 schema」。`SCHEMA_VERSION=7`，`ensureSchema` 仍版本门控；半途失败自愈（每段 stamp + `driver.execute`）已闭 `D-SESSION-STORE-MIGRATION-STUCK-1`（`7e498e7092`）。`LookupFailed` deny **未放宽**。**vscode 旧账过期**。**2026-09-12 复验**：Create 新 id PASS；旧 `session-100`/`session-101` 打开成功（非 `ALREADY_EXISTS`）；Info 可读。日志无 `Failed to open message store` / `ALREADY_EXISTS`。当时 Chat 另卡空 model catalog（`MODEL_PROFILE_REQUIRED` / `UNRESOLVED`），**不是**本行 store 症状，不挡关本行。**同日稍后** seed `provider:state` + 重启后 grpcurl Chat **PASS**（无 `MODEL_PROFILE_*`）。仍不升 PRD-008 | 引擎修法已合；换钉复验 store/Create 已做。禁止放宽 deny / 清 `.sessions` 当主线 | **原闭合条件「Create 先写 meta」仍撤回。** store 能打开 + Create 不再因迁移回 6 | engine | closed |  F/G/H/I/J wire 已接线 bytes：SwitchWorkDir / Compact / Todo / Usage / Status（children/model_info unread）。仍勿关本行。  C–J 本波七条已接线 bytes：Pause / Agent.History / TestModelProfile(params unread) / Back / Prune / Branch / Reset。仍勿关本行。  C–J 本波 wire 已落未接线：SuspendLoop / ResumeLoop / StopLoop / RunToolInBackground / StopShellTask / ListLoopSnapshots / FetchToolUsageDetail(context_sources unread)。仍勿关本行。 **A 槽 `d24-shell-remote-seven-bytes` @ 2026-09-16**：七条 SendShell/ListNodes/GetNode/Set·ExitMaintenance/ResetError/ListConfigs 已 grpcClient bytes。仍勿关本行。 **A 槽 `d24-remote-seven-bytes` @ 2026-09-16**：七条 GetConfig/DeleteConfig/Reload/Destroy·Cancel·Resume/CheckConnection 已 grpcClient bytes。仍勿关本行。 **A+I 槽 `d24-sessionstream-30-50` @ 2026-09-17**：SessionStream `streaming_delta`=30 + `permission_request`=50 已 decode。仍勿关本行。 **A 槽 `d24-sessionstream-51` @ 2026-09-17**：SessionStream `ask_user_question`=51 已 decode。仍勿关本行。 **I 槽 `d24-sessionstream-52` @ 2026-09-17**：SessionStream `client_tool_call`=52 已 decode。仍勿关本行。
| D27 | P3 | **Lens 编辑态 1px 垫高**：整树 `setChildren` 已核验收口。本轮 `provideTurnEditComposer` 在 `setEditingTurnId` 前把 composer 交给 renderer，edit-host 测量前已挂入；已删 `minHeight = '1px'` | 工位 B `lens-edit-host` 收口 | 渲染时 composer 已在 host 内，再删 1px 下限后 `conversationLens.test.ts` 仍无 ListView 0px console | M6 / conversation | closed |
| D19 | P2 | **L1 源码复核残留**（[a11y-rwd-l1.md](a11y-rwd-l1.md)）：(1) Engine/Connection **无动画节点**，不挂 `.ua-motion`；(2) T1 HC 已覆盖 Preferences pane 与 Visualize overlay；(3) Connection 300px 已有分区导航 + Back。**(4) Web 省略门控与点名文案已在 E2-1 收口** | 三项源码残留已收；手测/axe 仍归 D17 | 手测/axe 记 D17，不重开本行 | M7 a11y | closed |
| D20 | P2 | **CS-6 Settings 默认窗 300px 目视**：`uaClientSettingsChrome.css` 已保证 narrow-width 下搜索框与 group title 不 `display:none` 且可省略；`settingsUaToc` 有合同测。本机隔离 launch 因 `@grpc/grpc-js` 缺失未能开窗目视 | 代码完成线不阻塞；活窗目视仍欠 | 隔离 profile 打开默认窗 Settings，缩到约 300px，确认搜索框与 Client 组标题仍可见、无 emptyCopy；失败记入 D17 | M7 verification | open |
| D28 | P2 | **Connection pane 旁路 UI**：(1) **已由 loop/C `identity-strip-occlusion` 收口**：Connection 模态 `layout` 按身份条底边写 `--ua-conversation-identity-reserved-top` 并下移 host（布局预留，非 z-index）；`connectionPreferencesPane.test.ts` 断言 host 盒与身份条不重叠。**(2)(3) 仍闭**：`writeConnectStatus` 写当前可见区（Devices → `.connection-hub-devices-status`）；Connect 成功/配对中用可读文案，不甩 `ok=true pairingPending=…`、不装已连接。未改 session bar chrome | 身份条布局预留 + 几何/契约测已落；未做活窗目视 | 身份条不被挡；几何测在重叠时失败；(2)(3) 保持 closed | UI / conversation | closed |
| D29 | P2 | **`enqueueMessageQueueItem` 无 Inbox/Composer 入队 UI**；断连缓存拒收。`conversation-disconnect-send` 已把断连 Send 接到该 API | Inbox 队列头 Enqueue 已落地 `b384a1dcf9e` | Inbox/Composer 能看见入队项 | UI / conversation | closed |
| D30 | P3 | **Inbox FAILED 行无重试钮**（时间线 retryable 行已接 ContinueGeneration @ `583b004a8b0`） | Inbox overlay `renderQueueItem` 已挂 Retry → `retryMessageQueueItem`；失败行仍可操作。引擎 roster unary 见 D37 | Inbox FAILED / UPLOAD_FAILED 行有 Retry，点击转发 roster API，失败不退化成死徽章 | conversation | closed |
| D37 | P3 | **引擎 roster 未 override `retryMessageQueueItem`** 到 `AgentService.RetryQueueItem` / `RetryQueueItemUpload`。接通后无 GetQueue，失败行本就不可见 | Retry unary 已由 C 槽 `roster-queue-retry` 收口。`inbox-getqueue-honesty` 已核对 catalog / connection **无** GetQueue / ListQueue，未发明 RPC；接通 Inbox 文案改为「Queue not listed」，stub fixture 不得冒充引擎队列。活引擎失败行仍不可见属 D24-adjacent（缺 list-queue unary），不重开本行 | `ConversationEngineRosterService` 接通后按 `upload` 转发对应 unary；空 item / 断连缓存 / 无 hook 不发；补 roster 测 | conversation | closed |
| D41 | P3 | Inbox `FAILED` 与 `UPLOAD_FAILED` 行共用 CSS class `upload-failed`，DOM 无法区分发送失败与上传失败 | C 槽 `inbox-fail-class`：行 class 已拆 `queue-failed` / `upload-failed`；Retry 仍 `upload: status === 'UPLOAD_FAILED'`；未发明 GetQueue | `conversationInboxOverlay.ts` 两状态行 class 互斥；`conversationInboxOverlay.test.ts` 断言 DOM 可区分且 FAILED 不再标 `upload-failed` | conversation | closed |
| D42 | P3 | **Maximize 后切 Trajectory 虚列表铺行**。CSS 只藏 `.conversation-lens-timeline`；忽略 0 尺寸以免 `layout(0)` 卸行。merge compile 后 T5a CSS+铺行两测绿 | merge `d6d0b7399db` 复证 2/2 | 最大化后切 Trajectory 仍能见 `untitled-u1` | conversation | closed |
| D31 | P3 | **Sources git 次级面**：Review 列表读失败 / Panel 写门控已在前刀。`sources-leftover-honesty` 收 Changes 已暂存 git 行 Unstage 不可用诚实态与 Review 列表打开 diff 失败。本刀（`review-open-selected`）收 `sourcesReviewCommands.contribution.ts` Open Selected 空 catch：失败走 `sourcesGitDiffOpenFailureMessage` + `ISourcesReviewListHost.setStatusMessage`，不标已审。未发明 `WriteGitUnstage`。**本刀（`review-git-read-throw`）**：Review / Changes 挂载后 `readGitChanges` throw 的 status DOM 已断言 `sourcesGitReadFailureMessage` / `Unable to read git changes:`。**本刀（`review-list-open-diff-dom`）**：Review / Changes 列表 `onDidOpen` 在 open-diff throw 后挂载断言 status DOM（`sourcesGitDiffOpenFailureMessage` / `Unable to open diff:`；Review 不标已审）。**本刀（`changes-stage-commit-dom`）**：Stage Selected / Commit throw 的 status DOM 已挂载。**本刀（`unstage-status-dom`）**：Unstage Selected throw 的 status DOM 已挂载。**本刀（`stage-commit-ok-false-dom`）**：Stage Selected / Commit 钩子 **返回** `{ supported:true, success:false, errorMessage:'denied' }`（不 throw）的 status DOM 已挂载（既有 localize + `denied`）。Review `runGitAction` throw notice 见 [D75](#d75)。未发明 WriteGitUnstage。**F4 隔离 profile 冒烟未跑**。不升 PRD-009/023。[R8](research-queue.md) **已闭**。Accept **A1 已落**。**P5 停线**（工位 D `a2-patches-source`）：独立审查只批准停线，不批准 A2。现有读面无 apply stdin；禁止只接 session；禁止把 `unified_diff`（缓存或再拉）或 Stage argv 映射到 Apply。**A2 仍 blocked**，须**新**选定设计 + **新**独立 Arch-First，不是「出现载荷就自动开」。见 [sources-accept-empty-success](../plans/sources-accept-empty-success.md)（`draft`）与 [ADR-008](../decisions/008-write-git-apply-hunks-empty.md) | 禁 F4 / 禁升 PRD；P5 停线；A2 须新选定 + Arch-First | F4 V-F1–V-F7 证据目录；A2 仅在新选定 + 新 Arch-First 之后（本停线不是解锁） | sources-git | open |
| D32 | P3 | **error retry 不经 lease.post / Actor `continueGeneration`**：Write 增 `continueGeneration`；host 映射 localFact；Lens Retry 走 `lease.post`；roster 不再直开 ContinueGeneration | A 槽 `host-write-retry` | `ConversationWriteMessage` 增加 `continueGeneration` 且 host 映射到 localFact；UI 改走 `lease.post`；补 host 流句柄与 UI 打开不打架的测 | M7 conversation | closed |
| D33 | P2 | **配对中 chip 可能误开 Engine 页**（已收口 @ 2026-09-07 工位 B）：`getEngineStatusCommandId(phase, pairingPending)` 与 `isConversationEngineLive` 同闸；StatusBar / IdentityStrip 传入 `snapshot.pairingPending` | 决策核实时发现；号原误写成 D24 | chip 在 pairingPending 时指向 Connection/SAS，不打开 Engine | M7 conversation | closed |
| D34 | P3 | **`getVisibleTimelineIndices` 空树 / 零高 layout 读 `lastVisibleElement` 抛错**：`AbstractTree.lastVisibleElement` 无 bounds check；`ListView.lastVisibleIndex` 在 `renderHeight=0` 为 -1。已改为 `renderHeight < 1` / 空 `flatItems` 守卫，不再裸 try/catch | 工位 B `timeline-hygiene` 收口 | 空树与已铺行后 `layout(0)` 不抛；`conversationTimelineApplyTree.test.ts` D34 测绿 | M6 / conversation | closed |
| D35 | P3 | **`getTimelineRowElement` 第二段 `querySelector([data-turn-id])` 死代码**：第一段选择器已含 `[data-turn-id]`。已删第二段 | 工位 B `timeline-hygiene` 收口 | 只留一条 `[data-turn-id], [data-fold-id]` 查询 | M6 / conversation | closed |
| D36 | P3 | **standalone `thinking`/`tool` 分支画假 process 行**：`conversation-lens-turn-process` + summary 像 fold。已改为诚实摘要行（header+body，无 fold chrome） | 工位 B `timeline-hygiene` 收口 | 无 `.conversation-lens-turn-process` / `.conversation-process-fold`；`conversationTimelineRenderer.test.ts` D36 测绿 | M6 / conversation | closed |
| D43 | P3 | **`ConversationPart.layout` 不把尺寸扇出到 conversation editor parts**。`createConversationEditorPart` 已做首次 layout（host 有尺寸用 host，否则 800×600），否则 `findGroup`/`splitSessionWindow` 会在 `getNeighborViews` 上抛 `Can't call getNeighborViews before first layout`。chrome 后续 resize / `ConversationPart.layout` 仍不会再 layout 各 `ConversationEditorPartImpl`，活窗拆列后改窗口大小网格可能不更新 | D 槽 S4/S5 只收首次 layout 合同；扇出属另一冲突域，本 slice 不扩 | `ConversationPart.layout`（或 session window service）按叶 host 尺寸调用各 conversation editor part.layout；补 resize 测 | M7 conversation | closed |
| D44 | P3 | **`registerUaPreferencesNavigationActions()` 已幂等（工位 B 本刀）**：已注册的 `openConnectionPreferences` / `openEnginePreferences` 直接跳过；二次调用不抛。生产仍只 `conversation.contribution.ts` 调一次；未发明新命令 | 本刀已收 | 该函数对已注册 id 直接返回；二次调用不抛；生产仍只 contribution 调一次 | conversation | closed |
| D45 | P3 | **`openResidentChat` `open.call` / `openStream` `subscribeSessionEventStream` 无总 catch**（与已修 `openContinuation` 同类）：bind 后 throw-on-open 会从 `drainIntents` 抛出，`whenEngineSessionReady` 拒收，后续 `ensureChatStream` 被打断 | A 槽 `host-open-catch` 已补同级 warn catch；Chat 仍 echo `chatStreamUp` 走 one-shot；测锁 throw-on-open。未关 D16；未转 listTools；未发明 heartbeat `client_id`；未碰 Create / `.sessions` | `open.call` / `subscribeSessionEventStream` 抛错只 warn、不拒 ready；Chat 仍 echo Up；`sessionViewHostChatClose.test.ts` throw-on-open 绿 | M7 universeAgent | closed |
| D46 | P3 | **Tools/Agents list RPC throw 后不 `clearCatalogPresentation`**：`engineToolsSection.refresh` / `engineAgentsSection.refresh` catch 只改 `mode=failed` 并画 error status，不卸已成功行。首拉 throw 仍 0 行（本刀已锁）；成功后再 Retry 失败可能 leftover 假 catalog | 本刀已收：catch 先清行再 `failed`；补成功→throw 测 | catch 先清行再 `failed`；补「成功 → throw」测：`getMode()==='failed'` 且 `getListEntryCount()===0` | conversation | closed |
| D47 | P3 | **`drainIntents` / `closeStream` `dispose` 无总 catch**：linger / connectionDown / overflow 同批先 `closeStream` 再 `closeChatStream`；`subscription.dispose()` → gRPC `call.end()`/`cancel()` 抛错会中断后续 intent，Chat 句柄泄漏。不拒 `whenEngineSessionReady`（close 不在 ready 路径）。未包整圈 `drainIntents` | A 槽 `host-close-stream-dispose-catch` 已补与 open 同级 warn catch，仍 `streams.delete`；测锁 throw-on-dispose。未关 D16；未转 listTools；未碰 D46/catalog；未跑 F4 | `closeStream` dispose 抛错只 warn、不跳过同批 `closeChatStream`；`sessionViewHostChatClose.test.ts` throw-on-dispose 绿 | M7 universeAgent | closed |
| D48 | P3 | **其余 host `dispose()` 无总 catch**（D47 只包 `closeStream`）：`closeResidentChat` `stream.dispose()`、`onEngineConnectionChanged` 三圈（event / Chat / continuation）、`openResidentChat` `existing?.dispose()`、`openContinuation` 旧句柄 dispose 抛错会跳过后续 delete / unload / `chatStreamDown` / `connectionDown`。不包整圈 `drainIntents` | A 槽 `host-remaining-dispose-catch` 已补与 D47 同级 warn catch，仍 delete / unload / 仍 post；测锁 linger Chat throw-on-dispose 与断连 event-stream throw。未关 D16；未转 listTools；未发明 heartbeat `client_id`；未碰 Create / `.sessions`；未跑 F4 | `closeResidentChat` / 断连三圈 / open 旧句柄 dispose 抛错只 warn；Chat 仍删；`connectionDown` 仍 post；`sessionViewHostChatClose.test.ts` linger Chat throw-on-dispose 与 disconnect event-stream throw 绿 | M7 universeAgent | closed |
| D49 | P3 | **MCP/Skills list RPC throw 后不 `clearCatalogPresentation`**：`engineMcpSection.refresh` / `engineSkillsSection.refresh` catch 只改 `mode=failed` 并画 error status，不卸已成功行。首拉 throw 仍 0 行（本刀已锁）；成功后再 Retry 失败可能 leftover 假 catalog | 本刀已收：catch 先清行再 `failed`；补成功→throw 测。未关 D16；未转 listTools；未发明 GetQueue | catch 先清行再 `failed`；补「成功 → throw」测：`getMode()==='failed'` 且 `getListEntryCount()===0` | conversation | closed |
| D50 | P3 | **composer catalog 成功后再 throw leftover**：`loadConnectedComposerCatalogs` agent/model catch 原是空注释；成功 load 后再 throw 会留下旧 select / `catalogModelIds`。tools catch 已清 `catalogToolNames`。首拉 throw 仍 No agent / No model（本刀已锁） | 本刀已收：agent throw 重置 No agent；model throw 重置 No model / `catalogModelIds=['']` / selectedIndex=0。补成功→throw 测。未关 D16；未转 listTools；未发明 GetQueue | agent/model throw 回到首拉诚实空；补「成功 → throw」测：options 不得保留上一轮 catalog | conversation | closed |
| D51 | P3 | **host `writeChat` 丢 `writeId` 且 success 用非法 `'written'`**：`chatStreamWrite` 带 writeId；host `mark()` 用 `'written'` 被 `isInputDeliveryFact` 拒收，且缺 `source: host-write-accepted` / `writeId` / `chatAttemptId`，permission inflight cleanup 永不跑 | A 槽 `host-write-receipt` 已收：`handleIntent` 传入 `writeId`；`mark()` 只用 accepted/failed 并盖 `HOST_WRITE_RECEIPT_SOURCE` / `writeId` / `chatAttemptId`。测锁 perm-live 成功 `removePendingAction`、write throw `pendingRespondFailed`（`hostWriteFailed`）且无未处理 rejection。未关 D16；未发明 heartbeat `client_id`；未碰 Create / `.sessions` / proto / F4；未包整圈 `drainIntents` | `handleIntent` 传入 writeId；receipt 为 accepted/failed 且带 source+writeId+chatAttemptId；permission_request perm-live → post allow → resident write 成功出 `removePendingAction`；write throw 出 `pendingRespondFailed` `hostWriteFailed` 且无未处理 rejection | M7 universeAgent | closed |
| D52 | P3 | **one-shot `connection.chat()` 只在 `onResponse` 标 accepted**：`TestConnection.chat()` 空 `async chat() {}`；生产 `makeBidiBytesClient` 可 `'end'` 且 0 `'data'`。无 `inputDelivery`，perm-live 座永不清 | A 槽 `oneshot-chat-receipt` 已收：no-resident `writeChat` `mark` 至多一次；await 后未 mark → accepted；catch 未 mark → failed。测锁无 callback 仍 `removePendingAction`、chat throw 仍 `pendingRespondFailed` `hostWriteFailed`。未关 D16；未发明 heartbeat `client_id` / GetQueue；未碰 Create / `.sessions` / proto / F4；未包整圈 `drainIntents` | no-resident `writeChat` mark 至多一次；chat resolve 无 callback 仍 accepted / 清座；chat throw 仍 failed / `hostWriteFailed` | M7 universeAgent | closed |
| D53 | P3 | **Team `memberStatus` / `taskList` 无 catch**：`refreshTeamData` 仅 `teamInfo` 有 catch；后两 unary 裸 await。Caller 是 `void this.refreshTeamData()`。成功画出 Alice 后再 throw 会 leftover 活行（非断连、无 stale note）且可未处理 rejection | A 槽 `navigator-team-leftover` 已收：catch 后清 leftover 并写失败 note；断连 leftover + stale-note 合同未改。测锁 success→throw。未关 D16；未跑 F4；未开 A2 | catch `memberStatus` / `taskList` throw；清 leftover 行；写失败 note（非 stale-note）；断连 leftover + stale-note 不变；`navigatorTeamSubviews.test.ts` success→throw 绿 | M7 navigator | closed |
| D54 | P3 | **Snapshots Restore/Delete 失败无 write-status**：`restoreThenRefreshList` / `deleteThenRefreshList` 吞 `ok:false` 与 throw 且不刷新（leftover 行是合同）；overlay 原无失败行，且不得用 `paintStatus`（会 reset body 卸行） | B 槽 `snapshots-write-status` 已收：panel 独立 write-status（不 unload 行）；`ok:false` / throw 画 `Unable to restore:` / `Unable to delete:`；测锁 status DOM + 行仍在。未关 D16；未发明 GetQueue / proto；未碰 F4 / 引擎 / `dev/loop` | overlay write-status 在 Restore/Delete `ok:false` / throw 可见且行仍在；`paintStatus` 不用于写失败；fail 仍不 refresh | conversation | closed |
| D55 | P3 | **MCP Add/Update/Remove 失败无 catalog status**：`addServer` / `updateSelectedServer` / `removeSelectedServer` 已 catch / `!result.ok` 并 `return false`；按钮 `void this.addServer()` 等。失败无 status（Plugins 已有 `showWriteFailed`）。末次 catalog 行应保留，不得 `clearCatalogPresentation` | B 槽 `mcp-write-status` 已收：写失败画 catalog status（不卸行、不 refresh）；测锁 status DOM + 行仍在。未关 D16；未发明 GetQueue / proto；未碰 F4 / 引擎 / `dev/loop` | catalog write-status 在 Add/Update/Remove `ok:false` / throw 可见且行仍在；不 `clearCatalogPresentation`；fail 仍不 refresh | conversation | closed |
| D56 | P3 | **Skills `createSkill` 失败无 write-status**：`createSkill` 在 `{ ok:false }` / throw 只 `return false`，不画 status；`saveSelectedSkillBody` 已画 `.engine-skill-body-status`。失败不得造假行、不得改选中、不得 `clearCatalogPresentation` | A 槽 `skills-create-status` 已收：fail 画 body/toolbar write-status；测锁 status DOM + 无假行。未关 D16；未发明 GetQueue / proto；未碰 F4 / 引擎 / `dev/loop` / MCP | create fail 可见 write-status；无假行、不改选中、不清 catalog 行；`engineSkillsSection.test.ts` ok:false / throw 绿 | conversation | closed |
| D57 | P3 | **Agents create/delete/reset / save 失败无 catalog write-status**：`createProfile` / `deleteSelectedProfile` / `resetSelectedProfile` / `saveSelectedProfile` 在 `ok:false` / 空 id / throw 只 `return false`。失败不得 refresh、不得改选中、不得 `clearCatalogPresentation`。`saveAgentsMarkdown` 应复用已有 `.engine-agents-editor-status`。Tools enablement 不在本刀 | B 槽 `agents-write-status` 已收：toolbar 写失败画 `.engine-catalog-write-status`；markdown 保存失败复用 editor-status；测锁 status DOM + 无假行。未关 D16；未发明 GetQueue / proto；未碰 F4 / 引擎 / `.sessions` / `dev/loop`；未做 Tools enablement | create/delete/reset / save fail 可见 write-status；无假行、不改选中、不清 catalog、不 refresh；`engineCatalogSections.test.ts` ok:false / throw 绿 | conversation | closed |
| D58 | P3 | **Tools enablement 保存失败无 catalog write-status**：`savePendingEnablement` / `toggleTool` 在空 id / throw 只 `return false`。失败不得 refresh、不得卸行、不得 `clearCatalogPresentation`；pending enablement dirty map 应保留 | B 槽 `tools-write-status` 已收：写失败画 `.engine-catalog-write-status`；测锁 status DOM + 行仍在 + dirty 仍在。未关 D16；未发明 GetQueue / proto；未碰 F4 / 引擎 / `.sessions` / `dev/loop`；未重做 Agents/MCP write-status | save / toggle fail 可见 write-status；行仍在、不 refresh、dirty map 保留；`engineCatalogSections.test.ts` 空 id / throw 绿 | conversation | closed |
| D59 | P3 | **Skills `toggleSkill` 失败无 write-status**：`setSkillEnabled` 在 `{ ok:false }` / throw 只 `await this.refresh()`，不画 status。失败不得清 catalog、不得改选中。refresh 回退 checkbox 可以，但须可见 write-status | A 槽 `skills-toggle-status` 已收：fail 画 `.engine-skill-write-status`（及 body status）；测锁 status DOM + 行仍在。未关 D16；未发明 GetQueue / proto；未碰 F4 / 引擎 / `.sessions` / `dev/loop` | toggle fail 可见 write-status；不清 catalog、不改选中；`engineSkillsSection.test.ts` ok:false / throw 绿 | conversation | closed |
| D60 | P3 | **MCP `toggleServer` 失败无 write-status**：`toggleMcpServer` 在 `{ ok:false }` / throw 只 `await this.refresh()`，不画 status。失败不得清 catalog、不得卸行。refresh 回退 checkbox 可以，但须可见 write-status | B 槽 `mcp-toggle-status` 已收：fail 画 catalog write-status（`showWriteFailed`）；refresh 回退 checkbox；测锁 status DOM + 行仍在。未关 D16；未发明 GetQueue / proto；未碰 F4 / 引擎 / `.sessions` / `dev/loop`；未重做 Add/Update/Remove | toggle fail 可见 write-status；不清 catalog、不卸行；`engineCatalogSections.test.ts` ok:false / throw 绿 | conversation | closed |
| D61 | P3 | **Connection `listDevices` / `listPending` throw 后写成空列表**：`refreshEngineDevices` / `refreshEnginePending` catch 原写 `enginePairedDevices = []` / `pendingPairs = []`。成功后再 throw 会 leftover 成静默空，不像失败 | D 槽 `connection-list-leftover` 已收：throw 保留末次快照并画失败 note（`.connection-hub-devices-status` / pending empty / `hubDirectoryBanner`）。未关 D16；未发明 GetQueue / proto；未碰 F4 / 引擎 / `.sessions` / `dev/loop` / D26 / D60 | throw 保留末次快照（或失败 note 可见）；`connectionPreferencesPane.test.ts` success→throw 绿 | conversation | closed |
| D62 | P3 | **Connection `renameDevice` / 成功后 `refreshDirectory` 无 try/catch**：`handleRenameSelectedDevice` 的 `!result.ok` 已画 `hubDirectoryBanner`，但 throw 会成未处理 rejection。Rotate/revoke 同文件已 catch 并画 banner。login/changePassword 不在本刀 | A 槽 `hub-rename-throw` 已收：catch throw 画 `hubDirectoryBanner`。测锁 rename throw 画 `.connection-hub-directory-banner`。未关 D16；未发明 GetQueue / proto；未碰 F4 / 引擎 / `.sessions` / `dev/loop`；未做 login/changePassword | rename / 成功后 refreshDirectory throw 画 banner；`connectionPreferencesPane.test.ts` rename throw 绿 | conversation | closed |
| D63 | P3 | **Agents Tools 页 `listTools` throw 后画 empty**：`ensureAgentToolsLoaded` catch 写 `agentTools = []`；`renderAgentTools` 在 connected 下把空列表画成 empty，接通后 throw 不成 failed | B 槽 `agents-tools-leftover` 已收：`agentToolsLoadFailed` 跟踪原因；`toolsStatus` `mode:'failed'` + `getCatalogFailedCopy(AGENT_TOOLS_FEATURE, reason)`；`clearCatalogPresentation` / 成功 load 清旗。测锁 tools 页 failed widget。未关 D16；未发明 GetQueue / proto；未碰 F4 / 引擎 / `.sessions` / `dev/loop` / D26 | catch 后 `.engine-agents-tools-panel .engine-catalog-status-widget[data-catalog-mode="failed"]`；`engineCatalogSections.test.ts` listTools throw 绿 | conversation | closed |
| D64 | P3 | **Connection `login` / `changePassword` 无 try/catch**：`handleLogin` / `handleChangePassword` 的 `!result.ok` 已画 `hubAuthBadge`，但 throw 会成未处理 rejection（void click handler）。rename 不在本刀 | A 槽 `hub-login-throw` 已收：catch throw 画 `hubAuthBadge`。测锁 login / changePassword throw 画 `.connection-hub-auth-badge`。未关 D16；未发明 GetQueue / proto；未碰 F4 / 引擎 / `.sessions` / `dev/loop`；未重做 D62 rename | login / changePassword throw 画 hubAuthBadge；`connectionPreferencesPane.test.ts` login / changePassword throw 绿 | conversation | closed |
| D65 | P3 | **Connection 其余 write throw 无 catch**：`handleTestConnection`（`activeProfileId` 的 `probeConnectionProfile`）、`handleLogout`、Hub fallback `revokeDevice` / `confirmDeviceCode`（engine hook 已 catch）裸 await。throw 会成未处理 rejection；Test 会停在 "Testing…" | A 槽 `hub-remaining-write-throw` 已收：catch throw 画 `testStatus` / `hubAuthBadge` / `hubDirectoryBanner` / `hubDeviceCodeStatus`。`!result.ok` 原路径未改。测锁四条 throw。未关 D16；未发明 GetQueue / proto；未碰 F4 / 引擎 / `.sessions` / `dev/loop`；未重做 D61 list leftover / D62 rename / D64 login·changePassword | probe / logout / Hub fallback revoke / confirmDeviceCode throw 画对应 hook；`connectionPreferencesPane.test.ts` 四条 throw 绿 | conversation | closed |
| D66 | P3 | **Skills `saveSelectedSkillBody` 失败只画 body-status**：`saveSelectedSkillBody` 在 `{ ok:false }` / throw 只 `showBodyStatus`，不走 `paintSkillWriteFailed`（create/toggle 已画 `.engine-skill-write-status`）。失败不得造假行、不得改选中、不得 `clearCatalogPresentation`、不得改 create/toggle / leftover refresh | A 槽 `skills-save-write-status` 已收：fail 走 `paintSkillWriteFailed` + 既有 `ua.engineSkillBodySaveFailed`；测锁 write-status + body-status + 行仍在。未关 D16；未发明 GetQueue / proto；未碰 F4 / 引擎 / `.sessions` / `dev/loop`；未改 create/toggle / leftover refresh | save fail 可见 `.engine-skill-write-status` 与 body-status；不清 catalog、不改选中；`engineSkillsSection.test.ts` ok:false / throw 绿 | conversation | closed |
| D67 | P3 | **MCP runtime `getMcpServerTools` throw 后 leftover 工具名**：`engineMcpRuntimePanel.ts` `loadTools` catch 写 `tools=[]` 并 hide list，不 `DOM.clearNode(this.toolsList)`。成功画出工具名后再 throw，`getDomNode().textContent` 仍见 leftover。不得 `clearToolsPresentation()`（会画 empty-with-selection 而非 failed） | B 槽 `mcp-runtime-tools-leftover` 已收：catch 先 `DOM.clearNode` 再 hide + `toolsStatus` `failed`。测锁成功→throw leftover 消失；首拉 throw 仍 failed。未关 D16；未发明 GetQueue / proto；未碰 F4 / 引擎 / `.sessions` / `dev/loop`；未改 `engineMcpSection.ts` | catch 后 leftover 工具名不在 tools list / `getDomNode().textContent`；`toolsStatus` `data-catalog-mode="failed"`；`engineMcpRuntimePanel.test.ts` 绿 | conversation | closed |
| D68 | P3 | **Agents Tools 页重连 leftover**：`ensureAgentToolsLoaded` 在 `agentTools.length > 0` 时 return；`refresh` 成功不 invalidate。`onDidChangeConnection` → `refresh` → `syncDetailHost` → tools tab 第二次 `listTools` throw 不跑，DOM 留首次工具行。不得清 Agents catalog；不得改 UNKNOWN keep-while-probing | A 槽 `catalog-reconnect-stale` 已收：connected+SUPPORTED `refresh` 成功在 `setProfiles`/`syncDetailHost` 前 invalidate `agentTools` / `agentToolsLoadFailed`。测锁 leftover 工具名消失 + `toolsStatus` failed + catalog ready / 仍有 profile 行；第二次 `listAgentProfiles` 成功。未关 D16 | 重连后 tools 页第二次 `listTools` throw 画 failed、leftover 工具名消失、Agents catalog 仍 ready；`engineCatalogSections.test.ts` 绿 | conversation | closed |
| D69 | P3 | **Tools catalog 成功 refresh 后详情 stale**：`engineToolsSection.refresh` 成功 `setTools` 不重调 `loadToolInfo`。重连后 `listTools` 仍成功、`getToolInfo` throw 时 `.engine-tools-info` 仍显示上次成功文案。不得当 D46（`listTools` throw 仍须清 catalog） | A 槽 `catalog-reconnect-stale` 已收：成功 refresh 若有 `selectedToolName` 则清 info + `void loadToolInfo`。测锁 failed copy、无 `Run a command`、catalog ready。未关 D16 | 重连后 `getToolInfo` throw 画 `ua.engineToolsInfoFailed`、不含上次成功文案；catalog 仍 ready；`engineCatalogSections.test.ts` 绿 | conversation | closed |
| D70 | P3 | **Skills catalog 成功 refresh 后正文 stale**：`engineSkillsSection.refresh` 成功 `setSkills` → `restoreSkillSelection` 不重调 `loadSkillBody`。重连后 `listSkills` 仍成功、`getSkillInfo` throw 时 textarea 仍显示上次成功文案。不得当 D49（`listSkills` throw 仍须清 catalog）；不得覆盖 `bodyDirty` 编辑器；不得改 create/toggle/save write-status | A 槽 `skills-refresh-reload-body` 已收：成功 refresh 若有选中且 `!bodyDirty` 则 `void loadSkillBody`。测锁 failed copy、无 `# Stale skill body`、catalog ready。未关 D16 | 重连后 `getSkillInfo` throw 画 `ua.engineSkillBodyLoadFailed`、不含上次成功文案；catalog 仍 ready；dirty 不覆盖；`engineSkillsSection.test.ts` 绿 | conversation | closed |
| D71 | P3 | **Agents Instructions 成功 refresh 后不重载 AGENTS.md**：`refresh` 成功 invalidate tools cache 并 `syncDetailHost()`，但不调 `loadAgentsEditorForSelection()`。重连后 Instructions 页仍显示上次成功 markdown；第二次 `saveAgentProfile` throw 也不画 `ua.engineAgentsMdLoadFailed`。不得覆盖 dirty editor；不得改 D68 tools invalidate / UNKNOWN keep-while-probing | B 槽 `agents-refresh-reload-editor` 已收：成功 refresh 后若 Instructions 页有选中且 `!agentsMarkdownDirty` 则 `void loadAgentsEditorForSelection()`（既有 `saveAgentProfile`）；Tools 页不重入以免多打 `listTools`。测锁 leftover `Stale agents md` 消失 + editor-status load-failed + catalog ready。未关 D16 | 重连后 Instructions 页第二次 `saveAgentProfile` throw 画 `ua.engineAgentsMdLoadFailed`、不含上次成功文案；dirty 不覆盖；catalog 仍 ready；`engineCatalogSections.test.ts` 绿 | conversation | closed |
| D72 | P3 | **Tools 成功 refresh 不清 `pendingEnablement`**：`engineToolsSection.refresh` 成功 `setTools` 后只 `updateSaveChrome()`，不 `pendingEnablement.clear()`。重连后 `listTools`/`listAgentProfiles` 仍成功时 Save 仍 enabled、`isToolEnablementDirty()` 仍 true。仅 `clearCatalogPresentation` 才清。不得当 D46（`listTools` throw 仍须清 catalog）；不得改 D69 详情重载 | A 槽 `catalog-refresh-clear-pending` 已收：成功 listTools+listAgentProfiles 后 `pendingEnablement.clear()` 再 `updateSaveChrome()`。测锁 dirty true → 重连后 dirty false、catalog ready。未关 D16 | 重连后 `isToolEnablementDirty()===false` 且 catalog ready；`engineCatalogSections.test.ts` 绿 | conversation | closed |
| D73 | P3 | **Agents 成功 refresh 不清 `agentToolPending`**：`engineAgentsSection.refresh` 成功 invalidate agentTools（D68）但不 `agentToolPending.clear()`。重连后 `listAgentProfiles`/`listTools` 仍成功时 `isAgentToolEnablementDirty()` 仍 true。仅 `clearCatalogPresentation` 才清。不得删 D68/D71 行 | A 槽 `catalog-refresh-clear-pending` 已收：成功 listAgentProfiles 后与 D68 invalidate 并列 `agentToolPending.clear()`。测锁 dirty true → 重连后 dirty false、catalog/tools ready（非 D68 leftover）。未关 D16 | 重连后 `isAgentToolEnablementDirty()===false` 且 catalog/tools 仍 ready；`engineCatalogSections.test.ts` 绿 | conversation | closed |
| D74 | P3 | **Connection `refreshHubDirectory` 的 `refreshDirectory` 无 try/catch**：Refresh devices 裸 await。throw 成未处理 rejection，且仍会 `refreshEngineDeviceLists`。只包 directory；list leftover / rename / login / remaining writes 不在本刀 | A 槽 `connection-hub-refresh-throw` 已收：catch throw 画 `hubDirectoryBanner`（`writeStatus` error）后 return。测锁 Refresh devices throw 画 `.connection-hub-directory-banner`。未关 D16；未发明 GetQueue / proto；未碰 F4 / 引擎 / `.sessions` / `dev/loop`；未重做 D61/D62/D64/D65 | `refreshDirectory` throw 画 banner；不调 `refreshEngineDeviceLists`；`connectionPreferencesPane.test.ts` Refresh devices throw 绿 | conversation | closed |
| D75 | P3 | **Review `runGitAction` throw 无 catch**：`conversationDiffReviewPane.ts` Clean/Unstage/Stage 经 `runGitAction` 仅 try/finally，throw 被吞；Panel 同路径已 `showActionNotice(getErrorMessage(error))`。不得改 `runAccept`；不得发明 WriteGitUnstage | D 槽 `review-pane-git-action-catch` 已收：catch 后 `this.showNotice(getErrorMessage(error))`，finally 仍 `updateReviewActions`。源扫锁 catch + showNotice。未关 D16；未关 D31 F4；未跑 F4；未开 A2 | `runGitAction` 含 catch + `showNotice`；`sourcesChangesGitWrite.test.ts` 源扫绿 | sources-git | closed |
| D76 | P3 | **composer `switchModel` 空 `resolvedModelId` 不回滚**：`applySessionModelIndex` 只在 throw 回滚；resolve 且 `resolvedModelId` 空/空白时 UI 留新选中 | B 槽 `composer-switch-model-empty-rollback` 已收：await 后 `!result.resolvedModelId.trim()` 走 `restoreSessionModelIndex` + `showGateNotice`（`conversationLensDockModelFailed`）；未发明 `UniverseAgentSwitchModelResult.ok`。测锁 empty resolve 回滚 + gate。throw 测保留。未关 D16 | await 后空/空白 `resolvedModelId` 回滚 select 并画 gate；`conversationLens.test.ts` empty resolve + throw 绿 | conversation | closed |
| D77 | P3 | **Lens 阅读列 stale banner 不跟 lease-only sync**：`applySessionViewTimeline` → `updateSyncChrome` 只改 SessionBar badge + inbox；`refreshStaleSnapshotBanner` 只挂 session/active/connection/layout。lease/`onDidApplyFrame` 置 `closed` 且不发 `onDidChangeSession` 时横幅不刷新 | A 槽 `lens-stale-banner-lease-sync` 已收：徽章后用同一 `sync` 刷 `.conversation-lens-stale-snapshot`；lease 测锁 disconnect 文案。PRD-007 `onSessionChanged` 测未改。未关 D16 | `updateSyncChrome` 用同一 `sync` 刷新 banner；lease 置 `closed` 且不发 `onDidChangeSession` 时可见 disconnect 文案；PRD-007 测保留 | conversation | closed |
| D78 | P3 | **Sessions view 接通后不刷新**：`conversationSessionsView.ts` 只听 `onDidChangeActiveSession` + `onDidChangeSession`，缺 `onDidChangeEngineConnection`。引擎 roster `getSessions()` 接通后变空，列表仍留 stub 行 | B 槽 `sessions-roster-engine-connect` 已收：补 listener → `refreshList()`；测锁接通后空态立即出现。未关 D16 / 未占 D22 F3 | 接通后 `refreshList()`；`conversationSessionsView.test.ts` 绿 | conversation | closed |
| D79 | P3 | **`openStream` throw-on-open 后 sync 假 live**：D45 catch 只 warn，不 `postAndDrain` `streamClosed`；Actor 仍 `sync.kind==='live'` | D 槽 `host-openstream-throw-sync` 已收：catch 复用既有 `streamClosed` 模板 `cause: { kind:'error', message }`；测锁 throw-on-open 折 closed chrome。未关 D16；未包整圈 `drainIntents`；未发明 proto / GetQueue / heartbeat `client_id`；未碰 Create / `.sessions` / lens / sessions view | throw-on-open 后 `streamClosed` 折 closed chrome，sync 不再假 live；`sessionViewHostChatClose.test.ts` 绿 | M7 universeAgent | closed |
| D80 | P3 | **`retryError` `postBound` reject 无 catch**：`conversationLensSessionBinding.ts` 只 `.then` `!outcome.accepted`；throw 成未处理 rejection。不得把 throw 映射成 mailbox / session / auth / disconnected | A 槽 `lens-retry-error-postbound-catch` 已收：`.catch` → `showPostFailure('failed')`；第五 reason + localize。既有三测保留；补 reject 测。未关 D16 | `.catch` 调 `showPostFailure('failed')`；`conversationLensDisposeGate.test.ts` postBound reject 绿且无 unhandled rejection | conversation | closed |
| D81 | P3 | **`UniverseAgentConnectionChannelClient` `hydrate` / `refreshPhaseAndNotify` 无 try/catch**：构造 `void this.hydrate()` / `onDidChangeConnection` → `void this.refreshPhaseAndNotify()`；IPC reject 成未处理 rejection。半应用不得 fire `_onDidChangeConnection` | B 槽 `channel-client-hydrate-catch` 已收：两方法体 try/catch，失败保留已写缓存、不 fire、不抛。测锁 hydrate reject 停在预 hydrate 默认；refreshPhase reject 保留已 apply 的 snapshot、phase 不半应用。未关 D16；未发明 proto / GetQueue / heartbeat `client_id` | IPC reject 无未处理 rejection；getter 停在末次好快照或预 hydrate 默认；`universeAgentRendererSync.test.ts` 绿 | M7 universeAgent | closed |
| D82 | P3 | **Navigator Reveal `openSubAgent` throw 无 catch**：`revealNavigatorAgentInConversation` 裸 await `openSubAgent`；调用点 `void`（`navigatorAgentsView` / `navigatorTeamList`）。overlay 未挂载会 throw（`conversationSessionChatService`）。未处理 rejection | D 槽 `navigator-reveal-throw-notice` 已收：await 路径 try/catch；throw 画 `INotificationService.error(getErrorMessage)`；void 调用点未改。测锁 throw → error notice、无未处理 rejection。未关 D16 / 未关 D21 | catch 后 notification error；`navigatorAgentsSubviews.test.ts` openSubAgent throw 绿 | M7 navigator | closed |
| D83 | P3 | **`resolveConfirmation` / `resolveQuestion` `postBound` reject 无 catch**：`conversationLensSessionBinding.ts` 只处理 `!outcome.accepted`；throw 成未处理 rejection（timeline `onResolveConfirmation` / `onQuestionRespond` 为 void-async）。不得把 throw 映射成 mailbox / session / auth / disconnected；不得改 `retryError`（D80）与 reading-column void 调用点 | A 槽 `lens-permission-question-postbound-catch` 已收：两处 await 包 try/catch → `showPostFailure('failed')`；第五 reason。既有 D80 测保留；补两处 reject 测。未关 D16 | try/catch 调 `showPostFailure('failed')`；`conversationLensDisposeGate.test.ts` resolveConfirmation / resolveQuestion postBound reject 绿且无 unhandled rejection | conversation | closed |
| D84 | P3 | **`UniverseAgentHubChannelClient` `hydrate` 无 try/catch**：构造 `void this.hydrate()`；`Promise.all` IPC reject 成未处理 rejection。半应用不得 fire `_onDidChangeAuthStatus` / `_onDidChangeDirectory` / `_onDidChangeProfiles`。不得包 `setActiveHubBaseUrl`；不得重开 D81 Connection Client | B 槽 `hub-client-hydrate-catch` 已收：hydrate 体 try/catch，失败保留预 hydrate 默认（signedOut / idle / []）、不 fire、不抛。未包 `setActiveHubBaseUrl`。测锁 IPC reject 停在预 hydrate 默认。未关 D16；未重开 D81 | IPC reject 无未处理 rejection；getter 停在预 hydrate 默认；`universeAgentRendererSync.test.ts` 绿 | M7 universeAgent | closed |
| D85 | P3 | **Promote `openExtensionTab` throw 无 catch**：`promoteSubAgentDialog` 裸 await `openExtensionTab`；overlay `onDidRequestPromote` 为 `void`。conversation part 缺失会 throw（`conversationSessionChatService`）。未处理 rejection | D 槽 `promote-subagent-throw-notice` 已收：await 路径 try/catch；throw 画 `INotificationService.error(getErrorMessage)`；void 调用点未改。测锁 throw → error notice、无未处理 rejection。未关 D16 | catch 后 notification error；`conversationSessionChat.test.ts` promote / openExtensionTab throw 绿 | M7 conversation | closed |
| D86 | P3 | **`submitDraft` `postBound` reject 无 catch**：`conversationLensComposer.ts` 只 try/finally 处理 `!outcome.accepted`；throw 成未处理 rejection（dock `void host.submitDraft()`）。不得把 throw 映射成 mailbox / session / auth / disconnected；不得改 dock void 调用点；不得改 retryError / resolveConfirmation / resolveQuestion（D80/D83） | A 槽 `composer-submit-postbound-catch` 已收：catch → `showPostFailure('failed')`；finally 仍 `submitInFlight = false`；第五 reason。既有 dispose-gate 测保留；补 reject 测。未关 D16 | catch 调 `showPostFailure('failed')`；`conversationLensDisposeGate.test.ts` submitDraft postBound reject 绿且无 unhandled rejection | conversation | closed |
| D87 | P3 | **`navigateAgentBreadcrumb` 裸 await 无 catch**：tab 路径 `replaceEditors` / `closeEditor` / `openEditor` 与 overlay 路径 `openEditor` 裸 await（`conversationSessionChatService.ts`）；调用点 `void`（`conversationEditorPane` / overlay `onDidSelectBreadcrumb`）。throw 成未处理 rejection | D 槽 `breadcrumb-navigate-throw-notice` 已收：公共方法体 try/catch（overlay 经 await 覆盖）；throw 画 `INotificationService.error(getErrorMessage)`；void 调用点未改。测锁 overlay `openEditor` throw → error notice、无未处理 rejection。未关 D16；未改 promote（D85）/ `openSubAgent`（D82）/ fork（D88）/ `openExtensionTab` | catch 后 notification error；`conversationSessionChat.test.ts` breadcrumb navigate throw 绿 | M7 conversation | closed |
| D88 | P3 | **Fork `openForkTab` throw 无通知**：`conversationForkActions.contribution.ts` `_tryForkAsChat` / `_openForkedSession` 裸 await `openForkTab`；conversation part 缺失会 throw（与 D85 同 throw，入口不同）。`ServicesAccessor.get` 须在首个 await 前 hoist（D82） | D 槽 `fork-open-tab-throw-notice` 已收：两处 `openForkTab` try/catch；throw 画 `INotificationService.error(getErrorMessage)`；`INotificationService` / `IConversationSessionChatService` 在首个 await 前 hoist；finally 仍 dispose cts。测锁 fork action 路径 throw → error notice、无未处理 rejection。未关 D16；未改 `openForkTab` / `openExtensionTab` throw 合同；未改 promote（D85）/ breadcrumb（D87）/ `openSubAgent`（D82） | catch 后 notification error；`conversationSessionChat.test.ts` fork openForkTab throw 绿 | conversation | closed |
| D89 | P3 | **Connection profile CRUD write throw 无 catch**：`handleAddDirectAddress` / `handleDisconnect` / `handleForgetSelectedProfile` / `handleConnectDevice` 裸 await（D65 明确留下）。throw 成未处理 rejection；`!result.ok` 已画 status | A 槽 `connection-profile-crud-throw` 已收：catch throw 画 `directAddressStatus` / 可见 `writeConnectStatus`。`!result.ok` 与 Forget 失败测未改。测锁四条 throw。未关 D16；未重做 D62–D65 / Connect Direct / `connectProfileWithPairing` | 四条 throw 画对应 status 且无未处理 rejection；`connectionPreferencesPane.test.ts` 四条 throw 绿 | conversation | closed |
| D90 | P3 | **`ensurePrimaryWindow` 在 `ensureLeaf` 成功前写入 `primarySessionKey`**：throw 后半应用 primary 且 `void` 成未处理 rejection | B 槽已收：先 leaf 再 key；catch 回滚；`primaryBootstrapInFlight` 串行并发 void；直接 stub throw harness。merge 复测 9/9。未关 D16 | throw 后 key 空、leaf 回滚、后续 bootstrap 成功；`conversationSessionWindowSideBySide.test.ts` 绿 | conversation | closed |
| D92 | P3 | **roster `setSessionGoal` / `cancelSessionGoal` remote `void` 无回滚**：`setEngineSessionGoal` / `cancelEngineSessionGoal` 乐观写 `sessionGoals` 后 `void` unary；throw / `{ ok:false }` leftover 假 goal。公开 API 保持同步 boolean | B 槽 `roster-goal-remote-rollback` 已收：乐观本地写保留；remote throw / `!ok` 回滚并再 fire。测锁 rollback + 无未处理 rejection。未关 D16；未扫其它 `void uaConnection.*` | remote throw / `{ ok:false }` 后 `getSessionGoal` 回滚；无未处理 rejection；既有 forwards / disconnected skip 测保留 | conversation | closed |
| D93 | P3 | **`splitSessionWindow` throw 无通知**：缺 conversation part 时 throw（`conversationSessionChatService.ts`）；`conversationSplitActions` `run` 返回该 Promise。未处理 rejection | D 槽 `split-session-throw-notice` 已收：方法体 try/catch；throw 画 `INotificationService.error(getErrorMessage)`；Action2 `run` 未改。测锁 missing part → error notice、无未处理 rejection。未关 D16；未改 `openExtensionTab` / `openForkTab` throw 合同 | catch 后 notification error；`conversationSessionChat.test.ts` splitSessionWindow missing part 绿 | M7 conversation | closed |
| D94 | P3 | **roster `renameSession` remote `void` 无回滚**：`renameEngineSession` 乐观写 `session.title` 后 `void` unary；throw / `{ ok:false }` leftover 假标题。公开 API 保持同步 boolean | B 槽 `roster-rename-delete-remote-rollback` 已收：乐观本地写保留；remote throw / `{ ok:false }` 回滚标题并再 fire/persist。测锁 rollback + 无未处理 rejection。未关 D16；未扫其它 `void uaConnection.*` | remote throw / `{ ok:false }` 后标题回滚；无未处理 rejection；既有 forwards / disconnected skip 测保留 | conversation | closed |
| D95 | P3 | **roster `deleteSession` remote `void` 无回滚**：`deleteEngineSession` 先卸行+goal 后 `void` unary；throw leftover 假空/错位。公开 API 保持同步 boolean。delete 只处理 throw，不发明 `{ ok }` | B 槽 `roster-rename-delete-remote-rollback` 已收：乐观卸行保留；remote throw 恢复行/index/`sessionGoals`/active/`listCompleted`/bind-failed 空态并再 fire/persist。测锁 last-entry + mid-list rollback。未关 D16 | remote throw 后行/标题/active/goal/bind-failed 空态回滚；无未处理 rejection；既有 last-delete 诚实空 / disconnected skip 测保留 | conversation | closed |
| D96 | P3 | **`openSessionBeside` 裸 await `ensureLeaf`**：`conversationSessionWindowService.ts` 先 `leaves.set` 再 `createConversationEditorPart` / `whenReady`；throw 后半应用 secondary leaf + Sessions Alt+click / Open beside 的 `void` 成未处理 rejection。D90 只收 primary bootstrap | A 槽 `open-beside-leaf-throw-rollback` 已收：try/catch + `rollbackHalfAppliedLeaf`；不 fire 成功；`logService.warn` + `getErrorMessage`；未加 `INotificationService`；void 调用点未改。测锁 throw 后 primary 仍在、无 secondary DOM、无未处理 rejection，再成功 beside。未关 D16；未重做 D90 | throw 后无 secondary leaf/DOM、primary 仍在、无未处理 rejection；随后 beside 成功；`conversationSessionWindowSideBySide.test.ts` 绿 | conversation | closed |
| D97 | P3 | **`openSessionBeside` max-leaves 先 hide secondary 再 `ensureLeaf`**：`visibleKeys.length >= CONVERSATION_SESSION_WINDOW_MAX_LEAVES`（2）时先 `hideSessionWindow(secondaryKey)`；D96 catch 只 `rollbackHalfAppliedLeaf(newKey)`，被驱逐的 secondary 留 hidden，2 窗变 1 | A 槽 `open-beside-max-leaves-and-require-primary` 已收：记住驱逐 key，catch 里 `restoreSessionWindow`（仍走既有 D96 rollback）；未重做 D90 bootstrap / D96 helper；未加 `INotificationService`；void 调用点未改。未关 D16 | throw 后 visible 回到 2、被 hide 的 secondary restore、无第三 leaf、无未处理 rejection；随后第三 beside 成功且 2-visible max 仍诚实；`conversationSessionWindowSideBySide.test.ts` 绿 | conversation | closed |
| D98 | P3 | **`openSessionBeside` 在 D90 吞掉的 primary 失败后仍 `ensureLeaf` secondary**：`await ensurePrimaryWindow` 后 `primarySessionKey` 空且无 primary leaf 时仍造 orphan `.conversation-session-leaf-secondary` | A 槽本刀已收：ensurePrimaryWindow 后 `getPrimarySessionKey()` 空或 primary leaf 缺失则 return；未加 `INotificationService`；未改 void 调用点；未重做 D90/D96。未关 D16 | primary 失败后无 primary/secondary leaf/DOM、无未处理 rejection；`conversationSessionWindowSideBySide.test.ts` 绿 | conversation | closed |
| D99 | P3 | **`goBack` / `goForward` throw 后栈已前进且 `void` 成未处理 rejection**：`conversationNavigationService.ts` 先 `moveBack`/`moveForward` 再 `openEditor`/`closeEditor`；`conversationNavigation.contribution.ts` 调用点为 `void`。throw 后栈与 `canGoBack`/`canGoForward` 不同步 | D 槽 `navigation-go-throw-notice` 已收：方法体 catch（保留 `navigating` finally）；throw 后反向 `moveForward`/`moveBack` + `INotificationService.error(getErrorMessage)`；void 调用点未改。测锁 closeEditor/openEditor reject → notice、栈恢复、无未处理 rejection。未关 D16；未改 session-window / roster / session-chat / split / fork | catch 后栈/`canGoBack`/`canGoForward` 恢复且 error notice；`conversationNavigation.test.ts` goBack/goForward throw 绿 | conversation | closed |
| D100 | P3 | **`goBack` / `goForward` 在 `!destination` 时栈已前进且不回滚**：`conversationNavigationService.ts` 先 `moveBack`/`moveForward` 再 `if (!stack.current) return`；D99 只在 throw 回栈。空 destination 时 `canGoBack`/`canGoForward` 与栈不同步 | D 槽 `navigation-peek-before-advance` 已收：先 `peekPrevious`/`peekNext`，无 destination 不改 index；D99 throw catch + notice + `navigating` finally 未改；void 调用点未改。测锁空 destination 不改 enablement/栈。未关 D16 | 空 destination 后栈/`canGoBack`/`canGoForward` 不变；`conversationNavigation.test.ts` 绿 | conversation | closed |
| D101 | P3 | **`closeNonRootTabs` 裸 await `closeEditors` 无 catch**：先 `closeSubAgentDialog` 再 `await editorService.closeEditors(toClose)`（`conversationSessionChatService.ts`）；`conversationNavigation.contribution.ts` 调用点为 `void`。throw 成未处理 rejection；dialog 已关；`fireCloseNonRootStateChange` 被跳过 | B 槽 `close-nonroot-tabs-throw-notice` 已收：`closeEditors` try/catch → `INotificationService.error(getErrorMessage)`；`finally` 仍 `fireCloseNonRootStateChange`；void 调用点未改。测锁 closeEditors reject → error notice、无未处理 rejection、close-non-root state 仍刷新。未关 D16；未改 split / fork / breadcrumb / promote / `openExtensionTab` / `openForkTab` throw 合同 | catch 后 notification error 且 finally 刷新 close-non-root state；`conversationSessionChat.test.ts` closeEditors throw 绿 | M7 conversation | closed |
| D102 | P3 | **`SessionsNavigation._navigateTo` 先 `_currentKey.set` 再 `await opener.openChat/openSession`**：opener reject 后 MRU cursor 已前进，`CanGoBackContext`/`CanGoForwardContext` 与仍可见会话不同步。`finally` 只清 `_navigating` | A 槽 `sessions-mru-opener-rollback` 已收：捕获 previous key；opener reject 后 restore + `logService.warn`；beyond-history 失败仍 `_beyondHistory`。未加 `INotificationService`。测锁 goBack/goForward throw → cursor + enablement 恢复、无未处理 rejection。未关 D16；未改 conversation navigation / session-chat / session-window | catch 后 cursor/`canGoBack`/`canGoForward` 恢复；`sessionNavigation.test.ts` opener reject 绿 | sessions-navigation | closed |
| D103 | P3 | **`SessionsNavigation` `_canGoBack`/`_canGoForward` 只看 index；`_navigateTo` 先 `_currentKey.set` 再 `getSession`**：session 已不在 store 时仍 `recency.remove` 且 `return true`，cursor 停在死 key，Back/Forward 仍对已消失会话为 true。D102 只收 opener reject | A 槽 `sessions-mru-stale-target-honesty` 已收：先 peek/resolve 再 set cursor；缺 session 则 prune、不把 cursor 留在死 key、return false；enablement 看可解析目标；beyond-history 新会话视图仍按原合同。D102 catch restore + warn 未改。未关 D16；未重做 D102 opener-reject；未碰 conversation/* | peek 后再 set；缺 session 不前进 cursor；enablement 不因 stale 邻项为 true；`sessionNavigation.test.ts` 原 19 + 新测绿 | sessions-navigation | closed |
| D104 | P3 | **`saveTurnEdit` 忽略 `updateUserTurnText` 并先 `exitComposerEdit`**：引擎断连后 `editEngineMessage` `callRemote:false` 回 `false`，时间线仍旧、无 `showPostFailure` | B 槽 `composer-turn-edit-save-rejected-notice` 已收：先看 boolean；false 画 `engine_disconnected`（`!isEngineConnected() && hasEngineConnectionHistory()`）或 `failed`、不退出编辑；true 仍 `exitComposerEdit`。测锁 false → notice + 仍 editing。未关 D16；未改 roster `editEngineMessage`；未改 `submitDraft`（D86） | `saveTurnEdit` 先看 `updateUserTurnText`；false 不 `exitComposerEdit` 并 `showPostFailure`；`conversationLensDisposeGate.test.ts` 绿 | conversation | closed |
| D105 | P3 | **`conversation.revealItem` 的 `acquireSessionView` 在 try 外**：`conversationRevealItem.contribution.ts` 先 acquire 再 try；引擎 roster 对非 engine-bound session throw（`conversationEngineRosterService.ts` ~601–610）。`navigatorAgentsView.ts` activity `void executeCommand` 成未处理 rejection。Sources Review 已 catch。不得改 `acquireSessionView` throw 合同 / navigator void 调用点 | D 槽 `reveal-item-acquire-throw-notice` 已收：acquire + reveal 包 try/catch → `INotificationService.error(getErrorMessage)`；`finally` 仅 dispose 已拿到的 lease；accessor.get 在 await 前 hoist。测锁 roster throw → error notice、无未处理 rejection。未关 D16；未改 navigator void / roster throw 合同 | catch 后 notification error 且未取得 lease 时不 dispose；`navigatorAgentsSubviews.test.ts` acquireSessionView throw 绿 | M7 navigator / conversation | closed |
| D106 | P3 | **`saveQueueEdit` 忽略 `updateMessageQueueItemContent` 并先 `exitComposerEdit`**：引擎断连后 `editEngineQueueItem` `wasEverConnected` 回 `false`，队列文仍旧、edit chrome 已卸、无 `showPostFailure` | A 槽 `composer-queue-edit-save-rejected-notice` 已收：先看 boolean；false 画 `engine_disconnected`（`!isEngineConnected() && hasEngineConnectionHistory()`）或 `failed`、不退出编辑、不 release hold；true 仍 exit + release + `renderInboxStatus`。测锁 false → notice + 仍 editing。未关 D16；未改 `saveTurnEdit`（D104）；未改 roster `updateMessageQueueItemContent` / `editEngineQueueItem` | `saveQueueEdit` 先看 `updateMessageQueueItemContent`；false 不 `exitComposerEdit` / 不 release 并 `showPostFailure`；`conversationLensDisposeGate.test.ts` 绿 | conversation | closed |
| D107 | P3 | **`deleteTurn` / `cancelToolCall` 忽略 roster boolean**：引擎断连后 `deleteEngineMessage` / `cancelEngineToolCall` `callRemote:false` 回 `false`，时间线行不变、无 `showPostFailure` | A 槽 `lens-delete-cancel-rejected-notice` 已收：先看 boolean；false 画 `engine_disconnected`（`!isEngineConnected() && hasEngineConnectionHistory()`）或 `failed`；本无本地 mutation 可回滚；true 仍静默成功。测锁 false+history → engine_disconnected、false 无 history → failed。未关 D16；未改 roster `deleteEngineMessage` / `cancelEngineToolCall` / `void uaConnection.*`；未改 `saveTurnEdit` / `saveQueueEdit` | `deleteTurn` / `cancelToolCall` 先看 roster boolean；false 调 `showPostFailure`；`conversationLensDisposeGate.test.ts` 绿 | conversation | closed |
| D108 | P3 | **`openSessionBeside` catch 已回滚但无用户 notice**：D96–D98 已 `rollbackHalfAppliedLeaf` + restore evicted secondary + `logService.warn`；`void` 调用点未改。throw 后窗数不变、用户无提示 | D 槽 `open-beside-failure-notice` 已收：既有 catch 补 `INotificationService.error(getErrorMessage(error))`（紧挨 `ILogService` 注入）；保留 warn；未重做 D96–D98 rollback；未改 `conversationSessionsView.ts` void。测锁 throw-on-create 仍回滚且记录 error notice。未关 D16 | catch 后 error notice + 仍回滚 / 无未处理 rejection；`conversationSessionWindowSideBySide.test.ts` 绿 | conversation | closed |
| D109 | P3 | **Inbox Stop 忽略 `cancelGeneration` boolean**：`onStopClicked` 只转发；断连后 `cancelEngineGeneration(..., false)` 回 `false`，无 dock `showPostFailure` | B 槽 `inbox-stop-cancel-rejected-notice` 已收：先看 boolean；false 画 `engine_disconnected`（`!isEngineConnected() && hasEngineConnectionHistory()`）或 `failed`。true 仍只 cancel。测锁 false+history / false 无 history / true 无 chrome。未关 D16；未改 roster `cancelGeneration`；未收 Goal `setSessionGoal` / `cancelSessionGoal` | `onStopClicked` 先看 `cancelGeneration`；false 经 delegate 调 dock `showPostFailure`；`conversationInboxOverlay.test.ts` 绿 | conversation | closed |
| D110 | P3 | **Inbox Goal 忽略 `setSessionGoal` / `cancelSessionGoal` boolean**：`onGoalClicked` 只转发后 `render()`；接通后 empty / missing session / same goal / missing RPC 回 `false`，无 dock `showPostFailure` | B 槽 `inbox-goal-rejected-notice` 已收：先看 boolean；false 画 `failed`（接通路径）；若 `!isEngineConnected() && hasEngineConnectionHistory()` 画 `engine_disconnected`。未抬断连 early-return / `goalButton.enabled = connected`。true 仍 `render()`。测锁 connected set/cancel false → failed、无假 goal。未关 D16；未改 `onStopClicked`（D109）；未改 roster `setSessionGoal` / `cancelSessionGoal` / `void uaConnection.*` | `onGoalClicked` 先看 set/cancel boolean；false 经 delegate 调 dock `showPostFailure`；`conversationInboxOverlay.test.ts` 绿 | conversation | closed |
| D111 | P3 | **`tryBootstrapPrimaryWindow` catch 已回滚但无用户 notice**：D90 已清 `primarySessionKey` + `rollbackHalfAppliedLeaf` + `logService.warn`；`INotificationService` 已注入（D108）。throw 后无半应用 primary，用户无提示。既有 primary-throw 测未断言 notice | D 槽 `primary-bootstrap-failure-notice` 已收：既有 catch 补 `INotificationService.error(getErrorMessage(error))`；保留 warn + D90 rollback；未重做 D90 / D108 `openSessionBeside` catch；未改 void 调用点。测锁 throw 后 `harness.errors` 含 boom。未关 D16 | catch 后 error notice + 仍回滚 / 无未处理 rejection；`conversationSessionWindowSideBySide.test.ts` 绿 | conversation | closed |
| D112 | P3 | **Inbox Goal 断连且有连接史时按钮禁用且点击静默返回**：`renderGoal` 仅 `goalButton.enabled = connected`；`onGoalClicked` 断连 early-return 不 `showPostFailure`。从未接通 stub 仍须 disabled。不得改 D109 Stop / Enqueue / Retry / D110 接通 boolean 映射 | B 槽 `inbox-goal-disconnected-notice` 已收：enable = connected \|\| history；断连+history 点击经 delegate `showPostFailure('engine_disconnected')`，return 在 prompt / set / cancel 前。stub 从未接通仍 disabled。测锁 failures `['engine_disconnected']` 且无 set/cancel。未关 D16；未改 Enqueue/Retry/Stop/Queue；未改 session-binding / session window | 断连+history Goal enabled；点击 `engine_disconnected` 且不 set/cancel；stub 从未接通仍 disabled；`conversationInboxOverlay.test.ts` 绿 | conversation | closed |
| D113 | P3 | **接通路径 `resolveConfirmation` / `resolveQuestion` 忽略 roster boolean**：`conversationLensSessionBinding.ts` 接通后 `resolveConfirmation`/`respondQuestion` 回 `false` 静默 return，不 `showPostFailure`。断连 `postBound` reject 已由 D83 画 `failed`。 | A 槽 `permission-question-rejected-notice` 已收：接通分支 `!forwarded` 后按 D110 映射画 `failed` 或 `engine_disconnected`，不 `focusTimelineRecord`。未改 D83 catch / deleteTurn / cancelToolCall / Inbox / session window。未关 D16。 | 接通 + roster false 调 `showPostFailure`；`conversationLensDisposeGate.test.ts` 绿 | conversation | closed |
| D114 | P3 | **接通路径 `enqueueMessageQueueItem` 回 false 时 Inbox 静默**：`onEnqueueClicked` `if (!queued) { return; }`，无 dock `showPostFailure`。既有测已锁 false 不假装入列。不得改 Retry/Stop/Goal/`isEngineQueueUnlisted` | B 槽 `inbox-enqueue-rejected-notice` 已收：先看 boolean；false 画 `failed`（接通路径）；若调用后 `!isEngineConnected() && hasEngineConnectionHistory()` 画 `engine_disconnected`。未抬断连 early-return。true 仍 `render()`。测锁 connected false → failed、无假行；断连+history → engine_disconnected。未关 D16；未改 Retry/Stop/Goal/Queue honesty | `onEnqueueClicked` 先看 enqueue boolean；false 经 delegate 调 dock `showPostFailure`；`conversationInboxOverlay.test.ts` 绿 | conversation | closed |
| D115 | P3 | **SessionBar `commitSessionTitleEdit` 忽略 `renameSession` boolean**：拒绝后仍 `updateSessionTitle` + `refreshSessionSelectOptions`，把拒绝标题当成功。`IConversationLensSessionBarHost` 原无 `showPostFailure`。 | A 槽 `sessionbar-rename-rejected-notice` 已收：读 boolean；false 按 D110 画既有 `showPostFailure`（`failed` / `engine_disconnected`），标题回滚、不 refresh select。未改 empty-trim / Escape / create·delete·switch。未关 D16。 | `renameSession` false 调 `showPostFailure`；标题/roster 仍旧；既有 Enter/empty/Escape 测仍绿；`conversationLens.test.ts` 绿 | conversation | closed |
| D116 | P3 | **SessionBar `deleteActiveSession` 忽略 `deleteSession` boolean**：先 `deleteComposerDraftsForSession` 再裸 `deleteSession`；roster 回 `false` 仍删草稿、无 `showPostFailure`、UI 当成功。 | A 槽 `sessionbar-delete-rejected-notice` 已收：先读 boolean；false 按 D110 画既有 `showPostFailure`（`failed` / `engine_disconnected`），不删草稿、不当删成功。true 仍删草稿。未改 create·switch·rename / Inbox / Sessions view。未关 D16。 | `deleteSession` false 调 `showPostFailure`；会话仍在、计数不变；既有 delete 成功测仍绿；`conversationLens.test.ts` 绿 | conversation | closed |
| D117 | P3 | **Sessions 侧栏 `deleteActiveSession` 忽略 `deleteSession` boolean**：`conversationSessionsView.ts` 调用后不读返回值，false 时无 `INotificationService.error`，列表可假装已删。无 dock `showPostFailure`。不得改 createNewSession / openSessionBeside / SessionBar / Inbox | B 槽 `sessions-view-delete-rejected-notice` 已收：读 boolean；false 画 `INotificationService.error`（诚实 failed 文案；`!isEngineConnected() && hasEngineConnectionHistory()` 映射 disconnected）；true 仍静默。测锁 false → error + 会话仍在 `getSessions()`。未关 D16 | `deleteSession` false → notification error 且列表仍在；`conversationSessionsView.test.ts` 绿 | conversation | closed |
| D118 | P3 | **`readPendingAttentionIds` 裸 `acquireSessionView`**：`conversationNotifications.contribution.ts` 先 acquire 再 try；引擎 roster 对非 engine-bound session throw（`conversationEngineRosterService.ts` ~601–610）。构造 seed `lastPendingAttentionIds` 与 `onSessionChanged` 都会调，throw 打断 permission toast 链。D105 已收 revealItem。不得改 revealItem / Inbox / SessionBar / Sessions view | A 槽 `notifications-acquire-throw-catch` 已收：acquire + collect 包 try/catch → `INotificationService.error(getErrorMessage)`；throw 回 `[]`；`finally` 仅 dispose 已拿到的 lease。测锁 roster throw → error notice、无未处理 rejection、构造后 turn-settle 仍跑。未关 D16；未改 revealItem / Inbox / SessionBar / Sessions view / notifyInactiveSession / openSessionAndRevealPending | acquire throw → [] + error notice + dispose 已拿 lease；无未处理 rejection；`conversationNotifications.contribution.test.ts` 绿 | conversation | closed |
| D119 | P3 | **Inbox Enqueue 断连且有连接史时按钮禁用且点击静默返回**：`renderEnqueueAction` 仅 `disabled = !connected`；`onEnqueueClicked` 断连 early-return 不 `showPostFailure`。从未接通 stub 仍须 disabled。不得改 Retry / Stop / Goal / `isEngineQueueUnlisted` / D114 接通 boolean 映射 | B 槽 `inbox-enqueue-disconnected-notice` 已收：enable = connected \|\| history；断连+history 点击经 delegate `showPostFailure('engine_disconnected')`，return 在 prompt / enqueue 前。stub 从未接通仍 disabled。测锁 failures `['engine_disconnected']` 且无 enqueueCalls。未关 D16；未改 Retry/Stop/Goal/Queue honesty；未改 D114 接通 false 映射 | 断连+history Enqueue enabled；点击 `engine_disconnected` 且不 enqueue；stub 从未接通仍 disabled；`conversationInboxOverlay.test.ts` 绿 | conversation | closed |
| D120 | P3 | **Create Snapshot 忽略 `createSnapshot` boolean**：`conversationCreateSnapshotActions.contribution.ts` `run` prompt 后不读返回值；connected gate 之后仍可 false（missing session / hook vanished / disconnect during prompt），用户无 notice。`ServicesAccessor.get` 须在首个 await 前 hoist。不得改 canCreate 门禁 / 取消 prompt / Snapshots overlay Restore/Delete / Inbox / SessionBar / Sessions view / roster | A 槽 `snapshot-create-rejected-notice` 已收：hoist accessor.get；读 boolean；false 画 `INotificationService.error`（诚实 failed；`!isEngineConnected() && hasEngineConnectionHistory()` 映射 disconnected）；true 仍静默。测锁 helper 三分支。未关 D16 | `createSnapshot` false → notification error；true 静默；`conversationCreateSnapshotActions.test.ts` 绿 | conversation | closed |
| D121 | P3 | **`bindLiveTreeLease` 裸 `acquireSessionView`**：`conversationSessionChatService.ts` 在 connected+ready 后裸 acquire；引擎 roster 对非 engine-bound id throw（同 D105/D118）。构造 + `onDidChangeActiveSession` / `onDidChangeEngineConnection` 会杀 Chat 服务。不得改 closeNonRootTabs / fork / breadcrumb / promote / revealItem / notifications | B 槽 `session-chat-livetree-acquire-catch` 已收：try/catch → `INotificationService.error(getErrorMessage)`；throw 不挂 lease。测锁构造不抛 + error notice + 无未处理 rejection + breadcrumb 空。未关 D16；未改 closeNonRootTabs / fork / breadcrumb / promote / revealItem / notifications | acquire throw → error notice、不挂 lease、无未处理 rejection；`conversationSessionChat.test.ts` 绿 | conversation | closed |
| D122 | P3 | **`bindSessionView` 裸 `acquireSessionView`**：empty/not-ready early return 后直接 acquire；引擎 roster 对 leftover stub / 非 engine-bound id 接通后 throw。`applyActiveSession` 已刷新 title/select 再 bind，throw 留半应用 lens 并打断后续 inbox/voice。不得用 `INotificationService.error`（host 已有 `showPostFailure`）；不得改 D83/D107/D113 映射 | D 槽 `lens-bind-acquire-throw-catch` 已收：acquire+attach 包 try/catch → `sessionViewLease=undefined` + `showPostFailure('failed')`。测锁 throw → failed、lease undefined、无未处理 rejection。未关 D16；未改 deleteTurn/cancelToolCall/permission/question/Inbox/SessionBar/notifications/sessionChat | acquire throw → `showPostFailure('failed')` 且 lease undefined；无未处理 rejection；`conversationLensDisposeGate.test.ts` 绿 | conversation | closed |
| D123 | P3 | **`NavigatorSessionLeaseHolder.refreshLease` 裸 `acquireSessionView`**：`setVisible` / `onDidChangeActiveSession` 后直接 acquire；引擎 roster 对 leftover stub / 非 engine-bound id throw。Team / Agents / Inspect 共用 holder，throw 杀掉可见性回调。不得改 `conversation.revealItem`（D105）、`bindLiveTreeLease`（D121）、`bindSessionView`（D122）、Inbox、Team leftover-clear（D53） | A 槽 `navigator-lease-acquire-throw-catch` 已收：try/catch → `lease=undefined` + `onAcquireError` → 三视图 `INotificationService.error(getErrorMessage)`。测锁构造/`setVisible` 不抛、error notice、无未处理 rejection、无半应用 lease。未关 D16 | acquire throw → 不挂 lease + error notice + 无未处理 rejection；`navigatorTeamSubviews.test.ts` 绿 | M7 navigator | closed |
| D124 | P3 | **`ConversationTimelineRevealService` lens 委托裸调用**：`revealItem` / `focusAccessibleTurn` / `scrollToFirstPendingConfirmation` 裸委托；lens throw 杀 focus 或让 scroll microtask 成未处理 rejection。D105 已包 contribution acquire。不得改 `conversation.revealItem` / `shouldOpenPendingOnFocus` / Inbox / navigator lease | B 槽 `timeline-reveal-service-throw-catch` 已收：三处 lens 调用 try/catch → `INotificationService.error(getErrorMessage)`；scroll 仍 coalescing。测锁 throw → error notice、无未处理 rejection、二次调用仍跑。未关 D16；未改 revealItem contribution / openPending / Inbox / navigator lease | lens throw → error notice；无未处理 rejection；二次调用仍跑；`conversationPendingSeat.test.ts` 绿 | conversation | closed |
| D125 | P3 | **`SourcesReviewAttributionService.resolveRevealItemId` / `buildChipMapForEntries` 裸 `acquireSessionView`**：引擎 roster 对 leftover stub / 非 engine-bound `activeSessionId` throw。Review chip / reveal-from-attribution 会挂。D118 已收 notifications acquire。不得改 Accept A2 / F4 / WriteGitUnstage / Inbox / navigator lease / timeline reveal | A 槽 `sources-attribution-acquire-throw-catch` 已收：acquire 包 try/catch → `INotificationService.error(getErrorMessage)`；throw 后 resolve 回 undefined、chip map 空；`finally` 仅 dispose 已拿到的 lease。测锁 roster throw。未关 D16；未跑 F4；未开 A2 | acquire throw → undefined / 空 map + error notice + dispose 已拿 lease；无未处理 rejection；`sourcesReviewAttribution.test.ts` 绿 | sources | closed |
| D126 | P3 | **`getAccessibleTurnContent` 仍裸委托 lens**（D124 leftover）：`conversationTimelineRevealService.ts` 只包了 reveal/focus/scroll；`getAccessibleTurnContent` 仍 `this.primaryLens?.getAccessibleTurnContent()`。`conversationAccessibleView.ts` `getProvider` 同步调用，lens throw 杀掉 Accessible View 打开。不得改 D124 已包三路径、D105 contribution、Inbox、sources attribution | B 槽 `timeline-reveal-accessible-content-catch` 已收：同 D124 try/catch → `INotificationService.error(getErrorMessage)`，返回 `undefined`。测锁 throw → error、`undefined`、二次调用仍跑、无未处理 rejection。D124 三测保留。未关 D16；未改 revealItem/focus/scroll / Accessible View provider / D105 / Inbox / sources | lens throw → error notice + `undefined`；无未处理 rejection；二次调用仍跑；`conversationPendingSeat.test.ts` 绿 | conversation | closed |
| D127 | P3 | **接通 `forkSubAgent` false 被父 action 当未处理并 fallthrough 本地 fork**：`conversationForkActions.contribution.ts` `_tryForkAsChat` 接通分支裸 `return roster.forkSubAgent(...)`；`chatForkActions.ts` 把 false 当 unhandled，走 `forkContributedChatSession` / 本地 tab。引擎可回 false（session 不在 engineSessions、无 `forkAgent`、断连后 cache）。用户无 notice、错走本地 fork。不得改 roster `forkEngineSubAgent` / `void uaConnection.forkAgent`；不得重开 D88 throw 合同 | B 槽 `fork-subagent-false-notice` 已收：false → `INotificationService.error`（诚实 failed）+ `return true`；true 仍静默。测锁 notice + handled + 0 `forkChatSession` + 无新 tab。connected-true / D88 保留。未关 D16；未改 roster fork / `uaConnection.forkAgent` | 接通 + `forkSubAgent` false → error notice 且 handled（无 fallthrough）；`conversationSessionChat.test.ts` 绿 | conversation | closed |
| D128 | P3 | **`handleRotateSelectedDeviceToken` 忽略 `UniverseAgentRotateTokenResult.success`**：只写 `result.message` 并按空 message 隐藏 banner；`{ success:false, message:'' }` 静默，`{ success:false, message:'denied' }` 无 error 色。对照 `handleConfirmDeviceCode` 已按 `success` 分 success/error。不得重做 D61–D65 / D74 / D89；不得扫其他 hub writes | A 槽 `connection-rotate-success-false-notice` 已收：`!success` 走 `writeStatus` error + 显示 banner（空 message 用 localize fallback）；`success` 空 message 仍隐藏。throw 路径未改。测锁 denied + 空 message。未关 D16 | `success:false` 画 `.connection-hub-directory-banner` error；空 message 仍可见 fallback；throw 测仍绿；`connectionPreferencesPane.test.ts` 绿 | conversation | closed |
| D129 | P3 | **`handleRevokeSelectedDevice` 引擎 hook 忽略 `UniverseAgentRevokeResult.success`**：只写 `result.message` 并始终显示 banner；`{ success:false, message:'denied' }` 无 error 色，`{ success:false, message:'' }` 空可见 banner。对照 D128 RotateToken 已按 `success` 走 `writeStatus` error。不得重做 D61–D65 / D74 / D89 / D128 rotate；不得扫 rename / pair / list leftover；Hub fallback `revokeDevice` `!ok` 不改语义 | A 槽 `connection-revoke-success-false-notice` 已收：引擎 hook `!success` 走 `writeStatus` error + 显示 banner（空 message 用 `connectionDeviceRevokeFailureMessage`）；不 refresh。`success` 仍 refresh + 原 banner。throw 路径未改。测锁 denied + 空 message + success refresh。未关 D16 | `success:false` 画 `.connection-hub-directory-banner` error；空 message 仍可见 fallback；`success:true` 仍 refresh；throw 测仍绿；`connectionPreferencesPane.test.ts` 绿 | conversation | closed |
| D131 | P3 | **Triggers `handleDelete` 成功不 `refresh()`**：`engineTriggersSection.ts` `deleteTrigger` resolve 后只清 `.engine-triggers-delete-status`，不重拉列表，已删行 leftover。对照 MCP `removeSelectedServer` 成功 `await refresh()`。钩子回 `{}`，不得发明 `ok`。throw 仍画 delete-status 且不卸行。不得扫 upsert / setEnabled / fire；不得改 MCP/Agents write-status（D55–D60） | B 槽 `engine-triggers-delete-refresh` 已收：成功 `await refresh()`；测锁删后无 `.engine-triggers-row`；throw 仍画 `.engine-triggers-delete-status` 且行仍在。未关 D16 / D31；未扫 upsert / setEnabled / fire；未改 MCP/Agents write-status | 成功 delete 后该 id 行消失（或列表空）；throw 画 status 且行仍在；既有 DeleteTrigger send 测绿 | conversation | closed |
| D133 | P3 | **`handleRenameSelectedDevice` `!ok` / catch 跳过 `writeStatus` error tone**：`!result.ok` 与 throw 只写 `textContent` + `display`，无 `.is-error`。对照 D128/D129 rotate/revoke 与 D74 refresh throw 已 `writeStatus(..., 'error')`。D62 throw 测只锁文案。不得重做 D128/D129 rotate/revoke；不得改 Hub fallback `revokeDevice`；不得扫 login/changePassword | A 槽 `connection-rename-failure-error-tone` 已收：`!ok` / catch 走 `writeStatus` error + 显示 banner；成功仍 `refreshDirectory`。测锁 denied + throw `.is-error`。未关 D16；未重做 D128/D129；未改 Hub fallback revoke；未扫 login/changePassword | `renameDevice` `{ ok:false, reason:'denied' }` 画 banner + `.is-error` 且不 refresh；throw 亦 `.is-error`；成功仍 refresh；`connectionPreferencesPane.test.ts` 绿 | conversation | closed |
| D134 | P3 | **Clipboard `handleClear` 成功不 `refresh()`**：`engineClipboardSection.ts` `clearClipboard` resolve 后只画 `formatEngineClipboardClearLabel`，不重拉列表，已清空行 leftover。对照 D131 Triggers 删除成功 `await refresh()`。钩子回 `{ removedCount }`，不得发明 `ok`。throw 仍画 clear-status 且不卸行。不得改 handleWrite / handleRead | B 槽 `engine-clipboard-clear-refresh` 已收：成功画 status 后 `await refresh()` 再回写 `formatEngineClipboardClearLabel`；测锁清后无 `.engine-clipboard-row` 且 status 仍在；throw 仍画 `.engine-clipboard-clear-status` 且行仍在。未关 D16 / D31；未改 handleWrite / handleRead；未发明 `ok` | 成功 clear 后行消失（或列表空）且 `formatEngineClipboardClearLabel` status 仍在；throw 画 status 且行仍在；既有 Clear send 测绿 | conversation | closed |
| D135 | P3 | **Hub fallback `revokeDevice` `!ok` / catch 跳过 `writeStatus` error tone**：`handleRevokeSelectedDevice` 在 `isEngineConnected` false 时 `!result.ok` 与 throw 只写 `textContent` + `display`，无 `.is-error`。对照 D129 引擎 hook `!success` 与 D133 rename 已 `writeStatus(..., 'error')`。既有 throw 测只锁文案。不得重做 D129 引擎 hook / D128 rotate / D133 rename；不得扫 login/changePassword / confirmDeviceCode / pairApprove | A 槽 `connection-revoke-hub-fallback-error-tone` 已收：`!ok` / catch 走 `writeStatus` error + 显示 banner；`!ok` 不 `refreshDirectory`；成功仍 refresh + `renderProfiles()`。测锁 denied + throw `.is-error` 且 `refreshed===0`。未关 D16 / D31；未改引擎 hook catch；未扫 rotate / rename / login | Hub fallback `{ ok:false, reason:'denied' }` 画 banner + `.is-error` 且不 refresh；throw 亦 `.is-error`；成功仍 refresh；`connectionPreferencesPane.test.ts` 绿 | conversation | closed |
| D136 | P3 | **Triggers `handleUpsert` 成功不 `refresh()`**：`engineTriggersSection.ts` `upsertTrigger` resolve 后只画 `formatEngineTriggerListLabel(result.trigger)`，不重拉列表，新/改 trigger leftover。对照 D131 `handleDelete` 成功 `await refresh()`。钩子回 `{ trigger }`，不得发明 `ok`。throw 仍画 upsert-status 且不卸行。不得改 handleDelete / setEnabled / fire | B 槽 `engine-triggers-upsert-refresh` 已收：成功画 status 后 `await refresh()` 再回写 `formatEngineTriggerListLabel`；测锁 Add 后出现 `.engine-triggers-row` 且 status 仍在；throw 仍画 `.engine-triggers-upsert-status` 且行仍在。未关 D16 / D31；未改 handleDelete / setEnabled / fire；未发明 `ok` | 成功 upsert 后列表含新/改行且 `formatEngineTriggerListLabel` status 仍在；throw 画 status 且行仍在；既有 Upsert send 测绿 | conversation | closed |
| D137 | P3 | **Clipboard `handleWrite` 成功不 `refresh()`**：`engineClipboardSection.ts` `writeClipboard` resolve 后只画 `formatEngineClipboardWriteLabel`，不重拉列表，新写入行 leftover-missing。对照 D134 Clear 成功 `await refresh()`。钩子回 `{ clipId }`，不得发明 `ok`。throw 仍画 write-status 且不卸行。不得改 handleClear / handleRead | D 槽 `engine-clipboard-write-refresh` 已收：成功画 status 后 `await refresh()` 再回写 `formatEngineClipboardWriteLabel`；测锁写后有 `.engine-clipboard-row` 且 status 仍在；throw 仍画 `.engine-clipboard-write-status` 且行仍在。未关 D16 / D31；未改 handleClear / handleRead；未发明 `ok` | 成功 write 后该 clipId 行出现且 `formatEngineClipboardWriteLabel` status 仍在；throw 画 status 且行仍在；既有 Write send 测绿 | conversation | closed |
| D138 | P3 | **RotateToken / 引擎 hook Revoke catch 跳过 `writeStatus` error tone**：`handleRotateSelectedDeviceToken` 与 `handleRevokeSelectedDevice` 引擎 hook catch 只写 `textContent` + `display`，无 `.is-error`。D128/D129 只修 `!success`；D135 只修 Hub fallback revoke。不得改 `!success` / 成功 banner；不得扫 Hub fallback / rename / login / changePassword / confirmDeviceCode / pairApprove | A 槽 `connection-banner-catch-error-tone` 已收：两 catch 走 `writeStatus` error + 显示 banner。测锁 throw boom + `.is-error`；Revoke throw 不调 Hub `revokeDevice`。未关 D16 / D31；未改 `!success` / 成功路径 / Hub fallback / rename / login | catch throw 画 banner + `.is-error`；Revoke throw 不调 Hub `revokeDevice`；既有 `!success` 测绿；`connectionPreferencesPane.test.ts` 绿 | conversation | closed |
| D139 | P3 | **swallowed primary 后 `openSessionBeside` 未锁 bootstrap notice**：`openSessionBeside after swallowed primary failure does not create a secondary` 已锁 rollback + 无未处理 rejection，未断言 `harness.errors`。D111 已在 `tryBootstrapPrimaryWindow` catch 画 `notificationService.error`；`openSessionBeside` await `ensurePrimaryWindow` 后 empty-primary early-return。不得在 early-return 二次 notice；不得改 D90 / D96–D98 / D108 / D111 catch 文案 | B 槽 `open-beside-swallowed-primary-notice-lock` 已收：该测断言 `harness.errors` 含 `'primary bootstrap boom'`；未在 empty-primary early-return 加 notice。未关 D16 / D31 | 该测 `harness.errors` 含 `'primary bootstrap boom'`；既有 primary-throw notice 测仍绿；`conversationSessionWindowSideBySide.test.ts` 绿 | conversation | closed |
| D140 | P3 | **SessionBar `createNewSession` 断连且有连接史时仍调 `createSession()` 且无 notice**：`conversationLensSessionBar.ts` 只 `writeComposerDraft` + `createSession()`。roster `wasEverConnected` 时 `createSession()` 回 `''` 且 no-op；接通时回 `''` 是 pending bind（`ensureEngineSession`），不是失败。不得把 `''` 当 boolean。不得禁用 New session；不得改 delete/rename/switch | A 槽 `sessionbar-create-disconnected-notice` 已收：断连+history 先 `writeComposerDraft` 再 `showPostFailure('engine_disconnected')` 且不调 `createSession`；未读返回值；未接通 stub 与接通路径仍 `createSession()`。测锁 create 调用 0 + dock `conversationLensPostFailedDisconnected`；接通仍调用。未关 D16；未改 delete/rename/switch；未改 Inbox Pause/Resume/Clear（`isEngineQueueUnlisted` 有 history 时藏行，notice 不可测） | 断连+history 点 New session 画 `engine_disconnected` 且不调 `createSession`；未接通仍创建且无 gate；接通仍调 `createSession`；`conversationLens.test.ts` 绿 | conversation | closed |
| D141 | P3 | **Sessions 侧栏断连+history New session 仍调 `createSession`**：`conversationSessionsView.createNewSession` 无 D117 式 notice。`ConversationEngineRosterService.createSession()` 在 `wasEverConnected` 回 `''` 且无操作；接通时 `''` 是 pending bind，不是失败。不得读返回值发明 boolean。不得改 delete / beside / filter / SessionBar / Inbox / roster | B 槽 `sessions-view-create-disconnected-notice` 已收：`!isEngineConnected() && hasEngineConnectionHistory()` 画 `INotificationService.error`（`Could not create session — engine disconnected.`）且不调 `createSession`；未读返回值；未连 stub 与接通路径仍调 `createSession()`。测锁 disconnect+history 0 次 create + 列表不变。未关 D16 | disconnected+history `createNewSession` → error 且 `createSession` 0 次；never-connected / connected 仍 create；`conversationSessionsView.test.ts` 绿 | conversation | closed |
| D142 | P3 | **`resolveConversationMermaidExtension` 在 `getExtension` reject 时无 catch**：`conversationMermaidHost.ts` 只 `.then`；`conversationLens.ts` `void resolve…then` 无 catch，reject 成未处理 rejection。透镜应保持 fallback（`mermaidExtensionInfo` undefined） | D 槽 `mermaid-resolve-catch` 已收：helper `.catch(() => undefined)`；测锁 reject → undefined 且无未处理 rejection；缺失扩展仍 undefined。未改 mount fallback / onFatalError；未改 `conversationLens.ts` | `getExtension` reject → resolve 回 `undefined`，无未处理 rejection；缺失扩展仍 `undefined`；`conversationVisualizeOverlay.test.ts` 绿 | conversation | closed |
| D143 | P3 | **`copyTurn` 裸 `writeText`**：`conversationLensSessionBinding.ts` 直接 `host.clipboardService.writeText(text)`；`writeText` 回 `Promise<void>`，reject 成未处理 rejection，Copy 看起来成功。不得映射 `engine_disconnected`；不得改 deleteTurn / cancelToolCall / resolveConfirmation | A 槽 `lens-copyturn-clipboard-catch` 已收：`writeText` reject → `showPostFailure('failed')`。测锁 reject → failed 且无未处理 rejection；resolve 不画失败。未关 D16；未改 deleteTurn / cancelToolCall / resolveConfirmation | writeText reject → `showPostFailure('failed')` 且无未处理 rejection；成功路径仍写入；`conversationLensDisposeGate.test.ts` 绿 | conversation | closed |
| D144 | P3 | **`requestResync` 无 catch**：`conversationEngineFrameSource.ts` `void this.sessionView.requestResync(this.leaseId)`；`applyViewFrame` 失败已调 `requestResync()`；reject 成未处理 rejection | B 槽 `engine-frame-resync-catch` 已收：`.catch(() => undefined)`；测锁 reject `'boom'` 无未处理 rejection 且坏帧不落地。未关 D16 / D31；未加 ILogService；未改 acknowledge / releaseLease / post | `requestResync` reject 无未处理 rejection；坏帧不 apply；`conversationEngineFrameSource.test.ts` 绿 | conversation | closed |
| D145 | P3 | **`acknowledge` / `releaseLease` 无 catch**：`conversationEngineFrameSource.ts` `void this.sessionView.acknowledge(...)` 与 `void this.sessionView.releaseLease(id)`；reject 成未处理 rejection | A 槽 `engine-frame-ack-release-catch` 已收：同 D144 `.catch(() => undefined)`；测锁 acknowledge reject 仍 apply、releaseLease reject 仍 onRelease/cache、无未处理 rejection。未关 D16 / D31；未加 ILogService；未改 requestResync / post / applyViewFrame | acknowledge reject 仍 apply 且无未处理 rejection；releaseLease reject 仍 onRelease/cache；既有 requestResync reject 测绿；`conversationEngineFrameSource.test.ts` 绿 | conversation | closed |
| D146 | P3 | **Projects `rebuildTree` / `openWindow` 无 catch**：`navigatorProjectsList.ts` `void this.rebuildTree()` 与 `void this.hostService.openWindow(...)` 无 catch。`getRecentlyOpened` throw 成未处理 rejection（赋值在 await 后，末次树仍在但未吞）。本地文件夹点击 `openWindow` reject 同漏。不得扩 ctor；优先静默吞 + 保留 last-good | B 槽 `navigator-projects-rebuild-open-catch` 已收：`rebuildTree` try/catch 保留末次 `localFolderEntries` / `treeNodes`；`openWindow` `.catch(() => undefined)`。测锁成功后再 throw 末次行与叶数不变、无未处理 rejection；`openWindow` reject 点本地文件夹无未处理 rejection；session 行仍只 `switchSession`。未关 D16；未加 ctor 参数；未发明 notice | `getRecentlyOpened` throw 无未处理 rejection 且 last-good 不变；`openWindow` reject 无未处理 rejection；session 行只 `switchSession`；`navigatorProjectsList.test.ts` 绿 | navigator | closed |
| D147 | P3 | **D8 可执行修复 step 1**：`build/checker/tsconfig.worker.json` 已对齐 browser 补充类型（include 加 `@webgpu/types`；exclude 去掉 `editContext.d.ts`）。工位 A `fbc1bc47cba` native tsc **48 → 2**（exit 1）。剩 2：`universeAgentConnectionChannelClient.ts` TS2345 `sessionList` ⊈ `UniverseAgentCapabilityKey`；`conversationPart.ts` TS2352 `IConversationEditorPart` 无 `layout`。未开 step 2B；未改 `src/vs/**`；**未恢复 valid-layers-check 门禁**。2026-09-09 复读：两处仍在；Node 对 `sessionList` 故意 `'UNKNOWN'`，mocha 消不掉 TS2345；`layout` 运行时存在，mocha 锁不了 TS2352。**compile-only，勿派 letter** | step 1 已做；剩 2 条非 WebGPU/EditContext，属业务类型债，须另开允许改 `src/vs` 的切片（不得借 D147 同刀改源码） | worker-project `tsc` exit 0（0× `error TS`）后闭本行；**不**因此闭 D8 | infra | open |
| D148 | P3 | **Triggers `handleDelete` 成功后 `refresh()` 冲掉成功文案**：D131 已 `await refresh()`；`refresh()` 开头清空 `deleteStatus`；后续 `listTriggers` throw 被 refresh 内部 catch，用户只见 list failed。对照 `handleUpsert` refresh 后回写。不得改 setEnabled/fire | A 槽 `loop/A` `f0f3b0938b4` 已收：成功画 `ENGINE_TRIGGER_DELETE_SUCCESS_COPY` → refresh → 再回写；测锁 list throw 后成功文案仍在 + catalog failed。merge 复测 23 passing。未关 D16；未改 setEnabled/fire | 成功 delete 后成功文案在 refresh/list-fail 后仍可见；throw 仍画失败且行仍在；`engineTriggersSection.test.ts` 绿 | conversation | closed |
| D149 | P3 | **overlay 面包屑 `existingTab` 先 `closeSubAgentDialog` 再 `openEditor`**：throw 时 overlay 已关、tab 未激活。不得改 promote close-first（D85 测锁 `isSubAgentDialogOpen()===false`） | B 槽 `loop/B` `8df5ebee622` 已收：`openEditor` 成功后再关 overlay；throw 测 overlay 仍开。merge 复测 36 passing。未关 D16；未改 promote | throw 时 overlay 仍开 + error notice；成功关 overlay 且激活 ancestor tab；`conversationSessionChat.test.ts` 绿 | conversation | closed |
| D150 | P3 | **Projects 首绘 `getRecentlyOpened` throw 丢掉当前文件夹**：D146 last-good 只保二次 throw；首绘无 last-good → 空白。recents 须独立 catch。不得改 `openWindow` swallow | D 槽 `loop/D` `b38c90c373e` 已收：recents 独立 try；首绘保当前文件夹 + status/note；二次 throw 仍 last-good。merge 复测 14 passing。未关 D16；未改 openWindow。残留：空工作区 welcome 或盖住树 note（status 仍在）；`setRecentsStatus` 后未立即 `layoutBody` | 首绘 recents throw 当前文件夹仍在且失败文案可见；last-good 测仍绿；`navigatorProjectsList.test.ts` 绿 | navigator | closed |
| D151 | P3 | **MCP `addServer` / Skills `createSkill` 成功后 `refresh()` 清 write 成功态**，后续 list 失败盖掉已成功写入（与 D148 同族） | A `799b56581dd` MCP add 回写「Added.」；B `100706ff8d2` createSkill 回写「Created.」。merge 复测 catalog 42 / skills 19 passing。未关 D16；未改 MCP update/remove/toggle、Skills toggle/saveBody | add/create 成功后 list-fail 仍见成功文案；既有 ok:false/throw 测绿 | conversation | closed |
| D152 | P3 | **MCP update/remove/toggle 与 Skills toggle/saveBody 成功后 refresh 仍冲掉成功文案**（D151 只收 add/create） | A `9814d192022` MCP 三写回写 Updated./Removed./Toggled.；B `92b722b8f31` Skills Updated./Saved.。merge 复测 catalog 45 / skills 23 passing。未关 D16 | 成功后 list-fail 仍见 write-success | conversation | closed |
| D153 | P3 | **Agents create/delete/reset / save markdown 成功后 refresh 仍冲成功文案**（D151/D152 只收 MCP+Skills） | A `d4a8d8d5309` 回写 Created./Deleted./Reset./Saved.；merge 复测 catalog 50 passing。未关 D16。残留：选中 profile 用 `saveAgentProfile` 当加载；`refresh()` 里 `void loadAgentsEditorForSelection` 不 await | 成功后 list-fail 仍见 write-success | conversation | closed |
| D154 | P3 | **Snapshots Restore/Delete 成功后 `refresh()` 冲掉写成功**：`listSnapshots` throw 后用户只见 list-fail | B `f011e26d5f3` 回写 Restored./Deleted.；成功测用 `flushMicrotasks`。merge 复测 31 passing。未关 D16；未改 ok:false/throw 不 refresh | 成功后 list-fail 仍见 write-success；overlay 仍开 | conversation | closed |
| D155 | P3 | **Plugins Enable/Reload/Unload 成功后 list-fail 无写成功态**。`scanNew` leftover 见 [D216](#d216) | D `e56636a0fbb` 回写 Enabled./Reloaded./Unloaded.；新测 `enginePluginsSection.test.ts`。merge 复测 5 passing。未关 D16；scanNew 回写 leftover 已由 [D216](#d216) 收 | 成功后 list-fail 仍见 write-success | conversation | closed |
| D156 | P3 | **空工作区首绘 recents throw 仍 welcome**：CSS 藏 recents status / `local:recents-failed`，用户只见空态 | A `15ef116e957` `shouldShowWelcome` 遇失败 note/status 返 false。merge 复测 15 passing。未关 D16；未改 last-good / openWindow / D150 有文件夹路径。残留：`setRecentsStatus` 未立即 `layoutBody` | 空工作区 recents throw 关 welcome 且失败文案可见；空成功 welcome 仍在 | navigator | closed |
| D157 | P3 | **工作区选择器 `commandId` 路径 `void executeCommand` 不 catch reject** → 未处理 rejection | A `1db937a63ae` `.catch(onUnexpectedError)`。merge 复测 picker 69 passing。未改 identity-strip。残留 `item.run()` 已由 D158 收 | reject 无未处理 rejection；成功 Sign in 测仍绿 | sessions | closed |
| D158 | P3 | **工作区选择器 `item.run()` 同步调用不 catch reject** → Manage 项走 `run` 优先，D157 覆盖不到 | A `9a0e83bc66e` `Promise.resolve(item.run()).catch(onUnexpectedError)`。merge 复测 picker 70 passing。未改 commandId / identity-strip | reject 无未处理 rejection；D157 Sign in 测仍绿 | sessions | closed |
| D159 | P3 | **`SessionsWindowNotifier` `void _notify(...)` 无 catch** → toast/focus/openSession reject 成未处理 rejection | B `092edec434c` 两处 `.catch(onUnexpectedError)`。merge 复测 notifier 8 passing。未单独锁 Completed/`focus`/`openSession` | NeedsInput + showToast reject 无未处理 rejection；既有 6 测仍绿 | sessions | closed |
| D160 | P3 | **鼠标前进/后退 `void openPrevious/NextSession` 无 catch** → 导航 reject 成未处理 rejection | D `cbb24c0a33b` 两处 `.catch(onUnexpectedError)`。merge 复测 mouse nav 6 passing | back/forward reject 无未处理 rejection；既有 3 测仍绿 | sessions | closed |
| D161 | P3 | **picker `void _dispatchPickerItem`（onSelect / 直跳 browse）无 catch** → `canSelectWorkspace` reject 漏 | A `6cfea5911d9` `.catch(onUnexpectedError)`。merge 复测 picker 71 passing。残留：mobile/web/automations void 入口无 widget 测，不可锁 | delegate.onSelect folder reject 无未处理 rejection；D157/D158 仍绿 | sessions | closed |
| D162 | P3 | **新会话提示选项 `void _select(option)` 无 catch** → `selectOption` reject 成未处理 rejection | A `57c7a6beb36` `.catch(onUnexpectedError)`。merge 复测 prompt options 9 passing。残留 `_selectedOptionId` 回滚已由 D163 收 | click reject 无未处理 rejection；既有测仍绿 | sessions | closed |
| D163 | P3 | **`selectOption` reject 不回滚 `_selectedOptionId`** → 失败后按钮仍像选中 | A `255538f4e54` catch 恢复 previous 再 rethrow。merge 复测 prompt options 10 passing | reject 后选中态回到点击前；D162 测仍绿 | sessions | closed |
| D164 | P3 | **`void _openSessionTurnChanges` 无 catch** → `openChangesEditor` reject 成未处理 rejection | B `8f509ff3ffd` 方法内 try/catch + `onUnexpectedError`。merge 复测 turn changes 10 passing | reject 无未处理 rejection；既有测仍绿 | sessions | closed |
| D165 | P3 | **浏览器 pill `void _openBrowser` 无 catch** → `openEditor`/`getPreferredGroup` reject 漏 | D `dad1f019b24` `.catch(onUnexpectedError)`。merge 复测 browsers 9 passing | reject 无未处理 rejection；既有测仍绿 | sessions | closed |
| D166 | P3 | **session 终端 removed/archived 清理 `void` 无 catch** → `safeDisposeTerminal` reject 漏 | A `6dec56ea1a0` `.catch(onUnexpectedError)`。merge 复测 terminal 78 passing。残留 archived 测已由 D168 收 | removed dispose reject 无未处理 rejection | sessions | closed |
| D168 | P3 | **D166 archived 路径缺 reject 回归测** | A `d552cf5584f` 只加测。merge 复测 terminal 79 passing | archived dispose reject 无未处理 rejection | sessions | closed |
| D169 | P3 | **telemetry `getAllTasks().then` 无 catch** → 新会话首请求 reject 漏 | B `b83a6f9c062` `.catch(onUnexpectedError)`。merge 复测 telemetry 7 passing。残留：同文件其它 `.then` 未扫 | isNewChat reject 无未处理 rejection | sessions | closed |
| D170 | P3 | **`void _registerForScheme` 无 catch** → trigger characters reject 漏 | D `fb01fed6d4d` `.catch(onUnexpectedError)`。merge 复测 completions 7 passing。残留 workbench 侧已由 D171 收 | trigger reject 无未处理 rejection | sessions | closed |
| D171 | P3 | **workbench `void _registerForScheme` 无 catch** | A `2dcb6d6b9bc` 两处 `.catch(onUnexpectedError)`。merge 复测 chatInputCompletions 54 passing | existing/added scheme reject 无未处理 rejection | workbench | closed |
| D172 | P3 | **Inbox Goal/Enqueue `void` click 无 catch** → `input` reject 漏 | B `ab26662bdde` `.catch(onUnexpectedError)`。merge 复测 inbox 30 passing。未改 false notice 合同 | reject 无未处理 rejection、不误弹 notice | conversation | closed |
| D173 | P3 | **Codex `void openCodexAuthUrl` 无 catch** → opener reject 漏 | A `c68ce95365e` auth URL + profile image `.catch(onUnexpectedError)`。merge 复测 12 passing | auth URL reject 无未处理 rejection | workbench | closed |
| D174 | P3 | **单条 feedback `void revealFeedback` 无 catch** | B `b4ef8494a43` `.catch(onUnexpectedError)`。merge 复测 6 passing。残留：context view 同类 void 已由 D473 收（**不重开本行**） | 单条 click reject 无未处理 rejection | sessions | closed |
| D175 | P3 | **Claude 权限 Learn more `void opener.open` 无 catch** → 文档链接 reject 漏 | A `9a51365629c` `.catch(onUnexpectedError)`。merge 复测 picker 4 passing | Learn more opener reject 无未处理 rejection | sessions | closed |
| D176 | P3 | **`void _dispatchActiveClientWhenResolved` 无 catch** → `whenResolved()` reject 漏 | B `e4844ce08ae` `.catch(onUnexpectedError)`。merge 复测 provider 206 passing | scope reject 无未处理 rejection、无 SessionActiveClientSet | sessions | closed |
| D177 | P3 | **untitled mode 回填 `void _applyMode` 无 catch** → `waitForPendingUpdates` reject 漏 | A `18f9ec25bdb` `.catch(onUnexpectedError)`。merge 复测 synchronizer 7 passing | waitForPendingUpdates reject 无未处理 rejection | workbench | closed |
| D178 | P3 | **`void provider.setRootConfigValue` 无 catch** → addMcpServer 写 root config reject 漏 | B `956cab78bbe` `.catch(onUnexpectedError)`。merge 复测 customization 3 passing。未改 workbench 同步 dispatch 副本 | setRootConfigValue reject 无未处理 rejection | sessions | closed |
| D179 | P3 | **快照删除 `void deleteSnapshot` 无 catch** → `dialogService.confirm` reject 漏 | A `33a85adf420` `.catch(onUnexpectedError)`。merge 复测 snapshots 32 passing。未改 restore / deleteThenRefreshList | confirm reject 无未处理 rejection、不发 DeleteSnapshot | conversation | closed |
| D180 | P3 | **自动化卡片删除 `void confirmDelete` 无 catch** → confirm reject 漏 | B `09937b546ea` `.catch(onUnexpectedError)`。merge 复测 automations 54 passing。未改 confirmAndDeleteAutomation / 右键 Delete | 卡片 confirm reject 无未处理 rejection、不删 | sessions | closed |
| D181 | P3 | **`sessionGitHubInfo` PR 号 `lookup.then` 无 catch** → `findPullRequestNumberByHeadBranch` reject 漏（`observableFromPromise` 第二订阅同样裸 then） | A `809535a6150` lookup `.catch` 记错误后回 `undefined`（按无 PR 驱逐可重试）。merge 复测 19 passing | lookup reject 无未处理 rejection | sessions | closed |
| D182 | P3 | **空态 Create `openCreateDialog` 无 catch** → `showAutomationDialog` reject 漏 | B `7092184f4ee` `.catch(onUnexpectedError)`。merge 复测 automations 55 passing。未改 openCreateDialog 本体 / D180 delete | create dialog reject 无未处理 rejection | sessions | closed |
| D183 | P3 | **`SessionsList` `getTreatment().then` 无 catch** → 分组上限实验取值 reject 漏 | A `558e453b187` `.catch(onUnexpectedError)`。merge 复测 sessionsList 82 passing | getTreatment reject 无未处理 rejection | sessions | closed |
| D184 | P3 | **config picker `onSelect` async 无 catch** → AutoApprove `dialogService.prompt` reject 漏 | B `f004fcdce21` `void (async () => …)().catch(onUnexpectedError)`。merge 复测 picker 16 passing。未改 setSessionConfigValue best-effort | prompt reject 无未处理 rejection、不写配置 | sessions | closed |
| D185 | P3 | **Copilot 按分支查 PR `lookup.then` 无 catch** → `findPullRequestNumberByHeadBranch` reject 漏 | A `42e796fd0a9` lookup `.catch` 记错误后回 `undefined`（按无 PR 驱逐可重试）。merge 复测 provider 75 passing | lookup reject 无未处理 rejection | sessions | closed |
| D186 | P3 | **sandbox `_discoverAndSeed` void / 排队 then 无 catch** → `seedSessions` throw 漏 | B `4f93c78dac8` 成功路径 try/catch + `void`/排队 `.catch(onUnexpectedError)`。merge 复测 contribution 15 passing。未改 provision | seed throw 无未处理 rejection | sessions | closed |
| D187 | P3 | **Mode picker 配置 Agents `executeCommand` 无 catch** → OpenEditor reject 漏 | A `4af67197837` `void executeCommand(...).catch(onUnexpectedError)`。merge 复测 modePicker 3 passing。未改 mode 分支 | configure command reject 无未处理 rejection | sessions | closed |
| D188 | P3 | **未登录模型通知三条就绪 `.then` 无 catch** → `getDefaultAccount` / extensions / whenReady reject 漏 | A `0cb46a24224` 三处 `.catch(onUnexpectedError)`。merge 复测 notification 11 passing。未改状态机 | getDefaultAccount reject 无未处理 rejection | workbench | closed |
| D189 | P3 | **Growth session `when(Restored).then` 无 catch** → lifecycle reject 漏 | B `df6572565d3` `.catch(onUnexpectedError)`。merge 复测 growth 8 passing。未改 dismiss / opener | when(Restored) reject 无未处理 rejection | workbench | closed |
| D190 | P3 | **无选中 profile 时 `probeEngine` 无 catch** → Test Connection throw 漏 | A `16b5047e127` 与 profile 分支同级 try/catch 画 test status。merge 复测 pane 102 passing。未改 profile 分支 | probeEngine throw 画 status、无未处理 rejection | conversation | closed |
| D191 | P3 | **Agents Activity `void executeCommand(revealItem)` 无 catch** → reveal reject 漏 | B `2bc77426082` `.catch(onUnexpectedError)`。merge 复测 agents 20 passing。未改 hierarchy / setTarget | reveal reject 无未处理 rejection | navigator | closed |
| D192 | P3 | **草稿换 cwd `void _changeWorkingDirectory` 无 catch** → `getUriTrustInfo` reject 漏 | A `d72003f13eb` 三处 `.catch(onUnexpectedError)`。merge 复测 provisional 52 passing。未改 `_queue` / 信任语义 | trust reject 无未处理 rejection | workbench | closed |
| D193 | P3 | **Agents 断连 Open Connection `executeCommand` 无 catch** → 开 Connection 页 reject 漏 | B `cd87c9623fb` 两处 `.catch(onUnexpectedError)`。merge 复测 catalog 51 passing。未改其它 catalog 节 | Open Connection reject 无未处理 rejection | conversation | closed |
| D167 | P3 | **桌面布局 `void openView/openViewContainer` 无 catch** → 视图打开 reject 漏 | D `f535773c208` 7 处 `.catch(onUnexpectedError)`。merge 复测 layout 140 passing | untitled/restore reject 无未处理 rejection | sessions | closed |
| D194 | P2 | **Composer 麦克风与 Route 下拉是假造 UI**（原登记）。**本切片已闭**：删整条假麦克风流水线与 dock/sessionBar Route SelectBox / `routeIndex`。**OV 跟进已闭**：断连 Agent 只留「No agent」（删 Stub agent）；Model 只留「No model」（无 Stub model）；`sendDockDraft` / 发送不再 `select` 假模型 index 1。**残留（非假造 UI）**：`IUniverseAgentConnection` 仍无 voice/transcript 方法与 routeIndex RPC；将来若产品要真语音/Route，须先有引擎 capability | 用户裁定 DELETE。工位 A `loop/A` 按删流水线 / 删 Route / 删断连 Stub agent·model 落地；未发明引擎 RPC | 麦克风/Route 假造 UI 与锁定假造的测试均不存在；断连 composer 无 Stub agent / Stub model；conversation contrib 无 `getUserMedia` / stub phrase / `'0:01'` 假转写路径 | UI / conversation | closed |
| D195 | P3 | **closed** `tryLoadSourcesGitChangeEntries` 不再吞 `ReadGitSummary` throw。Changes 已 `supported:true` 后 Summary throw 与 Changes throw 同级上抛；Changes/Review catch 画 `sourcesGitReadFailureMessage`，不用引擎 entries 装「无摘要」。工位 B `sources-git-summary-throw` 已收。未占 [D196](#d196)/[D198](#d198) | B 槽已收 | catch 不再静默；Summary throw 与 Changes throw 同级失败面（status）；`sourcesChangesGitRead.test.ts` 锁 throw | sources-git | closed |
| D196 | P3 | **`hasSourcesGitApplyHunksPayload` 只查 `patches.length > 0`**（原登记）。**本切片已闭**：谓词要求至少一条 `trim()` 非空 patch；`['']` / `['  ']` 不算载荷，`tryWriteSourcesGitApplyHunks` 不发 hook。未发明 hunk；未实施 A2；[D195](deferred-gaps.md)/[D198](deferred-gaps.md) 未动 | 工位 D `sources-git-apply-hunks-payload` 已收；A2 仍 blocked（P5 停线） | payload 谓词拒绝空串/空白 patch；补测 `['']` / `['  ']` 不得发 RPC | sources-git | closed |
| D197 | P3 | **Inspect 叶仍持有 `NavigatorSessionLeaseHolder`**（`agentInspectView.ts` `private readonly leaseHolder`），与方案 [navigator-engine-segments §2.5](../plans/navigator-engine-segments.md)「不给 Inspect 单独 lease」及 `agentInspectPanel.test.ts`「AgentInspectView does not hold its own session lease」断言相反。本 slice `navigator-leftover-stub-and-empty-honesty` 未改 `agentInspectView.ts`（只收 ViewTitle Inspect 选中传递）。该测在 tip `8fd03c1236d` 已红 | B 槽 `navigator-inspect-lease-honesty` 已收：`AgentInspectView` 去掉自持 lease / roster / notice，只订 `onDidChangeTarget` + `onDidChangeLiveAgentIds`；`isInspectTargetStale` 对 agent/member 在 `liveAgentIds === undefined` 标 stale（Team catch / tree-empty leftover 不再一律当新鲜）。测锁无 `leaseHolder`、undefined/空集 stale、在树内不 stale。写端哨兵已由 [D205](deferred-gaps.md) 收口 | 去掉 Inspect 的 leaseHolder，跟随只读 Agents/Team `setLiveAgentIds`；该测绿 | navigator / inspect | closed |
| D205 | P3 | **Agents/Team 树空 / pending / UNSUPPORTED 仍写 `setLiveAgentIds(source, undefined)`**（原登记）。**本切片已闭**：可见 leftover 写空 `Set`；叶隐藏 / pending / UNSUPPORTED / no-session 写 `undefined`；Team catch 后 live ids 与空列表一致；`isInspectTargetStale` 对 `undefined` 不标 stale（GC-5d），对空集仍 stale。未撤 Inspect leaseHolder 删除 | 工位 A `navigator-inspect-live-id-honesty` 已收 | 可见叶 leftover 写空 `Set`、叶隐藏才写 `undefined`；Team catch 后 live ids 与列表一致；GC-5d 两叶隐藏不标 stale；补测 | navigator / inspect | closed |
| D198 | P3 | **closed** 非空 session + `ReadGitChanges` `supported:true` + `entries=[]` 不再盖本地 SCM。`tryLoadGitEntries` / `usingGitRead` 空引擎列表与 `supported:false` 同回落 SCM + `sourcesGitLocalOnlyMessage`；`tryLoadSourcesGitChangeEntries` 空数组回 `undefined`。未发明文案；未实施 A2；未占 [D195](#d195)/[D196](#d196) | 工位 A `sources-git-empty-entries-scm` 已收 | 空引擎列表回落 SCM、不藏 leftover；Changes/Review leftover 测 + `tryLoad` 空数组 → undefined | sources-git | closed |
| D199 | P3 | **closed** MCP Add 无完整 config 不发 RPC、不发明 `echo`/`mcp-stub`；`isCompleteMcpAddConfig` 门 stdio command / sse·http url | 工位 D 已收 | Add 无完整 config 不发 RPC、不发明 command；补测无 `echo`/`mcp-stub` 默认 | conversation / catalog | closed |
| D200 | P3 | **closed** Skills / Plugins `UNKNOWN` 先 `clearCatalogPresentation` 再 loading；成功→UNKNOWN 测 `getListEntryCount()===0` | 工位 D 已收 | UNKNOWN 先清行再 loading；补成功→UNKNOWN 测：`getListEntryCount()===0` | conversation / catalog | closed |
| D206 | P3 | **closed** MCP `UNKNOWN` 先 `clearCatalogPresentation` 再 loading（D200 Skills/Plugins 同胞）；成功→UNKNOWN 测 `getMode()==='loading'` 且 `getListEntryCount()===0` | 工位 C 已收 | UNKNOWN 先清行再 loading/probing；不发 `listMcpServers`；补成功→UNKNOWN 测 | conversation / catalog | closed |
| D208 | P3 | **closed** Agents `UNKNOWN` 先 `clearCatalogPresentation` 再 loading（D200/D206 同胞）；成功→UNKNOWN 测 `getMode()==='loading'` 且 `getListEntryCount()===0` | 工位 B 已收 | UNKNOWN 先清行再 loading；不发 `listAgentProfiles`；补成功→UNKNOWN 测 | conversation / catalog | closed |
| D209 | P3 | **closed** Tools `UNKNOWN` 先 `clearCatalogPresentation` 再 loading（D200/D206/D208 同胞）；成功→UNKNOWN 测 `getMode()==='loading'` 且 `getListEntryCount()===0` | 工位 B 已收 | UNKNOWN 先清行再 loading；不发 `listTools`；补成功→UNKNOWN 测 | conversation / catalog | closed |
| D201 | P3 | **closed** Triggers 写成功后再 `ListTriggers` fail 不回刷 delete-success；`refresh()` 返回 listed 才回写 | 工位 D 已收 | list-fail 后清行 + 不回刷 delete-success；改掉锁死 leftover 的测 | conversation / catalog | closed |
| D202 | P3 | **Fork `return true` 仍把未真正 fork 的结果标成已处理**（原登记）。**本切片已闭**：`tryConnectedEngineFork` 分 `handled` / `forked`；`forkSubAgent` false 后 notice + `_tryForkAsChat` `return false`；`shouldSkipContributedForkFallback` 挡住本地 fallthrough。测锁 `handled !== forked` | 工位 A `conversation-fork-unknown-honesty` 已收 | 失败路径 `return false` 或明确 `handled` 契约；调用方不得把 notice 当成功 fork；补测 | conversation | closed |
| D203 | P3 | **`deleteSession` 失败后 composer draft 未回滚**（原登记）。**本切片已闭**：C 槽 delete 前快照 textarea；`false` 挂回原 session；optimistic `true` 仍删草稿，session 再现或 `onDidFailEngineAction('deleteSession')` 再 write + textarea。测锁 false 与 rollback | 工位 C `session-delete-draft-rollback` 已收 | 删除失败/回滚后 draft 仍挂回原 session；`conversationLens.test.ts` 锁失败不丢草稿 | conversation | closed |
| D204 | P3 | **capability `UNKNOWN` 时 composer catalog 被画成空**（原登记）。**本切片已闭**：`loadConnectedComposerCatalogs` 对 UNKNOWN 保末次成功行，无末次则画 probing；不打 list。`conversationComposerCatalog.test.ts` 锁 probing + keep-last | 工位 A `conversation-fork-unknown-honesty` 已收 | UNKNOWN 保末次成功行或显式 probing，不得装成「引擎没有 catalog」；补 `conversationComposerCatalog.test.ts` | conversation | closed |
| D207 | P3 | **closed** Projects 干净断连（`wasEverConnected && !engineConnected && !transportFailed`）不再把 leftover sessions 画成活 workdir。`buildNavigatorProjectsTree` 复用 `NAVIGATOR_STALE_SNAPSHOT_COPY`，保留 leftover 行。未改 transportFailed 注路径；未占 [D198](#d198) | 工位 D `navigator-projects-clean-disconnect-stale` 已收 | 干净断连 leftover 有 stale note 且仍见 leftover 行；补 `navigatorProjectsTree.test.ts` | navigator / projects | closed |
| D210 | P3 | **closed** ProviderModel `UNKNOWN` 先 `clearModelPresentation` 再 loading（D200/D206 同胞）；成功→UNKNOWN 测 `getMode()==='loading'` 且 `getListEntryCount()===0`。Rules 无 list RPC / 无 catalog 行，`render` 已藏 scope，跳过 | 工位 C 已收 | UNKNOWN 先清行再 loading；不发 `listModels`；补成功→UNKNOWN 测 | conversation / catalog | closed |
| D211 | P3 | **closed** McpRuntime `UNKNOWN` 先 `clearRuntimePresentation` 再 loading（D206 MCP 同胞）；成功→UNKNOWN 测 `getMode()==='loading'` 且 `getListEntryCount()===0` | 工位 C 已收 | UNKNOWN 先清行再 loading/probing；不发 `getMcpServerStatuses`；补成功→UNKNOWN 测 | conversation / catalog | closed |
| D212 | P3 | **closed** Clipboard Write/Clear 写成功后再 `ListClipboard` fail 不回刷 clipId / removedCount 成功文案（D201 Triggers 同胞）。`refresh()` 返回 listed 才回写。未占 Sessions stub / Projects rebuildTree / catalog UNKNOWN / D22 / D24 / D25 / D26 / A2 / mic·Route / `.sessions` | 工位 C `clipboard-write-list-fail` 已收 | list-fail 后清 write/clear 成功文案 + catalog failed；补成功→list-fail 测 | conversation / catalog | closed |
| D213 | P3 | **closed** Sessions 侧栏空态不再把 stub conversation 当产品路径。`conversationSessionsView` 空文案去 `in-memory` / stub；保留 New session 为真实本地动作，不发明引擎会话。测锁空态与接通后空名单 | 工位 A 已收 | 空/断连态不广告 stub conversation；New session 仍为真实动作；无 invented engine session 文案；补测 | conversation / sessions | closed |
| D214 | P3 | **closed** Projects 外层 `rebuildTree` catch 静默保 last-good，throw 后 leftover 仍像活树。未撤 [D207](#d207) 干净断连 builder note。catch 保 leftover 行并复用 `NAVIGATOR_STALE_SNAPSHOT_COPY`（树 note + 既有 recents status）。未占 D212/D213 | 工位 B `navigator-projects-rebuild-throw-stale` 已收 | throw 后 leftover 行仍在且 stale / status 可见，不得装成活树；补 `navigatorProjectsList.test.ts`；D207 树测仍绿 | navigator / projects | closed |
| D215 | P3 | **closed** Sources Files `fetchChildren` throw 不再装「No workspace files」。`collectFromItem` 上抛；成功后再 throw 保 leftover 行并画 `.sources-files-status`；首拉 throw 画 `sourcesFilesListReadFailureMessage`。未占 Sessions stub / Projects rebuildTree / catalog UNKNOWN | 工位 D `sources-files-fetch-leftover` 已收 | throw 不装空成功；leftover 行仍在 + 失败 status；补 `sourcesFilesList.test.ts` / model 测 | sources / files | closed |
| D216 | P3 | **closed** Plugins `scanNew` 写成功后再 `listPlugins` fail 不回刷 lastScan 成功文案（D201 Triggers / D212 Clipboard 同胞；D155 未收 scanNew）。`refresh()` 返回 listed 才回写 lastScan。未占 D22 / D24 / D25 / D26 / A2 / mic·Route / proto / 引擎仓 / `.sessions` | 工位 A `plugins-scan-list-fail` 已收 | list-fail 后清 scan-success + catalog failed；补成功→list-fail 测；listed 成功路径仍见 scan-success | conversation / catalog | closed |
| D217 | P3 | **closed** MCP Add/Update/Remove/Toggle 写成功后再 `listMcpServers` fail 不回刷 Added./Updated./Removed./Toggled.（D201 同胞）。`refresh()` 返回 listed 才回写。未占 Plugins scanNew（D216）；未发明 Add forms | 工位 B `catalog-write-list-fail` 已收 | list-fail 后清成功文案 + catalog failed；既有成功→list-fail 测 | conversation / catalog | closed |
| D218 | P3 | **closed** Skills create/toggle/save 写成功后再 `listSkills` fail 不回刷 Created./Updated./Saved.（D201 同胞）。`refresh()` 返回 listed 才回写。未发明 Add forms | 工位 B `catalog-write-list-fail` 已收 | list-fail 后清成功文案 + catalog failed；既有成功→list-fail 测 | conversation / catalog | closed |
| D219 | P3 | **closed** Agents New/Delete/Reset/Save 写成功后再 `listAgentProfiles` fail 不回刷 Created./Deleted./Reset./Saved.（D201 同胞）。`refresh()` 返回 listed 才回写。未发明 Add forms | 工位 B `catalog-write-list-fail` 已收 | list-fail 后清成功文案 + catalog failed；既有成功→list-fail 测 | conversation / catalog | closed |
| D220 | P3 | **closed** Agents Activity 成功画 leftover 后 `isAgentTreeFetchFailed` 不再把旧工具行装成活数据。`setActivityFromSnapshot` 在 fetch-fail 时保 leftover 行并画 `NAVIGATOR_ACTIVITY_FETCH_FAILED_COPY`；首拉 fail 空态仍「Failed to read tool activity」。未占 catalog write/list-fail / conversation leftover / D22 / D24 / D26 / A2 / mic·Route / 引擎仓 | 工位 D `navigator-agents-activity-fetch-leftover` 已收 | leftover 行仍在 + 失败 note；不得装空成功；补 `navigatorAgentsSubviews.test.ts`；首拉 fail 测仍绿 | navigator / agents | closed |
| D221 | P3 | **closed** PairApprove / PairReject 写成功后再 ListDevices / ListPending fail 不回刷 pair-success（D212 Clipboard 同胞）。`refreshEngineDeviceLists()` 返回 listed 才回写。未占 catalog write/list-fail / D22 / D24 / D26 / A2 / mic·Route / `.sessions` | 工位 C `connection-pair-write-list-fail` 已收 | list-fail 后清 pair 成功文案 + devices/pending failed；补成功→list-fail 测 | conversation / connection | closed |
| D222 | P3 | **closed** Plugins Enable/Reload/Unload 写成功后再 `listPlugins` fail 不回刷 Enabled./Reloaded./Unloaded.（D201 / D216 同胞；D155 用 `canPerformCatalogWrite(this.mode)` 代理回写）。`refresh()` 返回 listed 才回写。未占 MCP/Skills/Agents / connection / navigator / D22 / D24 / D26 / A2 / mic·Route / proto / 引擎仓 / `.sessions` | 工位 A `plugins-enable-listed-gate` 已收 | write-ok + list-fail 不回刷三成功文案；既有 `enginePluginsSection.test.ts` enable/reload/unload list-fail 测仍绿 | conversation / catalog | closed |
| D223 | P3 | **closed** Agents `saveAgentsMarkdown` 写成功后再 `listAgentProfiles` fail 不回刷 editor/catalog **Saved.**（D219 同胞）。`saveSelectedProfile` 已 listed 闸；二次回刷改闸 `listed`（同 `restoreCatalogWriteSuccessAfterRefresh`），不用 `canPerformCatalogWrite`（mode 在 leftover `ready`/`empty` 时是代理）。未发明 Add forms / 新 RPC | 工位 B `agents-markdown-listed-gate` 已收 | markdown Save 写-ok + list-fail 不回刷 Saved.；leftover ready 二次回刷测锁 listed 闸 | conversation / catalog | closed |
| D224 | P3 | **closed** Hub `confirmDeviceCode` / Rename / Revoke 写成功后再 `refreshDirectory` 非 throw fail 不回刷「Device code confirmed」/ revoke `result.message` / leftover 活目录（D221 同胞）。`refreshDirectoryListed()` 返回 listed 才回写。未重做 PairApprove/Reject；未发明 list RPC；未占 catalog / navigator / D22 / D24 / D26 / A2 / mic·Route / `.sessions` | 工位 C `connection-hub-write-list-fail` 已收 | write-ok + directory refresh fail 后清成功文案 + leftover；补成功→refresh-fail 测 | conversation / connection | closed |
| D225 | P3 | **closed** Context Variables `refresh()` 不再在 `listContextVariable` 前清空 leftover（D214 / D215 同胞）。成功后再 throw 保 leftover 行并画 failed；首拉 throw 仍 empty+failed。不得画「No context variables.」接通空成功。未占 Plugins/MCP/Skills/Agents write gates / connection pane / navigator / proto / 引擎仓 | 工位 D `context-variable-list-leftover` 已收 | 成功→list throw leftover 行仍在 + failed；不得装空成功；首拉 throw 仍 empty+failed；补 `engineContextVariableSection.test.ts` | conversation / context-variables | closed |
| D226 | P3 | **closed** Tools `refresh()` catch 在活画后不再 `clearCatalogPresentation()`（D225 同胞；不再走 D46 空失败）。成功后再 `listTools` / `listAgentProfiles` throw 保 leftover 行并画 failed；首拉 throw 仍 empty+failed。不得画「No engine tools yet.」接通空成功。未占 MCP Runtime / Agents / Plugins / connection pane / navigator / Context Variables / proto / 引擎仓 | 工位 A `tools-list-leftover` 已收 | 成功→list throw leftover 行仍在 + failed；不得装空成功；首拉 throw 仍 empty+failed；补 `engineCatalogSections.test.ts` | conversation / tools | closed |
| D227 | P3 | **closed** MCP Runtime `refresh()` catch 不再在活画后 `clearRuntimePresentation()`（D225 同胞）。成功后再 `getMcpServerStatuses` throw 保 leftover 行并画 failed；首拉 throw 仍 empty+failed。不得画「No MCP servers in runtime.」接通空成功。未发明 write RPC；未占 Tools / Agents / Plugins / connection pane / navigator / context variables | 工位 B `mcp-runtime-list-leftover` 已收 | 成功→list throw leftover 行仍在 + failed；不得装空成功；首拉 throw 仍 empty+failed；补 `engineMcpRuntimePanel.test.ts` | conversation / mcp-runtime | closed |
| D228 | P3 | **closed** Hub Refresh 按钮 `refreshDirectory` 非 throw fail 不再装作活目录；RotateToken 写成功后不发明 rotate-list RPC，复用 `refreshDirectoryListed` / `applyHubDirectoryRefreshFailure`，listed（且 engine device lists 未失败）才回刷 `result.message`（D224 同胞）。未重做 PairApprove/Reject 与 D224 confirm/rename/revoke 写路径。未占 catalog / navigator / proto / 引擎仓 / D22 / D26 / A2 / mic·Route / `.sessions` | 工位 C `connection-hub-refresh-rotate` 已收 | Refresh 非 throw fail 清 leftover 或标 failed；Rotate 写-ok 不得在 directory/device list fail 上留成功横幅；补 `connectionPreferencesPane.test.ts` | conversation / connection | closed |
| D229 | P3 | **closed** Agents Hierarchy 成功画 leftover 后 `isAgentTreeFetchFailed` 不再走 `setHierarchyState([], pendingCopy)` 把旧树节点装成首拉空失败。`setHierarchyAfterTreeFetchFail` 保 leftover 节点并画 `NAVIGATOR_AGENT_TREE_FETCH_FAILED_COPY`；首拉 fail 空态仍「Failed to read the agent tree」。未重做 Activity（仅共用空态 copy）；未占 catalog / connection / proto / 引擎仓 / D22 / D26 / A2 / mic·Route | 工位 D `navigator-hierarchy-fetch-leftover` 已收 | leftover 节点仍在 + 失败 note；不得装活树；首拉 fail 仍 empty+failed；补 `navigatorAgentsSubviews.test.ts` | navigator / agents | closed |
| D230 | P3 | **closed** Plugins `refresh()` catch 在活画后不再 `clearCatalogPresentation()`（D226 / D225 同胞；不再走 D46 空失败）。成功后再 `listPlugins` throw 保 leftover 行并画 failed；首拉 throw 仍 empty+failed。不得画「No engine plugins.」接通空成功。写/扫描成功文案仍在 list-fail 时清掉（D216 / D222 不回退）。未占 Skills/MCP/Agents/Tools/connection/navigator / proto / 引擎仓 / D22 / D26 / A2 / mic·Route | 工位 A `plugins-list-leftover` 已收 | 成功→list throw leftover 行仍在 + failed；不得装空成功；首拉 throw 仍 empty+failed；补 `enginePluginsSection.test.ts` | conversation / plugins | closed |
| D231 | P3 | **closed** Skills `refresh()` catch 在活画后不再 `clearCatalogPresentation()`（D226/D225 同胞；不再走 D49 空失败）。成功后再 `listSkills` throw 保 leftover 行并画 failed；首拉 throw 仍 empty+failed。不得画「No skills yet.」接通空成功。未占 Plugins/MCP/Agents/Tools/connection/navigator / proto / 引擎仓 | 工位 B `skills-list-leftover` 已收 | 成功→listSkills throw leftover 行仍在 + failed；不得装空成功；首拉 throw 仍 empty+failed；补 `engineSkillsSection.test.ts` | conversation / skills | closed |
| D232 | P3 | **closed** MCP Definitions `refresh()` catch 在活画后不再 `clearCatalogPresentation()`（D226 / D225 同胞；不再走 D49 空失败）。成功后再 `listMcpServers` throw 保 leftover 行并画 failed；首拉 throw 仍 empty+failed。不得画「No MCP servers yet.」接通空成功。未占 MCP Runtime（D227）/ Skills/Plugins/Agents/Tools / connection pane / navigator / proto / 引擎仓 | 工位 C `mcp-defs-list-leftover` 已收 | 成功→list throw leftover 行仍在 + failed；不得装空成功；首拉 throw 仍 empty+failed；补 `engineCatalogSections.test.ts` | conversation / mcp-definitions | closed |
| D233 | P3 | **closed** Agents `refresh()` catch 在活画后不再 `clearCatalogPresentation()`（D226 / D225 同胞；不再走 D46 空失败）。成功后再 `listAgentProfiles` throw 保 leftover 行并画 failed；首拉 throw 仍 empty+failed。不得画「No agent profiles yet.」接通空成功。未重做 markdown listed 闸（D223）；未占 Tools / MCP / Skills / Plugins / connection pane / navigator / proto / 引擎仓 | 工位 D `agents-list-leftover` 已收 | 成功→list throw leftover 行仍在 + failed；不得装空成功；首拉 throw 仍 empty+failed；补 `engineCatalogSections.test.ts` | conversation / agents | closed |
| D234 | P3 | **closed** ProviderModel `refreshModels()` catch 在活画后不再 `clearModelPresentation()`（D226 / D230 / D231 同胞）。成功后再 `listModels` throw 保 leftover 行并画 failed；首拉 throw 仍 empty+failed。不得画「No models in the registry.」接通空成功。未重开 D210：capability `UNKNOWN` 仍先清再 loading（该支无 list RPC）。未占 Plugins/Skills/MCP/Agents/Tools / connection pane / navigator / proto / 引擎仓 / D22 / D26 / A2 / mic·Route | 工位 A `provider-model-list-leftover` 已收 | 成功→listModels throw leftover 行仍在 + failed；不得装空成功；首拉 throw 仍 empty+failed；补 `engineProviderModelSection.test.ts`（`getListEntryCount()` / 列表可见性 + failed） | conversation / provider-model | closed |
| D235 | P3 | **closed** Composer `loadConnectedComposerCatalogs` 在 capability **SUPPORTED** 时 list throw 不再把活 catalog 画成 No agent / No model / 空 tools（D50 空失败的反面；D225 leftover 诚实）。catch 保 `rememberLastGoodComposerCatalog`，抵消 `refreshComposerCatalogs` 预清空；无 last-good 首拉 throw 仍空。未重开 D204 UNKNOWN probing；未改断连 refresh | 工位 B `composer-supported-throw-keep-last` 已收 | 成功→SUPPORTED throw 末次 agent/model/tools 仍在；首拉 throw 仍 No agent / No model / 空 tools；补 `conversationComposerCatalog.test.ts` | conversation / composer | closed |
| D236 | P3 | **closed** Team `refreshTeamData()` catch 在活画后不再 `setMemberEntries([], TEAM_FETCH_FAILED_COPY)` + `setTaskEntries([], …)` 把 leftover 卸掉（D53 已闭为清 leftover；本波与 D225/D229 对齐为保 leftover + failed note）。成功后再 `memberStatus` / `taskList` throw 保 leftover 行并画失败 note；首拉 throw 仍 empty+failed。不得装首拉空失败或「无成员」空成功。未占 Hierarchy fetch-fail（D229）/ Team `treeEmpty` / pending wipe / catalog / connection / proto / 引擎仓 / D22 / D26 / A2 / mic·Route | 工位 C `team-refresh-leftover` 已收 | 成功→throw leftover 行仍在 + failed note；不得装空失败/「无成员」空成功；首拉 throw 仍 empty+failed；补 `navigatorTeamSubviews.test.ts` | navigator / team | closed |
| D237 | P3 | **closed** Agents Hierarchy 成功画 leftover 后再进 pending（capability probing / liveTree 暂缺，非 `isAgentTreeFetchFailed`）不再走 `setHierarchyState([], pendingCopy)` 把旧树节点装成首拉空。`setHierarchyAfterPending` 保 leftover 节点并画 pending note；首拉 pending 仍 empty+pending copy。未重做 D229 fetch-fail helper；未占 Team list / Activity leftover / catalog / connection / proto / 引擎仓 / D22 / D26 / A2 / mic·Route | 工位 D `navigator-hierarchy-pending-leftover` 已收 | leftover 节点仍在 + pending note；不得装活树或首拉空；首拉 pending 仍 empty+pending copy；补 `navigatorAgentsSubviews.test.ts` | navigator / agents | closed |
| D238 | P3 | **closed** MCP Runtime `loadTools()` catch 在活画后不再 `this.tools = []` + `DOM.clearNode(this.toolsList)`（D67 已闭为清 leftover；本波与 D225/D227 对齐为保 leftover + failed）。成功后再 `getMcpServerTools` throw 保 leftover 工具行并画 failed；首拉 throw 仍 empty+failed。不得画「No tools on this MCP server.」接通空成功。未重做 D227 runtime server list leftover（`refresh()` catch）。未占 catalog / connection / navigator / proto / 引擎仓 / D22 / D26 / A2 / mic·Route | 工位 A `mcp-runtime-tools-leftover` 已收 | 成功→getMcpServerTools throw leftover 工具行仍在 + failed；不得装空成功；首拉 throw 仍 empty+failed；补 `engineMcpRuntimePanel.test.ts`（行数/可见性 + failed） | conversation / mcp-runtime | closed |
| D239 | P3 | **closed** Clipboard `refresh()` 不再在 `listClipboard` 前 `entries=[]` + `DOM.clearNode`（D226 / D225 同胞）。成功后再 throw 保 leftover 行并画 failed；首拉 throw 仍 empty+failed。不得画「No clipboard entries.」接通空成功。写/清成功文案仍在 list-fail 时清掉（D212 listed-gate 不回退）。未占 Triggers / MCP Runtime / Team / Hierarchy / Composer / proto / 引擎仓 / F3 / A2 / D26 / mic·Route / `.sessions` | 工位 B `clipboard-list-leftover` 已收 | 成功→list throw leftover 行仍在 + failed；不得装空成功；首拉 throw 仍 empty+failed；Write/Clear 成功文案 list-fail 不回刷；补 `engineClipboardSection.test.ts` | conversation / clipboard | closed |
| D240 | P3 | **closed** Triggers `refresh()` 打 list 前不再 `this.triggers = []` + `DOM.clearNode(this.listHost)`（Clipboard leftover / D226 同胞）。成功后再 `listTriggers` throw 保 leftover 行并画 failed；首拉 throw 仍 empty+failed。不得画「No triggers.」接通空成功。Fire/Upsert/Delete 写成功仍 `refresh()` 返回 listed 才回刷；leftover list-fail 清写成功文案。未占 Clipboard/MCP Runtime/Team/Hierarchy/Composer / proto / 引擎仓 / F3/A2/D26 / mic·Route / `.sessions` | 工位 C `triggers-list-leftover` 已收 | 成功→list throw leftover 行仍在 + failed；不得装空成功；首拉 throw 仍 empty+failed；listed-gate 仍诚实；补 `engineTriggersSection.test.ts` | conversation / triggers | closed |
| D241 | P3 | **closed** Team `refreshTeamData()` 在活画后再进 `treeEmpty` / pending（非 catch throw）不再 `setMemberEntries([], treeEmpty)` + `setTaskEntries([], treeEmpty)` 把 leftover 成员/任务卸掉。`setTeamAfterTreeEmpty` 保 leftover 行并画 pending/empty-tree note；首拉 pending/treeEmpty 仍 empty+copy。未重做 D236 catch；未占 Hierarchy / catalog / conversation / proto / 引擎仓 / D22 / D26 / A2 / mic·Route | 工位 D `team-treeempty-pending-leftover` 已收 | leftover 行仍在 + pending/empty-tree note；不得装「无成员」空成功或首拉空；首拉 pending/treeEmpty 仍 empty+copy；补 `navigatorTeamSubviews.test.ts` | navigator / team | closed |
| D242 | P3 | **closed** Snapshots overlay `refresh()` catch 走 `paintStatus`（会 `reset(this.body)` 卸活行）。成功后再 `listSnapshots` throw 保 leftover 行并画 failed；首拉 throw 仍 empty+failed。不得画 `conversationLensSessionBarSnapshotsEmpty` 空成功。Restore/Delete 写成功仍 `refresh()` 返回 listed 才回刷；leftover list-fail 清 Restored./Deleted.（D54/D154 listed-gate 不回退）。未占 catalog / connection / navigator / proto / 引擎仓 / F3 / A2 / D26 / mic·Route / `.sessions` | 工位 A `snapshots-list-leftover` 已收 | 成功→list throw leftover 行仍在 + failed；不得装空成功；首拉 throw 仍 empty+failed；listed-gate 仍诚实；补 `conversationEngineSnapshotsList.test.ts` | conversation | closed |
| D243 | P3 | **closed** Plugins `loadInfo()` / `getPluginInfo` catch 在活画 hook 行后不再 `hookEntries = []` + `clearHookRows()`（D238 MCP Runtime tools 同胞）。成功后再 `getPluginInfo` throw 保 leftover hook 行并画 failed；首拉 throw 仍 empty+failed。不得画「No hooks.」接通空成功。未重做 D230 catalog list leftover；未重做 D216/D222 listed-gate。未占 Snapshots/Sources/Triggers/Clipboard/Team / proto / 引擎仓 / F3/A2/D26 / mic·Route / `.sessions` | 工位 B `plugins-info-leftover` 已收 | 成功→getPluginInfo throw leftover hook 行仍在 + failed；不得装空成功；首拉 throw 仍 empty+failed；补 `enginePluginsSection.test.ts`（`getHookRowCount()` / 可见性 + failed） | conversation / plugins | closed |
| D244 | P3 | **closed** Sources Changes `refresh()` catch 在活画后不再 `allEntries = []` 把 leftover 变更行卸掉（Files leftover / D225 同胞）。成功后再 git-read throw 保 leftover 行并画 `sourcesGitReadFailureMessage`；首拉 throw 仍 empty+failed。未重开 D195 summary swallow；未实施 A2 / F4 / WriteGitUnstage；未改 Review list。 | 工位 C `changes-git-read-leftover` 已收 | 成功→git-read throw leftover 行仍在 + 失败文案；不得装空工作区成功；首拉 throw 仍 empty+failed；补 `sourcesChangesList.test.ts` | sources / changes | closed |
| D245 | P3 | **closed** Sources Review `refresh()` catch 在活画后不再 `this.allEntries = []` 把 leftover 审阅行卸掉（Changes leftover 同胞；D225 系）。成功后再 `readGitChanges` throw 保 leftover 行并画 git-read 失败文案；首拉 throw 仍 empty+failed。未改 Changes 列表；未发明 WriteGitUnstage；未开 A2 / F4；未重开 D195 | 工位 D `review-git-read-leftover` 已收 | 成功→git-read throw leftover 行仍在 + `sourcesGitReadFailureMessage`；首拉 throw 仍 empty+failed；补 `sourcesReviewList.test.ts` | sources / review | closed |
| D246 | P3 | **closed** SessionBar History overlay `refresh()` catch（以及 refresh 开头 loading）走 `paintStatus()`，会 `reset(this.body)` 卸已画 history 行（Snapshots D242 同构）。成功后再 `getHistory` throw 保 leftover 行并画 failed；首拉 throw 仍 empty+failed。活画后 refresh 开头不再 `paintStatus(loading)`。不得画 `conversationLensSessionBarHistoryEmpty` 空成功、不得只剩 loading。断连清行合同不回退。未占 Snapshots / Sources / Plugins / Hub·Connection / Composer / catalog·tools / navigator / proto / 引擎仓 / F3 / A2 / D26 / mic·Route / `.sessions` | 工位 A `history-list-leftover` 已收 | 成功→getHistory throw leftover 行仍在 + failed；不得装空成功 / 只剩 loading；首拉 throw 仍 empty+failed；断连仍清行；补 `conversationEngineHistoryList.test.ts` | conversation | closed |
| D247 | P3 | **closed** Agents 详情 Tools 页 `ensureAgentToolsLoaded()` catch 在活画后不再 `this.agentTools = []`（D238 MCP Runtime tools / D243 Plugins hooks 同胞；不是 D233 catalog list leftover）。成功后再 `listTools` throw 保 leftover 工具行并画 toolsStatus failed；首拉 throw 仍 empty+failed。Agents profile 列表 leftover（D233）不回退。未改 `refresh()` catalog catch / UNKNOWN 清 catalog（D210）/ markdown listed-gate（D223） | 工位 B `agents-tools-leftover` 已收 | 成功→listTools throw leftover 工具名仍在 + failed；不得装「No engine tools…」空成功；首拉 throw 仍 empty+failed；catalog 列表仍在；补 `engineCatalogSections.test.ts`（行可见性 + failed） | conversation / agents-tools | closed |
| D248 | P3 | **closed** Hub directory 活画后再 `refreshDirectory` fail/throw，`applyHubDirectoryRefreshFailure` / `renderHubDirectory` 不再 `hubDevices = []` 卸 leftover（**反转 D228 leftover-clear**；本波与 D225/D236 对齐为保 leftover + failed banner）。写成功文案（confirm/rename/revoke/Rotate）在 directory list-fail 时仍清掉（D224/D228 **listed-gate 不回退**）。未重做 PairApprove/Reject、engine `listDevices` leftover（`enginePairedDevices` 支已保）、pending pairs。未发明 rotate-list RPC。 | 工位 C `hub-directory-leftover` 已收 | 成功画过后再 Refresh fail leftover 行仍在 + failed banner；首拉 fail 仍 empty+failed；listed-gate 仍诚实；补 `connectionPreferencesPane.test.ts` | conversation / connection | closed |
| D249 | P3 | **closed** Composer `refreshComposerCatalogs()` 接通支路不再在 `void loadConnectedComposerCatalogs` 之前同步画 No agent / No model / 空 tools。有 last-good 时同步 `keepLastGoodComposerCatalogOrEmpty`（复用 D235 restore），再异步 load；无 last-good 首拉仍先空；SUPPORTED throw 无 last-good 仍空。未重开 D204 UNKNOWN probing；未改 D235 throw restore；未改断连 refresh | 工位 D `composer-preclear-keep-last` 已收 | 成功画过后再 refresh：load 完成前仍见 Coder / gpt-test / bash，不得闪空成功；首拉无 last-good 仍可先空；补 `conversationComposerCatalog.test.ts` 同步断言 | conversation / composer | closed |
| D250 | P3 | **closed** Plugins `refresh()` 在 capability `UNKNOWN` 时不再无条件 `clearCatalogPresentation()`（D204 Composer UNKNOWN probing+keep-last 同胞；catalog list throw leftover 仍是 D230）。成功画过后再进 probing 保 leftover 行并画 capability loading；首拉无 leftover 仍 empty + capability loading。不得画「No engine plugins.」接通空成功。未改 list throw leftover（D230）、listed-gate（D216/D222）、hooks leftover（D243）。未占 Skills/MCP/Agents/Tools / Composer / ProviderModel / Hub / Sources / navigator / proto / 引擎仓 / F3 / A2 / D26 / mic·Route / `.sessions` | 工位 A `plugins-unknown-leftover` 已收 @2026-09-10 | 成功→UNKNOWN leftover 行仍在 + capability loading；不得装空成功；首拉 UNKNOWN 仍 empty+capability loading；补 `enginePluginsSection.test.ts` | conversation / plugins | closed |
| D251 | P3 | **closed** Skills `refresh()` 在 capability `UNKNOWN` 时不再无条件 `clearCatalogPresentation()`（对齐 D204 Composer UNKNOWN keep-last / D231 list-throw leftover）。成功画过 skill 行后再变 UNKNOWN：保 leftover 行并画 capability loading；首拉 UNKNOWN 仍 empty + capability loading。不得画「No skills yet.」空成功。未改 list throw leftover（D231）、write listed-gate。未占 Plugins / MCP / Tools / Agents / ProviderModel / Composer / Hub / Sources / navigator / proto / 引擎仓 / F3 / A2 / D26 / mic·Route / `.sessions` | 工位 B `skills-unknown-leftover` 已收 | 成功→UNKNOWN leftover 行仍在 + capability loading；不得装空成功；首拉 UNKNOWN 仍 empty+capability loading；补 `engineSkillsSection.test.ts` | conversation / skills | closed |
| D252 | P3 | **closed** ProviderModel `refreshModels()` 在 capability `UNKNOWN` 时不再先 `clearModelPresentation()`（**反转 D210 / D234 UNKNOWN 先清**；对齐 D204 Composer UNKNOWN keep-last）。活画后再进 UNKNOWN 保 leftover 行 + capability loading（`Confirming engine capability…`），不得装「No models in the registry.」空成功。**不是发明 models list RPC**（该支仍不打 `listModels`）。首拉 UNKNOWN 仍 empty + capability loading（D210 首拉 probing 不回退成假列表）。未改 D234 list throw leftover。listed-gate 不涉及。未占 Plugins / Skills / MCP / Tools / Agents / Composer / Hub / Sources / navigator / proto / 引擎仓 / F3 / A2 / D26 / mic·Route / `.sessions` | 工位 C `provider-model-unknown-leftover` 已收 | 成功→UNKNOWN leftover 行仍在 + capability loading；不得装空成功；首拉 UNKNOWN 仍 empty+capability loading；不发明 list RPC；不重做 D234 throw leftover；补 `engineProviderModelSection.test.ts`（`getListEntryCount()` / 可见性 + loadingKind capability） | conversation / provider-model | closed |
| D253 | P3 | **closed** Agents catalog `refresh()` 在 capability `UNKNOWN` 时不再无条件 `clearCatalogPresentation()`（D204 Composer UNKNOWN keep-last / D233 list-throw leftover 同胞）。成功画过 profile 行后再 probing：保 leftover 行并画 capability loading；首拉 UNKNOWN 仍 empty + capability loading。不得画「No agent profiles yet.」空成功。只改 UNKNOWN 支路；未改 Tools leftover（D247）、catalog list throw（D233）、markdown listed-gate（D223） | 工位 D `agents-unknown-leftover` 已收 | 成功→UNKNOWN leftover 行仍在 + capability loading；不得装空成功；首拉 UNKNOWN 仍 empty+capability loading；补 `engineCatalogSections.test.ts`（行数/可见性 + loadingKind capability） | conversation / agents | closed |
| D254 | P3 | **closed** MCP Definitions `refresh()` 在 capability `UNKNOWN` 时不再无条件 `clearCatalogPresentation()`（D204 Composer UNKNOWN keep-last / D232 list-throw leftover 同胞）。成功画过 server 行后再 probing：保 leftover 行并画 capability loading；首拉 UNKNOWN 仍 empty + capability loading。不得画「No MCP servers yet.」空成功。只改 UNKNOWN 支路；未发明 list RPC；未改 disconnect / UNSUPPORTED / catch leftover（D232）/ listed-gate。未占 Tools / Plugins / Skills / Agents / ProviderModel / Composer / Hub / Sources / navigator / proto / 引擎仓 / F3 / A2 / D26 / mic·Route / `.sessions` | 工位 A `mcp-unknown-leftover` 已收 | 成功→UNKNOWN leftover 行仍在 + capability loading；不得装空成功；首拉 UNKNOWN 仍 empty+capability loading；不发明 list RPC；补 `engineCatalogSections.test.ts`（行数/可见性 + loadingKind capability） | conversation / mcp | closed |
| D255 | P3 | **closed** Tools `refresh()` 在 capability `UNKNOWN` 时不再无条件 `clearCatalogPresentation()`（对齐 D253 Agents / D250 Plugins；Tools catch leftover 仍是 D226）。成功画过 tool 行后再 probing：保 leftover 行并画 capability loading；`updateSaveChrome()` 仍不可写。首拉 UNKNOWN 仍 empty + capability loading。不得画「No engine tools yet.」空成功。**不是发明 list RPC**（UNKNOWN 仍不打 `listTools` / `listAgentProfiles`）。只改 UNKNOWN 支路；未改 disconnect / UNSUPPORTED / catch leftover / listed-gate / Agents tools tab（D247） | 工位 B `tools-unknown-leftover` 已收 | 成功→UNKNOWN leftover 行仍在 + capability loading；不得装空成功；首拉 UNKNOWN 仍 empty+capability loading；0 次额外 list RPC；补 `engineCatalogSections.test.ts`（行数/可见性 + capability loading） | conversation / tools | closed |
| D256 | P3 | **closed** MCP Runtime `refresh()` 在 capability `UNKNOWN` 时不再无条件 `clearRuntimePresentation()`（D204 Composer UNKNOWN keep-last / D227 list-throw leftover 同胞）。成功画过 runtime 行后再 probing：保 leftover 行并画 capability loading；首拉 UNKNOWN 仍 empty + capability loading。不得画「No MCP servers in runtime.」空成功。**不是发明 list RPC**（UNKNOWN 仍不打 `getMcpServerStatuses`）。只改 UNKNOWN 支路；未改 disconnect / UNSUPPORTED / list throw leftover（D227）/ tools throw leftover（D238） | 工位 C `mcp-runtime-unknown-leftover` 已收 | 成功→UNKNOWN leftover 行仍在 + capability loading；不得装空成功；首拉 UNKNOWN 仍 empty+capability loading；0 次 status RPC；补 `engineMcpRuntimePanel.test.ts`（行数/可见性 + status widget） | conversation / mcp-runtime | closed |
| D257 | P3 | **closed** Clipboard `refresh()` 在已接通但 hook 缺失（`!canSend || !hook`）时不再无条件 `clearListPresentation()`（D250–D253 catalog UNKNOWN leftover 同胞；无 capability 矩阵的钩子型节）。活画后再变 unsupported 保 leftover 行 + unsupported；首拉无行仍 clear + unsupported。不得画「No clipboard entries.」空成功。**禁止**改 disconnect 支路（History 断连清行合同不反转）。未改 throw leftover（D239）、write listed-gate（D212）、Read/Write/Clear 发送门。未发明 clipboard RPC / proto。未占 Triggers / Context Variables / Plugins / Skills / Agents / MCP / proto / 引擎仓 / F3 / A2 / D26 / mic·Route / `.sessions` | 工位 D `clipboard-unsupported-leftover` 已收 @2026-09-10 | 成功→去掉 listClipboard hook leftover 行仍在 + unsupported；不得装空成功；首拉 hook missing 仍 0 行 + unsupported；断连仍清行；补 `engineClipboardSection.test.ts`（`.engine-clipboard-row` 行数 + status mode） | conversation / clipboard | closed |
| D258 | P3 | **closed** Triggers `refresh()` 在已接通但 hook 缺失（`!canSend || !hook`）时不再无条件 `clearListPresentation()`（对齐 D257 Clipboard；list throw leftover 仍是 D240）。活画后再变 unsupported 保 leftover 行 + unsupported；首拉无行仍 clear + unsupported。不得画「No triggers.」空成功。**禁止**改 disconnect 支路。未改 throw leftover（D240）、listed-gate、Fire·Upsert·Delete 发送门。未发明 proto / RPC。未占 Clipboard / Context Variables / MCP / Tools / Runtime / proto / 引擎仓 / F3 / A2 / D26 / mic·Route / `.sessions` | 工位 A `triggers-unsupported-leftover` 已收 @2026-09-10 | 成功→去掉 listTriggers hook leftover 行仍在 + unsupported；不得装空成功；首拉 hook missing 仍 0 行 + unsupported；断连仍清行；补 `engineTriggersSection.test.ts`（`.engine-triggers-row` 行数 + status mode） | conversation / triggers | closed |
| D259 | P3 | **closed** Context Variables `refresh()` 在已接通但 hook 缺失（`!canSend || !hook`）时不再无条件 `clearListPresentation()`（对齐 D257 Clipboard no-hook leftover；list throw leftover 仍是 D225）。活画后再变 unsupported 保 leftover 行 + unsupported；首拉无行仍 clear + unsupported。不得画「No context variables.」空成功。**禁止**改 disconnect 支路（仍 clear）。未改 throw leftover（D225）、Read 发送门。未发明 proto/RPC。未占 Triggers / Clipboard / MCP/Tools/Runtime / proto / 引擎仓 / F3 / A2 / D26 / mic·Route / `.sessions` | 工位 B `context-variable-unsupported-leftover` 已收 @2026-09-10 | 成功→去掉 listContextVariable hook leftover 行仍在 + unsupported；不得装空成功；首拉 hook missing 仍 0 行 + unsupported；断连仍清行；补 `engineContextVariableSection.test.ts`（`.engine-context-variable-row` 行数 + status mode） | conversation / context-variable | closed |
| D260 | P3 | **closed** Connection Preferences 钩子型 list 在已接通但 hook 缺失时不再无条件卸 leftover（对齐 D257 Clipboard / D258 Triggers / D259 Context Variables）。`refreshEnginePending` / `refreshEngineDevices` 拆开 disconnect vs connected+no-hook：断连仍清 pending/devices；已接通失 hook 有 leftover 则不清，画 leftover 行 + unsupported；首拉无行仍空 + unsupported。`renderPendingPairs` 在 connected+no-hook 仍可见 leftover（不得整表 `display:none` 装 empty-success）。未改 throw leftover（D61）、listed-gate（D221/D224）、Hub directory throw leftover（D248）。未发明 proto / RPC。未占 Triggers / Clipboard / Context Variables / catalog MCP/Tools/Runtime / proto / 引擎仓 / F3 / A2 / D26 / mic·Route / `.sessions` | 工位 A `connection-nohook-leftover` 已收 @2026-09-10 | 成功 list 后去掉 `listPending` / `listDevices`（仍 connected）→ leftover 行仍可见 + unsupported；首拉 hook missing 0 行 + unsupported；断连仍清；补 `connectionPreferencesPane.test.ts`（DOM 行数 + status，不靠虚拟列表行名） | conversation / connection | closed |
| D261 | P3 | **closed** 活画过 engine roster 后再 `refreshEngineCatalog()`：`doRefreshEngineCatalog` 开头 `listCompleted = false`，`getSessions()` 在 connected + `!listCompleted` 时不再无条件 `return []`。有 leftover（`engineSessions.length > 0`）返回 last-good；首拉空仍 `[]`。未改 `markEngineSessionBindFailed()` 清 `engineSessions`（[D25](deferred-gaps.md) bind-failed：List throw / ghost bind 仍显示 bind-failed 行，不得回 Untitled / New session）。未发明 GetQueue / proto。未碰 `.sessions` / 引擎仓 / F3 / A2 / D26 store / catalog MCP/Tools/Runtime / Connection Preferences / Sources / navigator / mic·Route。compile 补刀：两测 Promise/resolver 的 `title` 已对齐为 `string \| undefined`（必有 key），消 TS2322 | 工位 A `roster-inflight-leftover` 已收 @2026-09-10 | 成功 list 后再 refresh（`listCompleted` 短暂 false，`engineSessions` 仍有行）→ `getSessions()` 行数不掉到 0；首拉 refresh 未完成仍 0 行；D25 `listSessions throw shows bind-failed` 仍绿；补 `conversationEngineRosterService.test.ts` | conversation / roster | closed |
| D262 | P3 | **closed** `isAgentTreeFetchFailed()` 为真且 lease **仍保留** `liveAgentTree` 时，`getNavigatorAgentTreePendingCopy` 不再因 `liveTree !== undefined` 丢掉 `treeFetchFailed`。Hierarchy / Activity / Team 走既有 leftover + fetch-fail note（`setHierarchyAfterTreeFetchFail` / `setTeamAfterTreeEmpty`），**不得** `collectLiveAgentTreeAgentIds` 当 live。首拉无 tree 仍 empty + fetch-fail。pending / treeEmpty / throw leftover（D229/D237/D241）与断连 stale note 不回退。未发明 proto。未占 roster / catalog / Connection Preferences / Sources / F3 / A2 / D26 / `.sessions` / mic·Route | 工位 B `navigator-fetchfail-retained-tree` 已收 @2026-09-10 | 活画后 `treeFetchFailed` + retained liveTree：节点/行仍在 + fetch-fail note；`getLiveAgentIds()` 不得当 live；首拉 `liveTree=undefined` fetch-fail / pending / treeEmpty 仍绿；补 `navigatorAgentsSubviews.test.ts` / `navigatorTeamSubviews.test.ts`（entry count + note 可见性） | navigator / agents | closed |
| D263 | P3 | **closed** MCP Runtime `onDidChangeSelection` 在 `!canShowCatalogRows(this.mode)` 时不再无条件 `clearToolsPresentation()`（对齐 D238 `loadTools` catch leftover；不是重做 D227/D238/D256 runtime list leftover）。活画过 tools 后，runtime list throw / UNKNOWN 把 mode 打成 failed/loading，再选中 leftover 服务器行：failed/loading 且 `tools.length > 0` 不清 tools，保 leftover 工具行 + 子 status failed/capability-loading；无 leftover tools / disconnect / UNSUPPORTED 仍可 clear。首拉无 tools 仍空。**不是发明 list RPC**。未改 `refresh()` UNKNOWN / list throw / `loadTools` catch。未占 Plugins hooks / Skills / Agents / roster / navigator / proto / 引擎仓 / F3 / A2 / D26 / mic·Route / `.sessions` | 工位 C `mcp-runtime-tools-selection-leftover` 已收 @2026-09-10 | 成功 load tools 后 UNKNOWN 或 list throw → mode failed/loading → 再选 leftover 行：工具行仍在（行数+可见性）；首拉无 tools 仍空；补 `engineMcpRuntimePanel.test.ts`（不靠虚拟列表行名） | conversation / mcp-runtime | closed |
| D264 | P3 | **closed** Plugins `onDidChangeSelection` 在 `!canShowCatalogRows(this.mode)` 时不再无条件 `clearInfoPresentation()`（D243 `loadInfo` catch leftover 同胞；本刀只改 **selection 清 hooks** 支路）。活画 hook 行后 catalog list throw / UNKNOWN 把 mode 打成 failed/loading，再选 leftover 插件行：failed/loading 且已有 leftover hook 行则保行 + failed/capability-loading；无 leftover / disconnect / UNSUPPORTED 仍可 clear。未重做 D230 list leftover / D250 UNKNOWN leftover / D243 throw leftover。未发明 proto。未占 MCP Runtime / Skills / Agents / navigator / proto / 引擎仓 / F3 / A2 / D26 / mic·Route / `.sessions` | 工位 D `plugins-hooks-selection-leftover` 已收 @2026-09-10 | 成功 load hooks 后再 list throw 或 UNKNOWN，`selectPluginForTest` leftover 插件：hook 行仍在（`getHookRowCount()` / 可见性）+ failed/capability-loading；现有 plugin list leftover 测仍绿；不靠虚拟列表 textContent 行名；补 `enginePluginsSection.test.ts` | conversation / plugins | closed |
| D265 | P3 | **closed** Skills `loadSkillBody` 在 `!canShowCatalogRows(this.mode)` 时不再无条件 `clearBodyEditor()`（D263 Runtime tools / D264 Plugins hooks 重选 leftover 同胞；由 selection 触发）。活画过 skill body 后，list throw / UNKNOWN 把 mode 打成 failed/loading，再选 leftover 技能：failed/loading 且已有 leftover body（`loadedBodyText` 或非空已画正文）则不清，保正文 + failed/capability-loading；`bodyDirty` 仍不清。无 leftover / disconnect / UNSUPPORTED 仍可 clear。首拉无 body 仍空。未重做 list leftover（D231）/ UNKNOWN leftover（D251）。未发明 proto。未占 Plugins hooks / MCP Runtime / Agents / roster / navigator / proto / 引擎仓 / F3 / A2 / D26 / mic·Route / `.sessions` | 工位 C `skills-body-selection-leftover` 已收 @2026-09-10 | 成功 load body 后再 list throw 或 UNKNOWN，`selectSkillForTest` leftover 技能：body 文本仍在 + failed/capability-loading；首拉无 body 仍空；不靠虚拟列表行名；补 `engineSkillsSection.test.ts` | conversation / skills | closed |
| D266 | P3 | **closed** Agents `loadAgentsEditorForSelection` 在 `!canShowCatalogRows` / 未连 / 无选中时，若 `!agentsMarkdownDirty` 不再无条件清空 `agentsEditorInput.value` / 藏编辑器（D233 list leftover / D253 UNKNOWN leftover 同胞；本刀只改 **selection 卸 markdown** 支路）。活画 AGENTS.md 后 profile list throw / UNKNOWN 把 mode 打成 failed/loading，再选 leftover profile：failed/loading 且已有 leftover markdown（`loadedAgentsMarkdown` 或编辑器已有活画正文）则保正文 + load-failed/capability-loading；`agentsMarkdownDirty` 仍不清。无 leftover / disconnect / UNSUPPORTED 仍可 clear。未改 Agents tools tab / `ensureAgentToolsLoaded` / UNKNOWN `listTools` 闸。未重做 profile list leftover（D233/D253）。未发明 proto。未占 Skills body / roster / navigator / Plugins / Runtime tools / proto / 引擎仓 / F3 / A2 / D26 / mic·Route / `.sessions` | 工位 D `agents-markdown-selection-leftover` 已收 @2026-09-10 | 成功 load markdown 后再 list throw 或 UNKNOWN，`selectProfileByIdForTest` leftover profile：editor value 仍在 + failed/capability-loading；现有 profile list leftover 测仍绿；不靠虚拟列表 textContent 行名；补 `engineCatalogSections.test.ts` | conversation / agents | closed |
| D267 | P3 | **closed** Agents tools tab `ensureAgentToolsLoaded()` 接通后无条件 `listTools()`，没有 `capabilities.tools.support === 'UNKNOWN'` 闸（Tools 节 `engineToolsSection.refresh()` D255 已有）。tools UNKNOWN 时会发明 list RPC，并可能冲掉 leftover `agentTools`。UNKNOWN 不打 `listTools`；有 leftover 则保行 + tools 子 status capability-loading；首拉无 leftover 仍空 + capability-loading。disconnect / UNSUPPORTED 仍可 clear。不是改 D266 markdown selection。不是改 profile list leftover。未发明 proto / 新 RPC。未占 Skills body / Plugins / Runtime tools / roster / navigator / proto / 引擎仓 / F3 / A2 / D26 / mic·Route / `.sessions` | 工位 A `agents-tools-unknown-leftover` 已收 @2026-09-10 | 成功 load agent tools 后再 UNKNOWN：工具行仍在 + capability-loading；0 次额外 `listTools`；首拉 UNKNOWN 仍空 + capability-loading；既有 markdown leftover / profile list leftover 仍绿；补 `engineCatalogSections.test.ts`（行数/可见性/loadingKind，不靠虚拟列表行名） | conversation / agents | closed |
| D268 | P3 | **closed** Snapshots `refresh()` 在已接通但缺 `listSnapshots` 时不再无条件 `paintStatus()`（会 `reset(body)` 卸行；对齐 D257 Clipboard / D258–D260 no-hook leftover）。活画后再失 hook 保 leftover 行 + UnavailableNoHook；首拉无 leftover 仍 0 行 + unavailable。不得画 SnapshotsEmpty。**禁止**改 disconnect 支路（仍清行；History 断连清行合同不反转）。未改 throw leftover（D242）、Restore/Delete listed-gate（D54/D154）。未发明 proto / RPC。未占 Agents tools / lens timeline / History / F3 / A2 / D26 / mic·Route / `.sessions` | 工位 D `snapshots-nohook-leftover` 已收 @2026-09-10 | 成功 paint 后去掉 listSnapshots（仍 connected）→ 行数不变 + UnavailableNoHook；不得装空成功；首拉 no-hook 仍 0 行 + unavailable；断连仍清行；throw leftover 仍绿；补 `conversationEngineSnapshotsList.test.ts` | conversation / snapshots | closed |
| D269 | P3 | **closed** `bindSessionView` 在 `isEngineConnected() && !isEngineSessionReady()` 时不再无条件刷空时间线（[D261](deferred-gaps.md) roster in-flight leftover 同胞）。已接通、仅 roster in-flight 且已有 leftover timeline（`lastAttachedEntries.length > 0`）则保留已画条目、不 `applyEntries([])`、不卸 lease。空 `sessionId` 仍清。首拉无 leftover 仍可清空。未改 D261 `getSessions()`。未碰 Snapshots。未发明 proto。未占 Agents tools / catalog / navigator / F3 / A2 / D26 / `.sessions` / `dev/loop` | 工位 B `lens-timeline-inflight-leftover` 已收 @2026-09-10 | 接通 + `!isEngineSessionReady` + leftover + 非空 sessionId → entry 数不掉；空 sessionId 仍清；首拉无 leftover 仍空；补 `conversationLensDisposeGate.test.ts` | conversation / lens | closed |
| D270 | P3 | **closed** Tools `onDidChangeSelection` 在 `!canShowCatalogRows(this.mode)` 时不再无条件 `loadToolInfo()` → `clearToolInfo()`（D264 Plugins hooks / D265 Skills body 重选 leftover 同胞；由 selection 触发）。活画过 tool info 后，list throw / UNKNOWN 把 mode 打成 failed/loading，再选 leftover 工具：failed/loading 且已有 leftover 详情则不清、不重拉，保详情 + failed/capability-loading；无 leftover / disconnect / UNSUPPORTED 仍可 clear。未重做 list leftover（D226）/ UNKNOWN leftover（D255）/ reconnect drop-stale（D69）。未发明 proto。未改 Agents tools tab（D271）。未占 Snapshots / lens / proto / 引擎仓 / F3 / A2 / D26 / mic·Route / `.sessions` | 工位 A `tools-info-selection-leftover` 已收 @2026-09-10 | 成功 load info 后再 list throw 或 UNKNOWN，`selectTool` leftover 工具：详情仍在 + failed/capability-loading；既有 list leftover / reconnect drop-stale 仍绿；不靠虚拟列表行名；补 `engineCatalogSections.test.ts` | conversation / tools | closed |
| D271 | P3 | **closed** Agents `syncDetailHost()` 在 `!canShowCatalogRows(this.mode)` 时不再无条件 `detailHost.style.display = 'none'`（D266 markdown leftover / D270 Tools info leftover 同胞；本刀只改 **profile catalog mode failed/loading 卸详情宿主**）。活画过 tools 面板后，profile list throw / UNKNOWN 把 mode 打成 failed/loading，再选 leftover profile：failed/loading 且已接通、已有 leftover 详情（selectedProfile + leftover tools 行，或 leftover AGENTS.md / 已活画 tools panel）则不把 `detailHost` 设为 `none`；可继续 `setActiveDetailTab` 但不得清 leftover 行。无 leftover / disconnect / UNSUPPORTED / 首拉无详情仍可 `display='none'`。不是 D267（tools capability UNKNOWN 不打 `listTools`）。未改 Tools 节（D270 已合）。未反转 D266 markdown keep。未发明 proto。未占 Snapshots / lens / proto / 引擎仓 / F3 / A2 / D26 / mic·Route / `.sessions` | 工位 B `agents-tools-detailhost-leftover` 已收 @2026-09-10 | 成功 load tools 面板后再 list throw 或 agentProfiles UNKNOWN，`selectProfileByIdForTest` leftover：`.engine-agents-tools-panel` 仍 `display !== 'none'`，leftover 工具行仍在（`getAgentToolRowCount()` / 可见性），不得装空成功；既有 D266 markdown leftover / D267 tools UNKNOWN leftover / D270 Tools info leftover / reconnect drop-stale 仍绿；不靠虚拟列表行名；补 `engineCatalogSections.test.ts` | conversation / agents | closed |
| D272 | P3 | **closed** Overview 网格 Model 行没有 leftover 缓存。活画 `listModels` 得到 `"N models"` 后：`onDidChangeConnection` 把 `modelDataLoaded = false` 再 `render()`；`resolveModelRow` 在 models `UNKNOWN` 时直接 `formatOverviewModelUnknownCopy()`，`paintSummary` → `DOM.clearNode(summaryGrid)` 全量重绘卸掉末次计数；`listModels` catch 写 `formatOverviewModelFailedCopy` 同样卸掉末次计数。对齐 D252 ProviderModel UNKNOWN keep-last / D204 Composer keep-last：**不是发明 RPC**（UNKNOWN 仍不打 `listModels`）。成功拿到 model count 后记住 last-good 摘要；之后 UNKNOWN / listModels throw：Model 行仍显示末次 `formatOverviewModelSummary(N)`，可另附 capability-loading / failed 提示（title）；首拉无 leftover 仍 UNKNOWN confirming / throw failed。disconnect / UNSUPPORTED 仍清 last-good，不得装 leftover 成功。未改 ProviderModel 节、Composer、Agents、Tools、Snapshots、lens。未发明 proto。 | 工位 C `overview-model-keep-last` 已收 @2026-09-10 | 成功 listModels 后再 UNKNOWN：仍见 `formatOverviewModelSummary(N)`，不得只剩 unknown copy；0 次额外 `listModels`。成功后再 listModels throw：仍见末次计数 + failed 诚实（不得只剩 failed copy、不得装空成功）。既有首拉 UNKNOWN / 首拉 throw / UNSUPPORTED 仍绿。补 `engineOverviewSection.test.ts` | conversation / overview | closed |
| D273 | P3 | **closed** Changes / Review `refresh()` 在 `tryLoadGitEntries()` 得到 `undefined`（connected 但无 `readGitChanges` hook）时不再无条件 `usingGitRead=false` 并 `collectSources*Entries(scm)`（对齐 D260 Connection / D257 Clipboard no-hook leftover）。已接通、曾 `usingGitRead` leftover 后再失 hook：保 leftover 行，不切 SCM、不画 `sourcesGitLocalOnlyMessage()`；可画 unsupported / no-hook 诚实 status。首拉无 leftover 仍 empty + localOnly/SCM。**禁止**改 disconnect 支路（仍走 SCM；History/Snapshots 断连清行合同不反转）。`supported:false` 仍走 SCM。未改 throw leftover（D244/D245）。helper `tryReadSourcesGitChanges` 仍 no-hook 回 `undefined`。未发明 WriteGitUnstage / proto。未开 A2 / F4。未占 catalog / Connection / Snapshots / lens / proto / 引擎仓 / F3 / A2 / D26 / mic·Route / `.sessions` | 工位 B `sources-git-read-nohook-leftover` 已收 @2026-09-10 | 成功 git-read 后去掉 `readGitChanges`（仍 connected）→ 行数/路径仍在，不得 localOnly、不得被 SCM stub 换掉；首拉 no-hook / disconnected / unsupported / throw leftover 仍绿；补 `sourcesChangesList.test.ts` / `sourcesReviewList.test.ts`（entry 数 / path / status DOM，不靠虚拟列表行名） | sources-git | closed |
| D274 | P3 | **closed** Agents Activity `setActivityFromSnapshot` 在 `fetchFailed && collectNavigatorActivityItems` 得 0 行时仍 `setActivityState([], NAVIGATOR_ACTIVITY_FETCH_FAILED_COPY)`，把已活画工具行卸掉（D220 夹具 timeline 始终含 grep，未覆盖 empty collect；Hierarchy `setHierarchyAfterTreeFetchFail` 已有 `hadHierarchySnapshot` 守卫）。已有 leftover 行（`hadActivitySnapshot` / 非空 `activityEntries`）且 fetch-fail、本次 collect 为 0：只画 fetch-failed note，**保留行**。首拉无 leftover 仍 empty+failed。断连 stale-note / UNSUPPORTED 清行不回退。未改 Hierarchy/Team 已闭合同。未改 Sources（D273 在 B）。未发明 proto。未占 F3 / A2 / D26 / D8 / D147 / 引擎仓 / `navigatorTeamList.ts`。 | 工位 D `navigator-activity-fetchfail-leftover` 已收 @2026-09-10 | 活画 Activity 工具行后 `treeFetchFailed` 且 collect 0 行：行仍在 + fetch-failed note；既有 D220 snapshot-with-tools / D262 Hierarchy retained-tree 仍绿；首拉 fail 仍 empty+failed；补 `navigatorAgentsSubviews.test.ts` | navigator / agents | closed |
| D275 | P3 | **closed** catalog 仍 `ready`/SUPPORTED 时，详情 RPC 第二次 throw 仍卸 leftover（Plugins/Runtime 已反转；Skills/Tools/Agents 原锁「drops stale」）。Skills `loadSkillBody` 一进就清 textarea、catch 再清 `loadedBodySource`；Tools 成功 list 后 `clearToolInfo()` 再 load，catch 把 `infoHost` 整段换成失败文案，活画后再缺 `getToolInfo` 也 unavailable 卸描述；Agents `loadAgentsEditorForSelection` 在 await 前用 `formatAgentsMarkdown(summaryToProfileDetail)` 盖掉 leftover，catch 只挂 load-failed。有 leftover 则 in-flight 不清正文；throw 保 leftover + failed/unavailable。不得只剩失败文案、不得装空成功。首拉无 leftover 仍空 + failed。断连 / UNSUPPORTED 仍清。未改 D265/D270/D266（mode=failed/loading 再选 leftover）。未改 list leftover、listed-gate、dirty 正文、Plugins/Runtime。不发明 proto；不改 Agents 用 `saveAgentProfile` 当读（只改 leftover 预清空）。 | 工位 C `catalog-detail-rpc-leftover` 已收 @2026-09-10 | 接通→list 成功→选中→详情活画→仍 SUPPORTED/ready→`setConnected(true)` 再 refresh：list 再成功，详情 RPC 第二次 throw（`getSkillInfo` / `getToolInfo` / `saveAgentProfile` 当 load）后 leftover 仍在 + failed；Tools 活画后缺 `getToolInfo` leftover + unavailable；首拉无 leftover 仍空 + failed。三份「drops stale / without leftover markdown」测已改。`engineSkillsSection.test.ts` / `engineCatalogSections.test.ts` 绿 | conversation / catalog | closed |
| D276 | P3 | **closed** Agents `getNavigatorAgentTreePendingCopy` 在 `agentTree` `UNKNOWN` 且 lease **仍保留** `liveTree` 时不再返回 undefined 把 leftover 当 live 画（D262 只收了 treeFetchFailed+retained）。`refreshFromLease` 走 pending loading（`setHierarchyAfterPending` + `setLiveAgentIds(undefined)`，Activity 不装 live）。Team `refreshTeamData` 在 `team` `UNKNOWN` 时不再只闸 UNSUPPORTED：有 leftover 则 0 次 `memberStatus`/`taskList`，`setTeamSnapshotNote` loading 保行；首拉无 leftover 仍 empty+loading。Team UNKNOWN 新文案 `navigatorTeam.loading`「正在读取…」，不复用 Reading agent tree…。不得 `collectLiveAgentTreeAgentIds` 非空 live ids；不得 `TEAM_MEMBERS_EMPTY_COPY`。未重做 fetch-fail/throw/pending/断连 stale（D262/D274/D236/D241）。未发明 proto。未占 catalog / Sources / Overview / 引擎仓 / `dev/loop`。 | 工位 D `navigator-read-unknown-leftover` 已收 @2026-09-11 | 活画后 agentTree/team → UNKNOWN：leftover 仍在 + loading note；`getLiveAgentIds()===undefined`；首拉无 leftover 仍 empty+loading；既有 D262/D274/D236/D241 仍绿；补 `navigatorAgentsSubviews.test.ts` / `navigatorTeamSubviews.test.ts` | navigator / agents | closed |
| D277 | P3 | **closed** H4b pairing 合成闸漏两处：Overview `paintSummary` Connection 行 `getConnectionPhaseStatusBarText(phase)` 未传 `snapshot.pairingPending`；renderer `UniverseAgentConnectionChannelClient.isEngineConnected()` 只看 `phase.kind === 'connected'`。合成态 `connected` + `pairingPending` 时 Overview 画「Engine · Direct」、chip 画「Engine not connected」，catalog/Overview 当接通。Roster 已用 `isConversationEngineLive(phase, pairingPending)`。本刀：`paintSummary` 传第二参；`cache.connected` / `isEngineConnected()` 对齐同一公式（`isUniverseAgentPhaseConnected(phase) && !pairingPending`），不复制新语义。未对齐 node `channelAlive`/`sessionToken`。未改 Connection pane / catalog 测 / Skills·Tools·Agents 源 / D272 Model keep-last。未发明 proto。 | 工位 A `overview-h4b-pairing-gate` 已收 @2026-09-10 | Overview：`isEngineConnected()===true` 桩 + `pairingPending:true` + phase connected → Connection 行「Engine not connected」；D272 keep-last 仍绿。Renderer：phase connected + `pairingPending` → `isEngineConnected()===false`；hydrate last-good 仍绿。`engineOverviewSection.test.ts` / `universeAgentRendererSync.test.ts` | conversation / overview | closed |
| D278 | P3 | **closed** D277 合入后 Skills/Tools/Agents `refresh()` 仍把 `!isEngineConnected()` 当真断连 `clearCatalogPresentation()`。合成态 phase `connected` + `pairingPending`（`isEngineConnected()===false` 仅因 pairing）会卸已活画 leftover。本刀：`snapshot.pairingPending` 且 `isConversationEngineLive(phase, false)` 且已有 leftover → 不清行，mode=`disconnected` + 未接通 note（keep-last，不装 `ready`）。真断连（phase 非 connected）仍清。未改 Overview/renderer H4b；未重做 D275。未改 MCP/Plugins/Runtime。不发明 proto。 | 工位 B `engine-catalog-refresh-disconnect` 已收 @2026-09-11 | Skills/Tools/Agents：leftover 后 pairingPending refresh 行仍在 + disconnected note，0 次额外 list；随后真断连清行。既有 disconnect / leftover-failed / UNKNOWN 仍绿。`engineSkillsSection.test.ts` 28 passing；`engineCatalogSections.test.ts` 70 passing。compile-client / commit 待父 | conversation / catalog | closed |
| D279 | P3 | **closed** D275 未覆盖 Plugins hooks / MCP Runtime tools：`loadInfo` / `loadTools` 一进就 `display='none'` 卸 leftover；refresh 后再拉详情 in-flight 空白；缺 `getPluginInfo` / `getMcpServerTools` 也无 leftover+unavailable。照抄 D275 Tools：`hasLeftover*`，in-flight 不 hide，throw 用 sibling status overlay 不整段清空。有 leftover 则 in-flight 不卸 DOM；二次 throw leftover + failed；活画后再缺 hook leftover + unsupported；首拉无 leftover 仍 hide/empty。未改 Skills/Tools/Agents（D275）；未改 list leftover、selection leftover（D263/D264）、Overview H4b、composer、connection pane、navigator、proto。不发明 proto。**D278 留给 B pairing×catalog，未占用。** | 工位 A `catalog-plugins-mcp-runtime-detail-leftover` 已收 @2026-09-11 | 接通→list 成功→选中→详情活画→仍 SUPPORTED/ready→`setConnected(true)` 再 refresh：详情 reload in-flight leftover DOM 仍在；二次 throw leftover + failed；活画后缺 hook leftover + unsupported；首拉无 leftover 仍 hide/empty。`enginePluginsSection.test.ts` 19 / `engineMcpRuntimePanel.test.ts` 12 绿 | conversation / plugins-mcp-runtime | closed |
| D280 | P3 | **closed** D277 合入后其余 `!isEngineConnected()` 清行面仍把 pairing 合成态当真断连：MCP `engineMcpSection.refresh`、Plugins `enginePluginsSection.refresh`、MCP Runtime `engineMcpRuntimePanel.refresh`、ProviderModel `engineProviderModelSection.refreshModels`、Snapshots `conversationEngineSnapshotsList`（`paintStatus` 卸 leftover）、Connection pane `refreshEngineDevices`/`refreshEnginePending`。本刀照抄 D278：`snapshot.pairingPending` 且 `isConversationEngineLive(phase, false)` 且已有 leftover → 不清行，mode=`disconnected` / overlay 未接通 note（Snapshots 走 `paintListFailed`），0 次额外 list。真断连（phase 非 connected）仍清。未重做 D278 Skills/Tools/Agents；未重做 D279 详情 leftover。不发明 proto。 | 工位 B `pairing-remaining-keep-last` 已收 @2026-09-11 | 各面 leftover 后 pairingPending keep-last + disconnected note，0 次额外 list；随后真断连清行。既有 disconnect / leftover-failed / UNKNOWN / D279 详情仍绿。`engineCatalogSections.test.ts` 71 passing；`enginePluginsSection.test.ts` 20 passing；`engineMcpRuntimePanel.test.ts` 13 passing；`engineProviderModelSection.test.ts` 6 passing；`conversationEngineSnapshotsList.test.ts` 35 passing；`connectionPreferencesPane.test.ts` 125 passing。compile-client / commit 待父 | conversation / catalog | closed |
| D281 | P3 | **closed** D277 后 Overview / Context Variables / Triggers / Clipboard / History 仍把 `!isEngineConnected()` 当真断连清 leftover。Overview `renderAsync` 开场藏 summary 并在 `!connected` 清 `lastGoodModelSummary`（D277 测用 `isEngineConnected()===true` 桩，生产 pairing 走 disconnected 早退）。本刀照抄 D278/D280：`keepLeftoverCatalogForPairingHold` — pairing-hold + leftover → keep-last + disconnected note / History `paintListFailed`，0 次额外 list；真断连仍清。未改 Rules/Hooks。未重做 D278–D280。不发明 proto。 | 工位 A `pairing-overview-lists-keep-last` 已收 @2026-09-11 | leftover 后 pairingPending keep-last + disconnected note，0 次额外 list；真断连仍清；D272/D277 测仍绿。`engineOverviewSection.test.ts` 14；`engineContextVariableSection.test.ts` 11；`engineTriggersSection.test.ts` 27；`engineClipboardSection.test.ts` 22；`conversationEngineHistoryList.test.ts` 11。compile-client / 新测执行待父 | conversation / overview-lists | closed |
| D282 | P3 | **closed** D277 后 composer `refreshComposerCatalogs` 在 `!isEngineConnected()` 把 agent/model/tools 重置为 No model / 空 tools（`catalogToolNames=[]`），丢掉已有 `lastGoodComposerCatalogs`。合成态 pairing 会卸 leftover 下拉。本刀：pairing-hold + last-good / 已画 leftover → `keepLastGoodComposerCatalogOrEmpty`（或保已画），不 list RPC；send 仍按 `!isEngineConnected()` 关门。真断连仍清默认空。不重做 D278–D281。不发明 proto。 | 工位 B `pairing-composer-catalog-keep-last` 已收 @2026-09-11 | leftover 后 pairingPending keep-last select/tools，0 次额外 list；真断连仍清；既有 UNKNOWN/throw keep-last 仍绿。`conversationComposerCatalog.test.ts` 15 passing @ merge compile | conversation / composer | closed |
| D283 | P3 | **closed** D277 后 Changes / Review `refresh` 把 `isEngineConnected()` 传给 `tryLoadGitEntries` / `canSendSourcesGitChanges`，pairing 合成态（phase `connected` + `pairingPending`）跳过 git-read；`shouldKeepSourcesGitReadNoHookLeftover` 也要 `connected===true`，pairing leftover 被 SCM / `localOnly` 换掉。本刀：`shouldKeepSourcesGitReadPairingHoldLeftover(phaseConnected, pairingPending, leftoverCount)`（common 只收 boolean，不 import conversationSessionStatus）。else-branch pairing-hold leftover → 保 `lastGoodEntries` / `allEntries`、`usingGitRead=true`、pairing 未接通 note，**不** `localOnly` SCM，0 次额外 `readGitChanges`。真断连（phase 非 connected）仍 SCM。首拉 leftoverCount 0 仍 SCM / empty。不重做 D273 no-hook；pairing 先于 no-hook。写门（stage/commit）仍 `!isEngineConnected()` 关门。未改 catalog/composer（D278–D282）。不发明 proto。 | 工位 D `sources-git-read-pairing-hold` 已收 @2026-09-11 | leftover 后 pairingPending 行仍在 + pairing note，不得 localOnly / SCM stub，0 次额外 read；真断连仍 SCM；helper 纯布尔。`sourcesChangesGitRead.test.ts` 17；`sourcesChangesList.test.ts` 12；`sourcesReviewList.test.ts` 26。compile-client 待父 | sources-git | closed |
| D284 | P3 | **closed** D278–D280 已 keep-last **列表**，但详情子面（Tools `loadToolInfo` / Skills `loadSkillBody` / Agents `syncDetailHost`·markdown·tools / Plugins `clearInfoPresentation` / MCP Runtime `clearToolsPresentation`）在 pairing 后 `mode=disconnected` 再选行或切 tab 仍当断连清 leftover。本刀复用 `keepLeftoverCatalogForPairingHold`：pairing-hold + leftover 详情不卸 DOM，overlay 未接通，0 次额外详情 RPC；真断连仍清。首拉无 leftover 仍 hide。不重做 list keep-last（D278–D280）与 D279 in-flight/throw leftover。不发明 proto。 | 工位 A `catalog-detail-pairing-hold` 已收 @2026-09-11 | leftover 详情后 pairing 再选/切 tab 仍在 + disconnected note；真断连仍清；D275/D279 仍绿 | conversation / catalog-detail | closed |
| D285 | P3 | **closed** D277 后 `isEngineConnected()` 在 `pairingPending` 时为 false（phase 仍 `connected`）。`bindSessionView` 的 [D269](deferred-gaps.md) 只在 `isEngineConnected() && !isEngineSessionReady()` 保 leftover；pairing 跳过该闸后 `sessionViewLifetime.clear()` + `acquireSessionView`（roster 回 stub 空帧）+ empty baseline，卸已画时间线。本刀：`snapshot.pairingPending && isConversationEngineLive(phase, false)`（`host.uaConnection`）且 leftover 且非空 sessionId → 不卸 lease、不 apply 空 baseline、不换 stub 空帧（同 D269 keep）。真断连（phase 非 connected）仍现行 rebind/clear。空 sessionId 仍清。首拉无 leftover 仍可清/rebind。未重做 D269 in-flight。未改 roster/proto。未占 catalog/composer/sources（D278–D284）。 | 工位 C `lens-timeline-pairing-leftover` 已收 @2026-09-11 | leftover 后 pairingPending（生产 stub `isEngineConnected: () => connected && !pairingPending`，phase 仍 connected）entry 数不掉、无空 baseline；随后真断连仍清；D269 仍绿；补 `conversationLensDisposeGate.test.ts` | conversation / lens | closed |
| D286 | P3 | **closed** D277 后 pairing 时 `isEngineConnected()` 为 false。`refreshTrajectoryRecords` 因此走 stub `getTurns`（engine-only 常空）`setRecords` 可卸 leftover 轨迹 turn id；`bindLiveTreeLease` 先 `clear()` 再 early-return，卸已挂 live-tree lease（overlay 冻结，再 bind 可 miss）。本刀：pairing-hold（`isConversationPairingHold` = `snapshot.pairingPending && isConversationEngineLive(phase, false)` via `uaConnection`）+ 本 sessionId 已有 lease → 轨迹仍用 lease snapshot turn ids；无 lease 但轨迹页已有 leftover records 且 stub ids 空 → 不 `setRecords`。live-tree：pairing-hold + 已有 `liveTreeLease` → 不清、继续 apply frames。真断连仍 stub/history 与 clear。首拉无 leftover 仍现行。未改 roster `bindLiveTreeObservationLease`（`!isEngineConnected` early-return 本就不清）。未重做 D285 bindSessionView / catalog / composer / sources。未发明 proto。 | 工位 A `trajectory-livetree-pairing-leftover` 已收 @2026-09-11 | leftover 后 pairingPending（生产 stub `isEngineConnected: () => connected && !pairingPending`，phase 仍 connected）轨迹 turn ids / live-tree catalog 不掉；随后真断连仍清；D285/D269 仍绿；补 `conversationLensDisposeGate.test.ts` / `conversationSessionChat.test.ts` | conversation / lens+chat | closed |
| D287 | P3 | **closed** D277 后 roster `isEngineConnected()` 在 pairing 为 false。`getTurns` / `getTrajectoryRecords` 只在 `isEngineConnected()` 读 `getCachedProjection`，pairing 掉 leftover 缓存走空 `session.turns`。本刀：pairing-hold（`isConversationPairingHold(this.uaConnection)`）且 cache 在 → 两 getter 走同一 cache 路径。真断连（phase 非 connected）仍 `session.turns` / wasEverConnected。首拉无 cache 仍空/fallback。未改 write RPC / create/delete / acquireSessionView。未重做 D285 bindSessionView / D286 projection+chat / catalog / composer / sources。未发明 proto。 | 工位 B `roster-pairing-cached-turns` 已收 @2026-09-11 | leftover cache → pairingPending（生产 stub `isEngineConnected: () => connected && !pairingPending`，phase 仍 connected）两 getter 仍从 cache；真断连仍 fallback；首拉无 cache 仍空。`conversationEngineRosterService.test.ts` 绿 | conversation / roster | closed |
| D288 | P3 | **closed** D277 后 roster `getSessionSync` 只在 `isEngineConnected()` 读 `getCachedProjection`。pairing 时 `isEngineConnected()` 为 false → `super.getSessionSync` stub chrome，Inbox / reading-column leftover engine sync（stale banner）消失。本刀：pairing-hold（`isConversationPairingHold(this.uaConnection)`）且 cache 在 → 返回 `projection.snapshot.sync`（同接通）。真断连仍 `super.getSessionSync`。首拉无 cache 仍 idle/super。未改 `showLiveChrome` / getTurns / getTrajectoryRecords（D287）。未发明 proto。 | 工位 A `roster-pairing-session-sync` 已收 @2026-09-11 | leftover cache → pairingPending 仍 engine sync；真断连仍 stub/super；首拉无 cache 仍 idle。`conversationEngineRosterService.test.ts` 绿 | conversation / roster | closed |
| D289 | P3 | **closed** D277 后 roster `acquireSessionView` 只在 `isEngineConnected()` 走 `engineFrameSource.acquire`，否则 stub 空帧。pairing 时 `isEngineConnected()` 为 false → 新 acquire（`readPendingAttentionIds` / `conversation.revealItem` / 连接抖动）丢掉 leftover engine snapshot：pending 空、「step not found」。本刀：pairing-hold（`isConversationPairingHold(this.uaConnection)`）且 leftover engine session / cached projection → 同接通 `engineFrameSource.acquire`。真断连（phase 非 connected）仍 stub。占位 / unbound 仍 throw 或 stub。未改 write RPC。未重做 D285 bind keep / D286 lease keep / D287–D288 getters。未发明 proto。 | 工位 B `roster-pairing-acquire-lease` 已收 @2026-09-11 | leftover cache → pairingPending 新 acquire 仍 leftover entries/pending；真断连仍 stub；首拉无 leftover 仍 stub。`conversationEngineRosterService.test.ts` 绿 | conversation / roster | closed |
| D290 | P3 | **closed** D277 后 Navigator `engineReady = isEngineConnected() && phase === 'connected'` 把 pairing 合成态当真断连。活画后 `had*` 通常保行，但 Agents `showDisconnectedSnapshot` Activity 在 `!hadActivitySnapshot` 无 D274 `activityEntries.length` 守卫可 `setActivityState([])`；Team `!engineReady && !hadTeamSnapshot` 清 members/tasks（catch 已保 `memberEntries.length`）。本刀：`isConversationPairingHold` + leftover → 保 in-memory Hierarchy/Activity 与 Team members/tasks，pairing/disconnected note，`setLiveAgentIds(..., undefined)`，0 次额外 `memberStatus`/`taskList` / tree RPC。Team early exit 对齐 catch；Activity 对齐 D274。真断连合同不改。首拉 pairing 仍 empty。未改 write RPC / `showLiveChrome`。Projects 仍按 `!isEngineConnected()` 走 stale/welcome，不扩第二模块。未重做 D287–D289 / D285。未发明 proto。 | 工位 D `navigator-pairing-hold-leftover` 已收 @2026-09-11 | 活画后 pairingPending + phase connected：行数不变 + note + live ids undefined + 0 次额外 tree/unary；随后真断连仍 stale；首拉 pairing 仍 empty；D262/D274/D276/D237/D241 仍绿。`navigatorAgentsSubviews.test.ts` / `navigatorTeamSubviews.test.ts` | navigator / agents+team | closed |
| D291 | P3 | **closed** D277 后 roster `isEngineConnected()` 在 pairing 为 false。Inbox chip / Sessions subtitle / `conversationOpenPendingOnFocus` 仍走 `countPendingConfirmations` → stub `frameSource.project()` → 0，即使 leftover engine `pendingActions` 仍在 `engineFrameSource.getCachedProjection`（D287 `getTurns` 已保 cache）。本刀：`canReadCachedEngineProjection()`（接通或 pairing-hold）且 cache 在 → 返回 `projection.snapshot.pendingActions.length`（与 stub 语义同）。真断连仍 `super`/0。首拉无 cache 仍 0/super。未改 write RPC。未重做 acquireSessionView / getTurns / getSessionSync。未改 Inbox/Sessions UI。未发明 proto。 | 工位 A `roster-pairing-pending-count` 已收 @2026-09-11 | leftover pending → pairingPending 计数不变；真断连仍 0/stub；首拉无 cache 仍 0。`conversationEngineRosterService.test.ts` 绿 | conversation / roster | closed |
| D292 | P3 | **closed** D286 pairing-hold 保 chat `bindLiveTreeLease` 但不核 `lease.sessionId === getActiveSessionId()`；roster `bindLiveTreeObservationLease` 在 `!isEngineConnected()` 直接 return。pairing 中切 session → 旧 session live-tree / 子代理 catalog 仍画，新 session leftover 不 bind。本刀：共享 `shouldKeepLiveTreeLeaseWhilePairing` / `shouldRebindLiveTreeLeaseWhilePairing`。pairing-hold **同 session** 仍保 lease（D286）。pairing-hold **active session 变了** → 向新 session leftover `acquireSessionView`（D289 已保 cache）重绑 observation/chat，**不清 leftover**，**不**新发 tree RPC（接通切 session 本就不在 pairing 发）。真断连 clear/keep 合同不变。写路径仍关。未重做 D285–D291 leftover keep-last。未发明 proto。 | 工位 C `pairing-livetree-session-identity` 已收 @2026-09-11 | session A leftover → pairing → 切 B leftover：overlay/tree 跟 B 不跟 A；同 session pairing 仍保 lease；D286 仍绿。`conversationSessionChat.test.ts` / `conversationEngineRosterLiveTree.test.ts` / `conversationSessionStatus.test.ts` | conversation / livetree | closed |
| D293 | P3 | **closed** D288 pairing-hold leftover `getSessionSync` 原样回 `projection.snapshot.sync`。closed leftover 诚实；leftover `{ kind:'live' }` / `'syncing'` / `'degraded'` 时 Inbox / 会话徽标仍写「Session live / Session syncing」，而 `isEngineConnected()` 为 false、写已关。本刀：pairing-hold + leftover **active** kind → 降为既有 `conversationRoster.engineCacheReason` closed chrome（「Cached snapshot (read-only)」）；closed leftover 仍原样；真断连仍 `super`；接通仍回 live snapshot。未改 `showLiveChrome` / D285–D292 keep-last。未发明 proto / 新 SyncChrome kind。 | 工位 B `roster-pairing-live-sync-demote` 已收 @2026-09-11 | leftover live/syncing → pairingPending 非 live/syncing（closed）；closed leftover 仍 closed；真断连仍 stub idle。`conversationEngineRosterService.test.ts` 绿 | conversation / roster | closed |
| D294 | P3 | **closed** D289 pairing-hold 仍 `acquire` leftover **engine** lease。Lens `!isEngineConnected()` 写（confirm / question / retry / client-tool / continue）经 `postBound` → `lease.post` 仍可写，而 Inbox sync 已降（D293）、pending chip 仍 leftover（D291）。本刀：`postBound` pairing-hold 先拒（既有 failed `PostOutcome`）；binding 写在 leftover lease 上不 post，用户可见既有 `engine_disconnected`；Inbox Enqueue 不因 `hasEngineConnectionHistory()` 在 pairing-hold 可点。读面 leftover / 真断连 / 首拉 pairing 不改。未改 `showLiveChrome` / D285–D293 keep·demote·rebind。未发明 proto。 | 工位 A `pairing-hold-lens-writes` 已收 @2026-09-11 | leftover lease + pairingPending：confirm/question/retry 不 post + `engine_disconnected`；Enqueue live→pairingPending 禁用；真断连合同仍绿。`conversationLensDisposeGate.test.ts` / `conversationInboxOverlay.test.ts` | conversation / lens+inbox | closed |
| D295 | P3 | **closed** D294 已拒 leftover `postBound` / Inbox Enqueue。Composer Send 仍只看草稿（`updateSendEnabled`）；`submitDraft` 在 `wasEverConnected` 时先走断连 enqueue 再 `postBound`；`saveTurnEdit` / `saveQueueEdit` / `deleteTurn` / `cancelToolCall` 无 pairing-hold 闸（roster false → toast，仍像可写）。本刀：pairing-hold Send `aria-disabled`；`submitDraft` 不 enqueue / `lease.post`；其余写复用 D294 `rejectPairingHoldWrite` 画 `engine_disconnected`、已在 edit 则留下。读面 leftover / 真断连 / 首拉不改。未改 `showLiveChrome` / D285–D294 keep·demote·Enqueue。未发明 proto。 | 工位 B `pairing-hold-remaining-writes` 已收 @2026-09-11 | leftover + pairingPending + draft：Send disabled、submit 不 enqueue/post；saveTurnEdit / saveQueueEdit / deleteTurn / cancelToolCall 不写 + `engine_disconnected`；D294 仍绿。`conversationLensDisposeGate.test.ts` | conversation / lens+composer | closed |
| D296 | P3 | **closed** D293 已在 roster `getSessionSync` 把 pairing-hold leftover `live`/`syncing`/`degraded` 降为 cached closed。Inbox 已走 `getSessionSync()`；SessionBar / stale banner 仍用 raw `host.sessionViewLease.snapshot.sync`（`applySessionViewTimeline` → `updateSyncChrome` / `refreshStaleSnapshotBanner`），leftover live lease 可写「Session live」而 Inbox 写 cached read-only。本刀：三处改走 `stubService.getSessionSync(sessionId)`。接通 leftover live 仍 live。真断连不变。未发明新 SyncChrome kind。未改 write RPC / `showLiveChrome` / D285–D295 keep·Enqueue·Send。未发明 proto。 | 工位 C `session-bar-pairing-sync-demote` 已收 @2026-09-11 | leftover live/syncing lease → pairing 徽章非「Session live/syncing」，跟 `getSessionSync` closed chrome；接通 leftover live 仍 live。`conversationLensDisposeGate.test.ts`；D293 roster 测仍绿 | conversation / lens+sessionbar | closed |
| D297 | P3 | **closed** D294–D295 已拒 leftover `postBound` / Send / save / deleteTurn / cancel。时间线 confirm/question/retry/delete/cancel 与 SessionBar title/delete/new 仍像可写；`beginTurnEdit` / `beginQueueEdit` / Inbox 队列行仍进编辑；roster `renameSession` / `deleteSession` 在 pairing-hold 走 `wasEverConnected && !isEngineConnected()` 的 `callRemote:false` 本地改名/删行。本刀：`writesEnabled` 从 lens 传到 tree/renderer/process-fold，pairing-hold 写钮 `aria-disabled` 且不 resolve；beginTurn/QueueEdit 早退；Inbox 队列行不进编辑；SessionBar 写 chrome 对齐 Send；roster pairing-hold rename/delete 回 false（真断连本地改名合同保留）。未改 `showLiveChrome` / D285–D296 keep·Enqueue·Send·sync demote。未发明 proto。 | 工位 D `pairing-hold-write-affordance` 已收 @2026-09-11 | leftover + pairingPending：时间线写钮 disabled 且不 resolve；beginTurn/QueueEdit 不进编辑；Inbox 队列行不 hold；SessionBar title/delete/new disabled；roster rename/delete false 且行不变；接通仍写；真断连本地 rename 仍绿。D294–D296 仍绿 | conversation / lens+roster | closed |
| D298 | P3 | **closed** pairing-hold 时 `isEngineConnected()` 为 false，`tryConnectedEngineFork` 原回 `{ handled:false, forked:false }`，`ConversationForkConversationAction` 落到 `forkChatSession` + `openForkTab`，透镜仍画 leftover 却开出本地 stub 页（假成功）。本刀：pairing-hold + history → `handled:true` / `forked:false` + 既有断连 notice，不调 `forkChatSession` / `openForkTab`。真断连+history 与从未接通 stub 仍 fallthrough。接通 fork 不变。Create Snapshot `!connected`+history 同 notice（不再静默）。未改 `showLiveChrome` / D285–D297 keep·write chrome·sync demote。未发明 proto。 | 工位 A `pairing-hold-fork-fallthrough` 已收 @2026-09-11 | leftover + pairingPending：handled 且不 fork、`forkChatSession` 0、notice；真断连+history / stub 仍本地 fork；接通仍 `forkSubAgent`。`conversationForkEngine.test.ts` / `conversationSessionChat.test.ts` | conversation / fork | closed |
| D299 | P3 | **closed** pairing-hold 时 `isEngineConnected()` 为 false，F1 Kill 原 `!isEngineConnected()` 静默 return，透镜仍画 leftover engine session。Kill 无本地成功路径（不像 fork），故对齐 Create Snapshot 闸：接通才 `killSubAgent`，false → failed notice；`!connected`+history（pairing-hold leftover 与真断连 leftover）→ disconnected notice 且不调 `killSubAgent`；从未接通仍静默。未改 `showLiveChrome` / D285–D298 keep·write chrome·fork fallthrough。未发明 proto / 本地 catalog id / 关 Fork tab。 | 工位 B `kill-leftover-notice` 已收 @2026-09-11 | leftover + !connected+history：notice、kill 0 次；接通 true 静默且 kill 1 次；接通 false → failed notice；从未接通静默。`conversationKillEngine.test.ts` | conversation / kill | closed |
| D300 | P3 | **closed** D291 leftover pending 仍计数、D297 座已 `aria-disabled`。CS-4 `openPendingOnFocus` 与通知 Show 仍 `scrollToFirstPendingConfirmation`，焦点跳到不可确认 leftover 座（假“去处理”）。本刀：`shouldAutoRevealPendingConfirmation` 在 pairing-hold 跳过自动滚；Show 仍 `switchSession`+`showConversationPart`。Inbox 待确认点击不闸。未改 `showLiveChrome` / D285–D299 keep·write·kill。未发明 proto。 | 工位 C `cs4-pairing-pending-scroll` 已收 @2026-09-11 | pairing-hold pending：CS-4 / Show 0 scroll；接通仍滚；Show 仍切会话。`conversationPendingSeat.test.ts` / openPendingOnFocus / notifications | conversation / pending-reveal | closed |
| D301 | P3 | **closed** D300 已闸 CS-4 / 通知 Show；Inbox `.conversation-lens-inbox-pending` 点击仍无条件 `onScrollToPendingConfirmation` → `scrollToFirstPendingConfirmation`，跳到 leftover 已禁用确认座（假“去处理”）。本刀：复用 `shouldAutoRevealPendingConfirmation`（`!isConversationPairingHold`）闸 Inbox 点击；D291 leftover 计数仍可见。接通 leftover/live 仍 1 scroll。真断连合同不改。未改 CS-4 / Show / `showLiveChrome` / D285–D300 keep·write·kill·scroll skip。未发明 proto。 | 工位 A `inbox-pending-click-pairing` 已收 @2026-09-12 | leftover pending 可见 → pairingPending 点击 0 scroll；接通仍 1 scroll。`conversationInboxOverlay.test.ts` | conversation / inbox | closed |
| D302 | P3 | **closed** D290 后 Projects 仍按 `!isEngineConnected()` 把 pairing-hold 当真断连：活画 leftover workDir / sessions / local folders 走 stale/welcome。本刀：`isConversationPairingHold` + leftover → 保 tree 行，不翻 welcome / 空 disconnected；真断连仍 stale/welcome；首拉 pairing 无 leftover 仍 empty。未改 Agents Hierarchy/Activity 或 Team。未发明 proto / sessionList RPC。未关 D16。未碰 D8/D147。 | 工位 B `navigator-projects-pairing-leftover` 已收 @2026-09-12 | 活画后 pairingPending + phase connected：行数不变 + 非 welcome；随后真断连仍 stale；首拉 pairing 仍 empty。`navigatorProjectsList.test.ts` | navigator / projects | closed |
| D303 | P3 | **closed** D301 已闸 leftover pending **点击**（`shouldAutoRevealPendingConfirmation`）；chip 仍是可指点的 link 样式、无 `aria-disabled`，读屏仍像“去处理”。本刀：`!shouldAutoRevealPendingConfirmation(ua)` 时 pending chip 对齐 Enqueue（`disabled` + `aria-disabled="true"`），**不隐藏**（D291 leftover 计数仍可见）。接通 leftover/live 仍 `aria-disabled="false"` 且点击 1 scroll（D301）。真断连合同不改。未改 CS-4 / 通知 Show / `showLiveChrome` / D285–D302 keep·write·kill·Projects。未发明 proto。未关 D8/D16/D147。 | 工位 A `inbox-pending-chip-aria` 已收 @2026-09-12 | pairing-hold leftover pending 可见且 `aria-disabled="true"`；接通 `"false"`；D301 0/1 scroll 仍绿。`conversationInboxOverlay.test.ts`。leftover：`.conversation-lens-inbox-pending` 仍是 link 色/下划线，未加 `:disabled` 视觉 | conversation / inbox | closed |
| D304 | P3 | **closed** D288–D302 多次「未改 `showLiveChrome`」。`mountTimeline` 两处 `showLiveChrome: () => host.stubService.isEngineConnected()` 在 pairing-hold 为 false，leftover `· Running` / `· Loading` 被卸。本刀：pairing-hold + leftover（D287 cached turns / D289 leftover lease）读面 live chrome 仍开；首拉 pairing 无 leftover 不假造；真断连仍藏。`writesEnabled` 仍 `!isConversationPairingHold`。未改 Inbox / Sessions / navigator / CS-4。未发明 proto / 新 chrome kind。未关 D8/D16/D147。 | 工位 B `pairing-hold-show-live-chrome-read` 已收 @2026-09-12 | leftover + pairingPending：`shouldShowReadingColumnLiveChrome` true；首拉 pairing false；真断连 false；writesEnabled 仍关。`conversationLensDisposeGate.test.ts` / `conversationProcessFold.test.ts` | conversation / lens | closed |
| D305 | P3 | **closed** D141 后 Sessions 侧栏 `createNewSession` 只闸 `!isEngineConnected() && hasEngineConnectionHistory()`。pairing-hold leftover 碰巧命中断连闸，但未测且耦合断连文案而非 pairing 写合同（同 D290→D302 类洞）。本刀：先 `isConversationPairingHold` 再 create，对齐 SessionBar；pairing-hold → 既有 disconnected notice、0 `createSession`；真断连+history 仍 notice；从未接通 stub 仍可 create。未改 delete/beside / Inbox / `showLiveChrome` / navigator。未发明 proto / sessionList RPC。未关 D8/D16/D147。 | 工位 C `sessions-view-pairing-create-guard` 已收 @2026-09-12 | leftover/history + pairingPending：notice + `createSession` 0；接通仍 create；真断连仍 notice。`conversationSessionsView.test.ts` | conversation / sessions | closed |
| D306 | P3 | **closed** D305 已闸 Sessions 侧栏 `createNewSession`；`deleteActiveSession` 仍无条件调 `stubService.deleteSession`。pairing-hold leftover-looks-live（`isEngineConnected()===true` + `pairingPending`）会静默删 leftover。本刀：先 `isConversationPairingHold` 再 delete，对齐 SessionBar / D305 create；pairing-hold → 既有 disconnected notice、0 `deleteSession`、roster 不变；真断连+history 仍 notice；接通仍删。未改 create / Inbox / `showLiveChrome` / navigator / chip CSS。未发明 proto。未关 D8/D16/D147。 | 工位 A `sessions-view-pairing-delete-guard` 已收 @2026-09-12 | leftover/history + pairingPending：notice + `deleteSession` 0 + roster 同；接通仍删；真断连仍 notice。`conversationSessionsView.test.ts` | conversation / sessions | closed |
| D307 | P3 | **closed** D294–D303 已闸 Inbox Enqueue / 队列行编辑 / pending 点击+aria。Goal / Stop 仍只看 `isEngineConnected()`；leftover-looks-live（phase `connected` + `pairingPending`，夹具可 `isEngineConnected()===true`）仍可 `setSessionGoal` / `cancelGeneration`。同 leftover 队列面板 Pause / Resume / Clear 仍改 leftover 队列；Retry 仍只看 `isEngineConnected()`。本刀：pairing-hold 闸 Goal/Stop/Pause/Resume/Clear/Retry；`onStopClicked` 先拒 pairing-hold，避免 connected→pairing 未 `render()` 时 Stop 仍 enabled 取消 leftover。接通 leftover 仍可写；真断连合同不改。未改 pending chip CSS / Sessions / `showLiveChrome` / navigator。未发明 proto。未关 D8/D16/D147。 | 工位 B `inbox-leftover-remaining-writes` 已收 @2026-09-12 | leftover + pairingPending：Goal/Stop/Pause/Resume/Clear/Retry disabled，0 goal/cancel/queue mutate；接通 leftover 仍可写。`conversationInboxOverlay.test.ts` | conversation / inbox | closed |
| D308 | P3 | **closed** D307 后 composer Permission / Model 仍按 `isEngineConnected() && typeof hook` 启写，`applySessionPermissionIndex` / `applySessionModelIndex` 直写 unary。leftover-looks-live（`isEngineConnected()===true` + `pairingPending`）会 `setPermissionMode` / `switchModel`。Permission 拒写原先 `return` 不回滚；Tune 按 `isEngineConnected()` 可把 leftover 工具画成「No tools」。本刀：`isComposerSessionWriteLive` = connected && `!isConversationPairingHold`；Permission 拒写回滚对齐 Model；More radios 仍挂 listener 但 apply 闸；Tune leftover catalog 仍画。未改 Inbox / Sessions / Roster / SessionBar / pending-chip CSS。未发明 proto。未关 D8/D16/D147。 | 工位 A `composer-session-selects-pairing-hold` 已收 @2026-09-12 | leftover-looks-live：Permission/Model/More `disabled`+`aria-disabled`，forced-enable 仍 0 unary 且 Permission 回旧 index；Tune 仍画 leftover；接通成功/失败回滚仍绿。`conversationLensDisposeGate.test.ts` | conversation / composer | closed |
| D309 | P3 | **closed** D280 pairing leftover 只 `paintListFailed`（断连文案、保行），Restore/Delete 仍是上次接通的 enabled。`restoreSnapshot` / `canSendDelete` 只看 `isEngineConnected()`，不看 `isConversationPairingHold`。既有测锁行+文案+0 额外 list，不锁 disabled/aria/0 unary/leftover-looks-live。leftover-looks-live（`isEngineConnected()===true` + pairingPending）可发 restore/delete。本刀：pairing leftover 行仍在，写钮 `disabled`+`aria-disabled`，forced click 0 unary；leftover-looks-live 同闸；接通 leftover 仍可写；真断连仍卸行。未改 Inbox / Sessions / composer-chrome / roster / pending-chip CSS。未发明 proto。未关 D8/D16/D147。 | 工位 A `snapshots-overlay-leftover-writes` 已收 @2026-09-12 | leftover + pairingPending：行仍在，Restore/Delete disabled+aria-disabled，forced click 0 unary；leftover-looks-live 亦 0 unary；接通 leftover 仍可写；真断连仍卸行。`conversationEngineSnapshotsList.test.ts` | conversation / snapshots-overlay | closed |
| D310 | P3 | **closed** roster `createSnapshot` 无 pairing-hold 拒写（`deleteSession` 有）。`isEngineConnected()` 折 pairing → wasEverConnected → `createEngineSnapshot(..., false)` → false、0 unary，leftover 合同未测（仅真断连 skip）。leftover-looks-live（`testEngineConnected===true` + pairingPending）走 live 发 unary。F1 只看 `roster.isEngineConnected()`，不先 `isConversationPairingHold`。本刀：roster / F1 先 pairing-hold 拒写；pairing-hold leftover → false + 0 unary + roster 不变；looks-live 亦 0 unary；接通仍转发；真断连 skip 仍绿；F1 pairing-hold → disconnected notice。未改 overlay list leftover KEEP（D309）/ Inbox / SessionsView / composer-chrome / pending-chip CSS。未发明 proto。未关 D8/D16/D147。 | 工位 B `roster-create-snapshot-pairing-hold` 已收 @2026-09-12 | leftover + pairingPending：`createSnapshot` false、`createSnapshotCalls.length===0`、roster 不变；looks-live 亦 0 unary；接通仍转发；真断连 skip 仍绿；F1 pairing-hold → disconnected + 0 createSnapshot。`conversationEngineRosterService.test.ts` / `conversationCreateSnapshotActions.test.ts` | conversation / roster-writes | closed |
| D311 | P3 | **closed** D281 KEEP leftover 行。Fire/Enable/Disable/Delete/Upsert 仍只看 `isEngineConnected()`（`canSendEngineTrigger*`）。leftover-looks-live（`isEngineConnected()===true` + pairingPending）跳过 keep-last、再 list、写钮仍 unary。本刀：pairing leftover 行仍在，写钮 disabled，forced click 0 unary，0 extra list；leftover-looks-live 同闸；接通 leftover 仍可写；真断连仍卸行。未改 Inbox / Sessions / composer-chrome / snapshots overlay / roster createSnapshot / F1。未重做 D278–D284 KEEP。不发明 proto。未关 D8/D16/D147。 | 工位 A `triggers-leftover-looks-live-writes` 已收 @2026-09-12 | leftover + pairingPending：行仍在，写钮 disabled，forced click 0 unary，0 extra list；leftover-looks-live 亦 0 unary / 0 extra list；接通 leftover 仍可写；真断连仍卸行。`engineTriggersSection.test.ts` | conversation / triggers | closed |
| D312 | P3 | **closed** D283 KEEP leftover 行；写门仍只看 `isEngineConnected()`。`canSendSourcesGitStagePaths` / Commit / ApplyHunks 只看 `connected`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）仍可 Stage/Commit/Accept。pairing KEEP 测不锁 `canWriteStage===false` / 0 unary。本刀：`isSourcesGitWriteLive(connected, pairingHold)`；Changes pairing leftover 行仍在 + pairing note，Stage/Commit disabled，forced 0 unary；looks-live 亦 0 unary；接通 leftover 仍可写；真断连仍 SCM / localOnly。未改 D283 KEEP；未发明 WriteGitUnstage / proto。未扩 Diff/Review 第二模块（同 helper 可选闸）。未关 D8/D16/D147 / D31 F4。 | 工位 B `sources-leftover-looks-live-writes` 已收 @2026-09-12 | leftover + pairingPending：行仍在 + pairing note，Stage/Commit disabled，forced 0 unary；looks-live 亦 0 unary；接通 leftover 仍可写；真断连仍 SCM。`sourcesChangesGitWrite.test.ts` / `sourcesChangesList.test.ts` | sources / git-writes | closed |
| D313 | P3 | **closed** catalog `canWrite()` = `canPerformCatalogWrite(mode) && isEngineConnected()`，无 `isConversationPairingHold`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）仍 unary Add/Update/Remove/Enable。本刀：`canPerformCatalogWriteLive`；MCP/Skills/Agents/Tools/Plugins 同闸；pairing leftover 行仍在，写钮 hidden，forced 0 unary；looks-live 亦 0 unary / 0 extra list；接通 leftover 仍可写；真断连仍卸行。未改 Inbox / Sessions / composer-chrome / snapshots / roster createSnapshot / Triggers / Sources。未重做 KEEP leftover list。不发明 proto。未关 D8/D16/D147。 | 工位 A `catalog-leftover-looks-live-writes` 已收 @2026-09-12 | leftover + pairingPending：行仍在，写钮 hidden，forced 0 unary；looks-live 亦 0 unary；接通 leftover 仍可写；真断连仍卸行。`engineCatalogSections.test.ts` / plugins / skills | conversation / engine-catalog | closed |
| D314 | P3 | **closed** D281 KEEP leftover 行。Write/Clear（Read 不 mutate）仍只看 `isEngineConnected()`（`canSendEngineClipboard*`）。leftover-looks-live（`isEngineConnected()===true` + pairingPending）跳过 keep-last、再 list、写钮仍 unary。本刀：pairing leftover 行仍在，Write/Clear disabled，forced click 0 unary，0 extra list；leftover-looks-live 同闸；接通 leftover 仍可写；真断连仍卸行。Read 不 mutate 未闸。未改 Triggers / Sources / Inbox / Sessions / composer / snapshots / roster。未重做 D281 KEEP。不发明 proto。未关 D8/D16/D147。 | 工位 B `clipboard-leftover-looks-live-writes` 已收 @2026-09-12 | leftover + pairingPending：行仍在，写钮 disabled，forced click 0 unary，0 extra list；leftover-looks-live 亦 0 unary / 0 extra list；接通 leftover 仍可写；真断连仍卸行。`engineClipboardSection.test.ts` / `engineClipboardList.test.ts` | conversation / clipboard | closed |
| D315 | P3 | **closed** D312 已给 `canSendSourcesGitApplyHunks` / `tryWriteSourcesGitApplyHunks` 加 `pairingHold`，Diff/Review 仍不传。leftover-looks-live（`isEngineConnected()===true` + pairingPending）Accept chrome 仍按 connected 开、forced click 仍 unary。本刀：`isConversationPairingHold(ua)` 作第三/末参；leftover-looks-live Accept chrome hidden，forced click 0 unary。未改 D311–D314（Triggers / Sources Changes / catalog / Clipboard）。未重做 D283/D312 KEEP。未发明 WriteGitUnstage / proto。未关 D8/D16/D147 / D31 F4。 | 工位 A `diff-review-accept-leftover-writes` 已收 @2026-09-12 | leftover-looks-live：Accept chrome hidden，forced click 0 unary。`sourcesDiffPanel.test.ts` / `sourcesWiringScan.test.ts` | sources / diff-review | closed |
| D316 | P3 | **closed** D225 KEEP leftover 行。List/Read 仍只看 `isEngineConnected()`（`canSendEngineContextVariable*`）。leftover-looks-live（`isEngineConnected()===true` + pairingPending）跳过 keep-last、再 list、Read 仍 unary。本刀：pairing leftover 行仍在，Read disabled，forced click 0 unary，0 extra list；leftover-looks-live 同闸；接通 leftover 仍可 Read；真断连仍卸行。未改 Triggers / Sources / catalog / Clipboard / Diff/Review。未重做 KEEP leftover list。不发明 proto。未关 D8/D16/D147。 | 工位 B `context-variable-leftover-looks-live` 已收 @2026-09-12 | leftover + pairingPending：行仍在，Read disabled，forced click 0 unary，0 extra list；leftover-looks-live 亦 0 unary / 0 extra list；接通 leftover 仍可 Read；真断连仍卸行。`engineContextVariableSection.test.ts` / `engineContextVariableList.test.ts` | conversation / context-variables | closed |
| D317 | P3 | **closed** D315 只闸 Diff/Review Accept。`canSendSourcesGitStagePaths` / `tryWriteSourcesGitStagePaths` 在 `sourcesDiffPanelView.ts` / `conversationDiffReviewPane.ts` 仍不传 `pairingHold`。本刀：`isConversationPairingHold(ua)` 作第四/末参；leftover-looks-live Stage chrome hidden，forced click / `runStage` 0 unary。未改 D312 Changes / D315 Accept。未发明 WriteGitUnstage / proto。未关 D8/D16/D147 / D31 F4。 | 工位 A `diff-review-stage-leftover-writes` 已收 @2026-09-12 | leftover-looks-live：Diff/Review Stage chrome hidden，forced click / `runStage` 0 unary。`sourcesDiffPanel.test.ts` / `sourcesWiringScan.test.ts` | sources / diff-review | closed |
| D318 | P3 | **closed** D299 只闸 `!isEngineConnected()`+history。`tryKillSubAgent` 仍只看 `roster.isEngineConnected()`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）仍 `killSubAgent` unary。本刀：先 `isConversationPairingHold(ua)`；pairing-hold / leftover-looks-live → 0 `killSubAgent`、disconnected notice；接通无 pairing 仍 kill；真断连 leftover 仍 notice；从未接通仍静默。未改 Create Snapshot / roster delete / Diff/Review / Clipboard。不发明 proto。未关 D8/D16/D147。 | 工位 A `kill-leftover-looks-live` 已收 @2026-09-12 | leftover-looks-live：0 kill unary + notice；接通仍 kill；真断连仍 notice；从未接通静默。`conversationKillEngine.test.ts` | conversation / kill | closed |
| D319 | P3 | **closed** D314 已闸 Write/Clear。Read 仍只看 `isEngineConnected()`（`canSendEngineClipboardRead` / `handleRead`）。leftover-looks-live（`isEngineConnected()===true` + pairingPending）Read 仍 unary。本刀：`pairingHold` 加入 `canSendEngineClipboardRead`；pairing leftover 行仍在，Read disabled，forced click 0 unary，0 extra list；leftover-looks-live 同闸；接通 leftover 仍可 Read；真断连仍卸行。未改 Write/Clear（D314）。未改 Triggers / Sources / catalog / Diff/Review / Kill。未重做 D281 KEEP。不发明 proto。未关 D8/D16/D147。 | 工位 B `clipboard-read-leftover-looks-live` 已收 @2026-09-12 | leftover + pairingPending：行仍在，Read disabled，forced click 0 unary，0 extra list；leftover-looks-live 亦 0 unary / 0 extra list；接通 leftover 仍可 Read；真断连仍卸行。`engineClipboardSection.test.ts` / `engineClipboardList.test.ts` | conversation / clipboard | closed |
| D320 | P3 | **closed** D298 只闸 pairing-hold 且 `isEngineConnected()` 为 false。`tryConnectedEngineFork` 仍先看 `roster.isEngineConnected()`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）仍 `forkSubAgent` unary。本刀：先 `isConversationPairingHold(ua)`；pairing-hold / leftover-looks-live → 0 `forkSubAgent`、disconnected notice、handled。接通无 pairing 仍 fork。真断连 leftover + 从未接通仍 unhandled（D298 本地 fork 合同）。未改 Kill（D318）/ Clipboard / Diff/Review / Connection hub。不发明 proto。未关 D8/D16/D147。 | 工位 A `fork-leftover-looks-live` 已收 @2026-09-12 | leftover-looks-live：0 fork unary + notice + handled；接通仍 fork；真断连 leftover / 从未接通仍 fallthrough。`conversationForkEngine.test.ts` | conversation / fork | closed |
| D321 | P3 | **closed** `canSendConnectionDeviceRotateToken` / `canSendConnectionDevicePairRequest` / `canSendConnectionDeviceRevokeRequest` 只看 `connected + hasHook`。pane Rotate / Revoke / PairApprove / PairReject 不传 `pairingHold`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）仍 unary。本刀：`pairingHold` 对齐 Clipboard Write；pane 写点传 `isConversationPairingHold`；leftover-looks-live chrome disabled，forced click 0 unary；接通 leftover 仍可写；真断连仍拒。未改 Kill/Fork/Clipboard/Diff/Review/catalog。不发明 proto。未关 D8/D16/D147。 | 工位 B `connection-device-leftover-looks-live-writes` 已收 @2026-09-12 | leftover-looks-live：Rotate/Pair/Revoke chrome disabled，forced click 0 unary；接通 leftover 仍可写；真断连仍拒。`connectionDeviceList.test.ts` / `connectionDevicePair.test.ts` / `connectionDeviceRevoke.test.ts` / `connectionPreferencesPane.test.ts` | conversation / connection-devices | closed |
| D322 | P3 | **closed** D317 gated `writeGitStagePaths` with pairingHold. After that reject, `runStage` in Diff/Review still falls through to `runGitAction(SOURCES_GIT_STAGE_COMMAND)` when `scmResource` exists. leftover-looks-live + SCM row: Stage can still mutate via `git.stage`. 本刀：`isConversationPairingHold(ua)` 则不 fallthrough `git.stage`。0 unary AND 0 `git.stage`。接通 leftover 无 pairing 仍可 git.stage / write。未发明 WriteGitUnstage / proto。未重做 D317 Accept/canSend。未关 D8/D16/D147 / D31 F4。 | 工位 A `diff-review-stage-git-stage-fallthrough` 已收 @2026-09-12 | leftover-looks-live + scmResource：Diff/Review `runStage` 0 unary、0 `git.stage`。`sourcesDiffPanel.test.ts` 12 passing | sources / diff-review | closed |
| D323 | P3 | **closed** D321 已闸 Rotate/Pair/Revoke。`updateDeviceActions` 仍 `renameDeviceButton.enabled = hasDevice`（无视 `pairingHold`）；`handleRenameSelectedDevice` 无 pairing-hold 检查。leftover-looks-live（`isEngineConnected()===true` + pairingPending）Rename chrome 仍开、forced click 仍 `renameDevice`。本刀：`isDeviceWritePairingHold()` / `isConversationPairingHold` 闸 chrome + handler；leftover-looks-live chrome disabled，forced click 0 `renameDevice`；接通 leftover 仍可 Rename；真断连 leftover 仍拒。未改 Rotate/Pair/Revoke（D321）。未改 Kill/Fork/Clipboard/Diff/Review。不发明 proto。未关 D8/D16/D147。 | 工位 B `hub-rename-leftover-looks-live` 已收 @2026-09-12 | leftover-looks-live：Rename chrome disabled，forced click 0 `renameDevice`；接通 leftover 仍可 Rename；真断连 leftover 仍拒。`connectionPreferencesPane.test.ts` | conversation / connection-devices | closed |
| D324 | P3 | **closed** D310 已闸 leftover-looks-live `createSnapshot`。`renameSession` / `deleteSession` 仍先 `isEngineConnected()` 再 pairing-hold。leftover-looks-live（`isEngineConnected()===true` + pairingPending）仍 `renameEngineSession` / `deleteEngineSession` unary。本刀：对齐 createSnapshot，先 `isConversationPairingHold`；leftover-looks-live rename/delete 回 false、0 engine unary；接通无 pairing 仍写；真断连 leftover 仍本地（pairing-hold 断连已 false）。未改 cancelGeneration / setSessionGoal / fork / kill。无 Sessions view chrome。不发明 proto。未关 D8/D16/D147。 | 工位 A `roster-rename-delete-leftover-looks-live` 已收 @2026-09-12 | leftover-looks-live：rename/delete false、0 unary；接通仍写；真断连 leftover 仍本地。`conversationEngineRosterService.test.ts` 97 passing | conversation / roster-writes | closed |
| D325 | P3 | **closed** D317/D322 已闸 Diff/Review Stage `writeGitStagePaths` + `git.stage` fallthrough。Unstage / Revert（`git.clean`）仍 `runGitAction(SOURCES_GIT_UNSTAGE_COMMAND / SOURCES_GIT_CLEAN_COMMAND)`，无 pairing-hold。leftover-looks-live + SCM 仍可 mutate。本刀：`isConversationPairingHold(ua)` 则 hide Unstage/Revert chrome，并对这两条命令 skip `runGitAction`。0 `git.unstage` / `git.clean`。接通 leftover 无 pairing 仍可 unstage/clean。未发明 WriteGitUnstage / proto。未重做 Stage/Accept。未关 D8/D16/D147 / D31 F4。 | 工位 B `diff-review-unstage-revert-leftover-looks-live` 已收 @2026-09-12 | leftover-looks-live + scmResource：Diff/Review Unstage/Revert chrome hidden，0 `git.unstage` / `git.clean`；接通 leftover 无 pairing 仍可。`sourcesDiffPanel.test.ts` 14 passing | sources / diff-review | closed |
| D326 | P3 | **closed** D307 已闸 Inbox Goal/Stop UI。D324 已闸 leftover-looks-live rename/delete。`cancelGeneration` / `setSessionGoal` / `cancelSessionGoal` 仍先 `isEngineConnected()`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）仍 unary。本刀：对齐 renameSession，先 `isConversationPairingHold`；leftover-looks-live cancel/goal 回 false、0 engine unary；接通无 pairing 仍转发；真断连 leftover 仍 skip unary。未改 fork/kill/cancelToolCall/queue/confirm/Sessions chrome。不发明 proto。未关 D8/D16/D147。 | 工位 A `roster-cancel-goal-leftover-looks-live` 已收 @2026-09-12 | leftover-looks-live：cancel/goal false、0 unary；接通仍转发；真断连 leftover 仍 skip。`conversationEngineRosterService.test.ts` 98 passing | conversation / roster-writes | closed |
| D327 | P3 | **closed** D312 已闸 Changes Stage/Commit；D325 已闸 Diff/Review Unstage/Revert。Changes `updateSelectionToolbar` 仍 `unstageSelectedButton.enabled = canUnstage`（无 pairing-hold）；`runResourceAction('unstage')` 仍 `executeCommand(SOURCES_GIT_UNSTAGE_COMMAND)`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）+ SCM 行仍可 `git.unstage`。本刀：pairing-hold 禁用 toolbar Unstage、隐藏行 Unstage chrome，并 skip `git.unstage` execute。接通 leftover 无 pairing 仍可 unstage。未重做 Stage/Commit/Accept/Diff-Review Unstage（D312/D315/D325）。未发明 WriteGitUnstage / proto。未关 D8/D16/D147 / D31 F4。 | 工位 B `sources-changes-unstage-leftover-looks-live` 已收 @2026-09-12 | leftover-looks-live + scmResource：Changes Unstage disabled，forced click / runOnSelected 0 `git.unstage`；接通 leftover 无 pairing 仍可。`sourcesChangesList.test.ts` 17 passing | sources / changes | closed |
| D328 | P3 | **closed** D294 `postBound` 已先拒 pairing-hold。`resolveConfirmation` / `resolveQuestion` 仍先 `isEngineConnected()` 再 `rejectPairingHoldWrite`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）走接通分支转发 `resolveConfirmation` / `respondQuestion` unary。本刀：先 `rejectPairingHoldWrite`；leftover-looks-live 画 `engine_disconnected`、0 stub unary；接通无 pairing 仍转发。未改 `postBound`（D294）/ composer Permission·Model（D308）/ roster 写。不发明 proto。未关 D8/D16/D147。 | 工位 D `lens-confirm-question-leftover-looks-live` 已收 @2026-09-12 | leftover-looks-live：0 `resolveConfirmation` / `respondQuestion` unary + disconnected notice；接通无 pairing 仍转发。`conversationLensDisposeGate.test.ts` | conversation / lens | closed |
| D329 | P3 | **closed** D318/D320 已闸 engine-layer Kill/Fork UI（`tryKillSubAgent` / `tryConnectedEngineFork`）。D324/D326 已闸 roster rename/delete/cancel/goal。`forkSubAgent` / `killSubAgent` 仍先 `isEngineConnected()`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）仍 unary。本刀：对齐 cancel/goal，先 `isConversationPairingHold`；leftover-looks-live fork/kill 回 false、0 engine unary；接通无 pairing 仍转发；真断连 leftover 仍 skip unary。未改 queue / cancelToolCall / resolveConfirmation / respondClientTool / respondQuestion / Sessions chrome。不发明 proto。未关 D8/D16/D147。 | 工位 A `roster-fork-kill-leftover-looks-live` 已收 @2026-09-12 | leftover-looks-live：fork/kill false、0 unary；接通仍转发；真断连 leftover 仍 skip。`conversationEngineRosterService.test.ts` 99 passing | conversation / roster-writes | closed |
| D330 | P3 | **closed** D307 已闸 Inbox Goal/Stop/Pause/Resume/Clear/Retry UI。D326/D329 已闸 roster cancel/goal/fork/kill。Lens `cancelToolCall` 已先 `rejectPairingHoldWrite`。roster `cancelToolCall` 与 queue 写（enqueue/retry/pause/resume/clear/hold/release/update）仍先 `isEngineConnected()`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）仍 unary。本刀：对齐 fork/kill，先 `isConversationPairingHold`；leftover-looks-live 回 false/no-op、0 engine unary；接通无 pairing 仍转发；真断连 leftover 仍 skip unary。未改 retryError / deleteTurn / updateUserTurnText / resolveConfirmation / respondClientTool / respondQuestion / Sessions chrome。不发明 proto。未关 D8/D16/D147。 | 工位 A `roster-queue-canceltool-leftover-looks-live` 已收 @2026-09-12 | leftover-looks-live：cancelToolCall/queue false/no-op、0 unary；接通仍转发；真断连 leftover 仍 skip。`conversationEngineRosterService.test.ts` 101 passing | conversation / roster-writes | closed |
| D331 | P3 | **closed** D328 已闸 lens `resolveConfirmation` / `resolveQuestion`。D330 已闸 roster cancelToolCall + queue。roster `retryError` / `deleteTurn` / `updateUserTurnText` / `resolveConfirmation` / `respondClientTool` / `respondQuestion` 仍先 `isEngineConnected()`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）仍 unary。本刀：对齐 cancelToolCall，先 `isConversationPairingHold`；leftover-looks-live 回 false、0 engine unary；接通无 pairing 仍转发。未改 cancelToolCall / queue / Sessions chrome。不发明 proto。未关 D8/D16/D147。 | 工位 A `roster-turn-confirm-leftover-looks-live` 已收 @2026-09-12 | leftover-looks-live：retryError/deleteTurn/updateUserTurnText/resolveConfirmation/respondClientTool/respondQuestion false、0 unary；接通仍转发。`conversationEngineRosterService.test.ts` 103 passing | conversation / roster-writes | closed |
| D332 | P3 | **closed** D305 已闸 Sessions view `createNewSession`。D324+ 已闸其它 roster 写。`createSession` / `switchSession` 仍先 `isEngineConnected()`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）仍 `ensureEngineSession`（engine Create）与 `requestAgentTreeRefresh`。本刀：先 `isConversationPairingHold`；create 回 `''`、0 `ensureEngineSession` / create unary（对齐 wasEverConnected 空回）；switch 可改 leftover 本地选中、0 `requestAgentTreeRefresh`；接通无 pairing 仍 create/refresh。未改 cancel/goal/fork/kill/queue/turn/confirm。不发明 proto。未关 D8/D16/D147。 | 工位 A `roster-create-switch-leftover-looks-live` 已收 @2026-09-12 | leftover-looks-live：create `''`、0 unary；switch 0 tree-refresh；接通仍 create/refresh。`conversationEngineRosterService.test.ts` 104 passing | conversation / roster-writes | closed |
| D333 | P3 | **closed** D332 已闸 `createSession` / `switchSession` 公路面。`activateEngineSession` 仍先 `isEngineConnected()`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）经 `adoptEngineSession` / bind-fail fallback / catalog refresh 仍 `requestAgentTreeRefresh`。本刀：先 `isConversationPairingHold`，跳过该私有方法内引擎写；本地 activate/select 仍可。接通无 pairing 仍 refresh。未改 create/switch 公路面。不发明 proto。未关 D8/D16/D147。 | 工位 A `roster-activate-leftover-looks-live` 已收 @2026-09-12 | leftover-looks-live：catalog refresh 达 `activateEngineSession`、0 `requestAgentTreeRefresh`；本地仍选 leftover。`conversationEngineRosterService.test.ts` 105 passing | conversation / roster-writes | closed |
| D334 | P3 | **closed** D290 已闸 pairing-hold leftover KEEP；D332/D333 已闸 roster switch/activate 的 `requestAgentTreeRefresh`。Navigator `refreshAgentTree()` 仍 `isEngineConnected() && sessionId` 即 refresh。既有 pairing-hold 测 `roster.setEngineConnected(false)`，不覆盖 leftover-looks-live。本刀：先 `isConversationPairingHold`；leftover-looks-live（`isEngineConnected()===true` + pairingPending）→ 0 `requestAgentTreeRefresh`；接通无 pairing 仍 refresh。未改 D290 KEEP / roster activate。不发明 proto。未关 D8/D16/D147。 | 工位 B `navigator-refresh-leftover-looks-live` 已收 @2026-09-12（未提交） | leftover-looks-live：`refreshAgentTree()` 0 `requestAgentTreeRefresh`；接通仍 refresh。`navigatorAgentsSubviews.test.ts` 39 passing | navigator / agents | closed |
| D335 | P3 | **closed** D334 已闸 leftover-looks-live `refreshAgentTree()` 0 unary。Refresh **chrome/command** 仍 `UA_ENGINE_CONNECTED_KEY` ← `isEngineConnected()`，leftover-looks-live（`isEngineConnected()===true` + pairingPending）看起来可点。本刀：`updateEngineConnectedContextKey` 先 `isConversationPairingHold`；leftover-looks-live → key false / precondition 关 Refresh；接通无 pairing 仍 enabled。Click/command 仍 0 `requestAgentTreeRefresh`（D334）。未改 pending-chip CSS / Sessions New / D290 KEEP。不发明 proto。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 A `navigator-refresh-chrome-leftover-looks-live` 已收 @2026-09-12（未提交） | leftover-looks-live：Refresh chrome/command 非 enabled；接通仍 enabled；D334 0 unary 仍绿。`navigatorAgentsSubviews.test.ts` 39 passing | navigator / agents | closed |
| D336 | P3 | **closed** D290 已闸 pairing-hold leftover KEEP。`refreshTeamData()` 仍先算 `engineReady = isEngineConnected() && phase connected`，KEEP 只在 `!engineReady`。leftover-looks-live（`isEngineConnected()===true` + pairingPending + phase connected）→ engineReady true → 仍发 teamInfo / memberStatus / taskList。既有 KEEP 测 `roster.setEngineConnected(false)`，不覆盖 leftover-looks-live。本刀：先 `isConversationPairingHold`；leftover-looks-live → 0 额外 Team unaries，KEEP leftover 行 + stale-note（同断连 pairing-hold）。既有 KEEP（`setEngineConnected(false)`）仍绿。未改 Agents / Projects / pending-chip CSS / Sessions New。不发明 proto。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 B `navigator-team-leftover-looks-live` 已收 @2026-09-12（未提交） | leftover-looks-live：0 额外 teamInfo/memberStatus/taskList + leftover 行 + stale-note；既有 KEEP 仍绿。`navigatorTeamSubviews.test.ts` 31 passing | navigator / team | closed |
| D337 | P3 | **closed** D278/D281 KEEP leftover 行只从 `refresh()` `!connected` 走 `applyDisconnectedRefresh`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）跳过 KEEP，仍 `getMcpServerStatuses` / `loadTools`→`getMcpServerTools`，Refresh chrome 仍开。本刀：先 `isConversationPairingHold`；leftover-looks-live KEEP leftover 行 + disconnected note，0 额外 statuses/tools，Refresh `enabled===false` / toolbar hidden；首拉 pairing 无 leftover 仍 empty、0 unary；真断连仍清；接通 leftover 仍 refresh。既有 KEEP（`!connected` pairing）仍绿。未改 Team / navigator / History / proto。不发明 proto。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 A `mcp-runtime-leftover-looks-live` 已收 @2026-09-12（未提交） | leftover-looks-live：KEEP leftover 行 + disconnected note，0 额外 `getMcpServerStatuses`/`getMcpServerTools`，Refresh off；首拉 pairing 仍 empty、0 unary；真断连仍清；接通 leftover 仍 refresh。`engineMcpRuntimePanel.test.ts` | conversation / mcp-runtime | closed |
| D338 | P3 | **closed** D281 已闸 pairing-hold leftover KEEP。History overlay `refresh` KEEP 只在 `!isEngineConnected()`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）仍额外 `getHistory`。既有 KEEP 测用生产 stub（`connected && !pairingPending`），不覆盖 leftover-looks-live。本刀：先 `isConversationPairingHold`；leftover-looks-live → 0 额外 `getHistory`，KEEP leftover 行 + disconnected note（同断连 pairing-hold）。既有 KEEP 仍绿。无 Restore/Delete 写。不发明 proto。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 D `history-leftover-looks-live` 已收 @2026-09-12（未提交） | leftover-looks-live：0 额外 getHistory + leftover 行 + disconnected note；既有 KEEP 仍绿。`conversationEngineHistoryList.test.ts` 13 passing | conversation / history | closed |
| D339 | P3 | **closed** Composer `refreshComposerCatalogs` KEEP 只在 `!isEngineConnected()`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）落入 `loadConnectedComposerCatalogs`，额外 `listAgentProfiles` / `listModels` / `listTools`；`updateGateRow` 按 `isEngineConnected()===true` 藏 dock gate。既有 KEEP 测 `connected && !pairingPending`（D282），不覆盖 leftover-looks-live。本刀：先 `isConversationPairingHold`；leftover-looks-live KEEP leftover agent/model/tools，0 额外 list，gate 仍 disconnected（不藏），Send 仍 pairing-hold-first。首拉 pairing 无 leftover 仍 empty、0 lists；真断连仍清；接通 leftover 仍 refresh。既有 KEEP 仍绿。未重做 Permission/Model（D308）。不发明 proto。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 B `composer-catalog-leftover-looks-live` 已收 @2026-09-12（未提交） | leftover-looks-live：KEEP leftover catalog，0 额外 list unaries，gate 仍 disconnected，Send disabled；首拉 pairing 仍 empty、0 lists；真断连仍清；接通 leftover 仍 refresh。`conversationComposerCatalog.test.ts` | conversation / composer | closed |
| D340 | P3 | **closed** D280/D281 KEEP leftover 行只从 ProviderModel `refreshModels` / Overview `renderAsync` 的 `!connected` 走 `applyDisconnected*`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）跳过 KEEP，仍额外 `listModels`。本刀：先 `isConversationPairingHold`；leftover-looks-live KEEP leftover 行/summary + disconnected note，0 额外 list；既有 KEEP（`!connected` pairing）仍绿。未改 MCP Runtime / History / composer / connectionPreferencesPane。不发明 proto。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 A `provider-model-overview-leftover-looks-live` 已收 @2026-09-12（未提交） | leftover-looks-live：KEEP leftover + 0 额外 listModels；既有 KEEP 仍绿。`engineProviderModelSection.test.ts` 6 / `engineOverviewSection.test.ts` 15；合计 22 passing | conversation / overview-catalog | closed |
| D341 | P3 | **closed** D280 pairing leftover KEEP 只从 `refreshEngineDevices` / `refreshEnginePending` 的 `!connected` 走 `applyDisconnected*Refresh`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）跳过 KEEP，仍额外 `listDevices` / `listPending`。既有 KEEP 测用生产 stub（`connected && !pairingPending`），不覆盖 leftover-looks-live。本刀：先 `isConversationPairingHold`；leftover-looks-live KEEP leftover device/pending 行 + disconnected note，0 额外 list unaries。既有 KEEP 仍绿。未闸 Hub Connect / Test / Disconnect。未重做 Rotate/Pair/Revoke/Rename（D321/D323）。不发明 proto。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 D `connection-hub-leftover-looks-live` 已收 @2026-09-12（未提交） | leftover-looks-live：KEEP leftover 行 + disconnected note，0 额外 `listDevices`/`listPending`；既有 KEEP 仍绿。`connectionPreferencesPane.test.ts` | conversation / connection-hub | closed |
| D342 | P3 | **closed** `canSendSourcesGitChanges` / `canSendSourcesGitSummary` 只看 `connected`，无 `pairingHold`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）仍额外 `readGitChanges` / `readGitSummary`。Pairing KEEP 只在空 `tryLoadGitEntries` 后跑。本刀：`pairingHold` 对齐 D312 `isSourcesGitWriteLive`；Changes/Review refresh 先 `isConversationPairingHold` 再 `tryLoadGitEntries`；leftover-looks-live KEEP leftover 行 + pairing note，0 额外 reads；首拉 leftoverCount 0 仍 SCM；真断连仍 SCM。既有 KEEP（`connected && !pairingPending`）仍绿。未重做 Stage/Commit/Unstage（D312/D327）。未发明 WriteGitUnstage / proto。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 A `sources-git-read-leftover-looks-live` 已关仓 @2026-09-12 `cfc6d64ded2` / merge compile-client 0 | leftover-looks-live：KEEP leftover + pairing note，reads 停在 post-load；既有 KEEP 仍 `readCalls===1`；首拉 pairing 仍 SCM；真断连仍 SCM。`sourcesChangesList.test.ts` / `sourcesChangesGitRead.test.ts` / `sourcesReviewList.test.ts` | sources / git-read | closed |
| D343 | P3 | **closed** D342 已闸 Changes/Summary leftover-looks-live。`canSendSourcesGitFileDiff` / `tryReadSourcesGitFileDiff` 仍只看 `connected`，无 `pairingHold`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）开 leftover 行仍额外 `readGitFileDiff`。既有 KEEP 测 `connected && !pairingPending` 或 `roster.setEngineConnected(false)`，不覆盖 leftover-looks-live。本刀：`pairingHold` 对齐 D342 `isSourcesGitWriteLive`；Changes/Review `readGitFileDiff` 传 `isConversationPairingHold`；leftover-looks-live KEEP leftover 行 + pairing note，开行 / 强制 FileDiff 0 额外 unary；首拉 leftoverCount 0 仍 SCM；真断连仍 SCM。既有 KEEP 仍绿。未重做 Stage/Commit/Unstage（D312/D327）。未重做 Changes/Summary KEEP（D342）。未发明 WriteGitUnstage / proto。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 A `sources-filediff-leftover-looks-live` 已关仓 @2026-09-12 `83fe233e56f` / merge compile-client 0 | leftover-looks-live：KEEP leftover + pairing note，开 leftover 行 `readGitFileDiff===0`；既有 KEEP 仍绿；首拉 pairing 仍 SCM；真断连仍 SCM。`sourcesChangesGitRead.test.ts` / `sourcesChangesList.test.ts` / `sourcesReviewList.test.ts` | sources / git-read-filediff | closed |
| D344 | P3 | **closed** D332/D333 已闸 leftover-looks-live create/switch/activate tree-refresh。roster catalog `refreshEngineCatalog` / `doRefreshEngineCatalog` 仍无 pairing-hold KEEP：`onUaConnectionChanged` 与 `setEngineConnected(true)` 在 `isEngineConnected()===true` 时额外 `listSessions`，leftover 被重列成 live，空表仍 `ensureEngineSession`。既有 KEEP 测 stub `connected && !pairingPending` 或 `setEngineConnected(false)`，不覆盖 leftover-looks-live。本刀：`refreshEngineCatalog` 先 `isConversationPairingHold` 则 no-op；leftover-looks-live → 0 额外 `listSessions`，leftover 行仍在，0 `ensureEngineSession`。未重做 D332/D333 create/switch/activate。未改 `bindLiveTreeObservationLease`。不发明 proto。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 B `roster-catalog-leftover-looks-live` 已关仓 @2026-09-12 `86fbf1fe379` / merge compile-client 0 | leftover-looks-live：0 额外 `listSessions` + leftover 行 + 0 `ensureEngineSession`；D332/D333 仍绿。`conversationEngineRosterService.test.ts` 106 passing | conversation / roster-catalog | closed |
| D345 | P3 | **closed** D290 已闸 pairing-hold leftover KEEP；D334/D335 已闸 leftover-looks-live `refreshAgentTree()` 0 unary + Refresh chrome。`refreshFromLease` KEEP 仍只在 `!engineReady`。leftover-looks-live（`isEngineConnected()===true` + pairingPending + phase connected）→ engineReady true → leftover 画成 live（`setLiveAgentIds`、无 stale-note）。既有 KEEP 测 `roster.setEngineConnected(false)`，不覆盖 leftover-looks-live。本刀：先 `isConversationPairingHold`（对齐 Team D336）；leftover-looks-live → leftover Hierarchy/Activity 行 + stale-note，`setLiveAgentIds` 清空，0 额外 unary。既有 KEEP（`setEngineConnected(false)`）仍绿。未改 D334 unary / D335 Refresh chrome / pending-chip CSS / Sessions New / Team `refreshTeamData`。不发明 proto。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 D `navigator-agents-lease-leftover-looks-live` 已关仓 @2026-09-12 `531eb6a04ec` / merge compile-client 0 | leftover-looks-live：leftover Hierarchy/Activity 行 + stale-note，`setLiveAgentIds` 清空，0 额外 unary；既有 KEEP 仍绿。`navigatorAgentsSubviews.test.ts` 40 passing | navigator / agents | closed |
| D346 | P3 | **closed** D280/D309 已闸 pairing leftover KEEP + Restore/Delete。Snapshots overlay `refresh` KEEP 只在 `paintedLiveSnapshots && isConversationPairingHold`，再 `if (!connected)`。leftover-looks-live 首拉（`isEngineConnected()===true` + pairingPending，无 leftover）跳过两闸仍 `listSnapshots`。既有 KEEP 测 leftover WITH leftover 或 stub `connected && !pairingPending`，不覆盖首拉 leftover-looks-live。本刀：先 `isConversationPairingHold`（对齐 History D338）；首拉 leftover-looks-live → empty / disconnected note，0 `listSnapshots`；leftover WITH leftover 仍 KEEP + 0 额外 list。既有写闸测仍绿。未重做 Restore/Delete（D309）。未改 History（D338）。不发明 proto。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 A `snapshots-first-pull-leftover-looks-live` 已关仓 @2026-09-12 `98f90c8e18b` / merge compile-client 0 | leftover-looks-live 首拉：empty + disconnected note，0 `listSnapshots`；leftover WITH leftover 仍 KEEP + 0 额外 list。`conversationEngineSnapshotsList.test.ts` 38 passing | conversation / snapshots-overlay | closed |
| D347 | P3 | **closed** D281 KEEP leftover 行；D314/D319 已闸 Write/Clear/Read。`refresh()` KEEP 只在 `entries.length > 0 && isConversationPairingHold`，随后 `if (!isEngineConnected())`。leftover-looks-live 首拉（`isEngineConnected()===true` + pairingPending，无 leftover）跳过 KEEP，仍 `listClipboard`。`canSendEngineClipboardListRequest` 仍只看 connected。本刀：pairing-hold-first（对齐 MCP Runtime D337 / Composer D339）；leftover-looks-live 首拉 → empty、0 `listClipboard`；leftover WITH leftover 仍 KEEP + 0 extra list。既有 write-gate 仍绿。未重做 Write/Clear/Read（D314/D319）。未改 Snapshots / MCP catalog。不发明 proto。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 B `clipboard-first-pull-leftover-looks-live` 已关仓 @2026-09-12 `7b8ebb00f0e` / merge compile-client 0 | leftover-looks-live 首拉：empty + 0 `listClipboard`；leftover WITH leftover 仍 KEEP + 0 extra list；write-gate 仍绿。`engineClipboardSection.test.ts` / `engineClipboardList.test.ts` 合计 34 passing | conversation / clipboard | closed |
| D348 | P3 | **closed** D313 已闸 MCP Definitions Add/Update/Remove/Enable leftover-looks-live 写。`keepLeftoverCatalogForPairingHold` 仍要求 `hadLiveCatalog`；leftover-looks-live first-pull（`isEngineConnected()===true` + pairingPending，无 leftover）KEEP false，connected===true，额外 `listMcpServers`。Runtime D337 已 pairing-hold-first。本刀：先 `isConversationPairingHold`（对齐 D337）；first-pull leftover-looks-live → empty、0 `listMcpServers`；leftover WITH leftover 仍 KEEP + 0 extra list。未重做 Add/Update/Remove/Enable（D313）。未改 MCP Runtime（D337）/ Snapshots / Clipboard。不发明 proto。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 D `mcp-catalog-first-pull-leftover-looks-live` 已关仓 @2026-09-12 `366bbcb7af1` / merge compile-client 0 | leftover-looks-live first-pull：empty + 0 `listMcpServers`；leftover WITH leftover 仍 KEEP + 0 extra list；既有写闸仍绿。`engineCatalogSections.test.ts` | conversation / engine-catalog-mcp | closed |
| D349 | P3 | **closed** D313 已闸 Skills/Tools/Agents/Plugins leftover-looks-live 写；D348 已闸 MCP Definitions first-pull。四节 `refresh()` KEEP 仍先 `keepLeftoverCatalogForPairingHold(hadLiveCatalog)` 再 `if (!connected)`。leftover-looks-live first-pull（`isEngineConnected()===true` + pairingPending，无 leftover）KEEP false，connected===true，额外 `listSkills` / `listTools` / `listAgentProfiles` / `listPlugins`。本刀：pairing-hold-first（对齐 D348 MCP / D337 Runtime）；first-pull leftover-looks-live → empty / disconnected，0 extra list；leftover WITH leftover 仍 KEEP + 0 extra list。未重做 catalog 写（D313）。未改 MCP Definitions（D348）/ Runtime / Triggers / Context / Snapshots / Clipboard。不发明 proto。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 A `catalog-first-pull-leftover-looks-live` 已关仓 @2026-09-12 `557cc615356` / merge compile-client 0 | leftover-looks-live first-pull：empty + 0 `listSkills`/`listTools`/`listAgentProfiles`/`listPlugins`；leftover WITH leftover 仍 KEEP + 0 extra list；既有写闸仍绿。`engineCatalogSections.test.ts` | conversation / engine-catalog-list | closed |
| D350 | P3 | **closed** D281 KEEP leftover 行；D311 已闸 Fire/Enable/Disable/Delete/Upsert。`refresh()` KEEP 只在 `triggers.length > 0 && isConversationPairingHold`，随后 `if (!isEngineConnected())`。leftover-looks-live 首拉（`isEngineConnected()===true` + pairingPending，无 leftover）跳过 KEEP，仍 `listTriggers`。`canSendEngineTriggerListRequest` 仍只看 connected。本刀：pairing-hold-first（对齐 Clipboard D347 / History D338）；leftover-looks-live 首拉 → empty、0 `listTriggers`；leftover WITH leftover 仍 KEEP + 0 extra list。既有 write-gate 仍绿。未重做 Fire/Enable/Disable/Delete/Upsert（D311）。未改 Skills/Tools/Agents/Plugins/MCP/Context/Snapshots。不发明 proto。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 B `triggers-first-pull-leftover-looks-live` 已关仓 @2026-09-12 `99e64bf258a` / merge compile-client 0 | leftover-looks-live 首拉：empty + 0 `listTriggers`；leftover WITH leftover 仍 KEEP + 0 extra list；write-gate 仍绿。`engineTriggersSection.test.ts` 31 / `engineTriggerList.test.ts` 6；合计 38 passing | conversation / triggers | closed |
| D351 | P3 | **closed** D316 已闸 pairing leftover KEEP + Read。Context Variables `refresh` KEEP 只在 `rows.length > 0 && isConversationPairingHold`，再 `if (!isEngineConnected())`。leftover-looks-live 首拉（`isEngineConnected()===true` + pairingPending，无 leftover）跳过两闸仍 `listContextVariable`。`canSendEngineContextVariableListRequest` 仍只看 connected。既有 KEEP 测 leftover WITH leftover 或 stub `connected && !pairingPending`，不覆盖首拉 leftover-looks-live。本刀：pairing-hold-first（对齐 Clipboard D347）；leftover-looks-live 首拉 → empty / disconnected，0 `listContextVariable`；leftover WITH leftover 仍 KEEP + 0 extra list。既有写闸测仍绿。未重做 Context writes / Read（D316）。未改 Skills/Tools/Agents/Plugins/Triggers/MCP/Snapshots。不发明 proto。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 D `context-first-pull-leftover-looks-live` 已关仓 @2026-09-12 `cc62fe37eac` / merge compile-client 0 | leftover-looks-live 首拉：empty + disconnected，0 `listContextVariable`；leftover WITH leftover 仍 KEEP + 0 extra list；write-gate 仍绿。`engineContextVariableSection.test.ts` / `engineContextVariableList.test.ts` 合计 18 passing | conversation / context-variables | closed |
| D352 | P3 | **closed** D349 已闸 Skills/Tools/Agents/Plugins `refresh()` / `listAgentProfiles` leftover-looks-live。Agents 详情 Tools 页 `ensureAgentToolsLoaded` 仍只 `if (!isEngineConnected()) return` 再 `listTools()`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）+ leftover Agents catalog + 开 Tools 页且 `agentTools` 空 → 跳过 return，额外 `listTools`。既有 KEEP 测 stub `connected && !pairingPending`，不覆盖 leftover-looks-live Tools 页。本刀：pairing-hold-first（对齐 D349 refresh）；leftover-looks-live + leftover catalog + 开 Tools（空 `agentTools`）→ 0 extra `listTools`，leftover tools KEEP 或 empty no-op；首拉 pairing 无 leftover tools 仍 empty；接通 leftover 仍 load。未重做 D349 `listAgentProfiles` / D313 写。不发明 proto。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 A `agents-tools-tab-leftover-looks-live` 已关仓 @2026-09-12 `736563e4ff3` / merge compile-client 0 | leftover-looks-live Tools 页：connected===true + pairingPending + leftover catalog + 开 tools（空 `agentTools`）→ 0 extra `listTools`；接通 leftover 仍 load；D349 仍绿。`engineCatalogSections.test.ts` | conversation / engine-agents-tools | closed |
| D353 | P3 | **closed** D289 保 leftover lease。阅读列 `requestDetail` 包装只要 `lease.requestDetail` 就转发 `sessionView.requestDetail`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）+ leftover lease 展开 leftover 工具仍额外 unary。本刀：pairing-hold-first 闸阅读列包装；leftover-looks-live + leftover lease → 缓存 leftover body / `{ ok:false, reason:'unavailable' }`，0 extra unary。接通 leftover 仍 fetch。首拉 pairing 无 lease 本就无 `requestDetail`。未改 `shouldShowReadingColumnLiveChrome` / `acquireLease` / `bindLiveTreeObservationLease`。不发明 proto。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 B `lens-detail-fetch-leftover-looks-live` 已关仓 @2026-09-12 `5c5f8d7c5b2` / merge compile-client 0 | leftover-looks-live + leftover lease：0 `requestDetail`；缓存 body 仍回；接通 leftover 仍 fetch。`conversationLensDisposeGate.test.ts` / `conversationTrajectoryDetailInspector.test.ts` | conversation / lens-detail-fetch | closed |
| D354 | P3 | **closed** Engine Preferences `updateDisconnectedBanner` 用 `disconnected = !isEngineConnected()`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）把断连横幅藏掉（看起来像 live）。既有测只锁真断连 / Web unsupported，不覆盖 leftover-looks-live。本刀：pairing-hold-first KEEP-chrome（`isConversationPairingHold \|\| !isEngineConnected()`）→ leftover-looks-live 仍画断连横幅（同 pairing-hold）；真接通无 pairing 仍藏；真断连仍画。未闸 Hub Connect/Test/Disconnect。未改 reading-column / Agents tools / Rules/Hooks / proto。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 D `engine-preferences-banner-leftover-looks-live` 已关仓 @2026-09-12 `7f6911cc086` / merge compile-client 0 | leftover-looks-live：`isEngineConnected()===true` + pairingPending 横幅仍可见（同 pairing-hold）；真接通无 pairing 仍藏；真断连仍画。`enginePreferencesPane.test.ts` 14 passing | conversation / engine-preferences-banner | closed |
| D355 | P3 | **closed** D304 KEEP leftover `· Running` / `· Loading`。`shouldShowReadingColumnLiveChrome` 仍 `if (isEngineConnected()) return true`。leftover-looks-live（`isEngineConnected()===true` + pairingPending）首拉（无 leftover lease/turns）假造 live chrome。既有 D304 KEEP stub `isEngineConnected: () => false` 或 `connected && !pairingPending`，不覆盖 leftover-looks-live。本刀：pairing-hold-first；`isConversationPairingHold` 不走 connected 短路，落入 leftover lease / `getTurns`（D304 体）。leftover-looks-live 首拉 → false；WITH leftover lease/turns → true（KEEP D304）；真接通无 pairing 仍 true；真断连仍 false。未改 `requestReadingColumnDetail`（D353）/ `bindLiveTreeObservationLease` / `acquireLease`。不发明 proto / pending-chip / Sessions New。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 A `reading-column-live-chrome-leftover-looks-live` 已关仓 @2026-09-12 `fd314598696` / merge compile-client 0 | leftover-looks-live 首拉：connected===true + pairingPending + 无 leftover → `shouldShowReadingColumnLiveChrome === false`；WITH leftover lease/turns → true；D304 KEEP 仍绿。`conversationLensDisposeGate.test.ts` | conversation / lens-live-chrome | closed |
| D356 | P3 | **closed** Engine Rules `render()` 只 `if (!this.connection.isEngineConnected())` 画 disconnected。leftover-looks-live（`isEngineConnected()===true` + pairingPending / `isConversationPairingHold`）跳过该闸，画 capability / unsupported（看起来像 live）。无 unary。本刀：pairing-hold-first KEEP-chrome（`isConversationPairingHold \|\| !isEngineConnected()`）→ leftover-looks-live 仍画 disconnected（同 pairing-hold / 真断连）。真接通无 pairing 仍走 capability/unsupported。未发明 list RPC / proto。未闸 Hub Connect/Test/Disconnect。未改 Hooks（D357）/ reading-column（D355）。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 B `rules-render-leftover-looks-live` 已关仓 @2026-09-12 `394f631a636` / merge compile-client 0 | leftover-looks-live：`isEngineConnected()===true` + pairingPending 仍 disconnected（非 capability/unsupported-as-live）；真接通无 pairing 仍 capability/unsupported；真断连仍 disconnected。`engineRulesSection.test.ts` | conversation / engine-rules | closed |
| D357 | P3 | **closed** `EngineHooksSection.render()` 只 `if (!isEngineConnected())` 画 disconnected。leftover-looks-live（`isEngineConnected()===true` + pairingPending / `isConversationPairingHold`）跳过闸，画 capability / unsupported（看起来像 live）。无 unary。本刀：pairing-hold-first KEEP-chrome（`isConversationPairingHold \|\| !isEngineConnected()`）→ leftover-looks-live 仍画 disconnected；真接通无 pairing 仍走 capability/unsupported；真断连仍 disconnected。未发明 list RPC / proto。未闸 Hub Connect/Test/Disconnect。未改 Rules（D356）/ reading-column（D355）。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 D `hooks-render-leftover-looks-live` 已关仓 @2026-09-12 `7ab30be22f2` / merge compile-client 0 | leftover-looks-live：`isEngineConnected()===true` + pairingPending 仍 disconnected；真接通无 pairing 仍 capability/unsupported；真断连仍 disconnected。`engineHooksSection.test.ts` | conversation / engine-hooks | closed |
| D358 | P3 | **closed** D352 已闸 Agents Tools 页 leftover-looks-live 0 extra `listTools`。`renderAgentTools()` empty 仍 `isEngineConnected() ? 'empty' : 'disconnected'`。leftover-looks-live（`isEngineConnected()===true` + pairingPending / `isConversationPairingHold`）+ empty `agentTools` 仍画 empty-as-live（「No engine tools…」）。本刀：pairing-hold-first KEEP-chrome；leftover-looks-live + empty tools → `disconnected`（同 pairing-hold / 真断连）+ Open Connection。真接通无 pairing 仍 `empty`。未改 `ensureAgentToolsLoaded`（D352）/ D349 `listAgentProfiles` / D313 写。不发明 proto / pending-chip / Sessions New / Hub Connect。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 A `agents-tools-empty-keep-chrome-leftover-looks-live` 已关仓 @2026-09-12 `710fb4ea6bf` / merge compile-client 0 | leftover-looks-live：`isEngineConnected()===true` + pairingPending + empty tools 仍 disconnected + Open Connection（非 empty-as-live）；真接通无 pairing 仍 empty；真断连仍 disconnected。`engineCatalogSections.test.ts` | conversation / engine-agents-tools | closed |
| D359 | P3 | **closed** D340 已 pairing-hold-first `refreshModels`（0 extra `listModels`）。`renderProviderGroup()` 仍 `connected = isEngineConnected()` 再 `resolveProviderMode(connected, entry.support)`。leftover-looks-live（`isEngineConnected()===true` + pairingPending / `isConversationPairingHold`）把 Provider 能力 chrome 画成 capability / unsupported-as-live，不是 disconnected。既有 KEEP stub `connected && !pairingPending` 不覆盖 looks-live。本刀：pairing-hold-first KEEP-chrome（`isConversationPairingHold \|\| !isEngineConnected()` 作 provider mode 的 connected 旗）→ leftover-looks-live 仍 disconnected；真接通无 pairing 仍 capability/unsupported fold；真断连仍 disconnected。未重写 `refreshModels`（D340）。未发明 provider list/CRUD RPC / proto。未闸 Hub Connect/Test/Disconnect。未改 Overview / Rules / Hooks / Agents。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 B `engine-provider-group-leftover-looks-live` 已关仓 @2026-09-12 `a8e8f42c74e` / merge compile-client 0 | leftover-looks-live：`isEngineConnected()===true` + pairingPending Provider 组仍 disconnected（非 capability/unsupported-as-live）；真接通无 pairing 仍 capability/unsupported fold；真断连仍 disconnected。`engineProviderModelSection.test.ts` | conversation / engine-provider-chrome | closed |
| D360 | P3 | **closed** Projects `rebuildTree` 用 `engineConnected = isEngineConnected()` 喂 `buildNavigatorProjectsTree`。KEEP leftover 仅 `isConversationPairingHold && hasProjectsLeftoverRows()` 早退。leftover-looks-live first-pull（`isEngineConnected()===true` + pairingPending / `isConversationPairingHold`，无 leftover 行）跳过 KEEP，画 **live engine projects chrome**。既有 KEEP stub `connected && !pairingPending` 或 `isEngineConnected: () => false` 不覆盖 first-pull。本刀：pairing-hold-first 传给 `buildNavigatorProjectsTree` 的 `engineConnected`（`!isConversationPairingHold && isEngineConnected()`）→ leftover-looks-live first-pull 同断连 / pairing-hold 无 leftover（空树 + welcome）；KEEP leftover 有行仍早退；真接通无 pairing 仍 live 树；真断连仍 disconnect 树。未改 Agents `refreshFromLease`（D345）/ Team `refreshTeamData`（D336）/ pending-chip CSS / Sessions New。未发明 proto。未闸 Hub Connect。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 D `navigator-projects-leftover-looks-live-first-pull` 已关仓 @2026-09-12 `a9fd2cffc08` / merge compile-client 0 | leftover-looks-live first-pull：`isEngineConnected()===true` + pairingPending 仍空树 + welcome（非 live engine chrome）；KEEP leftover 有行仍早退；真接通无 pairing 仍 live 树；真断连仍 disconnect 树。`navigatorProjectsList.test.ts` | navigator / projects | closed |
| D361 | P3 | **closed** Review Attribution 构造 `if (isEngineConnected()) everConnected=true`。leftover-looks-live first-pull（`isEngineConnected()===true` + pairingPending / `isConversationPairingHold`，无 prior live、无 leftover attribution records）把 `everConnected` 标成真接通，可把 `getAttributionHeaderSuffix` / `isAttributionEnabled` 画成 live（「Attribution from the connected engine.」）。既有 KEEP stub `connected && !pairingPending` 或 `setEngineConnected(false)` 不覆盖 leftover-looks-live。本刀：pairing-hold-first；leftover-looks-live first-pull 不单靠 `isEngineConnected()` 设 `everConnected`。已有 everConnected（prior live / mutations）KEEP；真接通无 pairing 仍设；真断连不发明；pairing-hold 不清除。文件 mutation 订阅设 everConnected 为真突变 KEEP。未发明 proto / WriteGitUnstage / Hub Connect/Test/Disconnect/Forget。未改 Sources git-read/write（D312/D327/D342/D343）。未改 pending-chip CSS / Sessions New。未关 D8/D16/D147 / D26 / D31 F4。未宣称 leftover-honesty 程序完成。未复用 D312–D360。 | 工位 A `sources-review-attribution leftover-looks-live everConnected KEEP-chrome` 已关仓 @2026-09-12 `96f645a8bb5` / merge compile-client 0 | leftover-looks-live first-pull：`isEngineConnected()===true` + pairingPending → `isAttributionEnabled()===false` 且无 connected-engine header（无 leftover records）；leftover records + prior everConnected KEEP；真接通无 pairing + records 仍可 enable。`sourcesReviewAttribution.test.ts` | sources / review-attribution | closed |
| D362 | P3 | **closed** D341 已 pairing-hold-first `refreshEngineDevices` / `refreshEnginePending`。`applyDesktopConnectionControlVisibility()` 仍 `enginePairSeat = isEngineConnected()`。leftover-looks-live（`isEngineConnected()===true` + pairingPending / `isConversationPairingHold`）+ hub signedOut 把 Hub devices 节画成 live engine pair-seat。既有 KEEP stub `connected && !pairingPending` 不覆盖 leftover-looks-live。本刀：pairing-hold-first 仅 `enginePairSeat`（`!(isConversationPairingHold \|\| !isEngineConnected())`）→ leftover-looks-live + signedOut 节隐藏（同真断连）；真接通无 pairing 仍显示；hubSignedIn 仍显示。未重写 D341 list KEEP。未闸 Hub Connect / Test / Disconnect / Forget / PairApprove / PairReject / login / addDirectAddress。不发明 proto / pending-chip / Sessions New。未关 D8/D16/D147。未宣称 leftover-honesty 程序完成。 | 工位 B `connection-pane-engine-pair-seat-leftover-looks-live` 已关仓 @2026-09-12 `17a1205bd54` / merge compile-client 0 | leftover-looks-live + signedOut：`hubDevicesSection` hidden；真接通无 pairing 仍 shown；真断连 + signedOut hidden；hubSignedIn 仍 shown。`connectionPreferencesPane.test.ts` | conversation / connection-hub-devices | closed |
| D363 | P3 | **closed** D360 已 pairing-hold-first 传给 `buildNavigatorProjectsTree` / welcome 的 `engineConnected`。`onDidChangeEngineConnection(true)` 仍无条件 `wasEverConnected=true`。leftover-looks-live（`isEngineConnected()===true` + pairingPending / `isConversationPairingHold`）首拉无 leftover 行时事件把 `wasEverConnected` 翻真，再经 `engineConnected \|\| wasEverConnected` 画 **ever-connected / leftover-as-live engine chrome**。D360 `rebuildTree` `if (engineConnected) wasEverConnected=true` 已 pairing-hold-first，洞在事件处理器。本刀：pairing-hold-first 设 `wasEverConnected`（`connected && !isConversationPairingHold`）；leftover-looks-live 首拉事件不翻旗；KEEP leftover 有行仍早退（D360）；真接通无 pairing 仍翻旗并画 live 树；真断连不发明该旗；此前真接通后 `wasEverConnected` 保持为 KEEP。未改 Agents `refreshFromLease` / Team `refreshTeamData`。未发明 proto / pending-chip / Sessions New / Hub Connect。未关 D8/D16/D147/D26/D31 F4。未宣称 leftover-honesty 程序完成。未复用 D312–D360。未撤 D360 pairing-hold `engineConnected`。 | 工位 D `navigator-projects-waseverconnected-leftover-looks-live` 已关仓 @2026-09-12 `1f5e3268827` / merge compile-client 0 | leftover-looks-live first-pull：`onDidChangeEngineConnection(true)` 后仍空树 + welcome（`wasEverConnected` 仍 false，非 ever-connected live chrome）；KEEP leftover 有行仍早退；真接通无 pairing 仍 live 树。`navigatorProjectsList.test.ts` | navigator / projects | closed |
| D364 | P3 | **closed** D349 已闸 Agents `listAgentProfiles`；D352 已闸 Tools `listTools`；D358 已闸 empty KEEP-chrome。`loadAgentsEditorForSelection` 仍 `if (!canShowCatalogRows \|\| !isEngineConnected() \|\| !selectedProfile)`。leftover-looks-live（`isEngineConnected()===true` + pairingPending / `isConversationPairingHold`）+ leftover catalog + 开 Instructions 仍当 live，可额外 `saveAgentProfile`。post-await recheck 也不看 pairing-hold。本刀：pairing-hold-first 在 `saveAgentProfile` 前与 post-await recheck；leftover-looks-live → 0 extra `saveAgentProfile`，KEEP leftover markdown / disconnected。真接通无 pairing 仍 load。未重写 D349/D352/D358/D313 写。不发明 proto。未关 D8/D16/D147/D26/D31。未复用 D312–D363。未宣称 leftover-honesty 程序完成。 | 工位 A `agents-instructions leftover-looks-live` 已关仓 @2026-09-12 `5915f7d2aec` / merge compile-client 0 | leftover-looks-live + leftover catalog + 开 Instructions：0 extra `saveAgentProfile`，leftover markdown / disconnected KEEP；真接通无 pairing 仍 load。`engineCatalogSections.test.ts` | conversation / engine-agents-instructions | closed |
| D365 | P3 | **closed** D346 已 pairing-hold-first 于 Snapshots `refresh` **入口**（首拉 leftover-looks-live 0 extra list）。`await listSnapshots` 之后仍只 `!isEngineConnected()`。leftover-looks-live in-flight（接通后开 list，resolve 前翻 `pairingPending` 且 `isEngineConnected()===true`）仍 `paintSnapshots` 当 live。本刀：post-await pairing-hold-first（`isConversationPairingHold \|\| !isEngineConnected()` → `applyDisconnectedRefresh`）→ leftover KEEP + disconnected，不画 in-flight live。未改 D346 入口 KEEP / D309 Restore/Delete。未发明 proto。未关 D8/D16/D147。未复用 D312–D363。未宣称 leftover-honesty 程序完成。 | 工位 B `snapshots-post-await leftover-looks-live KEEP-chrome` 已关仓 @2026-09-12 `3652a575281` / merge compile-client 0 | 接通开 list → resolve 前 leftover-looks-live：不画 in-flight live；leftover 行仍在 + disconnected；Restore/Delete 仍 disabled。`conversationEngineSnapshotsList.test.ts` | conversation / snapshots-overlay | closed |
| D366 | P3 | **closed** D338 已 pairing-hold-first 于 History `refresh` **入口** KEEP leftover-looks-live（0 额外 getHistory）。`await getHistory` 后无 pairing-hold / connected 再检，in-flight `getHistory` 期间 leftover-looks-live（`isEngineConnected()===true` + pairingPending / `isConversationPairingHold`）仍 `paintEnvelopes` 新鲜 live 行。本刀：`paintEnvelopes` 前 pairing-hold-first 再检 → `applyDisconnectedRefresh()` KEEP leftover + disconnected note，不画新鲜 live。未改 D338 入口 KEEP。不发明 proto。未关 D8/D16/D147。未复用 D312–D363。未宣称 leftover-honesty 程序完成。 | 工位 D `history-post-await leftover-looks-live KEEP-chrome` 已关仓 @2026-09-12 `661e5ba9f3c` / merge compile-client 0 | in-flight getHistory + looks-live flip：leftover KEEP + disconnected note，无新鲜 live 行；D338 入口 KEEP 仍绿。`conversationEngineHistoryList.test.ts` | conversation / history | closed |
| D367 | P3 | **closed** D342 已 pairing-hold-first 于 Changes/Review `refresh` **入口**（leftover-looks-live 0 extra git-read）。`await tryLoadGitEntries()` 后 `seq !== refreshSeq` 即 `hasSourcesGitReadEntries(loaded)` 当 live。in-flight leftover-looks-live（`isEngineConnected()===true` + pairingPending / `isConversationPairingHold`）仍画新鲜 live engine git 行（无 pairing note）。本刀：seq 再检后 pairing-hold-first → KEEP `lastGoodEntries` / leftover + `gitReadPairingHold=true`，不把 `loaded` 当 live。未改 D342 入口 / D312/D327/D343 writes/filediff。不发明 proto / WriteGitUnstage。未关 D8/D16/D147/D26/D31。未复用 D312–D366。未宣称 leftover-honesty 程序完成。 | 工位 A `sources-post-await leftover-looks-live KEEP` 已关仓 @2026-09-12 `e2def431753` / merge compile-client 0 | in-flight `readGitChanges` + leftover-looks-live flip：leftover KEEP + pairing note，无新鲜 live 行；D342 入口 KEEP 仍绿。`sourcesChangesList.test.ts` / `sourcesReviewList.test.ts` | sources / git-read | closed |
| D368 | P3 | **closed** D337 已 pairing-hold-first 于 MCP Runtime `refresh()` 入口 / post-await `getMcpServerStatuses`。`loadTools` 无 pairing-hold、无 post-await 再检。leftover-looks-live（`isEngineConnected()===true` + pairingPending / `isConversationPairingHold`）仍 `getMcpServerTools` / `renderTools()` 当 live。本刀：`loadTools` 入口 pairing-hold-first（0 extra `getMcpServerTools`；KEEP leftover + disconnected）；await 后再检（`isConversationPairingHold \|\| !isEngineConnected()` → KEEP leftover，不画 live）。未改 D337 `refresh` / D313 catalog 写。不发明 proto。未关 D8/D16/D147。未复用 D312–D366。未宣称 leftover-honesty 程序完成。 | 工位 B `mcp-runtime-tools leftover-looks-live` 已关仓 @2026-09-12 `9d64f04502c` / merge compile-client 0 | leftover-looks-live：0 extra `getMcpServerTools`，KEEP leftover + disconnected；in-flight resolve after looks-live flip：不画 live / KEEP leftover。`engineMcpRuntimePanel.test.ts` | conversation / mcp-runtime | closed |
| D369 | P3 | **closed** D336 已 pairing-hold-first 于 Team `refreshTeamData` **入口** KEEP leftover-looks-live（0 额外 unary）。`await memberStatus` / `taskList` 后仍只 `!isEngineConnected() \|\| phase !== connected`，随后 `setTeamSnapshotNote(undefined)`。leftover-looks-live in-flight（接通后开 refresh，resolve 前翻 `pairingPending` 且 `isEngineConnected()===true` + phase connected）清 stale-note 并把新鲜 members/tasks 画成 live。本刀：post-await pairing-hold-first（`isConversationPairingHold \|\| !isEngineConnected() \|\| phase !== connected`）→ KEEP leftover 行 + `NAVIGATOR_STALE_SNAPSHOT_COPY`，不画 in-flight live。未改 D336 入口 KEEP / Agents `refreshFromLease` / Projects。不发明 proto。未关 D8/D16/D147。未复用 D312–D366。未宣称 leftover-honesty 程序完成。 | 工位 D `navigator-team-post-await leftover-looks-live KEEP` 已关仓 @2026-09-12 `17a1ce225f3` / merge compile-client 0 | 接通开 refresh → resolve 前 leftover-looks-live：leftover KEEP + stale-note，无新鲜 live；D336 入口 KEEP 仍绿。`navigatorTeamSubviews.test.ts` | navigator / team | closed |
| D370 | P3 | **closed** Inbox Enqueue 入口已 pairing-hold-first；Goal 已在 `await quickInputService.input` 后再检 pairing。`onEnqueueClicked` 在 prompt resolve / cancel 后直写 `enqueueMessageQueueItem`。leftover-looks-live in-flight（接通后开 Enqueue，resolve 前翻 `pairingPending` 且 `isEngineConnected()===true`）仍入队。本刀：post-await pairing-hold-first（`isConversationPairingHold` → return，0 enqueue）+ 同入口 `!isEngineConnected()`（断连+history → `engine_disconnected`，0 enqueue）。未改 Enqueue chrome / pending-chip CSS / Sessions New / Goal。不发明 proto / Hub Connect / D8/D147。未复用 D312–D369。未宣称 leftover-honesty 程序完成。 | 工位 A `inbox-enqueue-post-await leftover-looks-live` 已关仓 @2026-09-12 `8ce500447aa` / merge compile-client 0 | 接通开 Enqueue → resolve 前 leftover-looks-live：0 extra enqueue，leftover KEEP；入口 disable-when-pairingPending 仍绿。`conversationInboxOverlay.test.ts` | conversation / inbox | closed |
| D371 | P3 | **closed** D310 已 pairing-hold-first 于 Create Snapshot **入口**（`shouldHoldCreateSnapshotWrite` 再 `canCreateEngineSnapshot`）。`await quickInputService.input` 后无 pairing-hold 再检。leftover-looks-live in-flight（prompt 开着翻 pairingPending 且 `isEngineConnected()===true`）仍 `roster.createSnapshot`。本刀：prompt resolve（cancel 已 return）后再 pairing-hold-first；`shouldHoldCreateSnapshotWrite` → `notifyCreateSnapshotUnavailable`、0 `createSnapshot`。抽出 `tryCreateSnapshotAfterPrompt` 以便 helper-only 测。未改 D309 Restore/Delete、Snapshots list（D365/D346）、Hub Connect、proto、D8/D147。未复用 D312–D370。未宣称 leftover-honesty 程序完成。 | 工位 B `create-snapshot-post-await leftover-looks-live` 已关仓 @2026-09-12 `34660517c96` / merge compile-client 0 | prompt 后 leftover-looks-live：0 createSnapshot + unavailable notice；`shouldHoldCreateSnapshotWrite(looksLive)===true` 仍绿。`conversationCreateSnapshotActions.test.ts` | conversation / create-snapshot | closed |
| D372 | P3 | **closed** D349 已 pairing-hold-first 于 Plugins `refresh()` 入口 / post-await `listPlugins`。`loadInfo` 无 pairing-hold、无 post-await 再检。leftover-looks-live（`isEngineConnected()===true` + pairingPending / `isConversationPairingHold`）仍 `getPluginInfo` / 把 `result.hooks` 画成 live。本刀：`loadInfo` 入口 pairing-hold-first（0 extra `getPluginInfo`；KEEP leftover hooks + disconnected）；await 后再检（`isConversationPairingHold \|\| !isEngineConnected()` → KEEP leftover，不画 live）。未改 D349 `refresh` KEEP / D313 catalog 写 / Skills `loadSkillBody` / Tools `getToolInfo`。不发明 proto。未关 D8/D16/D147。未复用 D312–D371。未宣称 leftover-honesty 程序完成。 | 工位 D `plugins-loadInfo leftover-looks-live` 已关仓 @2026-09-12 `d7781b61708` / merge compile-client 0 | leftover-looks-live：0 extra `getPluginInfo`，KEEP leftover hooks + disconnected；in-flight resolve after looks-live flip：不画 live / KEEP leftover。`enginePluginsSection.test.ts` | conversation / plugins | closed |
| D373 | P3 | **closed** Inbox KEEP-chrome 仍把 leftover-looks-live 当 live。`isEngineQueueUnlisted` = `isEngineConnected() \|\| hasEngineConnectionHistory()` → leftover-looks-live 走 live「Queue not listed」；`isGenerating` = `isEngineConnected() && streaming` → leftover-looks-live 画 generating。本刀：pairing-hold-first 于两 chrome helper；leftover-looks-live 走 leftover/disconnected chrome（非 live queue-unlisted / 非 generating）。真断连+history 仍 unlisted。未改 D370 Enqueue/Goal 写闸、pending-chip CSS、Sessions New、Hub Connect。不发明 proto。未关 D8/D16/D147。未复用 D312–D372。未宣称 leftover-honesty 程序完成。 | 工位 A `inbox-keep-chrome leftover-looks-live` 已关仓 @2026-09-12 `7f755ed8dc7` / merge compile-client 0 | leftover-looks-live：`isEngineConnected()===true` + pairingPending 走 leftover/disconnected chrome（非 Queue not listed / 非 generating）；真接通无 pairing 仍 unlisted；真断连+history 仍 unlisted。`conversationInboxOverlay.test.ts` | conversation / inbox | closed |
| D374 | P3 | **closed** roster KEEP-chrome 仍把 leftover-looks-live（`isEngineConnected()===true` + pairingPending / `isConversationPairingHold`）当接通。`sessionSyncFromCachedProjection` 见 `isEngineConnected()` 即原样回 leftover live/syncing；`onUaConnectionChanged` / `setEngineConnected` 见接通即翻 `wasEverConnected`（含首拉）；`shouldAdvertiseClientWorkspaceTools` 见接通即 advertise。既有 getSessionSync 测走生产 pairing（`isEngineConnected()===false`），不是 leftover-looks-live。本刀：最小 helper `isEngineLiveChrome`（pairing-hold-first）；leftover-looks-live 降 leftover live/syncing 为 cached closed chrome；首拉 leftover-looks-live 不翻 `wasEverConnected`；pairing-hold 不 advertise。未改 `refreshEngineCatalog`/listSessions、写 mutators、`bindLiveTreeObservationLease`/acquireLease、Projects `wasEverConnected`（D363）、proto、D8/D147。未复用 D312–D373。未宣称 leftover-honesty 程序完成。 | 工位 B `roster leftover-looks-live KEEP-chrome` 已关仓 @2026-09-12 `96ac97f7f8a` / merge compile-client 0 | leftover-looks-live：`isEngineConnected()===true` + pairingPending 夹具下 getSessionSync 降 live/syncing 为 closed；首拉 `hasEngineConnectionHistory()===false`；`shouldAdvertiseClientWorkspaceTools()===false`。既有生产 pairing demote 仍绿。`conversationEngineRosterService.test.ts` | conversation / roster | closed |
| D375 | P3 | **closed** `sessionViewHost` `post` / `sendHeartbeatAck` / `writeChat` 只闸 `!isEngineConnected()`。leftover-looks-live（`isEngineConnected()===true` + `getConnectionSnapshot().pairingPending`）仍 accept post、发 `heartbeat_ack`、发 writeChat。本刀：三处 pairingPending first 再 `!isEngineConnected()`（platform/node，不 import workbench `isConversationPairingHold`）。`TestConnection` 可独立 `setPairingPending`（不 stub `connected && !pairingPending`）。未改 requestDetail / onEngineConnectionChanged / scheduleAgentTreeRefresh / acquireLease / bindLiveTreeObservationLease。不发明 proto / heartbeat `client_id`。未关 D8/D16/D147。未复用 D312–D374。未宣称 leftover-honesty 程序完成。 | 工位 D `host-write leftover-looks-live` 已关仓 @2026-09-12 `d5eb661d286` / merge compile-client 0 | leftover-looks-live：`isEngineConnected()===true` + pairingPending → post `not_authenticated`、0 heartbeat_ack、0 writeChat。`sessionViewHostWriteReceipt.test.ts` / `sessionViewHostHeartbeatAck.test.ts` | universeAgent / sessionViewHost | closed |
| D376 | P3 | **closed** D349 已 pairing-hold-first 于 Skills `refresh()` 入口 / post-await `listSkills`。`loadSkillBody` 入口只闸 `!isEngineConnected()` / `canShowCatalogRows`，post-await 再检也只 `!isEngineConnected()`。leftover-looks-live（`isEngineConnected()===true` + pairingPending / `isConversationPairingHold`）仍 extra `getSkillInfo` / 把 live body 画进 textarea。本刀：`loadSkillBody` 入口 pairing-hold-first（0 extra `getSkillInfo`；KEEP leftover body + disconnected）；await 后再检（`isConversationPairingHold \|\| !isEngineConnected()` → KEEP leftover，不画 live）。未改 D349 `refresh` KEEP / D313 catalog 写 / Plugins `loadInfo` / Tools `loadToolInfo`。不发明 proto。未关 D8/D16/D147。未复用 D312–D375。未宣称 leftover-honesty 程序完成。 | 工位 A `skills-loadSkillBody leftover-looks-live` 已关仓 @2026-09-12 `ffc233148ce` / merge compile-client 0 | leftover-looks-live：`isEngineConnected()===true` + pairingPending 下 0 extra `getSkillInfo`，KEEP leftover body + disconnected；in-flight resolve after looks-live flip：不画 live / KEEP leftover。既有生产 pairing 重选仍绿。`engineSkillsSection.test.ts` | conversation / skills | closed |
| D377 | P3 | **closed** D349 已 pairing-hold-first 于 Tools `refresh()` / extra `listTools`。`loadToolInfo` 入口只 `!isEngineConnected()` / `canShowCatalogRows`，post-await 无 connected/pairing 再检。leftover-looks-live（`isEngineConnected()===true` + pairingPending / `isConversationPairingHold`）仍 extra `getToolInfo` / 把详情画成 live。本刀：`loadToolInfo` 入口 pairing-hold-first（0 extra `getToolInfo`；KEEP leftover + disconnected）且 await 后再检（`isConversationPairingHold \|\| !isEngineConnected()` → KEEP leftover，不画 live）。未改 D349 Tools refresh / extra listTools、D352 Agents Tools tab、D313 writes、Skills `loadSkillBody`、proto、D8/D147。未复用 D312–D376。未宣称 leftover-honesty 程序完成。 | 工位 B `tools-loadToolInfo leftover-looks-live` 已关仓 @2026-09-12 `8f6238622ac` / merge compile-client 0 | leftover-looks-live：`isEngineConnected()===true` + pairingPending 夹具下 0 extra `getToolInfo`，KEEP leftover + disconnected；in-flight resolve after looks-live flip：不画 live / KEEP leftover。既有生产 pairing leftover-info 仍绿。`engineCatalogSections.test.ts` | conversation / tools | closed |
| D378 | P3 | **closed** D375 已闸 `post` / `sendHeartbeatAck` / `writeChat`。`onEngineConnectionChanged` 仍见 `isEngineConnected()===true` 即 `connectionGeneration++` / `connectionUp=true` / `bringUpBoundSession` → `finishBringUp` → `postConnectionUp` + `scheduleAgentTreeRefresh`。leftover-looks-live（`isEngineConnected()===true` + `getConnectionSnapshot().pairingPending`）仍当 live reconnect。本刀：`pairingPending` first 再 `!isEngineConnected()`（platform/node，不 import workbench `isConversationPairingHold`）。leftover-looks-live 不 `connectionDown`、不 bring-up（0 `connectionUp` / 0 `ensureEngineSession` / 0 agent-tree pull）。未改 D375 post/writeChat/heartbeat、requestDetail、acquireLease、bindLiveTreeObservationLease、`scheduleAgentTreeRefresh` 本体。不发明 proto / heartbeat `client_id`。未关 D8/D16/D147。未复用 D312–D377。未宣称 leftover-honesty 程序完成。 | 工位 D `host-bringup leftover-looks-live` 已关仓 @2026-09-12 `745f37bf8da` / merge compile-client 0 | leftover-looks-live：`isEngineConnected()===true` + pairingPending → 0 Create/Resume、0 stream/chat、0 tree fetch、无 `connection_down`。`sessionViewHostEngineBind.test.ts` | universeAgent / sessionViewHost | closed |
| D379 | P3 | **closed** D344 已 pairing-hold-first 于 roster `refreshEngineCatalog()` 入口（0 extra listSessions）。`doRefreshEngineCatalog` await `listSessions` 后无 pairing-hold 再检。leftover-looks-live（`isEngineConnected()===true` + pairingPending / `isConversationPairingHold`）flip mid-flight 仍用 in-flight list 替换 leftover，再 `activateEngineSession` / `ensureEngineSession()`（Create）并 persist 成 live。本刀：await 后 pairing-hold-first → KEEP leftover，0 Create / activate-as-live。未改 D344 入口 KEEP、写 mutators、bindLiveTreeObservationLease/acquireLease、D374 chrome。不发明 proto。未关 D8/D16/D147。未复用 D312–D378。未宣称 leftover-honesty 程序完成。 | 工位 A `roster doRefreshEngineCatalog leftover-looks-live` 已关仓 @2026-09-12 `bdbb9f286c3` / merge compile-client 0 | leftover-looks-live：`isEngineConnected()===true` + pairingPending 夹具下 in-flight listSessions + looks-live flip → leftover KEEP、0 extra Create；入口 leftover-looks-live no-op 仍绿。`conversationEngineRosterService.test.ts` | conversation / roster-catalog | closed |
| D380 | P3 | **closed** D378 已闸 `onEngineConnectionChanged` bring-up，留下 `scheduleAgentTreeRefresh` 本体只见 `!isEngineConnected()`。leftover-looks-live（`isEngineConnected()===true` + `getConnectionSnapshot().pairingPending`）仍 `pullNow` / `scheduleRefresh` → `fetchAgentTree` + `postAgentTreeBound` live tree。already-up stream（`handleHostStreamPayload` / `openStream`）或 `onRequestAgentTreeRefresh` 仍拉。本刀：`pairingPending` first 再 `!isEngineConnected()`（platform/node，不 import workbench `isConversationPairingHold`）。leftover-looks-live 为 no-op（0 `fetchAgentTree` / 0 `postAgentTreeBound`）。未改 D375 post/writeChat/heartbeat、D378 bring-up、requestDetail、acquireLease、bindLiveTreeObservationLease、heartbeat `client_id`、proto。未关 D8/D16/D147。未复用 D312–D379。未宣称 leftover-honesty 程序完成。 | 工位 D `host-tree-refresh leftover-looks-live` 已关仓 @2026-09-12 `07d035334aa` / merge compile-client 0 | leftover-looks-live：`isEngineConnected()===true` + pairingPending → 0 tree fetch / 0 `postAgentTreeBound`。D378 bring-up 测仍绿。`sessionViewHostEngineBind.test.ts` | universeAgent / sessionViewHost | closed |
| D381 | P3 | **closed** D313 已闸 Plugins `canWrite()` leftover-looks-live 写入口。`scanNew` `await scanNewPlugins()` 后无 pairing-hold 再检：`lastScan = result` + `renderScanResult()` 把 in-flight scan 画成 live；随后 `refresh()` KEEP（D349）无法卸 `lastScan`。本刀：await 后 pairing-hold-first（`isConversationPairingHold \|\| !isEngineConnected()`）→ 不赋 `lastScan` / 不画 live scan，KEEP leftover。未改 D349 refresh KEEP / D372 loadInfo / D313 write entry / Skills/Tools / proto。未关 D8/D16/D147。未复用 D312–D380。未宣称 leftover-honesty 程序完成。 | 工位 B `plugins-scanNew leftover-looks-live` 已关仓 @2026-09-12 `56cbd76f7d3` / merge compile-client 0 | 接通开 scan → resolve 前 leftover-looks-live：`isEngineConnected()===true` + pairingPending，不画 live scan，KEEP leftover。既有 canWrite / D372 loadInfo 仍绿。`enginePluginsSection.test.ts` | conversation / plugins | closed |
| D382 | P3 | **closed** D375 已闸 `post` / `sendHeartbeatAck` / `writeChat`；D378 已闸 bring-up；D380 已闸 `scheduleAgentTreeRefresh`。`requestResync` / `acknowledge` 仍无 pairingPending / connected 闸，leftover-looks-live（`isEngineConnected()===true` + `getConnectionSnapshot().pairingPending`）仍 `postAndDrain` `requestResync` / `frameAck`。本刀：两处 pairingPending first 再 `!isEngineConnected()`（platform/node，不 import workbench `isConversationPairingHold`）。leftover-looks-live → 0 `requestResync` / 0 `frameAck`。未知 lease 仍 silent。未改 D375 post/writeChat/heartbeat、D378 bring-up、D380 tree-refresh、requestDetail/fillHistory、acquireLease/bindLiveTree。不发明 proto / heartbeat `client_id` / Hub Connect。未关 D8/D16/D147。未复用 D312–D381。未宣称 leftover-honesty 程序完成。 | 工位 D `host-resync-ack leftover-looks-live` 已关仓 @2026-09-12 `22e61f616e6` / merge compile-client 0 | leftover-looks-live：`isEngineConnected()===true` + pairingPending → 0 `requestResync` / 0 `frameAck`；接通且非 pairing 仍 posts without throwing。`sessionViewHostOverlayAck.test.ts` | universeAgent / sessionViewHost | closed |
| D383 | P3 | **closed** D308 已闸 composer Permission/Model 入口 leftover-looks-live（`isComposerSessionWriteLive`）。`applySessionPermissionIndex` / `applySessionModelIndex` await `setPermissionMode` / `switchModel` 后无 pairing-hold 再检。leftover-looks-live（`isEngineConnected()===true` + pairingPending / `isConversationPairingHold`）flip mid-flight 仍把 in-flight `ok:true` / resolvedModelId 写成 live index。本刀：await 后 pairing-hold-first（`isConversationPairingHold \|\| !isEngineConnected()`）→ restore leftover index，不 keep live apply。未改 D308 入口 KEEP / chrome disabled。未改 Composer catalog / Overview / Hub Connect / Sessions New / pending-chip CSS。不发明 proto。未关 D8/D16/D147。未复用 D312–D382。未宣称 leftover-honesty 程序完成。 | 工位 A `composer-permission-model-post-await leftover-looks-live` 已关仓 @2026-09-12 `48e906b3720` / merge compile-client 0 | leftover-looks-live：`isEngineConnected()===true` + pairingPending 夹具下 in-flight unary + looks-live flip → leftover index restore、非 live Permission/Model。既有入口 `leftover-looks-live pairing-hold session selects stay disabled and do not write` 仍绿。`conversationLensDisposeGate.test.ts` | conversation / composer-chrome | closed |
| D384 | P3 | **closed** D297 已闸 SessionBar title/delete/new pairing-hold（`writesEnabled = !isConversationPairingHold`）。既有 `updateSessionBarWriteChrome pairing-hold disables title delete and new` 与 `SessionBar write chrome disables while pairing-hold leftover` 有 pairingPending + connected phase，但无 `isEngineConnected()===true` leftover-looks-live 显式断言。本刀：补 leftover-looks-live 夹具（`isEngineConnected()===true` + pairingPending / `isConversationPairingHold`）锁 title/delete/new KEEP-chrome disabled（disconnected, not live）。CODE `updateSessionBarWriteChrome` 已 pairing-hold-first，未改生产。未改 New-button chrome / Hub Connect / pending-chip CSS / composer catalog。不发明 proto。未关 D8/D16/D147。未复用 D312–D383。未宣称 leftover-honesty 程序完成。 | 工位 A `sessionbar-write-chrome leftover-looks-live` 已关仓 @2026-09-12 `3207713b8d3` / merge compile-client 0 | leftover-looks-live：`isEngineConnected()===true` + pairingPending 夹具下 title disabled + aria-disabled true；new/delete enabled false。既有 pairing-hold 测仍绿。`conversationLensDisposeGate.test.ts` | conversation / session-bar | closed |
| D385 | P3 | **closed** D361 已 pairing-hold-first 构造 `everConnected`。`onDidChangeConnection` 仍 `if (snapshot.sessionToken) everConnected=true`；`subscribeFileMutations` 仍无条件 `everConnected=true` + append。leftover-looks-live first-pull（`isEngineConnected()===true` + pairingPending / `isConversationPairingHold`）+ token / 首拉 mutation 仍发明 live attribution / mismatch / connected-engine suffix。本刀：pairing-hold-first；不从 `sessionToken` 或 first-pull mutation 发明 `everConnected`；不 ingest 新 live mutation；KEEP leftover chips；suffix/mismatch = disconnected KEEP-chrome，非「Attribution from the connected engine.」。未改 Sources Stage chrome / `sourcesChangesList.ts` / `sourcesChangesGitWrite.ts` / `sourcesDiffPanelView.ts` / SessionBar / Hub Connect。不发明 proto / WriteGitUnstage。未关 D8/D16/D147。未复用 D312–D384。未宣称 leftover-honesty 程序完成。 | 工位 B `attribution leftover-looks-live invent` 已关仓 @2026-09-12 `ca80b313c9c` / merge compile-client 0 | leftover-looks-live first-pull + `sessionToken` / mutation：0 chips、无 mismatch note、无 connected-engine suffix；KEEP leftover chips 仍在且 suffix 非 connected-engine。`sourcesReviewAttribution.test.ts` | sources / attribution | closed |
| D386 | P3 | **closed** Unary Stage leftover-looks-live 已 0。KEEP-chrome 缺口：`resolveSourcesDiffWriteActions` `showStage: canWriteStage \|\| hasLocalStage`，leftover-looks-live（`isEngineConnected()===true` + pairingPending）+ 本地 SCM / `git.stage` 仍画 live Stage。Changes 行 `rowAction==='stage'` 始终 display+enabled；Diff panel / Review Stage 只看 `actions.showStage`，Unstage 已 pairing-hold hide。本刀：pairing-hold-first hide/disable Stage（同 Unstage）；resolver `pairingHold` 时 local SCM `showStage` 为 false。未发明 Unstage write / WriteGitUnstage。未改 Attribution / Hub Connect / Sessions New / pending-chip CSS。未关 D8/D16/D147。未复用 D312–D385。未宣称 leftover-honesty 程序完成。 | 工位 D `sources-stage-chrome leftover-looks-live` 已关仓 @2026-09-12 `1af912fc527` / merge compile-client 0 | leftover-looks-live + scmResource + `git.stage`：Stage hidden/disabled，0 unary / 0 `git.stage`。既有 no-SCM leftover-looks-live Stage 0-unary 仍绿。 | sources / stage-chrome | closed |
| D387 | P3 | **closed** `deleteActiveSession()` 已 pairing-hold-first（0 delete）。`ConversationSessionsDeleteSessionAction` ViewTitle 仅 `when: view === CONVERSATION_SESSIONS_VIEW_ID`，无 pairing-hold chrome，leftover-looks-live（`isEngineConnected()===true` + pairingPending / `isConversationPairingHold`）仍画 Delete enabled。本刀：sessions-view scoped `conversationSessions.deleteEnabled`（pairing-hold-first 为 false；不复用 Navigator `UA_ENGINE_CONNECTED_KEY`）作 Delete `precondition`。KEEP-chrome Delete disabled / aria-disabled，click/run 0 `deleteSession`。未改 New-button chrome / CSS / Hub Connect / pending-chip CSS。不发明 proto。未关 D8/D16/D147。未复用 D312–D386。未宣称 leftover-honesty 程序完成。 | 工位 A `sessions-delete-toolbar leftover-looks-live` 已关仓 @2026-09-12 `be3058d35f4` / merge compile-client 0 | leftover-looks-live：`isEngineConnected()===true` + pairingPending 夹具下 Delete toolbar disabled / aria-disabled，0 `deleteSession`。既有 `deleteActiveSession leftover history while pairingPending shows notice and does not delete` 仍绿。`conversationSessionsView.test.ts` | conversation / sessions-view | closed |
| D388 | P3 | **closed packaging-p1-asar-machine 无窗子集**：首次 `npm run gulp vscode-linux-x64` **exit 1**（`microsoft-authentication` 空 `AsyncUtils.js.map`，本地 `node_modules` 损坏，非 asar 缺包）。已从 `extensions/git/` 同包副本复原（2529 字节，gitignored，未 `git add`）。第二次冻结 gulp **exit 0**；`$PRODUCT/universe-agent-studio` `test -x`；asar 命中 `/@grpc/grpc-js/package.json` 与 `/@grpc/proto-loader/package.json`；remote 两份 `package.json` 无 grpc。dest 为本包装树本次产物。SHA=`3b4cc89f6e53f14935d8396e7061c11fc74907df`。未改 gulp / `.moduleignore`。未启动产物 / 300px / Dial。未关 D18/D20（两行）/D12。证据：[d388-packaging-p1-asar-evidence](d388-packaging-p1-asar-evidence.md)。切片：[d388-packaging-p1-asar-machine](../roadmap/active/d388-packaging-p1-asar-machine.md)。**不是** leftover-looks-live。未提交。 | 空 `.map` 已复原；第二次 gulp 绿；§5（缺包条 N/A）已齐 | 冻结 gulp exit 0 + 根二进制 `test -x` + asar 两包 `package.json` + remote 无 grpc。**不得**用本行关 D18/D20（两行）/D12 | packaging / gulp-desktop | closed |
| D389 | P3 | **planned packaging-p3-linux-deb-machine 无窗 Linux-deb 子集**：dest 身份锁已绿（`test -x` **且** asar；DEST_SHA=`3b4cc89f6e53f14935d8396e7061c11fc74907df`；PREPARE_HEAD retry 3=`442b5457e39e563e4d8c2fce0a46b5d20bf44df6`；**未**再 gulp 桌面包）。`prepare-deb` **三次 exit 1**：失败诚实表 **sysroot**（`install-sysroot.ts` `fetchUrl` GitHub asset `x86_64-linux-gnu-glibc-2.28-gcc-10.5.0.tar.gz` @ `https://api.github.com/repos/microsoft/vscode-linux-build-agent/releases/assets/354882747` `TimeoutError`；首败 11 attempts / 5.95 min；retry 2 同 URL / 11 attempts / 5.68 min；retry 3 同 URL / 11 attempts / 5.7 min）。八档进树 / `build-deb` / 进包 **未跑**（不假绿）。证据：[d389-packaging-p3-linux-deb-evidence](d389-packaging-p3-linux-deb-evidence.md)。切片：[d389-packaging-p3-linux-deb-machine](../roadmap/active/d389-packaging-p3-linux-deb-machine.md)。**不得**关 D18/D20（两行）/D12。不覆盖 prepare-rpm/build-rpm（`rpmbuild` MISSING）/ snap / Inno / Darwin / I6 / 启动 / 300px / Dial。**不是** leftover-looks-live。未改 `dep-lists.ts` / linux gulpfile / `install-sysroot.ts`。未发明镜像。未提交。 | 父方案 P3 合同过宽（Linux 子集 + rpm + 拆行关 D18）；本切片无窗机器子集；B 三次环境不足（GitHub sysroot 超时） | dest 身份锁（asar + dest SHA）+ `prepare-deb` exit 0 + 八档进树 + `build-deb` exit 0 + `.deb` 八档逐档进包。**不得**用本行关 D18/D20（两行）/D12 | packaging / gulp-linux-deb | planned |
| D390 | P3 | **planned packaging-p2-vscode-web-machine 无窗排除面**：父方案 §6 产物扫描是加分项、**不是** D18 closer。合同：`npm run gulp vscode-web` exit 0；dest=`$(dirname "$REPO")/vscode-web` → WT 池 `/home/clarence/Projects/Agents/vscode-WorkTrees/vscode-web`（一份；gulp rimraf；仅 merge 实施）；产物树 **无** `@grpc/grpc-js`（rg/test；依赖根 `remote/web`；**无 asar**）。**禁止**再跑 `vscode-linux-x64` / `prepare-deb`（第四次）。实施：**A 首败 exit 1**（mangler；记录保留）。D391 去跨文件 override 后 mangler 过；8192 两次 + env 16g/32g **exit 134**（[D392](#d392)）。本 tick 直接 node CLI 16g/32g 各一次仍 **exit 134**（heap 旗标已生效；GC ~3 GB）。dest **未写出**。B/C 未跑（不假绿）。`remote/web/package.json` 无 grpc。未改 gulp / `.webignore` / `remote/web` / allowlist / `package.json`。证据：[d390-packaging-p2-vscode-web-evidence](d390-packaging-p2-vscode-web-evidence.md) §A4。切片：[d390-packaging-p2-vscode-web-machine](../roadmap/active/d390-packaging-p2-vscode-web-machine.md)。**不得**关 D18/D20（两行）/D12/D389。**不是** leftover-looks-live。未提交。 | D391 已消 implicit-public；直接 CLI heap 已抬仍 ~3 GB OOM；P2 dest ≠ 桌面包 dest，仍禁再打桌面/prepare-deb | 冻结 gulp exit 0 + dest 身份 + 产物树无 `@grpc/grpc-js`。**不得**用本行关 D18/D20（两行）/D12/D389 | packaging / gulp-vscode-web | planned |
| D391 | P3 | **planned** `ChangesetReviewActionViewItem`（`sessionChangesEditor.ts`）跨文件 override `protected updateChecked`/`getTooltip` → D390 首败 mangler implicit-public。本刀：去掉两 override；`render` 后按 checked 写 container `title`/`aria-label`（checked→「Mark as Not Viewed」，unchecked→「Mark as Viewed」）；checked 变化走 capture click/keydown + microtask。不调跨文件 `protected updateAriaLabel`/`updateTooltip`。未改 mangler / allowlist / gulp / `.webignore` / `remote/web`。未把 base `protected` 改 `public`。可见性修复 **保留**。重跑：mangler **不再**报这两成员；gulp **未** 0（D392 OOM，含直接 CLI 16g/32g）。 | D390 须 `compileBuildWithManglingTask`；桌面 gulp 无 mangling 故 D388 未挡 | `npm run gulp vscode-web` exit 0 且不再报这两成员 implicit-public。**不得**用本行关 D389/D18/D20（两行）/D12 | packaging / mangler-visibility | planned |
| D392 | P3 | **planned** D391 后原样 `npm run gulp vscode-web` 两次 + env 16g/32g 均 **exit 134**（§A2 / §A3；CLI 仍 8192）。本 tick 不改 `package.json`：`node --experimental-strip-types --max-old-space-size=16384 ./node_modules/gulp/bin/gulp.js vscode-web`（13:44:39–13:45:41）与 `32768`（13:46:09–13:46:56）各一次仍 **exit 134**；探针确认 16384 → heap limit 16480 MB；GC 仍死在约 2.9–3.1 GB。dest 未写出。未改 `package.json` / gulpfile。证据：[d390-packaging-p2-vscode-web-evidence](d390-packaging-p2-vscode-web-evidence.md) §A2 / §A3 / §A4。 | CLI heap 已生效仍 ~3 GB；swap 当时已满；只改 `package.json` 8192→16384 会复现已红的 16g 命令 | 冻结 gulp exit 0（dest 写出后再跑 B/C）。**不得**假绿空 dest；**不得**用本行关 D389/D18/D20（两行）/D12 | packaging / gulp-heap | planned |
| D393 | P3 | **closed** I5 `codeOssMigration` 无窗合同测：锁已导出 `shouldOfferCodeOssMigration` 四门（`alreadyOffered` / `!sourceExists` / `sourceEqualsDest` → false；`isNewApplication` **或** `!hasDestSettingsJson` → true，含返回用户且已有 dest settings 的 false 格）；`getProfileMigrationCopies` 仅 `settings.json` / `keybindings.json` / `snippets`，路径不含 `globalStorage` / `state.vscdb` / `workspaceStorage`；`getCodeOssUserDataFolderName(false\|true)` → `code-oss-dev` / `Code - OSS`。见 `src/vs/workbench/contrib/universeAgentMigration/test/common/codeOssMigration.test.ts`。未改 contribution。未 gulp。未关 D12/D18/D20/D389–D392。未提交。 | 工位 A `codeOssMigration` I5 合同测已收；`scripts/test.sh --run …/codeOssMigration.test.ts` 10 passing / 0 fail | 合同测 0 fail。**不得**用本行关 D12/D18/D20/D389–D392 | product / I5 | closed |
| D394 | P3 | **closed** I2 `userDataPath` 无窗合同测：锁已导出 `getUserDataPath`（`src/vs/platform/environment/node/userDataPath.ts`）在 `VSCODE_DEV` 下硬编码 `universe-agent-studio-dev`（`VSCODE_APPDATA` 夹具下路径含该名、**不含** `code-oss-dev`，即使传入 productName 为 `code-oss-dev` / `Code - OSS`）；`--user-data-dir` 仍优先于 I2 改名（[product-identity](../plans/product-identity.md) §3.2）；`getDefaultUserDataPath('UniverseAgentStudio')` basename 为 `UniverseAgentStudio`。见 `src/vs/platform/environment/test/node/userDataPath.test.ts`。未改生产。未 gulp。未关 D12/D18/D20/D389–D393。未发明 proto。不是 leftover-looks-live。未下沉 `universeAgentScheme.ts`。未提交。 | 工位 A `userDataPath` I2 合同测已收；`scripts/test.sh --run …/userDataPath.test.ts` 8 passing / 0 fail（既有 4 + I2 3 + assertCleanState） | 合同测 0 fail。**不得**用本行关 D12/D18/D20/D389–D393 | product / I2 | closed |
| D395 | P3 | **closed** I5 路径解析无窗合同测（[product-identity](../plans/product-identity.md) §3.2 / §6 I5）：锁已导出 `resolveCodeOssUserDataUri` / `getDestSettingsResource`。`resolveCodeOssUserDataUri(URI.file('/tmp/universe-agent-studio-dev'), false)` path 以 `/code-oss-dev` 结尾且 **不含** dest 名 `universe-agent-studio-dev`；`resolveCodeOssUserDataUri(URI.file('/tmp/UniverseAgentStudio'), true)` path 以 `/Code - OSS` 结尾；`getDestSettingsResource(URI.file('/tmp/universe-agent-studio-dev'))` path 为 `/tmp/universe-agent-studio-dev/User/settings.json`。见 `src/vs/workbench/contrib/universeAgentMigration/test/common/codeOssMigration.test.ts`。未改 contribution。未 gulp。未下沉 `universeAgentScheme.ts`。未发明 proto。不是 leftover-looks-live。未关 D12/D18/D20/D389–D392。D393/D394 仍闭。未提交。 | 工位 A `codeOssMigration` I5 路径解析合同测已收；`scripts/test.sh --run …/codeOssMigration.test.ts` 13 passing / 0 fail（D393 既有 10 + 新 3） | 新 suite 0 fail；D393 既有 10 passing 仍绿。**不得**用本行关 D12/D18/D20（两行）/D389–D392 | product / I5 | closed |
| D396 | P3 | **closed** I2 `UNIVERSE_AGENT_SCHEME` 下沉无窗合同（[product-identity](../plans/product-identity.md) §2 / §6 I2）：新建 `src/vs/platform/product/common/universeAgentScheme.ts`，导出已存在常量 `UNIVERSE_AGENT_SCHEME`（`'universe-agent'`；从 `universeAgentDeepLink.contribution.ts` 下沉，不发明新函数 / proto / wrapper）。contribution **只改 import** 并 re-export。node 层测读仓内 `product.json` `urlProtocol` 与 common 常量断言一致（不 import browser contrib）。见 `src/vs/platform/product/test/node/universeAgentScheme.test.ts`。既有 `universeAgentSettingsDeepLink.test.ts` 仍绿。未 gulp。未 compile-client。未关 D12/D18/D20（两行）/D389–D392。未重测 D393/D394/D395。不是 leftover-looks-live。未提交。 | 工位 A I2 scheme 下沉合同测已收；`scripts/test.sh --run …/universeAgentScheme.test.ts` 2 passing / 0 fail；既有 deep-link browser 测 8 passing / 0 fail | 新 suite 0 fail：`urlProtocol === UNIVERSE_AGENT_SCHEME === 'universe-agent'`；handler 只改 import。既有 deep-link browser 测仍绿。**不得**用本行关 D12/D18/D20（两行）/D389–D392 | product / I2 | closed |
| D397 | P3 | **closed** I5 一次性命令 / offered 标记无窗合同（[product-identity](../plans/product-identity.md) §6 I5）：锁已导出 `MIGRATE_FROM_CODE_OSS_COMMAND_ID` / `MIGRATION_OFFERED_STORAGE_KEY`（`src/vs/workbench/contrib/universeAgentMigration/common/codeOssMigration.ts`）。`MIGRATE_FROM_CODE_OSS_COMMAND_ID === 'universeAgent.migrateFromCodeOss'`；`MIGRATION_OFFERED_STORAGE_KEY === 'universeAgent.migration.offered'`。见 `src/vs/workbench/contrib/universeAgentMigration/test/common/codeOssMigration.test.ts`（**新 suite 两格**；不重测 D393 四门 / allow-list / 旧名、不重测 D395 路径解析、不重测 D394 `userDataPath`、不重测 D396 `UNIVERSE_AGENT_SCHEME`）。未改 contribution 生产。未 gulp。未 compile-client。未发明 proto。不是 leftover-looks-live。未关 D12/D18/D20（两行）/D389–D392。D393–D396 仍闭。未提交。 | 工位 A `codeOssMigration` I5 命令/offered 常量合同测已收；`scripts/test.sh --run …/codeOssMigration.test.ts` 15 passing / 0 fail（D393+D395 既有 13 + 新 2） | 新 suite 0 fail：两常量全等。D393+D395 既有仍绿。**不得**用本行关 D12/D18/D20（两行）/D389–D392 | product / I5 | closed |
| D398 | P3 | **closed** I2 知识层改口：`docs/glossary.md` `universe-agent://` 行与 `docs/systems/conversation/commands.md` §4 深链段去掉「不绑 `product.urlProtocol`」；scheme 与 `product.urlProtocol` 统一为 `universe-agent`，handler 路由不变（对齐 [page-access-schemes](../plans/page-access-schemes.md) X1 / [product-identity](../plans/product-identity.md) I2 / D396）。未 gulp。未 compile-client。未关 D12/D18/D20（两行）/D389–D392。未重测 D393–D397。未发明 proto。不是 leftover-looks-live。未提交。 | 工位 A I2 glossary/commands 改口已收 | 两文件不再写「不绑 `product.urlProtocol`」。**不得**用本行关 D12/D18/D20（两行）/D389–D392 | product / I2-honesty | closed |
| D399 | P3 | **closed** [prd-020-turn-fixture-bench](../plans/prd-020-turn-fixture-bench.md) **B0 only**（§4.2 / §4.3 / §8）。无窗、无 DOM：`conversationPrd020ScaleFixture.ts` 导出 `PRD020_CONVERSATION_TURN_COUNT=1000` / `PRD020_TRAJECTORY_RECORD_COUNT=5000` / `createPrd020ConversationTurns` / `createPrd020TrajectoryRecords` / `createPrd020ScaleSession`。偶数下标 `user`、奇数 `assistant`（`stubEcho: true`），文案含 `Stub:`，id 稳定 `prd020-t-0001`… / `prd020-r-0001`…；session `{ id:'prd020-scale', title:'PRD-020 scale (Stub)', source:'local' }`。`createTestFrameSourceCallback` 补测试专用 `refresh(sessionId)`（调已有 `frameSource.refresh`）；`onSessionChanged` 仍只 fire。**不**造 `replaceSessionCatalogForTests` / 构造 `initialSessions`。`prd020-scale` **未**进 `STUB_TRAJECTORY_FIXTURE_SESSION_IDS` / `createSeedSessions`。**本刀不含** B1 bench / B1s 包装 / B2 / B3。不重开 [D10](#d10)。未 gulp。未 compile-client。未发明 proto。不是 leftover-looks-live。未关 D12/D16/D18/D20（两行）/D389–D392。未提交。 | 工位 A PRD-020 B0 规模夹具已收；`scripts/test.sh --run …/conversationPrd020ScaleFixture.test.ts` 6 passing / 0 fail | 新 fixture 测 0 fail；`createTestFrameSourceCallback().refresh` 存在且调用 `frameSource.refresh`；工厂长度/id/Stub 合同齐；生产 seed 不变。**不得**用本行关 D10/D16/D12/D18/D20（两行）/D389–D392；**不得**同刀做 B1 | conversation / prd-020-B0 | closed |
| D400 | P3 | **closed** [product-identity](../plans/product-identity.md) **I5 migrate copy only**（§3.2 / I5「只拷三类文件」）。无窗 mocha-common：已把 `universeAgentMigration.contribution.ts` 的 `migrateCodeOssUserData` / `copyProfileAllowList` / `copyIfExists` 下沉到 `codeOssMigration.ts` 并导出；contribution **只改 import**，不改 offer / 通知 / 命令注册。`InMemoryFileSystemProvider` + `FileService`：源 `User/` 含 `settings.json` + `keybindings.json` + `snippets/x.json` + **`globalStorage/foo`** + **`state.vscdb`** + **`workspaceStorage/ws`** + `profiles/p1/settings.json`；`migrateCodeOssUserData` 后 dest 仅有 allow-list（含 named profile `p1/settings.json`），excluded 三族 path 不出现；返回值 = 实际 copy 次数（缺文件 skip、不计入）。**不**重测 D393 四门 / allow-list planner / D395 路径 / D397 常量 / D394 `userDataPath` / D396 scheme / D398 改口 / D399 B0。**不含** B1 bench / B1s / B2 / B3。不重开 [D10](#d10)。未 gulp。未 compile-client。未发明 proto。不是 leftover-looks-live。未关 D12/D16/D18/D20（两行）/D389–D392。未提交。 | 工位 A I5 migrate copy 下沉合同测已收；`scripts/test.sh --run …/codeOssMigration.test.ts` 17 passing / 0 fail（D393+D395+D397 既有 15 + 新 2） | 新 suite 0 fail：dest 有 allow-list、无 excluded、named profile 已拷、返回值诚实。既有 D393+D395+D397 仍绿。**不得**用本行关 D12/D18/D20（两行）/D389–D392；**不得**同刀做 B1 | product / I5-migrate | closed |
| D401 | P3 | **closed** [product-identity](../plans/product-identity.md) **I3a derive only**（§4.2.2 / I3a）。无窗 mocha-node（对齐 D396 harness）：给 `build/brand/generate-icons.mjs` 加 **direct-run 闸门**（`import.meta.url === pathToFileURL(process.argv[1]).href` 才 `main()`），并 **export 已有** `deriveBoldSvg` / `deriveInvertedSvg` / `svgForSize`（不发明新派生规则 / 不跑 sharp / 不写 PNG·ICO·ICNS·BMP·XPM）。新测 `src/vs/platform/product/test/node/universeAgentBrandDerive.test.ts`：读仓内 `resources/brand/universe-agent-studio.svg`；`deriveBoldSvg(source) ===` 入仓 `universe-agent-studio-bold.svg`（`stroke-width` 3→24；删除三条 `<line x1="256" y1="256"` 辐射线；中心圆 r 12→20；外圆/六边/三角仍在）；`deriveInvertedSvg(source) ===` 入仓 `universe-agent-studio-light.svg`（`#546E7A`→`#AB9185`、`#FFFFFF`→`#000000`、`aria-label="UniverseAgentStudio Light"`；**仍留**辐射线；**无** `singular-white`）；`svgForSize(full,bold,16\|24\|32\|48)===bold` 且 `svgForSize(full,bold,64)===full`。源 SVG 须 `aria-label="UniverseAgentStudio"`。**不**重测 D393–D400。**不含** B1 / B1s / B2 / B3。不重开 [D10](#d10)。未 gulp。未 compile-client。未发明 proto。不是 leftover-looks-live。未关 D12/D16/D18/D20（两行）/D389–D392。未提交。 | 工位 A I3a derive 合同测已收；`scripts/test.sh --run …/universeAgentBrandDerive.test.ts` 5 passing / 0 fail | 新 suite 0 fail：三函数输出与入仓 SVG 全等；≤48 走粗线、>48 走主源。**不得**用本行关 D12/D18/D20（两行）/D389–D392；**不得**同刀做 B1 / 跑生成器写栅格 | product / I3a-derive | closed |
| D402 | P3 | **closed** [product-identity](../plans/product-identity.md) **I3a raster encode only**（§4.2 Inno BMP / rpm XPM）。无窗 mocha-node（对齐 D396/D401 harness）：给 `build/brand/generate-icons.mjs` **export 已有** `encodeBmp24` / `encodeXpm` / `fillCanvas`（不发明新编码规则 / 不跑 sharp / **不写** PNG·ICO·ICNS·BMP·XPM / **不跑** `main()`）。新测 `src/vs/platform/product/test/node/universeAgentBrandRaster.test.ts`（**不**改 `universeAgentBrandDerive.test.ts`）。`encodeBmp24(2,1, [255,0,0,255, 0,0,255,255])`：`BM`；DIB width=2 height=1 bitcount=24；像素区 8 字节（rowSize 4 对齐）；底行向上 BGR = `00 00 FF` + `FF 00 00` + 2 字节 pad。`encodeXpm(2,1, [255,0,0,255, 0,0,0,0], 'code_xpm')`：头 `"2 1 2 1"`；palette 含 `#FF0000` 与 `None`（alpha < 16）；`static char * code_xpm[]`；**无** `singular-white`。`fillCanvas(4,4, 2×2 不透明白, 2)`：四角 RGBA=`0x54,0x6E,0x7A,255`（脚本 `BRAND_BG`）；中心 2×2 为白。**不**重测 D393–D401。**不含** B1 / B1s / B2 / B3。不重开 [D10](#d10)。未 gulp。未 compile-client。未发明 proto。不是 leftover-looks-live。未关 D12/D16/D18/D20（两行）/D389–D392。 | 工位 A I3a raster encode 合同测已收；`scripts/test.sh --run …/universeAgentBrandRaster.test.ts` 4 passing / 0 fail（新 suite 3 + assertCleanState）；未提交 | 新 suite 0 fail：BMP 头/BGR/行对齐、XPM 头/透明色、canvas 铺底+居中合成。**不得**用本行关 D12/D18/D20（两行）/D389–D392；**不得**同刀做 B1 / 跑生成器写栅格 / 断言 `HICOLOR_SIZES` 常量表 | product / I3a-raster | closed |
| D403 | P1 | **closed** Maximize 不再对 `.conversation-lens-dock-gate-row` / `.conversation-lens-inbox-overlay` 写 `display:none`；CssScan 锁 class 合同，D42 三断言仍绿。 | 工位 A D403 已收；`scripts/test.sh --run …/conversationLensCssScan.test.ts` 4 passing / 0 fail | CssScan class 合同 0 fail；D42 三断言仍绿。不重开 D42。未关 D404。 | conversation / lens-css | closed |
| D404 | P1 | **closed** 窄宽 Back 后 live `pairingConfirmHost` 在 `.connection-preferences-detail` 外；`syncPairingConfirmHostParent` 只 remount、永不 `selectZone`。 | 工位 A D404；`connectionPreferencesPane.test.ts` 136 passing / 0 fail | 窄宽 + live host + Back：`!detail.contains(host)`；宽栏 / showing-detail 仍走 `assertSasVisibleBesideActiveZone`。未改 pairing 产品流。 | conversation / connection-pane | closed |
| D405 | P2 | **[session-subscription-lifecycle](../plans/session-subscription-lifecycle.md) S1–S3 收口 + S4a/S4b 代码**：`releaseLeasesOwnedBy` + channel `acquireLeaseFor` + app 双钩；`scheduleStreamReopen`；`applyDefaultUnaryDeadline` + `_withTransport` 相位恢复；缓存 Resume 失败回落 + 渲染端 `whenBindReady` 可重臂。G-CORE-2 已登记。**S4a** electron-main 注入打点 `DiagnosticsPort`（`count` 落主进程日志、`warn` 走 logService）。**S4b** `streamReopenSkipReason` 含 `fail_closed`，宿主自记 `mailbox.overflow` 后停止 reopen 直至 `acquireLease` / 连接翻转。§4 单元 Exit 已绿（含 S4b 顺序测）。**§5 V-S1–V-S3 / V-S4b-1 手测未跑**。已合入 merge 的 S1–S3 于 `9f8ca6a236d`；S4a/S4b 已合入 merge 且 `npm run compile` 0。不占 D406/D407。 | 本槽禁 compile / 无隔离 profile 开窗；S4a 已给计数式验收观察点 | 隔离 profile 过 V-S1–V-S3 / V-S4b-1；S4a/S4b 合入 merge 且 compile-client 0 才可 push | universeAgent / session-lifecycle | open |
| D406 | P3 | **Navigator Agents / Team 活画→UNSUPPORTED 卸 leftover**：capability 从 live 落到 `UNSUPPORTED` 时 Agents `setHierarchyState([], unsupported)` + `setActivityFromSnapshot`、Team `setMemberEntries([], …)` 会卸成首拉空。本刀 KEEP：Agents 走 `setHierarchyAfterPending(unsupported)` + Activity leftover 只 `setActivityNote`（不重画 live snapshot）；Team 走 `setTeamAfterTreeEmpty(TEAM_UNSUPPORTED_COPY)`。补「活画 → UNSUPPORTED」测。首拉 empty+unsupported / UNKNOWN / fetch-fail / pairing leftover 既有测保持绿。未碰 Sources / lens / pairing 产品流 / proto / `dev/loop`。未 compile-client。未提交。 | 工位 B `navigator-unsupported-leftover`；`scripts/test.sh --run …/navigatorAgentsSubviews.test.ts --run …/navigatorTeamSubviews.test.ts` **74 passing / 0 fail**（既有 71 + 新 3） | 活画→UNSUPPORTED：行还在、note=unsupported、`getLiveAgentIds()===undefined`、0 额外 unary；首拉 UNSUPPORTED 仍 empty+unsupported；UNKNOWN / fetch-fail / pairing leftover 既有测绿。**不得**宣称 leftover wave 完成。未关 D16。 | M7 navigator | closed |
| D407 | P2 | **closed** Engine Preferences「Test Engine」改走 `probeEngine()` + `formatConnectionProbeStatus`；disconnected / 无 transport 走 probe 失败 reason，不写「Connected」。测锁 `probeEngine` 被调用且文案以 `Reachable —` / `Unreachable —` 开头。已合入 merge `9f8ca6a236d`。未升 PRD-008。merge compile-client 红属 D405 类型错，不挡本行闭合。 | 工位 D `engine-test-probe`；`enginePreferencesPane.test.ts` **16 passing / 0 fail** | `runEngineTest` 调 `probeEngine`；失败 `Unreachable —` + reason；成功 `Reachable —` + H4b phase。不把「仅 phase 文案」当成功。 | conversation / engine-preferences | closed |
| D408 | P3 | **closed** 连接级自动重连：`_markTransportFailed` 在已有 `_activeProfileId`、非 pairingPending、非用户 disconnect 时退避后 `connectProfile(id, { reconnect: true })`（base 1000 / cap 30000 / ±20% jitter，可注入 timer；成功归零 n；disconnect/dispose/cancelPairing 取消定时器）。pairing_required / unauthenticated / 无 active profile 不重拨。未改 proto / sessionCore / S2 `scheduleStreamReopen`。已合入 `e09eab0a1f6`。 | 工位 A `connection-reconnect-backoff`；合入 merge | 断连无需再点 Connect 即可回到 `connected`。未升 PRD-008。未关 D405 手测。 | universeAgent / connection | closed |
| D409 | P3 | **closed** `NavigatorSessionLeaseHolder.refreshLease` acquire throw 后只 `lease=undefined` + `onAcquireError`，不调 `onLeaseChanged`。`setVisible` 本身不 `scheduleRefresh`。hide→show 再 acquire throw 时 Team/Agents 旧行继续像 live。本刀 catch 仍通知；holder 回到无 lease。补 hide→show throw 测：不是旧 lease 仍 live。既有 Team 首拉 notice 测仍绿。未碰 connection reconnect / conversation host / proto / Sources / `dev/loop`。已合入 `c061be1cbbb`。不占 D408。 | 工位 B `navigator-lease-acquire-throw`；`scripts/test.sh --run` holder + Team + Agents **76 passing / 0 fail**（holder 1 + Team 既有含首拉 + hide→show 1 + Agents 既有） | acquire throw 后 `onLeaseChanged`；`getLease()===undefined`；hide→show 不是 leftover-looks-live。Team 首拉 notice 仍绿。**不得**宣称 leftover wave 完成。未关 D16。 | M7 navigator | closed |
| D410 | P2 | **closed** `_fireReconnect` 在 `connectProfile({reconnect:true})` 失败后再走同一 `_scheduleReconnect` 退避（attempt 递增，不 tight-loop）。覆盖 `{ok:false, code:'transport_failed'}` 与非 `UniverseAgentTransportError` throw；pairing_required / hub_auth_expired / hub_session_required 仍停拨。D408 合同保持。A letter-commit `f4acb938092`（375/0；OV PASS）。已合入 `9974bfa2822`。未升 PRD-008。未关 D405。不占 D411/D412。 | 工位 A `d410-reconnect-fail-reschedule`；375/0；merge compile-client 0 | 失败后再调度测绿；既有 D408 suite 仍绿。未升 PRD-008。未关 D405 手测。 | universeAgent / connection | closed |
| D411 | P2 | **closed** Engine Preferences「Test Engine」`runEngineTest` 复用 Connection 导出的 `writeStatus`：成功 `success` tone、失败/throw `error` tone、`Testing…` 中性。页脚与 banner 按钮同走 `runEngineTest`。Engine CSS `.engine-test-status.is-success/.is-error` 对齐 Connection 色。B `cf5a51c1eb7`（19/0）。已合入 `0ae05e427f9`。未升 PRD-008。未关 D8/D16/D147/D405。 | 工位 B `engine-test-tone`；19/0；merge compile-client 0 | `runEngineTest` 走 `writeStatus`；Reachable `.is-success`；Unreachable/throw `.is-error`；Testing 无 success/error。未关 D16。 | conversation / engine-preferences | closed |
| D412 | P3 | **closed** Engine catalog / section 写状态复用 Connection 导出的 `writeStatus`：成功 `.is-success`、失败/`ok:false`/throw `.is-error`、clear/pending 中性。覆盖 MCP/Skills/Tools/Agents/Plugins/Triggers/Clipboard/Context Variable 与 Snapshots overlay（不重做 leftover-keep）。写失败不 `clearCatalogPresentation`。未 compile-client。未关 D8/D16/D147/D405。未升 PRD-008。 | 工位 D `d412-catalog-write-tone`；`scripts/test.sh --run` catalog/skills/clipboard/triggers/snapshots/plugins **244/0** | 各写状态走同一 `writeStatus` + 测锁 classList。未关 D16。 | conversation / engine-preferences | closed |
| D413 | P2 | **closed** 明文 profile 重连走 `connect()` 曾无条件 `reason:'initial'`，把 D408/D410 的 `transport_lost` 洗成 initial（状态栏 Reconnecting…→Connecting…）。另：`_connectProfileRaw` 在 resolver `!ok` / handshake `kind==='failed'` 且 `code==='transport_failed'` 时只 return，不 `_scheduleReconnect`；用户在 `transportState==='failed'` 下再点 Connect（`reconnect===true`）失败后定时器已被开头 `_cancelReconnectTimer()` 清掉，非 `_fireReconnect` 则停拨。本刀：`connect()` 在相位已是 `connecting`+`transport_lost`、或 `_transportState==='failed'`、或来自 `connectProfile({reconnect:true})` 时保持 `transport_lost`（pairingPending 成功仍可 initial）；上述 `transport_failed` 返回在仍有 active profile、reconnect 或既有 failed 传输、非 halt 码（`pairing_required` / `hub_auth_*` / `trust_missing` / `pin_mismatch`）时 `_scheduleReconnect`。测锁明文重连相位、resolver/handshake `transport_failed` 再拨、`pin_mismatch` / pairing 不拨。D410 保持 closed。未升 PRD-008。未关 D8/D16/D147/D405。未开 D405 手测。 | 工位 A `d413-reconnect-phase-reschedule` `a615755bb8b`；`node test/unit/node/index.js --run …/universeAgentConnection.test.ts` **380/0** | 明文重连相位保持 `transport_lost`；reconnect 模式 `transport_failed` 再拨；`pin_mismatch` / pairing 不拨。未升 PRD-008。未关 D405 手测。 | universeAgent / connection | closed |
| D414 | P3 | **closed** leftover 工具详情诚实态 + Plugins scan 结果复用 Connection `writeStatus`（D412 只收 catalog *写* 状态）。leftover `paintToolInfoHonesty` 失败/断连/API 不可用 error；成功详情 layout 未改。scan 空/找到 success（scan 完成即成功）；clear / list-fail 后 scan 中性；scan throw 仍走既有 `showWriteFailed` write-status error（scanResult 中性）。CSS `.engine-tools-info-status` / `.engine-plugins-scan-result` 对齐 D411/D412 色变量。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。 | 工位 B `d414-leftover-scan-tone` `a2a12199684`；`scripts/test.sh --run` tools+plugins **30/0**；catalog Tools leftover KEEP **9/0** | leftover `.engine-tools-info-status.is-error`；scan 空/找到 `.is-success`；clear 无 tone；scan throw write-status `.is-error`。未关 D16。 | conversation / engine-preferences | closed |
| D415 | P3 | **closed** pairing-hold / leftover-looks-live `applyDisconnectedRefresh` KEEP 后自动 `paintToolInfoHonesty`（disconnected copy + `writeStatus` error）。无需再 `selectTool`。既有 KEEP leftover 测仍绿。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D416。 | 工位 B `d415-pairing-hold-tool-info`；`scripts/test.sh --run` `engineToolsSection.test.ts` **6/0**（含 2 条无重选）；catalog Tools leftover KEEP **13/0** | pairing-hold / leftover-looks-live refresh 后无需重选即见 disconnected copy + error tone。未关 D16。 | conversation / engine-tools | closed |
| D416 | P3 | **closed** 用户点 Connect（`connectProfile({reconnect:true})` 或 `_transportState==='failed'` 隐式重连，非 `_fireReconnect`）时，明文 `connect()` / handshake `catch` 的**非** `UniverseAgentTransportError` throw 再走 `_maybeScheduleReconnectAfterTransportFailedReturn`（仍有 active profile、非 halt、非 pairingPending、非用户 disconnect）。`_markTransportFailed` 仍只臂 TransportError；首连 throw / pairingPending 不拨。D408/D410/D413 测仍绿。未升 PRD-008。未关 D8/D16/D147/D405。未开 D405 手测。不占 D415。 | 工位 A `d416-user-reconnect-throw`；`node test/unit/node/index.js --run …/universeAgentConnection.test.ts` **385/0**（既有 380 + 新 5） | 用户重连非 TE throw 再拨；首连 / pairingPending 不拨；既有 D410 `_fireReconnect` 测仍绿。未升 PRD-008。未关 D405 手测。 | universeAgent / connection | closed |
| D417 | P3 | **closed** `_connectProfileRaw` 入口已 `_cancelReconnectTimer()` 的 `resolve` / `loadGrpcModule` / `getOrCreateIdentity` / `createSigner` throw（`connect()` / handshake `catch` 外）在用户重连时再走 `_maybeScheduleReconnectAfterTransportFailedReturn`（仍有 active profile、非 halt、非 pairingPending、非用户 disconnect）。实现：`connectProfile` 外包 catch；D416 两处 catch 再拨后二次调度被 timer 闸门吃掉。首连 / pairingPending 不拨。`loadGrpcModule` 模块非注入，与 resolve 同走该 catch。D416 五测仍绿。未升 PRD-008。未关 D8/D16/D147/D405。不占 D418/D419。 | 工位 A `d417-prereq-throw-reschedule`；`node test/unit/node/index.js --run …/universeAgentConnection.test.ts` **390/0**（既有 385 + 新 5） | 用户重连 resolve / identity / signer throw 再拨；首连 / pairingPending 不拨；既有 D416 测仍绿。未升 PRD-008。未关 D405 手测。 | universeAgent / connection | closed |
| D418 | P3 | **closed** pairing-hold / leftover-looks-live `applyDisconnectedRefresh` KEEP 后自动 `showBodyStatus(disconnected)`。无需再 `selectSkillForTest`。既有 KEEP leftover 测仍绿。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D417/D419。 | 工位 B `d418-pairing-hold-skill-body`；`scripts/test.sh --run` `engineSkillsSection.test.ts` **32/0**（含 2 条无重选）；catalog Skills leftover KEEP **1/0** | pairing-hold / leftover-looks-live refresh 后无需重选即见 leftover body disconnected copy。未关 D16。 | conversation / engine-skills | closed |
| D419 | P3 | **closed** pairing-hold / leftover-looks-live `applyDisconnectedRefresh` KEEP 后自动 `keepLeftoverHooksDisconnected`（disconnected copy + leftover hooks 仍在）。无需再 `selectPlugin`。既有 KEEP leftover / leftover-looks-live 重选测仍绿。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D417/D418。 | 工位 D `d419-pairing-hold-plugin-hooks`；`scripts/test.sh --run` `enginePluginsSection.test.ts` **27/0**（含 2 条无重选） | pairing-hold / leftover-looks-live refresh 后无需重选即见 leftover hooks 断连诚实态。未关 D16。 | conversation / engine-plugins | closed |
| D420 | P3 | **closed** `connect()` 在 try/catch **之前** `await _ensureTransport()`（`loadGrpcModule` / `_createTransport`）。直接 `connect()` 若此处 throw，D416/D417 收不到。本刀：该 throw 走 `_scheduleReconnect`（`_canScheduleReconnect`：仍有 active profile、非 pairingPending、非用户 disconnect；ensureTransport 裸 throw 无 halt 码）。`connectProfile` 已先建 transport 时 `_ensureTransport` 仍是 no-op。无 profile / pairingPending / disconnect 不拨。D416/D417 测仍绿。未升 PRD-008。未关 D8/D16/D147/D405。不占 D421/D422。 | 工位 A `d420-connect-ensure-transport`；`node test/unit/node/index.js --run …/universeAgentConnection.test.ts` **394/0**（既有 390 + 新 4） | 直接 `connect()` `_ensureTransport` throw 再拨；无 profile / pairingPending / disconnect 不拨；既有 D416/D417 测仍绿。未升 PRD-008。未关 D405 手测。 | universeAgent / connection | closed |
| D421 | P3 | **closed** pairing-hold / leftover-looks-live `applyDisconnectedRefresh` KEEP leftover user body 后自动 `updateBodyEditorChrome`（Save 关 / textarea readOnly），再 `showBodyStatus(disconnected)`（D418 诚实态仍在；bundled leftover 仍 read-only、不改成 live）。无需再 `selectSkillForTest`。既有 KEEP leftover / D418 无重选测仍绿。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D420/D422。 | 工位 B `d421-pairing-hold-skill-save-chrome`；`scripts/test.sh --run` `engineSkillsSection.test.ts` **36/0**（含 2 条 user Save chrome 无重选 + D418 bundled 无重选仍绿）；catalog Skills leftover KEEP **1/0** | KEEP leftover user body 后无需重选即见 Save 关 / readOnly；bundled leftover 仍 read-only + disconnected copy。未关 D16。 | conversation / engine-skills | closed |
| D422 | P3 | **closed** pairing-hold / leftover-looks-live `applyDisconnectedRefresh` KEEP 后自动 `showAgentsEditorStatus(disconnected, error)`（leftover 详情是 Instructions AGENTS.md editor，不是 Tools 面板）。无需再 `selectProfile`。既有 KEEP leftover / leftover-looks-live 重选测仍绿。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D420/D421。 | 工位 D `d422-pairing-hold-agents-md`；`scripts/test.sh --run` `engineCatalogSections.test.ts` **94/0**（既有 92 + 新 2 条无重选） | pairing-hold / leftover-looks-live refresh 后无需重选即见 leftover AGENTS.md disconnected copy + error tone。未关 D16。 | conversation / engine-agents | closed |
| D423 | P3 | **closed** pairing-hold / leftover-looks-live `applyDisconnectedRefresh` KEEP leftover user AGENTS.md 后自动 `updateAgentsEditorChrome`（Save 关 / textarea readOnly），再 `showAgentsEditorStatus(disconnected, error)`（D422 诚实态仍在；built_in leftover 仍 read-only、不改成 live）。无需再 `selectProfile`。既有 KEEP leftover / D422 无重选测仍绿。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D424/D425。 | 工位 D `d423-pairing-hold-agents-save-chrome`；`scripts/test.sh --run` `engineCatalogSections.test.ts` **97/0**（既有 94 + 新 2 条 user Save chrome 无重选 + 1 条 built_in 仍 read-only） | KEEP leftover user AGENTS.md 后无需重选即见 Save 关 / readOnly；built_in leftover 仍 read-only + disconnected copy。未关 D16。 | conversation / engine-agents | closed |
| D424 | P3 | **closed** pairing-hold / leftover-looks-live `applyDisconnectedRefresh` KEEP 后自动 `keepLeftoverRuntimeToolsDisconnected`（disconnected copy + leftover 工具行仍在）。无需再 `selectServerForTest`。既有 KEEP leftover / leftover-looks-live 重选测仍绿。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D423/D425。 | 工位 B `d424-pairing-hold-mcp-runtime-tools`；`scripts/test.sh --run` `engineMcpRuntimePanel.test.ts` **20/0**（含 2 条无重选） | pairing-hold / leftover-looks-live refresh 后无需重选即见 leftover tools 断连诚实态。未关 D16。 | conversation / engine-mcp-runtime | closed |
| D425 | P3 | **closed** pairing-hold / leftover-looks-live `applyDisconnectedRefresh` KEEP 只 hide write toolbar + `renderStatus()`，leftover 行 checkbox 仍像 live。本刀：row renderer 接 `canWrite()` disable；KEEP 后 `splice` leftover 行（`list.rerender()` 无 dynamic heights 是空操作）关 toggle chrome，不重选。既有 MCP KEEP leftover / 0 unary 测仍绿。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D423/D424。 | 工位 A `d425-pairing-hold-mcp-toggle`；`scripts/test.sh --run` catalog+mcp-runtime **111/0**（catalog 既有 92 + 新 2 条无重选 + runtime 17） | KEEP leftover 后无需重选即见行 toggle `aria-disabled` + 写钮 0 unary + leftover 仍选中。未关 D16。 | conversation / engine-mcp | closed |
| D426 | P3 | **closed** pairing-hold / leftover-looks-live `applyDisconnectedRefresh` KEEP 只 hide Save + `renderStatus()` + D415 info honesty，leftover 行 checkbox 仍像 live（`list.rerender()` 无 dynamic heights 是空操作）。本刀：KEEP 后 `splice` leftover 行（对照 D425 MCP）关 toggle chrome，不重选。既有 Tools KEEP leftover / D415 无重选测仍绿。MCP UNKNOWN / list-fail leftover toggle 已由 D427 收口。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D427/D428。 | 工位 A `d426-pairing-hold-tools-toggle`；`scripts/test.sh --run` tools+catalog **103/0**（tools 既有 5 + 新 2 条无重选；catalog 既有 + 新 2 条无重选） | KEEP leftover 后无需重选即见行 toggle `aria-disabled` + 写钮 0 unary + leftover 仍选中。未关 D16。 | conversation / engine-tools | closed |
| D427 | P3 | **closed** MCP leftover after capability UNKNOWN / list-fail 不 `splice`，行 toggle 仍像 live（D425 只收 pairing-hold KEEP）。本刀：UNKNOWN leftover / list throw leftover 在 hide toolbar + `renderStatus()` 后复用 D425 `closeLeftoverRowToggleChrome`（`canWrite()` disable + `splice`；`list.rerender()` 无 dynamic heights 是空操作），无需重选。既有 UNKNOWN leftover / list-fail leftover / D425 KEEP 无重选测仍绿。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D426/D428。 | 工位 B `d427-mcp-unknown-listfail-toggle`；`scripts/test.sh --run` catalog+mcp-runtime **115/0**（catalog 既有 + 新 2 条 UNKNOWN/list-fail 无重选 + D425 KEEP 无重选仍绿 + runtime 20） | UNKNOWN / list-fail leftover 后无需重选即见行 toggle `aria-disabled` + 写钮 0 unary + leftover 仍选中。未关 D16。 | conversation / engine-mcp | closed |
| D428 | P3 | **closed** pairing-hold / leftover-looks-live `applyDisconnectedRefresh` KEEP 只 hide Scan/Enable 工具栏 + `renderStatus()`，leftover Enable/Reload/Unload/Scan 钮仍 `enabled`（`aria-disabled=false`）。无行 toggle（行只 name+summary）；`list.rerender()` 无 dynamic heights 是空操作，splice 也画不出写 chrome。本刀：KEEP 后 `updateWriteToolbar` / `updateRowActions` 按 `canWrite()` disable，不重选。既有 Plugins KEEP leftover / 0 unary / D419 hooks 测仍绿。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D426/D427。 | 工位 D `d428-pairing-hold-plugin-enable-chrome`；`scripts/test.sh --run` `enginePluginsSection.test.ts` **31/0**（既有 29 + 新 2 条无重选） | KEEP leftover 后无需重选即见 Enable/Reload/Unload/Scan `aria-disabled` + 写钮 0 unary + leftover 仍选中。未关 D16。 | conversation / engine-plugins | closed |
| D429 | P3 | **closed** Tools leftover after capability UNKNOWN / list-fail 不 `splice`，行 toggle 仍像 live（D426 只收 pairing-hold KEEP）。本刀：UNKNOWN leftover / list throw leftover 在 hide toolbar + `renderStatus()` 后复用 D426 `closeLeftoverRowToggleChrome`（`canWrite()` disable + `splice`；`list.rerender()` 无 dynamic heights 是空操作），无需重选。首拉 UNKNOWN 仍清行。既有 KEEP leftover / D415 无重选测仍绿。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D430/D431/D432。 | 工位 A `d429-tools-unknown-listfail-toggle`；`scripts/test.sh --run` `engineToolsSection.test.ts` **11/0**（既有 KEEP/D415 + 新 UNKNOWN/list-fail 无重选 2 + 首拉 UNKNOWN 1） | UNKNOWN / list-fail leftover 后无需重选即见行 toggle `aria-disabled` + 写钮 0 unary + leftover 仍选中。未关 D16。 | conversation / engine-tools | closed |
| D430 | P3 | **closed** Plugins leftover after capability UNKNOWN / list-fail 不关 Enable/write chrome：D428 只收 pairing-hold KEEP。本刀：UNKNOWN leftover / list throw leftover 在 hide toolbar + `renderStatus()` 后 `closeLeftoverWriteChrome`（`updateWriteToolbar` + `updateRowActions` 按 `canWrite()` disable），不重选。无行 toggle；`list.rerender()` 无 dynamic heights 是空操作。既有 UNKNOWN leftover / list-fail leftover / D428 KEEP 无重选测仍绿。首拉 UNKNOWN 仍清行。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D429/D431。 | 工位 D `d430-plugins-unknown-listfail-enable-chrome`；`scripts/test.sh --run` `enginePluginsSection.test.ts` **33/0**（既有 31 + 新 2 条无重选） | UNKNOWN / list-fail leftover 后无需重选即见 Enable/Reload/Unload/Scan `aria-disabled` + 写钮 0 unary + leftover 仍选中。未关 D16。 | conversation / engine-plugins | closed |
| D431 | P3 | **closed** Skills leftover after capability UNKNOWN / list-fail 不关 leftover user body Save chrome（D421 只收 pairing-hold KEEP `applyDisconnectedRefresh`）。本刀：UNKNOWN leftover / list throw leftover 在 hide toolbar + `hideWriteStatus()` + `renderStatus()` 后 `closeLeftoverBodyEditorChrome`（`updateBodyEditorChrome`；`canWrite()` 已 false），无需重选。首拉 UNKNOWN 仍清行。未发明行 toggle。既有 KEEP leftover / D418 无重选测仍绿。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D429/D430/D432。 | 工位 B `d431-skills-unknown-listfail-save-chrome`；`scripts/test.sh --run` `engineSkillsSection.test.ts` **38/0**（既有 36 + 新 2 条 UNKNOWN/list-fail 无重选 + D421 KEEP 无重选仍绿） | UNKNOWN / list-fail leftover 后无需重选即见 Save 关 / textarea `readOnly` + 0 `saveSkillContent` unary。未关 D16。 | conversation / engine-skills | closed |
| D432 | P3 | **closed** Skills leftover after capability UNKNOWN / list-fail / pairing-hold KEEP 不关行 toggle：D431 只收 leftover user body Save。本刀：row renderer 接 `canWrite()` disable；KEEP / UNKNOWN leftover / list throw leftover 在 Save chrome 后 `closeLeftoverRowToggleChrome`（`canWrite()` disable + `splice`；`list.rerender()` 无 dynamic heights 是空操作），无需重选。首拉 UNKNOWN 仍清行。既有 KEEP leftover / D431 Save chrome 无重选测仍绿。`toggleSkill` `canWrite()` 早退锁 0 `setSkillEnabled` unary。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D433/D434/D435。 | 工位 A `d432-skills-leftover-toggle`；`scripts/test.sh --run` `engineSkillsSection.test.ts` **42/0**（既有 38 + 新 KEEP pairing-hold / leftover-looks-live / UNKNOWN / list-fail 无重选 4） | KEEP / UNKNOWN / list-fail leftover 后无需重选即见行 toggle `aria-disabled` + 0 `setSkillEnabled` unary + leftover 仍选中。未关 D16。 | conversation / engine-skills | closed |
| D433 | P3 | **closed** Agents leftover after capability UNKNOWN / list-fail 不关 leftover user AGENTS.md Save chrome：D422/D423 只收 pairing-hold KEEP（`applyDisconnectedRefresh` 里 `updateAgentsEditorChrome`）。本刀：UNKNOWN leftover / list throw leftover 在 hide toolbar + `updateWriteActions()` + `renderStatus()` 后 `closeLeftoverAgentsEditorChrome`（`updateAgentsEditorChrome`；`canWrite()` 已 false），无需重选。首拉 UNKNOWN 仍清行。未发明行 toggle。未重做 D422/D423 KEEP。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D432/D434。 | 工位 B `d433-agents-unknown-listfail-save-chrome`；`scripts/test.sh --run` `engineCatalogSections.test.ts` **105/0**（既有 KEEP 无重选仍绿 + 新 2 条 UNKNOWN/list-fail 无重选） | UNKNOWN / list-fail leftover 后无需重选即见 Save 关 / textarea `readOnly` + 0 `saveAgentProfile` write unary。未关 D16。 | conversation / engine-agents | closed |
| D434 | P3 | **closed** MCP Runtime leftover after capability UNKNOWN / list-fail 不画 leftover tools 诚实态：D424 只收 pairing-hold KEEP（`applyDisconnectedRefresh` `keepLeftoverRuntimeToolsDisconnected`）。`refresh()` UNKNOWN / list throw leftover 只 hide Refresh + `renderStatus()`，leftover 工具名仍像 live。本刀：无需重选即 leftover tools 画 capability-loading / 不可用（UNKNOWN）或 failed（list-fail）；首拉 UNKNOWN 仍清行。不重做 D424 KEEP。Refresh 钮保持关。勿发明 listTools；勿 extra `getMcpServerStatuses`。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D432/D433/D435。 | 工位 D `d434-mcp-runtime-unknown-listfail-tools`；`scripts/test.sh --run` `engineMcpRuntimePanel.test.ts` **22/0**（既有 KEEP/D424 无重选仍绿 + 新 UNKNOWN/list-fail 无重选 2） | UNKNOWN leftover 后无需重选即 leftover tools capability-loading / 不可用；list-fail leftover 后无需重选即 leftover tools failed；首拉 UNKNOWN 仍清行。未关 D16。 | conversation / engine-mcp-runtime | closed |
| D435 | P3 | **closed** Agents leftover after capability UNKNOWN / list-fail 不关 Tools 页 leftover chrome：D433 只收 AGENTS.md Save。KEEP `applyDisconnectedRefresh` 在 leftover tools 时会 `renderAgentTools()`（`canEditAgentTools()` / checkbox 跟 `canWrite()`）。本刀：UNKNOWN leftover / list throw leftover 在 `closeLeftoverAgentsEditorChrome` 后若 `agentTools.length > 0` 则 `renderAgentTools()`，无需重选。首拉 UNKNOWN 仍清行。未发明 catalog 行 toggle。未重做 D433 editor chrome / D422/D423 KEEP。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D436/D437/D438。 | 工位 A `d435-agents-unknown-listfail-tools-chrome`；`scripts/test.sh --run` `engineCatalogSections.test.ts` **107/0**（既有 KEEP/D433 无重选仍绿 + 新 2 条 UNKNOWN/list-fail Tools chrome 无重选） | UNKNOWN / list-fail leftover 后无需重选即见 Tools 页写 chrome 关 + 0 extra `listTools` / 0 tools save unary。未关 D16。 | conversation / engine-agents | closed |
| D436 | P3 | **closed** MCP Runtime leftover after capability UNKNOWN / list-fail 期间 in-flight `getMcpServerTools` 仍可 `renderTools()` 当 live：D434 只在 `refresh()` UNKNOWN / list throw 画 leftover tools 诚实态。本刀：`loadTools` resolve 前/后若 catalog 已是 UNKNOWN leftover 或 list-fail leftover，不 `renderTools()` 当 live；保持 leftover tools capability-loading / failed。不重做 D434 `refresh()` 无重选诚实态。不发明 listTools。首拉 UNKNOWN 仍清行。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D435/D437。 | 工位 D `d436-mcp-runtime-inflight-unknown-listfail`；`VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/engineMcpRuntimePanel.test.ts` **24/0**（既有 22 + 新 UNKNOWN/list-fail in-flight 2） | UNKNOWN leftover 期间 in-flight `getMcpServerTools` resolve 不画 live，leftover tools 仍 capability-loading；list-fail leftover 同理仍 failed。未关 D16。 | conversation / engine-mcp-runtime | closed |
| D437 | P3 | **closed** Triggers leftover after list-fail 不关写钮：`refresh()` 开头已 `updateWriteActions()`（当时仍 connected），list throw leftover 只画 failed status，不重算写钮。写门原只看 connected+pairingHold，list-fail leftover 仍像 live。本刀：catch 保 leftover 行后置 `leftoverListFailed`（mode failed）再 `updateWriteActions()`；Fire/Enable/Delete/Upsert chrome 与 handler 早退。KEEP `applyDisconnectedRefresh` 已关写钮，不重做 KEEP。0 Fire/Enable/Delete/Upsert unary。未发明新 RPC。未改 `engineCatalogSections.test.ts`。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D435/D436/D438。 | 工位 B `d437-triggers-listfail-write-chrome`；`npm run transpile-client` 后 `scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/engineTriggersSection.test.ts` **32/0**（既有 KEEP / listed-gate 仍绿；旧「still fires」改锁关写钮） | list-fail leftover 后无需重选即见写钮关 + leftover 行仍在 + 0 Fire/Enable/Delete/Upsert unary。KEEP leftover 写钮仍关。未关 D16。 | conversation / engine-triggers | closed |
| D438 | P3 | **closed** Clipboard leftover after list-fail 不关 Write/Read/Clear：`refresh()` 开头已 `updateWriteAction` / `updateReadAction` / `updateClearAction`（当时仍 connected），list throw leftover 只画 failed status，不重算写钮。写门原只看 connected+pairingHold，list-fail leftover 仍像 live。本刀：catch 保 leftover 行后置 `leftoverListFailed`（mode failed）再重算 Write/Read/Clear chrome；handler 早退。KEEP `applyDisconnectedRefresh` 已关写钮，不重做 KEEP。0 write/read/clear unary。未发明新 RPC。未改 `engineCatalogSections.test.ts`。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D435/D436/D437。 | 工位 A `d438-clipboard-listfail-write-chrome`；`npm run transpile-client` 后 `scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/engineClipboardSection.test.ts` **27/0**（两旧测合成一条关写钮锁；既有 KEEP leftover / listed-gate 仍绿） | list-fail leftover 后无需重选即见 Write/Read/Clear 关 + leftover 行仍在 + 0 write/read/clear unary。KEEP leftover 写钮仍关。未关 D16。 | conversation / engine-clipboard | closed |
| D439 | P3 | **closed** Context Variables leftover after list-fail 不关 Read：`refresh()` 开头已 `updateReadAction()`（当时仍 connected），list throw leftover 只画 failed status，不重算 Read。读门原只看 connected+pairingHold，list-fail leftover 仍像 live。本刀：catch 保 leftover 行后置 `leftoverListFailed`（mode failed）再 `updateReadAction()`；`handleRead` 早退。KEEP `applyDisconnectedRefresh` 已关 Read，不重做 KEEP。0 `readContextVariable` unary。未发明新 RPC。未改 Snapshots / Clipboard / Triggers / MCP Runtime / `engineCatalogSections.test.ts`。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D440。 | 工位 A `d439-context-variable-listfail-read-chrome`；`npm run transpile-client` 后 `scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/engineContextVariableSection.test.ts` **14/0**（旧「still Reads」改锁关 Read；既有 KEEP leftover / pairing-hold 0 unary 仍绿） | list-fail leftover 后无需重选即见 Read 关 + leftover 行仍在 + 0 read unary。KEEP leftover Read 仍关。未关 D16。 | conversation / engine-context-variable | closed |
| D440 | P3 | **closed** Snapshots leftover after list-fail 不关 Restore/Delete：`refresh()` catch `paintListFailed` 保 leftover 行，不重算行写钮。`isSnapshotWriteLive()` 原只看 connected+pairingHold，list-fail leftover 仍像 live。本刀：catch 保 leftover 行后置 `leftoverListFailed` 再 `disableLeftoverWriteButtons()`；`isSnapshotWriteLive()` AND 该旗，Restore/Delete handler 早退。list success / `paintSnapshots` 清旗。KEEP `applyPairingHoldLeftoverRefresh` 已关写钮，不重做 KEEP。0 restore/delete unary。未发明新 RPC。未改 Context Variables / Clipboard / Triggers / MCP Runtime / `engineCatalogSections.test.ts`。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D441。 | 工位 A `d440-snapshots-listfail-write-chrome`；`npm run transpile-client` 后 `scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationEngineSnapshotsList.test.ts` **38/0**（旧「still restores and deletes」改锁关写钮；既有 KEEP leftover / pairing-hold 0 unary 仍绿） | list-fail leftover 后无需重选即见 Restore/Delete 关 + leftover 行仍在 + 0 restore/delete unary。KEEP leftover 写钮仍关。未关 D16。 | conversation / engine-snapshots | closed |
| D441 | P3 | **closed** Connection Devices leftover after list-fail 不关 Rename/Revoke/Rotate（及 Pending Confirm/Reject）：`refreshEngineDevices` catch 已置 `engineDevicesListFailed` 并 `renderHubDirectory()`→`updateDeviceActions()`，写门原只看 hasDevice+!pairingHold。`refreshEnginePending` catch 已置 `pendingPairsListFailed`，Confirm/Reject 只看 pairingHold。本刀：复用既有旗，list-fail leftover 关 Rename/Revoke/Rotate + Confirm/Reject chrome；handler 早退（不弹 dialog、0 unary）。`renderPendingPairs` 后重算 Pending chrome。KEEP pairing-hold 已关写钮，不重做 KEEP。未改成功 listed leftover still Renames。未发明新 RPC / 第二套旗。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D442。 | 工位 A `d441-connection-devices-listfail-write-chrome`；`npm run transpile-client` 后 `scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/connectionPreferencesPane.test.ts` **136/0**（文件 `test(` 仍 135，未新增测；既有 list-fail leftover 加锁关写钮；KEEP leftover / pairing-hold 0 unary / `connected leftover still Renames` 仍绿） | list-fail leftover 后无需重选即见 Rename/Revoke/Rotate 关 + leftover 行仍在 + 0 rename/revoke/rotate unary；Pending list-fail leftover 同理 Confirm/Reject 关 + 0 pair unary。KEEP leftover 写钮仍关。未关 D16。 | conversation / connection-devices | closed |
| D442 | P3 | **closed** Sources Changes leftover after git-read list-fail 不关 Stage Selected / Commit / 行 Stage：`refresh()` catch 保 leftover 行并画 `sourcesGitReadFailureMessage`，不置 fail 旗、不重算写 chrome。`isSourcesGitWriteLive` 原只看 connected+pairingHold，list-fail leftover 仍像 live。本刀：catch 保 leftover 行后置 `leftoverListFailed`；`isSourcesGitWriteLive` / `canWriteStage` / `canWriteCommit` AND 该旗；行 Stage + toolbar Stage Selected + Commit chrome 关；`tryStagePaths` / `runCommit` / `runResourceAction` stage 早退（不弹 dialog、0 `writeGitStagePaths` / `writeGitCommit` unary）。成功 `applyRefreshPresentation` / live list 清旗。KEEP pairing-hold leftover 已关写钮，不重做 KEEP。首拉 throw empty+failed 不变。未翻 Unstage 成功 leftover。未发明新 RPC / WriteGitUnstage。未改 conversation catalog（D437–D441）。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D443。 | 工位 A `d442-sources-changes-listfail-write-chrome`；`npm run transpile-client` 后 `scripts/test.sh --run src/vs/workbench/contrib/sources/test/browser/sourcesChangesList.test.ts` **21/0**（文件 `test(` 仍 20，未新增测；旧「still stages and commits」改锁关写钮；既有 KEEP leftover / pairing-hold 0 unary / Unstage 成功 leftover 仍绿） | list-fail leftover 后无需重选即见 Stage Selected / Commit 关 + leftover 行仍在 + status 仍 boom + 0 stage/commit unary。KEEP leftover 写钮仍关。未关 D16。 | sources / sources-changes | closed |
| D443 | P3 | **closed** Sources Review Accept leftover after git-read list-fail 不关 ApplyHunks helper 门：D442 已关 Stage / Commit（`leftoverListFailed` AND `isSourcesGitWriteLive`）。`canSendSourcesGitApplyHunks` / `tryWriteSourcesGitApplyHunks` 原省略 leftover（默认 false），list-fail leftover 在 Accept helper 门仍像 live。本刀：两函数补 `leftoverListFailed=false`（排在 pairingHold 后，与 Stage/Commit 同序）并传入 `isSourcesGitWriteLive` / canSend。调用方传旗则拒 Accept；git-read 可保持默认 false。Accept chrome 仍由 A1 空 payload 隐藏（`hasSourcesGitApplyHunksPayload(sessionId, [])` 恒 false）；不发明 patches / A2 / WriteGitUnstage / 新 RPC；不向 Panel / Review 灌 leftover 旗。KEEP pairing-hold Accept 已拒，不重做 KEEP。未翻 Unstage 成功 leftover。未改 `sourcesChangesList.ts` / conversation catalog（D437–D441）。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D444。 | 工位 A `d443-sources-applyhunks-listfail-write-chrome`；`npm run transpile-client` 后 `scripts/test.sh --run src/vs/workbench/contrib/sources/test/browser/sourcesChangesGitWrite.test.ts` **18/0**（文件 `test(` 仍 17，未新增测；既有 leftover 测加锁拒 Accept helper；KEEP pairing-hold 0 unary 仍绿） | leftoverListFailed=true 时 `canSendSourcesGitApplyHunks` / `tryWriteSourcesGitApplyHunks` 拒写且 0 apply unary。KEEP pairing-hold Accept 仍拒。未关 D16。 | sources / sources-apply-hunks | closed |
| D444 | P3 | **closed** Sources Changes / Review leftover after git-read list-fail 不关 ReadGitFileDiff：D442 已关 Changes Stage/Commit；D443 已关 ApplyHunks helper。Changes leftover `onDidOpen` 仍走 `openSourcesChangeEntry` → `readGitFileDiff`；`canSendSourcesGitFileDiff` / `tryReadSourcesGitFileDiff` 原省略 leftover（默认 false），leftover 行是 git-read（`gitPath`、无 `scmResource`）故 `needsSourcesGitFileDiff` 仍真，open 仍打 unary。Review leftover `onDidOpen` 同洞且无 `leftoverListFailed`。本刀：两读函数补 `leftoverListFailed=false`（排 pairingHold 后）并传入 `isSourcesGitWriteLive` / canSend。Changes `readGitFileDiff` 传 D442 旗。Review 加同义旗：catch 保 leftover 行后置 true；首拉 empty+failed / 成功 paint / pairing-hold KEEP 清旗；`readGitFileDiff` 传旗。Changes / Review `onDidOpen` leftover 早退，不清 failed status、不 mark reviewed、不走假 preview。Changes/Summary list 门仍 pairingHold-only，refresh 可重试。KEEP pairing-hold 已 0 FileDiff，不重做 KEEP。未翻 Unstage / Mark All / Mark as reviewed / Open Selected。未发明 WriteGitUnstage / A2 / 新 RPC。未改 conversation catalog（D437–D441）。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D445。 | 工位 A `d444-sources-listfail-filediff-chrome`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run` 三文件合计 **65/0**（含 `assertCleanState` 1；文件 `test(` 17+20+27=64，未新增 `test(`；既有 leftover / KEEP pairing-hold 0 FileDiff 仍绿；leftover 开行后 failed status 仍在 + Review 0 mark） | list-fail leftover 后开 leftover 行 0 FileDiff + leftover 行仍在 + failed status。KEEP pairing-hold 仍 0 FileDiff。未关 D16。 | sources / sources-filediff | closed |
| D445 | P3 | **closed** Navigator leftover after capability UNKNOWN / list·tree-fetch leftover / pairing-hold KEEP 不关 Inspect / Reveal / row-open / leftover session switch：Agents KEEP 只画 note + `liveAgentIds=undefined`，行 Inspect/Reveal `Action(..., true)` 与 hierarchy/activity `onDidOpen` 仍像 live。Team leftover KEEP 后 member/task `onDidOpen` 仍 Reveal / setTarget。Projects leftover KEEP 后 `openTreeNode` 仍 `switchSession`。本刀：leftover KEEP 置关旗并同步行 Action chrome（无需重选）；Inspect / Reveal / row-open / leftover session switch handler 早退。成功 live 清旗。首拉 UNKNOWN / 首拉 fail 仍 empty、无 leftover 行、无 leftover action chrome。既有 leftover-row KEEP + failed/stale note 不改。未改 Sources / hub Connect / inbox / engine catalog。未发明新 RPC。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。D444 Sources FileDiff leftover 由 A 同波合入。不占 D446。 | 工位 D `d445-navigator-leftover-inspect-reveal-chrome`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run` `navigatorAgentsSubviews.test.ts` + `navigatorTeamSubviews.test.ts` + `navigatorProjectsList.test.ts` **109/0**（Agents 45、Team 37、Projects 26 + harness 1；既有 leftover-row KEEP / failed·stale note 仍绿；新 12 条无重选关 chrome） | UNKNOWN / list·tree-fetch leftover / pairing-hold KEEP 后无需重选即见 leftover 行仍在 + Inspect/Reveal/row-open/leftover session switch 关 + forced click 0 `revealNavigatorAgentInConversation` / 0 `CONVERSATION_REVEAL_ITEM` / 0 leftover-as-live `switchSession`。首拉 UNKNOWN/fail 仍 empty。未关 D16。 | navigator / leftover-row-actions | closed |
| D446 | P3 | **closed** Sources KEEP pairing-hold leftover 开行仍假 preview / 可标 Review，且 Open Selected 不传 FileDiff leftover/pairing 门：D444 只关 list-fail leftover `onDidOpen`（`leftoverListFailed`），KEEP pairing-hold leftover 清该旗后开行仍走 `openSourcesChangeEntry` → FileDiff 拒后假 preview；Review `markReviewedAfterSuccessfulOpen` 当成功。Open Selected 不传 `readGitFileDiff` / leftover 旗，leftover 行同样假 preview + 可标已审。本刀：`shouldSkipSourcesGitFileDiffOpen(leftoverListFailed, pairingHoldLeftover)`；Changes / Review `onDidOpen` 与 Open Selected 共用（host `isSourcesGitFileDiffOpenSkipped` + `readGitFileDiff`）。leftover → 0 FileDiff、不假 preview、Review 不标已审、failed/pairing status 仍在。未翻 Unstage / Mark All / Conversation。未发明 WriteGitUnstage / A2 / 新 RPC。未改 navigator / hub / inbox。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D447。 | 工位 A `d446-sources-leftover-keep-open`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run` 五文件 **87/0**（含 `assertCleanState` 1；既有 D442/D444 leftover list-fail 仍绿）。D443 写门文件未改、未重跑。 | KEEP pairing-hold leftover 开行 0 FileDiff + 不假 preview + Review 0 mark + pairing status 仍在。Open Selected leftover 同门：0 unary、不假 preview、不标已审。未关 D16。 | sources / sources-filediff-open | closed |
| D447 | P3 | **closed** Conversation leftover keep-last 三洞：roster 成功 list 后再 `listSessions` throw 走 `markEngineSessionBindFailed` 把 leftover 卸成 bind-failed-only；Hub leftover 在 `refreshDirectory` fail 后 `applyHubDirectoryRefreshFailure` 不 splice，`handleConnectDevice` 无门，Connect 仍像 live；Inbox leftover-disconnect `bindLeftoverQueueWrite` 只看 pairing-hold，Pause 仍可点。本刀：list-fail leftover KEEP 末次行 + failed（`createSession` / tree refresh 0）；首拉 throw 仍 bind-failed 占位（D25）。Hub leftover 置 `hubDirectoryListFailed` 并 splice，Connect 无需重选即关 + handler 早退 0 unary。Inbox leftover-disconnect Pause/Resume/Clear 关 + forced click 0。未改 Sources / Navigator / D446。未发明新 RPC。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。 | 工位 D `d447-conversation-leftover-keep`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run` 三文件 **287/0**（roster `test(` 109、pane 135、inbox 42 + harness 1；既有首拉 fail / KEEP leftover / pairing-hold 仍绿） | leftover list-fail 保末次会话行 + `isEngineSessionReady()===false` + 0 Create/tree；Hub leftover Connect 关 + 0 `addHubDeviceProfile`；Inbox leftover-disconnect Pause 关 + 0 pause。首拉 fail 合同不变。未关 D16。 | conversation / leftover-keep | closed |
| D448 | P3 | **closed** Sources leftover after git-read list-fail 不关 Changes Unstage 与 Review Mark：D442 已关 Stage/Commit；D444/D446 已关 FileDiff 开行。Unstage 工具栏 / 行 / `runResourceAction` 原只 AND pairing-hold，list-fail leftover 仍像 live。Review Mark as reviewed / Mark All / 命令原不看 `leftoverListFailed`。本刀：Unstage chrome 与 handler 对齐 Stage leftover 闸；Review 行钮 / Mark All / 右键 / `toggleReviewedSelected` / `markAllReviewed` / 命令 AND 该旗并早退。leftover → Unstage / Mark chrome 关；force-click 0 `git.unstage`、0 `markReviewed` / `markAllReviewed`。KEEP pairing-hold Unstage 已关，不重做 KEEP。接通非 leftover Unstage/Mark 仍活。未发明 WriteGitUnstage / 新 RPC。未改 Navigator / Conversation / draft plan。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D449（D 槽占用）。 | 工位 A `sources-leftover-write-chrome`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run` 两文件 **52/0**（含 `assertCleanState` 1；Changes `test(` 21、Review `test(` 30；既有 Stage/Commit/FileDiff leftover 与 pairing-hold Unstage / 接通 Unstage 仍绿） | leftoverListFailed 后 Unstage / Mark chrome 关 + leftover 行仍在 + 0 `git.unstage` / 0 mark。KEEP pairing-hold Unstage 仍关。接通非 leftover Unstage/Mark 仍活。未关 D16。 | sources / leftover-unstage-mark | closed |
| D449 | P3 | **closed** D447 KEEP leftover 不是活写面：`hasLeftoverEngineCatalogListFailed()` 已挡 `createSession`；`renameSession`/`deleteSession` 及同文件 pairingHold→接通后 `dispatchEngineAction` 的 mutator 仍像 live；SessionBar `updateSessionBarWriteChrome` 只看 pairing-hold；`isDeviceWriteListFailed()` 只看 `engineDevicesListFailed`，Hub leftover 行 Rename/Revoke 仍开。本刀：上述 mutator 接同一 leftover helper（false + 0 unary）；SessionBar KEEP leftover 关 title/Delete/New；`isDeviceWriteListFailed` 含 `hubDirectoryListFailed`。未发明新 RPC。未实施 conversation-timeline-session-pills / session-subscription-lifecycle。未改 Sources / Navigator / draft plan。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D448（A 占用）。 | 工位 D `conversation-leftover-keep-writes`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run` 三文件 **322/0**（roster `test(` 110、dispose 78、pane 138 + harness；既有 pairing-hold 0 unary / createSession KEEP / Connect leftover / `connected leftover still Renames` 仍绿） | KEEP leftover rename/delete（及同批 mutator）false + 0 unary；SessionBar 写 chrome 关；hubDirectoryListFailed leftover Rename/Revoke 关 + 0 hub/engine revoke unary。未关 D16。 | conversation / leftover-keep | closed |
| D450 | P3 | **closed** D449 已闸 SessionBar KEEP leftover（`connected && isEngineSessionReady()===false`）；composer chrome 仍只看 pairing-hold。connected + `!pairingHold` + `isEngineSessionReady()===false` 会像活 Composer 写面（Send 开、gate 藏成 live、Permission/Agent/Model 可写）。本刀：`isComposerSessionWriteLive` / `updateSendEnabled` / `updateGateRow` / `applySessionPermissionIndex` / `applySessionModelIndex` 接同一判定。夹具 `pairingPending===false` 且 `isEngineSessionReady===false`（既有 leftover-looks-live 是 pairingPending true，盖不住本洞）。未改 Inbox / Timeline / SessionBar / Sources / Hub / Navigator。未发明新 RPC。未实施 conversation-timeline-session-pills / session-subscription-lifecycle。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D451（D 占用）。 | 工位 A `d450-composer-leftover-keep-writes`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run` 两文件 **98/0**（harness 1、dispose `test(` 78、catalog `test(` 19；既有 leftover-looks-live pairing-hold 选框仍绿；接通 `isEngineSessionReady` true 仍可写） | leftover list-fail：Send 关；gate 不藏成 live；Permission/Agent/Model 关；apply 含 force 仍 0 `setPermissionMode`/`switchModel`，index 回 leftover。pairing-hold 选框测仍绿；接通 `isEngineSessionReady` true 仍可写。未关 D16。 | conversation / composer-dock | closed |
| D451 | P3 | **closed** KEEP leftover list-fail 下 Inbox Goal / Stop / Enqueue 仍像活写面：D449 已让 roster mutator 早退，overlay chrome 仍只看 pairing-hold + `isEngineConnected()`。`connected && pairingPending===false && isEngineSessionReady()===false` 时 leftover streaming 仍开 Stop，Goal/Enqueue 仍开 quickInput。本刀：`isSessionGoalAvailable` / `isGenerating` / `isLeftoverQueueWriteLive` / `shouldRejectEnqueueWrite` / `renderEnqueueAction` / `renderStop` 接同一 leftover 闸；打开 quickInput 之前拒写；force-click 0 `setSessionGoal` / `cancelGeneration` / `enqueue`。未改 connected queue unlisted。未重做 D447 Pause leftover-disconnect。未改 Composer dock / Timeline / SessionBar / Sources / Hub / Navigator。未发明新 RPC。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D450（A 占用）。 | 工位 D `d451-inbox-leftover-keep-writes`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationInboxOverlay.test.ts` **44/0**（`test(` 43 + harness；新 KEEP leftover list-fail 测：connected、pairingPending false、`isEngineSessionReady` false；既有 pairing-hold remaining writes / leftover-disconnect Enqueue / connected queue unlisted / 接通 ready Goal/Stop 仍绿） | leftover list-fail：Goal/Stop/Enqueue 无需重选即关；点击不弹 quickInput；force-click 0 写。leftover streaming Stop 仍关。pairing-hold remaining writes 仍绿；接通 ready 时 Goal/Stop 仍活。未关 D16。 | conversation / inbox | closed |
| D452 | P3 | **closed** KEEP leftover list-fail 下 Timeline Allow/Skip/Retry/Delete + user-bubble 仍像活写面：D449 SessionBar / D450 Composer 已闸 `connected && isEngineSessionReady()===false`，阅读列 `writesEnabled` 仍只看 pairing-hold。本刀：`isReadingColumnWritesEnabled` 接同一 leftover 谓词；user-bubble 点击 `!writesEnabled()` 早退；`beginTurnEdit` KEEP leftover 早退（既有 `isComposerKeepLeftoverListFailed`）。DoD chrome 由 `writesEnabled:false` 座/Retry 测覆盖。未改 conversationSessionsView（D453，D 槽占用/排队合入 `b53f9ec6624`；A 无该提交，不把 D453 改写成 open）。未发明新 RPC。未实施 conversation-timeline-session-pills / session-subscription-lifecycle。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。 | 工位 A `d452-timeline-leftover-keep-writes`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run` 两文件 **93/0**（harness 1、disposeGate `test(` 81、timelineRenderer `test(` 11；KEEP leftover `writesEnabled` false + `beginTurnEdit`；pairing-hold false；ready true；Confirmation/Question `writesEnabled:false`；user-bubble false 0 `onEditUserTurn`、true 仍调用） | leftover list-fail：Timeline Allow/Skip/Retry/Delete + user-bubble 不是活写面。pairing-hold 仍关写；接通 ready 仍可写。未关 D16。D453 由 D 槽占用/排队合入。 | conversation / timeline | closed |
| D453 | P3 | **closed** KEEP leftover list-fail 下 Sessions 侧栏 New/Delete 仍像活写面：D449 已让 roster mutator 早退；`updateDeleteEnabledContextKey` / `createNewSession` / `deleteActiveSession` 仍只看 pairing-hold。`connected && pairingPending===false && isEngineSessionReady()===false` 时 Delete chrome 仍开；`createSession` 回空串不弹 notice。本刀：三处接同一 leftover 闸；Delete context key false、ViewTitle aria-disabled；调用 createSession/deleteSession 之前拒写+既有 disconnected notice。未重做 D305/D306 pairing-hold。未改 Timeline / Composer / Inbox / SessionBar / Sources / Hub / Navigator。未发明新 RPC。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D452（A 占用）。 | 工位 D `d453-sessions-view-leftover-keep-writes`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationSessionsView.test.ts` **37/0**（`test(` 36 + harness；新 KEEP leftover list-fail 测：connected、pairingPending false、`isEngineSessionReady` false；既有 leftover-looks-live pairing-hold Delete / 接通 ready New/Delete 仍绿） | leftover list-fail：Delete context key false、ViewTitle Delete aria-disabled；createNewSession/deleteActiveSession 既有 disconnected notice、0 createSession/deleteSession、行不变。leftover-looks-live pairing-hold Delete 测仍绿；接通 ready 时 New/Delete 仍活。未关 D16。 | conversation / sessions-view | closed |
| D454 | P3 | **closed** KEEP leftover list-fail 下 SessionBar 处理函数仍像活写面：D449 已关 title/Delete/New **chrome**（`updateSessionBarWriteChrome` leftover 谓词）；`beginSessionTitleEdit` / `commitSessionTitleEdit` / `createNewSession` / `deleteActiveSession` 仍只看 pairing-hold。`connected && pairingPending===false && isEngineSessionReady()===false` 时 force-click / 直接调仍进 title-edit 并打到 `createSession`/`deleteSession`/`renameSession`。本刀：四函数接与 D449 chrome 相同 leftover 闸；leftover 不进 title-edit；New/Delete/commit 在 roster 写之前既有 disconnected notice。未改 Timeline / Inbox overlay / Sessions 侧栏 / Navigator / Composer dock。未实施 session-pills。未发明新 RPC。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D455（D 占用 Navigator leftover Refresh/switch）。 | 工位 A `d454-sessionbar-leftover-handlers`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationLensDisposeGate.test.ts` **85/0**（harness 1、`test(` 84；新 KEEP leftover handler：connected、pairingPending false、`isEngineSessionReady` false；既有 pairing-hold chrome / leftover-looks-live chrome / 接通 ready chrome 仍绿） | leftover list-fail：begin 不进编辑；create/delete/commit 拒写+notice、0 活 roster 写。pairing-hold 测仍绿；接通 ready 仍可写。未关 D16。 | conversation / session-bar | closed |
| D455 | P3 | **closed** KEEP leftover list-fail 下 Navigator Refresh 与 Projects leftover session 切会话仍像活写面：D449 谓词 `connected && isEngineSessionReady()===false`；pairingPending false。Agents `refreshAgentTree` / `updateEngineConnectedContextKey` 原只看 pairing-hold+connected；Projects `isProjectsSessionSwitchLive` 原只看 `leftoverSessionSwitchClosed`（live rebuild 里 `!engineConnected \|\| sessionList UNKNOWN`），capability 仍 SUPPORTED 时 leftover 行仍 `switchSession`。本刀：两处接同一 leftover 闸；Refresh chrome 关；handler 早退 0 `requestAgentTreeRefresh` / 0 `switchSession`。未重做 D445 Inspect/Reveal。未改 leftover 状态 glyph。未改 Inbox / SessionBar / Timeline / Sessions / Sources / Hub。未发明新 RPC。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D454（A 占用 SessionBar）。 | 工位 D `d455-navigator-leftover-refresh-switch`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run` 两文件 **75/0**（含 `assertCleanState` 1；Agents `test(` 46、Projects `test(` 28；新 KEEP leftover list-fail 测：connected、pairingPending false、`isEngineSessionReady` false；既有 pairing-hold Refresh 0 unary / 接通 ready Refresh / live session switch 仍绿） | leftover list-fail：Refresh context key false、ViewTitle aria-disabled；`refreshAgentTree` 0 `requestAgentTreeRefresh`；Projects leftover session 行 force-open 0 `switchSession`。pairing-hold Refresh 0 unary 仍绿；接通 ready Refresh / live session switch 仍活。未关 D16。 | navigator / leftover-keep-writes | closed |
| D456 | P3 | **closed** KEEP leftover list-fail 下 Fork / Kill 仍像活写面：`tryConnectedEngineFork` / `tryKillSubAgent` 只闸 `isConversationPairingHold`，随后 `isEngineConnected()` 仍调 `forkSubAgent`/`killSubAgent`。谓词与 D449/D454 相同：`isEngineConnected()===true && pairingPending===false && isEngineSessionReady()===false`（不是 leftover-looks-live pairing-hold）。Roster `hasLeftoverEngineCatalogListFailed()` 已让 leftover mutator 回 false，但 UI 仍像活写：打到 roster 并弹 **failed** copy。本刀：两函数接同一 leftover 闸（roster `isEngineSessionReady?.()===false` + connected）；leftover 有 connection history 则 pairing-hold 同类 disconnected notice、return handled、**0** fork/kill。未发明第三套旗 / 新 RPC。未改 Inbox / SessionBar / Timeline / Sessions / Composer / Navigator / Sources / Hub。未实施 conversation-timeline-session-pills / session-subscription-lifecycle。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D457（D 占用 Navigator leftover Inspect/Reveal + Refresh context key）。 | 工位 A `d456-fork-kill-leftover-handlers`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run` 两文件 **15/0**（含 `assertCleanState` 1；Fork `test(` 8、Kill `test(` 6；新 KEEP leftover list-fail 测：connected、pairingPending false、`isEngineSessionReady` false、0 `forkSubAgent`/`killSubAgent`；既有 leftover-looks-live pairing-hold / 接通 ready fork·kill 仍绿） | leftover list-fail：disconnected notice + 0 `forkSubAgent` / 0 `killSubAgent`。既有 leftover-looks-live pairing-hold 仍绿；接通 `isEngineSessionReady===true` 仍可 fork/kill。未关 D16。 | conversation / fork-kill leftover | closed |
| D457 | P3 | **closed** KEEP leftover list-fail 下 Navigator Agents/Team leftover 行 Inspect/Reveal/row-open 仍像活写面，且 Agents Refresh context key 在 roster leftover 翻转后滞后：D445 已关 tree-fetch / pairing-hold leftover Inspect；D455 已闸 `refreshAgentTree` unary，context key 原只订 connection 事件。KEEP leftover（`isEngineConnected()===true && pairingPending===false && isEngineSessionReady()===false`）时 `engineReady` 仍 true，行仍像 live Inspect。本刀：Agents/Team `is*RowActionLive` 接 D455 同一 leftover 谓词（chrome + handler 早退）；Refresh context key 订 `onDidChangeSession` 重算。未重做 D445 pairing-hold/tree-fetch 路径、未重做 D455 Refresh handler / Projects leftover `switchSession`。未改 Inbox / SessionBar / Timeline / Sessions / Sources / Hub / glyph / draft plans。未发明新 RPC。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D456（A 占用 Fork/Kill leftover handlers）。 | 工位 D `d457-navigator-leftover-inspect-refresh-chrome`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run` 两文件 **86/0**（含 `assertCleanState` 1；Agents `test(` 47、Team `test(` 38；新 KEEP leftover list-fail 测：connected、pairingPending false、`isEngineSessionReady` false；既有 pairing-hold / D445 tree-fetch leftover / D455 Refresh 0 unary / 接通 ready 仍绿） | leftover list-fail 后无需等 connection 事件：Inspect/Reveal/row-open chrome 关 + force 0 `revealNavigatorAgentInConversation` / 0 `CONVERSATION_REVEAL_ITEM` / 0 Team setTarget；Refresh context key false。pairing-hold / D445 tree-fetch leftover / D455 Refresh 0 unary / 接通 ready 仍绿。未关 D16。 | navigator / leftover-keep-writes | closed |
| D458 | P3 | **closed** KEEP leftover list-fail 下 Create Snapshot 与 Snapshots 浮层 Restore/Delete 仍像活写面：`shouldHoldCreateSnapshotWrite` / `tryCreateSnapshotAfterPrompt` 只闸 pairing-hold；KEEP leftover 仍 `canCreateEngineSnapshot(true,…)` 并弹 title prompt，roster leftover 回 false 会弹 **failed**。`isSnapshotWriteLive` 只看 connected+pairingHold+`leftoverListFailed`，不看 `isEngineSessionReady`。谓词与 D449/D456 相同：`isEngineConnected()===true && pairingPending===false && isEngineSessionReady()===false`（不是 leftover-looks-live pairing-hold）。本刀：Create Snapshot 闸 leftover（prompt 前 + after-prompt）；有 history → disconnected notice、0 `createSnapshot`。`isSnapshotWriteLive` 接同一 leftover 谓词（roster `isEngineSessionReady?.()===false`）；浮层 leftover Restore/Delete chrome 关、force 0 restore/delete。未发明第三套旗 / 新 RPC。未改 Inbox / SessionBar / Timeline / Sessions / Composer / Fork-Kill / Navigator / Sources / Hub。未实施 conversation-timeline-session-pills / session-subscription-lifecycle。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D459（D 占用 Sources leftover）。 | 工位 A `d458-snapshot-leftover-keep-writes`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run` 两文件 **52/0**（含 `assertCleanState` 1；Create Snapshot `test(` 12、Snapshots `test(` 39；新 KEEP leftover list-fail 测：connected、pairingPending false、`isEngineSessionReady` false、0 create / 0 restore/delete；既有 pairing-hold / leftover-looks-live / D440 list-fail leftover / 接通 ready 仍绿） | leftover list-fail：Create Snapshot 不弹 title prompt、disconnected notice、0 `createSnapshot`；after-prompt 同样 0 create + disconnected notice。浮层 Restore/Delete chrome 关、force 0 restore/delete。pairing-hold / leftover-looks-live / D440 list-fail leftover 仍绿；接通 `isEngineSessionReady===true` 仍可写。未关 D16。 | conversation / snapshots leftover | closed |
| D459 | P3 | **closed** KEEP leftover list-fail 下 Sources Changes/Review/Diff 写面仍像活写面：`isSourcesGitWriteLive` 原只看 connected+pairingHold+leftoverListFailed，**不看** session leftover。谓词与 D449 相同：`isEngineConnected()===true && pairingPending===false && isEngineSessionReady()===false`。KEEP leftover 时 git-read 可仍成功（`leftoverListFailed===false`），Stage/Commit 仍像 live 并对 leftover session 打 engine git 写。本刀：`isSourcesGitWriteLive` 增参 `engineSessionReady=true` 默认；Changes/Review/Diff chrome + handler 共用 `isSourcesKeepLeftoverWrite`。Stage / Commit / Unstage / Mark / Diff Stage·Revert·Unstage chrome 关；force 0 `writeGitStagePaths` / `writeGitCommit` / `git.unstage` / `markReviewed` / `markAllReviewed`（及 Diff 对应写）。Open Selected 的 mark 回调接与 list `onDidOpen` 同一 KEEP leftover 写关（host `isSourcesGitWriteClosed`）；FileDiff 读门仍只走 D444/D446，不并 session leftover。未重做 git-read leftoverListFailed 路径。未发明 WriteGitUnstage / A2 / 新 RPC。未改 Conversation / Navigator / Hub / Inbox / draft plans。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。不占 D458（A 占用 conversation snapshots leftover）。 | 工位 D `d459-sources-leftover-session-writes`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run` 六文件 **109/0**（含 `assertCleanState` 1；Review `test(` 32、showForPaths `test(` 10、Changes leftover 16 + open 7、gitWrite `test(` 17、Diff `test(` 15、wiringScan 11；新 Open Selected KEEP leftover：connected、pairingPending false、`isEngineSessionReady` false、`leftoverListFailed` 仍 false → FileDiff 仍开、`markReviewed===0`；既有 pairing-hold / leftoverListFailed / 接通 ready 仍绿） | leftover list-fail：Stage/Commit/Unstage/Mark/Diff Stage·Revert·Unstage chrome 关 + 0 对应写。Open Selected KEEP leftover FileDiff 仍开且不 mark。pairing-hold / D442–D448 leftoverListFailed 仍绿；接通 ready 仍可写。未关 D16。 | sources / leftover-keep-writes | closed |
| D460 | P3 | **closed** KEEP leftover list-fail 下 session-binding 会话级写面仍像活写：D452 已关 Timeline `writesEnabled` chrome；`resolveConfirmation` / `resolveQuestion` / `deleteTurn` / `cancelToolCall` / `retryError` 处理函数只闸 `rejectPairingHoldWrite`。谓词与 D449 相同：`isEngineConnected()===true && pairingPending===false && isEngineSessionReady()===false`（不是 leftover-looks-live pairing-hold）。KEEP leftover 时 force-click / 直接调仍打 unary / `postBound continueGeneration`。本刀：五函数接同一 `rejectKeepLeftoverListFailWrite`；leftover → `engine_disconnected` notice、0 unary / 0 postBound。`copyTurn` 本地剪贴板、`openVisualizeOverlay` 读面不闸。未重做 Timeline chrome。未闸引擎全局 catalog / Connection Hub / Inbox Hold-Retry / FileDiff 读门 / draft plans。未发明新 RPC。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。未拆 D461。 | 工位 A `d460-permission-question-session-binding-leftover`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationLensDisposeGate.test.ts` **96/0**（harness 1、`test(` 95；新 KEEP leftover：connected、pairingPending false、`isEngineSessionReady` false、0 unary/postBound；既有 pairing-hold / leftover-looks-live / 接通 ready 仍绿） | leftover list-fail：confirm/question/deleteTurn/cancelToolCall/retryError 拒写+disconnected notice、0 unary / 0 postBound。pairing-hold / leftover-looks-live 仍绿；接通 `isEngineSessionReady===true` 仍可写。未关 D16。 | conversation / session-binding leftover | closed |
| D461 | P3 | **closed** D461 扫过：无剩余会话级 leftover 写面。谓词 `isEngineConnected()===true && pairingPending===false && isEngineSessionReady()===false`。候选已按代码核对、否掉：Inbox Pause/Resume/Clear 已走 D451 `isLeftoverQueueWriteLive`；Hold-Retry 禁止闸且接通 leftover 队列 unlisted 不画行；pending 钮只滚座。Subagent overlay Promote/breadcrumb/maximize 为本地窗导航；嵌套 Lens 复用 D449–D460 闸。History overlay 仅 `getHistory` 读面。Permission/Question/Retry/Delete/Cancel chrome 已 `writesEnabled`（D452）+ handler（D460）。SessionBar/Sessions `switchSession` 在 list-fail KEEP（`engineSessionBindFailed`）不打 `requestAgentTreeRefresh`，属 leftover 行间导航。未改 prod。未闸 catalog / Hub / Hold-Retry / FileDiff 读门 / copyTurn / visualize / Navigator glyph / draft plans。未发明新 RPC。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。 | 工位 A `d461-session-leftover-write-scan`；无 prod；未跑 `transpile-client` / `scripts/test.sh` | 代码扫：无剩余会话级 leftover 写洞。不得宣称 leftover wave 完成。 | conversation / leftover-keep-writes | closed |
| D462 | P3 | **closed** KEEP leftover list-fail 下 Composer **处理函数** twin 仍像活写面：D450 已关 Send/gate chrome；`submitDraft` / `saveTurnEdit` / `saveQueueEdit` 只闸 `rejectPairingHoldWrite`。谓词与 D449/D460 相同：`isEngineConnected()===true && pairingPending===false && isEngineSessionReady()===false`（不是 leftover-looks-live pairing-hold）。KEEP leftover 时 `submitDraft` 仍 `postBound` 回 `no_such_session`；turn/queue edit 仍打 `updateUserTurnText` / queue 写。本刀：三函数接同一 `rejectKeepLeftoverListFailWrite`；leftover → `engine_disconnected`、**0 `postBound`** / 0 turn 写 / 0 queue 写。未重做 `updateSendEnabled` / gate chrome（D450）。未闸 catalog / Hub / Inbox Hold-Retry / FileDiff / draft plans。未发明新 RPC。未关 D8/D16/D147/D405。未升 PRD-008。不得宣称 leftover wave 完成。 | 工位 A `d462-composer-leftover-handlers`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run` 两文件 **125/0**（harness 1、dispose `test(` 104、catalog `test(` 20；新 KEEP leftover handler：connected、pairingPending false、`isEngineSessionReady` false、0 postBound / 0 updateUserTurnText / 0 queue 写；既有 pairing-hold / leftover-looks-live / D450 Send chrome / 接通 ready 仍绿） | leftover：0 postBound / 0 updateUserTurnText / 0 queue 写 + disconnected notice。pairing-hold / leftover-looks-live / 接通 ready 仍绿。未关 D16。 | conversation / composer leftover | closed |
| D463 | P3 | **closed** `openStream` remote/error `onClosed` 在 post `streamClosed` 后 `streams.delete(key)`（同步 throw 同样删）。不在 onClosed 本地 dispose（Actor `closeStream` 仍关订阅）。S2 新 attempt 键不再留旧 `ActiveStream`。未改 `node/sessionCore/**`；未扩 S4a/S4b。 | 工位 D `d463-stream-handle-recycle`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run` 四文件 **35/0**（harness 1、reopen 9、intent 11、actor-loop 6、chat 8；新 remote/error close 不含已关 attempt；既有 reopen/S4b/throw-on-open 仍绿） | remote/error `onClosed` 后该 attempt 不在 `streams`；连续失败重开 `streams.size===1`；S4b fail-closed 仍绿 | universeAgent / session-lifecycle | closed |
| D464 | P3 | **Composer `@` 复用同一套会话 pill**：产品方向已选定，宿主与发送合同不在 S1–S4。 | textarea 画不出行内 `<a>`；PRD-015 用户卡纯文本未改 | 另开切片：先拍输入宿主（chip vs contenteditable）与发出载荷，再动 PRD-015；复用 `conversationSessionPill` + 同一 resolve | conversation / composer | open |
| D465 | P3 | **sideChat 不画 pill**：catalog `sideChat` / 非根 `user` 本期不装饰、不跳。协议与 session-windows §3.3b 仍有效。 | 打开形态未在 pill 切片展开，避免猜 tool/fork | 另开方案：sideChat 按已签收对话框合同画 pill 并走 `openSubAgent` | conversation / session-pills | open |
| D466 | P3 | **closed** merge 槽 `npm run compile` 0 @ `36f5f2efbf3`（首轮 5 个 TS 为 merge fallout，已修）。本会话只允许一路编译，未跑 valid-layers-check / 全仓 eslint。 | `/tmp/merge-compile-401f668.log` | compile 0；不宣称 leftover wave 完成 | infra | closed |
| D467 | P3 | **已删除会话的隐藏叶 DOM / part 永不回收**（lease 已释放）。 | 设上限会丢 tab，违 PRD-016 验收 2；本期拒绝 LRU dispose | 另开 session-windows 修订再谈回收策略；不得在 pill 切片加上限 | conversation / session-windows | open |
| D468 | P2 | **closed** merge compile 后复跑点名 mocha **151 passing / 0 fail**（pills+reveal+side-by-side+import 34 + lens+catalog 116；与先前 35/117 差在 `assertCleanState` 计法）。禁止为绿删断言。 | `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run` 六文件；`/tmp/merge-mocha-d468.log` | 0 fail；未关 D8/D16/D147/D405 | conversation / session-pills | closed |
| D469 | P2 | **closed** `onDidChangeActiveSession` 生产 listener 只挡 `ENGINE_BIND_FAILED_SESSION_ID` + `getSessions()` miss，**不**调用 `isEngineRosterPlaceholderSessionId`（该谓词含 stub 种子 `untitled` / `visualize`）。方案 §3.4 / S3 / 测试 15 / 风险表改口对齐。mocha 锁 `fireActiveSession('untitled')` 仍 reveal。未拆谓词。未关 D8/D16/D147/D405。未升 PRD-008。 | 工位 A `d469-d470-pills-listener-lease`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationSessionWindowReveal.test.ts` **12/0**（harness 1、`test(` 11；新 untitled listener reveal） | stub 种子 untitled listener 会 reveal；bind-failed 仍不建叶。未关 D16。 | conversation / session-pills | closed |
| D470 | P2 | **closed** 测试 17 遍历叶 `activeGroup.editors`，对每个 tab `openEditor` 后取 `ConversationEditorPane.activeConversationLens`，隐藏后根 + fork 的 `sessionViewLease` 均 undefined，restore 后均重取。未发明 pane 大 API。未关 D8/D16/D147/D405。未升 PRD-008。 | 工位 A `d469-d470-pills-listener-lease`；同上 mocha **12/0** | 隐藏叶所有 tab 透镜 lease 释放；restore 重取。未关 D16。 | conversation / session-pills | closed |
| D471 | P3 | **closed** 同 Electron 进程组合 mocha：Sessions 已接 `stubConversationTimelineLinkServices` + `TestEditorService`（无 `createEditorParts`）。本刀：`conversationSessionSplit.test.ts` / `conversationEditorFence.test.ts` 按 reveal/sxs 接真 `EditorService`（有 `createEditorParts`）+ `stubConversationLensRuntimeServices`，`waitForPane` 等到 `ConversationEditorPane.activeConversationLens` 再测，dispose 后不再建透镜。未改生产合同。未重做 Sessions。pills 假 `openEditor` 未扩。`conversationSessionChat.test.ts` 仍用测试 pane。不得宣称 leftover/pills 完成。 | 工位 D `d471-split-fence-harness`；`npm run transpile-client` 后 split **6/0**、fence **9/0**（各含 `assertCleanState` 1） | split/fence mocha 绿；不得宣称 leftover/pills 完成 | conversation / test-harness | closed |
| D472 | P2 | **closed** stub/fixture 字面 `/session/<key>/chat/<id>` 在投影助手 `ConversationStubTurn.text` 时（`timelineItemToEntry` / stub `getTurns`）改写；adapter 不再改 href/text。不猜 live-engine（R9 仍开）。无新 RPC。未升 PRD-008。 | merge `43757006bbe` compile-client 0；四文件 mocha **46 passing / 0 fail**（harness 1、sessionView 19、rewrite+getTurns 7、pills 15、import 4） | catalog 命中改写且 pill 可点；miss 原文；adapter 不 import rewrite helper。R9 仍开。未关 D16。不得宣称 pills 全方案完成 | conversation / session-pills | closed |
| D473 | P3 | **closed** D174 残留：context view 选 comment 的 `void revealFeedback` 无 catch。本刀只补 `agentFeedbackContextView.ts` `.catch(onUnexpectedError)`，对齐 widget 单条 click。测锁 context view 选中 comment → stub reject → 0 unhandled rejection。不重开 D174。未扫其它 sessions void。未 compile-client。不得宣称 leftover/pills 完成。 | 工位 D `agent-feedback-context-view-reveal-catch`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/sessions/contrib/agentFeedback/test/browser/agentFeedbackAttachment.test.ts` **7/0** | context view 选 comment reject 无未处理 rejection；既有 6 条仍绿。不重开 D174。 | sessions / agentFeedback | closed |
| D474 | P3 | **closed** D180/D182 残留：`automationsView.ts` 四处 `void` 无 catch（`runNow` / `openEditDialog` / `markAllRunsRead` / `onSessionOpen`→`openRunSession`）。本刀四处补 `.catch(onUnexpectedError)`，对齐已有 delete/create。mocha 锁 runNow：`whenDispatched` reject 且 `dialogService.error` throw → 0 unhandled（`runNow` 内部 try/catch，单 stub inner reject 不漏）。其余三处内部亦有 try/catch，现有 harness inner stub 不漏 unhandled，调用点 catch 已补、未另写 mocha。不重开 D180/D182。未扫其它 sessions void。未碰 grpc / conversation pills / catalog onOpenConnection。未 compile-client。不得宣称 leftover/pills 完成。 | 工位 D `automations-remaining-void-catch`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/sessions/contrib/sessions/test/browser/automationsView.test.ts` **56/0** | 四处 catch；runNow stub reject 0 unhandled；D180/D182 测仍绿。不重开 D180/D182。未关 D8/D16/D147/D24/D405。 | sessions / automations | closed |
| D475 | P3 | **closed** D186 残留：`cloudSandboxAgentHostContribution.ts` 两处 `void this._disconnectEnvironment(address)` 无 catch（teardown ~431；connect 取消 ~657）。本刀两处补 `.catch(onUnexpectedError)`。未改 `_disconnectEnvironment` 方法体既有 try/catch。mocha 锁 teardown：发现负向调和 `removeRemoteAgentHost` reject 且 `logService.warn` throw → 0 unhandled（方法体内 try/catch，单 stub inner reject 不漏）。connect 取消：现有 harness 经 `onConnect` 关设置（`setUserConfiguration` 不 fire 配置事件，故不走 `_teardownAll`）同 warn-再抛路径另锁 0 unhandled。不重开 D186。未扫其它 sessions void。未碰 grpc / conversation pills。未 compile-client。不得宣称 leftover/pills 完成。 | 工位 D `sandbox-disconnect-void-catch`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/sessions/contrib/providers/remoteAgentHost/test/browser/cloudSandboxAgentHostContribution.test.ts` **17/0**（含 `assertCleanState` 1；既有 15 + teardown / connect-cancel 各 1） | 两处 catch；teardown + connect 取消 stub reject 0 unhandled；D186 seed 测仍绿。不重开 D186。未关 D8/D16/D147/D24/D405。 | sessions / cloudSandbox | closed |
| D476 | P3 | **closed** `sessionChangesEditor.ts` `handleHeaderMiddleClick` 的 `void executeCommand(CHANGESET_REVIEW_ACTION_ID)` 无 catch。本刀补 `.catch(onUnexpectedError)`。mocha 锁：review capability + 有效 resource stats 后门控通过，`executeCommand` reject → 0 unhandled，`handleHeaderMiddleClick` 仍 true。现有 editor-input harness 驱不动 middle-click 门控，测侧最小 stub `SessionChangesUIElementFactory`（export 仅供该测）。未改 Changes 生产合同。未扫其它 sessions void。未碰 grpc / pills / automations / sandbox。未 compile-client。不得宣称 leftover/pills 完成。 | 工位 D `session-changes-middle-click-catch`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/sessions/contrib/changes/test/browser/sessionChangesEditorInput.test.ts` **5/0**（含 `assertCleanState` 1；既有 3 + middle-click 1） | review 门控后 command reject 0 unhandled 且仍 return true。未关 D8/D16/D147/D24/D405。 | sessions / changes | closed |
| D477 | P3 | **closed** `sessionsTelemetry.contribution.ts` 全部 `void this._getSessionActionPayload(...).then(...)` 与 `void this._getOrFetchWorkspaceFileCount(...).then(...)` 无 catch（L200 `getAllTasks` 已有，不重做）。本刀补 `.catch(onUnexpectedError)`。mocha 锁 requestSent：workspace 有 folder 时 `_getOrFetchWorkspaceFileCount` reject → 0 unhandled。`_startWorkspaceFileCountFetch` 仍把 search reject 收成 `-1`，故测侧替换该方法以驱动 void then reject。未扫其它文件 void。未碰 grpc / pills / changes / sandbox。未 compile-client。不得宣称 leftover/pills 完成。 | 工位 D `sessions-telemetry-then-catch`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/sessions/contrib/sessions/test/browser/sessionsTelemetry.contribution.test.ts` **8/0**（含 `assertCleanState` 1；既有 6 + file-count reject 1） | 上述 then 均有 catch；requestSent file-count reject 0 unhandled；既有测仍绿。未关 D8/D16/D147/D24/D405。 | sessions / telemetry | closed |
| D478 | P3 | **closed** `baseAgentHostSessionsProvider.ts` 两处 `void this._resolveRunningSessionConfig(...)`（`setSessionConfigValue` / `replaceSessionConfig`）无 catch。方法体已有 try/catch；warn 再抛会漏。本刀两处补 `.catch(onUnexpectedError)`。未改方法体既有 try/catch。mocha 锁 setSessionConfigValue：`resolveSessionConfig` throw 且 `logService.warn` 再抛 → 0 unhandled。common 测驱不动 running set/replace。未扫其它 void。未碰 grpc / pills / telemetry。未 compile-client。不得宣称 leftover/pills 完成。 | 工位 D `agenthost-resolve-config-void-catch`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/sessions/contrib/providers/agentHost/test/browser/localAgentHostSessionsProvider.test.ts` **207/0**（11 pending 既有；含 `assertCleanState` 1 + warn 再抛 1） | 两处 catch；running resolve warn 再抛 0 unhandled；既有测仍绿。未关 D8/D16/D147/D24/D405。 | sessions / agentHost | closed |
| D479 | P3 | **closed** `sessionTurnChanges.ts` 四处 `void this._openSessionTurnChanges(...)` 调用点无 `.catch(onUnexpectedError)`：live last-turn agentHost、most-recent chat、historical turn、`_openTransientLastTurnChanges`。方法体已有 try/catch 且 catch 调 `onUnexpectedError`；若 handler warn 再抛，void promise 仍漏 unhandled。本刀四处补 `.catch(onUnexpectedError)`。未改方法体既有 try/catch。未碰 `_openStandaloneChanges` `openEditor`。mocha 锁 agentHost last-turn：`openChangesEditor` reject 且 `setUnexpectedErrorHandler` 先 warn 再抛 → 0 unhandled，调用点仍走完。未扫其它文件 void。未碰 grpc / conversation pills / catalog `onOpenConnection`。未 compile-client。不得宣称 leftover/pills 完成。 | 工位 D `session-turn-changes-void-catch`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/sessions/contrib/chat/test/browser/sessionTurnChanges.test.ts` **10/0**（含 `assertCleanState` 1；既有 8 + warn 再抛 1） | 四处 catch；warn 再抛 0 unhandled；既有测仍绿。未关 D8/D16/D147/D24/D405。不重开 D174/D180/D182/D186。 | sessions / chat | closed |
| D480 | P3 | **closed** D479 残留：`sessionTurnChanges.ts` `_openStandaloneChanges` 的 `openEditor` 返回 Promise，当时既无 `void` 也无 `.catch(onUnexpectedError)`。本刀只补 `void this._editorService.openEditor(...).catch(onUnexpectedError)`；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层 `.catch(onUnexpectedError)`（对齐 D479 方法体内 catch + 调用点 catch）。mocha 锁无 session owner → standalone multi-diff：`openEditor` reject 且 handler 先 warn 再抛 → 0 unhandled。未改 `_openSessionTurnChanges` 方法体。未改 Changes 生产合同。未扫其它文件 void。未碰 grpc / conversation pills / catalog `onOpenConnection`。未 compile-client。不得宣称 leftover/pills 完成。 | 工位 D `standalone-openeditor-catch`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/sessions/contrib/chat/test/browser/sessionTurnChanges.test.ts` **11/0**（含 `assertCleanState` 1；既有 8 + D479 warn 再抛 1 + standalone warn 再抛 1） | standalone openEditor catch；warn 再抛 0 unhandled；D479 既有断言仍绿。未关 D8/D16/D147/D24/D405。不重开 D174/D180/D182/D186/D479。 | sessions / chat | closed |
| D481 | P3 | **closed** `sessionInputBanners.ts` Reveal CI `void this._revealPullRequest(...)` 无 catch。方法体已有 try/catch；`logService.error` 再抛会漏。本刀补 `.catch(onUnexpectedError)`。未改方法体既有 try/catch。未改 `_revealComment`。mocha 锁点 Reveal CI：`executeCommand` reject 且 `logService.error` throw → 0 unhandled。未扫其它 void。未碰 grpc / conversation pills / catalog `onOpenConnection`。未 compile-client。不得宣称 leftover/pills 完成。 | 工位 D `reveal-pr-void-catch`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/sessions/contrib/sessionInputBanners/test/browser/sessionInputBanners.test.ts` **3/0**（含 `assertCleanState` 1；既有 1 + error 再抛 1） | 调用点 catch；error 再抛 0 unhandled；既有测仍绿。未关 D8/D16/D147/D24/D405。不重开 D174/D180/D182/D186/D479/D480。 | sessions / sessionInputBanners | closed |
| D482 | P3 | **closed** `wslAgentHost.contribution.ts` 四处 `void` 无 catch：IntervalTimer / `_reconcile` 的 `_reconnectWSLEntriesIfRunning`；循环内与 `scheduleRetry` 的 `_attemptWSLReconnect`。`_attemptManagedReconnect` 已有 try/catch；`logService.error` 再抛会漏。本刀四处补 `.catch(onUnexpectedError)`。未改 reconnect 策略/合同。mocha 锁 scheduleRetry：`reconnect` reject 且 `logService.error` throw → 0 unhandled。IntervalTimer / `_reconcile` / 循环内 void 受 `isWindows` 闸，本 Linux mocha 驱不动，未另写假测。未扫其它文件。未碰 grpc / pills / catalog `onOpenConnection`。未 compile-client。不得宣称 leftover/pills 完成。 | 工位 D `wsl-void-catch`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/sessions/contrib/providers/remoteAgentHost/test/browser/wslAgentHost.contribution.test.ts` **3/0**（含 `assertCleanState` 1；既有 pause 1 + scheduleRetry error 再抛 1） | 四处 catch；scheduleRetry error 再抛 0 unhandled；既有 pause 测仍绿。未关 D8/D16/D147/D24/D405。不重开 D174/D180/D182/D186/D479/D480/D481。 | sessions / wslAgentHost | closed |
| D483 | P3 | **closed** `localAgentHostSessionsProvider.ts` `createNewSession` 的 `void this._resolveDevContainerAvailability(...)` 无 catch。方法体已有 try/catch + `logService.warn`；与 D478 同型：warn 再抛会漏。本刀补 `.catch(onUnexpectedError)`。未改方法体既有 try/catch。mocha 锁 createNewSession：`isAvailable` reject 且 `logService.warn` 再抛 → 0 unhandled。未扫其它文件。未碰 grpc / pills / catalog `onOpenConnection`。未 compile-client。不得宣称 leftover/pills 完成。 | 工位 C `createNewSession-devcontainer-void-catch`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/sessions/contrib/providers/agentHost/test/browser/localAgentHostSessionsProvider.test.ts` **208/0**（11 pending 既有；含 `assertCleanState` 1 + D478 warn 再抛 1 + 本刀 1） | 调用点 catch；isAvailable warn 再抛 0 unhandled；既有测仍绿。未关 D8/D16/D147/D24/D405。不重开 D478。 | sessions / agentHost | closed |
| D484 | P3 | **closed** `sessionArtifacts.ts` `_actions()` 四处 `void` 无 catch：`openExternal` `_openerService.open`、`openResource` `openChatTurnFile` / `_openerService.open`、`openImages` `executeCommand`、`copy` `writeText`。本刀四处补 `.catch(onUnexpectedError)`；无内层 try/catch，单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（对齐 D480）。mocha 锁 copy：`writeText` reject 且 handler 先 warn 再抛 → 0 unhandled。未改 pill 合同 / `buildSessionArtifactSections`。未扫其它文件 void。未碰 grpc / pills / catalog `onOpenConnection`。未 compile-client。不得宣称 leftover/pills 完成。 | 工位 H `session-artifacts-actions-catch`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/sessions/contrib/chat/test/browser/sessionArtifacts.test.ts` **5/0**（含 `assertCleanState` 1；既有 3 + copy warn 再抛 1） | 四处 catch；copy warn 再抛 0 unhandled；既有测仍绿。未关 D8/D16/D147/D24/D405。不重开 D479/D480/D481。 | sessions / chat | closed |
| D485 | P3 | **closed** `conversationEngineHistoryList.ts` 四处 `void this.refresh()` 无 catch（connection/roster listeners；`show()`）。`refresh()` 方法体已有 try/catch（paintListFailed）。与 D478 同型：方法体内已吞则 warn/paint 再抛会漏。本刀四处补 `.catch(onUnexpectedError)`。未改 refresh 方法体。未改 leftover-looks-live / pairing-hold 合同。mocha 锁 show/refresh：`getHistory` reject 且 stub `paintListFailed` throw → 0 unhandled。未扫其它文件。未碰 grpc / pills / catalog `onOpenConnection`。未发明 GetQueue。未 compile-client / compile。不得宣称 leftover/pills 完成。 | 工位 I `history-refresh-void-catch`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationEngineHistoryList.test.ts` **15/0**（含 `assertCleanState` 1；既有 13 + paint 再抛 1） | 四处 catch；show refresh paint 再抛 0 unhandled；既有 leftover 测仍绿。未关 D8/D16/D147/D24/D405。 | conversation / history | closed |
| D486 | P3 | **closed** `conversationEngineSnapshotsList.ts` 六处 `void` 无 catch：listeners/show 的 `refresh()`（~212/217/222/248）、`restoreThenRefreshList`（~382）、`deleteThenRefreshList`（~429）。方法体已有 try/catch（restore/delete 另 `paintWriteStatus`）；paint 再抛会漏。行内 `deleteSnapshot` `.catch(onUnexpectedError)` 已有，不重做。本刀六处补 `.catch(onUnexpectedError)`。未改方法体。未改 leftover-looks-live / pairing-hold 写闸。mocha 锁 Restore：`restoreSnapshot` reject 且 `paintWriteStatus` throw → 0 unhandled。未碰 grpc / pills / catalog `onOpenConnection`。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。 | 工位 J `d486-snapshots-void-catch`；`npm run transpile-client` 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationEngineSnapshotsList.test.ts` **41/0**（含 `assertCleanState` 1；既有 39 + restore reject + paint 再抛 1） | 六处 catch；restore reject + paint 再抛 0 unhandled；既有测仍绿。未关 D8/D16/D147/D24/D405。 | conversation / snapshots | closed |
| D487 | P3 | **closed** `grpcProtoCodec.readProtoFields` 现记录 wire type 1（64-bit）payload，不再 silent skip。`encodeDouble` / `lastFixed64` 走 IEEE 754 LE（proto wire type 1，不另发明格式）。`decodeMemorySearchResult` 读 `score`=4 填 `MemorySearchResultWire.score`；Search / SearchDeep 共用该 decode。fixture 改用生产 `encodeDouble`（去掉 test-only `encodeFixed64Skip`）。未关 D24。未发明 proto 字段号。未碰 pills / Connect / SaveSkillContent / Watch / ResolveTurn / ResolveAnchor。不得宣称 leftover/pills 完成。 | 工位 A `d487-memory-score`；增量 transpile 后 `node test/unit/node/index.js --run src/vs/platform/universeAgent/test/node/grpcMemoryUnaryWire.test.ts` **22/0**（含 `Errors`/`assertCleanState` 1；既有 Search score=1.5 / SearchDeep score=2.25 + 聚焦 encodeDouble/lastFixed64 非 0 分 1） | codec lastFixed64/encodeDouble + Search/SearchDeep score 已填 + 非 0 分测。未关 D8/D16/D147/D24/D405。不得宣称 leftover/pills 完成。 | platform / universeAgent | closed |
| D488 | P3 | **closed** `navigatorAgentsView.ts` 三处 `void` 无 catch：hierarchy `onDidOpen` / `revealHierarchyNode` 的 `revealNavigatorAgentInConversation`（方法体已有 try/catch）；`openInspectPanel` 的 `openView`（无内层 try）。`executeCommand` ~509 已有 catch，不重做。本刀：两处 reveal 单层 `.catch(onUnexpectedError)`（D478）；openView 双链 catch（D480）。mocha 锁 reveal：`openSubAgent` throw 且 `notification.error` 再抛 → 0 unhandled；inspect：`openView` reject 且 `setUnexpectedErrorHandler` 先 warn 再抛 → 0 unhandled。未改 leftover-looks-live / pairing-hold。未扫其它文件。未碰 grpc / pills / catalog `onOpenConnection`。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186。 | 工位 C `navigator-agents-void-catch`；点名 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/navigator/test/browser/navigatorAgentsSubviews.test.ts` **51/0**（含 `assertCleanState` 1；既有 47 + reveal error-path 2 + inspect warn 再抛 1） | 三处 catch；reveal error-path 再抛 + inspect warn 再抛 0 unhandled；既有 leftover 测仍绿。未关 D8/D16/D147/D24/D405。 | workbench / navigator | closed |
| D489 | P3 | **closed** `conversationSessionsView.ts` `openSessionBeside` 的 `void this.sessionWindowService.openSessionBeside(sessionId)`（`Promise<void>`）无 catch。服务方法已有 try/catch + `logService.warn`；与 D478 同型：warn 再抛会漏。本刀补 `.catch(onUnexpectedError)`。未改 `conversationSessionWindowService.ts` 方法体。未改 leftover-looks-live / pairing-hold。mocha 锁 Sessions view `openSessionBeside`：inner throw 且 catch-path warn 再抛 → 0 unhandled。未扫其它文件。未碰 grpc / pills / catalog `onOpenConnection`。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。 | 工位 D `open-session-beside-void-catch`；增量 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationSessionsView.test.ts` **39/0**（含 `assertCleanState` 1；既有 37 + warn 再抛 1） | 调用点单层 catch；warn 再抛 0 unhandled；既有测仍绿。未关 D8/D16/D147/D24/D405。不重开 D174/D180/D182/D186。本刀不是 D482。 | conversation / sessions-view | closed |
| D490 | P3 | **closed** `agentHostUntitledProvisionalSessionService.ts` 两处 `void` 无 catch：`onDidDisposeSession` 的 `disposeSession`（方法体无 try → 双链 `.catch(onUnexpectedError)`）；`_retryPendingBackendDisposals` 的 `_disposeBackend`（方法体已有 try/catch → 单层）。~330/344/434 已有 catch，不重做。shutdown `.catch(() => { /* swallow on shutdown */ })` 未改成 onUnexpectedError。mocha 锁：onDidDisposeSession dispose warn 再抛且 handler warn-then-rethrow → 0 unhandled；pending cleanup dispose warn 再抛 → 0 unhandled。未扫其它文件。未碰 grpc / pills / catalog `onOpenConnection`。未发明 proto。未 compile-client。不重开 D174/D180/D182/D186。不得宣称 leftover/pills 完成。 | 工位 H `untitled-provisional-void-catch`；`VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/chat/test/browser/agentSessions/agentHostUntitledProvisionalSessionService.test.ts` **54/0**（含 `assertCleanState` 1；既有 51 + 本刀 2） | 两处 catch；warn 再抛 0 unhandled；既有测仍绿。未关 D8/D16/D147/D24/D405。不重开 D174/D180/D182/D186。 | workbench / chat | closed |
| D491 | P3 | **closed** `navigatorTeamList.ts` 两处 `void invokeFunction` 无 catch：members `onDidOpen` 的 `revealNavigatorAgentInConversation`（async，方法体已有 try/catch）；`openInspectPanel` 的 `openView`（无内层 try/catch）。本刀 reveal 调用点单层 `.catch(onUnexpectedError)`（D478）；openView 双链（D480）。未改 `navigatorAgentsView.ts`。未改 leftover-looks-live / pairing-hold 行闸。mocha 锁：member reveal `openSubAgent` throw 且 `notification.error` 再抛 → 0 unhandled；Inspect `openView` reject 且 handler 先 warn 再抛 → 0 unhandled。未扫其它文件。未碰 grpc / pills / catalog `onOpenConnection`。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。 | 工位 I `team-list-void-catch`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/navigator/test/browser/navigatorTeamList.test.ts` **9/0**（含 `assertCleanState` 1；既有 6 + notify 再抛 1 + openView warn 再抛 1） | 两处 catch；notify / warn 再抛 0 unhandled；既有测仍绿。未关 D8/D16/D147/D24/D405。不重开 D174/D180/D182/D186。 | navigator / team | closed |
| D492 | P3 | **closed** `agentHostCodexApprovalsPicker.ts` `_handleFooterActionItem` 的 `void this._openerService.open(...)` 无 catch。`open` 返回 Promise；无内层 try。本刀补 `.catch(onUnexpectedError).catch(onUnexpectedError)`（对齐 D480：单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏）。未改 Claude picker。mocha 锁选 Learn more：opener reject 且 handler 先 warn 再抛 → 0 unhandled。未扫其它文件。未碰 grpc / catalog `onOpenConnection`。未发明 proto。未 compile-client。不重开 D174/D180/D182/D186。D24 仍开。不得宣称 leftover/pills 完成。 | 工位 J `codex-approvals-learn-more-catch`；增量 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/sessions/contrib/providers/agentHost/test/browser/agentHostCodexApprovalsPicker.test.ts` **2/0**（含 `assertCleanState` 1；Learn more warn 再抛 1） | Learn more opener reject + warn 再抛 0 unhandled。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186。 | sessions / agentHost | closed |
| D493 | P3 | **closed** `engineClipboardSection.ts` 六处 `void` 无 catch：`handleWrite` / `handleRead` / `handleClear` 点击；connection `refresh`；`setSectionActive` `refresh`；failed `onRetry` `refresh`。方法体已有 try/catch；catch-path paint 再抛会漏。本刀六处补 `.catch(onUnexpectedError).catch(onUnexpectedError)`（单层在 `setUnexpectedErrorHandler` warn 再抛时仍漏，对齐 D480）。未改方法体。未改 leftover-looks-live / listed-gate / pairing-hold。未碰 catalog `onOpenConnection`。mocha 锁：refresh list reject 且 status.render 再抛 → 0 unhandled；Write reject 且 write-status paint 再抛 → 0 unhandled；Read reject 且 handler 先 warn 再抛 → 0 unhandled。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不重开 D174/D180/D182/D186。D24 仍开。不得宣称 leftover/pills 完成。 | 工位 C `clipboard-void-catch`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/engineClipboardSection.test.ts` **31/0**（含 `assertCleanState` 1；既有 27 + paint 再抛 2 + warn 再抛 1） | 六处 catch；paint / warn 再抛 0 unhandled；既有 leftover 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186。 | conversation / clipboard | closed |
| D494 | P3 | **closed** `engineContextVariableSection.ts` 三处 `void this.refresh()` 无 catch（connection listener；`setSectionActive`；failed `onRetry`）。`refresh()` 方法体已有 try/catch（paint failed）。与 D478 同型：方法体内已吞则 catch-path render 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480）。未改 refresh 方法体 leftover / leftover-looks-live。未改 catalog `onOpenConnection`。未改 clipboard。mocha 锁 setSectionActive：`listContextVariable` reject 且 stub `status.render` failed throw 且 handler 先 warn 再抛 → 0 unhandled。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186。本刀不是 D482。D24 仍开。 | 工位 D `context-var-refresh-void-catch`；增量 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/engineContextVariableSection.test.ts` **16/0**（含 `assertCleanState` 1；既有 14 + warn 再抛 1） | 三处 catch；warn 再抛 0 unhandled；既有 leftover 测仍绿。未关 D8/D16/D147/D24/D405。不重开 D174/D180/D182/D186。本刀不是 D482。 | conversation / context-variable | closed |
| D495 | P3 | **closed** `conversationNavigation.contribution.ts` 五处 void Promise 无调用点 catch：窗口钮 `goBack(part)` / `goForward(part)`；`closeNonRootTabs`；鼠标后退/前进 `goBack()` / `goForward()`（无 part）。本刀五处补 `.catch(onUnexpectedError).catch(onUnexpectedError)`（对齐 D480：单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏）。未改 `conversationNavigationService.goBack`/`goForward` / `closeNonRootTabs` 方法体。mocha 扩 warn-then-rethrow（有 part / 无 part / closeNonRootTabs）。未扫其它文件。未碰 grpc / pills / catalog `onOpenConnection`。未发明 proto。未 compile-client。不重开 D174/D180/D182/D186。D24 仍开。不得宣称 leftover/pills 完成。 | 工位 H `conversation-nav-void-catch`；测文件已扩 warn-then-rethrow；条数合入时复跑 | 五处双链 catch；mocha warn-then-rethrow 已扩。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186。不得宣称 leftover/pills 完成。 | conversation / navigation | closed |
| D496 | P3 | **closed** `navigatorProjectsList.ts` 两处 `void` 无 catch：`openWindow`（Promise，原 `.catch(() => undefined)`）与 `refresh` 的 `rebuildTree()`（async）。`rebuildTree` 方法体已有 try/catch（D146 last-good），未改方法体；内层不调 `onUnexpectedError`，单层调用点 catch 在 handler warn 再抛时仍漏。本刀两处补 `.catch(onUnexpectedError).catch(onUnexpectedError)`（openWindow=D480；rebuildTree void 同 D480）。未改 `navigatorAgentsView` / `navigatorTeamList`。未改 leftover-looks-live / pairing-hold。mocha 锁：local-folder `openWindow` reject 且 handler 先 warn 再抛 → 0 unhandled；`refresh` 在 `rebuildTree` reject 且 handler 先 warn 再抛 → 0 unhandled。未扫其它文件。未碰 grpc / pills / catalog `onOpenConnection`。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186。D24 仍开。 | 工位 I `projects-void-catch`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/navigator/test/browser/navigatorProjectsList.test.ts` **31/0**（含 `assertCleanState` 1；既有 28 + openWindow warn 再抛 1 + rebuildTree warn 再抛 1） | 两处 catch；openWindow / rebuildTree warn 再抛 0 unhandled；既有 leftover 测仍绿。未关 D8/D16/D147/D24/D405。不重开 D174/D180/D182/D186。 | navigator / projects | closed |
| D497 | P3 | **closed** `sessionInputBanners.ts` 两处 `void` 无 catch：`prModel.refresh()` / `ciModelRef.object.refresh()`（`refresh(): Promise<void>`）。无内层 try。本刀两处补 `.catch(onUnexpectedError).catch(onUnexpectedError)`（对齐 D480：单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏）。**不重做** D481 Reveal CI catch。mocha 锁：draft PR 的 PR refresh reject、Open PR 的 CI refresh reject，handler 先 warn 再抛 → 0 unhandled。未扫其它 void。未碰 grpc / pills / catalog `onOpenConnection`。未发明 proto。未 compile-client。不重开 D481/D174/D180/D182/D186。D24 仍开。不得宣称 leftover/pills 完成。 | 工位 J `banners-refresh-void-catch`；增量 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/sessions/contrib/sessionInputBanners/test/browser/sessionInputBanners.test.ts` **5/0**（含 `assertCleanState` 1；既有 1 + D481 error 再抛 1 + PR/CI warn 再抛 2） | 两处 catch；PR/CI refresh warn 再抛 0 unhandled；D481 既有断言仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D481/D174/D180/D182/D186。 | sessions / sessionInputBanners | closed |
| D498 | P3 | **closed** `engineTriggersSection.ts` 三处 `void this.refresh()` 无 catch（connection listener；`setSectionActive`；failed `onRetry`）。`refresh()` 方法体已有 try/catch（paint failed）。与 D478 同型：方法体内已吞则 catch-path render 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480）。未改 refresh 方法体 leftover / leftover-looks-live / listed-gate。未改 catalog `onOpenConnection`。未改 clipboard。mocha 锁 setSectionActive：`listTriggers` reject 且 stub `status.render` failed throw 且 handler 先 warn 再抛 → 0 unhandled。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186。本刀不是 D482。D24 仍开。 | 工位 C `triggers-refresh-void-catch`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/engineTriggersSection.test.ts` **33/0**（含 `assertCleanState` 1；既有 31 + warn 再抛 1） | 三处 catch；warn 再抛 0 unhandled；既有 leftover 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186。 | conversation / triggers | closed |
| D499 | P3 | **closed** `engineSkillsSection.ts` 五处 `void` 无 catch：connection / 构造 `refresh()`；成功 list 后与 list selection 的 `loadSkillBody(...)`；failed `onRetry` `refresh()`。两方法体已有 try/catch（refresh paint failed；loadSkillBody `showBodyStatus` load-failed）。与 D478 同型：方法体内已吞则 catch-path 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480）。未改方法体 leftover / leftover-looks-live。未改 catalog `onOpenConnection`。未改 create/toggle/save void。未改 triggers。mocha 锁：构造 refresh `listSkills` reject 且 stub `status.render` failed throw 且 handler 先 warn 再抛 → 0 unhandled；`selectSkillForTest` `getSkillInfo` reject 且 body-status paint throw 且 handler 先 warn 再抛 → 0 unhandled。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186。本刀不是 D482。D24 仍开。 | 工位 D `skills-refresh-loadbody-void-catch`；增量 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/engineSkillsSection.test.ts` **44/0**（含 `assertCleanState` 1；既有 41 + warn 再抛 2） | 五处 catch；warn 再抛 0 unhandled；既有 leftover 测仍绿。未关 D8/D16/D147/D24/D405。不重开 D174/D180/D182/D186。本刀不是 D482。 | conversation / skills | closed |
| D500 | P3 | **closed** `engineToolsSection.ts` 五处 `void` 无 catch：connection listener / constructor `refresh()`；list selection / 成功 refresh 后 `loadToolInfo`；failed `onRetry` `refresh()`。两方法体已有 try/catch（paint failed / info honesty）。与 D478 同型：方法体内已吞则 catch-path paint 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480）。未改方法体 leftover / leftover-looks-live。未改 catalog `onOpenConnection`。未改 `savePendingEnablement` void。mocha 锁：constructor refresh list reject 且 status.render failed throw 且 handler 先 warn 再抛 → 0 unhandled；selectTool `getToolInfo` reject 且 infoHost paint throw 且 handler 先 warn 再抛 → 0 unhandled。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186。D24 仍开。 | 工位 H `tools-refresh-loadinfo-void-catch`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/engineToolsSection.test.ts` **13/0**（含 `assertCleanState` 1；既有 10 + warn 再抛 2） | 五处双链 catch；warn 再抛 0 unhandled；既有 leftover 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186。 | conversation / engine-tools | closed |
| D501 | P3 | **closed** `engineMcpRuntimePanel.ts` 九处 `void this.refresh()` / `void this.loadTools(...)` 无 catch（Refresh 钮；connection listener；ctor；`render()`；select `loadTools`；success 后 `loadTools`；failed `onRetry` refresh；leftover tools fail retry refresh；tools failed `onRetry` loadTools）。两方法体已有 try/catch（paint failed）。与 D478 同型：方法体内已吞则 catch-path render 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480）。未改方法体 leftover / leftover-looks-live。未改 catalog `onOpenConnection`。未改 `engineMcpSection.ts`。mocha 锁：connection `refresh` 在 `getMcpServerStatuses` reject 且 stub `status.render` failed throw 且 handler 先 warn 再抛 → 0 unhandled；select `loadTools` 在 `getMcpServerTools` reject 且 stub `toolsStatus.render` failed throw 且 handler 先 warn 再抛 → 0 unhandled。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186。D24 仍开。 | 工位 I `mcp-runtime-void-catch`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/engineMcpRuntimePanel.test.ts` **26/0**（含 `assertCleanState` 1；既有 23 + warn 再抛 2） | 九处 catch；warn 再抛 0 unhandled；既有 leftover 测仍绿。未关 D8/D16/D147/D24/D405。不重开 D174/D180/D182/D186。 | conversation / mcp-runtime | closed |
| D502 | P3 | **closed** `enginePluginsSection.ts` 八处 `void this.refresh()` / `void this.loadInfo(...)` 无 catch（connection listener；ctor；selection；refresh 成功后；list-fail `onRetry`；info-fail `onRetry`；write-fail `onRetry`；writeFailed renderStatus `onRetry`）。方法体已有 try/catch；catch-path render 再抛会漏。本刀八处补 `.catch(onUnexpectedError).catch(onUnexpectedError)`（对齐 D480：单层在 handler warn 再抛时仍漏）。未改方法体 leftover / leftover-looks-live。未碰 catalog `onOpenConnection`。未改 scan/enable/reload/unload void。mocha 锁：connection refresh list reject 且 status.render failed throw 且 handler 先 warn 再抛 → 0 unhandled；select loadInfo `getPluginInfo` reject 且 infoStatus.render failed throw 且 handler 先 warn 再抛 → 0 unhandled。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不重开 D174/D180/D182/D186。D24 仍开。不得宣称 leftover/pills 完成。 | 工位 J `plugins-refresh-loadInfo-void-catch`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/enginePluginsSection.test.ts` **35/0**（含 `assertCleanState` 1；既有 32 + warn 再抛 2） | 八处 catch；refresh / loadInfo warn 再抛 0 unhandled；既有 leftover 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186。 | conversation / plugins | closed |
| D503 | P3 | **closed** `engineProviderModelSection.ts` 四处 `void this.refreshProviders()` / `void this.refreshModels()` 无 catch（`refresh()` 内两处；failed `onRetry` 两处）。`refresh()` 本身同步、只扇出两个 async；ctor / `onDidChangeConnection` 的 `void this.refresh()` 不是 Promise，不在调用点加 catch。两方法体已有 try/catch（paint failed）。与 D478 同型：方法体内已吞则 catch-path render 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480）。未改方法体 leftover / leftover-looks-live。未改 catalog `onOpenConnection`（契约死码）。mocha 锁：ctor `refreshProviders` `listProviderStatus` reject 且 stub `providerStatus.render` failed throw 且 handler 先 warn 再抛 → 0 unhandled；ctor `refreshModels` `listModels` reject 且 stub `modelStatus.render` failed throw 且 handler 先 warn 再抛 → 0 unhandled。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186。D24 仍开。 | 工位 C `provider-model-void-catch`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/engineProviderModelSection.test.ts` **17/0**（含 `assertCleanState` 1；既有 14 + warn 再抛 2） | 四处双链 catch；warn 再抛 0 unhandled；既有 leftover 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186。不得宣称 leftover/pills 完成。 | conversation / provider-model | closed |
| D504 | P3 | **closed** `engineRulesSection.ts` 三处 `void this.refresh()` 无 catch：connection listener / constructor；failed `onRetry`。`refresh()` 方法体已有 try/catch（paint failed）。与 D478 同型：方法体内已吞则 catch-path render 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480 / D500）。未改 refresh 方法体 leftover / leftover-looks-live。未改 catalog `onOpenConnection`。未改 hooks。mocha 锁：constructor refresh `listProjectRules` reject 且 status.render failed throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186。D24 仍开。本行只关本文件 fire-and-forget，leftover 程序未全局完成。 | 工位 D `leftover-d504-engine-rules`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/engineRulesSection.test.ts` **5/0**（含 `assertCleanState` 1；既有 3 + warn 再抛 1） | 三处双链 catch；warn 再抛 0 unhandled；既有 leftover 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186。本文件 leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / engine-rules | closed |
| D505 | P3 | **closed** `engineHooksSection.ts` 三处 `void this.refresh()` 无 catch：connection listener；constructor `refresh()`；failed `onRetry`。`refresh()` 方法体已有 try/catch（paint failed）。与 D478 同型：方法体内已吞则 catch-path render 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480）。未改方法体 leftover / leftover-looks-live。未改 catalog `onOpenConnection`。mocha 锁：constructor refresh `listHookPoints` reject 且 stub `status.render` failed throw 且 handler 先 warn 再抛 → 0 unhandled；onDidChangeConnection 二次 refresh 同路径 → 0 unhandled。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186。本刀只闭本文件 void catch。D24 仍开。 | 工位 H `leftover-d505-engine-hooks`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/engineHooksSection.test.ts` **7/0**（含 `assertCleanState` 1；既有 4 + warn 再抛 2） | 三处双链 catch；warn 再抛 0 unhandled；既有 leftover 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186。不得宣称 leftover 完成。 | conversation / engine-hooks | closed |
| D506 | P3 | **closed** `engineMcpSection.ts`（definitions catalog，非 runtime）八处 `void` 无 catch：Add/Update/Remove 钮 `addServer`/`updateSelectedServer`/`removeSelectedServer`；connection listener / constructor `refresh()`；`showWriteFailed` / list-fail / writeFailed `renderStatus` 的 `onRetry` `refresh()`。方法体已有 try/catch（paint failed / write failed）。与 D478 同型：方法体内已吞则 catch-path render 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480）。未改方法体 leftover / leftover-looks-live。未改 catalog `onOpenConnection`。未改 `toggleServer` 行内未 catch Promise。未改 `engineMcpRuntimePanel.ts`（D501）。mocha 锁于 `engineCatalogSections.test.ts`：constructor refresh `listMcpServers` reject 且 stub `status.render` failed throw 且 handler 先 warn 再抛 → 0 unhandled；Remove 钮 `removeSelectedServer` reject 且 stub `status.render` failed throw 且 handler 先 warn 再抛 → 0 unhandled。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186/D501。D24 仍开。本文件 leftover **不是**全局完成。 | 工位 I `leftover-d506-engine-mcp-section`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/engineCatalogSections.test.ts` **109/0**（含 `assertCleanState` 1；既有 106 + warn 再抛 2） | 八处双链 catch；warn 再抛 0 unhandled；既有 MCP leftover 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186/D501。不得宣称 leftover 完成。 | conversation / mcp-definitions | closed |
| D507 | P3 | **closed** `connectionPreferencesPane.ts` 九处 `void` 无 catch：ctor `initializeState` / `refreshEngineDeviceLists`；connection `refreshEngineDeviceLists`；Rename / Revoke / RotateToken / Confirm / Reject / Test Connection 点击。方法体已有 try/catch（`initializeState` 另吞 encryption）；catch-path paint 再抛会漏。本刀九处补 `.catch(onUnexpectedError).catch(onUnexpectedError)`（对齐 D480：单层在 handler warn 再抛时仍漏）。未改方法体 leftover / leftover-looks-live。未碰 catalog `onOpenConnection`。未改 Login/Logout/Connect/refreshHubDirectory 无 `void` 入口。mocha 锁：ctor `isEncryptionAvailable` 抛且 handler 先 warn 再抛 → 0 unhandled；ctor `listDevices` reject 且 devices-status paint throw 且 handler 先 warn 再抛 → 0 unhandled；Test/Rename/Revoke/Rotate/Confirm/Reject catch-path paint throw 且 handler 先 warn 再抛 → 0 unhandled。未扫其它文件。未碰 grpc / pills / engineOverview/enginePreferences OPEN_CONNECTION leftover。未发明 proto。未 compile-client。不重开 D174/D180/D182/D186。D24 仍开。不得宣称 leftover/pills 完成。 | 工位 J `leftover-d507-connection-preferences`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/connectionPreferencesPane.test.ts` **145/0**（含 `assertCleanState` 1；既有 136 + warn 再抛 8） | 九处双链 catch；warn 再抛 0 unhandled；既有 leftover 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186。 | conversation / connection-preferences | closed |
| D508 | P3 | **closed** `engineAgentsSection.ts` 十二处 `void` 无 catch：New/Reset/Delete `createProfile`/`resetSelectedProfile`/`deleteSelectedProfile`；Save AGENTS.md `saveAgentsMarkdown`；Tools Save `saveAgentToolEnablement`；selection / 成功 refresh 后 `loadAgentsEditorForSelection`；connection listener / constructor `refresh()`；Tools 页 / tools-fail `onRetry` 的 `ensureAgentToolsLoaded(...).then(renderAgentTools)`；list-fail `onRetry` `refresh()`。方法体已有 try/catch（paint failed / write failed / editor load-failed / tools leftover）。与 D478 同型：方法体内已吞则 catch-path paint 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480 / D500 / D503）。未改方法体 leftover / leftover-looks-live。未改 catalog `OPEN_CONNECTION` `executeCommand`（三处仍单链）。mocha 锁于 `engineCatalogSections.test.ts`：constructor refresh `listAgentProfiles` reject 且 stub `status.render` failed throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）；New 钮 `createProfile` reject 且 write-status paint throw 且 handler 先 warn 再抛 → 0 unhandled。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186。D24 仍开。本行只关本文件 fire-and-forget，leftover 程序未全局完成。 | 工位 C `leftover-d508-engine-agents`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/engineCatalogSections.test.ts` **111/0**（含 `assertCleanState` 1；既有 108 + Agents warn 再抛 2） | 十二处双链 catch；warn 再抛 0 unhandled；既有 Agents leftover 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186。本文件 leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / agents | closed |
| D509 | P3 | **closed** `enginePluginsSection.ts` 四处写路径 `void` 无 catch：Scan New `scanNew()`；Enable `enableSelected()`；Reload `reloadSelected()`；Unload `unloadSelected()`。方法体已有 try/catch（`showWriteFailed`）。与 D478 同型：方法体内已吞则 catch-path render 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480 / D500）。未改 refresh / loadInfo（D502 已双链）。未碰 catalog `onOpenConnection`。mocha 锁：Scan/Enable/Reload/Unload 点击 RPC reject 且 status.render failed throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186/D502。D24 仍开。本行只关本文件写路径 fire-and-forget，leftover 程序未全局完成。 | 工位 D `leftover-d509-engine-plugins-writes`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/enginePluginsSection.test.ts` **39/0**（含 `assertCleanState` 1；既有 34 + warn 再抛 4） | 四处双链 catch；warn 再抛 0 unhandled；既有 leftover / D502 refresh·loadInfo 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186/D502。本文件写路径 leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / plugins | closed |
| D510 | P3 | **closed** `engineSkillsSection.ts` 两处写 void 无 catch：New 钮 `createSkill()`；Save 钮 `saveSelectedSkillBody()`。两方法体已有 try/catch（`paintSkillCreateFailed` / `paintSkillSaveFailed`）。与 D478 同型：方法体内已吞则 catch-path paint 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480 / D500）。未改 refresh / loadSkillBody（D499 已双链）。未改 catalog `onOpenConnection`。未改 `toggleSkill`。未转 SaveSkillContent grpc（无 proto RPC；[D24](deferred-gaps.md) 仍开）。mocha 锁：New 钮 `saveSkillContent` reject 且 catch-path write-status paint throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）；Save 钮同路径 → 0 unhandled。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186/D499。D24 仍开。本行只关本文件写 void，leftover 程序未全局完成。 | 工位 F `leftover-d510-engine-skills-writes`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/engineSkillsSection.test.ts` **46/0**（含 `assertCleanState` 1；既有 44 + warn 再抛 2） | 两处双链 catch；warn 再抛 0 unhandled；既有 leftover / D499 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186/D499。本文件写 void 已收；leftover 程序未全局完成。 | conversation / skills-writes | closed |
| D511 | P3 | **closed** `conversationEngineSnapshotsList.ts` 七处单层 `.catch(onUnexpectedError)` 升级为双链（D486 已补单层；本刀对齐 D480：单层在 `setUnexpectedErrorHandler` warn 再抛时仍漏）。listeners/show 的 `refresh()` 四处；`restoreThenRefreshList`；`deleteThenRefreshList`；行内 `deleteSnapshot`。方法体不改。未改 leftover-looks-live / pairing-hold / listed-gate 写闸。mocha 锁：show `refresh` `listSnapshots` reject 且 stub `paintListFailed` throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）；Restore `restoreSnapshot` reject 且 `paintWriteStatus` throw 同锁；Delete `deleteThenRefreshList` reject 且 paint throw 同锁；`deleteSnapshot` confirm reject 且 handler 先 warn 再抛 → 0 unhandled。未扫其它文件。未碰 grpc / pills / catalog `onOpenConnection`。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D179/D180/D182/D186/D486。D24 仍开。本行只关本文件 fire-and-forget，leftover 程序未全局完成。 | 工位 G `leftover-d511-snapshots-double-catch`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationEngineSnapshotsList.test.ts` **43/0**（含 `assertCleanState` 1；既有 40 + warn 再抛 2） | 七处双链 catch；warn 再抛 0 unhandled；既有 leftover 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D486。本文件 leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / snapshots | closed |
| D512 | P3 | **closed** `engineContextVariableSection.ts` 一处 `void this.handleRead()` 无 catch（Read 钮点击）。`handleRead()` 方法体已有 try/catch（paint read-status）。与 D478 同型：方法体内已吞则 catch-path paint 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480 / D493 Read）。未改 refresh（已 D494）。未改 catalog `onOpenConnection`。未改 leftover-looks-live / leftoverListFailed / pairing-hold。mocha 锁 Read：`readContextVariable` reject 且 read-status paint throw 且 handler 先 warn 再抛 → 0 unhandled。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186/D494。D24 仍开。本站点 leftover fire-and-forget 已收；leftover 程序未全局完成。 | 工位 H `leftover-d512-context-var-read`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/engineContextVariableSection.test.ts` **17/0**（含 `assertCleanState` 1；既有 15 + warn 再抛 1） | Read 双链 catch；warn 再抛 0 unhandled；既有 leftover / D494 refresh 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186/D494。不得宣称 leftover 完成。 | conversation / context-variable | closed |
| D513 | P3 | **closed** `conversationSessionChatService.ts` 两处 `void` 无 catch：`mountSubAgentOverlay` 的 `promoteSubAgentDialog` / `navigateAgentBreadcrumb`。本文件无其它 `void this.<async>`。两方法体已有 try/catch（`notificationService.error`）。与 D478 同型：方法体内已吞则 catch-path notice 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480 / D500）。未改方法体 leftover / leftover-looks-live / pairing-hold。未改 overlay / 其它 conversation UI。mocha 锁：overlay pop-out promote 在 `openExtensionTab` throw 且 notice 再抛且 handler 先 warn 再抛 → 0 unhandled；overlay breadcrumb 在 `openEditor` throw 且 notice 再抛且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D85/D87/D149/D174/D180/D182/D186。D24 仍开。本行只关本文件 fire-and-forget，leftover 程序未全局完成。 | 工位 I `leftover-d513-session-chat-service`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationSessionChat.test.ts` **43/0**（含 `assertCleanState` 1；既有 40 + warn 再抛 2） | 两处双链 catch；warn 再抛 0 unhandled；既有 leftover 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D85/D87/D149/D174/D180/D182/D186。本文件 leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / session-chat | closed |
| D514 | P3 | **closed** `engineOverviewSection.ts` 两处 `void this.renderAsync()` 无 catch：`setSectionActive`（SUPPORTED 且 `!modelDataLoaded`）；`render()`（ctor / `onDidChangeConnection` / failed `onRetry`）。`renderAsync` 的 `listModels` 已有 try/catch；catch 后 `paintSummary` 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480）。未改方法体 leftover / leftover-looks-live。未改 catalog `onOpenConnection`（契约死码；`OPEN_CONNECTION` 命令 lambda 不加 catch）。未改 `enginePreferences` OPEN_CONNECTION。mocha 锁：`setSectionActive` `listModels` reject 且 catch 后 `paintSummary` throw 且 handler 先 warn 再抛 → 0 unhandled；`onDidChangeConnection` 二次 `renderAsync` 同路径 → 0 unhandled。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186。本刀只闭本文件 `renderAsync` void catch。D24 仍开。 | 工位 J `leftover-d514-engine-overview`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/engineOverviewSection.test.ts` **18/0**（含 `assertCleanState` 1；既有 15 + warn 再抛 2） | 两处双链 catch；warn 再抛 0 unhandled；既有 leftover 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186。本文件 leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / engine-overview | closed |
| D515 | P3 | **closed** `engineTriggersSection.ts` 六处写路径 `void` 无 catch：Add/Edit `handleUpsert`；Fire `handleFire`；Enable/Disable `handleSetEnabled`；Delete `handleDelete`。四方法体已有 try/catch（write-status）。与 D478 同型：方法体内已吞则 catch-path paint 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480 / D498）。未改 refresh（已 D498）。未改 catalog `OPEN_CONNECTION` / `onOpenConnection`。未改 leftover / leftover-looks-live / listed-gate / pairing-hold。mocha 锁：Add/Fire/Enable/Delete 点击 RPC reject 且 write-status paint throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186/D498。D24 仍开。本行只关本文件写 void，leftover 程序未全局完成。 | 工位 C `leftover-d515-engine-triggers-writes`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/engineTriggersSection.test.ts` **37/0**（含 `assertCleanState` 1；既有 32 + warn 再抛 4） | 六处双链 catch；warn 再抛 0 unhandled；既有 leftover / D498 refresh 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186/D498。本文件写 void 已收；leftover 程序未全局完成。 | conversation / triggers-writes | closed |
| D516 | P3 | **closed** `enginePreferencesPane.ts` 两处 `void this.runEngineTest()` 无 catch：banner Test Engine 点击；footer Test Engine 点击。`runEngineTest` 方法体已有 try/catch（`writeStatus`）。与 D478 同型：方法体内已吞则 catch-path paint 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480 / D514）。未改方法体 leftover / leftover-looks-live。未改 `OPEN_CONNECTION` 命令 lambda（不加 catch）。mocha 锁：banner `probeEngine` reject 且 catch-path `writeStatus` throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）；footer 同路径 → 0 unhandled。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186/D407/D411。本刀只闭本文件 `runEngineTest` void catch。D24 仍开。 | 工位 D `leftover-d516-engine-preferences-test`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/enginePreferencesPane.test.ts` **21/0**（含 `assertCleanState` 1；既有 18 + warn 再抛 2） | 两处双链 catch；warn 再抛 0 unhandled；既有 leftover / probe 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186/D407/D411。未碰 OPEN_CONNECTION。本文件 leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / engine-preferences | closed |
| D517 | P3 | **closed** `engineMcpSection.ts` 行渲染器 `(server, enabled) => this.toggleServer(...)` 丢 Promise。`toggleServer` 方法体已有 try/catch（`showWriteFailed`）。与 D478 同型：方法体内已吞则 catch-path paint 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480 / D506）。本刀只补 `void` + `.catch(onUnexpectedError).catch(onUnexpectedError)`。未改 D506 refresh / Add / Update / Remove。未改 catalog `onOpenConnection` / `OPEN_CONNECTION`。未改 `engineMcpRuntimePanel.ts`（D501）。mocha 锁于 `engineCatalogSections.test.ts`：行 checkbox 点击 `toggleMcpServer` reject 且 stub `status.render` failed throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186/D506。D24 仍开。本行只关行内 toggle Promise，leftover 程序未全局完成。 | 工位 F `leftover-d517-engine-mcp-toggle`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/engineCatalogSections.test.ts` **112/0**（含 `assertCleanState` 1；既有 110 + warn 再抛 1） | 行渲染器双链 catch；warn 再抛 0 unhandled；既有 MCP leftover / D506 refresh·Remove 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186/D506。不得宣称 leftover 完成。 | conversation / mcp-toggle | closed |
| D518 | P3 | **closed** `connectionPreferencesPane.ts` 八处 fire-and-forget 无 `void`：Login `handleLogin`；Logout `handleLogout`；Refresh devices `refreshHubDirectory`；Direct Connect `handleConnectDirectAddress`；Direct Add `handleAddDirectAddress`；profile Connect `handleConnectSelectedProfile`；Disconnect `handleDisconnect`；Forget `handleForgetSelectedProfile`。方法体已有 try/catch（Direct Connect / profile Connect 经 `connectProfileWithPairing`）；catch-path paint 再抛会漏。本刀八处补 `void` + `.catch(onUnexpectedError).catch(onUnexpectedError)`（对齐 D507 / D480：单层在 handler warn 再抛时仍漏）。未改方法体 leftover / leftover-looks-live。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`。未改 device-row Connect（`handleConnectDevice`）。未改 D507 九处。mocha 锁：八处 catch-path paint throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。未扫其它文件。未碰 grpc / pills / engineOverview/enginePreferences OPEN_CONNECTION leftover。未发明 proto。未 compile-client。不重开 D174/D180/D182/D186/D507。D24 仍开。不得宣称 leftover/pills 完成。 | 工位 G `leftover-d518-connection-pane-no-void`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/connectionPreferencesPane.test.ts` **153/0**（含 `assertCleanState` 1；既有 144 + warn 再抛 8） | 八处 void+双链 catch；warn 再抛 0 unhandled；既有 leftover / D507 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186/D507。本行只关本文件无 void 入口，leftover 程序未全局完成。 | conversation / connection-preferences | closed |
| D519 | P3 | **closed** `engineSkillsSection.ts` 一处行内 Promise 无 catch：`EngineSkillRowRenderer` `onToggle` `(skill, enabled) => this.toggleSkill(...)` 丢弃 Promise。`toggleSkill` 方法体已有 try/catch（`paintSkillToggleFailed`）。与 D478 同型：方法体内已吞则 catch-path paint 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480 / D510）。未改 New/Save（D510 已双链）。未改 refresh / loadSkillBody（D499 已双链）。未改 catalog `onOpenConnection`（`OPEN_CONNECTION` 不加 catch）。未转 SaveSkillContent grpc（无 proto RPC；[D24](deferred-gaps.md) 仍开）。mocha 锁：行 checkbox `setSkillEnabled` reject 且 catch-path write-status paint throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186/D499/D510。D24 仍开。本行只关本文件行内 toggle Promise，leftover 程序未全局完成。 | 工位 H `leftover-d519-engine-skills-toggle`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/engineSkillsSection.test.ts` **47/0**（含 `assertCleanState` 1；既有 46 + warn 再抛 1） | 行内 toggle 双链 catch；warn 再抛 0 unhandled；既有 leftover / D499 / D510 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186/D499/D510。本文件行内 toggle leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / skills-toggle | closed |
| D520 | P3 | **closed** `sourcesReviewList.ts` 两处 `void` 无 catch：chip `revealAttributionItem(toolCallId)`；`RunOnceScheduler` 的 `refresh()`。两方法体已有 try/catch（reveal 吞 `executeCommand`；refresh 吞 git-read 后 `setStatusMessage`）。与 D478 同型：方法体内已吞则 catch-path paint/hint 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480）。未改方法体 leftover / leftover-looks-live / pairing-hold / listed-gate / FileDiff / Mark 写闸。未改 `sourcesChangesList.ts` scheduler。mocha 锁于 `sourcesReviewList.test.ts`：scheduler `refresh` `readGitChanges` reject 且 stub `setStatusMessage` throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）；chip `revealAttributionItem` `executeCommand` reject 且 stub `updateHeaderHint` throw 且 handler 先 warn 再抛 → 0 unhandled。未扫其它文件。未碰 grpc / pills / catalog `onOpenConnection`。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186/D245。D24 仍开。本行只关本文件 fire-and-forget，leftover 程序未全局完成。 | 工位 I `leftover-d520-sources-review-list`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/sources/test/browser/sourcesReviewList.test.ts` **35/0**（含 `assertCleanState` 1；既有 32 + warn 再抛 2） | 两处双链 catch；warn 再抛 0 unhandled；既有 leftover 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186/D245。本文件 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sources / review | closed |
| D521 | P3 | **closed** `navigatorAgentsView.ts` 三处残留单层 `.catch(onUnexpectedError)` 升双链（对齐 D480：单层在 `setUnexpectedErrorHandler` warn 再抛时仍漏）：hierarchy `onDidOpen` reveal ~477；`revealHierarchyNode` ~783；activity `onDidOpen` `executeCommand` ~509。`openInspectPanel` `openView` ~815 已双链，不重做。方法体不改（reveal 方法体已有 try/catch）。未改 leftover-looks-live / pairing-hold。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`。mocha 锁既有三测为 warn-then-rethrow：revealHierarchyNode / hierarchy row-open 的 catch-path notify 再抛且 handler 先 warn 再抛 → 0 unhandled；activity list `executeCommand` reject 且 handler 先 warn 再抛 → 0 unhandled。inspect openView warn 再抛既有测不重做。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186/D488。D24 仍开。本行只关本文件残留单链，leftover 程序未全局完成。 | 工位 J `leftover-d521-navigator-agents-double-catch`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/navigator/test/browser/navigatorAgentsSubviews.test.ts` **51/0**（含 `assertCleanState` 1；既有 50，本刀把 3 条吞 handler 改为 warn 再抛，条数不变） | 三处双链 catch；warn 再抛 0 unhandled；既有 leftover / inspect 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D488。本文件 leftover fire-and-forget 已收；leftover 程序未全局完成。 | navigator / agents | closed |
## Gate-recovery：关仓声明与实测不符（2026-09-07，工位 E / `fix/gate-recovery`）

`loop/merge` @ `793ff6e201f`（提交信息为「关仓：T5a Uncaught 闸门与 statusbar 二次注册幂等**已复测**」）在**仓外独立 detached 工位**上实测：

| 项 | 实测 | 命令 |
|:---|:-----|:-----|
| `npm run compile` | **1275 错**，退出码 1 | `npm run compile` |
| `conversationIdentityStrip.test.ts` | 2 passing / **1 failing** | `./scripts/test.sh src/.../conversationIdentityStrip.test.ts` |
| 合入后 conversation 域 | 794 → **237** 条（`min_cases=714` 不达） | `./scripts/run-unit-custom.sh` |

失败不是断言，是根 suite 的输出闸门：`ListView` 在 `display:none` 下量行高，打出 `Measured item node at 0px`，被 `renderer.js` 的「Unexpected console output」判红并中断后续文件，于是 conversation 域丢掉约 70% 用例。

净新增的 5 个类型错（`IConversationEditorPart` 缺 `getContainer` / `layout` 被强转、`process.rawListeners` 的 `Function` 不匹配重载、一个死掉的 `getTimelineTree`）已在本工位随合并修掉，compile 回到 0。

### 已修（2026-09-07，用户裁定 `fix_now`）

两个独立的真 bug，都不是测试环境的怪癖：

1. `ConversationTimelineTree` 先 splice 再切显隐。`renderEmptyState` 把承载树的 `contentHost` 在空态设成 `display:none`，而 `rebuildTreeFromTurns` / `applyContentPatches` 都是**先** `setChildren` / `rerender`、**后**调 `renderEmptyState`。于是空 → 非空那一跳里，ListView 在还挂着上一轮 `display:none` 的子树里量行高，量到 0px 并把这些高度缓存下来。改成先显后改。
2. `ConversationSubAgentOverlay.scheduleLensLayout` 的 `requestAnimationFrame` 不可取消。dispose 之后回调仍会跑 `layoutLens` → `layoutBreadcrumb` → `BreadcrumbsWidget.layout`，后者每次新建一个 `DisposableStore` 交给 `_pendingDimLayout`，而 widget 已经 dispose 过，这个 store 再没有人释放。换成 `scheduleAtNextAnimationFrame` 存进 `MutableDisposable`，dispose 时一起取消。

反向验证（逐个撤掉再跑整域）：只撤第 1 个 → 4 条 `Measured item node at 0px`，域内 236 passing 后中止；只撤第 2 个 → 520 passing / 2 failing（`There are 6 undisposed disposables!`）；两个都在 → **799 passing / 0 failing**。`min_cases` 已从中止水位对应的 714 抬到 799。

**教训不是「loop 又红了」，而是「关仓声明没有机器背书」**：`.github/workflows/agent-ide.yml` 的 push 触发在本工位才刚加上 `loop/**`（提交 `127c6c0f915`）。在那之前 `loop/*` 上的「已复测」全靠人写，没有一次进过 CI。

## D2 工位池 compile 基线（2026-09-02，merge 工位 / `loop/merge`）

**路径**：`/home/clarence/Projects/Agents/vscode-WorkTrees/merge` · **SHA**：`569ce371`

| 命令 | SHA | 结果 |
|------|-----|------|
| `npm run compile` | `569ce371` | **PASS**（0 errors，~25s） |

**证据**：[`worktree-pool.md`](worktree-pool.md)「路径与基线」表 · 工位池 compile 基线行。

## D4 冒烟记录（2026-08-31，工位 A / `loop/A`）


**命令**：`.agents/skills/launch/scripts/launch.sh --repo <A> --source-user-data-dir /tmp/d4-smoke-udd --disable-workspace-trust --skip-prelaunch`（未跑 `npm run compile`，按 M4 切片 2 约束）。

**阻塞**：`code.sh` 立即退出 — `.build/electron/code-oss: 没有那个文件或目录`。工位 A 另缺 `out/`、`node_modules/`；`~/.vscode-oss-dev` 不存在（本次用最小 temp profile 仅验证启动链）。

| 检查 | 结果 |
|------|------|
| T1 中心 = `CONVERSATION_PART`，非 ChatEditor tab | **未验**（未启动） |
| T2 End 上 Preview + 下 `SOURCES_PART` | **未验** |
| T3 四钮 Nav/Conv/Preview/Sources；`Conversation ∨ (Editor ∨ Sources)` | **未验** |
| M3 Command Palette 不开 ChatEditor | **未验** |
| M3 Sidebar stub Sessions 列表 | **未验** |
| 截图 | 无 |

**下一步**：D3 已绿（`a6137373`）；在 **merge 工位**（有 `node_modules`/`out`/`.build/electron`）重跑 `launch.sh` 做环境探测。D4 **不得**因 T1–T3 勾选而 closed；closer 是 [M5 切片 4](../plans/m5-ui-shell-hardening.md) V1–V8。

## D4 M5 切片 4 验收记录（2026-09-01，merge 工位 / `loop/merge`）

**基线 SHA**：`c7ed501d`（切片 1–3 集成工位）。**启动修复 SHA**（仅 boot blocker，非 V1–V8 closer）：`bd2b8872`。

**门禁**：

| 命令 | SHA | 结果 |
|------|-----|------|
| `npm run compile` | `c7ed501d` | **PASS**（0 errors，~37s） |
| `npm run valid-layers-check` | `c7ed501d` | **FAIL**（exit 1；`EditContext`/`GPUBufferUsage`/`FileSystemHandle` 等 TS lib 报错，Node v26.7.0；与 M5 路由/布局无关） |
| `launch.sh --repo merge --disable-workspace-trust --skip-prelaunch` | `c7ed501d` | **BLOCKED** — workbench 未加载：`IPreferencesEditorPane` 运行时 re-export 缺失 + `workbench.action.chat.forkConversation` 重复注册 |
| 同上 | `bd2b8872` | **PASS** — CDP 就绪；隔离 temp profile |

**启动命令**（post-fix 实测）：

```bash
.agents/skills/launch/scripts/launch.sh \
  --repo /home/clarence/Projects/Agents/vscode-WorkTrees/merge \
  --disable-workspace-trust --skip-prelaunch
```

**Profile**：throwaway temp（`launch.sh` slim copy；无 `--source-user-data-dir`；`~/.vscode-oss-dev` 不存在）。

**自动化**：Playwright CLI over CDP（`npx @playwright/cli attach --cdp=…`）。证据目录：[d4-evidence/c7ed501d](d4-evidence/c7ed501d/)（JSON + `screenshots/v1.png`）。

| ID | 场景 | 结果 | 证据 / 备注 |
|:---|:-----|:-----|:------------|
| V1 | fresh profile：中心 Conversation；End 上 Preview、下 Sources；Aux hidden | **PARTIAL** | post-`bd2b8872`：`conversation`+`sources` 可见，`panel`/`aux` false，`convError` false；DOM 尺寸检测 `editor`/`sidebar` false（快照见四钮 + End Preview/Sources tab strip）。`c7ed501d` **未验**（未启动） |
| V2 | 依次切 Nav / Conv / Preview / Sources；四钮可达；常态不可全藏 Conv/Preview/Sources | **FAIL** | Command Palette 自动化不可靠；toggle 后 `panel:true`、`sources:false`，未证明互斥 |
| V3 | 只关 Preview；Sources/Conversation 不被改写；Panel 不弹出 | **FAIL** | `panel:true` after hide Preview；Panel 被意外显示 |
| V4 | maximize Panel/Aux 后恢复合法 agent shell | **FAIL** | maximize 命令未改变可测 layout JSON；恢复态与 V3 相同 |
| V5 | 隐藏 Conversation → Sessions 选会话 → show+focus | **PARTIAL** | roster `clicked:true`；之后 `conversation:true`（`v5.json`）；SessionBar 一致性与 focus 未 DOM 断言 |
| V6 | Palette / Chat menu / Quick Chat；无 ChatEditor/Quick Chat；Open Conversation OK | **FAIL** | `New Chat Editor` 后无 ChatEditor tab（`editorTabs:[]`）；**Quick Chat 快捷键打开 quick-input**（`v6-quick.json` `quickChat:true`）— M5 路由违背 |
| V7 | 重启恢复 Part 显隐与 End 尺寸；无 ChatEditor tab 还原 | **FAIL** | Reload 后全部 part `false`（`v7.json`）；布局未恢复 |
| V8 | Sources Files/Changes/Review 基本打开 | **PARTIAL** | 快照（V2 阶段）见 Files/Changes/Review tab；eval `sourcesTabs:[]`（automation 时 Sources 已隐藏） |

**Blocking failures（首轮 c7ed501d，已由 rerun-2230 闭合）：**

1. ~~`c7ed501d` boot 崩溃~~ → `bd2b8872` + `c95fd679` 修复。
2. ~~V2–V7 布局/路由失败~~ → rerun-2230 全 PASS。

**截图**：`dev/progress/d4-evidence/c7ed501d/screenshots/v1.png`（post-fix fresh profile）。

**状态（首轮记录，已 supersede）**：见下节 rerun-2230 — D4 **closed**。

## D4 M5 切片 4 验收记录（2026-09-01，主仓 / `agent-ide` working tree）

**代码修复**（待提交）：INV-052 `beforeHide` 须在 `setRuntimeValue` 之前采集；Panel maximize 恢复 `panelVisible`；.harness 四钮 titlebar 点击 + command id + V5/V7 命令链。

**启动**：`launch.sh --repo /home/clarence/Projects/Agents/vscode --disable-workspace-trust --skip-prelaunch`

**自动化**：`dev/progress/d4-evidence/c7ed501d/run-v1-v8.sh` · 证据：[d4-evidence/rerun-2230](d4-evidence/rerun-2230/)

| ID | 场景 | 结果 | 证据 / 备注 |
|:---|:-----|:-----|:------------|
| V1 | fresh profile：中心 Conversation；End Sources；panel/aux off | **PASS** | `conversation`+`sources` true；`panel`/`aux` false |
| V2 | 四钮 toggle；常态不可全藏 Conv/Preview/Sources | **PASS** | titlebar 点击后 `conversation`+`sources` 仍 true |
| V3 | 只关 Preview | **PASS** | `editor` false；`conversation`+`sources` 不变；`panel` false |
| V4 | maximize Panel 后恢复 | **PASS** | 恢复后合法 shell；`panel` false |
| V5 | 隐藏 Conversation → Sessions roster 选会话 | **PASS** | `workbench.view.sessions.focus` + roster `clicked:true`；`conversation` true |
| V6 | Open Chat / New Chat Editor / Quick Chat | **PASS** | 无 ChatEditor tab；`quickChatWidget` false |
| V7 | Reload 恢复 | **PASS** | `conversation`+`sources` 恢复；无 ChatEditor tab；`panel` false |
| V8 | Sources Files/Changes/Review | **PASS** | `sourcesTabs`: Files, Changes, Review |

**状态**：D4 **closed** @ rerun-2230（2026-09-01）。`valid-layers-check` 环境红仍记 D3 行，不阻塞 D4。

## D5 探针计划（2026-08-31，工位 B / `loop/B`）

**文档**：[eh-surface-matrix §Probe plan](../../docs/reference/code-oss-b2/eh-surface-matrix.md#probe-plan)。

**已选扩展**（安装状态 @ 2026-09-01）：

| 扩展 | ID | 覆盖 | 安装 |
|------|-----|------|------|
| YAML | `redhat.vscode-yaml` | LSP / 语言层（End 列 editor） | **OK** → `/tmp/d5-probe-ext-vsix` |
| Todo Tree | `gruntfuggly.todo-tree` | Sidebar `views` + Activity 布局挤占 | **OK** → 同上 |
| JavaScript Debugger | `ms-vscode.js-debug` | 调试视图（Sidebar/Panel） | **跳过 VSIX** — 产品内置；`code-cli.sh` 拒绝覆盖 builtin |

**安装命令（SSOT）**：

```bash
export VSCODE_SKIP_PRELAUNCH=1
REPO=/home/clarence/Projects/Agents/vscode-WorkTrees/merge
EXT_DIR=/tmp/d5-probe-ext-vsix
$REPO/scripts/code-cli.sh --extensions-dir="$EXT_DIR" \
  --force --install-extension=/tmp/d5-vsix/<id>.vsix
```

`launch.sh` 冒烟时传 **`--extensions-dir=/tmp/d5-probe-ext-vsix`**（或 `--` 转发给 `code.sh`）。勿用 `code.sh --install-extension`（会起完整 workbench）。

**矩阵**：3 行 + 2 探针对照行已标 **探针已选 @2026-08-31**；panel / terminal / `editor/decoration` 仍为 **待实测**。

**下一步**：

1. ~~D3 compile 绿 + D4 隔离 profile 启动~~ — D4 closed @ rerun-2230。
2. ~~VSIX 下载 + 安装~~ — 两扩展已装入 `EXT_DIR`；js-debug 用内置扩展，不装 VSIX。
`launch.sh` 冒烟：`dev/progress/d5-evidence/launch-with-probes.sh`（seed profile + `--clone-extensions` 注入 `EXT_DIR`）。
4. 截图 + 把覆盖行证据改为 **已实测 @\<date\>**，未覆盖 gap 记入本表或矩阵脚注。



## D5 EH 探针冒烟 wave 3（2026-09-02，merge launch / loop/A 证据）

**Launch：** `REPO=/home/clarence/Projects/Agents/vscode-WorkTrees/merge dev/progress/d5-evidence/launch-with-probes.sh -- dev/progress/d5-evidence/sample-workspace`

**Playwright：** `d5-wave3-375500` @ CDP from `launch.json`

**证据：** [d5-evidence/smoke-wave3-0001](d5-evidence/smoke-wave3-0001/)

| 检查 | 结果 | 备注 |
|------|------|------|
| Sample workspace + `launch.json` | **PASS** | 文件夹 `sample-workspace`；含 **D5 probe debug** 配置 |
| Activity **TODOs** click | **PASS** | `aria-label` TODOs → sidebar **TODOs: Tree**（产品槽位；无 gruntfuggly 独立 activity 图标） |
| 内置 `js-debug` F5 | **PASS** | 打开 `d5-probe-debug.js`；F5 → debug toolbar + status **Debug: D5 probe debug (sample-workspace)** |

**TODOs 澄清：** Agent IDE 预留 Activity **TODOs** 与扩展探针共用槽位；自动化以 **TODOs: Tree** 为可见性判据（非独立 “Todo Tree” pane 标题）。

**下一步：** ~~矩阵 panel/terminal/decoration 行~~ → D9 @ [smoke-d9-0001](d5-evidence/smoke-d9-0001/)（panel + decoration PASS；terminal blocked）。

## D9 EH 次级探针（2026-09-02，merge launch / loop/A 证据）

**探针：** `zhangmo8.git-panel` · `fabiospampinato.vscode-terminals` · `usernamehw.errorlens` @ `/tmp/d5-probe-ext-vsix`

**Launch：** `bash dev/progress/d5-evidence/smoke-d9-0001/run-smoke.sh`

**证据：** [d5-evidence/smoke-d9-0001](d5-evidence/smoke-d9-0001/)

| 检查 | 结果 | 备注 |
|------|------|------|
| git-panel → Panel **Git Panel** tab + views | **PASS** | `panelHeight` 300；与产品 Ports/Inspect 共存 |
| vscode-terminals autorun | **FAIL** | 无 xterm；Playwright 无法可靠 Toggle Terminal |
| errorlens + `d5-probe-todo.js` | **PASS** | squiggles + inline Error Lens |

**Blocker（terminal）：** 需人工确认 `Terminals: Run` / autorun 或改进 CDP 命令执行后再标矩阵 terminal 行「已实测」。

## D5 EH 探针冒烟记录（2026-09-01 rerun-2348，merge launch / loop/A 证据）

**Launch：** `REPO=/home/clarence/Projects/Agents/vscode-WorkTrees/merge dev/progress/d5-evidence/launch-with-probes.sh -- /tmp/d5-probe-sample.yaml`（post `npm run compile` on merge）。

**证据：** [d5-evidence/smoke-rerun-2348](d5-evidence/smoke-rerun-2348/)（保留 [smoke-20260901](d5-evidence/smoke-20260901/) 首轮 stale-out 记录）。

| 检查 | 结果 | 备注 |
|------|------|------|
| Workbench boot（merge `out/`） | **PASS** | EH 起；无 `IPreferencesEditorPane` |
| `redhat.vscode-yaml` 诊断 | **PASS** | 打开 sample yaml；squiggle ≥1 |
| `gruntfuggly.todo-tree` sidebar | **PARTIAL** | Activity 有产品 **TODOs** 段，无扩展独立图标；`Views: Show Todo Tree` palette 未出 pane header（见 tick 25–26 palette 重试） |
| 内置 `js-debug` F5 | **FAIL** | F5 与 `Debug: Start Debugging` → Node.js 均无 toolbar/view；需 `launch.json` 或调试配置自动化 |

**矩阵：** [eh-surface-matrix](../../docs/reference/code-oss-b2/eh-surface-matrix.md) 探针行仍为 **探针已选**（整体 smoke 未 PASS）。

**下一步：** Todo Tree 与 Agent IDE activity 槽位冲突排查；js-debug 需 workspace `launch.json` 或稳定 picker 自动化后重跑。


## D15 W1 Web 冒烟（2026-09-07，工位 A / `loop/A`）

**路径**：`/home/clarence/Projects/Agents/vscode-WorkTrees/A` · **SHA**：`1556dde3d326d344e175f9ca16d1a48c79a79686`

**命令**：`./scripts/code-web.sh --browserType none --host 127.0.0.1 --port 18080`（未跑 `npm run compile`；`out/` 已在。仓内 Node 缺失时脚本自行 `gulp node` 下载。）

**证据**：[d15-evidence/w1-1556dde3](d15-evidence/w1-1556dde3/)

| ID | 场景 | 结果 | 备注 |
|:---|:-----|:-----|:-----|
| V1 | Conversation 存在 | **PASS** | `.part.conversation` + 时间线 + SessionBar + Composer |
| V2 | Agent IDE chrome | **PASS** | 标题 `UniverseAgentStudio Dev`；四钮 Navigator / Conversation / Preview / Sources |
| V3 | Connection / Engine 省略桌面连接控件 | **PASS** | Hub / Direct / Test Connection / Test Engine 隐藏；点名文案「此环境不支持本机 Engine 连接」 |
| 连接态 | `IUniverseAgentConnection` | **disconnected** | 状态栏 `Engine not connected`；`connectProfile` 合同为 `unsupported_environment`；不画连接控件 |

**状态**：D15 **closed** @ w1-1556dde3。未升 PRD-019 `implemented`。内置扩展缺 `watch-web` dist 为非阻塞噪声。

## D8 `valid-layers-check` 复测（2026-09-02，merge 工位 / `loop/merge`）

**路径**：`/home/clarence/Projects/Agents/vscode-WorkTrees/merge` · **SHA**：`c0dfee3d`

| 步骤 | 结果 |
|------|------|
| `.nvmrc` | `24.18.0` |
| `PATH=$NVM_DIR/versions/node/v24.18.0/bin:…` 后 `node -v` | **v24.18.0** |
| `npm run valid-layers-check` | **FAIL**（`layersChecker` exit 0；`layersTypeCheck` exit 1；**166** 条 `error TS`） |
| `@typescript/native` | **7.0.2**（`layersTypeCheck` 经 `process.execPath` + 解析的 native `tsc`） |

### 分项目错误计数（同 PATH，`tsc --project build/checker/tsconfig.<p>.json`）

| 项目 | TS 错误数 |
|------|-----------|
| `browser` | **0** |
| `electron-browser` | **0** |
| `worker` | 46 |
| `node` | 52 |
| `electron-main` | 34 |
| `electron-utility` | 34 |

### 前 20 条唯一 `error TS` 文案（全量去重）

1. `TS2304: Cannot find name 'CharacterBoundsUpdateEvent'.`
2. `TS2304: Cannot find name 'EditContextEventHandlersEventMap'.`
3. `TS2304: Cannot find name 'EditContextInit'.`
4. `TS2304: Cannot find name 'GPUBufferUsage'.`
5. `TS2304: Cannot find name 'TextUpdateEvent'.`
6. `TS2322: Type 'CanvasRenderingContext2D | GPUCanvasContext | …' is not assignable to type 'GPUCanvasContext'.`
7. `TS2339: Property 'editContext' does not exist on type 'HTMLDivElement'.`
8. `TS2339: Property 'editContext' does not exist on type 'HTMLElement'.`
9. `TS2339: Property 'getAsFileSystemHandle' does not exist on type 'DataTransferItem'.`
10. `TS2339: Property 'queryPermission' does not exist on type 'FileSystemHandle'.`
11. `TS2339: Property 'requestPermission' does not exist on type 'FileSystemHandle'.`
12. `TS2339: Property 'showDirectoryPicker' does not exist on type 'CodeWindow'.`
13. `TS2345: Argument of type 'string | number | symbol' is not assignable to parameter of type 'string'.`
14. `TS2552: Cannot find name 'GPUTextureUsage'. Did you mean 'GPUTexture'?`
15. `TS2552: Cannot find name 'TextFormatUpdateEvent'. Did you mean 'ITextUpdateEvent'?`
16. `TS2709: Cannot use namespace 'EditContext' as a type.`
17. `TS2769: No overload matches this call.`
18. `TS7006: Parameter 'f' implicitly has an 'any' type.`

（全量仅 **18** 种唯一文案；其余 166 条为同文案多文件/多行重复。）

### `layersTypeCheck.ts` / tsconfig 结论

- **`build/checker/layersTypeCheck.ts` 本身无逻辑缺陷**：并行对 6 个 `tsconfig.<env>.json` 调 TS7 native `tsc`；失败来自各 env 的 **compilerOptions / include 与入图文件不匹配**，不是「没调到 tsc」。
- **`tsconfig.browser.json` 已正确**：`lib` 含 `DOM`/`DOM.Iterable`；`include` 含 `src/typings/*.d.ts`、`@webgpu/types`、`@types/wicg-file-system-access` → **单跑 browser 全绿**。
- **红域共性**：报错文件均在 `src/vs/**/browser/**`（及 `workbench/services/driver/browser`），却出现在 **`node` / `worker` / electron-node 系** 检查中——TypeScript 从 `include` 内的 `src/*.ts`（node）或 `common/**`（worker）**沿 import 拉入 browser 源码**，但：
  - `tsconfig.node.json` 仅 `lib: ES2024`（无 DOM），且 **exclude** `src/typings/editContext.d.ts`；
  - `tsconfig.worker.json` 无 `@webgpu/types` include，且同样 **exclude** `editContext.d.ts`。
- **因此**：不是「Node 24 vs 26」单因；也不是 browser checker 缺 lib。是 **非 browser 环境的 checker 图里出现了 browser 实现文件，却未套用 browser 的补充类型配置**。

### 可执行修复（优先级）

1. **对齐 worker checker 与 browser 的补充类型**（低风险、应优先试）：在 `tsconfig.worker.json` 增加与 browser 相同的 `@webgpu/types` include；**移除**对 `src/typings/editContext.d.ts` 的 exclude（browser 未 exclude）。复跑 worker 计数应下降。**已做 @ D147**（见下节）：本 SHA worker **48 → 2**，未归零。
2. **node / electron-main / utility**：`include` 含根 `src/*.ts` 引导文件会拉入整棵 workbench/browser 依赖图。可选路径：
   - **A（upstream 对齐）**：对照当前 `microsoft/vscode` main 的 `build/checker/tsconfig.node.json` 是否仍含 `src/*.ts`；若 upstream 已删或改 include，cherry-pick；
   - **B（checker-only）**：在 node 系 tsconfig 为 layer typecheck 增加与 browser 相同的 `lib` + 补充 `include`（仅影响 `valid-layers-check`，不改变 emit）；或
   - **C（架构）**：切断 bootstrap → browser 的 typecheck 边（工作量大，仅当 A/B 不可接受）。
3. **PATH**：CI/脚本须 `PATH=$NVM_DIR/versions/node/v24.18.0/bin:$PATH`（或 `hash -r`）；仅 `nvm use` 不足。
4. **验收**：`PATH=…/v24.18.0/bin:… npm run valid-layers-check` exit 0；六项目单独 `tsc` 均为 0 错。

**状态**：**open**（browser 域已绿；step 1 见 [D147](#d147)；step 2B 未开；**不**恢复门禁）。

**裁决（2026-09-02，用户委托）**：在修好之前 **`valid-layers-check` 不作集成门禁**（[health-gates](health-gates.md) 已改为 `compile` + `eslint`），D8 降 **P3**。理由：① 上表证明 `browser` / `electron-browser` 两域 **0 错**，红全部来自 node / worker 系 checker 把 browser 源码拉进图却没套 browser 补充类型——与本仓任何 `contrib/{conversation,sources}` 或 `platform/universeAgent` 变更无关；② 它本就不查 import path，M6 需要的 renderer ↔ `platform/universeAgent/node` 边界由 ESLint `local/code-layering` + boundary 测承担（ADR-003 Consequences 已更正）；③ 继续挂在 Blockers 只会让每轮 closeout 都产生一条无法闭合的红项。修复按上文「可执行修复」1 → 2B 走，任何空闲 tick 可做，做完即恢复门禁。

## D147 worker checker step 1（2026-09-09，工位 A / `loop/A`）

**路径**：`/home/clarence/Projects/Agents/vscode-WorkTrees/A` · **SHA**：`fbc1bc47cbad116b1f4d8865aaf12df7a6d4c8bb`

**改动（仅）**：`build/checker/tsconfig.worker.json` — include 加 `../../node_modules/@webgpu/types/dist/index.d.ts`（与 `tsconfig.browser.json` 同路径）；exclude 去掉 `../../src/typings/editContext.d.ts`。

**命令**（与 `layersTypeCheck.ts` 相同：Node 24.18.0 + `@typescript/native` `bin/tsc`）：

```bash
PATH="$NVM_DIR/versions/node/v24.18.0/bin:$PATH" \
  node node_modules/@typescript/native/bin/tsc --project build/checker/tsconfig.worker.json --pretty false
```

| 时机 | exit | `error TS` 条数 |
|:-----|:-----|:----------------|
| 改前 | 1 | **48**（D8 @ `c0dfee3d` 记 46；本 SHA 复测 48） |
| 改后 | 1 | **2** |

改后剩余（样本即全量）：

1. `src/vs/platform/universeAgent/common/universeAgentConnectionChannelClient.ts(81,41)` `TS2345`：`UniverseAgentNavigatorCapabilityKey`（含 `"sessionList"`）不可赋给 `UniverseAgentCapabilityKey`。
2. `src/vs/workbench/browser/parts/conversation/conversationPart.ts(159,5)` `TS2352`：`IConversationEditorPart` 转为带 `layout(...)` 的类型可能有误（缺 `layout`）。

**未做**：step 2B；`src/vs/**`；`tsconfig.node.json` / electron-main / electron-utility / `tsconfig.browser.json`；`npm run compile`；恢复 `valid-layers-check` 门禁。

**状态**：D147 **open**（worker tsc 未 exit 0）。D8 **open**。

## D17 三域基线红（切片 3 · `d2abb648c0e`）

名单正文见 [test-baseline-failures.txt](test-baseline-failures.txt)（0 数据行）。下列为一行一场景（首次 SHA / 场景 / `baseline` / owner）。**不含** D16 三文件。

| 首次 SHA | 场景 | 标记 | owner |
|:---------|:-----|:-----|:------|
| `d2abb648c0e` | conversation：`ConversationDiffReviewPane` 泄漏 **已修**（pane `dispose`/`clearEditors` + disposable listener；navigation/aggregation/fence 停引真实 contribution，改 stub）。afterEach 六行已从名单删除。官方 conversation 单 glob 不再被该泄漏中断 | leftover | B `diffreview-leak` |
| `d2abb648c0e` | conversation：editor aggregation S1a scoped uniqueness **已修**（工位 B `create-scoped`）：生产 `createConversationEditorPart` 已按 part `createScoped`；测败在 mainPart 懒建缺 `IStatusbarService.createScoped`。夹具已补，未缩断言。名单该行已删 | leftover | B `create-scoped` |
| `d2abb648c0e` | conversation：Lens visualize 两行 **已修**（A `visualize-leftover`：查询前 `revealVisualizeTurn(..., 'visualize-v2')`；merge compile 后 `--grep visualize` 3/3）。名单 0 数据行 | leftover | A `visualize-leftover` |
| `d2abb648c0e` | conversation：StatusBar 引擎入口 / H4b 相位文案 **夹具已改**（工位 A `statusbar-leftover`：去二次 `registerAction2` + Emitter leak；未缩断言、未关 D16、未改名单） | leftover | A `statusbar-leftover` |
| `d2abb648c0e` | conversation：SessionsView 四行 + OpenPendingOnFocus 无 pending **已修**（工位 B `sessions-openpending-harness`）：夹具对齐 untitled+visualize seed（空行测 `createSession()`、filter 补 rename visualize、create 落点 `sessions[2]`），未缩断言。名单五行已删 | leftover | B `sessions-openpending-harness` |
| `d2abb648c0e` | conversation：split / side-by-side **首次 layout 已修**（D `createConversationEditorPart` + 夹具）；后续 chrome 扇出 [D43](#d43) **已闭**（`ConversationPart.layout` 按叶 host 再 layout 各 editor part + resize 测） | leftover | D `s4-s5-first-layout` / D `d43-layout-fanout` |
| `793ff6e201f` | conversation：名单已空后官方单 glob **仍有未列红**（工位 A `d17-conversation-glob-reprove`）：IdentityStrip afterEach `Measured item node at 0px` 中断 glob；Lens untitled/two-leaves 虚窗未 reveal；S5 restore afterEach breadcrumb `DisposableStore` 泄漏。夹具/诚实收口后官方 glob **795/0/0**。名单仍 0 数据行。未关 D16 | leftover | A `d17-conversation-glob-reprove` |
| `d2abb648c0e` | sources：`collectSourcesReviewEntries` 委托 `toResource`/`fullTitle` 未定义 | baseline | A `test-baseline-slice3` |
| `d2abb648c0e` | universeAgent：`FileMutationJoin` lifecycle+snapshot **已修**（B：无 `diff_stats` 时 omit optional `diffStats`，不再写出 `undefined`）。名单该一行已删 | leftover | A `filemutation-join` |
| `d2abb648c0e` | universeAgent：11 个 node 测 Electron ESM `Failed to fetch dynamically imported module`（无 JUnit testcase，未进名单）：connectionResolver / deviceAuthHandshake / deviceGrantCrypto / hubControlPlane / hubDirectoryClient / hubSessionStore / observeCandidateLeaf / pairingOrchestrator / universeAgentChannel / universeAgentConnection / universeAgentHubService。Electron 采集仍排除这 11 个（[`scripts/run-unit-custom.sh`](../../scripts/run-unit-custom.sh) 只匹配 `test/node/`；`browser/universeAgentConnection` 仍跑）；可加载的 `test/node` 仍进同一 `--tfs universeAgent`。**这 11 个已改由 Node runner 跑并接回门禁**（`universeAgentNode` 418 条，见下节） | baseline | A `test-baseline-slice3` / A `unit-custom-xml` / E `gate-recovery` |

官方 conversation 单 glob：DiffReview afterEach 泄漏已修，不再中断后续用例。universeAgent 官方单 glob 仍会在首个 unloadable 文件处动态 import 失败；**unit-custom 现经 `scripts/run-unit-custom.sh`**：conversation / sources 仍单 glob；universeAgent 按文件列表采集（Electron 跳过上表 11 个 `test/node`，其余同 `--tfs universeAgent`）；那 11 个改由 Node runner 以 `universeAgentNode` 接回门禁。名单按分批收齐后的 JUnit 差集去重。

### 那 11 个文件已接回门禁（2026-09-07，工位 E / `fix/gate-recovery`）

「Electron 不可加载」只说明加载器不对，不说明测试不该跑。实测这 11 个文件在 **Node runner 下 418 条全过、0 skipped、0 failing**，此前被 `run-unit-custom.sh` 整体跳过，等于 418 条真测试完全不在门禁内。

| 项 | 内容 |
|:---|:-----|
| runner 改动 | `test/unit/node/index.js` 加 `--tfs <域名>`，用 `mocha-multi-reporters` 同时保留 spec 输出与 JUnit XML，文件命名沿用 Electron runner 同一套布局 |
| 新域 | `universeAgentNode` → `test-results/linux-x64-universeagentnode-results.xml` |
| 门禁 | `check-test-baseline.sh` 域列表从三域改为四域；`test-baseline-failures.txt` 头部加 `universeAgentNode=418` / `max_skipped=0` |
| 反向验证 | XML 缺失 → 报 missing 并退 1；用例数降到 417 → 报 `below min_cases=418` 并退 1 |

**未做**：这 11 个在 Electron 下为何 `Failed to fetch dynamically imported module` 仍未查（Node runner 能跑不代表 Electron 侧的加载问题消失）。该问题只影响 Electron 采集路径，不再影响这批测试是否被执行。

## 维护规则

1. **新增**：分配下一 `D<n>` ID；须有可验证 Exit Condition。
2. **闭合**：`Status` → `closed`；并在 `status.md` 记摘要。
3. **与 Research Queue 分工**：本表 = 已知怎么做但优先级/环境不够；Research Queue = 先搞清再干。
| D522 | P3 | **closed** `connectionPreferencesPane.ts` Hub 设备行 Connect 丢 `handleConnectDevice` Promise：`HubDevicesRenderer(device => this.handleConnectDevice(device), …)`。方法体已有 try/catch 与 `writeConnectStatus`。与 D478 同型：catch-path paint 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480 / D518）。本刀只改 `device => void this.handleConnectDevice(device).catch(onUnexpectedError).catch(onUnexpectedError)`。未改方法体 leftover / leftover-looks-live。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`。未改 D518 八处 void。mocha 锁：设备行 Connect 点击 `addHubDeviceProfile` reject 且 catch-path `writeConnectStatus` throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不重开 D174/D180/D182/D186/D518。D24 仍开。不得宣称 leftover/pills 完成。 | 工位 G `leftover-d522-connect-device`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/connectionPreferencesPane.test.ts` **154/0**（含 `assertCleanState` 1；既有 153 + warn 再抛 1） | 设备行 Connect 双链 catch；warn 再抛 0 unhandled；既有 leftover / D518 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186/D518。本行只关设备行 Connect Promise，leftover 程序未全局完成。 | conversation / connection-preferences | closed |
| D523 | P3 | **closed** `sourcesChangesList.ts` 三处 `void` 无 catch：Enter 键 `runCommit()`；`RunOnceScheduler` 的 `refresh()`；`onRowAction` 的 `runResourceAction`。三方法体已有 try/catch（commit/stage 吞 write；refresh 吞 git-read 后 `setStatusMessage`）。与 D478 同型：方法体内已吞则 catch-path paint 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480）。未改方法体 leftover / leftover-looks-live / listed-gate / FileDiff / Mark 写闸。未发明 WriteGitUnstage。未改 `sourcesReviewList.ts`（D520 已闭）。未改 `sourcesDiffPanelView.ts`。mocha 锁于 `sourcesReviewList.test.ts`：scheduler `refresh` `readGitChanges` reject 且 stub `setStatusMessage` throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）；Enter `runCommit` `writeGitCommit` reject 且 catch-path paint throw 且 handler 先 warn 再抛 → 0 unhandled；`onRowAction` stage `writeGitStagePaths` reject 且 catch-path paint throw 且 handler 先 warn 再抛 → 0 unhandled。既有 Changes commit/stage throw DOM 测仍绿。未扫其它文件。未碰 grpc / pills / catalog `onOpenConnection`。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186/D245/D520。D24 仍开。本行只关本文件 fire-and-forget，leftover 程序未全局完成。 | 工位 C `leftover-d523-sources-changes-void`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/sources/test/browser/sourcesReviewList.test.ts` **38/0**（含 `assertCleanState` 1；既有 35 + warn 再抛 3） | 三处双链 catch；warn 再抛 0 unhandled；既有 leftover / D520 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186/D245/D520。本文件 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sources / changes | closed |
| D524 | P3 | **closed** `sourcesDiffPanelView.ts` 六处 `void` 无 catch：`onDidChangeRef` `renderRef(ref)`；header Stage `runStage` / Accept `runAccept` / Revert `runGitAction(CLEAN)` / Unstage `runGitAction(UNSTAGE)`；`renderBody` `renderRef(currentRef)`。方法体已有 try/catch + notices（Stage/Accept/git 写失败 `showActionNotice`；load fail `showLoadNotice`）。与 D478 同型：方法体内已吞则 catch-path notice 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480）。未改 `runAccept` 方法体 / Accept A2 / `WriteGitApplyHunks` 载荷。未发明 `WriteGitUnstage`。未碰 `conversationDiffReviewPane.ts`（槽 I）/ `sourcesChangesList.ts`（槽 C）。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`（不加 catch）。mocha 锁于 `sourcesDiffPanel.test.ts`：Stage 点击 `writeGitStagePaths` reject 且 stub `showActionNotice` throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）；`renderRef` load fail 且 stub `showLoadNotice` throw 同锁。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186。D24 仍开。本行只关本文件 fire-and-forget，leftover 程序未全局完成。 | 工位 D `leftover-d524-sources-diff-panel-void`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/sources/test/browser/sourcesDiffPanel.test.ts` **18/0**（含 `assertCleanState` 1；既有 15 + warn 再抛 2） | 六处双链 catch；warn 再抛 0 unhandled；既有 leftover 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186。本文件 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sources / diff-panel | closed |
| D525 | P3 | **closed** `sourcesFilesList.ts` 两处 fire-and-forget 无 catch：`RunOnceScheduler` 的 `refresh()`；`list.onDidOpen` 的 `openEditor`。`refresh()` 方法体已有 try/catch（吞 fetchChildren）；catch-path `setStatusMessage` 再抛会漏。`onDidOpen` 无内层 try/catch，`openEditor` reject 会漏。本刀两处补 `.catch(onUnexpectedError).catch(onUnexpectedError)`（对齐 D478 方法体已吞 / D480：单层在 handler warn 再抛时仍漏）。未改 explorer model / leftover presentation helpers。未改 Changes/Review/Diff panel。mocha 锁于 `sourcesFilesList.test.ts`：scheduler `refresh` collect throw 且 stub `setStatusMessage` throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）；`onDidOpen` `openEditor` reject 且 handler 先 warn 再抛 → 0 unhandled（无 paint 路径）。接线扫描锁 scheduler / onDidOpen 双链。未扫其它文件。未碰 grpc / pills / catalog `onOpenConnection`。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186/D215。D24 仍开。本行只关本文件 fire-and-forget，leftover 程序未全局完成。 | 工位 H `leftover-d525-sources-files-refresh`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/sources/test/browser/sourcesFilesList.test.ts` **5/0**（含 `assertCleanState` 1；既有 2 + warn 再抛 2） | 两处双链 catch；warn 再抛 0 unhandled；既有 leftover 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186。本文件 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sources / files | closed |
| D526 | P3 | **closed** `conversationDiffReviewPane.ts` 五处 `void` 无 catch：Revert `runGitAction(SOURCES_GIT_CLEAN_COMMAND)`；Unstage `runGitAction(SOURCES_GIT_UNSTAGE_COMMAND)`；Stage `runStage()`；Accept `runAccept()`；Preview `executeCommand('sources.diff.moveToPreview')`。`runGitAction` 方法体已有 catch + `showNotice`（[D75](#d75)）；`runStage` / `runAccept` 方法体已有 try/catch。与 D478 同型：方法体内已吞则 catch-path `showNotice` 再抛会漏。Preview `executeCommand` 无方法体 catch。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故五处均再链一层（D480）。未改 `runAccept` 语义 / A2（仍空 patches、不 `git.stage`）。未发明 `WriteGitUnstage`。未碰 `sourcesDiffPanelView`（槽 D）。未碰 `sourcesReviewList`（[D520](#d520)）。mocha 锁于 `sourcesDiffPanel.test.ts`：Revert/Unstage/Stage 点击 `executeCommand` reject 且 stub `showNotice` throw 且 handler 先 warn 再抛 → 0 unhandled；Accept 点击空 payload 不可用 notice 且 stub `showNotice` throw 同锁；Preview `executeCommand` reject 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。`sourcesWiringScan.test.ts` 源扫五处 `void` 均为 `.catch(onUnexpectedError).catch(onUnexpectedError)`。未扫其它文件。未碰 grpc / pills / catalog `onOpenConnection`。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D75/D174/D180/D182/D186。D24 仍开。本行只关本文件 fire-and-forget，leftover 程序未全局完成。 | 工位 I `leftover-d526-diff-review-pane-void`；定点 transpile 后分文件 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run`：`sourcesDiffPanel.test.ts` **21/0**（含 `assertCleanState` 1；既有 15 + warn 再抛 5）；`sourcesWiringScan.test.ts` **13/0**（含 `assertCleanState` 1；既有 11 + 五处 void 源扫 1）；`sourcesChangesGitWrite.test.ts` **18/0**（含 `assertCleanState` 1；既有 17） | 五处双链 catch；warn 再抛 0 unhandled；D75 `runGitAction` catch + `showNotice` 源扫仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D75/D174/D180/D182/D186/D520。本文件 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sources / review-pane | closed |
| D527 | P3 | **closed** `navigatorTeamList.ts` 两处：`RunOnceScheduler` `void this.refreshTeamData()` 无 catch（`refreshTeamData` 方法体已有 try/catch，D53 leftover）；members `onDidOpen` reveal 残留单层 `.catch(onUnexpectedError)` 升双链。`openInspectPanel` `openView` ~723 已双链，不重做。对齐 D478/D480：方法体内已吞则 catch-path notice/paint 再抛会漏；单层在 `setUnexpectedErrorHandler` warn 再抛时仍漏。未改 leftover-looks-live / pairing-hold / listed-gate。未碰 `navigatorAgentsView.ts`（D521）。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`。mocha 锁于 `navigatorTeamSubviews.test.ts`：scheduler `refreshTeamData` `memberStatus` throw 且 stub `setTeamSnapshotNote` throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）；member row-open reveal catch-path notify 再抛且 handler 先 warn 再抛 → 0 unhandled。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D53/D174/D180/D182/D186/D491/D521。D24 仍开。本行只关本文件 fire-and-forget，leftover 程序未全局完成。 | 工位 J `leftover-d527-navigator-team-catch`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/navigator/test/browser/navigatorTeamSubviews.test.ts` **41/0**（含 `assertCleanState` 1；既有 38 + warn 再抛 2） | 两处双链 catch；warn 再抛 0 unhandled；既有 D53 leftover 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D53/D174/D180/D182/D186/D521。本文件 leftover fire-and-forget 已收；leftover 程序未全局完成。 | navigator / team | closed |
| D528 | P2 | **closed** `sourcesChangesList.ts` 三按钮 click 丢 Promise：`stageSelectedButton` `runOnSelected('stage')`；`unstageSelectedButton` `runOnSelected('unstage')`；`commitButton` `runCommit()`。三方法体已有 try/catch（D523 已收 Enter / scheduler / onRowAction）。与 D478 同型：方法体内已吞则 catch-path paint 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480）。未改方法体 leftover / leftover-looks-live / listed-gate / FileDiff / Mark 写闸。未发明 WriteGitUnstage。Enter 键 leftover 测不重做。mocha 锁于 `sourcesChangesList.test.ts`：Stage / Unstage / Commit **按钮 click** `writeGitStagePaths` / `git.unstage` / `writeGitCommit` reject 且 stub `setStatusMessage` throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。`sourcesWiringScan.test.ts` 源扫三处 click 均为 `.catch(onUnexpectedError).catch(onUnexpectedError)`。未扫其它文件。未碰 grpc / pills / catalog `onOpenConnection`。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186/D245/D523。D24 仍开。本行只关本文件三按钮 click fire-and-forget，leftover 程序未全局完成。 | 工位 C `leftover-d528-changes-three-buttons`；定点 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/sources/test/node/sourcesWiringScan.test.ts` **15/0**（含 Errors 1；既有 13 + 三按钮 click 源扫 1）。`sourcesChangesList.test.ts` Node runner 因 `.css` 无法加载；`VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/sources/test/browser/sourcesChangesList.test.ts` **27/0**（含 `assertCleanState` 1；既有 23 + warn 再抛 3）。Enter leftover 回归 `sourcesReviewList.test.ts` **38/0**。 | 三按钮 click 双链 catch；warn 再抛 0 unhandled；既有 leftover / D523 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D523。本文件三按钮 leftover fire-and-forget 已收；leftover 程序未全局完成。 | leftover | closed |
| D529 | P3 | **closed** `conversationSessionsView.ts` `openSessionBeside` 残留单层 `.catch(onUnexpectedError)` 升双链。`ConversationSessionWindowService.openSessionBeside` 方法体已有 try/catch（ensureLeaf fail 后 warn）；catch-path warn 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480）。未改方法体 leftover / leftover-looks-live / pairing-hold。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`。mocha 锁于 `conversationSessionsView.test.ts`：既有 inner-warn-throw（handler 吞）仍绿；另锁 catch-path warn throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186。D24 仍开。本行只关本文件 fire-and-forget，leftover 程序未全局完成。 | 工位 D `leftover-d529-sessions-view-double-catch`；定点 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/conversation/test/browser/conversationSessionsView.test.ts` 因 `.css` ESM 加载失败（`aria.css`，既有 Node runner 不能跑 browser 测）。改 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationSessionsView.test.ts` **40/0**（含 `assertCleanState` 1；既有 38 + warn 再抛 1） | 调用点双链 catch；warn 再抛 0 unhandled；既有 inner-warn-throw 测仍绿。未关 D8/D16/D147/D24/D405/D487。本文件 leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / sessions | closed |
| D530 | P3 | **closed** `conversationSessionWindowService.ts` 三处 `void` 无 catch：`onDidChangeActiveSession` `revealSessionWindow(sessionKey)`；`attachGrid` pending `revealSessionWindow(pending.sessionKey, pending.options)`；`attachGrid` 无 pending `ensurePrimaryWindow(getActiveSessionId())`。两方法体已有 try/catch（create throw 后 `notificationService.error`）。与 D478 同型：方法体内已吞则 catch-path notice 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480）。未改方法体 leftover / leftover-looks-live。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`。mocha 锁实际调用点：`fireActiveSession` / delayed-slot pending attach / ctor `attachGrid` ensurePrimary，catch-path notice throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。源扫三处均为双链。未扫其它文件。未碰 pills。未发明 proto / Connect 映射。未 compile-client。不得宣称 leftover/pills 完成。不重开 D90/D139。D24 仍开。本行只关本文件 fire-and-forget，leftover 程序未全局完成。 | 工位 F `leftover-d530-session-window-void`；定点 transpile 后分文件 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run`：`conversationSessionWindowReveal.test.ts` **15/0**（含 `assertCleanState` 1；既有 11 + warn 再抛 2 + 源扫 1）；`conversationSessionWindowSideBySide.test.ts` **13/0**（含 `assertCleanState` 1；既有 11 + warn 再抛 1）。`node test/unit/node/index.js --run` 无法加载 browser 测（`.css` import；既有限制） | 三处双链 catch；warn 再抛 0 unhandled；既有 reveal / side-by-side 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D90/D139。本文件 leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / session-window | closed |
| D531 | P3 | **closed** `conversationLensSessionBinding.ts` `copyTurn`：D143 已在 `writeText` reject 后 `showPostFailure('failed')`，但该 catch 若 `showPostFailure` 再抛会漏 rejection。单层 `.catch(onUnexpectedError)` 在 `setUnexpectedErrorHandler` warn 再抛时仍漏（D480）。本刀：`writeText` reject 仍 `showPostFailure('failed')`（不 `throw err`，剪贴板失败已有 UI notice，不当 unexpected）；再链 `.catch(onUnexpectedError).catch(onUnexpectedError)`。未改 deleteTurn / cancelToolCall / resolveConfirmation。未闸 leftover-looks-live。mocha 锁于 `conversationLensDisposeGate.test.ts`：既有 reject → `failed` 且 0 unhandled 仍绿；新增 `showPostFailure` throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）且仍 `failed`。未扫其它文件。未碰 grpc / pills / catalog `onOpenConnection`。未发明 proto。未 compile-client。不重开 D143/D174/D180/D182/D186。D24 仍开。不得宣称 leftover/pills 完成。本行只关 copyTurn catch-path，leftover 程序未全局完成。 | 工位 G `loop/G-leftover-d531-copyturn-catch`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationLensDisposeGate.test.ts` **106/0**（含 `assertCleanState` 1；既有 104 + warn 再抛 1）。`node test/unit/node/index.js --run` 同文件因 `.css` `ERR_UNKNOWN_FILE_EXTENSION` 未能加载（既有 Node runner 限制，非本刀回归） | copyTurn 双链 catch；writeText reject 仍 `failed`；showPostFailure throw + warn 再抛 0 unhandled；既有 D143 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D143。本文件 copyTurn fire-and-forget 已收；leftover 程序未全局完成。 | conversation / lens | closed |
| D532 | P3 | **closed** `conversationTurnContentAdapter.ts` `actionHandler` 丢 `handleTimelineLink(href)` Promise。方法体内层 try/catch **只包** `URI.parse`，后面 `activateConversationLink` / `openUaClientExternalLink` / `openLinkFromMarkdown` 的 await reject 仍漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀只改 `void this.handleTimelineLink(href).catch(onUnexpectedError).catch(onUnexpectedError)`。未改方法体 leftover / leftover-looks-live。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`。未碰 `grpcClient`。mocha 锁于既有 `conversationTimelineSessionPills.test.ts`：同会话 tool pill 点击 `openSubAgent` reject 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。方法体内层 parse catch 不够锁本洞。未扫其它文件。未碰 pills 装饰合同。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186。D24 仍开。本行只关本调用点 fire-and-forget，leftover 程序未全局完成。 | 工位 H `leftover-d532-timeline-link`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationTimelineSessionPills.test.ts` **17/0**（含 `assertCleanState` 1；既有 15 + warn 再抛 1）。指定 `node test/unit/node/index.js --run` 因 browser 测拉 `.css` 无法加载（既有限制） | 调用点双链 catch；warn 再抛 0 unhandled；既有 pills 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186。本调用点 leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / timeline-link | closed |
| D533 | P3 | **closed** `conversationLeafSessionBar.ts` 一处 `void this.switchLeafSession(sessionId)` 无 catch（`switchToSession`）。`switchLeafSession` 为 async（await `revealSessionWindow`）。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480）。未改 `switchLeafSession` 方法体。未改 `conversationLensSessionBar.ts` helper（SelectBox 仍 `void host.switchLeafSession`）。未改 `conversationSessionsView.ts` / `conversationSessionWindowService.ts` / `conversationTurnContentAdapter.ts`。mocha 锁于既有 leaf bar 覆盖 `conversationSessionWindowReveal.test.ts`：`switchToSession` 在 `switchLeafSession` reject 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186。D24 仍开。本行只关本方法 fire-and-forget，leftover 程序未全局完成。 | 工位 I `leftover-d533-leaf-session-bar`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationSessionWindowReveal.test.ts` **13/0**（含 `assertCleanState` 1；既有 11 + warn 再抛 1）。父指令 `node test/unit/node/index.js --run` 无法加载 browser CSS，改走 electron `scripts/test.sh`（同型 leftover 切片） | 双链 catch；warn 再抛 0 unhandled；既有 leaf bar 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186。本方法 leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / leaf-session-bar | closed |
| D534 | P3 | **closed** `engineToolsSection.ts` Save 点击丢 `savePendingEnablement` Promise：`saveButton.onDidClick(() => void this.savePendingEnablement())`。方法体已有 try/catch（吞 `saveAgentProfile` throw / 空 id，画 `.engine-catalog-write-status`）。constructor / `onDidChangeConnection` `void this.refresh()` 已双链，不重做。与 D478 同型：方法体内已吞则 catch-path paint 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀只改 Save 点击 `void this.savePendingEnablement().catch(onUnexpectedError).catch(onUnexpectedError)`。未改方法体 leftover / leftover-looks-live / pairing-hold。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`（不加 catch）。未碰 grpcClient。mocha 锁于 `engineToolsSection.test.ts`：Save 点击 `saveAgentProfile` reject 且 catch-path `writeStatus` throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。既有 leftover D414 / refresh·loadToolInfo warn 再抛仍绿。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D58/D174/D180/D182/D186。D24 仍开。本行只关本文件 Save fire-and-forget，leftover 程序未全局完成。 | 工位 J `leftover-d534-engine-tools-save`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/engineToolsSection.test.ts` **14/0**（含 `assertCleanState` 1；既有 12 + warn 再抛 1）。父指定 `node test/unit/node/index.js --run` 因 browser `.css` ESM 无法加载（既有） | Save 点击双链 catch；warn 再抛 0 unhandled；既有 leftover / refresh·loadToolInfo 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D58/D174/D180/D182/D186。本文件 leftover fire-and-forget 已收（OPEN_CONNECTION 除外）；leftover 程序未全局完成。 | conversation / engine-tools | closed |
| D535 | P3 | **closed** `conversationLensSessionBar.ts` helper `switchToSession` 一处 `void host.switchLeafSession(sessionId)` 无 catch（SelectBox `onDidSelect` 生产路径）。D533 已双链 class `ConversationLeafSessionBar.switchToSession`（`void this.switchLeafSession`），helper 仍裸 void。`switchLeafSession` 为 async（await `revealSessionWindow`）。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480）。本刀只改 helper：`void host.switchLeafSession(sessionId).catch(onUnexpectedError).catch(onUnexpectedError)`；`switchLeafSession?` 签名收为 `Promise<void>` 以便 `.catch` 类型成立。未改 class `switchToSession` / `switchLeafSession` 方法体。未改 `conversationSessionWindowService.ts` / `conversationSessionsView.ts` / `conversationTurnContentAdapter.ts`。未碰 grpcClient。mocha 锁于既有 `conversationSessionWindowReveal.test.ts`：导出 helper `switchToSession(host, id)`（非 class `switchToSession`）在 `switchLeafSession` reject 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）；源扫 helper 行双链。既有 D533 class 测仍绿。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186/D533。D24 仍开。本行只关 helper/SelectBox fire-and-forget，leftover 程序未全局完成。 | 工位 F `leftover-d535-lens-session-bar-helper`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationSessionWindowReveal.test.ts` **18/0**（含 `assertCleanState` 1；既有 16 + helper warn 再抛 1 + helper 源扫 1）。`node test/unit/node/index.js --run` 因 browser `.css` ESM 无法加载（既有） | helper 双链 catch；SelectBox helper 路径 warn 再抛 0 unhandled；既有 D533 class 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D533。本 helper leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / lens-session-bar | closed |
| D536 | P3 | **closed** `conversationEngineRosterService.ts` 五处 fire-and-forget 无双链 catch：`setEngineConnected` / `onUaConnectionChanged` 的 `void this.refreshEngineCatalog()`（方法体 `doRefreshEngineCatalog` 已 try/catch，list throw 后 persist 再抛会漏）；`createSession` 的 `void this.ensureEngineSession().then(...)`（已有 fail-action catch，catch-path `fire` 再抛会漏）；`bindLiveTreeObservationLease` 的 `monitorPendingEngineSessionBind` / `monitorListedEngineSessionBind`（async 无方法体 catch，`whenBindReady` reject 会漏）。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故再链一层（D480）。`void this.engineSessionEnsure.finally` **未加** catch：赋值是 `Promise.resolve`，finally 回调只清 inflight，当前不丢会 reject 的 Promise。未改方法体 leftover / leftover-looks-live / pairing-hold。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`。未碰 grpcClient。mocha 锁于既有 roster 测：`setEngineConnected` / `onUaConnectionChanged` list throw 且 catch-path `saveRosterStorage` throw 且 handler 先 warn 再抛 → 0 unhandled；`createSession` acquire throw 且 catch-path fire throw 同锁；live-tree listed/pending `whenBindReady` reject 同锁（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。源扫五处 void 均为双链；finally 仍无 catch。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D25/D174/D180/D182/D186。D24 仍开。本行只关本文件 fire-and-forget，leftover 程序未全局完成。 | 工位 C `loop/C-leftover-d536-roster-voids`；定点 transpile 后分文件 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run`：`conversationEngineRosterService.test.ts` **115/0**（含 `assertCleanState` 1；既有 110 + warn 再抛 3 + 源扫 1）；`conversationEngineRosterLiveTree.test.ts` **7/0**（含 `assertCleanState` 1；既有 4 + warn 再抛 2）。`node test/unit/node/index.js --run` 因 browser `.css` ESM 无法加载（既有） | 五处双链 catch；`engineSessionEnsure.finally` 经检查不加；warn 再抛 0 unhandled；既有 leftover / live-tree 测仍绿。未关 D8/D16/D147/D24/D405/D487。本文件 leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / engine-roster | closed |
| D537 | P3 | **closed** `conversationLensDock.ts` 三处 `void host.submitDraft()` 无 catch（Enter 发送；send 容器 capture click；`sendButton.onDidClick`）。`submitDraft` 为 async；方法体已有 try/catch（`postBound` reject 后 `showPostFailure('failed')`）。与 D478 同型：方法体内已吞则 catch-path paint 再抛会漏。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀三处改 `void host.submitDraft().catch(onUnexpectedError).catch(onUnexpectedError)`。未改方法体 leftover / leftover-looks-live / pairing-hold。未改同文件 `void applySessionPermissionIndex` / `void applySessionModelIndex`（既有裸 void，非本刀）。未碰 `conversationEngineRosterService` / `conversationLensSessionBar` / `conversationEditorPane`。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`。未碰 grpcClient。mocha 锁于既有 `conversationLensDisposeGate.test.ts`：Send 点击 / capture click / Enter，`host.submitDraft` reject 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。源扫三处均为双链。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D143/D174/D180/D182/D186/D531。D24 仍开。本行只关本文件三处 submitDraft fire-and-forget，leftover 程序未全局完成。 | 工位 D `loop/D-leftover-d537-dock-submit-draft`；定点 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/conversation/test/browser/conversationLensDisposeGate.test.ts` 因 `.css` ESM 加载失败（`list.css`，既有 Node runner 不能跑 browser 测）。改 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationLensDisposeGate.test.ts` **110/0**（含 `assertCleanState` 1；既有 106 + warn 再抛 3 + 源扫 1） | 三处双链 catch；warn 再抛 0 unhandled；既有 leftover / D531 copyTurn 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D531。同文件 permission/model `void applySession*` 仍裸。本文件 submitDraft leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / lens-dock | closed |
| D538 | P3 | **closed** `conversationEditorPane.ts` 面包屑 `onDidSelect` 丢 `navigateAgentBreadcrumb` Promise：`void this.sessionChatService.navigateAgentBreadcrumb(parsed.sessionKey, chatId)`。方法体已有 try/catch（D87 notice）；overlay 调用点已双链（D513）。与 D478 同型：方法体内已吞则 catch-path notice 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀只改 editor pane 调用点 `void this.sessionChatService.navigateAgentBreadcrumb(...).catch(onUnexpectedError).catch(onUnexpectedError)`。未改 overlay `onDidSelectBreadcrumb`（D513）。未改方法体 leftover / leftover-looks-live。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`。mocha 锁于 `conversationEditorFence.test.ts`：editor pane 面包屑祖先点击 `navigateAgentBreadcrumb` reject 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。既有 overlay leftover `conversationSessionChat.test.ts` 不重做。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D87/D174/D180/D182/D186/D513。D24 仍开。本行只关本调用点 fire-and-forget，leftover 程序未全局完成。 | 工位 G `loop/G-leftover-d538-editor-breadcrumb`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationEditorFence.test.ts` **10/0**（含 `assertCleanState` 1；既有 8 + warn 再抛 1）。`node test/unit/node/index.js --run` 因 browser `.css` ESM 无法加载（既有） | pane 点击双链 catch；warn 再抛 0 unhandled；既有 fence 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D87/D513。本调用点 leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / editor-pane | closed |
| D539 | P3 | **closed** `conversationLensReadingColumn.ts` `onReviewNavClick` 丢 `executeCommand(SOURCES_REVIEW_SHOW_FOR_PATHS_COMMAND, …)` Promise。`executeCommand` 返回 Promise；裸 `void` 在 reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀抽出 `executeReadingColumnSourcesReview`：`void commandService.executeCommand(...).catch(onUnexpectedError).catch(onUnexpectedError)`；`mountTimeline` 的 `onReviewNavClick` 只调该函数。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`（不加 catch）。未改 leftover-looks-live / pairing-hold / `requestReadingColumnDetail`。未碰 `grpcClient` / roster / dock / editor pane / session bar。mocha 锁于既有 `conversationLensDisposeGate.test.ts`：`executeCommand` reject 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）；源扫 `onReviewNavClick` 走 helper 且 helper 为双链。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186。D24 仍开。本行只关本调用点 fire-and-forget，leftover 程序未全局完成。 | 工位 H `loop/H-leftover-d539-reading-column-review`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationLensDisposeGate.test.ts` **108/0**（含 `assertCleanState` 1；既有 106 + warn 再抛 1 + 源扫 1）。`node test/unit/node/index.js --run` 同文件因 `.css` `ERR_UNKNOWN_FILE_EXTENSION` 未能加载（既有 Node runner 限制，非本刀回归） | 调用点双链 catch；warn 再抛 0 unhandled；既有 reading-column leftover / D531 copyTurn 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186。本调用点 leftover fire-and-forget 已收（OPEN_CONNECTION 除外）；leftover 程序未全局完成。 | conversation / reading-column | closed |
| D540 | P3 | **closed** `conversationEngineFrameSource.ts` `onHostFrame` 的 `void this.sessionView.acknowledge(...)`（`acknowledge` 为 async）。D145 已补 `.catch(() => undefined)` 吞 reject，帧仍落地。单层 / 吞 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏（D480）。本刀只改该调用点为 `.catch(onUnexpectedError).catch(onUnexpectedError)`。未改 `requestResync` / `releaseLease` 的 `.catch(() => undefined)`。未改 roster service。未改 leftover-looks-live / pairing-hold。mocha 锁于 `conversationEngineFrameSource.test.ts`：既有 acknowledge reject 仍 apply 且 0 unhandled 仍绿；新增一帧 `acknowledge` reject 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）且帧仍落地。未扫其它文件。未碰 grpc / pills / catalog `onOpenConnection`。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D145/D144/D174/D180/D182/D186。D24 仍开。本行只关本调用点 fire-and-forget，leftover 程序未全局完成。 | 工位 I `leftover-d540-frame-ack`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationEngineFrameSource.test.ts` **23/0**（含 `assertCleanState` 1；既有 21 + warn 再抛 1）。`node test/unit/node/index.js --run` 因 browser 测限制未跑 | 调用点双链 catch；warn 再抛 0 unhandled；既有 acknowledge reject 仍 apply 测绿。未关 D8/D16/D147/D24/D405/D487。不重开 D145。本调用点 leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / frame-source | closed |
| D541 | P3 | **closed** `conversationLensSessionBinding.ts` `retryError`：D80 已在 `postBound` reject 后 `showPostFailure('failed')`，但该 catch 若 `showPostFailure` 再抛会漏 rejection。单层 `.catch(onUnexpectedError)` 在 `setUnexpectedErrorHandler` warn 再抛时仍漏（D480）。本刀：`postBound` reject 仍 `showPostFailure('failed')`（不 `throw err`，写失败已有 UI notice，不当 unexpected）；再链 `.catch(onUnexpectedError).catch(onUnexpectedError)`。未改 `copyTurn`（D531）。未改 deleteTurn / cancelToolCall / resolveConfirmation / resolveQuestion。未闸 leftover-looks-live。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`。mocha 锁于 `conversationLensDisposeGate.test.ts`：既有 reject → `failed` 且 0 unhandled 仍绿；新增 `showPostFailure` throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）且仍 `failed`。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不重开 D80/D143/D174/D180/D182/D186/D531。D24 仍开。不得宣称 leftover/pills 完成。本行只关 retryError catch-path，leftover 程序未全局完成。 | 工位 J `loop/J-leftover-d541-retryerror-catch`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationLensDisposeGate.test.ts` **107/0**（含 `assertCleanState` 1；既有 105 + warn 再抛 1）。`node test/unit/node/index.js --run` 同文件因 `.css` `ERR_UNKNOWN_FILE_EXTENSION` 未能加载（既有 Node runner 限制，非本刀回归） | retryError 双链 catch；postBound reject 仍 `failed`；showPostFailure throw + warn 再抛 0 unhandled；既有 D80 leftover 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D80/D531。本文件 retryError fire-and-forget 已收；leftover 程序未全局完成。 | conversation / lens | closed |
| D542 | P3 | **closed** `conversationLensDock.ts` 两处 `void applySessionPermissionIndex` / `void applySessionModelIndex` 无 catch（Permission SelectBox `onDidSelect` ~152；Model SelectBox `onDidSelect` ~184）。两函数均为 async；方法体已有 try/catch（`setPermissionMode` / `switchModel` reject 后 restore + `showGateNotice`）。与 D478 同型：方法体内已吞则 catch-path paint 再抛会漏。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀两处改 `void applySession*(...).catch(onUnexpectedError).catch(onUnexpectedError)`。未改方法体 leftover / leftover-looks-live / pairing-hold。未改 `conversationLensComposerChrome.ts` More radios `void applySessionPermissionIndex`（槽 D）。未改同文件 `submitDraft` 三处（D537）。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`。未碰 grpcClient。mocha 锁于既有 `conversationLensDisposeGate.test.ts`：Permission / Model SelectBox `onDidSelect`，unary reject 且 catch-path `showGateNotice` throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。源扫两处均为双链。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D76/D308/D383/D450/D537。D24 仍开。本行只关本文件两处 applySession fire-and-forget，leftover 程序未全局完成。 | 工位 F `loop/F-leftover-d542-dock-apply-session`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationLensDisposeGate.test.ts` **116/0**（含 `assertCleanState` 1；既有 112 + warn 再抛 2 + 源扫 1）。`node test/unit/node/index.js --run` 同文件因 `.css` `ERR_UNKNOWN_FILE_EXTENSION` 未能加载（既有 Node runner 限制，非本刀回归） | 两处双链 catch；warn 再抛 0 unhandled；既有 leftover / D537 submitDraft 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D537。未改 composerChrome More radios。本文件 applySession leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / lens-dock | closed |
| D543 | P3 | **closed** `conversationEngineRosterService.ts` `continueEngineGeneration` 一处 `void pending;` 丢 `postIfHeld` 返回的 Promise。D536 已双链 refresh / monitor / `ensureEngineSession` 五处，本路径仍裸 void。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀只改 `void pending.catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** D536 已双链的 refresh / monitor 调用点。未改方法体 leftover / leftover-looks-live / pairing-hold。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`。未碰 grpcClient。mocha 锁于既有 `conversationEngineRosterService.test.ts`：`retryError` 在 `postIfHeld` reject 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。源扫 `void pending` 为双链；refresh/monitor 五处仍双链、未改。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D536/D174/D180/D182/D186。D24 仍开。本行只关本调用点 fire-and-forget，leftover 程序未全局完成。 | 工位 C `loop/C-leftover-d543-roster-void-pending`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationEngineRosterService.test.ts` **116/0**（含 `assertCleanState` 1；既有 114 + warn 再抛 1；源扫仍锁 refresh/monitor 并扩 pending 双链）。`node test/unit/node/index.js --run` 因 browser `.css` ESM 无法加载（既有） | 调用点双链 catch；warn 再抛 0 unhandled；既有 leftover / D536 refresh·monitor 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D536。本调用点 leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / engine-roster | closed |
| D544 | P3 | **closed** `conversationLensComposerChrome.ts` 一处 `void applySessionPermissionIndex(...)` 无 catch（More 菜单 permission radio 点击）。`applySessionPermissionIndex` 为 async；方法体已有 try/catch（`setPermissionMode` reject 后 `showGateNotice`）。与 D478 同型：方法体内已吞则 catch-path paint 再抛会漏。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `void applySessionPermissionIndex(...).catch(onUnexpectedError).catch(onUnexpectedError)`。同文件无其它 `void applySession*`。未改 `conversationLensDock.ts` `void applySessionPermissionIndex` / `void applySessionModelIndex`（槽 F）。未改方法体 leftover / leftover-looks-live / pairing-hold。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`。未碰 grpcClient。mocha 锁于既有 `conversationLensDisposeGate.test.ts`：live More radio 点击，`setPermissionMode` reject 且 catch-path `showGateNotice` throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。源扫本文件 `void applySession*` 均为双链。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D308/D383/D450/D537。D24 仍开。本行只关本文件 fire-and-forget，leftover 程序未全局完成。 | 工位 D `loop/D-leftover-d544-chrome-permission-index`；定点 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/conversation/test/browser/conversationLensDisposeGate.test.ts` 因 `.css` ESM 加载失败（`list.css`，既有 Node runner 不能跑 browser 测）。改 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationLensDisposeGate.test.ts` **115/0**（含 `assertCleanState` 1；既有 113 + warn 再抛 1 + 源扫 1） | 一处双链 catch；warn 再抛 0 unhandled；既有 leftover-looks-live More radios 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D537。dock `applySession*` 仍裸。本文件 leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / lens-composer-chrome | closed |
| D546 | P3 | **closed** `conversationLens.ts` 构造里 `void resolveConversationMermaidExtension(...).then(...)` 无调用点 catch。D142 已在 helper 把 `getExtension` reject 吞成 `undefined`，但 `.then` 里 `setMermaidExtensionInfo` / `applySessionViewTimeline` 再抛仍漏；helper 若日后 reject 也会漏。单层 `.catch(onUnexpectedError)` 在 `setUnexpectedErrorHandler` warn 再抛时仍漏（D480）。本刀抽出 `scheduleConversationMermaidExtensionResolve`：`void resolve…then` 再链 `.catch(onUnexpectedError).catch(onUnexpectedError)`；构造改为调该函数。未改 `conversationMermaidHost.ts` helper / mount fallback / onFatalError。未改 leftover-looks-live / pairing-hold。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`。mocha 锁于 `conversationLensDisposeGate.test.ts`：resolve 成功且 then 抛且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）；`getExtension` reject（helper 回 `undefined`）且 then 抛且 handler 先 warn 再抛 → 0 unhandled（同锁）；源扫构造走 helper 且 helper 为双链。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D142/D174/D180/D182/D186。D24 仍开。本行只关本调用点 fire-and-forget，leftover 程序未全局完成。 | 工位 G `loop/G-leftover-d546-lens-mermaid`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationLensDisposeGate.test.ts` **116/0**（含 `assertCleanState` 1；既有 112 + warn 再抛 2 + 源扫 1）。`node test/unit/node/index.js --run` 同文件因 `.css` `ERR_UNKNOWN_FILE_EXTENSION` 未能加载（既有 Node runner 限制，非本刀回归） | 调用点双链 catch；then-throw / getExtension reject+then-throw + warn 再抛 0 unhandled；既有 leftover / D142 helper 测不重做仍合同。未关 D8/D16/D147/D24/D405/D487。不重开 D142。本调用点 leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / lens-mermaid | closed |
| D547 | P3 | **closed** `conversationTrajectoryDetailInspector.ts` `beginFetch` 一处 `void Promise.resolve(request(detailRef)).then(...)` 无调用点 catch。`.then` 第二参已把 `requestDetail` reject 收成 `{ ok:false, reason:'failed' }`；`settle` 的 `_onDidChange.fire()` 若 listener 抛、且 `setUnexpectedErrorHandler` warn 再抛，void promise 仍漏 unhandled。单层 `.catch(onUnexpectedError)` 在 handler warn 再抛时仍漏，故再链一层（D480）。本刀只补 `.catch(onUnexpectedError).catch(onUnexpectedError)`。未改 `settle` / leftover-looks-live / pairing-hold / `requestReadingColumnDetail`。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`。mocha 锁于既有 `conversationTrajectoryDetailInspector.test.ts`：`requestDetail` reject 且 `onDidChange` throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）且仍 `failed`。源扫 `beginFetch` 为双链。未扫其它文件。未碰 grpc / pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D40/D174/D180/D182/D186/D353。D24 仍开。本行只关本调用点 fire-and-forget，leftover 程序未全局完成。 | 工位 H `loop/H-leftover-d547-trajectory-detail`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationTrajectoryDetailInspector.test.ts` **10/0**（含 `assertCleanState` 1；既有 8 + warn 再抛 1 + 源扫 1）。`node test/unit/node/index.js --run` 因 browser 测限制未跑。未 commit。 | 调用点双链 catch；warn 再抛 0 unhandled；既有 leftover-looks-live 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D40/D353。本调用点 leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / trajectory-detail | closed |
| D548 | P3 | **closed** `conversationSplitActions.contribution.ts` ~90 `void cycleSameConversationChatTablist(...).then(...)`。函数返回 Promise（`openEditor`）。裸 `void` 且 `.then` 无 rejection handler，reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀只改该调用点：`.then(() => focusActiveConversationChatTab(...)).catch(onUnexpectedError).catch(onUnexpectedError)`。未改 `cycleConversationChatTab` / Action2 `nextChatTab` / `previousChatTab` / `splitSessionWindow`。未改 leftover-looks-live / pairing-hold。mocha 锁于既有 `conversationSessionSplit.test.ts`：tablist ArrowRight 且 `openEditor` reject 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）；源扫该 void 为双链。未扫其它文件。未碰 grpc / pills / catalog `onOpenConnection`。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D93/D174/D180/D182/D186。D24 仍开。本行只关本调用点 fire-and-forget，leftover 程序未全局完成。 | 工位 I `loop/I-leftover-d548-split-tablist`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationSessionSplit.test.ts` **8/0**（含 `assertCleanState` 1；既有 5 + warn 再抛 1 + 源扫 1）。`node test/unit/node/index.js --run` 因 browser `.css` ESM 无法加载（既有） | 调用点双链 catch；warn 再抛 0 unhandled；既有 split 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D93。本调用点 leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / split-tablist | closed |
| D549 | P3 | **closed** `conversationLensComposer.ts` 接通支路 `void loadConnectedComposerCatalogs(host, generation)` 无 catch。`loadConnectedComposerCatalogs` 为 async；方法体内三 list 已 try/catch（D50/D235 leftover restore）。与 D478 同型：方法体内已吞则 catch-path restore 再抛会漏。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀只改该调用点 `void loadConnectedComposerCatalogs(host, generation).catch(onUnexpectedError).catch(onUnexpectedError)`。未改方法体 leftover / leftover-looks-live / pairing-hold。未改 `conversationLens.ts` 门面。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`（不加 catch）。未碰 grpcClient。mocha 锁于 `conversationComposerCatalog.test.ts`：`refreshComposerCatalogs` 接通后 list throw 且 catch-path `setOptions` throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。源扫该 void 为双链。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D50/D235/D249/D339/D174/D180/D182/D186。D24 仍开。本行只关本调用点 fire-and-forget，leftover 程序未全局完成。 | 工位 J `loop/J-leftover-d549-load-composer-catalogs`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationComposerCatalog.test.ts` **24/0**（含 `assertCleanState` 1；既有 21 + warn 再抛 1 + 源扫 1）。`node test/unit/node/index.js --run` 因 browser `.css` ESM 无法加载（既有） | 调用点双链 catch；warn 再抛 0 unhandled；既有 leftover / KEEP / leftover-looks-live 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D50/D235/D249/D339。本调用点 leftover fire-and-forget 已收（OPEN_CONNECTION 除外）；leftover 程序未全局完成。 | conversation / composer | closed |
| D550 | P3 | **closed** `conversationInboxOverlay.ts` 两处单链 `.catch(onUnexpectedError)`：L117 `void this.onGoalClicked()`（Goal `onDidClick`）；L478 `void this.onEnqueueClicked()`（Enqueue click）。两方法均为 async；写失败走 `showPostFailure`，成功走 `render`。与 D478 同型：方法体内已吞则 catch-path paint 再抛会漏。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀两处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。未改方法体 leftover / leftover-looks-live / pairing-hold。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`（不加 catch）。未碰 grpcClient。mocha 锁于既有 `conversationInboxOverlay.test.ts`：Goal / Enqueue 点击，catch-path `showPostFailure` / `render` throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。源扫两处均为双链、非单链。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186。D24 仍开。本行只关本文件两处 fire-and-forget，leftover 程序未全局完成。 | 工位 C `loop/C-leftover-d550-inbox-overlay`；定点 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationInboxOverlay.test.ts` **49/0**（含 `assertCleanState` 1；既有 43 + warn 再抛 4 + 源扫 1）。未跑 `node test/unit/node/index.js`（browser `.css` ESM 既有限制）。未 commit。 | 两处双链 catch；Goal/Enqueue `showPostFailure`·`render` throw + warn 再抛 0 unhandled；源扫非单链。既有 leftover / KEEP 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186。本文件两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / inbox-overlay | closed |
| D551 | P3 | **closed** `conversationEngineHistoryList.ts` 四处单层 `.catch(onUnexpectedError)` 升级为双链（D485 已补单层；本刀对齐 D480：单层在 `setUnexpectedErrorHandler` warn 再抛时仍漏）。listeners / `show()` 的 `refresh()` 四处。`refresh()` 方法体已有 try/catch（`paintListFailed`）。catch-path paint throw + handler warn 再抛会漏。本刀四处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。未改 refresh 方法体 leftover / leftover-looks-live / pairing-hold。未碰 inbox overlay。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。mocha 锁于 `conversationEngineHistoryList.test.ts`：`show()` 在 `getHistory` reject 且 catch-path `paintListFailed` throw 且 handler 先 warn 再抛 → 0 unhandled（`unhandledRejections []`，`unexpectedWarns [paintBoom, paintBoom]`）。源扫四处 void 均为双链。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D174/D180/D182/D186/D485。D24 仍开。本行只关本文件 fire-and-forget，leftover 程序未全局完成。 | 工位 D `loop/D-leftover-d551-engine-history-list`；增量 transpile 后 `VSCODE_SKIP_PRELAUNCH=1 ./scripts/test.sh --run src/vs/workbench/contrib/conversation/test/browser/conversationEngineHistoryList.test.ts` **16/0**（含 `assertCleanState` 1；既有 13 + warn 再抛 1 + 源扫 1）。未 commit。 | 四处双链 catch；warn 再抛 0 unhandled；既有 leftover 测仍绿。未关 D8/D16/D147/D24/D405/D487。不重开 D174/D180/D182/D186/D485。本文件 leftover fire-and-forget 已收；leftover 程序未全局完成。 | conversation / history | closed |
| D552 | P3 | **closed** `sessionViewHost.ts` 五处裸 `void` fire-and-forget：`void this.bringUpBoundSession` 两处（acquireLease / onEngineConnectionChanged）；`void this.writeChat`；`void this.fillHistory`；`void this.sendHeartbeatAck`（heartbeat arm）。四方法均为 async。D23 已补 `sendHeartbeatAck` 方法体 bind/`resident.write`/`chat` try/catch；D38 已补 `fillHistory` / `bringUpBoundSession` 方法体 catch。方法体内已吞则 catch-path 再抛会漏。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀五处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** D23 方法体 inner catch。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`。未碰 grpcClient。未碰 D145 frame swallows。mocha 源扫锁于既有 `sessionViewHostPostDiscipline.test.ts`：五处均为双链；裸 `void this.writeChat(` 在剥双链后不在源。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D23/D38。D24 仍开。本行只关本文件这些 call site fire-and-forget，leftover 程序未全局完成。 | 工位 J `loop/J-d24-host-void-leftover-catch`；定点 transpile 后 `node test/unit/node/index.js --run src/vs/platform/universeAgent/test/node/sessionViewHostPostDiscipline.test.ts` **3/0**（既有 post-discipline 1 + 源扫 1 + Errors 1）。未 commit。 | 五处双链 catch；裸 writeChat 源扫不在。未关 D8/D16/D147/D24/D405/D487。不重开 D23/D38。本文件这些 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | M7 universeAgent | closed |
| D556 | P3 | **closed** `pluginListWidget.ts` 七处裸 `void this.refresh()`：plugins autorun；marketplaces `onDidChange`；activeHarness autorun；itemProvider `onDidChange`；ctor 初刷；`installMarketplacePlugin` 成功后；`setVisible`。`refresh()` 为 async。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀七处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。未改方法体 leftover。未改同文件 `void this.queryMarketplaceSnapshot` / `void this.queryMarketplace` / `void this.filterPlugins`。未碰 mcpListWidget / grpcClient / `OPEN_CONNECTION` / D145。mocha 源扫锁于 `pluginListWidgetRefreshCatch.test.ts`：七处均为双链；裸 `void this.refresh();` 不在源。cwd-first 读源（`out` 为 symlink）。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。D24 仍开。本行只关本文件这些 call site，leftover 程序未全局完成。 | 工位 G `loop/G-d24-plugin-list-leftover`；增量 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/pluginListWidgetRefreshCatch.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。 | 七处双链 catch；裸 `void this.refresh();` 源扫不在。未关 D8/D16/D147/D24/D405/D487。同文件其它 void 仍裸。本文件这些 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | chat / plugin-list | closed |
| D553 | P3 | **closed** `universeAgentConnectionChannelClient.ts` 两处裸 `void` fire-and-forget：`void this.refreshPhaseAndNotify()`（`onDidChangeConnection`）；`void this.hydrate()`（构造）。两方法均为 async。D81 已补方法体 try/catch。方法体内已吞则 catch-path 再抛会漏。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀两处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** D81 方法体 inner catch。未碰 `universeAgentHubChannelClient.ts` hydrate。未碰 `void this.remote.requestAgentTreeRefresh`。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`。未碰 grpcClient。未碰 D145 `.catch(() => undefined)`。mocha 源扫锁于新增 `universeAgentConnectionChannelClientScan.test.ts`（node）：两处均为双链；裸 `void this.hydrate();` / `void this.refreshPhaseAndNotify();` 不在源。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D23/D38/D81/D552。D24 仍开。本行只关本文件这两处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 C `loop/C-d24-connection-channel-leftover`；定点 transpile 后 `node test/unit/node/index.js --run src/vs/platform/universeAgent/test/node/universeAgentConnectionChannelClientScan.test.ts` **2/0**（源扫 1 + Errors 1）。同 runner 另跑 `universeAgentRendererSync.test.ts` **22/0**（D81 hydrate/refreshPhase 测仍绿）。未 commit。 | 两处双链 catch；裸 hydrate/refreshPhase 源扫不在。未关 D8/D16/D147/D24/D405/D487。不重开 D23/D38/D552。本文件这两处 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | M7 universeAgent | closed |
| D554 | P3 | **closed** `universeAgentHubChannelClient.ts` 构造一处裸 `void this.hydrate()`（`hydrate` 为 async）。D84 已补方法体 try/catch（失败保留预 hydrate 默认、不 fire）。方法体内已吞则 catch-path 再抛会漏。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `void this.hydrate().catch(onUnexpectedError).catch(onUnexpectedError)`；`onUnexpectedError` 自 `base/common/errors.js`。**未改** `setActiveHubBaseUrl` 的 `void this.remote.setActiveHubBaseUrl`。未碰 `connectionChannelClient.ts` / grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。mocha 源扫锁于 `universeAgentHubChannelClientHydrateCatch.test.ts`：本文件 hydrate 为双链；裸 `void this.hydrate();` 不在本文件。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D84。D24 仍开。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 D `loop/D-d24-hub-channel-leftover`；增量 transpile 后 `node test/unit/node/index.js --run src/vs/platform/universeAgent/test/node/universeAgentHubChannelClientHydrateCatch.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。 | 一处双链 catch；裸 `void this.hydrate();` 本文件源扫不在。未关 D8/D16/D147/D24/D405/D487。不重开 D84。本 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | M7 universeAgent | closed |
| D555 | P3 | **closed** `mcpListWidget.ts` leftover fire-and-forget：三处已知裸 `void this.refresh()`（ctor / `setVisible` / access 恢复）；三处无 `void` 的 `this.refresh()`（`onChange` / 两 autorun）；`void this.queryMcpSearch()`；两处 `void this.queryGallerySnapshot`；两处 `delayedGallerySearch.trigger` 丢 Promise。三方法均为 async。`queryGallerySnapshot` / `queryMcpSearch` 方法体已有 try/catch（catch-path `renderMcpHome` / `filterServers`）。方法体内已吞则 catch-path paint 再抛会漏。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀上述 call site 改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。未改 `filterServers`（sync void）。未改 `await this.refresh()` / `await this.queryGallerySnapshot()`。未改 `registerMcpInlineButtonAction` 的 `void action()`、`installMarketplaceServer` 点击、`executeCommand`。未锁 `pluginListWidget` / voice。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145。mocha 源扫锁于 `mcpListWidgetCatchScan.test.ts`（cwd-first 读 `.ts`，不需 Electron）：六处 refresh 双链；query / delayed 双链；裸 `void this.refresh();` 不在源。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这些 call site，leftover 程序未全局完成。 | 工位 F `loop/F-d24-mcp-list-leftover`；增量 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/mcpListWidgetCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。 | 本文件 refresh / queryMcpSearch / queryGallerySnapshot fire-and-forget 双链；源扫锁本文件。未关 D8/D16/D147/D24/D405/D487。不锁 sibling widgets。leftover 程序未全局完成。 | workbench / mcp-list | closed |
| D557 | P3 | **closed** `voiceEventStreamView.ts` 两处裸 `void this.refresh()`：autorun mid-turn→idle（~82）；`renderBody` 初绘（~111）。`refresh()` 为 async；方法体已有 try/catch（`logService.warn` + `renderEmpty`）。方法体内已吞则 catch-path paint 再抛会漏。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀两处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。未改 `refresh()` 方法体 leftover / leftover-looks-empty / pairing-hold。未改 `voiceTranscriptsView.ts`。未碰 catalog `onOpenConnection` / `OPEN_CONNECTION`。未碰 grpcClient。未碰 D145 frame swallows。mocha 源扫锁于新增 `voiceEventStreamViewCatchScan.test.ts`：两处均为双链；裸 `void this.refresh();` 与单链不在源。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。D24 仍开。本行只关本文件这两处 fire-and-forget，leftover 程序未全局完成。 | 工位 H `loop/H-d24-voice-event-leftover`；定点 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/agentsVoice/test/node/voiceEventStreamViewCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。 | 两处双链 catch；裸 refresh 源扫不在。未关 D8/D16/D147/D24/D405/D487。未改 voiceTranscriptsView。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | agentsVoice / event-stream | closed |
| D558 | P3 | **closed** `voiceTranscriptsView.ts` 两处裸 `void this.refresh()`（autorun ~84；`renderBody` ~113）。`refresh()` 为 async，方法体已有 try/catch（`renderEmpty`）。方法体内已吞则 catch-path paint 再抛会漏。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀两处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** `voiceEventStreamView.ts`。未碰 grpcClient / `OPEN_CONNECTION` / D145。mocha 源扫锁于新 `voiceTranscriptsViewScan.test.ts`（本文件 only）：两处均为双链；裸 `void this.refresh();` / 单链不在源。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。D24 仍开。本行只关本文件这两处 call site，leftover 程序未全局完成。 | 工位 I `loop/I-d24-voice-transcripts-leftover`；定点 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/agentsVoice/test/node/voiceTranscriptsViewScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。 | 两处双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。未改 voiceEventStreamView。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / agentsVoice | closed |
| D559 | P3 | **closed** `universeAgentMigration.contribution.ts` 一处裸 `void this.maybeOffer()`（ctor ~89）。`maybeOffer` 为 async；方法体 probe `exists` 已 try/catch，store/prompt 再抛仍漏。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。未改 migrate 命令 / offer 门 / 拷贝 allow-list。未碰 grpcClient / sessionViewHost / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 frame swallows。mocha 源扫锁于 `universeAgentMigrationContributionScan.test.ts`：本文件 `void this.maybeOffer()` 一处且双链；裸/单链不在源。未扫其它文件。残留：同文件 prompt `run` 的 `hostService.reload()` / `executeCommand(MIGRATE_FROM_CODE_OSS_COMMAND_ID)` 仍无 catch（非本 call site，不扩本行）。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D552。D24 仍开。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 J `loop/J-d24-migration-offer-leftover`；增量 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/universeAgentMigration/test/node/universeAgentMigrationContributionScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。 | ctor maybeOffer 双链；源扫裸/单链不在。未关 D8/D16/D147/D24/D405/D487。不重开 D552。本 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / universeAgentMigration | closed |
| D560 | P3 | **closed** `dictationOnboarding.ts` 两处裸 `void this.refreshMicrophones()`：devicechange ~596；无 preview 初刷 ~621。`refreshMicrophones` 为 async；方法体 `enumerateDevices` 已 try/catch，`renderPicker` 再抛仍漏。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀两处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** `DictationOnboardingService.refreshMicrophones` 的 `void this.currentBanner?.refreshMicrophones`。未改 `void this.startPreview()` / `preview.listen().then` / `switchMicrophone().then(refresh).catch(log)`。未碰 `voiceModeOnboarding.ts` / grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145。mocha 源扫锁于新增 `dictationOnboardingCatchScan.test.ts`（本文件 only）：两处均为双链；裸 `void this.refreshMicrophones();` / 单链不在源。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。D24 仍开。本行只关本文件这两处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 A `loop/A-d24-dictation-mic-leftover`；增量 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/dictationOnboardingCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。 | 两处双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。同文件 startPreview / service 门面 / listen.then / switchMicrophone 仍裸或单链。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / dictation-onboarding | closed |
| D561 | P3 | **closed** `universeAgentConnectionChannelClient.ts` 一处裸 `void this.remote.requestAgentTreeRefresh(sessionId)`（~107）。接口 `IUniverseAgentConnection.requestAgentTreeRefresh` 标 `void`，但 `ProxyChannel.toService` 仍返回 Promise；裸 `void` 在 IPC reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `void Promise.resolve(this.remote.requestAgentTreeRefresh(sessionId)).catch(onUnexpectedError).catch(onUnexpectedError)`（`Promise.resolve` 收 typed-void / runtime-Promise）。**未改** D553 已双链的 hydrate / refreshPhaseAndNotify。未碰 `universeAgentHubChannelClient.ts` / grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。mocha 源扫锁于既有 `universeAgentConnectionChannelClientScan.test.ts`（本文件 only）：该 call site 为双链；裸/单链不在源。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D81/D553。D24 仍开。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 C `loop/C-d24-tree-refresh-leftover`；定点 transpile 后 `node test/unit/node/index.js --run src/vs/platform/universeAgent/test/node/universeAgentConnectionChannelClientScan.test.ts` **3/0**（D553 源扫 1 + D561 源扫 1 + Errors 1）。未 commit。 | 本 call site 双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。不重开 D553。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | M7 universeAgent | closed |
| D562 | P3 | **closed** `universeAgentHubChannelClient.ts` `setActiveHubBaseUrl` 一处裸 `void this.remote.setActiveHubBaseUrl(hubBaseUrl)`。接口 typed `void`，但 `ProxyChannel.toService` 每个方法都是 `async function`（`ipc.ts`），IPC reject 成未处理 rejection。裸 `void` 漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `void this.remote.setActiveHubBaseUrl(hubBaseUrl).catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** hydrate 双链（D554 仍在）。未改方法体先 `cache.applyHubBaseUrl`（IPC 失败时 renderer leftover 新 URL 不回滚，非本刀）。未碰 `connectionChannelClient.ts` / `void this.remote.requestAgentTreeRefresh` / grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。mocha 源扫锁于 `universeAgentHubChannelClientHydrateCatch.test.ts`（本文件 only）：setActiveHubBaseUrl 为双链；裸/单链不在；hydrate 双链仍在。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D84/D554。D24 仍开。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 D `loop/D-d24-hub-set-url-leftover`；增量 transpile 后 `node test/unit/node/index.js --run src/vs/platform/universeAgent/test/node/universeAgentHubChannelClientHydrateCatch.test.ts` **3/0**（源扫 2 + Errors 1）。未 commit。 | 一处双链 catch；裸/单链源扫不在；hydrate 双链仍在。未关 D8/D16/D147/D24/D405/D487。不重开 D84/D554。本 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | M7 universeAgent | closed |
| D563 | P3 | **closed** `mcpListWidget.ts` leftover fire-and-forget（D555 明确留下）：`registerMcpInlineButtonAction` 的 `void action()`（authenticate / showOutput 返回 Promise）；两处 `install.onDidClick(() => this.installMarketplaceServer(...))`（方法 async）。裸 Promise 在 reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀：`action()` 仅当 `instanceof Promise` 才 `.catch(onUnexpectedError).catch(onUnexpectedError)`（sync void 跳过）；两处 install 改 `void this.installMarketplaceServer(...).catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** D555 已双链的 refresh / query / delayedGallerySearch。未改方法体 leftover。未碰 `pluginListWidget` / grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145。mocha 源扫锁于既有 `mcpListWidgetCatchScan.test.ts`：refresh/query 六处仍双链；`void action();` 不在源；两处 install 双链。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D555。D24 **仍开**。本行只关本文件这些 call site，leftover 程序未全局完成。残留：同文件 `executeCommand` 三处 click（AddConfiguration ×2、openSettings）仍无 catch（非本 call site）。列表 Sign In（~357）authenticate reject 无 `notificationService.error`（卡片路径 ~1728 有），属既有 UX，非本刀。 | 工位 F `loop/F-d24-mcp-install-leftover`；未 compile-client；`node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/mcpListWidgetCatchScan.test.ts` **3/0**（源扫 2 + Errors 1）。未 commit。 | action Promise 双链、sync void 跳过；两处 install 双链；D555 refresh/query 仍双链。未关 D8/D16/D147/D24/D405/D487。不重开 D555。本文件这些 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / mcp-list | closed |
| D564 | P3 | **closed** `pluginListWidget.ts` D556 留下的三处裸 query fire-and-forget：两处 `void this.queryMarketplaceSnapshot()`（`renderPluginHome` / discovery Retry）；一处 `void this.queryMarketplace()`（`toggleBrowseMode(true)`）。两方法均为 async；方法体已有 try/catch（catch-path `renderPluginHome` / empty marketplace）。方法体内已吞则 catch-path paint 再抛会漏。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀三处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** 已双链的七处 `void this.refresh()`（D556）。未改 `await this.queryMarketplace()`。未改 `filterPlugins`（虽为 async，非本 call site）。未改 `delayedMarketplaceSearch.trigger` / `queryPluginSearch`。未碰 mcpListWidget / grpcClient / `OPEN_CONNECTION` / D145。mocha 源扫锁于既有 `pluginListWidgetRefreshCatch.test.ts`：两处 snapshot + 一处 query 均为双链；裸 `void this.queryMarketplaceSnapshot();` / `void this.queryMarketplace();` 不在源；七处 refresh 仍双链。cwd-first 读源（`out` 为 symlink）。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D556。D24 仍开。本行只关本文件这些 call site，leftover 程序未全局完成。 | 工位 G `loop/G-d24-plugin-query-leftover`；增量 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/pluginListWidgetRefreshCatch.test.ts` **3/0**（源扫 refresh 1 + query 1 + Errors 1）。未 commit。 | 三处 query 双链；裸 query void 源扫不在；D556 refresh 七处仍双链。未关 D8/D16/D147/D24/D405/D487。不重开 D556。本文件这些 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | chat / plugin-list | closed |
| D565 | P3 | **closed** `voiceModeOnboarding.ts` 两处裸 `void this.refreshMicrophones()`：devicechange（~781）；初刷（~782）。方法为 async；`enumerateDevices` 已 try/catch，`updateMicrophonePicker` 再抛仍漏。裸 `void` 在 reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀两处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** `dictationOnboarding.ts` / `voiceEventStreamView.ts`。未碰 grpcClient / `OPEN_CONNECTION` / D145。mocha 源扫锁于新 `voiceModeOnboardingCatchScan.test.ts`（本文件 only，cwd-first）：两处均为双链；裸/单链不在源。未扫其它文件。残留：同文件 `executeCommand` / `updateValue` 仍单链 `logService.error`（非本 call site）。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。D24 仍开。本行只关本文件这两处 call site，leftover 程序未全局完成。 | 工位 H `loop/H-d24-voice-mic-leftover`；定点 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/agentsVoice/test/node/voiceModeOnboardingCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。 | 两处双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。未改 dictationOnboarding / voiceEventStreamView。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / agentsVoice | closed |
| D566 | P3 | **closed** `aiCustomizationManagementEditor.ts` 八处裸 `void` fire-and-forget：六处 `refreshCustomizationMigrationInfo()`（enablement 配置变更；prompts 变更；createContent 初刷；harness autorun；load-error retry；goBackToList 回 migration）；一处 `migrateSelectedCustomizations`（Migrate 点击）；一处 `revealMigratedCustomizations`（migrate 成功后）。三方法均为 async。`refreshCustomizationMigrationInfo` 方法体已有 try/catch（catch-path `renderCustomizationMigrationPage` / `onUnexpectedError`）；`migrateSelectedCustomizations` / `revealMigratedCustomizations` 无方法体 catch。方法体内已吞则 catch-path paint 再抛会漏；裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。**跳过** `await this.refreshCustomizationMigrationInfo()`（migrate 成功路径，已 await）。本刀八处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。未改方法体 leftover。未改同文件 `void this.listWidget.setSection` / `void this.showEmbeddedEditor` / `handleEditorActionButton` / `saveExistingCustomization` / `saveBuiltinPromptCopy` 单层 catch。未碰 mcpListWidget / pluginListWidget / grpcClient / `OPEN_CONNECTION` / D145。mocha 源扫锁于 `aiCustomizationManagementEditorCatchScan.test.ts`（本文件 only）：refresh 六处 / migrate 一处 / reveal 一处均为双链；裸/单链不在源；await 仍无 catch。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。D24 仍开。本行只关本文件这些 call site，leftover 程序未全局完成。 | 工位 I `loop/I-d24-customization-editor-leftover`；增量 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/aiCustomizationManagementEditorCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。 | 八处双链 catch；await 站点未加 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。同文件其它 void 仍裸。本文件这些 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | chat / customization-editor | closed |
| D567 | P3 | **closed** `agentPluginsView.ts` 一处裸 `void this.show(this.currentQuery)`（`RunOnceScheduler` ~280）。`show` 为 async；方法体无 try/catch（`queryMarketplacePlugins` 已吞 fetch 成 `[]`；`getPluginInstallUri` / `list.model` / `updateBody` 再抛仍漏）。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。未改 `show()` 方法体 leftover / leftover-looks-empty。未改同文件 `editorService.openEditor` 丢 Promise。未碰 grpcClient / `OPEN_CONNECTION` / D145 / sessionViewHost。mocha 源扫锁于 `agentPluginsViewShowCatchScan.test.ts`（本文件 only，cwd-first）：`void this.show(this.currentQuery)` 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。D24 仍开。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 J `loop/J-d24-agent-plugins-show-leftover`；增量 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/agentPluginsViewShowCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。 | scheduler `show` 双链；源扫裸/单链不在。未关 D8/D16/D147/D24/D405/D487。本 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / chat-agent-plugins | closed |
| D568 | P3 | **closed** `dictationOnboarding.ts` 两处裸 `void this.startPreview()`：preview 初启 ~616；`setVisible(true)` ~638。`startPreview` 为 async；方法体无 try/catch（`preview.listen` / `refreshMicrophones` reject 会漏；`refreshMicrophones` 内 `enumerateDevices` 已 try/catch，`renderPicker` 再抛仍漏）。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀两处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** D560 已双链的两处 `refreshMicrophones`。**未改** `DictationOnboardingService.refreshMicrophones` 的 `void this.currentBanner?.refreshMicrophones`。未改 `preview.listen().then` / `switchMicrophone().then(refresh).catch(log)`。未碰 `voiceModeOnboarding.ts` / grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145。mocha 源扫锁于既有 `dictationOnboardingCatchScan.test.ts`（本文件 only）：两处 startPreview 均为双链；裸 `void this.startPreview();` / 单链不在源；D560 refreshMicrophones 两处仍双链。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。D24 仍开。本行只关本文件这两处 startPreview call site fire-and-forget，leftover 程序未全局完成。 | 工位 A `loop/A-d24-dictation-preview-leftover`；定点 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/dictationOnboardingCatchScan.test.ts` **3/0**（D560 源扫 1 + D568 源扫 1 + Errors 1）。未 commit。 | 两处 startPreview 双链 catch；裸/单链源扫不在；D560 refresh 仍双链。未关 D8/D16/D147/D24/D405/D487。不重开 D560。残留：banner 门面 `currentBanner?.refreshMicrophones`、`preview.listen().then`、`switchMicrophone().then(refresh).catch(log)`。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / dictation-onboarding | closed |
| D569 | P3 | **closed** `embeddedMcpServerDetail.ts` 一处裸 `void this.loadSourceDefinition(server, server.source, renderGeneration)`（~158）。方法为 async；方法体已有 try/catch（读失败画 `mcpDefinitionLoadFailed`），catch-path `setDefinition` 再抛仍漏。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。未改方法体 leftover。未碰 `mcpListWidget.ts` / grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。mocha 源扫锁于 `embeddedMcpServerDetailCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 C `loop/C-d24-mcp-embedded-detail-leftover`；未 compile-client；`node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/embeddedMcpServerDetailCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。 | 本 call site 双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。不改 mcpListWidget。本 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / chat-mcp-detail | closed |
| D570 | P3 | **closed** `agentHostSessionListStore.ts` 一处裸 `void this.refresh(CancellationToken.None)`（workspace-folders 监听 ~111）。`refresh` 为 async `Promise<void>`。`_doRefresh` 仅吞 `listSessions()`；`_makeEntryFromMetadata` / `_isSessionInWorkspace` / fire 再抛仍漏。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。未改方法体 leftover / leftover-looks-empty（`listSessions` 空 catch）。未碰 grpcClient / hubChannelClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。mocha 源扫锁于 `agentHostSessionListStoreCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 D `loop/D-d24-agenthost-session-list-leftover`；定点 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/agentHostSessionListStoreCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。 | 本 call site 双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / agentHost-session-list | closed |
| D571 | P3 | **closed** `mcpListWidget.ts` leftover fire-and-forget（D563 明确留下）：三处 click 丢 `this.commandService.executeCommand(...)` Promise——donor AddConfiguration ~1214；openSettings ~1338；card AddConfiguration ~1588。`ICommandService.executeCommand` 返回 `Promise`。裸 Promise 在 reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀三处改 `void this.commandService.executeCommand(...).catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** D555 已双链的 refresh / query / delayedGallerySearch。**未改** D563 已双链的 action / installMarketplaceServer。未改 `await commandService.executeCommand(McpCommandIds.AgentHostServerOptions, ...)`（已 await）。未碰 `pluginListWidget` / grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145。mocha 源扫锁于既有 `mcpListWidgetCatchScan.test.ts`：refresh/query 六处仍双链；action/install 仍双链；三处 executeCommand 双链。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D555/D563。D24 **仍开**。本行只关本文件这些 call site，leftover 程序未全局完成。残留：列表 Sign In（~357）authenticate reject 无 `notificationService.error`（卡片路径 ~1728 有），属既有 UX，非本刀。 | 工位 F `loop/F-d24-mcp-execute-command-leftover`；未 compile-client；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/mcpListWidgetCatchScan.test.ts` **4/0**（源扫 3 + Errors 1）。未 commit。 | 三处 executeCommand 双链；D555 refresh/query 仍双链；D563 action/install 仍双链。未关 D8/D16/D147/D24/D405/D487。不重开 D555/D563。本文件这些 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / mcp-list | closed |
| D572 | P3 | **closed** `pluginListWidget.ts` D556/D564 留下的两处裸 `void this.filterPlugins()`：`toggleBrowseMode(false)`（~1727）；`toggleGroup`（~2008）。`filterPlugins` 为 async。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀两处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** 已双链的七处 `void this.refresh()`（D556）。**未改** 已双链的 `queryMarketplaceSnapshot` ×2 / `queryMarketplace` ×1（D564）。未改 `await this.filterPlugins()`。未改 `delayedFilter.trigger(() => this.filterPlugins())`。未改 `refresh()` 内无 await 的 `this.filterPlugins()`。未改 marketplace install 点击。未碰 mcpListWidget / grpcClient / `OPEN_CONNECTION` / D145。mocha 源扫锁于既有 `pluginListWidgetRefreshCatch.test.ts`：两处 filterPlugins 均为双链；裸 `void this.filterPlugins();` / 单链不在源；七处 refresh 与三处 query 仍双链。cwd-first 读源（`out` 为 symlink）。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D556/D564。D24 仍开。本行只关本文件这两处 call site，leftover 程序未全局完成。 | 工位 G `loop/G-d24-plugin-filter-leftover`；定点 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/pluginListWidgetRefreshCatch.test.ts` **4/0**（源扫 refresh 1 + query 1 + filter 1 + Errors 1）。未 commit。 | 两处 filterPlugins 双链；裸/单链源扫不在；D556 refresh 七处与 D564 query 三处仍双链。未关 D8/D16/D147/D24/D405/D487。不重开 D556/D564。残留：两处 marketplace `install.onDidClick` 仍裸调 async `installMarketplacePlugin`。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | chat / plugin-list | closed |
| D573 | P3 | **closed** `voiceModeOnboarding.ts` 两处单层 `logService.error` Promise catch（D565 残留）：settings/instructions `executeCommand(commandId)`（~1030）；`updateValue` persist Voice Mode voice（~1093）。均为丢弃的 Promise；单层 catch 在 mocha `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀两处改 `void … .catch(onUnexpectedError).catch(onUnexpectedError)`。**跳过** `audio.play()` 单层 `logService.trace` catch（另有 `this.stop()`；换成 onUnexpectedError 会藏预览失败日志且丢 stop）。**未改** D565 `refreshMicrophones` 双链。**未改** D145 `context.close().catch(() => { /* already closing */ })`。未碰 `dictationOnboarding.ts` / grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。mocha 源扫锁于既有 `voiceModeOnboardingCatchScan.test.ts`（本文件 only）：executeCommand / persist 均为双链；单链 log-catch 不在源；D565 两处 refresh 仍双链；D145 swallow 与 `audio.play` log-catch 仍在。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关这两处 call site，leftover 程序未全局完成。 | 工位 H `loop/H-d24-voice-execute-command-leftover`；定点 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/agentsVoice/test/node/voiceModeOnboardingCatchScan.test.ts` **3/0**（D565 源扫 1 + D573 源扫 1 + Errors 1）。未 commit。 | 两处双链 catch；单链 log-catch 源扫不在。未关 D8/D16/D147/D24/D405/D487。不重开 D565。跳过 audio.play / D145 swallow。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / agentsVoice | closed |
| D574 | P3 | **closed** `aiCustomizationManagementEditor.ts` 四处裸 `void` fire-and-forget（D566 明确留下）：三处 `listWidget.setSection`（createContent 初刷 ~1188 `this.selectedSection`；`selectSection` ~2050 `section`；sections 列表点击 ~2475 `sectionId`）；一处 `showEmbeddedEditor`（migration 页 `openCustomizationInEmbeddedEditor` ~1496）。`setSection` 为 async；`showEmbeddedEditor` 为 async。`showEmbeddedEditor` 方法体已有 try/catch（catch-path `console.error` + `goBackToList`）；`setSection` 无方法体 catch（`await whenSectionLoaded`）。方法体内已吞则 catch-path 再抛会漏。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。**跳过** 已 await 的 `await this.listWidget.setSection`（`revealMigratedCustomizations`；`openPromptsItemFromPluginDetail`）与 `await this.showEmbeddedEditor`（createNewItem*）。**未改** D566 已双链的 refresh/migrate/reveal。未改 `handleEditorActionButton` / `saveExistingCustomization` / `saveBuiltinPromptCopy` 单层 `console.error`。未碰 mcpListWidget / pluginListWidget / grpcClient / `OPEN_CONNECTION` / D145。mocha 源扫锁于既有 `aiCustomizationManagementEditorCatchScan.test.ts`（本文件 only）：三处 setSection + 一处 showEmbeddedEditor 均为双链；D566 八处仍双链；裸/单链不在源；await 仍无 catch。未扫其它文件。残留：`onDidSelectItem` / `openPromptsItemFromPluginDetail` 两处 `this.showEmbeddedEditor(...)` 无 void/await/catch；`onDidRequestCreate` / `onDidRequestCreateManual` 丢 async Promise；`handleEditorActionButton` / `saveExistingCustomization` / `saveBuiltinPromptCopy` 仍单层 `console.error`。`revealMigratedCustomizations` 先 `selectSection`（现双链 void setSection）再 `await setSection`，并发两次 load 为既有行为。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D566。D24 仍开。本行只关本文件这些 call site，leftover 程序未全局完成。 | 工位 I `loop/I-d24-customization-section-leftover`；增量 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/aiCustomizationManagementEditorCatchScan.test.ts` **3/0**（D566 源扫 1 + D574 源扫 1 + Errors 1）。未 commit。 | 四处双链 catch；await 站点未加 catch；D566 八处仍双链。未关 D8/D16/D147/D24/D405/D487。不重开 D566。本文件这些 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | chat / customization-editor | closed |
| D575 | P3 | **closed** `agentPluginsView.ts` 一处丢 Promise 的 `this.editorService.openEditor(...)`（list `onDidOpen` ~362）。`openEditor` 返回 Thenable/`Promise`；裸调用在 reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `void this.editorService.openEditor(...).catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** D567 已双链的 `void this.show(this.currentQuery)`。未改 `show()` 方法体 leftover / leftover-looks-empty。未碰 grpcClient / sessionViewHost / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于既有 `agentPluginsViewShowCatchScan.test.ts`（本文件 only，cwd-first）：openEditor 一处且双链；裸/单链不在源；D567 show 双链仍在。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 仍开。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 J `loop/J-d24-agent-plugins-open-editor-leftover`；增量 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/agentPluginsViewShowCatchScan.test.ts` **3/0**（D567 源扫 1 + D575 源扫 1 + Errors 1）。未 commit。 | openEditor 双链；源扫裸/单链不在；D567 show 双链仍在。未关 D8/D16/D147/D24/D405/D487。不重开 D567。本 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / chat-agent-plugins | closed |
| D576 | P3 | **closed** `dictationOnboarding.ts` D560/D568 留下的三处 leftover fire-and-forget：`preview.listen(option.deviceId).then(updateHint)`（selectMicrophone ~806）；`switchMicrophone(option.deviceId).then(refresh)` 单链 `logService.error`（~808）；`DictationOnboardingService.refreshMicrophones` 的 `void this.currentBanner?.refreshMicrophones`（~932）。`listen` / banner `refreshMicrophones` 均为 async；服务门面 typed `void`，runtime 仍是 Promise。裸 `void` / 单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀：listen 保留 then 后双链；switchMicrophone 升为双链 `onUnexpectedError`（替换 log catch）；service 门面 `void Promise.resolve(...).catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** D560 已双链的两处 `void this.refreshMicrophones()`。**未改** D568 已双链的两处 `void this.startPreview()`。**跳过** D145 `context.close().catch(() => { /* already closing */ })`。未改 `executeCommand` 单链 log-catch（非本 call site）。未碰 `voiceModeOnboarding.ts` / grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。mocha 源扫锁于既有 `dictationOnboardingCatchScan.test.ts`（本文件 only）：listen / switchMicrophone / service refresh 均为双链；裸/单链/log-catch 不在源；D560 refresh 两处与 D568 startPreview 两处仍双链。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D560/D568。D24 仍开。本行只关本文件这三处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 A `loop/A-d24-dictation-listen-leftover`；定点 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/dictationOnboardingCatchScan.test.ts` **4/0**（D560 源扫 1 + D568 源扫 1 + D576 源扫 1 + Errors 1）。未 commit。 | 三处双链 catch；D560/D568 仍双链。未关 D8/D16/D147/D24/D405/D487。不重开 D560/D568。残留：同文件 `executeCommand` 仍单链 log-catch；D145 swallow 仍在。本文件这三处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / dictation-onboarding | closed |
| D577 | P3 | **closed** `chatPetAchievements.contribution.ts` 三处裸 `void` fire-and-forget：`void this.initializeCustomizationObservation()`（ctor autorun ~209）；`void this.initializeMcpObservation()`（ctor autorun ~210）；`void this.establishCustomizationBaseline(skills, instructions, source, true)`（source 切换 ~240）。三方法均为 async。`initializeMcpObservation` 方法体已有 try/catch（`queryLocal` 失败 `logService.error` 后 return）；`initializeCustomizationObservation` / `establishCustomizationBaseline` 无方法体 catch（`whenSectionLoaded` / `timeout(0)` 再抛仍漏）。方法体内已吞则 catch-path 再抛会漏。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀三处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`；补 `onUnexpectedError` import。**跳过** 已 await 的 `await this.establishCustomizationBaseline(...)`（while 基线循环）。未改方法体 leftover / leftover-looks-empty。未改 `queryLocal` 内层 swallow。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `chatPetAchievementsObserveCatchScan.test.ts`（本文件 only，cwd-first）：三处均为双链；裸/单链不在源；await 站点未加 catch。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这三处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 C `loop/C-d24-chatpet-observe-leftover`；定点 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/chatPetAchievementsObserveCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。gitlink `dev/loop` 脏勿 add。 | 三处双链 catch；裸/单链源扫不在；await 基线未加 catch。未关 D8/D16/D147/D24/D405/D487。本文件这三处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / chat-pet-achievements | closed |
| D578 | P3 | **closed** `sessionsSignInDialog.ts` 一处裸 `void this.show()`（ctor ~81）。`show` 为 async；方法体无 try/catch（`await this.dialog.show()` reject / `cancel()` 再抛仍漏）。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** footer `executeCommand(RETURN_TO_VSCODE_EDITOR_COMMAND_ID)` 单链 `.catch(onUnexpectedError)`（非本 call site）。未改方法体 leftover。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。mocha 源扫锁于 `sessionsSignInDialogCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 D `loop/D-d24-sessions-signin-leftover`；定点 transpile 后 `node test/unit/node/index.js --run src/vs/sessions/test/node/sessionsSignInDialogCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。 | 本 call site 双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。残留：footer executeCommand 仍单链。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / sign-in-dialog | closed |
| D579 | P3 | **closed** `promptFileContributions.ts` 一处裸 `void this.updateRegistration()`（`PromptValidatorContribution` ctor ~74）。`updateRegistration` 为 async `Promise<void>`；方法体无 try/catch（`await this.chatModeService.getLocalModes()` → `waitForPendingUpdates` reject 会漏）。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`；`onUnexpectedError` 自 `base/common/errors.js`。未改方法体 leftover。未改 `ModelTracker.validate` 丢 `delayer.trigger(async () => validate)` Promise。未碰 mcpListWidget / grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。mocha 源扫锁于新增 `promptFileContributionsCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 F `loop/F-d24-prompt-registration-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/promptFileContributionsCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。 | 本 call site 双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / chat-prompt-syntax | closed |
| D580 | P3 | **closed** `pluginListWidget.ts` D572 留下的两处 marketplace `install.onDidClick(() => this.installMarketplacePlugin(item, install))`：`appendMarketplacePluginRow`（~1488）；`appendMarketplacePluginCard`（~1509）。`installMarketplacePlugin` 为 async；方法体已有 try/catch（fail 画 notification）。方法体内已吞则 catch-path 再抛会漏。裸 click Promise 在 reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀两处改 `void this.installMarketplacePlugin(...).catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** 已双链的七处 `void this.refresh()`（D556）。**未改** 已双链的 `queryMarketplaceSnapshot` ×2 / `queryMarketplace` ×1（D564）。**未改** 已双链的两处 `void this.filterPlugins()`（D572）。未改列表 renderer Install `onDidClick(async () => …)`（~469）。未碰 mcpListWidget / grpcClient / `OPEN_CONNECTION` / D145。mocha 源扫锁于既有 `pluginListWidgetRefreshCatch.test.ts`：两处 install 均为双链；裸 `install.onDidClick(() => this.installMarketplacePlugin(item, install));` / 单链不在源；七处 refresh、三处 query、两处 filter 仍双链。cwd-first 读源（`out` 为 symlink）。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D556/D564/D572。D24 仍开。本行只关本文件这两处 call site，leftover 程序未全局完成。 | 工位 G `loop/G-d24-plugin-install-leftover`；定点 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/pluginListWidgetRefreshCatch.test.ts` **5/0**（源扫 refresh 1 + query 1 + filter 1 + install 1 + Errors 1）。未 commit。 | 两处 installMarketplacePlugin 双链；裸/单链源扫不在；D556 refresh 七处、D564 query 三处、D572 filter 两处仍双链。未关 D8/D16/D147/D24/D405/D487。不重开 D556/D564/D572。残留：列表 renderer Install ~469 仍 `onDidClick(async () => …)` 丢 Promise。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | chat / plugin-list | closed |
| D581 | P3 | **closed** `chatAttachmentWidgets.ts` 一处裸 `void this.loadImageBytes(resource, renderImageElements)`（hydrated image ~558）。`loadImageBytes` 为 async `Promise<void>`。方法体 `readFile` 已 try/catch（文件不存在保 icon fallback）；`render` / `createImageElements` 再抛仍漏。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。未改方法体 leftover / leftover-looks-empty。未改同文件 `void renderPreviewImage()` / `void this.fileService.readFile(...).then`。未碰 `voiceModeOnboarding.ts`（已 D573）/ grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于 `chatAttachmentWidgetsCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 H `loop/H-d24-attachment-image-leftover`；定点 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/chatAttachmentWidgetsCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。 | 本 call site 双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。同文件其它 void 仍裸。本 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / chat-attachments | closed |
| D582 | P3 | **closed** `aiCustomizationManagementEditor.ts` 三处单层 Promise catch（D574 明确留下）：`void this.handleEditorActionButton()` click ~2622（原 `console.error` + `notificationService.error`）；`void this.saveExistingCustomization(saveRequest)` 退出编辑 ~2834（原 `console.error` + `notificationService.warn`）；`void this.saveBuiltinPromptCopy(saveRequest).then(refresh, error => console.error…)` ~2999。三方法均为 async；`saveBuiltinPromptCopy` / `saveExistingCustomization` 无方法体 catch；`handleEditorActionButton` 无方法体 catch（内再 fire-and-forget save copy）。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀三处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`；saveBuiltinPromptCopy **保留**成功 then `void this.listWidget?.refresh()`，只换 rejection 路径。**未改** D566 refresh/migrate/reveal 双链。**未改** D574 setSection/showEmbeddedEditor 双链。**未改** `handleBuiltinSave` 方法体 try/catch（仍 `console.error` + warn；已 await）。**未改** `showEmbeddedEditor` 方法体 load-model `console.error`。未碰 mcpListWidget / pluginListWidget / grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。mocha 源扫锁于既有 `aiCustomizationManagementEditorCatchScan.test.ts`（本文件 only）：三处均为双链；save copy 成功 then 仍 refresh；单层 console+notification 不在这三处；D566 八处与 D574 四处仍双链；await saveBuiltinPromptCopy 仍无 catch。未扫其它文件。残留：`onDidSelectItem` / `openPromptsItemFromPluginDetail` 两处 `this.showEmbeddedEditor(...)` 无 void/await/catch；`onDidRequestCreate` / `onDidRequestCreateManual` 丢 async Promise；`goBackToList` 与 save-copy then 内 `void this.listWidget?.refresh()` 仍裸。fire-and-forget 三处不再画专用 localize 通知（改走 `onUnexpectedError`；`handleBuiltinSave` 仍有专用 warn）。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D566/D574。D24 **仍开**。本行只关本文件这三处 call site，leftover 程序未全局完成。 | 工位 I `loop/I-d24-customization-save-leftover`；定点 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/aiCustomizationManagementEditorCatchScan.test.ts` **4/0**（D566 源扫 1 + D574 源扫 1 + D582 源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 三处双链 catch；save copy 成功 then 仍 refresh；D566/D574 双链仍在。未关 D8/D16/D147/D24/D405/D487。不重开 D566/D574。本文件这三处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | chat / customization-editor | closed |
| D583 | P3 | **closed** `chatWidget.ts` 两处裸 `void this.cancelEditing()`：`onDidFocusOutside` ~2073；`inputPart.onDidClickOverlay` ~2237。`cancelEditing` 为 async `Promise<void>`。方法体 `dialogService.confirm` 仅 try/finally，无 catch（confirm reject 会漏；`finishedEditing` 再抛仍漏）。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀两处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。补 `onUnexpectedError` import。**跳过** 测内已 await 的 `await widget.cancelEditing()`。**未改** `chatExecuteActions.ts` `return widget.cancelEditing()`。**未改** 同文件 `void this.input.requestModelByIdentifier(...)`。未改方法体 leftover。未碰 `agentPluginsView.ts`（已 D575）。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。mocha 源扫锁于新增 `chatWidgetCancelEditingCatchScan.test.ts`（本文件 only，cwd-first）：两处均为双链；裸 `void this.cancelEditing();` / 单链不在源。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这两处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 J `loop/J-d24-chatwidget-cancel-edit-leftover`；增量 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/chatWidgetCancelEditingCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。 | 两处双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。未改 agentPluginsView。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / chat-widget | closed |
| D584 | P3 | **closed** `dictationOnboarding.ts` D576 留下的一处单层 `logService.error` Promise catch：settings/instructions `executeCommand(commandId as string, ...args)`（~671）。丢弃的 Promise；单层 catch 在 mocha `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `void … .catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** D560 已双链的两处 `void this.refreshMicrophones()`。**未改** D568 已双链的两处 `void this.startPreview()`。**未改** D576 已双链的 listen / switchMicrophone / service refresh。**跳过** D145 `context.close().catch(() => { /* already closing */ })`。未碰 `voiceModeOnboarding.ts` / grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。mocha 源扫锁于既有 `dictationOnboardingCatchScan.test.ts`（本文件 only）：executeCommand 为双链；单链 log-catch 不在源；D560 refresh 两处、D568 startPreview 两处、D576 listen/switch/service 仍双链；D145 swallow 仍在。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D560/D568/D576。D24 仍开。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 A `loop/A-d24-dictation-exec-leftover`；定点 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/dictationOnboardingCatchScan.test.ts` **5/0**（D560 源扫 1 + D568 源扫 1 + D576 源扫 1 + D584 源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；单链 log-catch 源扫不在；D560/D568/D576 仍双链。未关 D8/D16/D147/D24/D405/D487。不重开 D560/D568/D576。跳过 D145 swallow。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / dictation-onboarding | closed |
| D585 | P3 | **closed** `chatPlanReviewPart.ts` 七处裸 `void` fire-and-forget：三处 `void this.markUsed()`（onDidDismiss ~117；submitApproval ~774；submitRejection ~798）；两处 `void this.enterFeedbackMode({ focus: false })`（restore draft ~261；onInlineFeedbackChanged ~536）；两处 `void this.submitFeedback()`（Enter KEY_DOWN ~409；Submit click ~580）。三方法均为 async。`markUsed` 无 await、无方法体 catch（`classList` / `_planReviewRegistration.clear()` 再抛仍漏）。`enterFeedbackMode` 无方法体 catch（`openEditor` reject 会漏）。`submitFeedback` 仅 try/finally 无 catch（`savePlanFile` reject 会漏）。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀七处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`；补 `onUnexpectedError` import。**跳过** `await this.markUsed()` / `await this.enterFeedbackMode({ focus: true })`。**未改** `void this.enterReviewMode()`（~196）。**未改** `exitFeedbackMode` / `submitApproval` / `submitRejection` 丢 Promise。未改 `submitFeedback: () => this.submitFeedback()` 回调。未改方法体 leftover。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `chatPlanReviewPartCatchScan.test.ts`（本文件 only，cwd-first）：七处均为双链；裸/单链不在源；await 站点未加 catch。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这七处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 C `loop/C-d24-plan-review-leftover`；定点 transpile 后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/chatPlanReviewPartCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。gitlink `dev/loop` 脏勿 add。勿 add `out`。 | 七处双链 catch；裸/单链源扫不在；await 站点未加 catch。未关 D8/D16/D147/D24/D405/D487。残留：`void this.enterReviewMode()`；`exitFeedbackMode` / `submitApproval` / `submitRejection` 丢 Promise；注册回调 `submitFeedback()` 无 void/catch。本文件这七处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / chat-plan-review | closed |
| D586 | P3 | **closed** `sessionsSignInDialog.ts` D578 留下的一处 footer 单链 `void commandService.executeCommand<void>(RETURN_TO_VSCODE_EDITOR_COMMAND_ID).catch(onUnexpectedError)`（`createSessionsSignInDialogOptions` renderDialogFooter ~37）。`ICommandService.executeCommand` 返回 `Promise`。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** D578 已双链的 ctor `void this.show()`。未改方法体 leftover。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于既有 `sessionsSignInDialogCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源；D578 show 双链仍在。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 D `loop/D-d24-sessions-exec-leftover`；定点 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/test/node/sessionsSignInDialogCatchScan.test.ts` **3/0**（D578 源扫 1 + D586 源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在；D578 show 双链仍在。未关 D8/D16/D147/D24/D405/D487。不重开 D578。本 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / sign-in-dialog | closed |
| D587 | P3 | **closed** `promptFileContributions.ts` D579 留下的一处裸 `this.delayer.trigger(async () => { ... await this.validator.validate ...})`（`ModelTracker.validate` ~180）。`Delayer.trigger` 返回 `Promise<void>`；task 内 `await this.validator.validate` reject 会漏。裸调用在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `void this.delayer.trigger(...).catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** D579 已双链的 ctor `void this.updateRegistration()`。未改方法体 leftover。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于既有 `promptFileContributionsCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源；D579 updateRegistration 双链仍在。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。不重开 D579。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 F `loop/F-d24-prompt-delayer-leftover`；定点 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/promptFileContributionsCatchScan.test.ts` **3/0**（D579 源扫 1 + D587 源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在；D579 ctor 双链仍在。未关 D8/D16/D147/D24/D405/D487。不重开 D579。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / chat-prompt-syntax | closed |
| D588 | P3 | **closed** `pluginListWidget.ts` D580 留下的一处列表 renderer Install `onDidClick(async () => …)`（~469）。内 `await this.pluginInstallService.installPlugin(...)`；IIFE 方法体已有 try/catch（fail 画 notification）。方法体内已吞则 catch-path paint 再抛会漏。裸 click Promise 在 reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `onDidClick(() => { void (async () => { … })().catch(onUnexpectedError).catch(onUnexpectedError); })`。**未改** 已双链的七处 `void this.refresh()`（D556）。**未改** 已双链的 `queryMarketplaceSnapshot` ×2 / `queryMarketplace` ×1（D564）。**未改** 已双链的两处 `void this.filterPlugins()`（D572）。**未改** 已双链的两处 `installMarketplacePlugin`（D580）。未碰 mcpListWidget / grpcClient / `OPEN_CONNECTION` / D145。mocha 源扫锁于既有 `pluginListWidgetRefreshCatch.test.ts`：该 IIFE 一处且双链；裸 `installButton.onDidClick(async () => {` / `onDidClick(async () => {` 不在源；七处 refresh、三处 query、两处 filter、两处 marketplace install 仍双链。cwd-first 读源（`out` 为 symlink）。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D556/D564/D572/D580。D24 仍开。本行只关本文件这一处 call site，leftover 程序未全局完成。 | 工位 G `loop/G-d24-plugin-list-install-leftover`；定点 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/pluginListWidgetRefreshCatch.test.ts` **6/0**（源扫 refresh 1 + query 1 + filter 1 + marketplace install 1 + list-renderer Install 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site IIFE 双链 catch；裸 `onDidClick(async` 源扫不在；D556 refresh 七处、D564 query 三处、D572 filter 两处、D580 install 两处仍双链。未关 D8/D16/D147/D24/D405/D487。不重开 D556/D564/D572/D580。残留：同文件 `refresh()` 内无 await 的 `this.filterPlugins()`；`delayedFilter.trigger` / `delayedMarketplaceSearch.trigger`；`galleryItemRenderer.ts` / `embeddedAgentPluginDetail.ts` 仍 `onDidClick(async`。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | chat / plugin-list | closed |
| D589 | P3 | **closed** `chatAttachmentWidgets.ts` D581 留下的两处 leftover fire-and-forget：`void renderPreviewImage()`（hover 预览 ~686，局部 async）；`void this.fileService.readFile(attachment.imageData).then(...)`（element screenshot ~1598）。`renderPreviewImage` 为 async（`getOrCreateImageThumbnail` / `createObjectURL` 再抛仍漏）。`readFile` 返回 Thenable/`Promise`；then 成功路径 `appendPreview`、失败路径 warn + 卸 section 再抛仍漏。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀两处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`；readFile **保留** then（成功 appendPreview + 失败 warn/卸 DOM）。**未改** D581 `loadImageBytes` 双链。未改方法体 leftover / leftover-looks-empty。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于既有 `chatAttachmentWidgetsCatchScan.test.ts`（本文件 only，cwd-first）：两处均为双链；裸 `void renderPreviewImage();` / 单链不在源；readFile then 仍在；D581 loadImageBytes 仍双链。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。不重开 D581。D24 **仍开**。本行只关这两处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 H `loop/H-d24-attachment-preview-leftover`；定点 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/chatAttachmentWidgetsCatchScan.test.ts` **待 mocha**。未 commit。 | 两处双链 catch；裸/单链源扫不在；D581 loadImageBytes 仍双链。未关 D8/D16/D147/D24/D405/D487。不重开 D581。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / chat-attachments | closed |
| D590 | P3 | **closed** `aiCustomizationManagementEditor.ts` 两处裸 `this.showEmbeddedEditor(...)`（D582 明确留下）：`onDidSelectItem` ~1045；`openPromptsItemFromPluginDetail` ~3515。`showEmbeddedEditor` 为 async。方法体已有 try/catch（catch-path `console.error` + `goBackToList`）；方法体内已吞则 catch-path 再抛会漏。裸 Promise 在 reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀两处改 `void … .catch(onUnexpectedError).catch(onUnexpectedError)`。**跳过** 已 await 的 `await this.showEmbeddedEditor`（createNewItem*）与 D574 已 void 的 migration `openCustomizationInEmbeddedEditor` ~1496。**未改** D566 refresh/migrate/reveal 双链。**未改** D574 setSection 三处 / migration showEmbeddedEditor 双链。**未改** D582 handleEditorActionButton / saveExistingCustomization / saveBuiltinPromptCopy 双链。未改方法体 load-model `console.error`。未碰 mcpListWidget / pluginListWidget / grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。mocha 源扫锁于既有 `aiCustomizationManagementEditorCatchScan.test.ts`（本文件 only）：两处均为双链；D574 migration void 仍一处；await 仍无 catch。未扫其它文件。残留：`onDidRequestCreate` / `onDidRequestCreateManual` 丢 async Promise；`goBackToList` 与 save-copy then 内 `void this.listWidget?.refresh()` 仍裸。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D566/D574/D582。D24 **仍开**。本行只关本文件这两处 call site，leftover 程序未全局完成。 | 工位 I `loop/I-d24-customization-open-editor-leftover`；定点 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/aiCustomizationManagementEditorCatchScan.test.ts` **5/0**（D566 源扫 1 + D574 源扫 1 + D582 源扫 1 + D590 源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 两处双链 catch；await 与 D574 void 未改；D566/D574/D582 双链仍在。未关 D8/D16/D147/D24/D405/D487。不重开 D566/D574/D582。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | chat / customization-editor | closed |
| D591 | P3 | **closed** `chatWidget.ts` 一处裸 `void this.input.requestModelByIdentifier(currentElement.modelId)`（startEditing ~2184）。`requestModelByIdentifier` 返回 `Promise<boolean>`（`_requestProgrammaticLanguageModel` → `requestProgrammaticSelection`）。Promise executor 调 `_reconcilePendingProgrammaticSelection`（`resolveModel` / `applyProgrammaticSelection` 再抛会 reject）。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** D583 两处 `cancelEditing` 双链。**跳过** 已 await / return 的 `requestModelByQualifiedName`（`executeHandoff` / `applyPromptMetadata`）。未改 `chatInputPart.ts` `void result.finally(...)`。未改方法体 leftover。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于既有 `chatWidgetCancelEditingCatchScan.test.ts` 新增 D591 suite（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源；D583 两处 cancelEditing 仍双链。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 J `loop/J-d24-chatwidget-model-leftover`；定点 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/chatWidgetCancelEditingCatchScan.test.ts` **3/0**（D583 源扫 1 + D591 源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在；D583 两处仍双链。未关 D8/D16/D147/D24/D405/D487。不重开 D583。残留：`chatInputPart` `void result.finally`；`executeHandoff` `acceptInput` 单链 log-catch。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / chat-widget | closed |
| D592 | P3 | **closed** `galleryItemRenderer.ts` 一处列表 renderer Install `onDidClick(async () => …)`（~87）。内 `await this._provider.install(element)`；IIFE 方法体已有 try/finally（刷新 Install 钮）。方法体内已吞则 finally-path 再抛会漏。裸 click Promise 在 reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `onDidClick(() => { void (async () => { … })().catch(onUnexpectedError).catch(onUnexpectedError); })`；补 `onUnexpectedError` import。未碰 pluginListWidget / mcpListWidget / grpcClient / `OPEN_CONNECTION` / D145。mocha 源扫锁于新增 `galleryItemRendererCatchScan.test.ts`（本文件 only，cwd-first）：该 IIFE 一处且双链；裸 `onDidClick(async` 不在源。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。D24 仍开。本行只关本文件这一处 call site，leftover 程序未全局完成。 | 工位 A `loop/A-d24-gallery-install-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/galleryItemRendererCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site IIFE 双链 catch；裸 `onDidClick(async` 源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | chat / gallery-item | closed |
| D593 | P3 | **closed** `embeddedAgentPluginDetail.ts` 四处 leftover `onDidClick(async () => …)` fire-and-forget：Install ~350；Uninstall ~419；Copy ~576；Open ~593。Install/Uninstall 方法体已有 try/catch（fail 画 notification）；方法体内已吞则 catch-path paint 再抛会漏。Copy `writeText` 无外层 catch；Open 内层 catch 只 fallback `openerService.open`。裸 click Promise 在 reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀四处改 `onDidClick(() => { void (async () => { … })().catch(onUnexpectedError).catch(onUnexpectedError); })`（同 D588 IIFE）。保留既有 try/catch/notification。补 `onUnexpectedError` import。**未改** `button.onDidClick(() => action.run())`（~442）。**未改** `splitButton.onDidClick(() => setEnablement(...))`（~485）。未碰 pluginListWidget / grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `embeddedAgentPluginDetailCatchScan.test.ts`（本文件 only，cwd-first）：四处 IIFE 均为双链；裸 `onDidClick(async () => {` / 单链不在源；Install/Uninstall notification 仍在。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这四处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 C `loop/C-d24-plugin-detail-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/embeddedAgentPluginDetailCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。gitlink `dev/loop` 脏勿 add。勿 add `out`。 | 四处 IIFE 双链 catch；裸 `onDidClick(async` 源扫不在。未关 D8/D16/D147/D24/D405/D487。不重开 D588。残留：`action.run()` 仍无 catch。本文件这四处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | chat / plugin-detail | closed |
| D594 | P3 | **closed** `newChatInput.ts` 两处裸 `void this.commandService.executeCommand`（OTEL status hover `_renderStatusHover`）：`vscode.open` + `URI.parse(OTEL_DOCS_URL)`（~299 Learn More）；`OPEN_OTEL_SETTINGS_COMMAND`（~306 Manage）。`ICommandService.executeCommand` 返回 `Promise`。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀两处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`；补 `onUnexpectedError` import。**未改** `this.hoverService.hideHover(true)`。**未改** `void this.toggleDictation()`。未改方法体 leftover。**跳过** `OPEN_CONNECTION` / D145 swallows。未碰 grpcClient / catalog `onOpenConnection`。未发明 proto。mocha 源扫锁于新增 `newChatInputCatchScan.test.ts`（本文件 only，cwd-first）：两处均为双链；裸/单链不在源；hideHover 两处仍在；toggleDictation 仍裸。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关这两处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 D `loop/D-d24-newchat-exec-leftover`；定点 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/test/node/newChatInputCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 两处双链 catch；裸/单链源扫不在；hideHover / toggleDictation 未改。未关 D8/D16/D147/D24/D405/D487。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / new-chat-input | closed |
| D595 | P3 | **closed** `aiCustomizationListWidget.ts` 一处裸 `this.delayedFilter.trigger(() => { … filterItems / announceItemCount / telemetry …})`（search `onDidChange` ~689）。`Delayer.trigger` 返回 `Promise<void>`；task 内 `filterItems` / announce / telemetry 抛出会漏。裸调用在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `void this.delayedFilter.trigger(...).catch(onUnexpectedError).catch(onUnexpectedError)`。补 `onUnexpectedError` import。**未改** `filterItems()` 方法体及其它同步调用。未碰 grpcClient / pluginListWidget / mcpListWidget / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `aiCustomizationListWidgetCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 F `loop/F-d24-list-delayer-leftover`；定点 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/aiCustomizationListWidgetCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。未改 pluginListWidget / mcpListWidget。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / chat-customization-list | closed |
| D596 | P3 | **closed** `pluginListWidget.ts` D588 留下的三处裸 `Delayer.trigger`：`searchInput.onDidChange` 的 `this.delayedMarketplaceSearch.trigger(() => this.queryMarketplace())`（~804）；`this.delayedMarketplaceSearch.trigger(() => this.queryPluginSearch())`（~806）；`this.delayedFilter.trigger(() => this.filterPlugins())`（~811）。`Delayer.trigger` 返回 `Promise<T>`；task 内 `queryMarketplace` / `queryPluginSearch` / `filterPlugins` 均为 async。裸 trigger 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀三处改 `void ….trigger(...).catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** 已双链的七处 `void this.refresh()`（D556）。**未改** 已双链的 `queryMarketplaceSnapshot` ×2 / `queryMarketplace` ×1（D564）。**未改** 已双链的两处 `void this.filterPlugins()`（D572）。**未改** 已双链的两处 `installMarketplacePlugin`（D580）。**未改** 已双链的列表 renderer Install IIFE（D588）。未改 `delayedMarketplaceSearch.cancel()`。未碰 mcpListWidget / grpcClient / `OPEN_CONNECTION` / D145。mocha 源扫锁于既有 `pluginListWidgetRefreshCatch.test.ts`：三处 trigger 均为双链；裸/单链 trigger 不在源；七处 refresh、三处 query、两处 filter、两处 marketplace install、一处 list-renderer Install 仍双链。cwd-first 读源（`out` 为 symlink）。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D556/D564/D572/D580/D588。D24 仍开。本行只关本文件这三处 trigger call site，leftover 程序未全局完成。 | 工位 G `loop/G-d24-plugin-delayer-leftover`；定点 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/pluginListWidgetRefreshCatch.test.ts` **7/0**（源扫 refresh 1 + query 1 + filter 1 + marketplace install 1 + list-renderer Install 1 + Delayer trigger 1 + Errors 1）。未 commit。勿 add `out`。 | 三处 Delayer.trigger 双链；裸/单链源扫不在；D556/D564/D572/D580/D588 仍双链。未关 D8/D16/D147/D24/D405/D487。不重开 D556/D564/D572/D580/D588。本文件这三处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | chat / plugin-list | closed |
| D597 | P3 | **closed** `sessionChangesEditor.ts` D476 留下的一处单链 `void this.commandService.executeCommand(CHANGESET_REVIEW_ACTION_ID, resource).catch(onUnexpectedError)`（`SessionChangesUIElementFactory.handleHeaderMiddleClick` ~98）。`ICommandService.executeCommand` 返回 `Promise`。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** 方法体 / `return true`。`onUnexpectedError` 已 import。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `sessionChangesEditorCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源；`handleHeaderMiddleClick` 仍 `return true`。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。不重开 D476。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 H `loop/H-d24-changeset-exec-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/changes/test/node/sessionChangesEditorCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在；方法体 / `return true` 未改。未关 D8/D16/D147/D24/D405/D487。不重开 D476。本 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / changes | closed |
| D598 | P3 | **closed** `aiCustomizationManagementEditor.ts` D590 留下的两处丢 async Promise：`onDidRequestCreate` ~1049 `this.createNewItemWithAI(promptType)`；`onDidRequestCreateManual` ~1054 `this.createNewItemManual(...)`。两方法均为 async（`generateCustomization` / `showEmbeddedEditor` / `executeCommand` reject 会漏）。裸 Promise 在 reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀两处改 `void … .catch(onUnexpectedError).catch(onUnexpectedError)`。**未套** `goBackToList` ~2817 与 save-copy then ~2994 的 `void this.listWidget?.refresh()`：`AICustomizationListWidget.refresh(): void` 同步，套 `.catch` 会 `undefined.catch` 运行时抛。不发明 `Promise.resolve`，不改 listWidget 签名。**未改** D566 refresh/migrate/reveal 双链。**未改** D574 setSection 三处 / migration showEmbeddedEditor 双链。**未改** D582 handleEditorActionButton / saveExistingCustomization / saveBuiltinPromptCopy 双链。**未改** D590 两处 showEmbeddedEditor 双链。未碰 mcpListWidget / pluginListWidget / grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。mocha 源扫锁于既有 `aiCustomizationManagementEditorCatchScan.test.ts`（本文件 only）：两处 create 均为双链；裸/单链不在源；两处 `void this.listWidget?.refresh();` 仍裸（无 `.catch`）；D566/D574/D582/D590 双链仍在。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D566/D574/D582/D590。D24 **仍开**。本行只关本文件这两处 create call site，leftover 程序未全局完成。 | 工位 I `loop/I-d24-customization-create-leftover`；定点 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/aiCustomizationManagementEditorCatchScan.test.ts` **6/0**（D566 源扫 1 + D574 源扫 1 + D582 源扫 1 + D590 源扫 1 + D598 源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 两处 create 双链 catch；refresh 两处仍裸（sync void）。D566/D574/D582/D590 双链仍在。未关 D8/D16/D147/D24/D405/D487。不重开 D566/D574/D582/D590。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | chat / customization-editor | closed |
| D599 | P3 | **closed** `chatInputPart.ts` 一处裸 `void result.finally(() => this._updateInputContentContextKeys())`（`_requestProgrammaticLanguageModel` ~1887）。`requestProgrammaticSelection` 返回 `Promise<boolean>`；`finally` 再返回 Promise（`result` reject 或 `_updateInputContentContextKeys` 再抛均会漏）。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** `return result`。**未改** `chatWidget.ts`（已 D583/D591）。未改方法体 leftover。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `chatInputPartCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源；`return result` 仍紧随其后。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 J `loop/J-d24-input-finally-leftover`；定点 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/chatInputPartCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在；`return result` 未改。未关 D8/D16/D147/D24/D405/D487。不重开 D583/D591。未碰 chatWidget。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / chat-input | closed |
| D600 | P3 | **closed** `mcpListWidget.ts` 一处裸 `this.delayedFilter.trigger(() => this.filterServers())`（search `onDidChange` ~1221）。`Delayer.trigger` 返回 `Promise<void>`；task 内 `filterServers` 为 sync void，抛出仍会 reject。裸 trigger 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `void this.delayedFilter.trigger(...).catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** D555 已双链的 refresh / query / delayedGallerySearch。**未改** D563 已双链的 action / installMarketplaceServer。**未改** D571 已双链的三处 executeCommand。未改其它 `this.filterServers()` 同步调用。未碰 grpcClient / pluginListWidget / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145。未发明 proto。mocha 源扫锁于既有 `mcpListWidgetCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源；D555/D563/D571 仍双链。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。不重开 D555/D563/D571。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 A `loop/A-d24-mcp-delayer-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/mcpListWidgetCatchScan.test.ts` **5/0**（源扫 refresh/query 1 + action/install 1 + executeCommand 1 + D600 delayedFilter 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在；D555/D563/D571 仍双链。未关 D8/D16/D147/D24/D405/D487。不重开 D555/D563/D571。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / mcp-list | closed |
| D601 | P3 | **closed** `modePicker.ts` D187 留下的一处单链 `void this.commandService.executeCommand(AICustomizationManagementCommands.OpenEditor, AICustomizationManagementSection.Agents).catch(onUnexpectedError)`（configure `onSelect` else ~255）。`ICommandService.executeCommand` 返回 `Promise`。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** mode 分支 `_selectMode`。`onUnexpectedError` 已 import。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `modePickerCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源；mode 分支仍无 executeCommand catch。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。不重开 D187。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 C `loop/C-d24-modepicker-exec-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/providers/copilotChatSessions/test/node/modePickerCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。gitlink `dev/loop` 脏勿 add。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在；mode 分支未改。未关 D8/D16/D147/D24/D405/D487。不重开 D187。本 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / mode-picker | closed |
| D602 | P3 | **closed** `newChatInput.ts` D594 留下的 leftover `toggleDictation` fire-and-forget：pill `void this.toggleDictation()`（VoiceInputModeActionViewItem ~1164）；mic `const toggle = () => this.toggleDictation()`（~1305，丢 Promise，CLICK/Tap/Enter/Space 再 `void toggle()`）。`toggleDictation` 为 async（`runDictationShortcut` reject 会漏）。裸 `void` / 丢 Promise 在 reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀两处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** D594 两处 OTEL `executeCommand` 双链。**未改** `this.hoverService.hideHover(true)`。**未改** command handler `activeDictationComposer?.toggleDictation()`。未改方法体 leftover。**跳过** `OPEN_CONNECTION` / D145 swallows。未碰 grpcClient / catalog `onOpenConnection`。未发明 proto。mocha 源扫锁于既有 `newChatInputCatchScan.test.ts`（本文件 only，cwd-first）：pill void 与 toggle helper 均为双链；裸/单链不在源；D594 OTEL 两处仍双链；hideHover 两处仍在；command handler 仍裸。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。不重开 D594。D24 **仍开**。本行只关这两处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 D `loop/D-d24-newchat-dictation-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/test/node/newChatInputCatchScan.test.ts` **3/0**（D594 源扫 1 + D602 源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 两处双链 catch；裸/单链源扫不在；D594 OTEL 双链仍在；command handler 仍裸。未关 D8/D16/D147/D24/D405/D487。不重开 D594。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / new-chat-input | closed |
| D603 | P3 | **closed** `automationsView.ts` 六处 leftover 单链 `.catch(onUnexpectedError)`：`runNow` ~328；`confirmDelete` ~337；`openEditDialog` ~350；`openCreateDialog` ~465；`markAllRunsRead` ~686；`openRunSession` ~752。D474 已补单层 catch；方法体已有 try/catch。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀六处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。未改方法体 leftover。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `automationsViewCatchScan.test.ts`（本文件 only，cwd-first）：六处均为双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。不重开 D474/D180/D182。D24 **仍开**。本行只关本文件这六处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 F `loop/F-d24-automations-exec-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/sessions/test/node/automationsViewCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 六处双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。不重开 D474/D180/D182。本文件这六处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / automations | closed |
| D604 | P3 | **closed** `desktopSessionLayoutController.ts` 七处 leftover 单链 `.catch(onUnexpectedError)`：`openView(CHANGES_VIEW_ID)` ~105 / ~364；`_openDefaultAuxiliaryBarContainer()` ~560 / ~579；`openViewContainer(savedContainerId)` ~575 / ~621；`_openDefaultAuxiliaryBarContainer(defaultContainerId)` ~628。`IViewsService.openView` / `openViewContainer` 与 `_openDefaultAuxiliaryBarContainer` 均返回 Promise。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀七处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** `_openDefaultAuxiliaryBarContainer` 方法体（仍 `return openView` / `return openViewContainer`，无 catch）。`onUnexpectedError` 已 import。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `desktopSessionLayoutControllerCatchScan.test.ts`（本文件 only，cwd-first）：七处均为双链；裸/单链不在源；方法体 return 仍无 catch。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这七处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 G `loop/G-d24-layout-view-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/layout/test/node/desktopSessionLayoutControllerCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 七处双链 catch；裸/单链源扫不在；方法体 return 未改。未关 D8/D16/D147/D24/D405/D487。本文件这七处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / layout | closed |
| D605 | P3 | **closed** `sessionCustomizations.ts` 一处裸 `void this._commandService.executeCommand(AICustomizationManagementCommands.OpenEditor, { section, revealUri })`（`SessionCustomizations._reveal` ~142）。`ICommandService.executeCommand` 返回 `Promise`。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。补 `onUnexpectedError` import。未改方法体 leftover / `buildSessionCustomizationSections`。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `sessionCustomizationsCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 H `loop/H-d24-session-custom-exec-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/chat/test/node/sessionCustomizationsCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / chat-customizations | closed |
| D606 | P3 | **closed** `account.contribution.ts` 一处裸 `void this.commandService.executeCommand(CHAT_PET_OPEN_ACHIEVEMENTS_COMMAND_ID)`（SessionsChatPetAchievementBadges 回调 ~726）。`ICommandService.executeCommand` 返回 `Promise`。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`；补 `onUnexpectedError` import。**未改** `this.hoverService.hideHover(true)` / `this.clickPanelDisposable.clear()`。**未改** Action 回调四处 `() => this.commandService.executeCommand(...)`（Promise 交 `Action.run`，非裸 `void`）。未改方法体 leftover。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `accountContributionCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源；hideHover / clear 仍紧随 pet 回调；MANAGE_CHAT Action 两处仍非 `void`。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 I `loop/I-d24-account-pet-exec-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/accountMenu/test/node/accountContributionCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在；hideHover / clear 未改。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / account-menu | closed |
| D607 | P3 | **closed** `sessionsMouseNavigation.ts` 两处单链 `void this.sessionsService.openPreviousSession().catch(onUnexpectedError)` / `openNextSession()`（`handleMouseNavigation` ~54/~56）。`ISessionsService.openPreviousSession`/`openNextSession` 返回 `Promise<void>`（`await _navigation.goBack`/`goForward`，方法体无 catch）。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀两处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** 方法体 / 既有 browser 测。`onUnexpectedError` 已 import。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `sessionsMouseNavigationCatchScan.test.ts`（本文件 only，cwd-first）：两处均为双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这两处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 J `loop/J-d24-sessions-mouse-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/sessions/test/node/sessionsMouseNavigationCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 两处双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / mouse-nav | closed |
| D608 | P3 | **closed** `chatCompositeBar.ts` 一处单链 `void this._commandService.executeCommand(CLOSE_CHAT_COMMAND_ID, { session, chat }).catch(onUnexpectedError)`（AUXCLICK 关 tab ~420）。`ICommandService.executeCommand` 返回 `Promise`。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** `renameChat` 单链（~579）。`onUnexpectedError` 已 import。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `chatCompositeBarCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源；renameChat 仍单链。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 A `loop/A-d24-chatbar-close-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/test/node/chatCompositeBarCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在；renameChat 仍单链。未关 D8/D16/D147/D24/D405/D487。本 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / chat-bar | closed |
| D609 | P3 | **closed** `agentHostClaudePermissionModePicker.ts` D492 留下的一处单链 `void this._openerService.open(URI.parse(CLAUDE_PERMISSION_MODE_LEARN_MORE_URL)).catch(onUnexpectedError)`（`_handleFooterActionItem` ~98）。`IOpenerService.open` 返回 `Promise`。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** Codex picker（D492 已双链）。`onUnexpectedError` 已 import。未改既有 browser 测。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `agentHostClaudePermissionModePickerCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。不重开 D492。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 C `loop/C-d24-claude-picker-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/providers/agentHost/test/node/agentHostClaudePermissionModePickerCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。gitlink `dev/loop` 脏勿 add。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在；Codex picker 未改。未关 D8/D16/D147/D24/D405/D487。不重开 D492。本 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / claude-picker | closed |
| D610 | P3 | **closed** `newChatInput.ts` D602 留下的 leftover command handler `handler: () => activeDictationComposer?.toggleDictation()`（TOGGLE_DICTATION_COMMAND_ID ~192）。`toggleDictation` 为 async（`runDictationShortcut` reject 会漏）。裸 handler 丢 Promise 在 reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `void activeDictationComposer?.toggleDictation()?.catch(onUnexpectedError).catch(onUnexpectedError)`；composer undefined 时 optional chain 空操作。`onUnexpectedError` 已 import。**未改** D594 两处 OTEL `executeCommand` 双链。**未改** D602 pill void / mic toggle helper 双链。**未改** `this.hoverService.hideHover(true)`。未改方法体 leftover。**跳过** `OPEN_CONNECTION` / D145 swallows。未碰 grpcClient / catalog `onOpenConnection`。未发明 proto。mocha 源扫锁于既有 `newChatInputCatchScan.test.ts`（本文件 only，cwd-first）：handler 一处且双链；裸/单链不在源；D594 OTEL 两处仍双链；D602 pill/toggle 仍双链。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。不重开 D594/D602。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 D `loop/D-d24-newchat-handler-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/test/node/newChatInputCatchScan.test.ts` **4/0**（D594 源扫 1 + D602 源扫 1 + D610 源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在；D594/D602 双链仍在。未关 D8/D16/D147/D24/D405/D487。不重开 D594/D602。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / new-chat-input | closed |
| D611 | P3 | **closed** `sessionWorkspacePicker.ts` 两处 leftover 单链 `.catch(onUnexpectedError)`：`void Promise.resolve(item.run())`（`_dispatchPickerItem` ~741）；`void this.commandService.executeCommand(item.commandId)`（~744）。D157/D158 已补单层 catch。`item.run()` 经 `Promise.resolve`；`ICommandService.executeCommand` 返回 `Promise`。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀两处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** `_dispatchPickerItem(directBrowseItem)` `.catch.finally`（~556）；**未改** delegate `onSelect` `_dispatchPickerItem(item)` 单链（~643）；**未改** restore `.catch`（~1669）；**未改** 既有 browser 测。`onUnexpectedError` 已 import。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `sessionWorkspacePickerCatchScan.test.ts`（本文件 only，cwd-first）：两处均为双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。不重开 D157/D158/D161。D24 **仍开**。本行只关本文件这两处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 F `loop/F-d24-workspace-picker-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/chat/test/node/sessionWorkspacePickerCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 两处双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。不重开 D157/D158/D161。残留：~556 finally / ~643 onSelect / ~1669 restore 仍单链。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / workspace-picker | closed |
| D612 | P3 | **closed** `pluginListWidget.ts` D572/D596 留下的 `refresh()` 内裸 `this.filterPlugins()`（~1029 else 支）。`refresh()` 已 async；`filterPlugins` 为 async。裸调用丢 Promise：reject 漏 unhandled，且 `refresh()` 会在 filter 完成前 resolve。其它支已 `await this.queryMarketplace()` / `await this.queryPluginSearch()`。七处 `void this.refresh()` 已双链（D556），故本刀 `await this.filterPlugins()`（不套 void 双链）。**未改** 已双链的七处 `void this.refresh()`（D556）。**未改** 已双链的 `queryMarketplaceSnapshot` ×2 / `queryMarketplace` ×1（D564）。**未改** 已双链的两处 `void this.filterPlugins()`（D572）。**未改** 已双链的两处 `installMarketplacePlugin`（D580）。**未改** 已双链的列表 renderer Install IIFE（D588）。**未改** 已双链的三处 `Delayer.trigger`（D596）。未碰 mcpListWidget / grpcClient / `OPEN_CONNECTION` / D145。mocha 源扫锁于既有 `pluginListWidgetRefreshCatch.test.ts`：`refresh()` else 为 `await this.filterPlugins()`；裸 `this.filterPlugins();` 不在源；`await this.filterPlugins()` 共 3；两处 void filter 仍双链。cwd-first 读源（`out` 为 symlink）。未扫其它文件。未碰 pills。未发明 proto。未 compile-client。不得宣称 leftover/pills 完成。不重开 D556/D564/D572/D580/D588/D596。D24 仍开。本行只关本 call site leftover，leftover 程序未全局完成。 | 工位 G `loop/G-d24-plugin-refresh-filter-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/pluginListWidgetRefreshCatch.test.ts` **8/0**（源扫 refresh 1 + query 1 + filter 1 + marketplace install 1 + list-renderer Install 1 + Delayer trigger 1 + D612 await 1 + Errors 1）。未 commit。勿 add `out`。 | `refresh()` else `await this.filterPlugins()`；裸 `this.filterPlugins();` 源扫不在；D556/D564/D572/D580/D588/D596 仍双链。未关 D8/D16/D147/D24/D405/D487。不重开 D556/D564/D572/D580/D588/D596。本 call site leftover 已收；leftover 程序未全局完成。 | chat / plugin-list | closed |
| D613 | P3 | **closed** `codeReviewService.ts` 一处裸 `void reviewThreadsRef.object.refresh()`（activeReviewContext autorun ~148）。`GitHubPullRequestReviewThreadsModel.refresh` 返回 `Promise<void>`。裸 `void` 在 Promise reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。补 `onUnexpectedError` import。**未改** `startPolling()`（返回 `IDisposable` 同步，不得套 `.catch`）。未改第二处 `createPullRequestReviewThreadsModelReference` / `resolveThread`。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `codeReviewServiceCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源；`startPolling` 仍无 `.catch`。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 H `loop/H-d24-review-refresh-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/codeReview/test/node/codeReviewServiceCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在；`startPolling` 未改。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / code-review | closed |
| D614 | P3 | **closed** `aiCustomizationOverviewView.ts` leftover `loadCounts()`（async ~169）五处 fire-and-forget：`onDidChangeCustomAgents` ~100；`onDidChangeSlashCommands` ~101；`onDidChangeWorkspaceFolders` ~104；autorun ~107；`renderBody` `void this.loadCounts()` ~120。丢 Promise / 裸 `void` 在 reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀五处改 `void this.loadCounts().catch(onUnexpectedError).catch(onUnexpectedError)`。补 `onUnexpectedError` import。**未改** 方法体 leftover。**未改** `this.openOverview()` 丢 Promise。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `aiCustomizationOverviewViewCatchScan.test.ts`（本文件 only，cwd-first）：五处均为双链；裸/单链不在源；`openOverview` 仍裸。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这五处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 I `loop/I-d24-overview-counts-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/aiCustomizationTreeView/test/node/aiCustomizationOverviewViewCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 五处双链 catch；裸/单链源扫不在；openOverview 未改。未关 D8/D16/D147/D24/D405/D487。本文件这五处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / customization-overview | closed |
| D615 | P3 | **closed** `copilotChatSessionsProvider.ts` 一处 leftover `void this.gitService.openRepository(uri).then(..., error => warn)`（`_resolveLocalGitHubInfo` ~2720）。`IGitService.openRepository` 返回 `Promise`。`.then(success, error)` 的 error 回调只吞 reject；success-path throw 仍漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀保留 then success/error 回调，其后加 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** `await this.gitService.openRepository(repoUri)`。**未改** `_pullRequestNumberForBranch` 单链 `lookup.then(...).catch(onUnexpectedError)`。未改方法体 leftover。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `copilotChatSessionsProviderCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且 then 后双链；裸 then / 单链不在源；await openRepository 与 PR lookup 单链仍在。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 J `loop/J-d24-github-repo-then-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/providers/copilotChatSessions/test/node/copilotChatSessionsProviderCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site then 后双链 catch；裸/单链源扫不在；then 回调与 await/PR lookup 未改。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / github-repo-then | closed |
| D616 | P3 | **closed** `chatCompositeBar.ts` D608 留下的一处单链 `renameChat(...).catch(onUnexpectedError)`（inline tab rename `_startTabEditing` ~578）。`ISessionsManagementService.renameChat` 返回 `Promise`。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** CLOSE_CHAT 双链（D608）。`onUnexpectedError` 已 import。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于既有 `chatCompositeBarCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源；CLOSE_CHAT 仍双链。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。不重开 D608。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 A `loop/A-d24-chatbar-rename-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/test/node/chatCompositeBarCatchScan.test.ts` **3/0**（D608 源扫 1 + D616 源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在；CLOSE_CHAT 仍双链。未关 D8/D16/D147/D24/D405/D487。不重开 D608。本 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / chat-bar | closed |
| D617 | P3 | **closed** `localAgentHostSessionsProvider.ts` 一处 leftover 单链 `void this._resolveDevContainerAvailability(session.sessionId, workspaceUri).catch(onUnexpectedError)`（`createNewSession` ~257）。方法体已有 try/catch（warn 吞 `isAvailable` / fire）。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** 方法体 leftover。`onUnexpectedError` 已 import。未改既有 browser 测。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `localAgentHostSessionsProviderCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源；方法体 catch 仍在。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 C `loop/C-d24-devcontainer-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/providers/agentHost/test/node/localAgentHostSessionsProviderCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。gitlink `dev/loop` 脏勿 add。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在；方法体 catch 未改。未关 D8/D16/D147/D24/D405/D487。本 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / local-agent-host | closed |
| D618 | P3 | **closed** `baseAgentHostSessionsProvider.ts` 三处 leftover 单链 `.catch(onUnexpectedError)`：`void this._dispatchActiveClientWhenResolved(...)`（~3065）；`void this._resolveRunningSessionConfig(...)`（`setSessionConfigValue` ~3589 / `replaceSessionConfig` ~3645）。D176/D478 已补单层 catch。两方法均 async；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀三处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** 方法体既有 try/catch（resolve warn 不重抛）。未改既有 browser 测。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `baseAgentHostSessionsProviderCatchScan.test.ts`（本文件 only，cwd-first）：dispatch 一处且双链；resolve 两处均为双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。不重开 D176/D478。D24 **仍开**。本行只关本文件这三处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 D `loop/D-d24-host-dispatch-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/providers/agentHost/test/node/baseAgentHostSessionsProviderCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 三处双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。不重开 D176/D478。本文件这三处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / agentHost | closed |
| D619 | P3 | **closed** `sessionWorkspacePicker.ts` D611 留下的两处 leftover 单链 `.catch(onUnexpectedError)`：delegate `onSelect` `void this._dispatchPickerItem(item)`（~643）；restore `findWorkspace().then` `}).catch`（~1669）。`_dispatchPickerItem` 为 async（browse/connect `await` 会漏）；`findWorkspace` 返回 `Promise`。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀两处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** D611 `item.run()` / `executeCommand` 双链（~741/~744）。**未改** `_dispatchPickerItem(directBrowseItem)` `.catch.finally`（~556）。**未改** 既有 browser 测。`onUnexpectedError` 已 import。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于既有 `sessionWorkspacePickerCatchScan.test.ts`（本文件 only，cwd-first）：两处均为双链；裸/单链不在源；D611 两处仍双链；~556 仍 `.catch.finally`。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。不重开 D611/D157/D158/D161。D24 **仍开**。本行只关本文件这两处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 F `loop/F-d24-picker-dispatch-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/chat/test/node/sessionWorkspacePickerCatchScan.test.ts` **3/0**（D611 源扫 1 + D619 源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 两处双链 catch；裸/单链源扫不在；D611 双链仍在；~556 finally 仍单链。未关 D8/D16/D147/D24/D405/D487。不重开 D611/D157/D158/D161。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / workspace-picker | closed |
| D620 | P3 | **closed** `agentHostSessionConfigPicker.ts` 一处 leftover `_showPicker` onSelect IIFE `})().catch(onUnexpectedError)`（~815）。`void (async () => { ... })()`：`confirmAutoApproveLevel` await reject / 同步 throw 会漏；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `})().catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** `setSessionConfigValue` 三处 `.catch(() => { /* best-effort */ })`（isolation / onSelect property / stayOpen `onDidSelect`）。未改既有 browser 测。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `agentHostSessionConfigPickerCatchScan.test.ts`（本文件 only，cwd-first）：该 IIFE 一处且双链；单链不在源；best-effort 仍在。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 G `loop/G-d24-config-picker-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/providers/agentHost/test/node/agentHostSessionConfigPickerCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 IIFE 双链 catch；单链源扫不在；best-effort 未改。未关 D8/D16/D147/D24/D405/D487。本 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / config-picker | closed |
| D621 | P3 | **closed** `sessionGitHubInfo.ts` 一处 leftover `lookup.then(...).catch(onUnexpectedError)`（`_pullRequestNumberFor` ~142）。D181 已给 `findPullRequestNumberByHeadBranch` 内层 `.catch` 回 `undefined`。then 成功路径 `_logService.trace` / 驱逐 cache 再抛仍漏；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** D181 lookup 内层 catch。**未改** copilot `_pullRequestNumberForBranch` 单链。未改既有 browser 测。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `sessionGitHubInfoCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且 then 后双链；单链不在源；D181 内层 catch 仍在。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。不重开 D181。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 H `loop/H-d24-github-info-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/providers/agentHost/test/node/sessionGitHubInfoCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site then 后双链 catch；单链源扫不在；D181 内层 catch 未改。未关 D8/D16/D147/D24/D405/D487。不重开 D181。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / github-info-then | closed |
| D622 | P3 | **closed** `aiCustomizationOverviewView.ts` D614 留下的 leftover `this.openOverview()` 丢 Promise 两处：click ~151；keydown Enter/Space ~158。`openOverview` 为 async（`editorService.openEditor` reject 会漏）。丢 Promise 在 reject 时漏 unhandled；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀两处改 `void this.openOverview().catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** D614 五处 `loadCounts` 双链。**未改** 方法体 leftover。未碰 hover。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于既有 `aiCustomizationOverviewViewCatchScan.test.ts`（本文件 only，cwd-first）：两处均为双链；裸/单链不在源；D614 loadCounts 五处仍双链。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。不重开 D614。D24 **仍开**。本行只关本文件这两处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 I `loop/I-d24-overview-open-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/aiCustomizationTreeView/test/node/aiCustomizationOverviewViewCatchScan.test.ts` **3/0**（D614 源扫 1 + D622 源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 两处双链 catch；裸/单链源扫不在；D614 loadCounts 五处仍双链。未关 D8/D16/D147/D24/D405/D487。不重开 D614。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / customization-overview | closed |
| D623 | P3 | **closed** `copilotChatSessionsProvider.ts` 一处 leftover `_pullRequestNumberForBranch` 单链 `lookup.then(...).catch(onUnexpectedError)`（~1130）。`findPullRequestNumberByHeadBranch` 已 D185 内层 catch 回 `undefined`；then 成功路径 throw 与 `setUnexpectedErrorHandler` warn 再抛时单层 catch 仍漏 unhandled，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** D185 内层 `.catch(error => { onUnexpectedError(error); return undefined; })`。**未改** D615 `openRepository.then` 双链 / await `openRepository`。未改方法体 leftover。未碰 `sessionGitHubInfo.ts` 同类 lookup。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于既有 `copilotChatSessionsProviderCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且 then 后双链；裸 then / 单链不在源；D185 内层 catch 与 D615 openRepository 双链仍在。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。不重开 D615/D185。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 J `loop/J-d24-pr-lookup-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/providers/copilotChatSessions/test/node/copilotChatSessionsProviderCatchScan.test.ts` **3/0**（D615 源扫 1 + D623 源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site then 后双链 catch；裸/单链源扫不在；D185 内层 catch 与 D615 openRepository 双链未改。未关 D8/D16/D147/D24/D405/D487。不重开 D615/D185。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / pr-lookup-then | closed |
| D624 | P3 | **closed** `sessionWorkspacePicker.ts` D619 留下的一处 leftover 单链 `.catch(onUnexpectedError).finally`：`void this._dispatchPickerItem(directBrowseItem)`（~556）。`_dispatchPickerItem` 为 async；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）后再 `.finally`。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError).finally(() => { ... })`。finally 体仍清 `_directPickerGroup` / `_directPickerAttachesContext` 并 `triggerElement.focus()`，finally 内不 await。**未改** D611 `item.run()` / `executeCommand` 双链。**未改** D619 onSelect `_dispatchPickerItem(item)` / restore `findWorkspace().then` 双链。**未改** stayOpen/best-effort swallows。未改既有 browser 测。`onUnexpectedError` 已 import。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于既有 `sessionWorkspacePickerCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链+finally；D611/D619 仍双链。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。不重开 D611/D619。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 A `loop/A-d24-picker-finally-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/chat/test/node/sessionWorkspacePickerCatchScan.test.ts` **4/0**（D611 源扫 1 + D619 源扫 1 + D624 源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch + finally；D611/D619 双链仍在。未关 D8/D16/D147/D24/D405/D487。不重开 D611/D619。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / workspace-picker | closed |
| D625 | P3 | **closed** `sessionTurnChanges.ts` 四处 leftover 单链 `_openSessionTurnChanges(...).catch(onUnexpectedError)`：last-turn agent-host ~101；most-recent chat ~112；historical turn `{ id… }` ~136；`_openTransientLastTurnChanges` ~194。方法为 async；方法体已 try/catch（`onUnexpectedError` 不重抛）。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀四处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。**未改** ~155 `_openStandaloneChanges` `openEditor` 既有双链。**未改** 方法体 leftover。未包 sync void（`_openTransientLastTurnChanges` / `_openStandaloneChanges` 调用点）。`onUnexpectedError` 已 import。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `sessionTurnChangesCatchScan.test.ts`（本文件 only，cwd-first）：四处均为双链；裸/单链不在源；~155 仍双链。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这四处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 C `loop/C-d24-turn-changes-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/chat/test/node/sessionTurnChangesCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。gitlink `dev/loop` 脏勿 add。勿 add `out`。 | 四处双链 catch；裸/单链源扫不在；~155 仍双链。未关 D8/D16/D147/D24/D405/D487。本文件这四处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / turn-changes | closed |
| D626 | P3 | **closed** `sessionInputBanners.ts` 一处 leftover `void this._revealPullRequest(state.pullRequest).catch(onUnexpectedError)`（`revealCI.run` ~402）。`_revealPullRequest` 为 async；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。保留 `if (!state.debug)`。`onUnexpectedError` 已 import。**未改** ~245 `prModel.refresh` / ~252 `ciModelRef.object.refresh` 双链。**未改** `_revealComment`（sync）。未改 `sessionInputBannerWidget.ts`。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `sessionInputBannersCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源；refresh 双链仍在。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 D `loop/D-d24-banner-reveal-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/sessionInputBanners/test/node/sessionInputBannersCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在；refresh 双链未改。未关 D8/D16/D147/D24/D405/D487。本 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / input-banners | closed |
| D627 | P3 | **closed** `wslAgentHost.contribution.ts` 四处 leftover 单链 `.catch(onUnexpectedError)`：interval `_reconnectWSLEntriesIfRunning`（~85）；`_reconcile` 同方法（~94）；`_attemptWSLReconnect(entry.distro, …)`（~246）；`_scheduleWSLReconnect` `_attemptWSLReconnect(distro, name, address)`（~318）。两方法均返回 Promise。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀四处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** 方法体 leftover / D145 `listRunningDistros().catch(() => [])`。未碰 `cloudSandboxAgentHostContribution.ts`。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。mocha 源扫锁于新增 `wslAgentHostContributionCatchScan.test.ts`（本文件 only，cwd-first）：四处均为双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这四处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 F `loop/F-d24-wsl-reconnect-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/providers/remoteAgentHost/test/node/wslAgentHostContributionCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 四处双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件这四处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / wsl-reconnect | closed |
| D628 | P3 | **closed** `cloudSandboxAgentHostContribution.ts` 五处 leftover 单链 `.catch(onUnexpectedError)`：config change ~198 / ctor ~210 / retry ~221 的 `void this._discoverAndSeed()`；`_teardownEnvironment` ~431 / `connect` 取消 ~657 的 `void this._disconnectEnvironment(address)`。两方法均返回 Promise。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀五处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** `registerDiscoveryHandler(() => this._discoverAndSeed())`（registrar `await Promise.allSettled`）。**未改** `wslAgentHost.contribution.ts`。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `cloudSandboxAgentHostContributionCatchScan.test.ts`（本文件 only，cwd-first）：五处均为双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关这五处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 G `loop/G-d24-sandbox-discover-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/providers/remoteAgentHost/test/node/cloudSandboxAgentHostContributionCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 五处双链 catch；裸/单链源扫不在；registerDiscoveryHandler 未改。未关 D8/D16/D147/D24/D405/D487。本文件这五处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / cloud-sandbox-discover | closed |
| D629 | P3 | **closed** `sessionsTitleBarWidget.ts` `_openBlockedSession` 两处 leftover 单链 `.catch(onUnexpectedError)`：`openSessionToSide` ~656；`openSession` ~660。两方法均返回 Promise。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀两处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** `_showSessionsPicker`（`executeCommand` 无 await）。未碰 `sessionsView.ts`。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION` / D145 swallows。未发明 proto。mocha 源扫锁于新增 `sessionsTitleBarWidgetCatchScan.test.ts`（本文件 only，cwd-first）：两处均为双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关这两处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 H `loop/H-d24-titlebar-open-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/sessions/test/node/sessionsTitleBarWidgetCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 两处双链 catch；裸/单链源扫不在；`_showSessionsPicker` 未改。未关 D8/D16/D147/D24/D405/D487。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / titlebar-open | closed |
| D630 | P3 | **closed** `sessionsView.ts` 四处 leftover 单链 `.then(onOpened).catch(onUnexpectedError)`：`onSessionOpen` `openSessionToSide` ~217 / `openChat` ~220；`onChatOpen` `openChatToSide` ~230 / `openChat` ~233。三方法均返回 `Promise`。保留 `.then(onOpened)` 成功路径。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀四处改 `.then(onOpened).catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** ~211 同步 `onUnexpectedError(new Error(...))`。未碰 `sessionsTitleBarWidget.ts` / `sessionsList.ts`。未碰 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。mocha 源扫锁于新增 `sessionsViewCatchScan.test.ts`（本文件 only，cwd-first）：四处均为 then + 双链；裸 then / 单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这四处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 I `loop/I-d24-sessions-view-open-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/sessions/test/node/sessionsViewCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 四处 then 后双链 catch；裸 then / 单链源扫不在；~211 同步 Error 未改。未关 D8/D16/D147/D24/D405/D487。本文件这四处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / sessions-view-open | closed |
| D631 | P3 | **closed** `chatGroupsView.ts` 七处 leftover 单链 `.catch(onUnexpectedError)`：`_onChatDrop` ~176；`openChat(this._session!, resource)` ~494；`_splitChatIntoNewGroup(..., 'right')` ~670；`openChatInNewGroup` ~676；`openChat(session, URI.parse(activeResourceId))` ~734；`openChat(this._session, resource)` ~776；`_splitChatIntoNewGroup(URI.parse(resource), ...)` ~795。`_onChatDrop` / `_splitChatIntoNewGroup` / `openChatInNewGroup` 为 async；`ISessionsService.openChat` 返回 `Promise<void>`。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀七处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`（无 void，跟文件风格）。`onUnexpectedError` 已 import。**未改** 方法体 leftover（含 deserialize `onUnexpectedError(e)`、await `openChat` / `_splitChatIntoNewGroup`）。未包 sync void。未碰 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。mocha 源扫锁于新增 `chatGroupsViewCatchScan.test.ts`（本文件 only，cwd-first）：七处均为双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这七处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 J `loop/J-d24-chat-groups-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/test/node/chatGroupsViewCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 七处双链 catch；裸/单链源扫不在；方法体 leftover / sync `onUnexpectedError(e)` 未改。未关 D8/D16/D147/D24/D405/D487。本文件这七处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / chat-groups | closed |
| D632 | P3 | **closed** `sessionHeader.ts` 一处 leftover 单链 `renameSession(session, newTitle).catch(onUnexpectedError)`（inline rename `_startTitleEditing` ~362–364）。`ISessionsManagementService.renameSession` 返回 `Promise`。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** 其它方法。未碰 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `sessionHeaderCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 A `loop/A-d24-session-header-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/test/node/sessionHeaderCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / session-header-rename | closed |
| D633 | P3 | **closed** `sessionInputBannerWidget.ts` 一处 leftover `void this._runAction(action).catch(onUnexpectedError)`（`onDidClick` ~172）。`_runAction` 为 async / 返回 Promise。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** `sessionInputBanners.ts`（D626 已合）。**未改** dropdown `run: () => this._runAction(dropdownAction)`。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `sessionInputBannerWidgetCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 C `loop/C-d24-banner-widget-leftover` @ `bf4f41ca95a`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/sessionInputBanners/test/node/sessionInputBannerWidgetCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。gitlink `dev/loop` 脏勿 add。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。不重开 D626。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / input-banner-widget | closed |
| D634 | P3 | **closed** `newSessionPromptOptions.ts` 一处 leftover `void this._select(option).catch(onUnexpectedError)`（onDidClick ~187）。`_select` 为 async；单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** `_select` 方法体 leftover。未碰 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `newSessionPromptOptionsCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 D `loop/D-d24-prompt-options-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/chat/test/node/newSessionPromptOptionsCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在；`_select` 方法体未改。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / prompt-options | closed |
| D635 | P3 | **closed** `sessionBrowsersControl.ts` 一处 leftover 单链 `void this._openBrowser(input, chat).catch(onUnexpectedError)`（pill `open` ~155）。`_openBrowser` 返回 Promise。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** 方法体 leftover。未碰 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `sessionBrowsersControlCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 F `loop/F-d24-browsers-open-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/chat/test/node/sessionBrowsersControlCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在；方法体 leftover 未改。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / browsers-open | closed |
| D636 | P3 | **closed** `sessionsWindowNotifier.ts` 一处 leftover `void this._notify(session, newValue).catch(onUnexpectedError)`（autorunDelta ~90）。`_notify` 返回 Promise。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** ~77 Completed scheduler 单链。**未改** 方法体 leftover。未碰 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `sessionsWindowNotifierCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 G `loop/G-d24-window-notify-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/sessions/test/node/sessionsWindowNotifierCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在；~77 Completed scheduler 单链未改。未关 D8/D16/D147/D24/D405/D487。本 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / window-notify | closed |
| D637 | P3 | **closed** `sessionsList.ts` 三处 leftover 单链 `.catch(onUnexpectedError)`：`onDidRequestRename` `executeCommand(RENAME_SESSION_COMMAND_ID, session)` ~2478；chat-item `openChat(..., { preserveFocus })` ~2720；`updateSessionGroupLimit` `getTreatment(...).then` 的 `}).catch` ~2926。三处均返回 Promise。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀三处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。保留 `getTreatment().then` 形状，只加第二 catch。`onUnexpectedError` 已 import。**未改** ~780 `openerService.open`（D145 swallow）。未包 sync void（`manageAutomations` / voice activate）。未碰 `sessionsView.ts` / `sessionsTitleBarWidget.ts`。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `sessionsListCatchScan.test.ts`（本文件 only，cwd-first）：三处均为双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这三处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 H `loop/H-d24-sessions-list-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/sessions/test/node/sessionsListCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 三处双链 catch；裸/单链源扫不在；~780 / sync void 未改。未关 D8/D16/D147/D24/D405/D487。本文件这三处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / sessions-list | closed |
| D638 | P3 | **closed** `agentFeedbackAttachmentWidget.ts` 一处 leftover 单链 `void this._agentFeedbackService.revealFeedback(...).catch(onUnexpectedError)`（~106）。`revealFeedback` 返回 Promise。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** `agentFeedbackContextView.ts` 同类 void（后波）。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `agentFeedbackAttachmentWidgetCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 I `loop/I-d24-feedback-reveal-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/agentFeedback/test/node/agentFeedbackAttachmentWidgetCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在；context view 未改。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / agent-feedback-widget | closed |
| D639 | P3 | **closed** `singlePaneDockedTabsCoordinator.ts` 五处 leftover 单链 `_sequencer.queue(...).catch(onUnexpectedError)`：`_removeFilesTab` ~210；`_restoreCollapsedTabs` ~248；`_collapseNonManagedTabs` ~257 / ~539；`_reconcile(generation)` ~297。`_sequencer.queue` 返回 Promise。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀五处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** `singlePaneDetailPanelCoordinator.ts` / `singlePaneNewSessionStrategy.ts`（后批 leftover）。未碰 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `singlePaneDockedTabsCoordinatorCatchScan.test.ts`（本文件 only，cwd-first）：五处均为双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这五处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 J `loop/J-d24-docked-tabs-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/layout/test/node/singlePaneDockedTabsCoordinatorCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 五处双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。未碰 detail/newSession leftover。本文件这五处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / docked-tabs | closed |
| D640 | P3 | **closed** `sessionsWindowNotifier.ts` 一处 leftover Completed scheduler `void this._notify(session, SessionStatus.Completed).catch(onUnexpectedError)`（~77）。`_notify` 返回 Promise。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** ~90 NeedsInput/Error 双链（D636，不重开）。**未改** 方法体 leftover。未碰 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于既有 `sessionsWindowNotifierCatchScan.test.ts`（本文件 only，cwd-first）：新增 D640 suite 锁本 call site 一处且双链；裸/单链不在源；D636 suite 仍锁 newValue 双链。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 A `loop/A-d24-window-completed-leftover` @ `3353a2a34a7`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/sessions/test/node/sessionsWindowNotifierCatchScan.test.ts` **3/0**（D636 源扫 1 + D640 源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在；不重开 D636。未关 D8/D16/D147/D24/D405/D487。本 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / window-completed | closed |
| D641 | P3 | **closed** `agentFeedbackContextView.ts` 一处 leftover 单链 `void this._agentFeedbackService.revealFeedback(...).catch(onUnexpectedError)`（onDidOpen ~375）。`revealFeedback` 返回 Promise。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** `agentFeedbackAttachmentWidget.ts`（D638 已合）。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `agentFeedbackContextViewCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 C `loop/C-d24-feedback-tree-leftover` @ `3353a2a34a7`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/agentFeedback/test/node/agentFeedbackContextViewCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。gitlink `dev/loop` 脏勿 add。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在。未改 attachment widget。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / agent-feedback-context-view | closed |
| D642 | P3 | **closed** `singlePaneDetailPanelCoordinator.ts` 一处 leftover 单链 `void this._sequencer.queue(() => this._syncTarget(target, generation)).catch(onUnexpectedError)`（`_queueTarget` ~72）。`_sequencer.queue` 返回 Promise。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** `singlePaneDockedTabsCoordinator.ts`（D639 已合）/ `singlePaneNewSessionStrategy.ts`（工位 F）。未碰 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `singlePaneDetailPanelCoordinatorCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 D `loop/D-d24-detail-panel-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/layout/test/node/singlePaneDetailPanelCoordinatorCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。不重开 D639。未碰 docked-tabs / newSession leftover。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / detail-panel | closed |
| D643 | P3 | **closed** `singlePaneNewSessionStrategy.ts` 一处 leftover 单链 `void this._openEmptyFiles(...).finally(() => suppression.dispose()).catch(onUnexpectedError)`（~264–270）。`_openEmptyFiles` 返回 Promise。`.finally` 必须先跑（dispose 必执行）。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.finally(() => suppression.dispose()).catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未碰** docked-tabs（D639）/ detail-panel。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `singlePaneNewSessionStrategyCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 为 finally + 双链；finally 后单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 F `loop/F-d24-new-session-strategy-leftover` @ `3353a2a34a7`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/layout/test/node/singlePaneNewSessionStrategyCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site finally+双链 catch；finally 后单链源扫不在。未关 D8/D16/D147/D24/D405/D487。未碰 docked-tabs / detail-panel。本 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / new-session-strategy | closed |
| D644 | P3 | **closed** `sessionsTerminalContribution.ts` 两处 leftover 单链 `.catch(onUnexpectedError)`：removed `_closeTerminalsForSession(...).catch.finally(delete)` ~269；justArchived `_closeArchivedSessionTerminals(session)` ~272。两方法均返回 Promise。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀 ~269 改 `.catch(onUnexpectedError).catch(onUnexpectedError).finally(...)`（保留 finally body `_sessionTerminals.delete`）；~272 改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `sessionsTerminalContributionCatchScan.test.ts`（本文件 only，cwd-first）：~269 为双链+finally；~272 为双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这两处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 G `loop/G-d24-terminal-close-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/terminal/test/node/sessionsTerminalContributionCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 两处双链 catch（~269 带 finally）；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / terminal-close | closed |
| D645 | P3 | **closed** `sessionsTelemetry.contribution.ts` 二十四处 leftover 单链 `}).catch(onUnexpectedError);`（读文件核实，brief 估 ~22）：`_logRequestSent` `getAllTasks` ~203；同方法 `requestSent` then ~233；以及 ~239–436 会话/聊天/PR/commit/feedback 等 telemetry `then`（archived / unarchived / deleted / chatDeleted / chatRenamed / sessionRenamed / stickiness / maximize / createPR / draftPR / updatePR / mergePR / checkoutPR / initRepo / commit / commitAndSync / restored / fixCI / feedback 四事件）。这些 telemetry Promise 在 handler warn 再抛时单层 catch 仍漏（D480），故各改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未发明** 新 telemetry 事件。未碰 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `sessionsTelemetryCatchScan.test.ts`（本文件 only，cwd-first）：`}).catch(onUnexpectedError);` 已不在；双链 `}).catch(onUnexpectedError).catch(onUnexpectedError);` 共 24 处，等于改前 leftover 数。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这二十四处 telemetry call site fire-and-forget，leftover 程序未全局完成。 | 工位 H `loop/H-d24-sessions-telemetry-leftover` @ `3353a2a34a7`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/sessions/test/node/sessionsTelemetryCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 二十四处 telemetry 双链 catch；单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件这二十四处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / telemetry-then | closed |
| D646 | P3 | **closed** `agentHostUntitledProvisionalSessionService.ts` 四处 leftover 单链 `.catch(onUnexpectedError)`：`_changeWorkingDirectory(sessionResource, folder)` ~330；`_changeWorkingDirectory(..., resolveNewSessionPrimary)` ~344；`_queue(..., _reconcileGeneration)` ~434；`_disposeBackend(..., 'pending provisional cleanup')` ~702。四处均返回 Promise。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀四处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** ~303 `disposeSession`（已双链）。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `agentHostUntitledProvisionalSessionServiceCatchScan.test.ts`（本文件 only，cwd-first）：四处均为双链；~303 仍双链；裸/单链不在源。未扫其它文件。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这四处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 I `loop/I-d24-untitled-provisional-leftover` @ `3353a2a34a7`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/agentHostUntitledProvisionalSessionServiceCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 四处双链 catch；~303 仍双链；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件这四处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / untitled-provisional | closed |
| D647 | P3 | **closed** `agentHostSignedOutModelsNotification.ts` 三处 leftover 单链 `.then(...).catch(onUnexpectedError)`：`getDefaultAccount()` ~105–110；`whenInstalledExtensionsRegistered()` ~123–128；`whenReady` ~129–134。三处均返回 Promise。保留 `.then(...)` 成功路径。单层 catch 在 `setUnexpectedErrorHandler` warn 再抛时仍漏，故双链（D480）。本刀三处改 `.then(...).catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** `_update()` 同步调用。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `agentHostSignedOutModelsNotificationCatchScan.test.ts`（本文件 only，cwd-first）：三处均为 then + 双链；裸 then / 单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这三处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 J `loop/J-d24-signed-out-notify-leftover` @ `3353a2a34a7`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/agentHostSignedOutModelsNotificationCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 三处 then 后双链 catch；裸 then / 单链源扫不在；`_update()` 同步调用未改。未关 D8/D16/D147/D24/D405/D487。本文件这三处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / signed-out-notify | closed |
| D648 | P3 | **closed** `sessionHoverContent.ts` 一处 leftover 单链 `preferencesService.openSettings({...}).catch(onUnexpectedError)`（externalSession `onOpen` ~132–135）。`openSettings` 返回 Promise。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** `openerService.open`（D145 swallow）。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `sessionHoverContentCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 A `loop/A-d24-hover-open-leftover` @ `b572fb1e8e4`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/sessions/test/node/sessionHoverContentCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在。未改 openerService.open（D145）。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / hover-openSettings | closed |
| D649 | P3 | **closed** `agentHostCustomizationService.ts` 一处 leftover 单链 `void provider.setRootConfigValue(property, value).catch(onUnexpectedError)`（~90）。`setRootConfigValue` 返回 Promise。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未包** `startMcpServer`/`stopMcpServer`（返回 Promise 给调用方）。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `agentHostCustomizationServiceCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 C `loop/C-d24-root-config-leftover` @ `b572fb1e8e4`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/services/agentHost/test/node/agentHostCustomizationServiceCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。gitlink `dev/loop` 脏勿 add。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在。未包 startMcpServer/stopMcpServer。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / agentHost-root-config | closed |
| D650 | P3 | **closed** `sessions/contrib/chat/browser/agentHostInputCompletions.ts` 一处 leftover 单链 `void this._registerForScheme(scheme).catch(onUnexpectedError)`（autorun ~237）。`_registerForScheme` 为 async。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未碰** workbench `widget/input/editor/agentHostInputCompletions.ts`（工位 F）。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `agentHostInputCompletionsCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 D `loop/D-d24-sessions-completions-leftover` @ `b572fb1e8e4`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/sessions/contrib/chat/test/node/agentHostInputCompletionsCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在。未碰 workbench sibling。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | sessions / input-completions | closed |
| D651 | P3 | **closed** workbench `widget/input/editor/agentHostInputCompletions.ts` 两处 leftover 单链 `void this._registerForScheme(scheme).catch(onUnexpectedError)`：existing schemes ~117；added schemes ~124。`_registerForScheme` 为 async。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀两处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未碰** sessions `contrib/chat/browser/agentHostInputCompletions.ts`（工位 D）。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `agentHostInputCompletionsCatchScan.test.ts`（本文件 only，cwd-first）：两处均为双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这两处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 F `loop/F-d24-workbench-completions-leftover` @ `b572fb1e8e4`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/agentHostInputCompletionsCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 两处双链 catch；裸/单链源扫不在。未碰 sessions 同名文件。未关 D8/D16/D147/D24/D405/D487。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / input-completions | closed |
| D652 | P3 | **closed** `agentHostModeSynchronizer.ts` 一处 leftover 单链 `void this._applyMode(widget, sessionResource, agentUri).catch(onUnexpectedError)`（`_syncWidgetFromBackend` ~120）。`_applyMode` 返回 Promise。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `agentHostModeSynchronizerCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 G `loop/G-d24-mode-sync-leftover` @ `b572fb1e8e4`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/agentHostModeSynchronizerCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / mode-sync | closed |
| D653 | P3 | **closed** `chatTipContentPart.ts` 一处 leftover 单链 `this._handleTipAction(link, md).catch(onUnexpectedError)`（actionHandler ~130）。`_handleTipAction` 返回 Promise。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `chatTipContentPartCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 H `loop/H-d24-tip-action-leftover` @ `b572fb1e8e4`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/chatTipContentPartCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / chat-tip-action | closed |
| D654 | P3 | **closed** `chatRequestOriginPart.ts` 一处 leftover 单链 `void this._openSource?.().catch(onUnexpectedError)`（`_open` ~221）。`_openSource` 返回 Promise。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`，保留 optional chain。`onUnexpectedError` 已 import。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `chatRequestOriginPartCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 I `loop/I-d24-origin-open-leftover` @ `b572fb1e8e4`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/chatRequestOriginPartCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / chat-request-origin | closed |
| D655 | P3 | **closed** `abstractToolConfirmationSubPart.ts` 一处 leftover 单链 `this._editorService.closeEditors(toClose).catch(onUnexpectedError)`（~74）。`closeEditors` 返回 Promise。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`（void 前缀可选，本刀未加）。`onUnexpectedError` 已 import。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `abstractToolConfirmationSubPartCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 J `loop/J-d24-tool-close-leftover` @ `b572fb1e8e4`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/abstractToolConfirmationSubPartCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / tool-closeEditors | closed |
| D656 | P3 | **closed** `chatDebugServiceImpl.ts` 两处 leftover 单链 `.catch(onUnexpectedError)`：`this._invokeProvider(provider, sessionResource, cts.token)`（registerProvider ~406）；`entry.fetcher(...).then(...)`（`_tryFetchAvailableSessions` ~597）。两处均返回 Promise。保留既有 then / call 形态。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀两处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。文件仅这两处 `.catch(`。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `chatDebugServiceImplCatchScan.test.ts`（本文件 only，cwd-first）：两处各一且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这两处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 A `loop/A-d24-chat-debug-leftover` @ `3d97442dca2`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/chatDebugServiceImplCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 两处双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / chat-debug | closed |
| D657 | P3 | **closed** `chatSetupGrowthSession.ts` 一处 leftover 单链 `lifecycleService.when(LifecyclePhase.Restored).then(...).catch(onUnexpectedError)`（ctor ~65–72）。`when` 返回 Promise。保留 `.then(...)` 成功路径（dismiss-on-widget）。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。本文件仅此一处 `.catch(`。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `chatSetupGrowthSessionCatchScan.test.ts`（本文件 only，cwd-first）：该 then 一处且双链；单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 C `loop/C-d24-growth-session-leftover` @ `3d97442dca2`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/chat/test/node/chatSetupGrowthSessionCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。gitlink `dev/loop` 脏勿 add。勿 add `out`。 | 本 call site then+双链 catch；单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / growth-session | closed |
| D658 | P3 | **closed** `browserAutoReloadFeatures.ts` 一处 leftover 单链 `this._model.reload().catch(onUnexpectedError)`（`_reloadPendingChange` ~136）。`reload` 返回 `Promise<void>`。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `browserAutoReloadFeaturesCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 D `loop/D-d24-browser-reload-leftover` @ `3d97442dca2`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/browserView/test/node/browserAutoReloadFeaturesCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / browser-auto-reload | closed |
| D659 | P3 | **closed** `bracketPairColorizer2Telemetry.contribution.ts` 一处 leftover 单链 `this.init().catch(onUnexpectedError)`（ctor ~21）。`init` 返回 Promise。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `bracketPairColorizer2TelemetryCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 F `loop/F-d24-bracket-telemetry-leftover` @ `3d97442dca2`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/bracketPairColorizer2Telemetry/test/node/bracketPairColorizer2TelemetryCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / bracket-telemetry | closed |
| D660 | P3 | **closed** `notebookEditorInput.ts` 一处 leftover 单链 `this.resolve().catch(onUnexpectedError)`（side-loaded `onDidAddNotebookDocument` ~89）。`resolve` 返回 Promise。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** `_notebookModelResolverService.resolve` / `override async resolve`。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `notebookEditorInputCatchScan.test.ts`（本文件 only，cwd-first）：该 side-loaded `this.resolve()` call site 一处且双链；裸/单链不在源。未扫其它 resolve。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 G `loop/G-d24-notebook-resolve-leftover` @ `3d97442dca2`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/notebook/test/node/notebookEditorInputCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在。未改其它 resolve。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / notebook-editor-input | closed |
| D661 | P3 | **closed** `startupTimings.ts` 两处 leftover 单链 `.catch(onUnexpectedError)`：构造 `_report()` ~62；`_report` 内 `_appendStartupTimes(standardStartupError)` ~67。两方法均为 async。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀两处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。本文件仅此两处 `.catch(`。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `startupTimingsCatchScan.test.ts`（本文件 only，cwd-first）：两处各一且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这两处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 H `loop/H-d24-startup-timings-leftover` @ `3d97442dca2`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/performance/test/node/startupTimingsCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 两处双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / startup-timings | closed |
| D662 | P3 | **closed** `breakpointEditorContribution.ts` 一处 leftover 单链 `this.debugService.runTo(uri, lineNumber).catch(onUnexpectedError)`（runToLine action ~488）。`runTo` 返回 `Promise<void>`。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。保留 arrow、不加分号。`onUnexpectedError` 已 import。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `breakpointEditorContributionCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 I `loop/I-d24-run-to-line-leftover` @ `3d97442dca2`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/debug/test/node/breakpointEditorContributionCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / debug-runTo | closed |
| D663 | P3 | **closed** `inlineChatController.ts` 一处 leftover 单链 `void controller.run({ message, autoSend: true }).catch(onUnexpectedError)`（`#replaySavedUntitledInput` ~644）。`run` 返回 Promise。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。保留 `void` 与 un-awaited replay 注释。`onUnexpectedError` 已 import。**未改** ~501 `openEditor(..., SIDE_GROUP).catch(onUnexpectedError)`（D145-style opener 单链）。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `inlineChatControllerCatchScan.test.ts`（本文件 only，cwd-first）：run() 一处且双链；openEditor 仍单链。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 J `loop/J-d24-inline-replay-leftover` @ `3d97442dca2`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/inlineChat/test/node/inlineChatControllerCatchScan.test.ts` **3/0**（源扫 1 + openEditor 单链锁 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；openEditor 仍单链。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / inlineChat-replay | closed |
| D664 | P3 | **closed** `gettingStarted.ts` 两处 leftover 单链 `this.recentlyOpened.then(...).catch(onUnexpectedError)`：`buildRecentlyOpenedList` ~1130；`refreshRecentlyOpened` ~1149。`recentlyOpened` 为 `Promise<IRecentlyOpened>`。保留两处 then 成功路径（filter + setEntries / formatter 重刷）。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀两处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。本文件仅此两处 `.catch(`。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `gettingStartedCatchScan.test.ts`（本文件 only，cwd-first）：两处 then+双链；单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这两处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 A `loop/A-d24-getting-started-leftover` @ `a62700c5692`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/welcomeGettingStarted/test/node/gettingStartedCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 两处 then+双链 catch；单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / getting-started | closed |
| D672 | P3 | **closed** `suggestModel.ts` 一处 leftover 单链 `Promise.all([completions, wordDistance]).then(...).catch(onUnexpectedError)`（completions then ~589–649）。`Promise.all` 返回 Promise。保留 then 成功路径（CompletionModel / telemetry / invalid warn）。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。本文件仅此一处 `.catch(`。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `suggestModelCatchScan.test.ts`（本文件 only，cwd-first）：该 then 一处且双链；单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 A `loop/A-d24-suggest-model-leftover` @ `072e0112612`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/editor/contrib/suggest/test/node/suggestModelCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site then+双链 catch；单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | editor / suggest-model | closed |
| D665 | P3 | **closed** `surveyEditorPane.ts` 一处 leftover 单链 `this.editorService.closeEditor({ editor: submittedInput, groupId: this.group.id }).catch(onUnexpectedError)`（`showSuccess` 自动关 pane ~321）。`closeEditor` 返回 `Promise<void>`。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `surveyEditorPaneCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 C `loop/C-d24-survey-close-leftover` @ `a62700c5692`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/contrib/surveys/test/node/surveyEditorPaneCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。gitlink `dev/loop` 脏勿 add。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / survey-closeEditor | closed |
| D666 | P3 | **closed** `extHostDocumentContentProviders.ts` 一处 leftover 单链+finally：`$provideTextDocumentContent(handle, uri).then(...).catch(onUnexpectedError).finally(...)`（onDidChange ~84–89）。`$provideTextDocumentContent` 返回 Promise。保留 multiline 与 finally 体（`lastEvent` 清）。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError).finally(() => { ... })`，不压成一行。`onUnexpectedError` 已 import。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `extHostDocumentContentProvidersCatchScan.test.ts`（本文件 only，cwd-first）：该 then 一处且双链+finally；单链+finally 不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 D `loop/D-d24-virtual-doc-leftover` @ `a62700c5692`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/api/test/node/extHostDocumentContentProvidersCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch + finally；单链+finally 源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / virtual-doc | closed |
| D667 | P3 | **closed** `codexAccountService.ts` 一处 leftover 单链 `void readCodexProfileImageDataUri(...).then(...).catch(onUnexpectedError)`（`_updateProfileImage` ~185）。`readCodexProfileImageDataUri` 返回 Promise。保留 `.then(...)` 成功路径（写 `profileImageDataUri` 并 fire）。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** ~161 `void openCodexAuthUrl(...).catch(onUnexpectedError)`（D145 opener 单链）。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `codexAccountServiceCatchScan.test.ts`（本文件 only，cwd-first）：profile then 一处且双链；`openCodexAuthUrl` 仍单链。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 F `loop/F-d24-codex-profile-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/workbench/services/agentHost/test/node/codexAccountServiceCatchScan.test.ts` **3/0**（源扫 1 + openCodexAuthUrl 单链锁 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site then+双链 catch；`openCodexAuthUrl` 仍单链。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | workbench / codex-account | closed |
| D668 | P3 | **closed** `wordHighlighter.ts` 三处 leftover 单链 `runDelayer.trigger(...).catch(onUnexpectedError)`：`_onPositionChanged` ~275；`_run()` ~284；`restore` `_run(false, delay)` ~367。`Delayer.trigger` 返回 Promise。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀三处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。**未改** `workerRequest.result.then(..., onUnexpectedError)` 两处（~715 / ~741）。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `wordHighlighterCatchScan.test.ts`（本文件 only，cwd-first）：三处各一且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这三处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 G `loop/G-d24-word-highlighter-leftover`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/editor/contrib/wordHighlighter/test/node/wordHighlighterCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 三处双链 catch；裸/单链源扫不在。未改 then 回调 `onUnexpectedError`。未关 D8/D16/D147/D24/D405/D487。本文件这三处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | editor / wordHighlighter-delayer | closed |
| D669 | P3 | **closed** `formatActions.ts` 一处 leftover 单链 `this._instantiationService.invokeFunction(formatDocumentRangesWithSelectedProvider, …).catch(onUnexpectedError)`（`FormatOnPaste._trigger` ~208）。`formatDocumentRangesWithSelectedProvider` 返回 `Promise<void>`。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。保持一行。`onUnexpectedError` 已 import。**未改** `FormatOnType._trigger` 的 `.then/.finally`、`FormatDocumentAction` / `FormatSelectionAction` await 路径。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `formatActionsCatchScan.test.ts`（本文件 only，cwd-first）：该 call site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 H `loop/H-d24-format-ranges-leftover` @ `a62700c5692`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/editor/contrib/format/test/node/formatActionsCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | editor / format-on-paste | closed |
| D670 | P3 | **closed** `parameterHintsModel.ts` 一处 leftover 单链 `this.throttledDelayer.trigger(() => { return this.doTrigger(triggerId); }, delay).catch(onUnexpectedError)`（`trigger` ~123–126）。`doTrigger` 为 `async` / 返回 `Promise<boolean>`。保留 trigger 回调与 `}, delay)` 换行形状，只把末行改成双链。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。本文件仅此一处 `.catch(`。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `parameterHintsModelCatchScan.test.ts`（本文件 only，cwd-first）：该 delayer site 一处且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 I `loop/I-d24-parameter-hints-leftover` @ `a62700c5692`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/editor/contrib/parameterHints/test/node/parameterHintsModelCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | editor / parameter-hints-delayer | closed |
| D671 | P3 | **closed** `inPlaceReplace.ts` 两处 leftover 单链 `.catch(onUnexpectedError)`：`this.decorationRemover.then(() => this.decorations.clear())`（~123）；外层 `this.currentRequest.then(result => { ... })`（~125）。两处均返回 Promise。保留既有 then / 外层形态。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀两处改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。本文件仅此两处 `.catch(`。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `inPlaceReplaceCatchScan.test.ts`（本文件 only，cwd-first）：两处各一且双链；裸/单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本文件这两处 call site fire-and-forget，leftover 程序未全局完成。 | 工位 J `loop/J-d24-inplace-replace-leftover` @ `a62700c5692`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/editor/contrib/inPlaceReplace/test/node/inPlaceReplaceCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 两处双链 catch；裸/单链源扫不在。未关 D8/D16/D147/D24/D405/D487。本文件这两处 leftover fire-and-forget 已收；leftover 程序未全局完成。 | editor / inPlaceReplace | closed |
| D673 | P3 | **closed** `suggestWidget.ts` 一处 leftover 单链 `this._currentSuggestionDetails.then(...).catch(onUnexpectedError)`（焦点解析 ~435–453）。`_currentSuggestionDetails` 为 `CancelablePromise`。保留 then 成功路径（越界/换项早退、splice 重渲、setFocus、details/docs-side、setAriaOptions）。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。本文件仅此一处 `.catch(`。**未改** `suggestModel.ts`（工位 A）。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `suggestWidgetCatchScan.test.ts`（本文件 only，cwd-first）：该 then 一处且双链；then 体仍在；单链不在源。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 C `loop/C-d24-suggest-widget-leftover` @ `072e0112612`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/editor/contrib/suggest/test/node/suggestWidgetCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。gitlink `dev/loop` 脏勿 add。勿 add `out`。 | 本 call site then+双链 catch；then 体保留；单链源扫不在。未关 D8/D16/D147/D24/D405/D487。未改 suggestModel。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | editor / suggest-widget | closed |
| D674 | P3 | **closed** `asyncDataTree.ts` 一处 leftover 单链 `this.refreshAndRenderNode(node.element, false).catch(onUnexpectedError)`（`_onDidChangeCollapseState` ~1210–1211）。`refreshAndRenderNode` 返回 Promise。保留既有多行调用。单层 catch 在 handler warn 再抛时仍漏（D480），故双链。本刀改 `.catch(onUnexpectedError).catch(onUnexpectedError)`。`onUnexpectedError` 已 import。跳过 D145 swallows。未碰 grpcClient / catalog `onOpenConnection` / `OPEN_CONNECTION`。未发明 proto。跳过 Connect/SaveSkillContent/Watch/ResolveTurn/ResolveAnchor。mocha 源扫锁于新增 `src/vs/base/test/node/asyncDataTreeCatchScan.test.ts`（cwd-first；`base/browser/ui/tree/test` 树不存在）：该 call site 一处且双链；单链不在此调用。未扫其它文件。未碰 pills。未 compile-client。不得宣称 leftover/pills 完成。D24 **仍开**。本行只关本 call site fire-and-forget，leftover 程序未全局完成。 | 工位 D `loop/D-d24-async-tree-leftover` @ `072e0112612`；增量 transpile 测文件后 `node test/unit/node/index.js --run src/vs/base/test/node/asyncDataTreeCatchScan.test.ts` **2/0**（源扫 1 + Errors 1）。未 commit。勿 add `out`。 | 本 call site 双链 catch；单链源扫不在此调用。未关 D8/D16/D147/D24/D405/D487。本文件此 call site leftover fire-and-forget 已收；leftover 程序未全局完成。 | base / async-data-tree | closed |
