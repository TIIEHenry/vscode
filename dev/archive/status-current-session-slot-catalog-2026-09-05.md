---
title: "status.md Current Session 槽位 catalog 流水（归档）"
type: progress
status: archived
phase: M7
updated: 2026-09-05
summary: "2026-09-05 自 dev/progress/status.md Current Session 迁出；取代者为精简后的 status.md"
---

# status.md Current Session 槽位 catalog 流水（归档）

> 原 `dev/progress/status.md` §Current Session 中 A–D 槽 gRPC catalog / UI 绑定逐条流水。docs-burden-reduction S4 后不再维护于 status；历史查阅用本文件。

- **槽 A / `loop/A`：** 已 reset 到 `loop/merge` `a7a5ac7e`（Clipboard UI 已进 tip）。Conversation / Composer 无既有 Context Variable 座。把已 catalog 的 `ContextVariableService.List`（及 `Read`）绑进 Engine Preferences Context Variables 诚实列表。接通 + 有 hook 才发；空 `sessionId` / `agentId` / `name` 原样上线（不发明会话 / agent 默认；无选中行仍发空 id）；空 `name` / `updatedBy` / `contentPreview` 原样显示；`updatedAt` 0 原样显示。断连 / 无 hook 不发。**不**发明 RPC、不重做 catalog、不改 Conversation roster / SessionBar / Snapshots / Connection Devices / Triggers / Clipboard、不做 D22/F3。≠ Clipboard / DeviceService / TriggerService。测：接通 / 空 id / 断连 / 空列表。
- **槽 D / `loop/D`：** 已 reset 到 `loop/merge` `a7a5ac7e`（List / Read UI 已进 tip）。把已 catalog 的 `ClipboardService.Write`（及 `Clear`）绑进既有 Engine Preferences Clipboard 列表动作。接通 + 有 hook 才发；Write 始终发空 `sessionId` / `agentId` / `label` / `content` / `filePath` / `url`（`type` TEXT）；Clear 空 `sessionId` 原样上线；空 `clipId` / `removedCount` 0 原样显示。断连 / 无 hook 不发。**不**发明 RPC、不重做 catalog、不改 Conversation roster / SessionBar / Snapshots / Connection Devices / Triggers、不做 D22/F3。≠ List / Read / DeviceService / TriggerService。测：接通 / 空 id / 断连。
- **槽 C / `loop/C`：** 已 reset 到 `loop/merge` `c3e8f642`（UpsertTrigger UI 已进 tip）。Conversation / Composer 无既有 Clipboard 座。把已 catalog 的 `ClipboardService.List`（及 `Read`）绑进 Engine Preferences Clipboard 诚实列表。接通 + 有 hook 才发；空 `sessionId` / `clipId` 原样上线（不发明会话默认；无选中行仍发空 id）；空 `clipId` / `label` / `createdBy` 原样显示；`createdAt` 0 原样显示。断连 / 无 hook 不发。**不**发明 RPC、不重做 catalog、不改 Conversation roster / SessionBar / Snapshots / Connection Devices / Triggers、不做 D22/F3。≠ Write / Clear / DeviceService / TriggerService。测：接通 / 空 id / 断连 / 空列表。
- **槽 B / `loop/B`：** 已 reset 到 `loop/merge` `ae40d2ec`（DeleteTrigger UI 已进 tip）。把已 catalog 的 `TriggerService.UpsertTrigger` 绑进既有 Engine Preferences Triggers 列表动作（Add / Edit）。接通 + 有 hook 才发；空 `scope` / `scope_id` 原样上线；Add 始终发空 TriggerDto；Edit 无选中行仍发空 TriggerDto；空 `trigger_id` / `name` / `type` 原样上线；`enabled` false 原样上线。断连 / 无 hook 不发。**不**发明 RPC、不重做 catalog、不改 Conversation roster / SessionBar / Snapshots / Connection Devices、不做 D22/F3。≠ ListTriggers / DeleteTrigger / SetTriggerEnabled / FireTrigger / FireTriggerWebhook / DeviceService。测：接通 / 空 id / 断连。
- **槽 A / `loop/A`：** 已 reset 到 `loop/merge` `34b84015`（SetTriggerEnabled UI 已进 tip）。把已 catalog 的 `TriggerService.DeleteTrigger` 绑进既有 Engine Preferences Triggers 列表动作（Delete）。接通 + 有 hook 才发；空 `scope` / `scope_id` / `trigger_id` 原样上线（无选中行仍发）；响应空 `{}`。断连 / 无 hook 不发。**不**发明 RPC、不重做 catalog、不改 Conversation roster / SessionBar / Snapshots / Connection Devices、不做 D22/F3。≠ ListTriggers / UpsertTrigger / SetTriggerEnabled / FireTrigger / FireTriggerWebhook / DeviceService。测：接通 / 空 id / 断连。
- **槽 D / `loop/D`：** 已 reset 到 `loop/merge` `0dccfc51`（FireTrigger UI 已进 tip）。把已 catalog 的 `TriggerService.SetTriggerEnabled` 绑进既有 Engine Preferences Triggers 列表动作（Enable / Disable）。接通 + 有 hook 才发；空 `scope` / `scope_id` / `trigger_id` 原样上线（无选中行仍发）；`enabled` false 原样上线。断连 / 无 hook 不发。**不**发明 RPC、不重做 catalog、不改 Conversation roster / SessionBar / Snapshots / Connection Devices、不做 D22/F3。≠ ListTriggers / UpsertTrigger / DeleteTrigger / FireTrigger / FireTriggerWebhook / DeviceService。测：接通 / 空 id / enabled false / 断连。
- **槽 C / `loop/C`：** 已 reset 到 `loop/merge` `dcaf1840`（ListTriggers UI 已进 tip）。把已 catalog 的 `TriggerService.FireTrigger` 绑进既有 Engine Preferences Triggers 列表动作（无 FireTriggerWebhook 座）。接通 + 有 hook 才发；空 `scope` / `scope_id` / `trigger_id` 原样上线（无选中行仍发）；断连 / 无 hook 不发。**不**发明 RPC、不重做 catalog、不改 Conversation roster / SessionBar / Snapshots / Connection Devices、不做 D22/F3。≠ ListTriggers / UpsertTrigger / DeleteTrigger / SetTriggerEnabled / FireTriggerWebhook / DeviceService。测：接通 / 空 id / 断连。
- **槽 B / `loop/B`：** 已 reset 到 `loop/merge` `a8c136e0`。Device 不在本刀。无既有 Triggers chrome / Fire webhook 座。把已 catalog 的 `TriggerService.ListTriggers` 绑进 Engine Preferences Triggers 诚实列表。接通 + 有 hook 才发；空 `scope` / `scope_id` / `type_filter` 原样上线（不发明会话 / 项目 / 类型默认）；空 `trigger_id` / `name` / `type` 原样显示。断连 / 无 hook 不发。**不**发明 RPC、不重做 catalog、不改 Conversation roster / SessionBar / Snapshots / Connection Devices、不做 D22/F3。≠ UpsertTrigger / DeleteTrigger / SetTriggerEnabled / FireTrigger / FireTriggerWebhook / DeviceService。测：接通 / 空 id / 断连 / 空列表。
- **槽 A / `loop/A`：** 已 reset 到 `loop/merge` `929d27a9`。ListDevices / ListPending / PairApprove / PairReject 已在 tip，不重做。把已 catalog 的 `DeviceService.RotateToken` 绑进既有 Connection Devices 已配对列表动作（无 rotate/token 座，新增 paired-list 动作）。接通 + 有 hook 才发；空 `device_id` 原样上线（无选中行仍发）；Reserved `new_token`（ADR-261）不映射；断连 / 无 hook 不发。**不**发明 RPC、不重做 catalog、不改 Conversation roster / SessionBar / Snapshots / Engine Preferences、不做 D22/F3。≠ ListDevices / ListPending / PairApprove / PairReject / Revoke。测：接通 / 空 id / 断连。
- **槽 D / `loop/D`：** 已 reset 到 `loop/merge` `623693a6`。ListDevices / Team 不在本刀。ListPending / PairApprove / PairReject 已在 tip。把已 catalog 的 `DeviceService.Revoke`（`revoke`，≠ Hub `revokeDevice`）绑进既有 Connection Devices Revoke。接通 + 有 hook 才发；空 `device_id` 原样上线（选中行空 id 仍发）；断连 / 无 hook 仍走 Hub `revokeDevice`。**不**发明 RPC、不重做 catalog、不改 Conversation roster / SessionBar / Snapshots / Engine Preferences、不做 D22/F3。≠ ListDevices / ListPending / PairApprove / PairReject / RotateToken / Team。测：接通 / 空 id / 断连。
- **槽 C / `loop/C`：** 已 reset 到 `loop/merge` `9657dd63`。Team / ListPending / PairApprove / PairReject 已在 tip，不重做。把已 catalog 的 `DeviceService.ListDevices` 绑进既有 Connection Devices 已配对列表（Refresh 亦拉）。接通 + 有 hook 才发；空 `device_id` / `display_name` / `role` / `platform` 原样映射（空 devices[] 原样空列表）；断连 / 无 hook 不发、仍走 Hub 目录。**不**发明 RPC、不重做 catalog、不改 Conversation roster / SessionBar / Snapshots / Engine Preferences、不做 D22/F3。≠ Team / ListPending / PairApprove / PairReject / Revoke / RotateToken。测：接通 / 空 id / 断连 / 空列表。
- **槽 B / `loop/B`：** 已 reset 到 `loop/merge` `9657dd63`。Start / Kill 未绑进 tip。把已 catalog 的 `TeamService.Abort` 绑进既有 Navigator Team ViewTitle（Abort）。接通 + 有 hook 才发；空 `sessionId` / `agentId` / `reason` 原样上线；`teamId` 0 原样上线（无选中行 / 无 liveTeamId 仍发）。成功后再拉 MemberStatus / TaskList / TeamInfo。**不**发明 RPC、不重做 catalog、不改 Conversation roster / SessionBar / Snapshots、不做 D22/F3。≠ StartMember / KillMember / MessageMember / TaskUpdate / TaskCancel / CreateTeam。测：接通 / 空 id / 断连。
- **槽 A / `loop/A`：** 已 reset 到 `loop/merge` `44cbf5d6`。StartMember / KillMember 已在 tip catalog，Members UI 尚未转发。把已 catalog 的 `TeamService.StartMember` / `KillMember` 绑进既有 Team Members ViewTitle（Start / Kill）。接通 + 有 hook 才发；空 `sessionId` / `agentId` / `memberName` / `presetId` / `systemPrompt` / `modelType` 原样上线（无选中行仍发空 id；`dynamic` false 原样上线）；成功后再拉 MemberStatus。**不**发明 RPC、不重做 catalog、不改 Conversation roster / SessionBar / Snapshots、不做 D22/F3。≠ MessageMember / CreateTeam / Abort / TaskUpdate。
- **槽 C / `loop/C`：** 已 reset 到 `loop/merge` `993a652c`。Navigator Team 已有 TaskList / MemberStatus / TeamInfo。把已 catalog 的 `TeamService.MessageMember` 绑进既有 Team Members ViewTitle（Message）。接通 + 有 hook 才发；空 `sessionId` / `agentId` / `memberName` / `content` 原样上线（无选中行仍发空 id）；成功后再拉 MemberStatus。**不**发明 RPC、不重做 catalog、不改 Conversation roster / SessionBar / Snapshots、不做 D22/F3。≠ History / TaskUpdate / TaskCancel / StartMember / KillMember。
- **槽 D / `loop/D`：** 已 reset 到 `loop/merge` `44cbf5d6`。History / Team Tasks 已在 tip，不重做。把已 catalog 的 `DeviceService.ListPending` / `PairApprove` / `PairReject` 绑进既有 Connection Devices（Confirm / Reject / pending 列表 + Refresh）。接通 + 有 hook 才发；空 `pairing_code` / `display_name` / `role` 原样上线（无选中 pending / 空输入仍发）；断连 / 无 hook 不发 ListPending / PairReject，Confirm 仍走 Hub `confirmDeviceCode`。**不**发明 RPC、不重做 catalog、不改 Conversation roster / SessionBar / Snapshots / Engine Preferences、不做 D22/F3。≠ History / Team / ListDevices / Revoke / RotateToken。测：接通 / 空 id / 断连 / 空 pending。
- **槽 A / `loop/A`：** 已 reset 到 `loop/merge` `d1efe8d7`。GetHistory 已在 tip catalog + 时间线 fold，History UI 仍是未挂载的回合索引。把 Conversation SessionBar History 接到已有 `SessionService.GetHistory` / `getHistory`（`ConversationEngineHistoryList` overlay；空 `sessionId` 原样上线；断连不发、不把 fixture 回合冒充引擎信封；浅预览 `cursor_seq` + payload，不另写 fold / 不重做 catalog）。≠ Snapshots list/restore/delete。测：History overlay 接通 / 空 id / 断连 / 空列表。无 D22/F3、无全量编译。
- **槽 B / `loop/B`：** 已 reset 到 `loop/merge` `d1efe8d7`。Navigator Team 已有 TaskList / MemberStatus / TeamInfo。把已 catalog 的 `TeamService.TaskUpdate` / `TaskCancel` 绑进既有 Team Tasks ViewTitle（Update / Cancel）。接通 + 有 hook 才发；空 `sessionId` / `agentId` / `taskId` / `newStatus` / `message` 原样上线（无选中行仍发空 id）；成功后再拉 TaskList。**不**发明 RPC、不重做 catalog、不改 Conversation roster / SessionBar / Snapshots、不做 D22/F3。≠ History / MessageMember / CreateTeam。
- **槽 D / `loop/D`：** 已 reset 到 `loop/merge` `f234d1e5`。RemoteAgent 会话 unary / FileTransfer 流式已在 tip，不重做。RemoteChat / ResumeRemoteSession / CancelRemoteSession 不在本刀。把 `PtyService.PtyStream` 写入 gRPC catalog（`universeagent.pty.v1.PtyService`），node bidi `openPtyStream`（`makeResidentBidiHandleClient` / `makeBidiStreamRequest`；`stream PtyClientMessage` ↔ `stream PtyServerMessage`）。合同可选。空 `engine_session_id` / `client_session_id` / `tab_id` / `pty_session_id` 原样上线；空 `working_directory` / `shell_command` / `initial_command` / `title` / `error_message` / `shell_args` / environment / `data` 原样映射；`columns` / `rows` / `exit_code` 0 原样上线并映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer / Terminal UI。≠ `RemoteChat` / `AgentService.Chat` / `UploadAttachment` / `DownloadAttachment` / `SendShellSessionClientControl`。测：catalog + 转发 / 空 id / dispose 静音。不重做 RemoteChat / FileTransfer / GetRemoteSessionHistory。
- **槽 C / `loop/C`：** 已 reset 到 `loop/merge` `f234d1e5`。CreateRemoteSession / DestroyRemoteSession / GetRemoteSessionStatus / GetRemoteSessionHistory / Reload / DeleteConfig / SaveConfig 已在 tip，不重做。ResumeRemoteSession / CancelRemoteSession 不在本刀。把 `RemoteAgentService.RemoteChat` 写入 gRPC catalog（`universeagent.remoteagent.v1.RemoteAgentService`），node server-stream `openRemoteChatStream`（`makeServerStreamClient`，snake_case `call_id`/`task`/`responses` RemoteResponse + `override_pending`；帧 oneof `result` RemoteChatResult / `progress` RemoteProgressEvent）。合同可选。空 `call_id` / `task` 原样上线；空 `responses`（含空 `type` / `request_id` / permission `decision`/`reason` / `question_answers_json`）原样上线；`override_pending` false 原样上线；空 result / progress 帧（含 0 步数 / 空 pending / 空 messages）原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `CreateRemoteSession` / `DestroyRemoteSession` / `GetRemoteSessionStatus` / `GetRemoteSessionHistory` / `ResumeRemoteSession` / `CancelRemoteSession` / `ListNodes` / `GetNode` / `CheckConnection` / `SetMaintenance` / `ExitMaintenance` / `ResetError` / `ListConfigs` / `GetConfig` / `SaveConfig` / `DeleteConfig` / `Reload` / `DeviceService` / `SessionService` / `AgentService.Chat`。测：catalog + 转发 / 空 id / dispose 静音。不重做 Resume / Cancel / Status / History。
- **槽 D / `loop/D`：** 已 reset 到 `loop/merge` `26869e90`。Reload / DeleteConfig / SaveConfig / ResetError / SetMaintenance / ExitMaintenance / ListNodes / GetNode / CheckConnection / ListConfigs / GetConfig 已在 tip，不重做。CreateRemoteSession / DestroyRemoteSession / GetRemoteSessionStatus 不在本刀。把 `RemoteAgentService.GetRemoteSessionHistory` 写入 gRPC catalog（`universeagent.remoteagent.v1.RemoteAgentService`），node unary `getRemoteSessionHistory`（snake_case `call_id`/`since_version`/`page_size`；响应 `messages[]` RemoteChatMessage / `version` / `has_more`）。合同可选。空 `call_id` 原样上线；`since_version` / `page_size` 0 原样上线；空 `messages`（含空 content / tool call ids）原样映射；`version` 0 原样映射；`has_more` false 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `RemoteChat` / `CreateRemoteSession` / `DestroyRemoteSession` / `GetRemoteSessionStatus` / `ResumeRemoteSession` / `CancelRemoteSession` / `ListNodes` / `GetNode` / `CheckConnection` / `SetMaintenance` / `ExitMaintenance` / `ResetError` / `ListConfigs` / `GetConfig` / `SaveConfig` / `DeleteConfig` / `Reload` / `DeviceService` / `SessionService`。测：catalog + 转发 / 空 id。不重做 CreateRemoteSession / DestroyRemoteSession / GetRemoteSessionStatus / Reload / DeleteConfig。
- **槽 C / `loop/C`：** 已 reset 到 `loop/merge` `26869e90`。Reload / DeleteConfig / SaveConfig / ResetError / SetMaintenance / ExitMaintenance / ListNodes / GetNode / CheckConnection / ListConfigs / GetConfig 已在 tip，不重做。CreateRemoteSession / DestroyRemoteSession 不在本刀。把 `RemoteAgentService.GetRemoteSessionStatus` 写入 gRPC catalog（`universeagent.remoteagent.v1.RemoteAgentService`），node unary `getRemoteSessionStatus`（snake_case `call_id`；响应 `status`/`call_id`/`progress`/`elapsed_ms`/`expires_at`/`pending_permissions[]` RemotePendingPermission / `pending_questions[]` RemotePendingQuestion）。合同可选。空 `call_id` 原样上线；空 `status` / `call_id` / `progress` 原样映射；`elapsed_ms` / `expires_at` 0 原样映射；空 `pending_permissions` / `pending_questions`（含空嵌套串）原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `CreateRemoteSession` / `DestroyRemoteSession` / `GetRemoteSessionHistory` / `ResumeRemoteSession` / `CancelRemoteSession` / `RemoteChat` / `ListNodes` / `GetNode` / `CheckConnection` / `SetMaintenance` / `ExitMaintenance` / `ResetError` / `ListConfigs` / `GetConfig` / `SaveConfig` / `DeleteConfig` / `Reload` / `DeviceService` / `SessionService` / `AgentService.Status`。测：catalog + 转发 / 空 id。不重做 CreateRemoteSession / DestroyRemoteSession / Reload / DeleteConfig。
- **槽 A / `loop/A`：** 已 reset 到 `loop/merge` `cba72dab`。CreateRemoteSession / DestroyRemoteSession 已在 tip，不重做。GetRemoteSessionStatus / GetRemoteSessionHistory 不在本刀。把 `RemoteAgentService.ResumeRemoteSession` 写入 gRPC catalog（`universeagent.remoteagent.v1.RemoteAgentService`），node unary `resumeRemoteSession`（snake_case `call_id`/`node_id`；响应 `success`/`call_id`/`status`/`message`/`expires_at`）。合同可选。空 `call_id` / `node_id` 原样上线；`success` false 原样映射；空 `call_id` / `status` / `message` 原样映射；`expires_at` 0 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `CreateRemoteSession` / `DestroyRemoteSession` / `GetRemoteSessionStatus` / `GetRemoteSessionHistory` / `CancelRemoteSession` / `RemoteChat` / `ListNodes` / `GetNode` / `CheckConnection` / `SaveConfig` / `DeleteConfig` / `Reload` / `DeviceService` / `SessionService` / `AgentService.ResumeSession`。测：catalog + 转发 / 空 id。不重做 CreateRemoteSession / DestroyRemoteSession / GetRemoteSessionStatus / GetRemoteSessionHistory。
- **槽 B / `loop/B`：** 已 reset 到 `loop/merge` `cba72dab`。CreateRemoteSession / DestroyRemoteSession / Reload / DeleteConfig / SaveConfig / ResetError / ExitMaintenance / SetMaintenance / GetConfig / ListConfigs / ListNodes / GetNode / CheckConnection 已在 tip，不重做。ResumeRemoteSession 不在本刀。把 `RemoteAgentService.CancelRemoteSession` 写入 gRPC catalog（`universeagent.remoteagent.v1.RemoteAgentService`），node unary `cancelRemoteSession`（snake_case `call_id`/`reason`；响应 `success`/`call_id`/`status`/`message`）。合同可选。空 `call_id` 原样上线；空 `reason` 原样上线；`success` false 原样映射；空 `call_id` / `status` / `message` 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ResumeRemoteSession` / `RemoteChat` / `CreateRemoteSession` / `DestroyRemoteSession` / `GetRemoteSessionStatus` / `GetRemoteSessionHistory` / `SaveConfig` / `Reload` / `DeviceService` / `SessionService` / `AgentService.Cancel`。测：catalog + 转发 / 空 id。不重做 CreateRemoteSession / DestroyRemoteSession / ResumeRemoteSession。
- **槽 B / `loop/B`：** 已 reset 到 `loop/merge` `b9ccdc9a`。SaveConfig / ResetError / ExitMaintenance / SetMaintenance / GetConfig / ListConfigs / ListNodes / GetNode / CheckConnection 已在 tip，不重做。CreateRemoteSession 不在本刀。把 `RemoteAgentService.DestroyRemoteSession` 写入 gRPC catalog（`universeagent.remoteagent.v1.RemoteAgentService`），node unary `destroyRemoteSession`（snake_case `call_id`；响应 `success`/`message`）。合同可选。空 `call_id` 原样上线；`success` false 原样映射；空 `message` 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `CreateRemoteSession` / `GetRemoteSessionStatus` / `GetRemoteSessionHistory` / `ResumeRemoteSession` / `CancelRemoteSession` / `RemoteChat` / `SaveConfig` / `ResetError` / `DeleteConfig` / `Reload` / `DeviceService` / `SessionService`。测：catalog + 转发 / 空 id。不重做 SaveConfig / ResetError / ListNodes。
- **槽 B / `loop/B`：** 已 reset 到 `loop/merge` `10f59d8c`。GetConfig / ListConfigs / ListNodes / GetNode / CheckConnection 已在 tip，不重做。ResetError 不在本刀。把 `RemoteAgentService.SaveConfig` 写入 gRPC catalog（`universeagent.remoteagent.v1.RemoteAgentService`），node unary `saveRemoteAgentConfig`（snake_case `config` RemoteAgentConfig + `skip_connection_test`/`async_test`；响应 `success`/`message`/`connection_test` ConnectionReport + `async_test_id`）。合同可选。空 `id` / `name` / `description` / `tags` / `session_lifecycle` 原样上线；`skip_connection_test` / `async_test` false 原样上线；`success` false 原样映射；空 `message` / `async_test_id` 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListNodes` / `GetNode` / `CheckConnection` / `ListConfigs` / `GetConfig` / `ResetError` / `DeleteConfig` / `ConfigService.Set` / `DeviceService`。测：catalog + 转发 / 空 id。不重做 GetConfig / ListConfigs / ListNodes。
- **槽 A / `loop/A`：** 已 reset 到 `loop/merge` `10f59d8c`。ListNodes / GetNode / CheckConnection / ListConfigs / GetConfig 已在 tip，不重做。SetMaintenance / ExitMaintenance 不在本刀。把 `RemoteAgentService.ResetError` 写入 gRPC catalog（`universeagent.remoteagent.v1.RemoteAgentService`），node unary `resetError`（snake_case `node_id`；响应 `success`）。合同可选。空 `node_id` 原样上线；`success` false 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListNodes` / `GetNode` / `CheckConnection` / `SetMaintenance` / `ExitMaintenance` / `ListConfigs` / `GetConfig` / `DeviceService`。测：catalog + 转发 / 空 id。不重做 ListNodes / GetNode / CheckConnection / ListConfigs / GetConfig / SetMaintenance / ExitMaintenance。
- **槽 C / `loop/C`：** 已 reset 到 `loop/merge` `5f434404`。SetMaintenance / ExitMaintenance / ListNodes / GetNode / CheckConnection / ListConfigs / GetConfig 已在 tip，不重做。ResetError / SaveConfig 不在本刀。把 `RemoteAgentService.DeleteConfig` 写入 gRPC catalog（`universeagent.remoteagent.v1.RemoteAgentService`），node unary `deleteRemoteAgentConfig`（snake_case `node_id`；响应 `success`）。合同可选。空 `node_id` 原样上线；`success` false 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `SetMaintenance` / `ExitMaintenance` / `ResetError` / `SaveConfig` / `ListNodes` / `GetNode` / `CheckConnection` / `ListConfigs` / `GetConfig` / `Reload` / `DeviceService`。测：catalog + 转发 / 空 id。不重做 SetMaintenance / ExitMaintenance / ResetError / SaveConfig。
- **槽 D / `loop/D`：** 已 reset 到 `loop/merge` `5f434404`。ListNodes / GetNode / CheckConnection / ListConfigs / GetConfig / SetMaintenance / ExitMaintenance 已在 tip，不重做。DeleteConfig 不在本刀。把 `RemoteAgentService.Reload` 写入 gRPC catalog（`universeagent.remoteagent.v1.RemoteAgentService`），node unary `reloadRemoteAgents`（空 request `{}`；响应 `success`/`added[]`/`removed[]`/`changed[]`/`errors[]`/`duration_ms`）。合同可选。空 `added` / `removed` / `changed` / `errors`（含空串）原样映射；`success` false 原样映射；`duration_ms` 0 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListNodes` / `GetNode` / `CheckConnection` / `SetMaintenance` / `ExitMaintenance` / `ResetError` / `ListConfigs` / `GetConfig` / `SaveConfig` / `DeleteConfig` / `DeviceService` / `Plugin.Reload`。测：catalog + 转发 / 空 id。不重做 ListNodes / GetNode / CheckConnection / ListConfigs / GetConfig / SetMaintenance / ExitMaintenance。
- **槽 B / `loop/B`：** 已 reset 到 `loop/merge` `4fe736f3`。ListNodes 已在 tip，不重做。ListConfigs / GetNode / CheckConnection 不在本刀。把 `RemoteAgentService.GetConfig` 写入 gRPC catalog（`universeagent.remoteagent.v1.RemoteAgentService`），node unary `getRemoteAgentConfig`（snake_case `node_id`；响应 RemoteAgentConfig）。合同可选。空 `node_id` 原样上线；空 `id` / `name` / `description` / `tags` / `session_lifecycle` 原样映射；`enabled` false 原样映射；`max_concurrent_sessions` 0 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListNodes` / `GetNode` / `CheckConnection` / `ListConfigs` / `ConfigService.Get` / `DeviceService`。测：catalog + 转发 / 空 id。不重做 ListNodes。
- **槽 A / `loop/A`：** 已 reset 到 `loop/merge` `4fe736f3`。ListNodes 已在 tip，不重做。把 `RemoteAgentService.ListConfigs` 写入 gRPC catalog（`universeagent.remoteagent.v1.RemoteAgentService`），node unary `listConfigs`（空 request `{}`；响应 `configs[]` RemoteAgentConfig）。合同可选。空 `id` / `name` / `description` / `tags` / `session_lifecycle` 原样映射；`enabled` / `tls` / `use_watch` false 原样映射；`port` / `max_concurrent_sessions` / budget ints / health-check ints / `degraded_error_rate_threshold` 0 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListNodes` / `GetNode` / `CheckConnection` / `GetConfig` / DeviceService。测：catalog + 转发 / 空 id。不重做 ListNodes / GetNode / CheckConnection。
- **槽 C / `loop/C`：** 已 reset 到 `loop/merge` `8fc96a1e`。ListNodes / GetNode / CheckConnection 已在 tip，不重做。ListConfigs / GetConfig 不在本刀。把 `RemoteAgentService.SetMaintenance` 写入 gRPC catalog（`universeagent.remoteagent.v1.RemoteAgentService`），node unary `setMaintenance`（snake_case `node_id`；响应 `success`）。合同可选。空 `node_id` 原样上线；`success` false 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListNodes` / `GetNode` / `CheckConnection` / `ExitMaintenance` / `ResetError` / `ListConfigs` / `GetConfig` / `DeviceService`。测：catalog + 转发 / 空 id。不重做 ListNodes / GetNode / CheckConnection / ListConfigs / GetConfig。
- **槽 D / `loop/D`：** 已 reset 到 `loop/merge` `8fc96a1e`。ListNodes / GetNode / CheckConnection 已在 tip，不重做。SetMaintenance 不在本刀。把 `RemoteAgentService.ExitMaintenance` 写入 gRPC catalog（`universeagent.remoteagent.v1.RemoteAgentService`），node unary `exitMaintenance`（snake_case `node_id`；响应 `success`）。合同可选。空 `node_id` 原样上线；`success` false 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `SetMaintenance` / `ResetError` / `ListNodes` / `GetNode` / `CheckConnection` / `ListConfigs` / `GetConfig` / `DeviceService`。测：catalog + 转发 / 空 id。不重做 ListNodes / GetNode / CheckConnection / Device / Trigger / Clipboard。
- **槽 D / `loop/D`：** 已 reset 到 `loop/merge` `b3c772aa`。Device / Trigger / Clipboard / ContextVariable 已在 tip，不重做。GetNode 未占 CheckConnection。把 `RemoteAgentService.CheckConnection` 写入 gRPC catalog（`universeagent.remoteagent.v1.RemoteAgentService`），node unary `checkConnection`（snake_case `node_id`/`session_params` SessionParams；响应 ConnectionReport）。合同可选。空 `node_id` 原样上线；空 `preferred_model` / 空 `required_tools`（含空串）/ 空 `mode` / 空 `system_prompt_suffix` 原样上线；`max_tokens` / `max_turns` / `max_execution_time_ms` 0 原样上线；`reachable` / `authenticated` / `can_create_session` false 原样映射；`latency_ms` 0 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `GetNode` / `ListNodes` / `ListConfigs` / `GetConfig` / `DeviceService`。测：catalog + 转发 / 空 id。不重做 Device / Trigger / Clipboard / ContextVariable。
- **槽 C / `loop/C`：** 已 reset 到 `loop/merge` `dcd4ff32`。ListNodes 不在 tip、不重做。把 `RemoteAgentService.GetNode` 写入 gRPC catalog（`universeagent.remoteagent.v1.RemoteAgentService`），node unary `getNode`（snake_case `node_id`；响应 RemoteAgentInfo）。合同可选。空 `node_id` 原样上线；空 `id` / `name` / `description` / `status` / `endpoint` / `tags` 原样映射；`last_heartbeat_at` 0 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListNodes` / `CheckConnection` / `ListConfigs` / `GetConfig` / `DeviceService`。测：catalog + 转发 / 空 id。不重做 ListNodes / Device / Trigger / Clipboard。
- **槽 B / `loop/B`：** 已 reset 到 `loop/merge` `db483c0a`。Device / Trigger / Clipboard / ContextVariable 已在 tip，不重做。Device 不在本刀。把 `RemoteAgentService.ListNodes` 写入 gRPC catalog（`universeagent.remoteagent.v1.RemoteAgentService`），node unary `listNodes`（snake_case `filter_status`/`filter_tags`；响应 `nodes[]` RemoteAgentInfo）。合同可选。空 `filter_status` / 空 `filter_tags`（含空串）原样上线；空 `id` / `name` / `description` / `status` / `endpoint` / `tags` 原样映射；`last_heartbeat_at` / `total` / `online_count` 0 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `GetNode` / `CheckConnection` / `ListConfigs` / `GetConfig` / `DeviceService`。测：catalog + 转发 / 空 id。不重做 Device / Trigger / Clipboard。
- **槽 A / `loop/A`：** 已 reset 到 `loop/merge` `db483c0a`。ListDevices / PairApprove / PairReject / Revoke / RotateToken 已在 tip，不重做。把 `DeviceService.ListPending` 写入 gRPC catalog（`universeagent.device.v1.DeviceService`），node unary `listPending`（空 request `{}`；响应 `pending[]`：`pairing_code`/`device_id`/`display_name`/`platform`/`requested_at`/`expires_in_seconds`）。合同可选。空 `pairing_code` / `device_id` / `display_name` / `platform` 原样映射；`requested_at` 0 原样映射；`expires_in_seconds` 0 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListDevices` / `PairApprove` / `PairReject` / `Revoke` / `RotateToken`。测：catalog + 转发 / 空 id。不重做 ListDevices / PairApprove / PairReject / Revoke / RotateToken。- **槽 D / `loop/D`：** 已 reset 到 `loop/merge` `4c217833`。ListTriggers / UpsertTrigger / DeleteTrigger / Clipboard 已在 tip，不重做。Clipboard 不在本刀。把 `TriggerService.FireTrigger` 写入 gRPC catalog（`universeagent.trigger.v1.TriggerService`），node unary `fireTrigger`（snake_case `scope`/`scope_id`/`trigger_id`；响应 `status`/`event_id`/`reason`）。合同可选。空 `scope` / 空 `scope_id` / 空 `trigger_id` 原样上线；空 `status` / `event_id` / `reason` 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListTriggers` / `UpsertTrigger` / `DeleteTrigger` / `SetTriggerEnabled` / `AgentService.FireTriggerWebhook` / `DeviceService` / Clipboard。测：catalog + 转发 / 空 id。不重做 ListTriggers / UpsertTrigger / DeleteTrigger / Clipboard。
- **槽 C / `loop/C`：** 已 reset 到 `loop/merge` `4c217833`。Write / Read / List 已在 tip，不重做。把 `ClipboardService.Clear` 写入 gRPC catalog（`universeagent.clipboard.v1.ClipboardService`），node unary `clearClipboard`（snake_case `session_id`；响应 `removed_count`）。合同可选。空 `sessionId` 原样上线；`removed_count` 0 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `Write` / `Read` / `List` / DeviceService / TriggerService。测：catalog + 转发 / 空 id。不重做 Write / Read / List。
- **槽 B / `loop/B`：** 已 reset 到 `loop/merge` `2a5fab8e`。ListTriggers / UpsertTrigger / DeleteTrigger / Device 已在 tip，不重做。Device 不在本刀。把 `TriggerService.SetTriggerEnabled` 写入 gRPC catalog（`universeagent.trigger.v1.TriggerService`），node unary `setTriggerEnabled`（snake_case `scope`/`scope_id`/`trigger_id`/`enabled`；响应 `trigger` TriggerDto）。合同可选。空 `scope` / 空 `scope_id` / 空 `trigger_id` 原样上线；`enabled` false 原样上线；空 `trigger_id` / `name` / `type` / `prompt_template` / `pause_reason` / `cron_expression` 原样映射；`enabled` false 原样映射；`interval_ms` / `run_at_epoch_ms` 0 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListTriggers` / `UpsertTrigger` / `DeleteTrigger` / `FireTrigger` / `AgentService.FireTriggerWebhook` / `DeviceService`。测：catalog + 转发 / 空 id。不重做 ListTriggers / UpsertTrigger / DeleteTrigger / Device。- **槽 A / `loop/A`：** 已 reset 到 `loop/merge` `2a5fab8e`。ListDevices / PairApprove / PairReject 已在 tip，不重做。把 `DeviceService.RotateToken` 写入 gRPC catalog（`universeagent.device.v1.DeviceService`），node unary `rotateToken`（snake_case `device_id`；响应 `success`/`message`）。合同可选。空 `device_id` 原样上线；空 `message` 原样映射；`success` false 原样映射。Reserved `new_token`（ADR-261）不映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListDevices` / `PairApprove` / `PairReject` / `Revoke` / `ListPending`。测：catalog + 转发 / 空 id。不重做 ListDevices / PairApprove / PairReject / Revoke。
- **槽 D / `loop/D`：** 已 reset 到 `loop/merge` `5a222ab8`。ListDevices / PairApprove 已在 tip，不重做。Clipboard 不在本刀。把 `DeviceService.Revoke` 写入 gRPC catalog（`universeagent.device.v1.DeviceService`），node unary `revoke`（snake_case `device_id`；响应 `success`/`message`）。合同可选。空 `device_id` 原样上线；空 `message` 原样映射；`success` false 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListDevices` / `PairApprove` / `PairReject` / `RotateToken` / `ListPending` / Hub `revokeDevice`。测：catalog + 转发 / 空 id。不重做 ListDevices / PairApprove / PairReject / Clipboard。
- **槽 C / `loop/C`：** 已 reset 到 `loop/merge` `5a222ab8`。Write / Read 已在 tip，不重做。把 `ClipboardService.List` 写入 gRPC catalog（`universeagent.clipboard.v1.ClipboardService`），node unary `listClipboard`（snake_case `session_id`；响应 `entries[]` ClipboardEntrySummary：`clip_id`/`label`/`type`/`created_by`/`created_at`）。合同可选。空 `sessionId` 原样上线；空 `clip_id` / `label` / `created_by` 原样映射；`created_at` 0 原样映射。`type` typed `ClipboardEntryType` wire。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `Write` / `Read` / `Clear` / DeviceService / TriggerService。测：catalog + 转发 / 空 id。不重做 Write / Read。
- **槽 B / `loop/B`：** 已 reset 到 `loop/merge` `6c3eab2a`。ListTriggers / UpsertTrigger / Device 已在 tip，不重做。Device 不在本刀。把 `TriggerService.DeleteTrigger` 写入 gRPC catalog（`universeagent.trigger.v1.TriggerService`），node unary `deleteTrigger`（snake_case `scope`/`scope_id`/`trigger_id`；响应空 `DeleteTriggerResponse` `{}`）。合同可选。空 `scope` / 空 `scope_id` / 空 `trigger_id` 原样上线。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListTriggers` / `UpsertTrigger` / `SetTriggerEnabled` / `FireTrigger` / `AgentService.FireTriggerWebhook` / `DeviceService`。测：catalog + 转发 / 空 id。不重做 ListTriggers / UpsertTrigger / Device。
- **槽 A / `loop/A`：** 已 reset 到 `loop/merge` `6c3eab2a`。ListDevices / PairApprove 已在 tip，不重做。把 `DeviceService.PairReject` 写入 gRPC catalog（`universeagent.device.v1.DeviceService`），node unary `pairReject`（snake_case `pairing_code`；响应 `success`/`message`）。合同可选。空 `pairing_code` 原样上线；空 `message` 原样映射；`success` false 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListDevices` / `PairApprove` / `Revoke` / `RotateToken` / `ListPending`。测：catalog + 转发 / 空 id。不重做 ListDevices / PairApprove。
- **槽 A / `loop/A`：** 已 reset 到 `loop/merge` `4ad29d8b`。ListDevices 已在 tip，不重做。把 `DeviceService.PairApprove` 写入 gRPC catalog（`universeagent.device.v1.DeviceService`），node unary `pairApprove`（snake_case `pairing_code`/`display_name`/`role`；响应 `success`/`device_id`/`message`）。合同可选。空 `pairing_code` / `display_name` / `role` 原样上线；空 `device_id` / `message` 原样映射；`success` false 原样映射。Reserved `device_token`（ADR-261）不映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListDevices` / `PairReject` / `Revoke` / `RotateToken` / `ListPending`。测：catalog + 转发 / 空 id。不重做 ListDevices。
- **槽 B / `loop/B`：** 已 reset 到 `loop/merge` `4ad29d8b`。ListTriggers / Device 已在 tip，不重做。Device 不在本刀。把 `TriggerService.UpsertTrigger` 写入 gRPC catalog（`universeagent.trigger.v1.TriggerService`），node unary `upsertTrigger`（snake_case `scope`/`scope_id`/`trigger` TriggerDto；响应 `trigger`）。合同可选。空 `scope` / 空 `scope_id` 原样上线；空 `trigger_id` / `name` / `type` / `prompt_template` / `pause_reason` / `cron_expression` 原样上线并映射；`enabled` false 原样上线并映射；`interval_ms` / `run_at_epoch_ms` 0 原样上线并映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListTriggers` / `DeleteTrigger` / `SetTriggerEnabled` / `FireTrigger` / `AgentService.FireTriggerWebhook` / `DeviceService`。测：catalog + 转发 / 空 id。不重做 ListTriggers / Device。
- **槽 C / `loop/C`：** 已 reset 到 `loop/merge` `45c696ef`。Write 已在 tip，不重做。把 `ClipboardService.Read` 写入 gRPC catalog（`universeagent.clipboard.v1.ClipboardService`），node unary `readClipboard`（snake_case `session_id`/`clip_id`；响应 `entry` ClipboardEntry：`clip_id`/`label`/`type`/`content`/`created_by`/`created_at`）。合同可选。空 `sessionId` / 空 `clipId` 原样上线；空 `clip_id` / `label` / `content` / `created_by` 原样映射；`created_at` 0 原样映射。`type` typed `ClipboardEntryType` wire。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `Write` / `List` / `Clear` / DeviceService / TriggerService。测：catalog + 转发 / 空 id。不重做 Write。
- **槽 D / `loop/D`：** 已 reset 到 `loop/merge` `45c696ef`。Clipboard 不在本刀。List 已在 tip，不重做。把 `ContextVariableService.Read` 写入 gRPC catalog（`universeagent.contextvariable.v1.ContextVariableService`），node unary `readContextVariable`（snake_case `session_id`/`name`/`agent_id`；响应 `entry`：`name`/`content`/`scope`/`updated_by`/`updated_at`）。合同可选。空 `sessionId` / 空 `name` / 空 `agentId` 原样上线；空 `name` / `content` / `updated_by` 原样映射；`updated_at` 0 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `List` / Clipboard。测：catalog + 转发 / 空 id。不重做 List / Clipboard / Memory / FileTransfer。
- **槽 A / `loop/A`：** 已 reset 到 `loop/merge` `9ddd037f`。HealthCheck / Doctor / FileTransfer / Shutdown / History 不在本刀。把 `DeviceService.ListDevices` 写入 gRPC catalog（`universeagent.device.v1.DeviceService`），node unary `listDevices`（空 request `{}`；响应 `devices[]`：`device_id`/`display_name`/`role`/`platform`/`paired_at`/`last_seen_at`/`active`）。合同可选。空 `device_id` / `display_name` / `role` / `platform` 原样映射；`paired_at` / `last_seen_at` 0 原样映射；`active` false 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `PairApprove` / `PairReject` / `Revoke` / `RotateToken` / `ListPending`。测：catalog + 转发 / 空 id。不重做 HealthCheck / Doctor / Shutdown / History / FileTransfer。
- **槽 B / `loop/B`：** 已 reset 到 `loop/merge` `9ddd037f`。HealthCheck / Doctor 已在 tip，不重做。Device 不在本刀。把 `TriggerService.ListTriggers` 写入 gRPC catalog（`universeagent.trigger.v1.TriggerService`），node unary `listTriggers`（snake_case `scope`/`scope_id`/`type_filter`；响应 `triggers[]` TriggerDto）。合同可选。空 `scope` / 空 `scope_id` / 空 `type_filter` 原样上线；空 `trigger_id` / `name` / `type` / `prompt_template` / `pause_reason` / `cron_expression` 原样映射；`enabled` false 原样映射；`interval_ms` / `run_at_epoch_ms` 0 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `UpsertTrigger` / `DeleteTrigger` / `SetTriggerEnabled` / `FireTrigger` / `AgentService.FireTriggerWebhook` / `DeviceService`。测：catalog + 转发 / 空 id。不重做 Device / Doctor / HealthCheck。
- **槽 C / `loop/C`：** 已 reset 到 `loop/merge` `71d74b00`。Shutdown / History / HealthCheck / Doctor / FileTransfer 不在本刀。Device / Trigger 不在本刀。把 `ClipboardService.Write` 写入 gRPC catalog（`universeagent.clipboard.v1.ClipboardService`），node unary `writeClipboard`（snake_case `session_id`/`agent_id`/`label`/`type`/`content`/`file_path`/`url`；响应 `clip_id`）。合同可选。空 `sessionId` / 空 `agentId` / 空 `label` / 空 `content` / 空 `filePath` / 空 `url` 原样上线；空 `clip_id` 原样映射。`type` typed `ClipboardEntryType` wire。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `Read` / `List` / `Clear` / DeviceService / TriggerService。测：catalog + 转发 / 空 id。不重做 Shutdown / History / Device / Trigger。
- **槽 D / `loop/D`：** 已 reset 到 `loop/merge` `71d74b00`。Clipboard 不在本刀。Memory / FileTransfer / System 族已在 tip，不重做。把 `ContextVariableService.List` 写入 gRPC catalog（`universeagent.contextvariable.v1.ContextVariableService`），node unary `listContextVariable`（snake_case `session_id`/`agent_id`；响应 `current[]` / `inherited[]`：`name`/`scope`/`updated_by`/`updated_at`/`content_preview`）。合同可选。空 `sessionId` / 空 `agentId` 原样上线；空 `name` / `updated_by` / `content_preview` 原样映射；`updated_at` 0 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `Read` / Clipboard。测：catalog + 转发 / 空 id。不重做 Clipboard / Memory / FileTransfer。
- **槽 C / `loop/C`：** 已 reset 到 `loop/merge` `ee5b9da1`。HealthCheck / Doctor / FileTransfer 不在本刀。把 `SystemService.Shutdown` 写入 gRPC catalog（`universeagent.system.v1.SystemService`），node unary `shutdown`（snake_case `force`/`grace_period_ms`；响应 `accepted`/`message`）。合同可选。`force` false / `grace_period_ms` 0 原样上线；空 `message` 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `Connect` / `GetAuthNonce` / `HealthCheck` / `Doctor`。测：catalog + 转发 / 空字段。不重做 HealthCheck / Doctor / FileTransfer。
- **槽 D / `loop/D`：** 已 reset 到 `loop/merge` `ee5b9da1`。Memory.Save / Search / SearchDeep / Read / List / Delete / Reflect / Rebuild / Revert 与 FileTransfer 族已在 tip，不重做。把 `MemoryService.History` 写入 gRPC catalog（`universeagent.memory.v1.MemoryService`），node unary `historyMemory`（snake_case `scope`/`category`/`filename`/`limit`；响应 `changes[]`：`version`/`change_type`/`summary`/`timestamp`/`author`）。合同可选。空 `scope` / 空 `category` / 空 `filename` 原样上线；`limit` 0 原样上线；空 `change_type` / `summary` / `author` 原样映射；`version` / `timestamp` 0 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer / Memory pane。≠ `Save` / `Search` / `SearchDeep` / `Read` / `List` / `Delete` / `Reflect` / `Rebuild` / `Revert`。测：catalog + 转发 / 空 id。不重做 FileTransfer / Revert / Rebuild。
- **槽 D / `loop/D`：** 已 reset 到 `loop/merge` `778d9587`。Memory 族已在 tip，不重做。GetUploadProgress / Revert / Rebuild / UploadAttachment 不在本刀。把 `FileTransferService.DownloadAttachment` 写入 gRPC catalog（`universeagent.filetransfer.v1.FileTransferService`），node server-stream `openDownloadAttachmentStream`（`makeServerStreamClient`，snake_case `file_path`/`offset`/`max_bytes`/`session_id`/`artifact_id`；帧 `offset`/`data`/`total_size`/`is_last`/`checksum_sha256`）。合同可选。空 `filePath` / 空 `sessionId` / 空 `artifactId` 原样上线；`offset` / `maxBytes` 0 原样上线；空 `data` / 空 `checksumSha256` 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `UploadAttachment` / `GetUploadProgress`。测：catalog + 转发 / 空 id / dispose 静音。不重做 GetUploadProgress / Revert / Rebuild / UploadAttachment。
- **槽 C / `loop/C`：** 已 reset 到 committed `loop/merge` `778d9587`（不用 merge 槽脏 WIP）。GetUploadProgress（B）/ Revert（A）/ Rebuild 不在本刀。把 `FileTransferService.UploadAttachment` 写入 gRPC catalog（`universeagent.filetransfer.v1.FileTransferService`），node client-stream `openUploadAttachmentStream`（`stream UploadChunk` → `UploadResponse`；snake_case `transfer_id`/`filename`/`mime_type`/`checksum_sha256`/`session_id`/`queue_item_id` + `chunk` bytes + `offset`）。合同可选。空 `transferId` / `filename` / `sessionId` / `queueItemId` 原样上线；空 `chunk` 原样上线。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `DownloadAttachment` / `GetUploadProgress`。测：catalog + 转发 / 空 id。不重做 Memory / Watch / SubscribeToolDetail。
- **槽 A / `loop/A`：** 已 reset 到 `loop/merge` `66ac150c`。Hub 无遗漏。Session 族已在 tip，不重做。Upload / Download / GetUploadProgress / Revert / Rebuild 不在本刀。把 `SystemService.HealthCheck` 写入 gRPC catalog（`universeagent.system.v1.SystemService`），node unary `healthCheck`（空 request `{}`；响应 `status`/`version`/`active_sessions`/`uptime_ms`）。合同可选。空 `status` / 空 `version` 原样映射；`active_sessions` / `uptime_ms` 0 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `Connect` / `GetAuthNonce` / `Doctor` / `Shutdown`。测：catalog + 转发 / 空字段。不重做 FileTransfer / Memory。- **槽 A / `loop/A`：** 已在 `loop/merge` tip `d2488881`（不 reset，以免抹掉 WIP）。Save / Search / SearchDeep / Read / List / Delete 已在 tip，不重做。Reflect / Rebuild 不在本刀。把 `MemoryService.Revert` 写入 gRPC catalog（`universeagent.memory.v1.MemoryService`），node unary `revertMemory`（snake_case `scope`/`category`/`filename`/`target_version`；响应 `success`/`message`/`reverted_to_version`）。合同可选。空 `scope` / 空 `category` / 空 `filename` 原样上线；`target_version` 0 原样上线。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer / Memory pane。≠ `Save` / `Search` / `SearchDeep` / `Read` / `List` / `Delete` / `Reflect` / `Rebuild` / `History`。测：catalog + 转发 / 空 id。不重做 Delete / SearchDeep / Reflect / Rebuild。
- **槽 B / `loop/B`：** 已 reset 到 `loop/merge` `66ac150c`。GetUploadProgress / Memory.Revert / Memory.Rebuild 已在 tip，不重做。FileTransfer 流式与 Memory.Revert / Rebuild 不在本刀。A 取下一缺 `HealthCheck`。把 `SystemService.Doctor` 写入 gRPC catalog（`universeagent.system.v1.SystemService`），node unary `doctor`（空 request `{}`；响应 `checks[]`：`name`/`passed`/`message`/`fix_hint` + `all_passed`）。合同可选。空 `name` / `message` / `fix_hint` 原样映射；`passed` / `all_passed` false 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `Connect` / `GetAuthNonce` / `HealthCheck` / `Shutdown`。测：catalog + 转发 / 空 id。不重做 Memory / FileTransfer / HealthCheck。
- **槽 A / `loop/A`：** 已在 `loop/merge` tip `d2488881`（不 reset，以免抹掉 WIP）。Save / Search / SearchDeep / Read / List / Delete 已在 tip，不重做。Reflect / Rebuild 不在本刀。把 `MemoryService.Revert` 写入 gRPC catalog（`universeagent.memory.v1.MemoryService`），node unary `revertMemory`（snake_case `scope`/`category`/`filename`/`target_version`；响应 `success`/`message`/`reverted_to_version`）。合同可选。空 `scope` / 空 `category` / 空 `filename` 原样上线；`target_version` 0 原样上线。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer / Memory pane。≠ `Save` / `Search` / `SearchDeep` / `Read` / `List` / `Delete` / `Reflect` / `Rebuild` / `History`。测：catalog + 转发 / 空 id。不重做 Delete / SearchDeep / Reflect / Rebuild。
- **槽 B / `loop/B`：** 已 reset 到 `loop/merge` `d2488881`。Memory 族已在 tip，不重做。把 `FileTransferService.GetUploadProgress` 写入 gRPC catalog（`universeagent.filetransfer.v1.FileTransferService`），node unary `getUploadProgress`（snake_case `transfer_id`/`session_id`；响应 `exists`/`bytes_received`/`partial_path`）。合同可选。空 `transferId` / 空 `sessionId` 原样上线；`bytes_received` 0 / 空 `partial_path` 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `UploadAttachment` / `DownloadAttachment`。测：catalog + 转发 / 空 id。不重做 Memory。
- **槽 B / `loop/B`：** 已 reset 到 `loop/merge` `a9d19628`。Git 族、TokenUsage、Memory.Save / Memory.Search 已在 tip，不重做。Memory.Delete 不在本刀。把 `MemoryService.SearchDeep` 写入 gRPC catalog（`universeagent.memory.v1.MemoryService`），node unary `searchDeepMemory`（snake_case `scope`/`query`/`keywords`/`categories`/`limit`/`include_content`；响应 `results[]`：`category`/`filename`/`title`/`score`/`snippet`/`forgot`/`scope`；`searched_categories`）。合同可选。空 `scope` / 空 `query` / 空 `keywords` / 空 `categories` 原样上线；`limit` 0 原样上线；`include_content` false 原样上线；空 `category` / `filename` / `title` / `snippet` / `scope` 原样映射；空 `searched_categories` 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `Save` / `Search` / `Read` / `List` / `Delete` / `Reflect` / `Rebuild` / `Revert` / `History`。测：catalog + 转发 / 空 id。不重做 Save / Search / TokenUsage / Git。
- **槽 A / `loop/A`：** 已 reset 到 `loop/merge` `a9d19628`。Save / Search 已在 tip，不重做。Read / List 不在本刀。把 `MemoryService.Delete` 写入 gRPC catalog（`universeagent.memory.v1.MemoryService`），node unary `deleteMemory`（snake_case `scope`/`category`/`filename`；响应 `success`/`message`）。合同可选。空 `scope` / 空 `category` / 空 `filename` 原样上线。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer / Memory pane。≠ `Save` / `Search` / `SearchDeep` / `Read` / `List` / `Reflect` / `Rebuild` / `Revert` / `History`。测：catalog + 转发 / 空 id。不重做 Save / Search / Read / List。
- **槽 C / `loop/C`：** 已 reset 到 `loop/merge` `0f109747`。Save / Search / Read / List 已在 tip，不重做。Delete / SearchDeep 不在本刀。把 `MemoryService.Reflect` 写入 gRPC catalog（`universeagent.memory.v1.MemoryService`），node unary `reflectMemory`（snake_case `scope`/`categories`；响应 `diagnoses[]`：`type`/`category`/`filename`/`description`/`suggestion` + `summary`）。合同可选。空 `scope` / 空 `categories` 原样上线；空 `type` / `category` / `filename` / `description` / `suggestion` / `summary` 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer / Memory pane。≠ `Save` / `Search` / `SearchDeep` / `Read` / `List` / `Delete` / `Rebuild` / `Revert` / `History`。测：catalog + 转发 / 空 id。不重做 Delete / SearchDeep / Read / List。
- **槽 D / `loop/D`：** 已 reset 到 `loop/merge` `0f109747`。Save / Search / Read / List 已在 tip，不重做。Memory.Reflect 不在本刀。把 `MemoryService.Rebuild` 写入 gRPC catalog（`universeagent.memory.v1.MemoryService`），node server-stream `openRebuildMemoryStream`（`makeServerStreamClient`，snake_case `scope`/`dry_run`；帧 `phase`/`message`/`progress`/`files_processed`/`files_total`）。合同可选。空 `scope` 原样上线；`dry_run` false 原样上线。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer / Memory pane。≠ `Save` / `Search` / `SearchDeep` / `Read` / `List` / `Delete` / `Reflect` / `Revert` / `History`。测：catalog + 转发 / 空 id / dispose 静音。不重做 Save / Search / Read / List。
- **槽 A / `loop/A`：** 已 reset 到 `loop/merge` `41a59b3b`。Git 族与 FileService 已在 tip，不重做。TokenUsage 不在本刀。把 `MemoryService.Save` 写入 gRPC catalog（`universeagent.memory.v1.MemoryService`），node unary `saveMemory`（snake_case `scope`/`content`/`category`；响应 `success`/`message`/`file_path`）。合同可选。空 `scope` / 空 `content` / 空 `category` 原样上线。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer / Memory pane。≠ `Search` / `SearchDeep` / `Read` / `List` / `Delete` / `Reflect` / `Rebuild` / `Revert` / `History`。测：catalog + 转发 / 空 id。不重做 TokenUsage / Git。
- **槽 B / `loop/B`：** 已 reset 到 `loop/merge` `41a59b3b`。Git 族与 File 族已在 tip，不重做。Memory.Save 不在本刀。把 `MemoryService.Search` 写入 gRPC catalog（`universeagent.memory.v1.MemoryService`），node unary `searchMemory`（snake_case `scope`/`query`/`keywords`/`limit`；响应 `results[]`：`category`/`filename`/`title`/`score`/`snippet`/`forgot`/`scope`）。合同可选。空 `scope` / 空 `query` / 空 `keywords` 原样上线；`limit` 0 原样上线；空 `category` / `filename` / `title` / `snippet` / `scope` 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `Save` / `SearchDeep` / `Read` / `List` / `Delete` / `Reflect` / `Rebuild` / `Revert` / `History`。测：catalog + 转发 / 空 id。不重做 Git / File / TokenUsage。
- **槽 C / `loop/C`：** 已 reset 到 `loop/merge` `58a6c30d`。Git 族与 TokenUsage 已在 tip，不重做。Save / Search 不在本刀。把 `MemoryService.Read` 写入 gRPC catalog（`universeagent.memory.v1.MemoryService`），node unary `readMemory`（snake_case `scope`/`category`/`filename`/`section`/`mode`/`forgot`；响应 `content`/`metadata` MemoryFileMetadata：`category`/`filename`/`title`/`tags`/`created_at`/`updated_at`/`version`）。合同可选。空 `scope` / 空 `category` / 空 `filename` / 空 `section` / 空 `mode` 原样上线；`forgot` false 原样上线。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer / Memory pane。≠ `Save` / `Search` / `SearchDeep` / `List` / `Delete` / `Reflect` / `Rebuild` / `Revert` / `History`。测：catalog + 转发 / 空 id。不重做 Save / Search / TokenUsage。
- **槽 D / `loop/D`：** 已 reset 到 `loop/merge` `58a6c30d`。TokenUsage / Git 族已在 tip，不重做。Memory.Read 不在本刀。把 `MemoryService.List` 写入 gRPC catalog（`universeagent.memory.v1.MemoryService`），node unary `listMemory`（snake_case `scope`/`category`；响应 `categories[]`：`category`/`files[]`（`filename`/`title`/`updated_at`）/`file_count`）。合同可选。空 `scope` / 空 `category` 原样上线；空 `filename` / `title` / `category` 原样映射；`updated_at` / `file_count` 0 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer / Memory pane。≠ `Save` / `Search` / `SearchDeep` / `Read` / `Delete` / `Reflect` / `Rebuild` / `Revert` / `History`。测：catalog + 转发 / 空 id。不重做 TokenUsage / Git / Memory.Read。
- **槽 A / `loop/A`：** 已 reset 到 `loop/merge` `c63e3fa8`。FileService 族与 ReadGitSummary / ReadGitChanges 已在 tip，不重做。把 `GitService.WriteGitCommit` 写入 gRPC catalog（`universeagent.git.v1.GitService`），node unary `writeGitCommit`（snake_case `session_id`/`message`/`sign_off`/`amend`；响应 `supported`/`reason`/`success`/`error_message`/`exit_code`/`stdout`）。合同可选。空 `sessionId` 原样上线；空 `message` 原样上线；`sign_off` / `amend` false 原样上线。**不改** Conversation roster / SessionBar / Snapshots overlay / SCM / Engine Preferences / Composer。≠ `ReadGitSummary` / `ReadGitChanges` / `ReadGitFileDiff` / `WriteGitStagePaths` / `WriteGitApplyHunks`。测：catalog + 转发 / 空 id。不重做 Summary / Changes / FileDiff / StagePaths。
- **槽 B / `loop/B`：** 已 reset 到 `loop/merge` `c63e3fa8`。ReadGitSummary / ReadGitChanges 已在 tip，不重做。WriteGitCommit 不在本刀。把 `GitService.WriteGitApplyHunks` 写入 gRPC catalog（`universeagent.git.v1.GitService`），node unary `writeGitApplyHunks`（snake_case `session_id`/`argv`/`patches`；响应 `supported`/`reason`/`success`/`error_message`/`exit_code`/`stdout`）。合同可选。空 `sessionId` 原样上线；空 `argv` / 空 `patches` 原样上线。**不改** Conversation roster / SessionBar / Snapshots overlay / SCM / Engine Preferences / Composer。≠ `ReadGitSummary` / `ReadGitChanges` / `ReadGitFileDiff` / `WriteGitStagePaths` / `WriteGitCommit`。测：catalog + 转发 / 空 id。不重做 FileService / ReadGitSummary / ReadGitChanges。
- **槽 D / `loop/D`：** 已 reset 到 `loop/merge` `c18e0241`。GitService 族已在 tip，不重做。GetSessionUsage 不在本刀。把 `TokenUsageService.GetGlobalUsage` 写入 gRPC catalog（`universeagent.tokenusage.v1.TokenUsageService`），node unary `getGlobalUsage`（空 request `{}`；响应 `usage` TokenUsageData：`input_tokens`/`output_tokens`/`thinking_tokens`/`cache_read_tokens`/`cache_write_tokens`/`total_cost_micros`/`currency`/`request_count`）。合同可选。空 `currency` 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `GetSessionUsage` / `Agent.Usage`。测：catalog + 转发 / 空 id。不重做 GitService / GetSessionUsage。
- **槽 A / `loop/A`：** 已 reset 到 `loop/merge` `64d82ba8`。FileService 族已在 tip，不重做。把 `GitService.ReadGitSummary` 写入 gRPC catalog（`universeagent.git.v1.GitService`），node unary `readGitSummary`（snake_case `session_id`；响应 `supported`/`reason`/`branch`/`change_count`）。合同可选。空 `sessionId` 原样上线。**不改** Conversation roster / SessionBar / Snapshots overlay / SCM / Engine Preferences / Composer。≠ `ReadGitChanges` / `ReadGitFileDiff` / `WriteGitStagePaths` / `WriteGitCommit` / `WriteGitApplyHunks`。测：catalog + 转发 / 空 id。不重做 FileService。
- **槽 B / `loop/B`：** 已 reset 到 `loop/merge` `64d82ba8`。FileService 已在 tip，不重做。ReadGitSummary 不在本刀。把 `GitService.ReadGitChanges` 写入 gRPC catalog（`universeagent.git.v1.GitService`），node unary `readGitChanges`（snake_case `session_id`；响应 `supported`/`reason`/`branch`/`entries`：`path`/`old_path`/`kind`/`index_state`）。合同可选。空 `sessionId` 原样上线；空 `path` / `old_path` / `kind` / `index_state` / `reason` / `branch` 原样映射。**不改** Conversation roster / SessionBar / Snapshots overlay / Sources Review / Engine Preferences / Composer。≠ `ReadGitSummary` / `ReadGitFileDiff` / `WriteGitStagePaths` / `WriteGitCommit` / `WriteGitApplyHunks`。测：catalog + 转发 / 空 id。不重做 FileService / ReadGitSummary。
- **槽 C / `loop/C`：** 已 reset 到 `loop/merge` `64d82ba8`。FileService 族已在 tip，不重做。ReadGitSummary / ReadGitChanges 不在本刀。把 `GitService.ReadGitFileDiff` 写入 gRPC catalog（`universeagent.git.v1.GitService`），node unary `readGitFileDiff`（snake_case `session_id`/`path`/`index_state`；响应 `supported`/`reason`/`path`/`unified_diff`）。合同可选。空 `sessionId` / 空 `path` / 空 `indexState` 原样上线。**不改** Conversation roster / SessionBar / Snapshots overlay / SCM / Engine Preferences / Composer。≠ `ReadGitSummary` / `ReadGitChanges` / `WriteGitStagePaths` / `WriteGitCommit` / `WriteGitApplyHunks`。测：catalog + 转发 / 空 id。不重做 FileService / ReadGitSummary / ReadGitChanges。
- **槽 D / `loop/D`：** 已 reset 到 `loop/merge` `64d82ba8`。FileService 族已在 tip，不重做。Summary / Changes 不在本刀。把 `GitService.WriteGitStagePaths` 写入 gRPC catalog（`universeagent.git.v1.GitService`），node unary `writeGitStagePaths`（snake_case `session_id`/`commands[].argv`；响应 `supported`/`reason`/`success`/`error_message`/`exit_code`/`stdout`）。合同可选。空 `sessionId` 原样上线；空 `commands` / 空 `argv` 原样上线。**不改** Conversation roster / SessionBar / Snapshots overlay / SCM / Engine Preferences / Composer。≠ `ReadGitSummary` / `ReadGitChanges` / `ReadGitFileDiff` / `WriteGitCommit` / `WriteGitApplyHunks`。测：catalog + 转发 / 空 id。不重做 Summary / Changes / FileService。
- **槽 D / `loop/D`：** 已 reset 到 `loop/merge` `32d6a81f`。ListFiles / ReadFile / GetFileInfo / WriteFile 已在 tip，不重做。ForceWriteFile 不在本刀。把 `FileService.AgentMerge` 写入 gRPC catalog（`universeagent.file.v1.FileService`），node unary `agentMerge`（snake_case `session_id`/`path`/`base_content`/`current_content`/`user_content`；响应 `accepted`）。合同可选。空 `sessionId` / 空 `path` 原样上线；空 `base_content` / `current_content` / `user_content` 原样上线。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListFiles` / `ReadFile` / `GetFileInfo` / `WriteFile` / `ForceWriteFile`。测：catalog + 转发 / 空 id。不重做 ListFiles / ReadFile / GetFileInfo / WriteFile。
- **槽 B / `loop/B`：** 已 reset 到 `loop/merge` `98e92771`。ListFiles / ReadFile 已在 tip，不重做。GetFileInfo 不在本刀。把 `FileService.WriteFile` 写入 gRPC catalog（`universeagent.file.v1.FileService`），node unary `writeFile`（snake_case `path`/`content`/`base_hash`/`session_id`/`base_content`；响应 `status`/`new_hash`/`size`/`modified_at`/`current_content`/`current_hash`/`merged_content`）。合同可选。空 `path` / 空 `sessionId` / 空 `baseHash` 原样上线；空 `content` / 空 `base_content` 原样上线。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListFiles` / `ReadFile` / `GetFileInfo` / `ForceWriteFile` / `AgentMerge`。测：catalog + 转发 / 空 id。不重做 ListFiles / ReadFile。
- **槽 merge：** 合入 A+B 的 `PermissionService.SyncPermissionRule` catalog（node unary `syncPermissionRule`；typed `RuleAction` wire；空 sessionId / toolName / scope / reason 原样上线；无 Conversation UI；≠ Respond / SetSessionGoal / PromotePermissionRule / GetSessionRules / SetPermissionMode）。`SessionService.Prewarm` / `AgentService.ClearSessionDemoFake` 已在 tip。合入 A 的 `AgentService.FetchToolUsageDetail` catalog（node unary `fetchToolUsageDetail`；空 sessionId / toolCallId 原样上线；无 Conversation UI；≠ FetchToolDetail / SubscribeToolDetail / Usage / Compact）、合入 B 的 `AgentService.FireTriggerWebhook` catalog（node unary `fireTriggerWebhook`；空 sessionId / triggerId / payloadJson 原样上线；无 Conversation UI；≠ FetchToolUsageDetail / SubscribeToolDetail / SwitchWorkDir）、合入 C 的 `AgentService.StopShellTask` catalog（node unary `stopShellTask`；空 sessionId / taskId 原样上线；无 Conversation UI；≠ CancelToolCall / RunToolInBackground / Cancel / StopLoop）、合入 D 的 `AgentService.SendShellSessionClientControl` catalog（node unary `sendShellSessionClientControl`；空 sessionId / toolCallId / refId / controlPayloadJson 原样上线；无 Conversation UI；≠ StopShellTask / SubscribeToolDetail / FetchToolUsageDetail / CancelToolCall / RunToolInBackground）、合入 A 的 `AgentService.Resume` catalog（node server-stream `openResumeStream`；空 sessionId / agentId 原样上线；无 Conversation UI；≠ Pause / ContinueGeneration / ResumeLoop / ResumeQueue / Session.Resume）、合入 B 的 `AgentService.RunToolInBackground` catalog（node unary `runToolInBackground`；空 sessionId / agentId / toolCallId 原样上线；无 Conversation UI；≠ CancelToolCall / StopShellTask / Cancel / Resume）、合入 C 的 `AgentService.ResumeLoop` catalog（node unary `resumeLoop`；空 sessionId / agentId 原样上线；无 Conversation UI；≠ Pause / PauseQueue / Cancel / Resume / ResumeQueue / Session.Resume / SuspendLoop / Branch / StopLoop）、合入 D 的 `AgentService.StopLoop` catalog（node unary `stopLoop`；空 sessionId / agentId / detail 原样上线；无 Conversation UI；≠ Pause / Cancel / SuspendLoop / Resume / ResumeLoop / Branch）、合入 A 的 `AgentService.Branch` catalog（node unary `branch`；空 sessionId / agentId / turnId 原样上线；无 Conversation UI；≠ Reset / Back / Prune / EditMessage / Fork）、合入 B 的 `AgentService.SuspendLoop` catalog（node unary `suspendLoop`；空 sessionId / agentId 原样上线；无 Conversation UI；≠ Pause / PauseQueue / Cancel / Resume / ResumeLoop / Branch / StopLoop）、合入 D 的 `AgentService.Reset` catalog（node unary `resetAgent`；空 sessionId / agentId 原样上线；无 Conversation UI；≠ ResetAgentProfile / Prune / Branch / Back）、合入 C 的 `AgentService.Prune` catalog（node unary `prune`；空 sessionId / agentId 原样上线；无 Conversation UI；≠ Reset / Back / Compact / History）、合入 B 的 `AgentService.Back` catalog（node unary `back`；空 sessionId / agentId / operationId 原样上线；无 Conversation UI；≠ Pause / PauseQueue / Prune / DeleteMessage）、合入 A 的 `AgentService.Pause` catalog（node unary `pauseAgent`；空 sessionId / agentId 原样上线；无 Conversation UI；≠ PauseQueue / Cancel / SuspendLoop / Resume / Back）、合入 C 的 `AgentService.History` catalog（node unary `getAgentHistory`；空 sessionId / agentId 原样上线；无 Conversation UI；≠ Session.GetHistory / Compact / Usage）、D 的 `AgentService.Regenerate` catalog（node server-stream `openRegenerateStream`；空 sessionId / agentId / turnId / messageId 原样上线；无 Conversation UI；≠ ContinueGeneration / History / Pause）。合入 A 的 `SessionService.ResolveTurn` catalog（node unary `resolveTurn`；空 sessionId / turnId / currentLeafTurnId 原样上线；无 Conversation UI；≠ ResolveAnchor / GetHistory / Export）、B 的 `AgentService.List` catalog（node unary `listAgents`；空 sessionId 原样上线；无 Conversation UI；≠ Tree / Status / ListAgentProfiles）。合入 C 的 `SessionService.ResolveAnchor` catalog（node unary `resolveAnchor`；空 sessionId / envelopeId / currentLeafTurnId 原样上线；无 Conversation UI；≠ ResolveTurn / GetHistory / Compact）、D 的 `AgentService.Usage` catalog（node unary `getUsage`；空 sessionId / agentId 原样上线；无 Conversation UI；≠ Todo / Status / Compact）。`AgentService.Compact`、`SessionService.Export`、`AgentService.Todo`、`SessionService.Purge`、`SessionService.Unshelve`、`AgentService.Status`、`SessionService.Resume`、overlay restore-refresh、`SessionService.Shelve`、overlay delete-refresh、overlay Delete、`SessionService.Info`、overlay Restore、`ListLoopSnapshots` 已在 tip。createSnapshot 动作转发已在 tip。DeleteSnapshot catalog、SessionBar Snapshots `listSnapshots`、RestoreSnapshot / CreateSnapshot catalog 已在 tip。接通后 `respondClientTool`→`SendClientToolResponse`、问题座→`RespondQuestion`、时间线 `turnEdit`→`EditMessage` 已在 tip。Inbox AutoDrive 诚实空 / DeleteMessage / ToolInfo / Respond / Enqueue / Kill / MessageQueue 已在 tip。
- **槽 A / `loop/A`：** 已 reset 到 `loop/merge` `98e92771`。ListFiles / ReadFile / GetCommandDef / Watch 已在 tip，不重做。把 `FileService.GetFileInfo` 写入 gRPC catalog（`universeagent.file.v1.FileService`），node unary `getFileInfo`（snake_case `path`/`session_id`；响应 `file` FileEntry：name / path / `is_directory` / size / `last_modified` / `mime_type`）。合同可选。空 `path` / 空 `sessionId` 原样上线。**不改** Conversation roster / SessionBar / Snapshots overlay / Explorer / Engine Preferences / Composer。≠ `ListFiles` / `ReadFile` / `WriteFile` / `ForceWriteFile` / `AgentMerge`。测：catalog + 转发 / 空 id。不重做 ListFiles / ReadFile。
- **槽 B / `loop/B`：** 已 reset 到 `loop/merge` `6c05a5e0`。Get / Set / SwitchModel / preferences / ResolveModel / ListCommands 已在 tip，不重做。Team 无遗漏。把 `ConfigService.Watch` 写入 gRPC catalog（`universeagent.config.v1.ConfigService`），node server-stream `openWatchConfigStream`（`makeServerStreamClient`，snake_case `keys`；帧 `key`/`old_value`/`new_value`/`scope`/`timestamp`）。合同可选。空 `keys` 原样上线。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences。≠ `Get` / `Set` / `ListModels` / `SwitchModel` / `GetCommandDef`。测：catalog + 转发 / 空 keys / dispose 静音。不重做 GetCommandDef。
- **槽 C / `loop/C`：** 已 reset 到 `loop/merge` `32d6a81f`。ListFiles / ReadFile / GetFileInfo / WriteFile 已在 tip，不重做。把 `FileService.ForceWriteFile` 写入 gRPC catalog（`universeagent.file.v1.FileService`），node unary `forceWriteFile`（snake_case `path`/`content`/`session_id`；响应 `WriteFileResponse`：`status`/`new_hash`/`size`/`modified_at`/`current_content`/`current_hash`/`merged_content`）。合同可选。空 `path` / 空 `sessionId` 原样上线；空 `content` 原样上线。**不改** Conversation roster / SessionBar / Snapshots overlay / Explorer / Engine Preferences / Composer。≠ `ListFiles` / `ReadFile` / `GetFileInfo` / `WriteFile` / `AgentMerge`。测：catalog + 转发 / 空 id。不重做 ListFiles / ReadFile / GetFileInfo / WriteFile。
- **槽 D / `loop/D`：** 已 reset 到 `loop/merge` `78360509`。ListCommands / GetCommandDef / Watch 已在 tip，不重做。把 `FileService.ReadFile` 写入 gRPC catalog（`universeagent.file.v1.FileService`），node unary `readFile`（snake_case `path`/`session_id`/`start_line`/`end_line`/`max_bytes`；响应 `content`/`total_size`/`mime_type`/`line_count`/`content_hash`）。合同可选。空 `path` / `sessionId` 原样上线；`start_line` / `end_line` / `max_bytes` 0 原样上线。**不改** Conversation roster / SessionBar / Snapshots overlay / Engine Preferences / Composer。≠ `ListFiles` / `GetFileInfo` / `WriteFile`。测：catalog + 转发 / 空 id。不重做 ListCommands / GetCommandDef / Watch。
- **治理方案八稿 2026-09-05 签收（`accepted`）：** [prd-008-engine-e2e](../plans/prd-008-engine-e2e.md) · [test-baseline-ci](../plans/test-baseline-ci.md) · [ADR-007](../decisions/007-upstream-sync.md) · [packaging-and-release](../plans/packaging-and-release.md) · [giant-file-split](../plans/giant-file-split.md) · [docs-burden-reduction](../plans/docs-burden-reduction.md) · [cross-repo-protocol](../plans/cross-repo-protocol.md) · [prd-020-turn-fixture-bench](../plans/prd-020-turn-fixture-bench.md)。第二轮为 Cursor CLI `cursor-grok-4.6-high` 只读对抗审查（7 份 Reject、ADR-007 Approve with changes），每条 Critical / Important 经 HEAD 复核后改入，记录在各稿「审查记录」第二轮表；实施顺序见 [看板](../parallel/active/verification-governance-plans.md)。**本会话未 commit**，未开任何实施切片。
- **钉死调试引擎：** 仓外 `vscode-debug-engine/` 听 `127.0.0.1:50061`。[debug-engine](../../docs/guides/debug-engine.md)。升 PRD-008 的 closer 现见 e2e 方案，本行仍不升。
- **仍 accepted：** [session-view-frame-fanout](../plans/session-view-frame-fanout.md) · [m7-gap-closeout](../plans/m7-gap-closeout.md)。D19 已闭；D20 活窗被 `@grpc/grpc-js` 挡住（打包方案承接）。
